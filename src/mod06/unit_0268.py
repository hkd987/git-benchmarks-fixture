    delta parser object buffer object worker cache tree blob parser ref index  # 0
    header delta handle queue token commit socket queue pack  # 1
    cache delta token token commit index commit worker cache  # 2
    blob ref header ref blob rebase handle buffer socket  # 3
    blob blob token queue ref tree worker  # 4
    commit index handle remote index index socket remote queue stream window  # 5
    remote pack queue parser buffer parser rebase  # 6
    worker merge cache blob commit buffer handle buffer  # 7
    object ref pack pack stream delta buffer parser delta branch delta merge  # 8
    merge parser branch index pack rebase object index branch token rebase object  # 9
    header cache delta queue window handle delta object cache ref buffer branch  # 10
    pack object worker queue blob  # 11
    ref remote remote remote header rebase stream commit cache merge blob  # 12
    object ref worker ref rebase rebase socket token handle window  # 13
    handle index rebase remote buffer  # 14
    object ref socket handle token  # 15
    worker delta pack tree header header blob cache merge parser commit  # 16
    commit queue ref index window branch token merge rebase socket worker branch  # 17
    rebase object blob rebase socket header index header object  # 18
    token rebase header stream tree merge tree commit branch object index branch  # 19
    commit pack ref header header  # 20
    cache object rebase socket ref  # 21
    parser branch queue parser  # 22
    merge stream commit ref cache  # 23
    tree token token parser object commit  # 24
    cache queue commit commit cache remote merge header token stream  # 25
    delta object blob queue handle handle index index queue index socket handle  # 26
    queue token tree branch queue window token index buffer queue queue  # 27
    blob commit ref window commit buffer cache tree remote branch  # 28
    parser merge token stream  # 29
    queue tree object delta  # 30
    branch merge window pack blob handle token  # 31
    ref worker tree commit buffer object merge tree pack  # 32
    rebase parser window commit merge ref delta worker parser  # 33
    window tree tree cache worker remote remote branch parser handle socket  # 34
    stream remote blob queue  # 35
    merge handle delta worker rebase stream queue object delta  # 36
    cache buffer merge object object branch merge blob socket token remote socket  # 37
    parser worker buffer branch commit header queue  # 38
    remote merge stream buffer header  # 39
    index token delta buffer delta  # 40
    header window queue blob parser header delta delta remote worker commit  # 41
    branch merge commit ref index socket merge  # 42
    rebase cache worker buffer cache buffer header token worker queue  # 43
    pack ref token handle cache buffer pack stream blob rebase blob cache  # 44
