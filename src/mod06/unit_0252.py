    handle pack pack window queue token pack handle handle  # 0
    rebase window delta ref remote tree header  # 1
    token commit delta socket buffer delta commit cache  # 2
    commit remote worker tree object token stream buffer socket ref  # 3
    tree branch pack header remote stream merge merge merge remote cache handle  # 4
    tree branch delta parser rebase window cache stream  # 5
    queue cache stream stream pack tree remote tree  # 6
    queue tree socket header parser  # 7
    worker branch worker worker delta parser delta worker merge  # 8
    window commit index ref  # 9
    handle worker blob branch branch handle buffer merge pack header  # 10
    ref index window buffer index delta parser ref  # 11
    remote object queue index socket parser header delta parser handle queue remote  # 12
    delta socket queue header rebase delta  # 13
    commit ref socket merge ref tree cache tree  # 14
    worker branch ref stream  # 15
    object buffer tree branch cache rebase blob  # 16
    cache worker remote header cache buffer socket socket remote token parser handle  # 17
    tree socket ref socket token parser cache queue queue buffer  # 18
    rebase blob handle cache delta rebase merge socket socket  # 19
    commit pack index pack buffer remote header index window stream buffer delta  # 20
    tree cache queue ref token rebase handle index merge branch rebase  # 21
    socket branch delta window merge ref buffer  # 22
    merge pack header blob socket header parser buffer commit socket buffer window  # 23
    handle remote token merge delta commit tree stream token blob merge worker  # 24
    stream merge pack delta handle branch cache  # 25
    blob parser merge pack commit parser pack  # 26
    commit blob branch worker branch delta  # 27
    commit queue rebase parser merge worker delta socket handle queue parser  # 28
    blob header blob parser queue delta token commit  # 29
    rebase tree handle window  # 30
    tree window branch stream  # 31
    branch header ref commit cache index token buffer merge  # 32
    index rebase tree parser stream  # 33
