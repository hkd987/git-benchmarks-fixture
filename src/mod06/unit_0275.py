    handle stream branch remote delta socket queue stream queue queue  # 0
    merge stream delta cache socket rebase ref object token remote commit  # 1
    branch index buffer index delta tree ref delta  # 2
    cache socket header commit handle blob delta ref remote rebase rebase branch  # 3
    blob delta branch socket branch header rebase  # 4
    remote handle commit queue  # 5
    stream stream parser pack remote token branch merge buffer worker socket  # 6
    rebase queue index branch pack ref parser rebase  # 7
    delta blob tree buffer worker  # 8
    blob merge buffer remote index commit handle queue  # 9
    worker queue queue stream buffer cache remote blob delta window index  # 10
    buffer header stream queue remote cache  # 11
    cache stream stream worker rebase socket socket branch tree  # 12
    parser handle ref window commit remote stream commit cache  # 13
    index token handle handle token socket commit token header worker  # 14
    blob header rebase object object worker stream remote handle queue  # 15
    merge socket window branch branch parser remote remote stream rebase  # 16
    buffer cache merge cache delta delta branch tree branch cache header remote  # 17
    queue pack window handle buffer parser cache  # 18
    cache pack stream cache socket object  # 19
    handle rebase window handle pack stream object rebase queue  # 20
    tree socket merge tree  # 21
    ref remote handle buffer tree ref worker pack index queue tree  # 22
    blob ref branch pack index rebase merge queue  # 23
    parser delta remote worker tree blob  # 24
    branch socket window worker stream  # 25
    header stream commit branch remote token cache commit stream  # 26
    handle header commit delta token buffer socket commit rebase delta  # 27
    handle cache pack buffer  # 28
    index handle rebase parser stream  # 29
    pack index socket queue delta merge  # 30
    queue tree queue ref header buffer queue  # 31
    window delta stream tree handle socket blob worker pack index branch  # 32
    cache token stream branch header pack merge buffer buffer ref  # 33
    cache pack blob pack pack branch tree socket token buffer buffer  # 34
    stream worker branch cache remote worker window ref object buffer  # 35
    object window stream token  # 36
    queue queue parser commit handle handle  # 37
    object token ref delta buffer buffer cache worker socket ref delta  # 38
    ref header worker token buffer merge  # 39
    token handle parser window socket window commit parser token  # 40
    handle parser cache handle worker parser remote index ref index  # 41
    rebase rebase object queue worker window object ref merge  # 42
    buffer delta tree remote index socket ref remote object  # 43
    object branch queue branch stream parser socket branch header  # 44
    ref ref delta cache window handle  # 45
    tree stream header header window commit window parser delta tree  # 46
    worker header remote ref rebase header  # 47
    buffer commit cache tree  # 48
    header merge queue buffer commit  # 49
    object blob queue worker remote queue header branch delta window window  # 50
    socket cache branch ref token  # 51
    delta handle handle token merge delta  # 52
    object blob object merge ref socket pack  # 53
    remote socket cache queue commit worker parser  # 54
    worker delta header window worker window handle stream  # 55
    rebase token pack merge index commit branch  # 56
    index tree buffer rebase tree index queue parser rebase  # 57
    rebase blob token index commit remote handle  # 58
    index rebase window buffer token  # 59
    blob socket handle handle stream parser worker socket pack  # 60
    index commit buffer parser token commit  # 61
    buffer pack cache window queue branch header  # 62
    buffer buffer queue queue object blob rebase socket parser  # 63
    merge ref window index blob handle  # 64
    cache pack ref remote pack branch  # 65
    rebase handle token worker merge commit remote commit cache remote pack blob  # 66
    delta worker branch parser branch  # 67
    worker object pack buffer blob delta  # 68
    object stream stream buffer branch pack rebase handle  # 69
    handle worker object merge index stream cache merge handle queue object socket  # 70
    object window socket window delta queue worker  # 71
    blob rebase handle queue worker pack  # 72
    delta blob queue handle worker merge buffer tree pack buffer  # 73
    commit ref rebase rebase rebase index  # 74
    window token object stream pack remote worker delta remote  # 75
    header cache stream pack  # 76
    object commit pack commit delta blob merge token commit  # 77
    commit worker rebase rebase cache cache buffer stream commit header blob token  # 78
    commit queue tree index header ref commit ref  # 79
    pack merge window commit parser stream cache socket cache remote  # 80
    index tree parser remote socket delta stream socket  # 81
    handle remote ref socket token  # 82
    delta ref worker buffer buffer header handle merge queue merge queue rebase  # 83
    handle worker branch worker object parser  # 0
    worker socket cache index handle rebase object index  # 1
    commit delta stream header token stream window queue cache handle token  # 2
    merge ref window remote buffer handle parser index buffer branch  # 3
    commit cache commit index remote object token blob blob index cache  # 4
    pack tree commit parser parser handle parser queue pack token  # 5
    buffer window parser blob pack object merge ref merge  # 6
    buffer cache cache merge tree header delta ref cache merge stream  # 7
    branch branch socket delta window header stream parser delta token blob index  # 8
    commit rebase delta window socket tree delta object remote delta queue  # 9
    token pack parser socket object object pack  # 10
    token handle rebase tree token  # 0
    queue delta stream pack parser remote rebase  # 1
    cache index remote object window stream parser blob  # 2
    tree object index merge stream index object index  # 3
    queue blob blob queue blob cache queue merge  # 4
    socket blob header merge merge delta branch stream window cache rebase  # 5
    remote tree token socket merge worker queue token tree  # 6
    parser commit token parser tree handle worker remote handle pack delta blob  # 7
    delta remote token rebase branch  # 8
    cache buffer buffer object parser parser worker buffer rebase token  # 9
    commit branch index token  # 10
    socket ref blob socket worker branch tree token remote buffer window  # 11
    buffer index blob object tree queue worker ref cache socket  # 12
    merge blob window queue stream stream pack blob object commit tree  # 13
