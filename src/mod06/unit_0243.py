    buffer ref remote token delta branch buffer  # 0
    remote handle socket remote parser window queue branch blob  # 1
    window commit tree blob tree branch blob  # 2
    parser object commit queue branch cache queue cache  # 3
    worker delta tree object cache queue delta queue buffer  # 4
    index buffer token token  # 5
    pack queue rebase handle  # 6
    queue cache branch token index  # 7
    handle index window remote pack cache  # 8
    ref handle parser branch ref cache handle  # 9
    remote commit remote index buffer worker index remote  # 10
    blob index header socket cache tree index socket  # 11
    tree handle rebase worker  # 12
    merge pack handle window delta pack commit merge pack blob  # 13
    pack buffer merge commit worker commit window index delta tree rebase  # 14
    header worker object parser worker parser tree token commit cache  # 15
    handle branch socket queue rebase buffer commit branch handle commit queue  # 16
    header index parser delta object buffer cache blob pack socket pack  # 17
    tree worker index tree  # 18
    delta commit handle header ref remote index handle tree window  # 19
    pack ref merge cache blob  # 20
    parser pack handle tree remote token worker delta  # 21
    rebase handle worker object header buffer commit  # 22
    delta handle commit object rebase rebase  # 23
    token stream branch tree handle merge commit ref branch queue blob commit  # 24
    merge handle parser worker merge  # 25
    worker socket object merge parser header pack rebase header tree  # 26
    pack branch token delta worker rebase remote tree buffer  # 27
    socket delta object socket worker ref stream tree object delta  # 28
    object window commit commit index branch ref header  # 29
    tree commit token stream  # 30
    socket buffer index parser delta token commit token buffer handle ref  # 31
    stream remote stream remote parser object rebase index buffer token object  # 32
    branch commit merge tree ref branch buffer parser merge  # 33
    window socket cache object window rebase window object branch stream blob  # 34
    object index header merge  # 35
    index commit commit header cache  # 36
    queue window queue blob object index header ref socket branch branch header  # 37
    socket remote index window index buffer token remote  # 38
    cache object stream window stream queue  # 39
    tree socket worker commit  # 40
    socket stream window handle handle  # 41
    stream commit branch branch token merge remote remote  # 42
    merge worker delta socket branch branch blob handle remote queue handle parser  # 43
    remote socket parser buffer cache object parser object  # 44
    parser pack token blob cache delta buffer queue socket  # 45
    token parser handle stream window stream header stream queue index  # 46
    object window delta worker blob  # 47
    header handle merge commit branch merge stream ref cache remote  # 48
    token stream ref queue token  # 49
    queue rebase ref queue pack merge handle blob stream branch  # 50
    remote cache commit handle  # 51
    remote cache rebase buffer stream rebase ref queue socket branch ref  # 52
    ref worker remote window handle commit blob window merge remote tree ref  # 53
    cache index cache window queue pack  # 54
    socket branch remote cache  # 55
    token header commit parser delta merge remote buffer window delta blob branch  # 56
    socket object delta object stream branch handle  # 57
    worker queue stream window  # 58
    worker token queue object tree rebase pack blob buffer rebase tree  # 59
    parser window cache window tree rebase buffer parser queue  # 60
    header worker queue merge handle merge index  # 61
    merge window ref branch  # 62
    handle worker worker handle queue cache token socket  # 63
    merge object queue merge  # 64
    merge index token delta delta  # 65
    queue blob branch pack delta token handle parser ref worker stream delta  # 66
    remote remote merge merge tree socket stream  # 67
    parser tree token header token  # 0
    object merge socket socket worker blob remote  # 1
    handle tree merge stream delta remote index worker  # 2
    stream remote stream object object commit remote branch pack worker  # 3
    rebase pack socket header socket socket worker commit object socket worker buffer  # 0
    delta header handle object parser branch stream header remote  # 1
    ref remote stream blob index header ref queue rebase token  # 2
    buffer ref cache cache tree  # 3
    index header header pack blob remote tree  # 4
    socket pack token buffer token queue stream branch  # 5
    delta ref handle worker parser token queue handle window blob handle  # 0
    token tree merge token buffer pack buffer merge  # 1
    window object merge index  # 2
