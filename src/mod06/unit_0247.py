    pack rebase remote branch socket handle  # 0
    token remote index commit rebase tree merge  # 1
    window parser stream branch socket socket buffer  # 2
    buffer worker branch merge merge merge rebase worker  # 3
    window merge stream rebase pack token worker  # 4
    queue delta index delta ref window object worker commit  # 5
    delta delta tree token blob cache window  # 6
    buffer header blob object merge branch blob  # 7
    token window parser queue ref branch handle worker remote  # 8
    pack pack rebase parser tree stream token remote blob tree worker rebase  # 9
    token header tree pack queue stream worker handle pack  # 10
    merge commit token worker rebase window pack handle  # 11
    pack window header blob queue handle parser blob  # 12
    remote ref worker object buffer index cache remote delta tree  # 13
    remote remote index stream token delta index cache delta stream token  # 14
    parser branch parser object pack index worker  # 15
    blob branch buffer pack branch  # 16
    ref buffer queue queue header branch  # 17
    cache branch parser remote parser stream queue blob index  # 18
    branch tree cache socket merge index ref handle pack handle ref  # 19
    parser merge header commit object  # 20
    delta cache handle rebase worker  # 21
    merge branch remote ref  # 22
    rebase parser window stream  # 23
    commit ref object ref  # 24
    stream cache worker token socket tree parser socket stream blob rebase  # 25
    ref object commit socket rebase queue queue worker token worker  # 26
    stream stream handle remote token rebase index cache branch pack  # 27
    branch handle ref delta header window worker rebase ref  # 28
    index handle branch ref socket merge worker  # 29
    rebase tree handle ref commit parser  # 30
    header rebase blob pack commit  # 31
    object branch pack worker blob pack blob pack header ref pack delta  # 32
    remote delta commit rebase rebase branch parser rebase handle delta  # 33
    tree cache pack queue  # 34
    remote object queue index parser object header tree queue merge  # 35
    tree worker queue parser pack merge stream window  # 36
    ref remote index object buffer blob tree index blob queue  # 37
    socket queue buffer parser handle  # 38
    window merge commit index worker index pack worker pack  # 39
    branch socket delta pack window ref header buffer remote  # 40
    buffer tree rebase stream  # 41
    token buffer parser merge buffer handle blob rebase socket branch token  # 42
    rebase cache window blob queue blob ref  # 43
    object worker ref index rebase ref remote  # 44
    parser remote socket token ref remote branch token blob cache stream stream  # 45
    parser handle handle blob ref buffer handle object  # 46
    stream window worker blob tree handle window  # 47
    stream worker cache blob stream queue  # 48
    header parser header queue buffer  # 49
    token parser object remote worker socket handle index index index window tree  # 50
    header remote parser pack cache remote  # 51
    remote merge delta cache tree header commit tree delta socket  # 52
    socket parser branch handle header queue ref index worker tree  # 53
    index commit cache object ref index  # 54
    index delta queue stream blob  # 55
    socket index blob buffer commit handle commit branch delta  # 56
    cache branch pack rebase  # 57
    ref parser pack blob socket handle object  # 58
    merge window branch handle window  # 59
    handle header parser header cache  # 60
    rebase delta parser stream ref tree handle remote handle pack blob object  # 61
    token tree delta token branch buffer cache buffer handle header  # 62
    pack token cache rebase index queue stream buffer worker  # 63
    worker queue ref window header commit tree branch blob buffer  # 64
    handle remote merge worker object parser blob token branch handle  # 65
    ref window worker worker delta stream  # 66
    worker merge stream delta commit socket token tree  # 67
    socket parser parser stream worker stream queue  # 68
    worker object tree header  # 69
    commit index object socket  # 70
    commit stream worker object branch socket stream tree  # 71
    ref handle worker buffer buffer index blob  # 72
    cache commit index blob worker parser token  # 73
    token pack worker token pack window delta branch branch parser tree  # 74
    index blob queue queue token queue index header  # 75
    remote header stream remote object ref commit pack ref handle handle  # 76
    tree handle index cache commit socket worker object window  # 77
    buffer ref remote index  # 78
    object token pack ref index  # 79
    tree index buffer cache header parser token buffer  # 80
    cache branch buffer remote delta pack stream  # 81
    commit token merge stream object stream tree commit  # 82
    rebase stream branch blob remote commit header buffer  # 83
    blob pack cache delta  # 84
    branch stream parser worker branch blob tree queue  # 85
    socket window stream commit parser token handle parser buffer queue queue  # 86
