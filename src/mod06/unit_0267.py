    merge object stream object cache  # 0
    object tree pack rebase cache tree worker remote pack merge delta  # 1
    blob header object token  # 2
    window cache ref token stream socket worker stream object  # 3
    header tree buffer remote blob  # 4
    rebase remote merge parser window token handle branch  # 5
    worker window tree pack ref worker commit index branch  # 6
    index rebase socket window pack buffer pack  # 7
    ref index stream remote tree parser index  # 8
    remote token merge merge cache remote remote buffer buffer remote pack object  # 9
    tree ref branch commit cache index handle  # 10
    pack rebase index worker  # 11
    handle handle socket cache parser queue rebase delta rebase  # 12
    tree queue parser window socket merge worker worker  # 13
    parser header worker buffer remote branch window socket index window parser socket  # 14
    window commit header branch object window socket token branch token branch  # 15
    tree handle ref blob stream merge index buffer object merge branch token  # 16
    header socket index tree buffer socket remote index  # 17
    remote branch merge delta stream parser handle  # 18
    queue handle buffer index tree cache stream pack stream index remote  # 19
    ref handle window delta  # 20
    branch delta header pack commit stream ref stream  # 21
    delta tree commit merge header rebase  # 22
    parser window index commit merge blob buffer token index  # 23
    tree token blob commit parser blob remote blob window commit rebase tree  # 24
    pack token merge blob header worker buffer parser delta stream pack branch  # 25
    worker pack token worker  # 26
    pack socket window tree merge window  # 27
    parser object ref queue window  # 28
    object tree delta window index parser  # 29
    socket blob window branch token  # 30
    worker parser worker buffer window blob branch merge object pack header socket  # 31
    queue socket merge rebase remote socket buffer stream  # 32
    token buffer header token  # 33
    commit blob tree parser token object branch queue token  # 34
    handle buffer pack queue parser stream  # 35
    branch remote pack tree branch tree rebase blob header stream ref blob  # 36
    cache remote worker token window  # 37
    object socket handle cache  # 38
    window queue socket queue branch  # 39
    socket handle remote window worker blob index  # 40
    merge tree object parser remote object remote ref parser socket header  # 41
    blob rebase rebase delta socket  # 42
    rebase branch header handle socket blob header stream  # 43
    blob object index buffer cache rebase ref  # 44
    ref branch branch token cache stream delta index  # 45
    pack branch merge pack branch  # 46
    index index ref rebase queue  # 47
    commit stream index header  # 48
    object queue tree branch queue  # 49
    ref parser merge commit socket queue header  # 50
    merge rebase buffer token delta buffer remote branch rebase remote  # 51
    pack stream buffer handle token header window pack blob  # 52
    merge tree remote index stream index buffer socket index socket  # 53
    handle ref buffer window branch tree header tree index cache object parser  # 54
    remote tree delta window worker pack blob rebase window branch  # 55
    header window ref buffer handle cache window socket ref object pack  # 56
    tree stream object pack queue queue ref  # 57
    stream stream index commit window parser header  # 58
    tree pack rebase stream rebase blob token pack tree stream stream parser  # 59
    remote delta rebase socket tree tree token handle ref commit parser  # 60
    remote worker branch handle socket  # 61
    rebase parser parser merge token handle blob  # 62
    handle socket blob buffer rebase delta  # 63
    rebase token merge blob cache queue index remote  # 64
    buffer commit remote branch cache rebase stream handle header remote  # 65
    parser queue cache object pack queue  # 66
    buffer parser cache token  # 67
    tree buffer stream parser buffer token  # 68
