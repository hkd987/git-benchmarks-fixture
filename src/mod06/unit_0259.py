    buffer cache pack cache buffer remote socket stream object header  # 0
    handle rebase worker remote  # 1
    delta stream window branch ref parser branch rebase token  # 2
    socket cache socket stream cache tree window  # 3
    ref delta parser pack handle cache token index handle blob blob  # 4
    object cache merge cache window token parser window tree  # 5
    tree tree queue blob worker commit  # 6
    commit buffer commit pack parser queue queue  # 7
    ref index socket rebase merge blob  # 8
    merge worker rebase socket commit token pack header window  # 9
    remote queue delta tree worker queue  # 10
    blob cache tree cache object  # 11
    socket blob header cache token buffer pack window cache merge tree parser  # 12
    parser index window buffer window header queue  # 13
    worker merge tree token token object buffer rebase pack parser  # 14
    index header commit ref delta handle header  # 15
    object blob ref socket index  # 16
    merge window rebase buffer token window remote tree  # 17
    handle delta branch window  # 18
    handle blob buffer socket token tree worker commit queue window blob remote  # 19
    worker window remote delta  # 20
    pack stream tree commit queue cache pack merge  # 21
    socket token branch header token socket window  # 22
    merge window cache tree blob header token pack queue ref worker pack  # 23
    queue socket delta merge ref window  # 24
    pack delta header parser handle stream header handle header buffer header socket  # 25
    rebase ref window buffer worker window header pack worker blob socket  # 26
    worker merge parser worker delta object window token blob token object header  # 27
    token tree index window queue  # 28
    blob pack remote pack index stream index header tree index branch  # 29
    merge branch tree commit worker stream merge remote header token branch socket  # 30
    commit parser commit merge merge object merge stream pack worker delta  # 31
    socket ref token window ref object blob tree pack handle delta commit  # 32
    merge header stream tree index branch rebase stream header worker  # 33
    rebase header parser commit parser cache  # 34
    branch remote queue pack  # 35
    cache rebase remote rebase parser object socket pack commit rebase handle pack  # 36
    buffer rebase commit pack handle queue  # 37
    window stream cache buffer index ref window index ref merge  # 38
    worker parser remote index pack header  # 39
    window parser worker socket branch delta socket  # 40
    token cache pack window object socket window  # 41
    header delta tree pack delta parser buffer remote token remote blob  # 42
    commit parser stream object queue object ref tree tree  # 43
    blob token remote buffer stream merge branch branch remote stream window  # 44
    queue delta blob stream ref delta tree parser commit ref rebase remote  # 45
    token handle parser object stream  # 46
    header object pack ref remote object delta  # 47
    window token queue index handle tree header commit parser branch  # 48
    handle tree index pack parser merge parser worker window merge handle  # 49
    rebase branch cache socket stream parser index branch window rebase index  # 50
    worker header window stream delta stream rebase commit queue window cache  # 51
    worker parser commit index header header socket token header worker object  # 52
    socket handle delta window worker  # 53
    buffer ref object stream commit tree object delta token stream  # 54
    cache worker queue branch blob cache rebase queue rebase parser index  # 55
    blob remote token handle handle token handle cache cache commit worker  # 56
    window queue delta blob ref handle index merge token commit token  # 57
    branch buffer index branch parser rebase pack header token window buffer  # 58
