    socket merge token tree queue object buffer rebase index token parser rebase  # 0
    queue branch tree remote delta ref stream header window rebase  # 1
    blob parser ref tree object stream tree token parser rebase delta handle  # 2
    delta merge tree window socket worker merge commit  # 3
    buffer handle header remote cache socket object  # 4
    tree stream cache queue socket commit cache remote object worker cache  # 5
    handle blob token branch index token index  # 6
    queue parser token delta blob index  # 7
    buffer buffer window remote socket pack buffer commit header blob cache  # 8
    parser socket object branch object window  # 9
    stream rebase remote buffer window object tree queue remote merge header  # 10
    branch pack tree merge ref remote branch socket pack ref branch merge  # 11
    index parser rebase buffer token  # 12
    object parser socket socket worker object pack branch token ref worker ref  # 13
    remote stream parser merge parser handle worker rebase tree stream  # 14
    merge stream buffer socket token worker socket worker worker window pack token  # 15
    ref branch socket header index ref branch cache buffer buffer stream pack  # 16
    stream buffer stream buffer stream header socket  # 17
    merge socket remote socket blob merge index cache buffer delta  # 18
    queue stream parser window index index blob ref pack tree  # 19
    tree rebase remote delta tree parser  # 20
    stream pack remote rebase window remote pack remote token stream  # 21
    window remote parser commit pack delta header pack commit ref index  # 22
    token token token ref handle commit remote merge stream  # 23
    token branch branch pack remote worker  # 24
    blob rebase rebase delta token stream handle branch  # 25
    buffer header pack stream remote cache tree branch blob branch window rebase  # 26
    socket pack worker socket ref cache window queue  # 27
    window worker handle window commit queue queue  # 28
    commit delta buffer parser ref pack queue token buffer blob ref commit  # 29
    window buffer worker stream stream blob  # 30
    socket object header socket ref commit parser tree index  # 31
    merge buffer header stream header index socket worker token commit worker queue  # 32
    rebase remote buffer object token buffer ref pack remote remote rebase tree  # 33
    window ref parser token cache  # 34
    buffer pack tree parser rebase index tree window buffer  # 35
    stream commit branch merge branch ref commit stream parser  # 36
    delta index parser stream header rebase  # 37
    blob socket header index  # 38
    delta buffer object rebase blob handle window cache token object handle object  # 39
    object branch merge delta queue index delta socket blob index remote blob  # 40
    socket merge token parser handle buffer  # 41
    parser handle branch token worker header socket blob header  # 42
