    handle delta window buffer delta blob token queue handle queue parser header  # 0
    object pack remote rebase rebase rebase socket ref socket header ref  # 1
    token handle token header  # 2
    object parser buffer window pack rebase cache delta  # 3
    tree merge window queue  # 4
    buffer window stream object rebase delta delta rebase  # 5
    ref queue commit window index tree  # 6
    index merge branch window tree rebase ref handle branch blob index blob  # 7
    stream window commit header parser merge delta pack handle rebase ref parser  # 8
    worker worker remote queue parser rebase buffer ref remote handle window blob  # 9
    queue object delta buffer queue object window  # 10
    commit blob commit merge handle object stream stream tree commit remote handle  # 11
    queue parser stream branch header handle merge rebase buffer window pack  # 12
    rebase queue index token buffer commit buffer blob commit commit remote branch  # 13
    remote remote blob commit buffer token index remote merge delta commit  # 14
    handle merge worker socket remote commit  # 15
    object queue parser handle worker pack queue object index  # 16
    worker ref socket object rebase token queue  # 17
    delta stream parser socket  # 18
    buffer tree branch remote commit pack tree parser branch  # 19
    window stream merge stream worker parser buffer  # 20
    rebase cache stream window blob pack  # 21
    handle pack branch tree  # 22
    token tree rebase worker worker blob tree queue tree cache  # 23
    pack handle header rebase branch queue delta tree handle parser cache  # 24
    delta pack cache rebase branch remote ref  # 25
    index blob ref delta buffer tree  # 26
    delta socket delta buffer  # 27
    socket tree pack handle tree rebase merge  # 28
    parser commit window cache tree object parser rebase queue merge queue buffer  # 29
    ref parser window buffer header  # 30
    pack handle parser buffer rebase token tree  # 31
    queue pack branch stream ref header merge merge  # 32
    worker socket delta header  # 33
    queue parser commit tree merge worker  # 34
    window rebase header header handle branch merge cache cache  # 35
    socket blob blob pack token worker  # 36
    object token parser parser header socket  # 37
    handle pack window merge queue stream cache remote ref rebase remote parser  # 38
    token merge index parser delta queue branch delta commit window remote  # 39
    object pack remote ref parser  # 40
    window token handle buffer blob  # 41
    tree parser object stream merge rebase pack tree socket socket queue  # 42
    index tree ref ref branch pack cache tree index blob remote merge  # 43
    branch handle ref queue remote rebase handle branch pack delta pack  # 44
    index delta handle object  # 45
    ref token worker rebase object token parser token ref remote socket handle  # 46
    tree rebase cache buffer ref merge merge buffer  # 47
    worker index buffer blob commit socket  # 48
    object ref cache handle header index remote commit stream header object token  # 49
    socket merge socket worker ref object  # 50
    blob buffer commit object  # 51
    remote blob rebase window token queue ref queue cache object  # 0
    stream stream header queue parser branch blob  # 1
    remote rebase branch token object pack stream pack remote parser  # 2
    delta parser window commit  # 3
    branch delta socket tree cache window  # 4
    remote branch cache token window token cache merge merge buffer  # 5
    window queue header index tree socket  # 6
    handle socket ref token handle  # 7
    ref object blob index remote blob parser handle  # 8
    merge merge ref ref  # 9
    stream rebase pack remote branch object buffer buffer object tree worker  # 10
    index remote remote handle index commit queue blob token  # 11
