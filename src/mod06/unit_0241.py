    rebase header stream merge token pack blob  # 0
    tree commit header remote tree remote blob merge parser  # 1
    window queue remote cache token window ref handle queue merge blob socket  # 2
    header rebase index commit pack commit ref pack merge buffer delta stream  # 3
    tree delta stream commit  # 4
    parser parser socket handle socket pack buffer socket tree buffer  # 5
    branch commit object header  # 6
    buffer commit merge worker remote rebase worker  # 7
    blob stream tree ref branch worker object stream  # 8
    parser delta rebase ref buffer rebase index handle  # 9
    socket remote branch index  # 10
    commit worker token object window worker  # 11
    branch handle worker socket buffer ref ref parser  # 12
    commit stream window window delta branch worker window tree handle  # 13
    pack commit tree tree window commit delta handle cache commit  # 14
    ref socket ref tree object tree worker delta cache tree  # 15
    delta cache window commit  # 16
    buffer index object commit object index merge  # 17
    branch token pack merge cache pack header  # 18
    handle ref token remote merge queue header  # 19
    worker token parser handle  # 20
    pack rebase pack object object  # 21
    ref stream queue token stream handle  # 22
    blob remote rebase object remote merge window branch ref merge  # 23
    token parser object window  # 24
    token header stream socket window remote stream  # 25
    cache merge worker stream buffer buffer parser tree commit cache  # 26
    ref commit merge tree  # 27
    handle worker queue tree delta pack queue buffer worker index  # 28
    object pack object delta  # 29
    index buffer stream tree queue merge cache stream token commit  # 30
    socket worker pack pack commit pack rebase  # 31
    cache header worker header remote cache ref index  # 32
    window index object index object queue token blob handle token delta index  # 33
    stream ref remote ref token remote rebase object cache socket  # 34
    window delta rebase window buffer pack ref worker commit buffer buffer  # 35
    index blob object tree delta token index rebase pack header blob stream  # 36
    buffer window commit handle commit commit object queue worker index blob  # 37
    rebase commit worker cache ref delta buffer header window parser  # 38
    object pack branch parser  # 39
    buffer tree pack rebase tree merge branch  # 40
    header queue object window window index merge  # 41
    branch pack ref remote worker delta branch buffer  # 42
    token handle cache merge socket  # 43
    stream index stream merge blob remote token cache index worker window  # 44
    header object pack queue commit stream queue cache worker rebase branch branch  # 45
    buffer window rebase commit parser  # 46
    delta object window queue parser header queue worker tree  # 47
    remote header socket rebase pack object tree parser queue pack  # 48
    handle rebase buffer header tree socket token queue header handle remote  # 49
    queue branch ref handle pack header commit index commit branch window  # 50
    parser socket queue window window ref branch stream  # 51
    handle branch rebase worker remote index handle buffer socket object stream  # 52
    object merge worker remote ref token handle token queue  # 53
    parser cache buffer branch pack window queue token parser index object socket  # 54
    pack commit cache token commit worker  # 55
    rebase commit worker handle index branch object branch delta pack cache  # 56
