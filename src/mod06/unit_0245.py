    stream commit stream queue  # 0
    tree branch tree stream ref cache queue socket tree  # 1
    queue rebase header header index tree  # 2
    branch header merge index  # 3
    parser ref object branch worker queue commit stream merge branch tree  # 4
    remote branch pack stream tree object object branch object rebase remote handle  # 5
    worker blob worker cache branch buffer branch delta worker branch commit  # 6
    ref socket queue parser rebase  # 7
    handle handle handle commit ref object socket stream ref tree blob pack  # 8
    delta blob ref merge index ref pack branch worker tree tree  # 9
    stream pack socket token tree queue worker token header delta tree  # 10
    queue stream pack ref header handle remote window commit  # 11
    index token index rebase  # 12
    queue pack pack commit commit tree  # 13
    commit window index stream commit ref worker remote cache  # 14
    tree ref object handle  # 15
    merge window rebase delta buffer merge cache merge header header queue  # 16
    index ref tree commit branch remote parser tree  # 17
    socket token worker stream rebase delta handle ref header delta  # 18
    parser buffer worker socket merge handle cache ref  # 19
    remote remote delta index buffer socket rebase pack  # 20
    object buffer index ref ref blob cache cache window worker  # 21
    socket window object socket  # 22
    stream branch stream queue handle delta pack token rebase window  # 23
    buffer stream object worker  # 24
    socket rebase window window merge window parser worker token token blob queue  # 25
    handle queue window pack blob commit index token header  # 26
    object stream remote window header token stream remote  # 27
    header cache stream header delta header blob handle handle  # 28
    window pack worker rebase parser blob  # 29
