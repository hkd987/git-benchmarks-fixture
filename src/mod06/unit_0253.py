    handle token rebase index handle header header blob branch blob  # 0
    delta cache ref token worker queue token commit window index parser branch  # 1
    object index socket blob stream remote index buffer commit  # 2
    branch token blob branch socket  # 3
    parser socket stream handle handle blob  # 4
    delta branch buffer cache tree merge parser rebase handle pack buffer  # 5
    socket stream blob handle queue socket  # 6
    delta blob window handle buffer token  # 7
    token token stream index handle ref rebase  # 8
    stream token delta window cache commit commit header merge ref token  # 9
    token ref remote merge pack parser ref parser token tree  # 10
    parser rebase parser tree buffer token object pack merge  # 11
    header socket ref rebase buffer branch tree pack queue remote  # 12
    token rebase cache merge worker  # 13
    merge rebase stream rebase worker delta object  # 14
    blob buffer parser delta  # 15
    buffer header buffer rebase object socket queue queue remote pack  # 16
    token commit stream parser branch parser commit  # 17
    buffer stream cache handle header parser index index cache cache worker  # 18
    tree socket delta branch cache  # 19
    stream delta blob parser queue queue  # 20
    buffer ref blob ref ref header  # 21
    tree ref ref stream pack cache blob index tree handle remote  # 22
    worker merge handle object buffer parser worker worker branch token  # 23
    delta rebase merge header  # 24
    commit ref index window ref header index handle blob queue  # 25
    parser pack blob merge pack blob worker branch index  # 26
    delta ref remote branch  # 27
    remote delta commit header commit buffer queue handle  # 28
    pack commit index branch delta object queue window index delta merge  # 29
    cache branch header buffer blob handle commit pack index rebase  # 30
    stream pack blob stream token rebase object tree commit worker worker  # 31
    token window window rebase pack stream object header socket header blob  # 32
    blob handle stream queue buffer socket window window socket  # 33
    branch stream window header  # 34
    blob queue buffer token header token merge  # 35
    object delta object tree cache object window remote rebase  # 36
    buffer tree rebase window window tree pack remote token stream  # 37
    pack handle commit pack socket blob token branch queue worker  # 38
    buffer worker rebase parser token window cache stream  # 39
    buffer index window handle remote cache ref worker parser  # 40
    queue token rebase handle branch handle remote cache pack queue cache object  # 41
    stream worker rebase handle remote delta branch queue remote tree branch ref  # 42
    cache stream merge remote delta parser  # 43
    stream socket object pack handle token object index header token  # 44
    token merge buffer buffer index remote  # 45
    rebase worker token pack header ref pack index pack header  # 46
    worker buffer pack header remote parser buffer commit stream  # 47
    index index socket buffer window worker index handle  # 48
    pack delta worker rebase header object window remote header remote delta  # 49
    cache blob merge pack cache cache rebase token cache delta buffer socket  # 50
    handle branch socket worker queue cache header object window tree  # 51
    handle queue window branch handle parser  # 52
    pack index remote remote header tree commit window window token index  # 53
    handle commit branch worker pack socket  # 54
    window blob blob tree remote tree  # 55
    index delta buffer object parser cache  # 56
    buffer header branch handle token  # 57
    buffer ref header ref socket token remote tree buffer ref socket socket  # 58
    queue object rebase handle commit parser queue pack header index index buffer  # 59
    commit rebase stream window worker handle window worker buffer cache  # 60
    rebase cache commit branch  # 61
    merge remote delta index blob commit rebase tree  # 62
    rebase delta object commit index remote remote window token  # 63
    parser index socket commit branch handle remote handle merge  # 64
    stream blob tree pack tree blob ref  # 65
    remote delta commit parser index buffer token delta merge pack object pack  # 66
    pack queue pack token worker parser merge index parser  # 67
    remote remote ref object cache branch queue delta token stream branch  # 68
    window stream remote merge stream  # 69
    worker branch commit header buffer delta pack  # 70
    delta delta socket header cache tree token window object remote  # 71
    branch object tree merge  # 72
    queue commit rebase index stream header window parser merge header queue merge  # 73
