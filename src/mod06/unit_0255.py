    socket rebase pack blob window remote queue branch queue index buffer index  # 0
    delta queue remote pack buffer tree token buffer token  # 1
    worker index ref tree rebase commit remote worker parser handle header branch  # 2
    branch window cache queue token token header  # 3
    window handle remote delta merge branch socket worker  # 4
    window queue rebase delta  # 5
    pack delta rebase rebase delta delta  # 6
    remote pack buffer header token index commit delta token branch  # 7
    socket header commit cache  # 8
    branch index delta handle remote commit cache header delta  # 9
    token stream merge blob  # 10
    blob object rebase merge  # 11
    index stream pack delta parser tree index  # 12
    socket buffer stream handle pack branch branch ref header object  # 13
    socket merge merge parser tree  # 14
    rebase object stream socket  # 15
    stream merge handle delta window object cache socket  # 16
    parser parser object merge rebase  # 17
    parser buffer commit stream pack cache  # 18
    cache remote socket handle rebase queue  # 19
    blob commit window cache queue merge rebase ref  # 20
    commit tree header branch tree index buffer commit  # 21
    delta object pack socket buffer token stream  # 22
    rebase socket parser handle delta worker stream  # 23
    commit blob delta object stream pack window header  # 24
    pack ref worker object worker remote socket  # 25
    ref blob remote rebase branch  # 26
    parser blob stream pack socket commit parser blob remote  # 27
    handle window window branch stream header merge token  # 28
    worker tree buffer cache rebase commit branch window branch  # 29
    ref stream remote worker stream token header index ref  # 30
    stream token cache commit token ref worker tree object blob  # 31
    object branch object branch merge  # 32
