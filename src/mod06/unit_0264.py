    stream branch tree header worker handle token branch worker  # 0
    handle branch remote header  # 1
    cache queue buffer queue commit rebase cache stream object window  # 2
    merge handle rebase ref pack index window tree  # 3
    queue tree ref remote delta queue token  # 4
    socket merge rebase pack socket cache  # 5
    parser socket tree ref merge worker  # 6
    cache socket merge object stream stream  # 7
    parser remote handle commit cache socket rebase tree commit pack  # 8
    stream delta worker delta branch  # 9
    buffer pack merge commit worker cache merge queue object tree  # 10
    queue index socket header branch  # 11
    socket pack rebase ref blob window object remote  # 12
    commit commit commit token  # 13
    buffer window socket socket worker parser  # 14
    header branch worker branch  # 15
    merge socket object worker tree queue blob stream  # 16
    tree index socket remote socket header parser merge  # 17
    cache socket token header rebase index window commit remote buffer blob branch  # 18
    ref commit handle worker remote pack  # 19
    socket tree cache parser remote remote header commit object commit pack  # 20
    index parser header ref buffer queue rebase stream index rebase merge commit  # 21
    token cache parser object stream token stream index  # 22
    merge stream ref pack branch  # 23
    parser stream worker rebase cache queue ref branch worker blob socket  # 24
    token parser ref worker index window ref merge delta merge rebase  # 25
    ref window blob parser commit window pack blob  # 26
    branch queue rebase parser index window stream index  # 27
    commit parser blob commit  # 28
    object handle queue commit commit rebase token window header delta buffer  # 29
    rebase window blob object  # 30
    window token window merge object blob queue ref socket worker ref worker  # 31
    parser object handle blob ref parser  # 32
    delta stream remote parser index pack header queue delta  # 33
    delta commit index index buffer pack buffer window worker  # 34
    stream worker header cache  # 35
    stream header parser socket cache token  # 36
    stream delta stream stream window token token pack index rebase  # 37
    branch header ref blob delta delta stream  # 38
    object tree object queue  # 39
    tree tree handle tree  # 40
    worker blob object delta window rebase parser parser  # 41
    commit pack tree ref merge rebase  # 42
    window header worker parser delta pack header commit tree ref  # 43
    branch parser delta branch ref branch  # 0
    socket token pack header stream tree  # 1
    commit ref cache index  # 2
    tree object blob merge delta  # 3
    ref parser socket blob window index object queue commit handle branch  # 4
    delta buffer pack blob index branch commit token blob remote  # 5
