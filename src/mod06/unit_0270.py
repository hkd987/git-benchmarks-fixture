    cache cache remote ref buffer worker ref pack delta window  # 0
    socket object window token parser rebase blob  # 1
    buffer token queue rebase commit merge tree stream object window  # 2
    remote token stream token branch parser merge pack index merge handle tree  # 3
    index pack window index header pack window header parser  # 4
    parser rebase merge header blob buffer  # 5
    window socket commit window delta object merge header  # 6
    branch rebase pack pack window  # 7
    worker tree cache cache worker object object token token buffer  # 8
    handle handle worker parser cache pack header delta tree branch  # 9
    delta header object parser branch pack index merge token queue commit buffer  # 10
    pack remote handle remote pack index window worker blob parser cache index  # 11
    header socket tree handle socket header  # 12
    pack stream worker buffer merge delta  # 13
    handle worker token remote parser index stream commit buffer tree  # 14
    socket handle commit pack index merge ref queue rebase  # 15
    buffer merge index remote handle object branch tree  # 16
    pack buffer window buffer buffer stream blob object merge  # 17
    remote token stream header cache  # 18
    rebase rebase stream handle stream rebase merge parser stream cache branch delta  # 19
    object object handle stream header worker ref object tree branch blob cache  # 20
    header handle token buffer merge window header window commit delta delta  # 21
    blob rebase token cache index stream worker branch socket  # 22
    object cache blob handle blob  # 23
    socket tree ref cache  # 24
    queue token ref worker merge  # 25
    queue blob token stream  # 26
    cache queue ref worker delta ref  # 27
    merge header buffer handle branch  # 28
    token handle branch header handle index rebase header  # 29
    stream socket window cache remote buffer object header cache branch merge remote  # 30
    token blob cache tree  # 31
    cache cache index worker stream cache  # 32
    cache index tree pack  # 33
    parser socket handle parser blob handle tree header  # 34
    branch rebase ref token stream ref  # 35
    handle stream object cache remote parser parser remote  # 36
    rebase rebase tree tree handle token pack ref parser  # 37
    worker blob queue merge  # 38
    buffer socket ref pack commit tree buffer cache  # 39
    tree cache cache header queue ref window branch  # 40
    worker header delta token ref token handle window parser handle stream  # 41
    ref remote remote merge tree queue  # 42
    object tree delta remote stream parser rebase handle  # 43
    branch rebase tree index commit header commit worker window merge handle cache  # 44
    index parser pack token socket rebase delta header queue  # 45
    index branch remote object handle  # 46
    blob ref delta stream commit index queue token remote token branch  # 47
    commit remote token object blob ref  # 48
    handle stream rebase commit delta socket handle  # 49
    parser commit blob cache header  # 50
    remote index branch branch tree rebase parser socket window handle handle  # 51
    token buffer merge socket remote commit  # 52
    blob pack socket socket stream stream socket tree  # 53
    handle pack object pack  # 54
    merge object delta handle cache socket delta blob token  # 55
    cache rebase object branch delta tree remote cache cache window  # 56
    commit ref header tree header rebase ref remote ref delta socket  # 57
    buffer handle index worker  # 58
    pack delta tree blob merge  # 59
    object token blob handle buffer commit  # 60
    parser window token delta index token header  # 61
    pack ref token worker pack index branch merge parser header  # 62
    blob stream index token socket merge header delta  # 63
    cache tree token object commit parser ref  # 64
