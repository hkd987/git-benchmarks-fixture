    buffer handle object worker cache window parser  # 0
    index commit commit object branch handle socket  # 1
    parser rebase buffer blob socket cache index ref  # 2
    handle object merge window merge token branch handle  # 3
    parser commit buffer cache commit pack  # 4
    branch branch buffer rebase  # 5
    worker tree cache window ref handle worker  # 6
    ref merge cache header delta parser  # 7
    blob token header parser parser  # 8
    window queue rebase socket stream merge  # 9
    handle branch branch blob socket window worker  # 10
    ref merge window rebase pack token blob handle stream rebase  # 11
    socket remote branch rebase queue  # 12
    merge rebase worker window branch  # 13
    tree delta worker cache pack header index branch  # 14
    header socket window commit index socket remote stream tree handle  # 15
    handle rebase pack worker commit remote  # 16
    ref parser socket header header merge commit worker stream  # 17
    merge delta parser tree queue commit buffer ref queue cache remote  # 18
    merge ref worker blob buffer pack buffer tree pack commit  # 19
    token cache token remote cache header ref cache window branch queue tree  # 20
    buffer buffer index window object merge cache buffer index merge remote pack  # 21
    blob tree queue socket  # 22
    remote tree blob delta  # 23
    ref blob buffer branch object header tree cache buffer header branch  # 24
    token token pack buffer token  # 25
    commit pack queue merge commit merge cache queue blob rebase  # 26
    rebase worker parser rebase  # 27
    pack queue delta socket remote buffer window delta handle pack commit  # 28
    rebase blob stream pack commit ref  # 29
    rebase branch stream handle parser queue worker header socket window  # 30
    cache rebase blob socket header rebase worker blob token remote  # 31
    blob delta object worker object  # 32
    index branch blob index pack  # 33
    object rebase cache merge pack handle blob handle index index  # 34
    object window index remote tree parser index commit pack  # 35
    tree merge rebase handle window pack queue index pack cache  # 36
    rebase commit token socket rebase remote index window object  # 37
    worker index delta ref  # 38
    branch ref rebase buffer blob object index remote stream blob buffer  # 39
    branch worker blob token header tree remote rebase worker  # 40
    merge parser branch cache  # 41
    parser remote stream cache worker object  # 42
    tree object handle commit token worker blob rebase tree rebase queue  # 43
    token worker merge cache branch object stream queue commit pack rebase  # 44
    remote blob worker commit tree remote merge pack header window buffer delta  # 45
    token worker handle commit rebase rebase buffer buffer socket cache  # 46
    branch tree merge tree stream index branch  # 47
    cache rebase index delta tree  # 48
    token window token stream remote  # 49
    remote remote remote handle pack ref delta ref remote remote parser  # 50
    worker branch socket window socket header tree  # 51
    buffer buffer window queue queue header object socket merge tree remote object  # 52
    token stream blob tree blob socket  # 53
    worker remote index socket branch ref object  # 54
    handle commit socket buffer buffer socket queue  # 55
    worker queue tree buffer rebase header tree worker buffer parser object tree  # 56
    window remote index rebase delta rebase  # 57
    token handle rebase delta  # 58
    commit header worker ref ref  # 59
    merge header remote window stream  # 60
    ref queue handle handle token buffer object ref index index pack merge  # 61
    handle merge window pack rebase  # 62
    branch ref buffer object token remote index remote rebase worker token  # 63
    stream blob stream buffer branch merge  # 64
    merge rebase rebase token  # 65
    blob parser queue header queue index merge header buffer rebase  # 66
    commit tree socket remote object ref commit  # 67
    window index blob token header cache ref  # 68
    branch index delta blob buffer tree socket pack  # 69
    socket parser stream buffer rebase buffer handle  # 70
    rebase socket rebase buffer  # 71
    index object remote queue branch worker remote index stream  # 72
    remote queue cache token remote  # 73
    worker queue commit window stream merge  # 74
    cache index socket rebase remote  # 75
    worker window ref index token branch object  # 76
    rebase header object token delta delta merge worker tree header stream  # 77
    pack merge blob handle buffer branch  # 78
    branch index remote object delta tree commit rebase worker tree buffer index  # 79
    commit merge branch queue worker object  # 80
    window parser buffer cache  # 81
    window delta token buffer window  # 82
    socket tree stream socket rebase rebase buffer queue parser index header  # 83
    buffer socket worker header socket object token parser ref  # 84
    buffer parser window window  # 85
