    commit token index commit stream index merge socket buffer stream pack cache  # 0
    delta parser remote window commit tree remote worker object cache ref  # 1
    worker buffer parser cache commit pack branch header  # 2
    pack token worker blob buffer blob ref stream window rebase  # 3
    queue branch handle stream token token blob  # 4
    branch ref window remote tree worker buffer rebase index worker  # 5
    parser branch buffer socket buffer header queue commit merge rebase header  # 6
    pack tree pack delta buffer header token tree merge cache  # 7
    index token rebase socket handle handle remote pack parser queue token commit  # 8
    handle parser stream header blob pack merge branch header object rebase stream  # 9
    delta pack stream ref socket  # 10
    parser buffer stream socket  # 11
    worker handle pack delta index remote worker index  # 12
    pack commit ref worker cache blob cache object  # 13
    handle object pack pack rebase remote delta worker socket  # 14
    merge pack worker pack cache  # 15
    header ref delta commit parser tree ref token  # 16
    index cache cache buffer object blob  # 17
    tree pack worker ref socket object  # 18
    commit socket stream index token  # 19
    socket handle branch index header tree  # 20
    parser delta blob socket cache socket header cache blob  # 21
    index buffer branch worker branch  # 22
    delta buffer stream socket token merge  # 23
    rebase index token tree merge header buffer queue header branch index merge  # 24
    queue queue remote tree buffer  # 25
    buffer index branch token commit parser parser buffer  # 26
    delta ref token parser worker index  # 27
    commit tree index header rebase  # 28
    commit index merge merge token window pack  # 29
    merge buffer branch ref commit handle header queue branch pack merge queue  # 30
    branch remote remote stream branch parser tree pack branch object ref delta  # 31
    ref blob buffer object socket blob parser socket stream handle handle  # 32
    handle socket pack branch queue rebase ref  # 33
    buffer cache header handle rebase window object ref handle socket worker  # 34
    commit tree parser rebase branch stream buffer  # 35
    buffer rebase delta header header worker header stream header token branch commit  # 36
    queue branch object parser index tree parser remote buffer  # 37
    handle header window tree merge buffer commit remote cache handle header stream  # 38
    queue pack worker window tree branch index tree parser window window queue  # 39
    branch handle pack blob tree header header window window  # 40
