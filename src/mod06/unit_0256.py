    tree handle socket remote handle remote commit queue  # 0
    commit parser window buffer stream parser  # 1
    socket stream merge pack remote delta token socket rebase merge window token  # 2
    stream buffer cache stream cache remote  # 3
    merge rebase socket cache  # 4
    blob commit rebase pack socket tree ref merge  # 5
    window window window delta token buffer token pack buffer commit  # 6
    token pack worker commit token  # 7
    header socket object pack stream socket cache merge  # 8
    remote header token tree queue handle parser ref pack buffer  # 9
    parser header pack parser cache delta object ref pack  # 10
    rebase object window branch token worker window cache  # 11
    cache window blob worker worker cache rebase tree commit index token  # 12
    index rebase tree tree object handle merge pack ref commit  # 13
    merge branch handle cache parser socket remote buffer  # 14
    remote parser cache branch remote tree remote ref cache delta  # 15
    handle index tree commit socket parser socket tree delta merge  # 16
    buffer object blob window commit  # 17
    rebase merge delta pack cache window tree socket queue  # 18
    cache window rebase index tree  # 19
    delta buffer index worker rebase index tree remote object index remote  # 20
    remote branch worker window rebase index pack  # 21
    parser tree delta pack header tree rebase  # 22
    cache worker delta blob handle rebase index  # 23
    parser branch handle window blob rebase tree branch parser ref stream stream  # 24
    blob pack window branch cache window commit index handle tree  # 25
    header worker handle pack handle token handle  # 26
    handle worker queue remote tree parser commit  # 27
    remote buffer tree rebase rebase rebase pack socket stream window socket  # 28
    merge commit merge object pack pack delta  # 29
    rebase blob buffer blob cache object pack remote worker  # 30
    index buffer cache cache cache blob buffer buffer window object buffer parser  # 31
    worker pack ref commit ref tree merge tree parser remote  # 32
    socket branch delta queue object object commit  # 33
    merge merge handle parser token merge window index rebase object  # 34
    window worker merge worker socket blob  # 35
    merge cache parser window window commit header socket commit index cache  # 36
    remote handle header stream socket socket socket worker ref  # 37
    stream index worker blob worker ref  # 38
    merge blob parser commit handle remote cache worker stream handle  # 39
    stream parser branch handle worker queue index  # 40
    ref parser header object parser worker handle rebase  # 41
    delta window pack index merge  # 42
    socket blob window rebase parser header rebase  # 43
    object commit commit rebase handle tree token pack branch  # 44
    queue pack merge worker branch token rebase  # 45
    handle header worker ref index token  # 46
    delta window window cache window blob worker branch pack stream  # 47
    token window queue window tree object rebase  # 48
    handle blob tree blob remote parser  # 49
    commit stream blob queue delta  # 50
    tree merge cache handle cache socket object tree queue token handle  # 51
    cache merge handle branch delta ref rebase handle buffer cache token ref  # 52
    handle remote header cache queue remote token stream worker index  # 53
    rebase buffer token remote branch ref  # 54
    pack header queue queue cache branch tree commit index remote window buffer  # 55
    blob cache handle index queue merge socket object stream queue  # 56
    commit queue cache parser cache merge object worker pack parser remote blob  # 57
    remote cache ref index object branch  # 58
    window pack token cache object token header  # 59
    socket delta commit rebase branch index rebase queue socket  # 60
    merge rebase socket pack blob tree blob tree  # 61
    token remote buffer object branch queue pack blob rebase  # 62
    delta parser tree commit merge  # 63
    worker cache blob blob pack ref remote handle remote worker  # 64
    pack index rebase token ref window window ref stream pack  # 65
    tree merge tree object tree socket pack branch blob  # 66
    socket pack branch remote header queue socket  # 67
    token stream rebase window delta delta handle parser index rebase buffer blob  # 68
    queue ref remote header object socket object ref index  # 69
    pack commit handle blob remote buffer buffer handle commit  # 70
    remote queue queue rebase branch  # 71
    parser stream merge commit  # 72
    index index commit window  # 73
    branch commit commit buffer  # 74
    socket delta commit object token cache index queue pack  # 75
    queue cache window token window pack blob window handle  # 76
    delta delta pack queue  # 77
    queue window remote window cache index socket rebase ref remote blob  # 78
    delta token delta branch  # 79
    delta remote stream header delta header index cache queue delta cache header  # 80
