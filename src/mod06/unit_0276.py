    header delta object socket remote merge parser merge  # 0
    merge remote parser socket blob object stream index cache object ref  # 1
    pack tree branch merge token object merge header stream merge  # 2
    merge window buffer index buffer cache parser pack  # 3
    buffer token rebase ref worker token queue tree stream token  # 4
    ref token cache remote commit socket window worker delta cache parser branch  # 5
    token rebase remote cache object merge buffer  # 6
    parser pack remote rebase socket cache  # 7
    handle tree merge blob branch window  # 8
    rebase token pack queue tree handle handle queue remote cache handle  # 9
    branch handle header buffer delta delta  # 10
    delta token branch queue commit handle commit socket blob cache cache window  # 11
    header ref stream buffer  # 12
    buffer worker object token socket worker  # 13
    cache header worker delta header merge rebase merge socket stream worker index  # 14
    token remote delta parser pack ref  # 15
    pack remote blob buffer rebase ref object handle token pack index  # 16
    merge worker header delta stream delta cache remote token buffer queue queue  # 17
    token pack remote window object tree token socket object remote socket queue  # 18
    blob window branch blob index remote remote  # 19
    branch socket worker buffer merge delta  # 20
    token stream tree ref index parser  # 21
    stream merge handle index window parser queue  # 22
    socket branch index index cache  # 23
    header worker remote object tree remote merge  # 24
    ref header buffer pack index tree buffer  # 25
    header delta pack queue queue buffer pack  # 26
    branch handle window tree branch buffer pack handle queue rebase handle  # 27
    commit worker delta cache pack object token  # 28
    buffer pack pack cache parser parser  # 29
    window branch delta window  # 30
    parser header tree header buffer socket index handle socket object  # 31
    object parser merge merge delta ref token handle  # 32
    header token object token delta delta window tree index delta rebase socket  # 33
    header index ref blob cache  # 34
    worker blob blob remote tree ref ref blob worker handle  # 35
    index branch parser header remote queue  # 36
    token index worker merge stream branch queue ref rebase  # 37
    window worker buffer branch queue branch socket header window  # 38
    socket parser buffer cache blob rebase token pack worker  # 39
    handle parser blob rebase delta merge window handle header token socket pack  # 40
    stream socket handle parser parser blob delta  # 41
    rebase pack buffer token buffer blob header blob merge  # 42
    tree token index socket buffer object worker stream rebase  # 43
    queue index blob object token header tree object rebase  # 44
    blob remote index object token  # 45
    socket tree merge ref  # 46
    cache commit handle socket worker object worker merge ref  # 47
    ref parser pack header blob  # 48
    stream stream buffer remote commit worker tree header buffer  # 49
    window pack tree object worker  # 50
    cache buffer delta worker pack handle ref  # 51
    merge branch index tree  # 52
    object ref merge rebase delta delta  # 53
    cache header merge branch  # 54
    rebase window cache delta branch socket token tree pack ref  # 55
    queue parser rebase worker tree token parser  # 56
    merge pack remote window commit parser rebase remote worker  # 57
    rebase header cache socket  # 58
    worker blob worker rebase buffer socket tree object remote window  # 59
    commit merge pack parser buffer index header pack pack branch stream  # 60
    worker merge stream handle  # 61
    delta pack pack tree delta  # 62
    stream queue handle parser commit socket  # 63
    token merge index branch tree remote worker buffer object ref token blob  # 64
    blob queue merge delta token  # 65
    parser remote socket pack merge socket delta  # 66
    parser delta tree stream pack tree parser header socket object delta window  # 67
    socket index rebase pack cache cache branch stream worker header stream  # 68
    parser delta branch rebase  # 69
    pack blob object window tree object worker parser branch window ref delta  # 70
    parser index tree parser index cache pack ref commit header  # 71
    buffer header branch delta commit pack parser branch pack rebase  # 72
    pack parser delta merge handle branch tree object delta  # 73
    ref blob token delta header blob cache branch parser window  # 74
    object buffer queue handle rebase socket  # 75
    pack ref window buffer object remote header  # 76
    header worker token rebase stream parser cache queue remote delta ref stream  # 77
    object socket socket queue cache handle  # 78
    cache parser cache remote socket worker blob rebase handle  # 79
    tree header parser worker ref handle window token  # 80
    parser rebase handle object handle parser index blob index  # 81
    index worker stream token ref  # 82
