    ref handle header queue pack cache branch index parser window tree  # 0
    remote ref buffer tree queue pack commit tree delta ref remote stream  # 1
    token ref parser pack tree handle socket socket commit commit socket window  # 2
    merge cache rebase object blob stream stream rebase index merge  # 3
    handle queue buffer index commit ref merge parser stream parser remote  # 4
    token tree merge header  # 5
    ref header buffer tree  # 6
    ref worker blob stream object rebase rebase pack token  # 7
    remote handle pack queue header worker pack header buffer  # 8
    commit parser queue delta merge commit object  # 9
    window index tree branch stream delta  # 10
    rebase stream cache rebase delta stream object window rebase window object  # 11
    worker window object ref cache index object window  # 12
    branch queue branch token buffer parser rebase token  # 13
    token commit buffer index object  # 14
    buffer pack parser pack parser parser  # 15
    tree object token rebase worker blob ref queue  # 16
    commit header ref branch  # 17
    token handle queue commit branch pack index worker header handle  # 18
    header merge worker remote merge socket worker socket pack socket queue  # 19
    buffer worker window handle pack queue tree handle merge object  # 20
    branch cache ref queue handle handle  # 21
    stream stream commit socket remote worker rebase branch object delta  # 22
    tree branch blob pack merge worker branch handle  # 23
    ref object remote header remote object stream index window cache  # 24
    merge object branch branch stream stream token index object worker  # 25
    rebase rebase delta tree commit parser rebase  # 26
    worker worker queue buffer blob token remote  # 27
    worker blob handle remote blob pack  # 28
    object commit handle worker commit ref ref remote queue header blob  # 29
    branch stream window ref queue commit handle socket  # 30
    stream parser object parser commit header merge worker socket delta  # 31
    parser handle token remote commit  # 32
    pack token pack cache merge object object stream  # 33
    parser pack rebase window handle ref header commit queue  # 34
    commit blob window cache index stream worker header buffer remote handle rebase  # 35
    stream delta ref commit ref  # 36
    socket branch handle parser stream branch header merge buffer branch  # 37
    tree parser buffer stream index index queue socket stream merge index  # 38
    token delta rebase socket blob blob socket socket tree pack cache branch  # 39
    ref window delta stream  # 40
    blob rebase object queue tree token worker buffer window handle  # 41
    pack worker handle ref worker blob  # 42
    branch cache pack ref handle branch socket socket token parser token merge  # 43
    ref index socket socket blob stream token rebase cache  # 44
    remote ref remote buffer socket  # 45
    index rebase stream window delta index delta ref branch delta ref  # 46
    merge rebase index remote object  # 47
    branch cache cache token merge stream token remote merge branch token ref  # 48
    tree token ref branch buffer queue token index index socket rebase blob  # 49
    queue merge tree header blob  # 50
    object stream merge pack window header rebase  # 51
    merge ref object index cache handle cache stream  # 52
    queue object stream cache  # 53
    branch worker buffer blob rebase rebase token window  # 54
    parser blob cache worker  # 55
    stream handle header remote stream token merge  # 56
    remote index worker index blob  # 57
    stream commit socket rebase  # 58
    object object index index pack header  # 59
    buffer token remote socket  # 60
    worker socket parser pack tree  # 61
    pack object delta token tree branch tree  # 62
    delta commit object parser remote remote  # 63
    header object branch cache token socket window index remote tree  # 64
    stream worker token remote token  # 65
    worker commit tree window header object remote remote blob parser  # 66
    branch worker stream rebase delta  # 67
    worker stream header remote  # 68
    commit remote header remote token object  # 69
    blob queue blob tree cache branch window commit ref  # 70
    stream buffer window handle index worker stream window remote  # 71
    window rebase socket buffer object cache  # 72
    commit ref delta blob handle token blob buffer branch socket blob  # 73
    buffer merge tree parser cache window window branch header cache  # 74
    queue window stream delta ref remote token rebase  # 0
    queue cache merge socket index branch handle header cache window index merge  # 1
    object index worker worker window parser handle  # 2
    delta remote queue ref cache tree token  # 3
    worker socket remote commit queue index token tree branch index header rebase  # 4
    cache worker delta parser  # 5
    socket pack socket delta  # 6
    object ref token token ref tree header commit  # 7
    worker buffer cache commit commit tree queue ref cache socket delta  # 8
    queue object tree cache branch delta  # 9
