    cache object blob parser window pack tree merge  # 0
    blob merge buffer commit  # 1
    merge rebase index object parser rebase socket token tree commit window window  # 2
    rebase ref delta pack rebase socket worker  # 3
    commit pack queue ref tree rebase index ref merge remote cache stream  # 4
    window merge stream queue tree pack buffer window handle commit tree buffer  # 5
    commit window pack worker branch pack pack handle tree  # 6
    socket object commit cache parser header buffer parser merge  # 7
    queue rebase buffer tree pack  # 8
    rebase merge parser cache blob remote tree remote stream  # 9
    socket header branch stream stream header object tree branch  # 10
    cache blob blob header  # 11
    socket handle merge delta rebase buffer ref header branch  # 12
    stream worker buffer merge blob pack tree pack buffer tree stream rebase  # 13
    blob merge commit commit blob  # 14
    branch tree handle window buffer tree parser rebase queue rebase  # 15
    header cache handle window cache window  # 16
    window stream token object blob delta stream  # 17
    cache buffer window ref tree token rebase rebase  # 18
    cache queue tree token  # 19
    ref header rebase parser index blob pack tree parser branch remote stream  # 20
    worker delta blob blob token parser parser window object index parser remote  # 21
    handle pack index socket  # 22
    merge merge queue cache  # 23
    token rebase blob object commit rebase rebase parser parser buffer stream  # 24
    buffer ref stream ref rebase commit commit handle blob worker delta rebase  # 25
    handle rebase window rebase delta index parser commit remote  # 26
    stream buffer socket cache queue commit queue pack pack rebase  # 27
    cache merge cache delta merge pack  # 28
    socket cache remote blob buffer worker blob  # 29
    stream token index tree pack  # 30
    merge stream blob queue parser pack commit socket parser rebase  # 31
    socket pack delta blob window  # 32
    branch parser parser object rebase ref  # 33
    worker object socket tree stream  # 34
    cache index parser ref cache header window  # 35
    merge ref delta remote merge worker header handle pack ref  # 36
    commit index tree buffer socket buffer buffer  # 37
    ref delta rebase handle queue blob  # 38
    merge token parser window ref delta  # 39
    cache window buffer pack merge buffer delta handle socket window  # 40
    tree queue cache queue commit pack queue cache merge delta handle  # 41
    pack blob cache worker commit object ref  # 42
    branch pack branch branch branch parser header blob commit delta header  # 43
    worker window merge socket  # 44
    pack parser stream blob stream object  # 45
    rebase window branch parser socket delta  # 46
    worker buffer remote ref  # 47
    remote parser rebase branch pack merge cache blob delta  # 48
