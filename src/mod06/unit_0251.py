    worker parser commit ref stream worker cache  # 0
    token window buffer branch commit branch delta branch  # 1
    socket object window buffer pack ref delta commit merge  # 2
    tree queue queue token index window object  # 3
    window worker index object window queue cache window index ref  # 4
    cache pack object window tree  # 5
    rebase buffer ref index pack tree tree socket stream worker handle buffer  # 6
    stream ref worker tree  # 7
    queue commit header blob cache worker token  # 8
    pack merge socket remote commit handle branch window handle  # 9
    handle cache window branch parser rebase parser queue handle stream index  # 10
    ref merge socket parser  # 11
    cache tree stream object branch  # 12
    pack window commit parser cache ref pack queue  # 13
    cache parser merge worker delta pack header tree buffer token delta blob  # 14
    branch rebase buffer blob remote worker delta rebase parser  # 15
    pack socket buffer pack commit remote merge worker socket branch ref  # 16
    worker worker socket worker stream cache stream window delta delta  # 17
    index object buffer commit cache  # 18
    token worker ref blob stream  # 19
    handle worker tree remote remote blob tree  # 20
    window ref token blob commit ref handle blob branch  # 21
    handle window delta pack merge parser ref header merge socket stream  # 22
    parser blob parser merge worker  # 23
    blob commit blob commit object rebase pack cache merge ref  # 24
    header cache rebase branch object header object queue buffer queue  # 25
    rebase token buffer window commit handle ref delta stream object  # 26
    token merge object handle rebase socket commit worker queue  # 27
    commit handle commit cache tree tree index socket  # 28
    ref handle header ref parser header header token branch delta rebase merge  # 29
    rebase stream token merge remote cache delta ref commit  # 30
    cache token blob queue  # 31
    blob buffer ref window branch pack pack stream ref  # 32
    cache window remote socket window queue index object index  # 33
    header tree token delta commit merge merge index window header delta handle  # 34
    parser delta remote merge stream branch queue ref  # 35
    socket parser socket handle cache tree parser parser window commit object  # 36
    queue ref branch merge blob  # 37
    index handle rebase commit pack branch cache  # 38
    token socket remote remote blob token  # 39
    commit window handle index rebase commit token object merge queue header  # 40
    delta branch delta parser tree  # 41
    token commit buffer header handle delta merge  # 42
    remote header delta buffer header  # 43
    rebase window handle merge delta header stream object branch remote  # 44
    queue pack rebase tree socket token  # 45
    handle ref pack buffer blob rebase  # 46
    queue window delta window tree blob  # 47
    header token index buffer tree object commit  # 48
    remote tree token window worker delta rebase merge index  # 49
    header cache cache pack pack  # 50
    object branch merge object parser pack worker pack queue branch  # 51
    header object object ref stream window branch socket object blob tree  # 52
    token commit socket remote handle index  # 53
    cache token tree merge queue  # 54
    handle header tree index object tree blob  # 55
    header handle branch commit remote tree queue delta commit index index cache  # 56
    parser remote pack handle branch index tree  # 57
    delta blob queue merge ref buffer  # 58
    worker handle worker branch blob pack buffer  # 59
    remote delta ref window merge parser socket object  # 60
    window remote pack commit blob buffer window pack delta delta buffer rebase  # 61
    token header handle delta index  # 62
    branch pack parser stream merge  # 63
    queue tree ref branch handle  # 64
