    tree worker token handle tree delta  # 0
    queue socket cache stream  # 1
    window handle buffer tree parser worker worker pack rebase cache  # 2
    worker handle branch object commit index pack pack  # 3
    rebase branch token ref window index merge handle handle window tree  # 4
    delta handle pack pack index cache cache object object cache socket token  # 5
    token blob pack tree queue parser buffer socket token worker tree  # 6
    buffer ref queue pack parser header socket  # 7
    parser header buffer ref pack header index parser  # 8
    stream worker cache cache merge  # 9
    stream index object header object delta queue  # 10
    header object branch window buffer  # 11
    pack blob rebase commit rebase branch  # 12
    cache ref branch handle delta tree rebase object  # 13
    handle handle handle stream tree tree buffer commit merge blob  # 14
    worker branch rebase remote socket object queue buffer cache  # 15
    header cache tree delta pack worker remote commit  # 16
    blob branch socket branch delta  # 17
    socket header commit handle merge header merge token buffer  # 18
    index handle branch ref buffer pack index buffer object remote remote window  # 19
    delta token ref buffer socket parser  # 20
    object merge header window worker queue worker worker  # 21
    commit object branch branch  # 22
    pack object parser cache merge tree socket merge token  # 23
    parser merge socket handle cache stream parser branch socket ref rebase  # 24
    queue stream ref cache blob header blob parser socket  # 25
    buffer header window index cache cache pack worker handle  # 26
    commit buffer header tree blob commit stream stream blob remote tree queue  # 27
    buffer merge index index parser cache buffer pack  # 28
    header object tree commit  # 29
    buffer worker window socket handle buffer  # 30
    handle delta worker header  # 31
    token queue merge window cache worker merge ref handle delta  # 32
    buffer object pack queue window branch header parser object token  # 33
    cache cache object object merge  # 34
    tree branch rebase stream ref remote branch socket merge merge  # 35
    index parser object commit  # 36
    index buffer rebase header commit object index delta pack buffer  # 37
    tree cache handle stream rebase  # 38
    delta object socket ref branch pack  # 39
    tree ref token remote branch branch rebase window  # 40
    handle queue branch token stream blob rebase object remote worker  # 41
    handle parser handle commit cache  # 42
    commit buffer delta commit  # 43
    ref cache socket ref  # 44
    handle index remote ref  # 45
    blob commit object buffer branch worker index object worker header ref commit  # 46
    window branch parser merge header rebase  # 47
    socket socket token object delta cache window rebase handle  # 48
    commit token delta ref remote cache commit token branch blob  # 49
    index stream header tree  # 50
    token blob merge handle window queue cache  # 51
    header token worker rebase handle  # 52
    delta merge commit buffer socket ref remote delta index header  # 53
    stream delta object buffer socket window  # 54
    merge token token handle rebase handle blob blob blob index  # 55
    window merge cache remote  # 56
    index queue cache rebase index index index buffer pack branch pack delta  # 57
    object tree header pack parser  # 58
    commit remote worker index commit  # 59
    token remote token rebase  # 60
    index object object delta remote ref pack tree worker  # 61
    remote index stream rebase delta header worker buffer  # 62
    cache merge tree ref buffer stream blob  # 63
    pack commit merge handle  # 64
    header header header handle merge worker parser worker  # 65
