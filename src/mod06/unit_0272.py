    header buffer queue cache buffer  # 0
    rebase parser object token header window window  # 1
    delta delta blob tree token remote stream  # 2
    header delta merge commit  # 3
    index blob token header token tree socket header handle token token  # 4
    rebase pack worker socket object commit window merge buffer socket rebase rebase  # 5
    delta ref branch handle commit stream buffer index header merge  # 6
    ref queue queue queue commit  # 7
    stream rebase pack worker pack stream buffer ref remote  # 8
    blob cache header stream merge queue object blob branch token  # 9
    handle merge token commit ref pack buffer token queue branch  # 10
    pack parser header remote index delta  # 11
    blob rebase handle cache rebase object handle token tree  # 12
    socket merge worker branch commit window  # 13
    parser index queue rebase header window index buffer cache commit buffer  # 14
    branch remote cache delta socket index merge worker cache cache index  # 15
    delta branch socket commit blob delta tree socket cache queue index stream  # 16
    remote tree worker tree buffer  # 17
    header branch object blob pack header stream buffer pack stream blob  # 18
    commit ref stream queue ref stream delta  # 19
    object parser window index socket  # 20
    queue cache blob object socket  # 21
    stream handle token delta  # 22
    object branch branch handle rebase parser  # 23
    header object worker cache index object socket branch  # 24
    socket pack delta delta cache remote index queue delta ref ref blob  # 25
    socket merge buffer branch remote parser cache branch delta  # 26
    rebase token blob index stream ref stream delta worker pack  # 27
    token buffer cache object window commit token ref worker  # 28
    rebase queue ref socket handle rebase window socket parser remote handle blob  # 29
    delta window index index blob buffer remote queue  # 30
    index worker remote cache delta remote branch header worker rebase  # 31
    window commit handle ref rebase commit header buffer tree blob  # 32
    commit remote blob socket worker pack rebase  # 33
    tree stream window window ref worker  # 34
    rebase object delta token parser blob stream buffer  # 35
    socket commit parser tree index blob buffer branch parser token pack  # 36
    pack parser window object worker branch  # 37
    commit pack header remote window token rebase buffer  # 38
    blob token stream parser ref  # 39
    socket cache blob handle merge tree index index ref index  # 40
    blob queue parser socket buffer queue index header branch  # 41
    queue object stream cache delta  # 42
    branch cache ref buffer rebase stream  # 43
    index branch commit stream cache delta socket branch worker index buffer commit  # 44
    parser tree parser window tree pack stream worker index worker  # 45
    merge queue socket handle cache ref  # 46
    branch queue rebase ref index handle object commit delta handle  # 47
    cache cache ref handle window merge ref  # 48
    merge branch token token branch buffer worker socket socket tree object ref  # 49
    delta blob buffer queue  # 50
    buffer merge delta cache parser remote branch window ref remote  # 51
    delta tree rebase branch token stream branch rebase socket cache  # 52
    blob ref branch merge  # 53
    branch parser ref cache  # 54
    cache stream handle parser queue commit header  # 55
    worker blob ref handle header pack  # 56
    socket cache token pack remote cache  # 57
    tree token delta blob worker  # 58
    commit ref blob cache pack buffer remote  # 59
    header buffer pack ref  # 60
    tree header pack header  # 61
    handle parser stream rebase branch pack window token header token rebase merge  # 62
    delta parser merge socket window worker ref handle  # 63
    header ref commit buffer queue cache token remote  # 64
    window tree window tree remote socket buffer socket remote worker handle delta  # 65
    buffer socket token delta ref rebase  # 66
    delta cache parser delta cache delta branch remote  # 67
    stream cache header header stream  # 68
    pack tree blob tree  # 69
    branch socket socket object cache index branch tree cache worker  # 70
    socket token cache delta branch buffer commit window  # 71
    parser ref merge tree merge header delta stream object delta  # 72
    index buffer cache remote stream index queue cache pack header rebase  # 0
    window stream merge socket  # 1
    handle token stream delta parser blob parser object socket cache ref rebase  # 2
    token queue parser ref token object branch handle socket handle  # 3
    commit parser cache delta stream rebase blob branch tree parser worker queue  # 4
    cache parser parser window window rebase  # 5
    socket branch socket index handle rebase queue  # 6
    parser branch delta remote delta socket  # 7
    window object merge pack index merge  # 8
    socket socket stream parser  # 9
    merge delta ref index tree  # 10
    queue token socket rebase pack index worker branch ref  # 11
    socket header object queue buffer window parser  # 0
    queue parser ref branch  # 1
    worker object handle blob  # 2
    pack queue window token blob header index header cache index branch pack  # 3
    queue rebase remote socket rebase token pack socket merge index pack  # 4
    blob queue stream token object cache window stream rebase  # 5
    ref blob worker window handle token remote merge worker  # 6
    pack commit branch buffer commit  # 7
