    socket window worker buffer index buffer token  # 0
    pack index stream commit ref merge token branch header commit socket rebase  # 1
    handle blob worker buffer pack index index handle  # 2
    cache cache merge socket buffer ref handle blob token delta blob  # 3
    token blob socket worker window queue delta merge queue window  # 4
    token parser object ref  # 5
    cache branch merge remote handle ref commit merge window  # 6
    remote rebase header handle handle  # 7
    ref header cache delta pack merge object stream  # 8
    parser queue queue pack window pack queue blob delta socket header  # 9
    parser parser header queue branch index ref header window header  # 10
    socket merge window index header  # 11
    queue parser handle parser pack merge worker worker  # 12
    merge commit delta pack token worker branch cache tree cache  # 13
    queue socket stream pack  # 14
    tree pack window handle socket blob window cache pack remote rebase tree  # 15
    queue index stream commit buffer stream blob  # 16
    index delta blob token pack queue branch object  # 17
    handle merge token remote remote  # 18
    parser cache header buffer rebase stream index window  # 19
    pack delta socket stream commit parser cache worker remote worker token buffer  # 20
    object pack ref object token branch parser parser  # 21
    cache blob delta commit rebase branch pack parser delta ref  # 22
    merge commit buffer remote index blob tree  # 23
    delta object handle token parser token cache parser header socket delta worker  # 24
    socket remote worker object handle buffer  # 25
    socket object blob merge blob remote delta stream index token queue commit  # 26
    pack commit commit queue commit handle merge merge stream stream  # 27
    socket object handle handle queue worker header window cache branch branch  # 28
    object ref remote stream ref ref index cache  # 29
    ref stream blob delta ref parser queue object pack  # 30
    header token handle index parser  # 31
    rebase buffer blob header  # 32
    blob window ref token queue  # 33
    index worker queue rebase delta object queue token  # 34
    index buffer window token  # 35
    token buffer blob worker cache parser socket  # 36
    merge merge worker delta stream ref cache parser handle blob ref  # 37
    merge queue rebase blob ref ref branch branch queue window queue  # 38
    stream window stream socket rebase blob object buffer buffer cache  # 39
    worker cache blob window token merge  # 40
    stream remote queue branch buffer ref worker socket worker  # 41
    commit commit delta cache remote worker object  # 42
    pack stream tree index commit remote header  # 43
    cache window stream window commit stream token branch index cache  # 44
    parser blob handle pack stream index cache window index window  # 45
    stream index merge cache delta header socket parser ref index  # 46
    remote cache merge cache worker ref handle ref commit token  # 47
    cache socket token window queue merge stream cache merge delta  # 48
    blob blob pack blob blob token ref branch index index remote merge  # 49
    index socket branch parser parser buffer  # 50
    buffer pack delta queue branch commit socket  # 51
    branch delta parser socket queue window  # 52
    parser parser socket tree parser parser index blob ref  # 53
    branch object rebase blob merge header queue window handle header commit  # 54
    parser header delta merge  # 55
    queue queue rebase worker index handle index token  # 56
    remote queue socket pack socket parser remote worker index tree commit  # 57
    worker blob handle delta index delta token queue  # 58
    socket remote window blob  # 59
    stream handle parser socket header handle stream blob object branch  # 60
    handle object remote delta  # 61
    commit merge tree tree worker queue blob token  # 62
    merge delta branch socket index stream handle delta remote ref worker  # 63
    handle stream ref index stream queue object object rebase object  # 64
    blob blob object window index branch header index branch  # 65
    socket queue queue window delta tree blob index socket merge  # 66
    rebase rebase token object parser branch queue handle queue blob token handle  # 67
    tree cache pack branch parser index remote  # 68
    merge worker window header commit handle branch worker pack remote rebase  # 69
    object pack remote ref worker rebase stream object  # 70
    handle worker header merge blob merge remote worker  # 71
    index stream rebase header handle socket worker token socket socket  # 72
    worker worker stream branch index branch window tree  # 73
    branch worker queue tree tree commit merge branch delta handle pack window  # 74
    ref remote socket commit cache index ref header blob index  # 75
    buffer object buffer remote worker ref pack  # 76
    header token tree rebase blob tree socket remote header socket pack delta  # 77
    remote merge commit merge window stream pack merge token handle  # 78
    pack delta merge blob  # 79
