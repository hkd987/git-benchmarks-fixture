    merge ref merge remote  # 0
    socket object branch remote delta stream index tree ref buffer  # 1
    stream token commit ref handle pack rebase delta stream  # 2
    worker object buffer remote delta object pack token commit window  # 3
    buffer pack header tree ref  # 4
    branch buffer ref branch token pack queue tree token  # 5
    object object rebase tree socket socket  # 6
    window header blob token rebase object  # 7
    remote object rebase worker queue object window tree pack socket parser  # 8
    pack stream parser index socket window queue rebase stream commit  # 9
    pack socket token queue remote socket ref  # 10
    remote merge ref token buffer object object socket rebase blob  # 11
    ref header queue rebase  # 12
    tree delta cache handle  # 13
    branch stream merge window remote worker delta  # 14
    commit tree merge token cache header header pack header header token  # 15
    socket object commit parser blob delta pack  # 16
    ref ref token buffer index socket stream rebase rebase index handle socket  # 17
    remote delta ref worker blob handle queue merge object branch object  # 18
    object pack header commit parser tree object window merge  # 19
    worker window stream blob window rebase commit  # 20
    branch stream commit index socket merge blob  # 21
    header token object cache worker token socket window blob pack  # 22
    socket index token header index queue tree  # 23
    ref object index stream rebase  # 24
    rebase object ref commit stream stream buffer commit merge ref  # 25
    delta parser rebase window remote rebase delta worker worker worker handle  # 26
    stream blob buffer pack delta queue stream  # 27
    merge cache commit remote delta window tree object remote  # 28
    parser stream header stream  # 29
    handle window window buffer stream token queue  # 30
    stream socket tree handle blob  # 31
    tree remote remote branch  # 32
    ref token object index socket  # 33
    worker merge rebase object  # 34
    rebase ref index stream header rebase ref  # 0
    buffer header window commit worker  # 1
    ref commit commit stream object queue buffer branch pack  # 2
    tree header worker header  # 3
    worker queue object blob  # 4
    handle merge socket header worker buffer queue buffer  # 5
    delta stream object buffer pack  # 6
    rebase buffer delta rebase worker socket  # 0
    window worker socket handle index header handle ref branch  # 1
    queue buffer stream blob cache pack  # 2
    socket token object cache cache cache ref merge parser commit pack delta  # 3
    merge rebase commit commit  # 4
    merge delta merge branch stream tree rebase remote branch  # 5
    object pack socket queue  # 6
    index tree pack remote worker header  # 7
    pack branch pack ref rebase  # 8
    tree header tree token commit commit commit socket token header ref header  # 9
    pack token tree pack branch tree pack merge commit  # 10
    rebase blob handle token header stream buffer blob  # 11
    blob stream rebase parser cache  # 12
    tree stream worker window socket cache  # 13
