    delta token ref pack cache token ref object commit stream rebase  # 0
    token header branch token merge  # 1
    window object worker header remote ref tree cache header socket remote  # 2
    merge buffer pack branch index handle  # 3
    stream socket parser buffer pack pack parser commit commit commit header worker  # 4
    header tree rebase blob handle window remote socket  # 5
    remote branch header stream ref queue token  # 6
    buffer index parser ref  # 7
    rebase token object token  # 8
    handle handle merge blob queue worker cache token buffer header branch  # 9
    tree index delta parser blob stream  # 10
    parser delta worker buffer index tree worker buffer cache object branch merge  # 11
    header queue index worker  # 12
    queue rebase commit cache tree stream parser parser pack socket  # 13
    socket parser index token buffer socket stream object  # 14
    ref tree branch buffer tree header ref  # 15
    merge token delta rebase remote remote commit object blob object  # 16
    blob rebase handle commit  # 17
    object branch cache parser token parser parser stream index  # 18
    index remote stream pack socket branch index parser commit rebase  # 19
    object commit object handle window merge commit pack worker socket  # 20
    commit parser remote commit  # 21
    header branch stream buffer token cache header  # 22
    parser tree index blob buffer buffer ref window commit delta buffer stream  # 23
    buffer index worker buffer index branch worker buffer queue rebase pack delta  # 24
    commit cache buffer stream stream stream tree queue token stream  # 25
    tree buffer buffer header commit delta tree  # 26
    handle commit rebase delta  # 27
    ref cache socket window merge delta blob tree stream pack tree  # 28
    queue rebase commit rebase object worker  # 29
    header stream stream blob handle buffer socket stream parser tree queue  # 30
    branch token queue delta handle rebase  # 31
    merge window header window worker token queue cache commit rebase worker  # 32
    blob delta window stream parser token stream cache  # 33
    remote tree stream worker queue token pack ref worker  # 34
    remote token object merge blob tree rebase commit commit  # 35
    parser parser cache ref parser pack object  # 36
    ref blob token branch socket ref merge socket remote  # 37
    remote rebase parser blob object stream ref object merge token socket ref  # 38
    ref branch socket rebase cache delta socket header merge commit  # 39
    socket stream parser tree parser blob rebase pack parser parser blob  # 40
    merge blob handle stream remote cache socket socket  # 41
    pack handle window delta  # 42
    socket pack queue queue parser  # 43
    cache window handle stream rebase  # 44
    parser header merge pack remote buffer rebase index stream  # 45
    rebase handle pack cache remote window pack cache  # 46
    index cache parser delta token rebase merge queue  # 47
    cache merge cache window header cache buffer blob socket blob commit  # 48
    socket socket buffer tree cache handle remote  # 49
    socket remote pack rebase commit pack pack rebase  # 50
    buffer index blob tree blob index handle  # 51
    index remote stream cache  # 52
    blob blob buffer rebase index  # 53
    branch stream parser stream socket ref parser pack remote stream socket  # 54
    remote tree branch parser buffer  # 55
    merge tree index socket blob stream socket  # 56
    blob header socket cache  # 57
    tree remote ref socket socket  # 58
    delta object token merge index header  # 59
    branch delta token tree  # 60
    token object cache rebase parser  # 61
    rebase socket branch parser pack handle  # 62
    delta cache socket object worker commit window  # 63
    socket blob worker ref merge delta buffer tree remote pack branch  # 64
    ref branch token object ref blob index branch blob merge tree queue  # 65
    worker index stream window branch parser header window cache ref  # 66
    merge branch pack branch  # 67
    remote socket remote rebase parser pack index  # 68
    delta queue blob merge blob tree commit buffer queue  # 69
    stream stream merge buffer queue stream token index parser  # 70
    merge delta merge delta object worker stream  # 71
    merge parser pack pack parser stream  # 72
    queue cache token ref branch stream handle commit remote handle blob token  # 73
    pack blob worker token commit blob worker blob blob pack object merge  # 74
    ref parser object worker branch delta token  # 75
    buffer socket header handle ref index  # 76
    socket parser object socket ref cache pack ref  # 77
    socket header index window branch branch ref worker  # 78
    buffer token token header pack tree queue remote blob  # 79
    handle parser stream branch buffer window delta rebase  # 0
    blob cache queue branch branch  # 1
    worker tree socket header object merge rebase  # 2
    worker cache ref buffer blob stream delta header commit commit worker stream  # 3
