    stream buffer tree socket stream ref ref parser cache blob worker pack  # 0
    worker remote token parser stream queue buffer  # 1
    pack buffer index parser branch merge merge cache parser header  # 2
    merge window queue branch buffer tree  # 3
    object index window ref stream ref remote delta branch commit commit  # 4
    commit blob stream cache queue stream token branch merge pack  # 5
    blob tree socket index socket window tree index token  # 6
    merge pack cache index token buffer handle branch delta stream header  # 7
    object parser buffer rebase window handle buffer rebase delta  # 8
    remote socket blob worker blob pack index  # 9
    handle index commit blob queue index queue rebase object cache  # 10
    socket delta buffer handle tree  # 11
    pack commit index socket blob buffer window worker parser header blob  # 12
    window blob worker merge object commit buffer pack ref queue window tree  # 13
    commit commit parser window ref token stream pack remote  # 14
    parser queue buffer window window ref cache queue handle window blob queue  # 15
    window pack socket index blob parser index handle object socket branch index  # 16
    branch cache header window pack ref cache delta  # 17
    remote token handle stream object delta blob worker  # 18
    token window cache worker socket  # 19
    stream header window object blob parser stream  # 20
    merge handle commit branch cache ref object  # 21
    socket token rebase ref stream stream object buffer  # 22
    parser stream rebase token object token worker token queue delta merge delta  # 23
    parser ref ref delta merge pack  # 24
    ref remote cache object worker window tree  # 25
    buffer header header socket worker worker object cache buffer queue object commit  # 26
    header window object commit object tree object rebase queue  # 27
    token object token stream object rebase parser index tree header merge  # 28
    token rebase socket socket token object remote handle ref object token merge  # 29
    window parser cache remote worker  # 30
    branch cache commit ref index  # 31
    tree branch pack stream  # 32
    window rebase header object header commit object ref parser delta  # 33
    pack stream ref cache  # 34
    object window tree tree worker window merge  # 35
    worker blob worker socket tree header  # 36
    commit tree window rebase handle  # 37
    index object remote branch merge token delta buffer cache parser branch  # 38
    rebase ref remote branch cache window index ref socket index  # 39
    stream buffer handle branch remote tree branch index blob  # 40
    header cache branch worker remote tree pack socket ref index  # 41
    branch header worker buffer parser  # 42
    commit object parser queue  # 43
    window ref remote remote buffer  # 44
    worker index merge header cache handle  # 45
    socket rebase socket token cache stream rebase worker delta  # 46
    cache branch token remote remote queue token parser header  # 47
    merge branch cache token parser token window parser queue blob blob ref  # 48
    merge branch pack cache parser branch merge blob header  # 49
    index token branch delta commit queue stream handle header header delta object  # 50
    stream handle branch header tree commit index index socket token worker  # 51
    socket rebase socket stream socket object tree socket window  # 52
    index rebase pack queue worker branch cache object socket header  # 53
    index window branch branch socket  # 54
    window remote commit parser ref remote blob pack  # 55
    index object remote parser commit  # 56
    ref index index remote rebase cache queue cache queue remote commit  # 57
    blob cache index ref ref stream delta branch commit cache  # 58
    token stream cache buffer branch header socket merge  # 59
    pack blob index worker socket object header ref  # 60
    tree rebase stream object handle  # 61
    queue delta delta stream token branch commit  # 62
    handle socket object merge token handle merge branch pack buffer header  # 63
    pack tree delta worker buffer token commit worker  # 64
    worker cache worker queue  # 65
    ref commit ref cache  # 66
    commit ref branch delta rebase object  # 67
    buffer stream remote pack cache window  # 68
    cache object worker socket blob index buffer tree  # 69
    header remote parser blob queue token  # 70
    ref parser delta merge stream buffer  # 71
    cache header handle index cache blob handle token  # 72
    object delta queue blob  # 73
    window buffer cache pack delta pack queue tree rebase queue rebase  # 74
    token commit remote commit rebase commit merge rebase token index handle  # 0
    window remote tree rebase buffer handle handle cache queue  # 1
    handle blob queue handle cache  # 2
    ref parser parser pack window rebase blob buffer  # 3
    handle object window delta queue merge stream merge window cache  # 4
    ref cache parser handle socket cache token  # 5
