    worker delta parser remote  # 0
    commit rebase delta token remote buffer buffer delta remote object  # 1
    parser blob delta pack branch delta header stream index commit ref rebase  # 2
    stream ref merge tree rebase object  # 3
    worker merge index socket header delta branch blob  # 4
    parser queue ref socket rebase stream pack handle cache index index  # 5
    token worker header token branch parser cache ref blob  # 6
    stream merge object remote blob handle pack merge  # 7
    delta worker header buffer rebase pack cache header pack  # 8
    pack commit socket ref index socket worker blob index commit merge  # 9
    index buffer remote object branch pack pack rebase remote  # 10
    commit queue queue worker queue merge object branch ref  # 11
    object blob stream merge blob merge cache  # 12
    index rebase pack parser delta parser cache tree pack  # 13
    blob delta token rebase index stream token rebase  # 14
    pack socket index index cache  # 15
    object handle object pack pack merge  # 16
    parser tree window pack  # 17
    worker header worker rebase worker cache branch socket window merge  # 18
    tree socket parser handle token commit  # 19
    window ref rebase object  # 20
    header worker cache ref merge socket  # 21
    parser delta branch blob worker delta branch rebase ref queue object socket  # 22
    commit stream cache tree parser worker  # 23
    rebase object rebase cache delta  # 24
    merge index stream window window branch handle header worker stream worker parser  # 25
    tree branch commit remote pack blob  # 26
    socket header object stream window buffer pack branch token handle worker token  # 27
    object tree ref parser remote cache window worker tree  # 28
    commit token socket merge commit header commit handle object  # 29
    object ref window branch blob commit cache delta merge buffer parser  # 30
    worker remote buffer pack buffer remote  # 31
    object buffer delta handle header token cache stream worker remote queue  # 32
    handle cache window tree ref worker remote ref rebase  # 33
    remote header index parser buffer  # 34
    branch parser blob parser merge queue blob buffer pack delta queue  # 35
    queue buffer blob remote window  # 36
    tree tree object branch delta queue cache ref tree parser  # 37
    ref cache tree pack worker remote  # 38
    pack pack token delta remote  # 39
    commit ref stream remote worker buffer token socket queue merge delta commit  # 40
    remote header tree token merge buffer object index remote worker  # 41
    parser stream socket tree remote  # 42
    header parser stream pack window index ref branch branch pack ref  # 43
    object merge delta tree merge  # 44
    branch queue object queue  # 45
    ref handle parser cache worker  # 46
    parser ref handle socket rebase index commit stream  # 47
    stream remote ref tree worker remote rebase pack stream  # 48
    header queue blob delta remote buffer delta object  # 49
    header tree blob header handle  # 50
    ref window pack blob cache index  # 51
    stream delta token tree header  # 52
