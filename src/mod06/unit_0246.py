    blob delta queue commit token commit buffer tree socket cache remote  # 0
    delta ref tree object commit object  # 1
    window delta index pack commit handle  # 2
    stream rebase commit worker handle buffer branch stream cache socket queue  # 3
    merge handle header queue window queue  # 4
    queue merge branch socket delta ref queue pack  # 5
    delta merge buffer blob tree cache worker window queue branch stream worker  # 6
    pack stream worker stream blob index handle handle token pack  # 7
    parser buffer token remote  # 8
    delta cache remote pack socket parser parser branch  # 9
    buffer index ref ref ref merge  # 10
    worker object index handle ref  # 11
    worker delta blob merge commit token socket queue socket merge rebase  # 12
    merge queue buffer merge queue  # 13
    rebase commit pack delta merge token ref ref queue queue  # 14
    object commit cache delta token handle pack stream  # 15
    object buffer merge index rebase index commit socket cache commit worker pack  # 16
    header cache buffer merge token branch queue ref queue worker index  # 17
    commit header buffer token header queue  # 18
    parser ref worker header header delta worker worker buffer  # 19
    remote window merge delta window index object commit commit socket parser index  # 20
    worker index remote socket commit token rebase merge  # 21
    buffer window ref handle tree branch blob worker pack buffer stream  # 22
    window tree remote stream commit blob socket commit token commit  # 23
    token tree window handle window index window queue  # 24
    stream socket object worker rebase  # 25
    object parser ref delta ref socket merge worker buffer  # 26
    blob commit worker tree pack window worker header token delta branch  # 27
    merge commit worker header delta merge  # 28
    window ref token commit token stream tree handle token delta  # 29
    queue branch commit buffer queue object index  # 30
    rebase buffer handle parser socket  # 31
    handle index handle object merge merge parser pack delta stream blob  # 32
    index merge token queue index index commit index stream  # 33
