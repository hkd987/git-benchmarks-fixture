    ref rebase queue worker  # 0
    stream remote branch token token  # 1
    buffer index ref ref queue merge worker socket branch  # 2
    buffer remote object buffer handle socket object stream commit worker ref  # 3
    stream rebase stream socket tree handle parser branch handle  # 4
    buffer branch parser window stream token handle header  # 5
    object stream rebase handle token  # 6
    delta object queue merge rebase header cache buffer stream stream merge  # 7
    header queue worker tree cache header handle worker  # 8
    branch worker rebase buffer delta socket worker  # 9
    commit branch blob commit worker socket header  # 10
    stream merge ref commit delta branch  # 11
    socket branch index buffer merge parser buffer pack parser buffer cache buffer  # 12
    object tree delta worker queue handle buffer header stream  # 13
    socket object window merge buffer commit blob ref merge  # 14
    pack branch blob blob worker ref delta queue  # 15
    handle worker branch queue merge  # 16
    handle rebase cache cache handle tree header tree worker ref header  # 17
    delta cache queue commit merge window handle ref socket  # 18
    tree handle handle remote cache buffer pack  # 19
    commit blob branch tree  # 20
    object worker buffer branch blob handle  # 21
    branch window merge stream branch cache header object remote queue header object  # 22
    pack blob token socket tree object worker  # 23
    ref ref ref worker  # 24
    branch stream stream window delta  # 25
    socket branch queue rebase  # 26
    stream ref index object merge worker commit buffer buffer branch  # 27
    buffer parser socket handle parser header queue token  # 28
    worker branch tree commit worker  # 29
    pack handle worker index commit rebase branch pack parser  # 30
    ref window object handle remote cache tree delta merge window delta  # 31
    parser handle cache rebase buffer cache  # 32
    index stream ref pack delta  # 33
    stream blob stream object window index socket pack cache handle  # 34
    branch stream worker header rebase handle queue rebase remote merge blob cache  # 35
    object queue queue worker socket pack pack tree rebase branch pack delta  # 36
    remote window commit header merge commit  # 37
    parser stream buffer handle ref ref socket rebase cache buffer merge  # 38
    parser cache header token queue  # 39
    index worker handle branch buffer blob header cache blob  # 40
    remote blob tree remote  # 41
    commit pack socket ref branch cache  # 42
    window tree queue parser  # 43
    index commit rebase remote handle token pack commit  # 44
    handle remote rebase tree branch window blob delta tree  # 45
    queue blob token tree remote object buffer remote branch rebase  # 46
    token merge index ref object  # 47
    token ref buffer socket handle remote rebase blob object pack  # 48
    rebase rebase tree tree blob remote pack blob  # 49
    queue rebase remote index index  # 50
    cache tree buffer worker  # 51
    pack index token blob cache handle rebase rebase worker  # 52
    object blob branch remote cache stream handle tree rebase object  # 53
