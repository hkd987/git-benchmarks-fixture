    blob blob branch stream  # 0
    queue buffer cache parser merge index socket object  # 1
    branch header blob stream header window worker object window commit  # 2
    pack remote socket socket handle token socket cache object window  # 3
    rebase header object pack tree handle branch commit merge  # 4
    window ref remote handle  # 5
    ref header cache handle worker worker blob  # 6
    buffer cache blob cache socket worker header ref pack worker object buffer  # 7
    buffer branch ref rebase handle object blob window index remote blob  # 8
    pack buffer index ref index token commit token remote window delta remote  # 9
    queue tree parser index commit worker queue stream  # 10
    index handle window pack branch  # 11
    remote index queue stream handle cache cache  # 12
    branch index worker blob  # 13
    buffer handle commit ref stream  # 14
    rebase ref window worker merge  # 15
    index rebase pack stream stream branch index rebase queue cache  # 16
    handle rebase delta rebase blob buffer  # 17
    remote handle queue parser object index socket merge worker worker  # 18
    pack tree cache parser tree tree ref queue queue parser cache  # 19
    window object queue remote window buffer  # 20
    delta index index object handle  # 21
    rebase queue tree worker parser object handle  # 22
    index cache ref delta merge handle commit merge tree  # 23
    blob rebase ref ref buffer ref queue  # 24
    ref buffer ref token parser object delta handle header rebase token remote  # 25
    merge remote ref buffer merge rebase  # 26
    handle merge branch stream merge blob handle stream window merge  # 27
    buffer pack window rebase rebase remote  # 28
    pack parser buffer queue header  # 29
    queue ref worker window remote  # 30
    socket header ref window socket  # 31
    header parser parser pack queue index  # 32
    delta token stream commit pack delta  # 33
    merge buffer delta queue window stream window buffer delta queue  # 34
    socket blob branch token object blob token window  # 35
    commit stream branch remote socket commit  # 36
    delta buffer queue header worker  # 37
    merge worker commit worker cache stream rebase merge rebase commit  # 38
    tree delta socket rebase delta buffer index ref buffer  # 39
    socket merge pack header queue queue header pack  # 40
