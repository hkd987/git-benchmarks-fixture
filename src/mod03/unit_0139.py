    ref socket handle header  # 0
    socket buffer pack stream ref tree ref blob window handle  # 1
    branch ref window token pack remote tree commit header  # 2
    ref queue token remote  # 3
    queue handle worker stream branch branch parser index object stream rebase branch  # 4
    socket header delta worker ref object  # 5
    parser branch branch object handle pack tree socket delta socket  # 6
    commit object header ref parser buffer handle object  # 7
    worker ref index index commit worker header cache socket  # 8
    tree queue queue object  # 9
    worker handle token header ref window ref worker buffer  # 10
    queue handle delta rebase  # 11
    object cache merge remote handle  # 12
    window tree tree branch ref object pack remote  # 13
    merge socket handle parser socket ref header socket socket delta remote  # 14
    object window pack worker rebase token index blob blob cache worker worker  # 15
    window window token cache blob handle branch handle  # 16
    index buffer ref parser  # 17
    stream merge cache delta worker  # 18
    stream pack object ref object stream blob socket  # 19
    tree token header remote handle object socket tree  # 20
    commit window cache queue socket index  # 21
    index window rebase queue queue queue header handle  # 22
    rebase remote buffer cache worker buffer  # 23
    tree index branch delta blob header delta merge token socket  # 24
    buffer handle header worker stream ref pack commit token worker  # 25
    index token stream blob delta stream  # 26
    tree delta cache object blob handle cache queue blob rebase  # 27
    blob object ref branch window token ref buffer blob window buffer parser  # 28
    remote stream cache remote parser queue  # 29
    token worker branch pack handle buffer merge rebase  # 30
    merge rebase window handle pack handle handle handle  # 31
    handle worker rebase window token commit delta  # 32
    buffer buffer blob delta token parser window parser buffer  # 33
    cache token branch merge cache pack  # 34
    queue buffer window buffer index  # 35
    tree window handle window pack commit buffer tree queue worker index delta  # 36
    object cache merge socket parser ref socket object branch parser  # 37
    socket socket stream delta cache object rebase queue stream branch stream window  # 38
    rebase queue delta object object parser pack header pack header  # 39
    branch merge buffer rebase buffer worker  # 40
    cache blob header delta pack commit parser  # 41
    token remote parser pack index object  # 42
    handle blob cache buffer pack  # 43
    object tree token merge delta socket  # 44
    buffer parser cache remote commit remote stream buffer queue delta  # 45
    handle commit ref pack pack pack branch commit buffer merge rebase  # 46
    window rebase handle blob window queue  # 47
    merge parser window tree  # 48
    buffer socket merge cache delta object remote  # 49
    window worker header pack queue  # 50
    pack window rebase rebase ref merge ref merge index parser remote rebase  # 51
    delta parser commit blob token merge queue buffer cache worker  # 52
    blob tree stream cache tree  # 53
    commit stream ref worker header stream object index parser worker  # 54
    handle pack header remote socket  # 55
    pack index queue delta parser header  # 56
    token object branch socket cache  # 57
    parser stream index stream pack handle queue socket window  # 58
    parser blob blob pack merge commit index queue token buffer  # 59
    parser parser handle cache header  # 60
    blob merge index window merge branch buffer remote socket pack pack blob  # 61
    blob handle window header stream ref tree stream branch queue  # 62
    socket rebase index ref object rebase worker rebase merge header commit merge  # 63
    stream parser stream parser queue window branch  # 64
    tree handle token ref delta queue branch merge blob commit parser  # 65
    pack object handle branch pack pack token token queue pack blob window  # 66
    rebase window handle window  # 67
    window pack buffer handle remote branch merge remote  # 68
    socket buffer delta ref rebase queue worker  # 69
    parser branch stream worker handle window pack merge remote buffer window parser  # 70
    tree worker queue delta cache  # 71
    rebase index commit socket handle rebase queue pack pack index  # 72
    worker stream index socket merge merge commit queue  # 73
    handle parser blob stream parser commit socket buffer  # 0
    buffer tree token handle  # 1
    object cache index index queue ref  # 2
    object parser buffer rebase  # 3
    object rebase object index queue socket object token handle socket  # 4
    buffer delta parser buffer object branch header remote cache stream  # 5
    remote parser ref index socket remote stream commit token  # 6
    window branch worker handle  # 7
    token socket blob queue tree stream blob index index token cache pack  # 8
    stream blob branch header stream delta cache blob tree window queue  # 9
    tree worker commit rebase remote commit blob  # 0
    stream window pack handle commit worker queue blob window socket index cache  # 1
    handle token object blob remote rebase  # 2
    pack delta window cache stream worker  # 3
    blob commit queue token merge index ref index  # 4
