    cache commit remote handle index delta blob ref token parser pack  # 0
    tree blob cache buffer blob  # 1
    header rebase branch pack cache commit ref tree delta cache parser commit  # 2
    merge socket token merge header branch tree token buffer merge  # 3
    rebase worker merge ref  # 4
    tree buffer buffer pack blob worker  # 5
    commit stream queue header  # 6
    object handle cache window stream object pack  # 7
    handle queue object socket  # 8
    blob delta merge object blob pack cache ref  # 9
    stream rebase handle tree merge header tree branch header index handle socket  # 10
    buffer delta window stream parser stream stream branch  # 11
    blob token branch token object window socket handle worker stream delta socket  # 12
    worker socket pack index socket  # 13
    stream cache index branch  # 14
    token rebase socket header header object object branch branch rebase  # 15
    worker commit ref cache object  # 16
    token token header tree  # 17
    header rebase blob token socket handle pack buffer header  # 18
    stream tree header branch worker tree  # 19
    socket token cache index pack branch parser queue object rebase  # 20
    buffer object merge object token blob index parser socket socket ref  # 21
    blob commit rebase delta token commit stream queue  # 22
    parser tree pack pack remote socket parser window window branch branch socket  # 23
    tree index rebase pack index  # 24
    merge stream queue stream parser ref delta parser parser tree delta  # 25
    socket ref parser buffer buffer ref commit  # 26
    index worker token window delta blob  # 27
    ref pack pack stream token worker buffer token queue branch window socket  # 28
    object handle tree buffer  # 29
    header window branch blob ref blob token ref blob  # 30
    queue window token rebase pack merge object  # 31
    ref handle header commit index parser socket  # 32
    parser worker commit tree object delta tree rebase buffer  # 33
    parser worker tree object  # 34
    buffer object index socket parser rebase index branch  # 35
    cache commit worker queue header worker  # 36
    tree stream worker stream socket rebase buffer  # 37
    rebase blob socket tree commit merge queue buffer pack  # 38
    index pack worker handle commit blob  # 39
    token window queue delta window  # 40
    parser merge merge ref ref parser header delta parser socket parser commit  # 41
