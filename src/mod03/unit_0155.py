    commit tree commit delta rebase pack commit ref queue index parser  # 0
    pack worker ref socket blob blob window token object queue  # 1
    token worker stream cache remote token blob token parser handle  # 2
    tree token blob stream delta cache token  # 3
    parser object tree branch object  # 4
    socket queue rebase commit pack  # 5
    cache object delta branch window rebase buffer header cache blob handle  # 6
    window tree buffer remote queue delta buffer  # 7
    parser window cache remote ref queue  # 8
    stream parser token parser ref commit parser tree ref pack  # 9
    header handle remote branch delta object handle parser  # 10
    buffer cache object token parser object tree  # 11
    pack object socket merge commit stream window commit window cache remote delta  # 12
    object rebase delta branch commit merge index  # 13
    worker socket queue pack rebase commit object  # 14
    socket worker cache commit pack remote handle window ref  # 15
    parser merge socket parser  # 16
    index stream object stream buffer socket blob remote rebase index  # 17
    worker commit ref ref index buffer rebase commit  # 18
    queue stream ref delta token branch remote  # 19
    cache pack token stream stream socket handle window window buffer delta merge  # 20
    commit worker rebase merge parser header handle blob  # 21
    handle remote worker queue commit queue rebase  # 22
    socket socket stream worker  # 23
    queue buffer commit worker  # 24
    handle ref ref token object commit  # 25
    object ref blob queue stream blob merge object  # 26
    tree delta blob remote rebase worker delta object socket worker  # 27
    stream cache window cache window handle  # 28
    stream header remote index socket header token delta parser branch object parser  # 29
    object parser merge token rebase commit branch index buffer  # 30
    tree worker queue stream tree object  # 31
    stream tree parser pack parser buffer pack ref  # 32
    delta pack queue merge index branch  # 33
    branch worker handle handle pack socket parser delta token queue remote  # 34
    branch merge window index commit handle header buffer pack cache  # 35
    commit parser window blob merge commit ref  # 36
    socket blob remote token object token branch object ref handle index  # 37
    pack object cache rebase ref header buffer rebase  # 38
    socket tree cache buffer merge  # 39
    socket handle socket branch branch token handle socket rebase  # 40
    index header rebase index pack  # 41
    worker merge pack object socket  # 42
    pack socket socket tree branch  # 43
    rebase cache object object delta branch  # 44
    tree remote stream delta token  # 45
    worker stream blob queue rebase stream  # 46
    header index object queue branch pack window  # 47
    cache queue queue handle  # 48
    window remote cache token queue header delta header commit remote  # 49
    pack pack queue token delta cache  # 50
    commit blob buffer buffer ref parser worker header buffer delta  # 51
    delta delta worker header queue index remote index branch index rebase  # 52
    tree cache ref branch  # 53
    stream merge pack parser cache tree worker  # 54
    blob remote tree index handle  # 55
    branch remote token worker delta delta blob queue socket ref  # 56
    commit window cache socket buffer pack queue  # 57
    parser blob queue worker blob ref  # 58
    remote pack cache index queue merge handle parser queue  # 59
    cache parser pack merge pack ref remote socket queue commit buffer parser  # 60
    stream object blob window header stream remote  # 61
    pack commit parser buffer object socket cache  # 62
    header remote tree cache queue cache pack cache  # 63
    socket queue rebase commit queue delta window socket parser queue  # 64
    parser rebase ref cache  # 65
    stream socket parser commit index commit remote worker header index merge  # 66
    branch delta blob ref socket index index stream pack merge object  # 67
    buffer token remote object blob buffer commit remote delta object  # 68
    tree cache branch pack remote index handle index  # 69
    merge commit header ref stream buffer stream branch stream ref  # 70
