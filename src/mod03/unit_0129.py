    stream branch queue handle remote buffer ref parser remote  # 0
    object delta header rebase stream delta socket rebase token token  # 1
    ref header blob remote pack ref remote  # 2
    cache ref socket commit  # 3
    rebase stream commit index stream worker buffer parser index socket  # 4
    delta worker commit parser delta index remote  # 5
    cache ref pack merge remote queue ref stream  # 6
    header merge delta cache blob buffer tree parser tree cache stream  # 7
    token queue worker pack merge remote blob parser buffer delta  # 8
    parser buffer worker cache tree ref worker object header branch parser  # 9
    commit remote index handle blob object tree stream worker queue  # 10
    window token parser cache index stream parser object  # 11
    tree ref ref stream buffer header object  # 12
    window handle commit token window remote index buffer rebase commit tree index  # 13
    handle queue commit parser index header remote  # 14
    blob cache worker merge object stream  # 15
    header pack worker branch worker header merge window  # 16
    blob rebase branch socket object  # 17
    header queue object object stream  # 18
    branch buffer handle socket cache cache stream header object handle  # 19
    worker stream remote remote branch delta  # 20
    window worker socket blob pack  # 21
    pack tree parser worker window handle delta branch  # 22
    index object remote delta stream remote branch  # 23
    commit rebase pack object ref cache  # 24
    queue window blob tree remote rebase queue  # 25
    header token remote queue worker delta  # 26
    object remote rebase remote merge  # 27
    blob remote remote blob  # 28
    token delta parser queue worker pack  # 29
    stream branch blob handle rebase delta handle remote branch socket delta  # 30
    pack pack commit cache cache remote pack rebase index parser  # 31
    cache blob socket ref blob cache index merge parser rebase merge  # 32
    stream merge token commit socket buffer queue worker remote stream cache  # 33
    cache worker branch ref window object cache tree buffer stream  # 34
    stream remote cache delta pack delta parser  # 35
    parser remote buffer tree blob  # 36
    socket tree socket merge cache ref worker cache  # 37
    window token pack tree token commit index token commit rebase header branch  # 38
    worker remote stream remote  # 39
    cache stream window worker  # 40
    ref pack delta rebase branch  # 41
    rebase token index object blob merge pack socket commit worker  # 42
    pack window remote socket token queue merge token  # 43
    delta window pack token queue delta stream  # 44
    socket buffer commit object  # 45
    stream object buffer blob ref socket index object pack  # 46
    branch stream ref worker commit worker ref branch parser object parser worker  # 47
    rebase tree cache branch tree  # 48
    parser header tree pack index  # 49
    commit remote token remote cache commit merge buffer remote queue object  # 50
    pack blob remote socket handle window object parser window branch worker pack  # 51
    blob token buffer window worker handle remote pack blob  # 52
    buffer ref delta tree remote socket commit  # 53
    token buffer header delta queue index  # 54
    stream window object commit header handle socket commit delta cache header object  # 55
    index commit commit stream index  # 56
    delta queue socket handle rebase merge  # 57
    stream buffer branch rebase buffer commit handle  # 58
    blob window rebase blob  # 59
    remote cache merge pack  # 0
    blob queue cache handle rebase commit handle  # 1
    object delta handle queue index parser commit window branch stream index  # 2
    token index object stream tree rebase delta header pack window  # 3
    handle queue remote buffer worker stream header worker token  # 4
    stream branch parser cache tree window worker object cache tree  # 5
    commit parser delta token rebase object window rebase window rebase  # 6
    remote header stream rebase parser  # 7
    ref object window remote branch rebase worker stream  # 8
    merge cache cache buffer blob pack header tree branch stream worker  # 9
    buffer pack parser branch tree header header worker socket header merge remote  # 10
    tree branch ref cache  # 11
