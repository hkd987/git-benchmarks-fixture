    buffer header blob ref window commit  # 0
    stream object handle token ref  # 1
    pack object remote buffer parser branch header ref  # 2
    window worker tree branch delta index token  # 3
    ref header queue rebase header  # 4
    buffer object branch ref merge worker merge window remote worker delta  # 5
    socket tree tree cache rebase rebase buffer  # 6
    commit buffer index buffer handle window merge  # 7
    handle pack rebase queue token  # 8
    rebase queue token remote window header stream  # 9
    handle queue queue handle stream socket merge ref handle delta  # 10
    object delta stream cache socket merge socket index object  # 11
    tree merge buffer rebase index handle merge window cache  # 12
    pack cache socket socket token handle queue  # 13
    buffer token stream pack socket stream  # 14
    window pack remote token ref window  # 15
    cache commit ref blob cache delta window queue parser merge  # 16
    ref rebase index rebase socket tree object queue token cache stream  # 17
    cache index window ref worker window handle token delta rebase remote  # 18
    index ref branch branch buffer rebase stream pack stream branch cache  # 19
    socket merge merge tree worker merge branch parser branch branch blob socket  # 20
    handle delta pack index socket blob remote blob  # 21
    commit pack delta pack merge index cache commit handle  # 22
    parser ref handle stream rebase worker delta merge branch stream ref stream  # 23
    tree pack commit token pack window remote  # 24
    merge tree commit index buffer  # 25
    ref buffer handle cache pack stream blob merge stream commit merge merge  # 26
    blob delta token token handle  # 27
    tree queue cache index object rebase blob ref handle  # 28
    commit window cache branch object branch  # 29
    window remote handle branch object delta stream stream worker remote remote  # 30
    token parser header blob worker header stream queue queue merge  # 31
    branch header token buffer tree window cache queue index worker merge blob  # 32
    tree remote stream branch token buffer remote handle socket  # 33
    token handle queue merge socket tree ref remote  # 34
    blob index merge queue tree remote window delta cache window rebase  # 35
    window stream worker header ref delta object  # 36
    window socket socket queue  # 37
    handle pack token commit tree window queue branch buffer delta merge blob  # 38
    buffer queue pack buffer token worker remote  # 39
    branch remote pack worker remote branch  # 40
    socket commit pack handle stream index window merge merge cache worker branch  # 41
    header worker stream commit cache ref cache delta blob branch pack blob  # 42
    blob worker worker ref pack parser index handle handle header  # 43
    index commit ref merge handle buffer remote  # 44
    handle object token index  # 45
    commit token stream socket tree token buffer buffer buffer  # 46
    worker commit remote window window  # 47
    object rebase worker rebase socket header commit buffer  # 48
    merge worker stream index queue blob index object worker  # 49
    pack commit stream delta  # 50
    blob merge handle socket branch branch  # 51
    worker buffer index worker stream  # 52
    ref worker header index delta blob tree  # 53
    index header remote buffer window index window header  # 54
    object pack queue delta token pack worker rebase window header  # 55
    branch index object remote pack queue index delta commit queue  # 56
    handle index delta branch header handle blob queue  # 57
    commit pack blob tree socket queue merge index  # 58
    ref rebase ref branch  # 59
    buffer delta handle window worker header tree remote  # 60
    delta blob queue queue object remote buffer  # 61
    delta header handle delta merge commit tree tree stream index branch buffer  # 62
    cache rebase queue tree  # 63
    merge index merge index queue  # 64
    tree remote index ref merge worker window worker cache blob  # 65
    queue worker worker worker rebase branch header header cache  # 66
    pack stream cache handle pack index  # 67
    delta remote queue queue cache stream handle window buffer buffer  # 68
    parser pack branch window commit queue queue buffer pack  # 69
    pack queue parser tree handle delta worker token handle token branch  # 70
    stream pack ref token buffer buffer buffer merge index token commit  # 71
    ref socket header header stream header commit  # 72
    header queue stream delta index header  # 73
    branch window tree pack merge branch cache  # 74
    handle stream socket cache rebase index  # 75
    branch window tree cache parser branch branch merge remote commit delta commit  # 76
    merge pack ref commit  # 77
    remote pack queue rebase token pack socket buffer commit header rebase  # 0
    branch remote window delta  # 1
    tree index commit worker handle cache buffer branch stream worker  # 2
    commit handle index pack delta ref remote queue  # 3
    pack window ref worker  # 4
    delta buffer socket delta merge branch window handle tree  # 5
    remote window socket handle branch index cache remote  # 6
    rebase merge parser stream merge ref commit queue handle index  # 7
    parser object rebase token index worker blob header object branch commit  # 8
    cache blob socket branch index  # 0
    tree tree index index window handle worker ref  # 1
    buffer tree socket window blob parser socket delta object index pack  # 2
    worker blob cache socket commit remote  # 3
    ref remote tree buffer delta  # 4
    blob worker delta blob  # 5
    merge branch queue merge remote delta header merge cache socket socket  # 6
