    parser branch pack parser worker worker remote socket token stream commit token  # 0
    merge queue buffer pack  # 1
    delta cache worker worker stream blob buffer merge  # 2
    cache branch token pack handle handle  # 3
    commit merge socket cache  # 4
    pack branch cache window buffer parser branch parser window blob  # 5
    window window rebase index parser commit merge  # 6
    cache index queue socket buffer  # 7
    index buffer socket branch buffer handle header queue blob blob delta handle  # 8
    merge header token delta handle socket tree header  # 9
    window ref worker delta tree cache buffer buffer  # 10
    stream rebase merge stream  # 11
    blob remote branch branch branch stream pack token token blob  # 12
    socket delta commit rebase rebase object object stream buffer cache stream  # 13
    stream branch index header pack  # 14
    worker window window rebase branch token commit pack cache tree  # 15
    blob stream branch queue  # 16
    handle token rebase worker  # 17
    object commit token buffer ref merge blob window  # 18
    worker ref branch delta socket handle header  # 19
    queue rebase tree remote tree object ref queue ref  # 20
    socket queue ref token header merge blob  # 21
    handle tree worker tree rebase pack tree  # 22
    object cache cache rebase pack cache socket index socket  # 23
    object pack tree blob parser tree window  # 24
    worker window stream merge rebase rebase  # 25
    object tree queue window token cache parser queue remote  # 26
    ref handle delta parser worker  # 27
    index header tree blob commit  # 28
    pack parser delta header window  # 29
    rebase header header parser  # 30
    header stream window rebase socket worker stream  # 31
    stream delta object cache object pack tree  # 32
    commit commit handle window rebase pack header stream remote socket  # 33
