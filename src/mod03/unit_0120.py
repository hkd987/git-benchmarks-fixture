    rebase delta object header socket blob header remote queue delta  # 0
    delta object socket blob buffer rebase  # 1
    tree blob queue socket header blob merge pack blob token ref  # 2
    socket window remote buffer delta window stream merge rebase token stream cache  # 3
    stream window ref token blob remote remote blob  # 4
    merge queue worker rebase parser buffer cache window merge parser worker  # 5
    branch remote object queue buffer index token commit merge tree queue  # 6
    pack stream stream stream ref pack  # 7
    pack merge header header handle handle socket delta commit blob  # 8
    token cache pack header stream handle merge handle pack  # 9
    ref parser blob cache socket branch header object pack socket handle  # 10
    remote merge header cache stream buffer header remote socket  # 11
    merge tree parser worker delta ref handle branch  # 12
    window stream queue ref index delta stream buffer rebase remote  # 13
    header remote stream merge  # 14
    parser index window header  # 15
    buffer merge stream merge socket  # 16
    token token worker window merge parser  # 17
    token ref delta tree buffer queue cache  # 18
    header socket pack stream worker rebase  # 19
    cache blob cache branch worker branch parser ref header parser  # 20
    ref merge ref rebase window  # 21
    remote window remote stream handle commit buffer index  # 22
    header branch socket worker blob window index cache  # 23
    worker token branch object cache pack cache worker pack buffer commit  # 24
    socket handle delta ref merge commit branch parser ref buffer ref buffer  # 25
    worker worker ref object blob commit window handle  # 26
    token rebase remote commit parser cache remote  # 27
    ref parser tree ref commit handle worker tree header socket  # 28
    delta pack cache ref ref branch header branch blob commit pack parser  # 29
    token object delta rebase branch tree branch  # 30
    handle stream stream buffer index object socket object socket queue stream  # 31
    header ref buffer branch remote commit stream index tree  # 32
