    object token stream blob socket token queue tree branch  # 0
    pack commit handle ref  # 1
    worker handle object pack object token worker merge delta  # 2
    buffer buffer rebase worker  # 3
    commit tree merge rebase  # 4
    index queue socket tree  # 5
    pack rebase object queue remote  # 6
    pack queue commit ref rebase window stream socket delta blob socket  # 7
    delta branch socket branch merge header index header cache merge  # 8
    pack queue socket queue token buffer  # 9
    stream tree commit parser socket blob pack  # 10
    object commit rebase token merge cache  # 11
    window blob blob queue index cache token token object  # 12
    remote rebase ref pack  # 13
    window pack tree stream  # 14
    cache delta rebase commit delta queue  # 15
    pack remote parser remote  # 16
    branch parser worker window merge ref socket  # 17
    delta buffer delta header branch  # 18
    commit rebase queue delta queue ref parser handle index buffer  # 19
    handle parser worker token index buffer stream  # 20
    commit cache queue window merge handle buffer index  # 21
    worker queue token token parser tree parser object  # 22
    remote tree stream socket window delta  # 23
    pack commit cache branch parser object ref delta branch header  # 24
    window index buffer tree tree handle remote remote commit pack  # 25
    object handle worker stream  # 26
    branch header commit delta delta delta queue socket cache delta window  # 27
    delta parser token tree stream delta blob token ref handle pack  # 28
    buffer handle parser merge socket branch commit object header parser branch  # 29
    object queue delta merge stream  # 30
    cache rebase window worker index  # 31
    tree stream commit window merge parser object ref parser window merge commit  # 32
    commit merge index branch parser header  # 33
    delta remote token merge delta merge socket token merge ref header buffer  # 34
    stream remote remote buffer token  # 35
    handle remote pack handle  # 36
    ref parser token socket delta pack merge branch commit pack branch index  # 37
    header handle buffer header parser branch token  # 38
    blob pack socket index blob queue ref commit merge remote  # 39
    remote buffer handle stream  # 40
    merge parser index delta rebase queue stream  # 41
    stream window buffer socket handle  # 42
    buffer rebase worker worker  # 43
    tree index worker window buffer merge index  # 44
    cache header branch remote delta cache worker  # 45
    rebase merge token pack pack  # 46
    window blob worker queue parser worker stream remote  # 47
    branch parser delta worker rebase  # 48
    worker queue socket merge merge tree socket pack pack worker  # 49
    branch branch rebase worker remote tree worker index token  # 50
    rebase socket token stream commit tree token  # 51
    header object parser stream index buffer stream branch index  # 52
    merge index queue buffer pack  # 53
    parser token token merge rebase token commit  # 54
    delta delta index index token ref worker handle parser tree branch  # 55
    commit index parser blob cache branch cache blob pack remote delta  # 56
    delta index token commit ref tree cache window worker  # 57
    parser window ref buffer queue blob ref socket stream merge  # 58
    delta socket blob token object delta rebase parser  # 59
    token socket stream header queue branch pack pack  # 60
    worker worker stream ref parser delta commit worker socket object  # 61
    delta delta window object queue socket  # 62
    buffer buffer buffer pack handle window  # 63
    blob pack blob worker branch  # 64
    header window worker window header  # 65
    blob window header window  # 66
    queue stream commit delta pack index window object parser commit cache blob  # 67
    object remote token window worker header parser buffer header object  # 68
    handle tree token commit stream header stream header rebase header blob branch  # 69
    commit handle window delta header worker  # 70
    worker delta cache commit token worker queue buffer buffer branch token stream  # 71
    window cache commit delta branch object delta ref token ref commit  # 72
    stream commit blob worker index merge stream worker token buffer  # 73
    queue merge branch queue pack rebase tree pack worker queue buffer commit  # 74
    commit buffer parser token parser window tree commit  # 75
    tree object remote merge index rebase socket  # 76
    object tree branch delta rebase header object  # 77
    buffer token blob worker worker tree stream ref  # 78
    header header remote delta window worker window  # 79
    queue delta commit stream token socket ref index commit socket socket  # 80
    token socket rebase tree handle index commit pack pack tree delta  # 81
    blob stream object buffer pack buffer  # 82
    token pack commit delta parser parser cache delta stream object index  # 83
    delta worker window cache remote remote handle blob rebase buffer parser  # 84
    socket window object rebase commit merge ref ref  # 85
