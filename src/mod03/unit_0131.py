    remote queue index handle stream merge ref buffer queue commit delta  # 0
    cache cache token pack handle pack  # 1
    blob branch stream branch index header buffer index buffer  # 2
    window window blob socket buffer object window socket remote header header  # 3
    cache ref rebase branch queue  # 4
    tree pack token merge object stream remote pack token parser  # 5
    window blob stream token  # 6
    cache window window index handle stream rebase window window cache remote parser  # 7
    delta buffer rebase rebase rebase worker cache worker  # 8
    commit header ref object remote  # 9
    queue branch delta tree  # 10
    parser remote blob commit cache buffer socket  # 11
    merge worker cache blob merge handle  # 12
    stream header index cache index header socket remote stream socket tree  # 13
    merge cache remote stream rebase index commit buffer  # 14
    parser delta socket remote pack merge blob queue  # 15
    tree object worker cache queue remote worker parser buffer merge tree  # 16
    object parser window rebase socket pack  # 17
    commit queue remote commit commit index ref merge handle buffer  # 18
    window rebase stream queue worker queue pack  # 19
    branch handle branch parser index blob index window parser  # 20
    remote header cache merge index remote  # 21
    delta header ref remote header handle commit cache  # 22
    rebase object branch token index commit  # 23
    commit socket tree parser worker ref  # 24
    parser merge tree object  # 25
    worker commit remote token  # 26
    stream stream ref object parser window delta  # 27
    parser worker merge pack  # 28
    worker ref header commit delta delta blob object delta ref  # 29
    remote object merge header socket  # 30
    remote header buffer pack  # 31
    stream header tree ref delta buffer handle queue handle stream socket  # 32
    index handle queue handle object  # 33
    remote rebase rebase token merge socket socket object handle token socket socket  # 34
    remote merge object blob merge commit  # 35
    token socket object window tree  # 36
    worker object tree commit rebase buffer  # 37
    rebase object socket merge blob blob window remote buffer socket  # 38
    worker tree socket ref cache  # 39
    ref blob commit cache token  # 40
    window buffer parser tree merge commit remote window stream socket stream commit  # 41
    rebase rebase blob handle delta ref socket stream  # 42
    stream buffer pack index  # 43
    index worker cache tree pack stream blob token worker queue pack header  # 44
    cache blob merge branch branch handle blob cache rebase socket rebase  # 45
    remote stream token index cache  # 46
    window worker parser rebase rebase parser cache parser header branch ref rebase  # 47
