    blob remote index parser parser blob ref window pack ref  # 0
    index window rebase merge socket socket  # 1
    blob index tree index merge queue branch  # 2
    index window tree merge remote  # 3
    stream branch ref window stream handle delta  # 4
    pack buffer token socket  # 5
    ref tree pack socket parser delta buffer stream blob object stream index  # 6
    index pack blob tree commit socket remote buffer commit cache  # 7
    window ref parser stream window rebase commit blob merge  # 8
    branch queue merge commit tree window remote  # 9
    token window pack parser queue  # 10
    object socket remote remote remote tree blob merge tree blob handle  # 11
    blob commit pack index socket rebase  # 12
    merge delta tree delta handle stream token tree queue parser  # 13
    handle branch worker parser token merge  # 14
    object stream queue cache object index object pack worker blob socket  # 15
    object socket blob parser remote ref  # 16
    parser tree rebase pack parser cache  # 17
    socket window merge header commit blob index remote window  # 18
    buffer delta delta worker index merge window  # 19
    index pack commit handle buffer delta blob parser window blob ref token  # 20
    parser header worker socket pack buffer branch header  # 21
    branch blob token commit branch tree token commit  # 22
    merge index merge tree commit header token stream blob delta stream  # 23
    header pack worker commit ref worker header branch queue  # 24
    index delta blob token blob commit stream remote merge socket  # 25
    buffer buffer commit pack rebase handle tree  # 26
    handle socket tree token delta delta merge remote index blob  # 27
    worker delta index branch window window merge rebase rebase ref merge pack  # 28
    tree delta pack commit worker worker branch index remote buffer blob header  # 29
    commit worker commit token buffer ref branch object index ref queue  # 30
    cache header handle branch object  # 31
    commit tree stream blob  # 32
    blob blob branch header pack branch parser  # 33
    buffer tree socket blob parser stream  # 34
    object handle rebase commit socket  # 35
    queue blob branch header ref tree buffer queue window branch token  # 36
    remote tree branch buffer buffer window queue blob tree  # 37
    stream commit remote cache blob worker index  # 38
    window index handle ref socket ref parser  # 39
    tree tree header tree buffer  # 40
    merge buffer blob merge token rebase rebase window object window commit header  # 41
    object cache blob token parser  # 42
    socket pack parser index cache token commit commit remote blob blob header  # 43
    remote remote cache delta window  # 44
    object buffer header socket queue buffer  # 45
    delta commit remote handle remote ref token  # 46
    worker buffer parser cache queue  # 47
    blob merge queue merge  # 48
    queue buffer object window handle remote  # 49
    worker branch delta pack rebase header cache  # 50
    token index buffer blob pack commit worker handle object pack  # 51
    handle rebase merge queue window merge pack object remote pack window blob  # 52
    worker tree remote remote branch worker  # 53
    delta handle header stream remote stream blob delta remote pack header  # 54
    buffer worker socket branch token pack commit remote window queue window socket  # 55
    token parser parser branch cache blob  # 56
    merge commit commit object rebase  # 57
    remote handle cache cache delta cache token branch cache tree ref pack  # 58
    header rebase queue delta stream  # 59
    worker blob tree stream ref  # 0
    handle buffer cache object rebase  # 1
    object window rebase token stream index index rebase stream  # 2
    parser commit merge token header index handle rebase merge  # 3
    remote token socket delta window tree merge rebase  # 4
    delta header merge remote  # 5
