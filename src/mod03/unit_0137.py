    buffer header handle worker socket handle  # 0
    token window header worker  # 1
    rebase rebase pack merge delta header cache buffer handle  # 2
    token pack queue stream  # 3
    parser rebase queue stream  # 4
    cache worker object stream window queue ref ref object ref stream socket  # 5
    commit socket parser pack socket socket stream queue rebase parser pack  # 6
    branch socket header buffer header parser  # 7
    rebase handle commit delta pack  # 8
    stream worker blob delta merge branch  # 9
    buffer index blob queue token parser pack remote remote delta branch remote  # 10
    merge buffer stream parser buffer blob index token rebase rebase handle ref  # 11
    socket parser pack tree stream worker ref ref worker  # 12
    buffer delta queue branch queue delta cache rebase stream commit  # 13
    token header branch commit token buffer tree merge rebase object rebase  # 14
    handle socket delta stream merge  # 15
    object worker pack pack remote tree merge blob handle ref branch  # 16
    buffer commit blob remote pack ref buffer window  # 17
    object remote handle object queue delta rebase  # 18
    delta remote ref pack merge tree rebase  # 19
    cache merge tree parser header tree stream ref tree  # 20
    merge index stream token object worker queue buffer parser buffer worker stream  # 21
    worker buffer branch branch merge buffer tree header parser cache ref  # 22
    blob object token blob cache commit queue object remote ref merge  # 23
    branch ref delta cache header  # 24
    blob ref window token worker handle  # 25
    token cache buffer delta  # 26
    buffer pack header ref commit stream worker socket stream header token  # 27
    index delta remote pack window rebase  # 28
    pack window buffer index worker delta token index commit  # 29
    cache blob stream merge buffer index parser commit stream remote buffer tree  # 30
    remote pack merge branch pack socket tree remote rebase buffer  # 31
    parser socket commit branch blob remote window  # 32
    tree queue tree worker window cache remote object  # 33
    parser queue delta rebase socket branch delta pack rebase branch  # 34
    rebase parser queue branch branch tree tree  # 35
    branch remote pack worker rebase branch delta socket object  # 36
    handle blob handle handle  # 37
    stream token worker header object token socket rebase ref stream remote  # 38
    handle blob cache token  # 39
