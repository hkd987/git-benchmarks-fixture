    handle commit token ref stream parser handle branch  # 0
    queue ref object branch worker remote merge  # 1
    window merge object object  # 2
    worker worker worker cache ref buffer rebase commit  # 3
    tree rebase pack tree cache object  # 4
    delta buffer buffer stream branch rebase buffer pack window  # 5
    window pack handle header blob cache commit commit socket  # 6
    buffer blob delta queue rebase tree socket merge ref window parser header  # 7
    index cache branch queue header queue socket parser object  # 8
    queue commit blob window window queue cache  # 9
    worker socket parser ref header commit pack delta delta pack  # 10
    header delta object token object parser  # 11
    rebase handle delta socket blob buffer index  # 12
    ref tree merge header buffer object queue ref delta delta  # 13
    commit branch cache token commit merge stream queue tree  # 14
    branch index tree cache header  # 15
    window worker object handle rebase stream handle branch header  # 16
    ref ref delta stream token window window handle blob stream branch  # 17
    header branch commit blob  # 18
    ref pack rebase branch  # 19
    handle worker blob handle  # 20
    branch blob rebase queue object ref ref ref header merge tree tree  # 21
    stream rebase object merge  # 22
    ref remote index buffer pack blob stream header stream  # 23
    window header merge handle remote pack branch pack ref handle object branch  # 24
    remote tree parser token cache remote  # 25
    socket ref blob delta window queue cache  # 26
    token delta token buffer rebase index worker cache socket  # 27
    index window branch commit branch index stream  # 28
    stream commit tree parser  # 29
    remote socket buffer window queue queue parser merge header  # 30
    window cache object delta header  # 31
    object index cache socket ref handle parser cache queue handle  # 32
    cache commit pack branch rebase  # 33
    blob socket header queue  # 34
    cache header branch branch index header delta  # 35
    queue merge handle socket handle  # 36
    stream queue worker buffer cache remote  # 37
    handle delta branch ref handle remote rebase cache branch buffer merge merge  # 38
    branch remote token worker socket buffer  # 39
    queue worker rebase parser object merge merge merge buffer rebase branch queue  # 40
    cache worker tree stream index worker parser cache stream object  # 41
    window header tree cache cache token buffer delta cache object merge queue  # 42
    window parser object merge delta commit branch pack rebase cache  # 43
    merge ref token object window cache blob tree  # 44
    pack queue parser delta buffer parser window cache worker window  # 45
    index pack branch worker stream worker blob buffer  # 46
    rebase worker stream delta socket token pack socket header  # 47
    token stream rebase object window buffer tree worker blob commit ref blob  # 48
    object ref queue commit parser commit pack rebase branch buffer  # 49
    branch window window handle index  # 50
    index token buffer tree object blob branch worker buffer merge ref  # 51
    token queue delta commit  # 52
    blob handle token delta remote commit branch delta  # 53
    stream rebase worker worker stream cache parser object queue ref token cache  # 0
    pack socket worker blob commit queue ref cache  # 1
    remote branch remote pack window index  # 2
    queue ref worker ref queue ref rebase index handle tree ref  # 3
    object index commit queue tree worker commit worker  # 4
    stream header remote queue branch window index ref stream remote index remote  # 5
    token pack delta branch blob stream object token socket remote remote tree  # 6
    socket ref rebase remote branch worker  # 7
    buffer blob blob worker branch queue object token parser window  # 8
    merge header window rebase merge  # 9
    handle blob blob object token tree blob object  # 10
