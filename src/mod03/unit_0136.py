    pack index queue buffer object queue merge rebase  # 0
    rebase ref queue buffer parser index rebase pack branch  # 1
    window parser index worker window header delta  # 2
    blob socket delta handle index worker  # 3
    merge queue buffer token rebase worker window  # 4
    delta window stream worker header  # 5
    socket blob merge merge handle  # 6
    window window merge cache header rebase merge rebase  # 7
    window stream commit index index remote handle index  # 8
    branch index rebase buffer ref object pack tree buffer  # 9
    delta rebase commit handle remote worker delta branch queue cache  # 10
    remote remote queue window token object index branch handle handle  # 11
    branch queue stream buffer index commit parser  # 12
    parser commit rebase token index buffer stream  # 13
    worker index commit queue blob merge  # 14
    handle merge delta delta  # 15
    parser ref buffer merge  # 16
    queue cache queue stream  # 17
    handle token ref window header  # 18
    branch merge object remote window cache parser commit buffer queue queue  # 19
    window tree object pack token  # 20
    ref stream merge merge tree merge blob header pack header  # 21
    branch cache object window worker  # 22
    branch socket object socket branch header window buffer  # 23
    worker parser handle handle buffer ref worker header tree socket cache  # 24
    branch handle socket ref buffer parser  # 25
    buffer branch rebase stream window remote  # 26
    handle token ref cache  # 27
    delta merge commit stream merge blob blob commit  # 28
    delta token blob queue pack commit  # 29
    pack token remote header rebase merge tree  # 30
    merge token header buffer stream blob stream blob window merge rebase queue  # 31
    worker remote rebase pack remote header pack  # 32
    commit worker queue socket buffer handle handle worker worker queue token merge  # 33
    merge tree handle object socket merge tree buffer window buffer  # 34
    object tree ref token branch buffer worker token index ref header pack  # 35
    pack object socket index branch header handle parser rebase parser  # 36
    object window buffer handle commit worker index delta stream ref commit remote  # 37
    remote remote stream rebase delta rebase header worker tree  # 38
    ref object blob header  # 39
    delta tree object buffer object merge parser  # 40
    header stream delta cache window cache socket worker delta delta  # 41
    handle delta worker tree branch handle delta ref pack ref  # 42
    branch merge parser blob handle merge buffer  # 43
    stream object object tree merge buffer index merge remote commit cache object  # 44
    index tree remote object handle tree stream commit worker ref header queue  # 45
    window commit object parser window token socket cache header parser remote  # 46
    window tree branch merge queue merge buffer remote pack  # 47
    pack merge header ref token commit parser parser queue  # 48
    cache window pack pack socket worker cache rebase rebase token pack  # 49
