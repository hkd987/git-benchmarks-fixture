    pack delta window ref stream header header socket parser  # 0
    merge parser buffer remote window index merge handle blob ref buffer  # 1
    merge window buffer blob blob buffer stream stream index  # 2
    stream branch pack delta worker pack stream pack buffer window  # 3
    header parser index delta commit queue remote commit delta rebase branch window  # 4
    cache commit pack object window merge  # 5
    tree commit rebase pack ref merge rebase  # 6
    window pack handle pack stream socket parser branch queue  # 7
    commit token tree queue  # 8
    parser index handle cache buffer branch branch rebase tree  # 9
    blob queue cache object rebase delta remote cache worker  # 10
    window merge commit ref queue buffer remote tree socket blob socket tree  # 11
    ref delta buffer tree  # 12
    branch branch rebase token window worker branch header commit window queue stream  # 13
    ref remote header object branch delta worker object blob object blob  # 14
    header merge commit commit  # 15
    remote rebase branch blob buffer parser queue remote cache remote blob rebase  # 16
    parser worker remote window cache  # 17
    window ref blob rebase index blob blob  # 18
    pack window socket worker cache branch stream  # 19
    parser token parser index  # 20
    buffer token handle merge queue buffer handle queue tree token header header  # 21
    stream object object index  # 22
    delta blob pack stream worker socket worker index rebase branch  # 23
    queue rebase stream tree  # 24
    worker cache socket blob index header  # 25
    pack queue handle parser index queue worker branch buffer  # 26
    window remote header delta remote blob rebase parser ref merge buffer socket  # 27
    parser branch socket worker worker stream  # 28
    parser cache ref commit worker commit index tree blob worker  # 29
    socket blob window queue merge header window handle blob  # 30
    token tree remote tree branch buffer  # 31
    pack window delta rebase cache parser remote blob index header cache  # 32
    queue window token object worker delta  # 33
    merge merge window remote cache stream buffer ref stream cache  # 34
    merge delta window socket  # 35
    rebase tree pack worker rebase tree index delta  # 36
    handle merge remote header ref merge blob delta index ref  # 37
    delta branch rebase buffer pack socket blob  # 38
    object header object tree rebase buffer delta parser  # 39
    socket branch merge handle rebase tree stream blob handle delta handle  # 40
    ref index parser stream index index window stream  # 41
    window tree handle token delta index handle commit cache token  # 42
    remote queue queue queue merge  # 43
    parser tree token delta ref worker window commit tree  # 44
    window queue socket blob socket remote token  # 45
    remote ref delta delta socket delta index cache cache tree branch blob  # 46
    rebase window ref ref buffer tree handle buffer token  # 47
    parser index tree header ref remote queue buffer commit  # 48
    commit token window merge  # 49
    buffer rebase blob rebase handle handle ref header commit token merge queue  # 50
    index window index stream rebase object stream buffer  # 51
    merge buffer worker blob commit cache pack handle buffer tree stream stream  # 52
    queue window remote ref  # 53
    rebase tree commit socket header branch worker  # 54
    branch index commit blob buffer merge window remote index parser branch  # 55
    tree handle parser socket cache branch pack  # 56
    tree cache merge token ref token delta blob worker  # 57
    blob merge header stream handle cache merge cache  # 58
    blob token worker worker ref  # 59
    rebase worker parser buffer socket  # 60
    handle object index ref delta  # 61
    rebase merge rebase index branch cache socket  # 62
    blob index branch window queue worker cache remote tree buffer pack  # 63
    handle index window index branch handle object branch tree pack  # 64
    object merge queue merge buffer socket socket  # 65
    buffer blob header queue commit worker worker object pack socket  # 66
    delta ref buffer rebase  # 67
    parser worker token queue pack branch branch  # 68
    window window ref blob pack cache rebase  # 69
    tree rebase buffer remote queue remote stream socket worker worker socket cache  # 70
    remote token object handle parser token remote blob merge token  # 71
    rebase stream buffer ref  # 72
    window remote token index window token header  # 73
    buffer delta delta remote rebase  # 74
    buffer object token rebase window  # 75
    buffer worker cache delta  # 76
    queue parser blob object cache ref rebase handle  # 77
    ref rebase socket merge merge worker delta stream  # 78
    rebase tree merge stream  # 79
    delta header parser socket buffer token commit blob commit ref delta  # 80
    parser pack window pack  # 81
    token remote cache cache socket  # 82
    tree pack pack window blob worker branch  # 83
    buffer rebase token socket ref parser  # 84
    handle stream object delta socket delta  # 85
