    token buffer remote buffer buffer socket index blob object  # 0
    stream worker handle merge  # 1
    ref parser buffer stream cache blob queue object queue parser queue  # 2
    stream buffer window token merge pack  # 3
    commit window branch stream socket blob remote merge parser window  # 4
    queue cache index branch socket handle ref  # 5
    handle handle object index merge socket socket queue object  # 6
    rebase object window stream token token tree pack queue token  # 7
    ref object rebase header remote worker token socket stream  # 8
    handle cache pack window handle index tree  # 9
    blob worker worker ref buffer index token  # 10
    worker rebase blob branch worker  # 11
    stream branch parser merge delta blob worker object  # 12
    branch worker pack cache stream token remote rebase parser  # 13
    header socket socket cache branch merge object  # 14
    commit buffer remote pack object window pack commit merge  # 15
    branch queue socket buffer ref buffer parser  # 16
    cache socket index tree parser queue  # 17
    ref stream ref commit worker parser blob socket remote socket stream  # 18
    remote merge parser branch stream rebase header merge window remote  # 19
    handle socket handle remote tree index header blob tree token queue cache  # 20
    queue object queue tree tree remote tree branch object  # 21
    worker buffer socket remote rebase header  # 22
    parser blob delta queue socket parser pack blob  # 23
    worker merge blob merge object handle  # 24
    index object header commit header cache worker  # 25
    socket stream rebase pack header  # 26
    index parser blob commit queue blob cache socket worker  # 27
    object header window rebase parser parser  # 28
    branch object worker window object merge handle queue commit socket  # 29
    branch token socket commit object handle handle commit worker buffer blob handle  # 30
    queue pack pack window stream rebase merge parser token  # 31
    rebase parser ref tree  # 32
    commit object merge object window remote window rebase token blob remote pack  # 33
    parser buffer delta worker token delta branch ref tree blob window  # 34
    queue index handle ref header  # 0
    stream worker pack index handle rebase window buffer remote socket  # 1
    index object rebase window pack stream handle tree rebase tree worker  # 2
    stream cache ref remote token  # 3
    queue socket window window remote tree socket window branch delta blob  # 4
    cache rebase handle tree delta worker remote commit socket queue  # 5
    stream worker branch cache pack token merge  # 6
    branch blob queue handle worker ref remote parser branch  # 7
    parser tree branch handle parser queue merge token object  # 8
    socket index buffer object socket handle  # 9
    commit delta queue commit queue ref worker queue index buffer header queue  # 10
    queue blob parser handle remote buffer handle stream blob header object remote  # 11
    token delta parser queue cache queue window delta cache stream merge  # 12
    buffer rebase blob window ref handle commit ref queue index  # 0
    object delta worker branch parser  # 1
    cache stream handle delta remote pack  # 2
    branch ref commit blob stream queue merge rebase  # 3
    socket token token branch rebase stream token merge ref  # 4
    blob handle parser ref pack  # 5
    merge tree token rebase blob cache  # 6
    remote remote header pack  # 7
    remote tree queue index handle blob header commit queue cache merge blob  # 8
    token buffer merge pack parser ref rebase rebase stream rebase  # 9
    pack tree pack socket socket delta remote index object  # 10
    pack remote parser index parser socket tree stream token pack index stream  # 11
