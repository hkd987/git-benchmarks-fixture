    socket buffer index branch queue handle branch commit socket object blob parser  # 0
    queue rebase stream remote blob parser commit branch delta cache  # 1
    buffer queue rebase handle pack rebase index rebase queue  # 2
    merge blob delta object commit ref window stream  # 3
    tree merge branch index stream blob tree merge  # 4
    object queue stream token token token delta parser header header cache  # 5
    branch handle window handle worker index tree tree blob  # 6
    blob worker cache parser header cache pack token worker queue rebase commit  # 7
    blob socket parser worker  # 8
    rebase handle parser buffer ref object token  # 9
    remote ref commit ref rebase branch  # 10
    branch ref branch ref blob tree queue  # 11
    cache tree remote remote stream tree tree tree merge  # 12
    tree tree parser buffer buffer  # 13
    worker handle delta blob stream parser stream remote header ref cache handle  # 14
    remote queue socket header branch  # 15
    tree pack header buffer remote token tree token pack token queue  # 16
    ref commit ref buffer  # 17
    tree delta header object blob socket  # 18
    parser socket delta buffer index  # 19
    remote stream ref token  # 20
    buffer parser delta tree remote merge buffer commit token merge tree  # 21
    branch object socket cache handle  # 22
    commit window token commit rebase socket blob queue  # 23
    socket buffer rebase window header tree  # 24
    handle worker branch delta tree pack pack merge blob window worker  # 25
    socket parser header commit  # 26
    handle blob delta window object queue parser delta tree header cache merge  # 27
    socket queue branch buffer  # 28
    worker rebase worker cache header merge rebase queue  # 29
    socket index window handle  # 30
    worker branch token token header header rebase handle cache stream stream ref  # 31
    commit buffer rebase socket window worker remote queue pack remote handle  # 32
    buffer parser pack remote pack ref index  # 33
    rebase merge object ref commit index queue ref worker index  # 34
    pack handle ref pack header object stream stream delta  # 35
    index parser branch commit  # 36
    remote header parser cache  # 37
    parser delta rebase token stream handle socket blob handle  # 38
    object index delta delta branch buffer  # 39
    index index remote token worker worker cache worker handle socket  # 40
