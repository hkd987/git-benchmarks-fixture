    delta tree index pack handle window handle object  # 0
    buffer parser object token cache index worker object delta handle handle  # 1
    branch blob commit ref  # 2
    index socket parser handle rebase delta stream parser  # 3
    delta cache handle queue pack handle delta rebase parser commit merge  # 4
    worker tree cache socket  # 5
    queue buffer worker remote ref  # 6
    stream ref delta buffer buffer  # 7
    parser queue rebase merge handle token  # 8
    window cache window pack worker object rebase  # 9
    blob pack remote blob token stream  # 10
    tree ref delta stream merge remote  # 11
    queue token rebase merge window  # 12
    ref cache object ref merge buffer branch index worker worker index  # 13
    window index ref pack object  # 14
    index blob pack header  # 15
    header merge delta socket buffer merge rebase rebase commit handle worker worker  # 16
    tree buffer branch delta window pack  # 17
    socket delta pack handle ref index socket socket handle parser parser  # 18
    window window blob object branch index handle  # 19
    delta cache remote handle delta ref  # 20
    rebase rebase buffer ref commit stream  # 21
    queue index header buffer index commit token parser merge token  # 22
    object handle tree rebase  # 23
    object buffer ref ref index delta queue  # 24
    commit socket merge header worker queue ref  # 25
    merge pack object ref merge  # 26
    blob header token parser tree  # 27
    queue index worker socket pack blob handle rebase index  # 28
    token merge handle token blob socket index  # 29
    worker branch window window index cache buffer token worker  # 30
    worker blob object tree rebase handle buffer cache delta object socket remote  # 31
    commit rebase merge queue branch stream worker object token worker  # 32
    token cache merge token tree branch  # 33
    delta window ref window  # 34
    branch queue header branch handle socket parser  # 35
    merge commit ref pack header cache delta merge commit  # 36
    branch window ref handle commit header ref pack ref branch header  # 37
    ref object socket handle object worker handle window socket  # 38
    window buffer buffer header queue commit remote ref token tree pack merge  # 39
    index delta stream pack object parser delta token buffer stream rebase branch  # 40
    pack commit commit merge pack worker index stream  # 41
    socket header index blob rebase rebase commit tree token merge  # 42
    pack parser cache branch blob remote delta  # 43
    handle remote tree index buffer blob stream queue blob remote  # 44
    blob index pack cache branch merge ref branch  # 45
    cache tree queue merge parser merge buffer branch window queue  # 46
    handle queue handle commit blob header remote  # 47
    branch ref merge index delta delta window tree branch index rebase blob  # 48
    buffer merge commit socket socket header delta merge rebase commit buffer  # 49
    header index blob index index worker rebase buffer  # 50
    rebase branch socket window window socket object socket remote ref object  # 51
    window window buffer commit  # 52
    window rebase socket socket tree token merge remote socket blob  # 53
    blob branch commit worker blob commit index worker remote  # 54
