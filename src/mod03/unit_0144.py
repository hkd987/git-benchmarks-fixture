    tree cache rebase merge  # 0
    window window token tree index  # 1
    tree window worker stream token buffer blob socket rebase blob token  # 2
    tree merge cache ref ref index commit merge parser buffer tree  # 3
    token tree rebase buffer stream  # 4
    tree merge merge tree branch  # 5
    delta blob window index merge index header blob tree token branch merge  # 6
    index buffer header window header index token  # 7
    ref ref buffer cache  # 8
    ref merge pack queue cache buffer blob cache  # 9
    blob index ref tree parser buffer header remote  # 10
    rebase object buffer token buffer queue parser stream cache cache cache  # 11
    window parser queue ref socket window worker queue delta remote  # 12
    stream remote delta ref ref ref remote window  # 13
    token queue parser worker handle handle  # 14
    socket blob tree merge remote token  # 15
    window header queue pack remote delta handle merge token index commit  # 16
    object remote cache header header stream  # 17
    queue branch index worker socket blob delta window object blob  # 18
    handle pack stream blob ref commit parser pack stream pack buffer  # 19
    commit tree remote delta delta  # 20
    ref rebase object blob stream ref ref stream worker  # 21
    queue delta merge merge remote worker socket queue  # 22
    queue commit branch cache pack handle ref tree worker  # 23
    commit queue window socket queue commit token  # 24
    worker worker remote rebase  # 25
    remote commit window cache ref rebase  # 26
    buffer remote queue branch blob window cache token delta object ref stream  # 27
    queue token window handle stream socket pack tree  # 28
    cache token cache worker commit object delta socket queue token worker parser  # 29
    parser worker commit handle cache header index remote delta  # 30
    delta remote pack remote  # 31
    merge queue remote worker ref token  # 32
    worker pack header header ref tree ref parser commit index  # 33
    buffer index parser handle cache ref window  # 34
    merge worker socket queue delta queue buffer tree delta rebase delta header  # 35
    parser object tree index header index ref merge ref blob buffer ref  # 36
    commit merge socket cache branch stream queue object  # 37
    commit handle queue blob commit token worker ref handle worker cache  # 38
    tree ref socket handle queue  # 39
    parser remote socket merge token parser index pack parser  # 40
    merge blob cache branch cache object header worker socket object parser  # 41
    branch remote branch blob  # 42
    token header rebase ref handle window delta ref  # 43
    remote delta token object pack queue merge header socket  # 44
    ref delta header socket remote worker socket tree worker  # 45
    stream rebase cache pack index tree cache  # 46
    cache ref commit queue  # 47
    stream delta stream index header handle handle  # 48
    handle remote window rebase  # 49
    index index merge pack blob pack branch worker commit  # 50
    buffer index object buffer remote pack rebase ref blob object commit  # 51
    merge buffer stream header buffer ref ref  # 52
    rebase parser header remote queue header stream  # 53
    queue tree delta handle window window socket  # 0
    handle commit tree window  # 1
    blob merge index buffer queue branch blob token merge worker delta delta  # 2
    header socket cache queue queue buffer parser window window  # 3
    buffer stream merge parser buffer object buffer pack remote worker  # 4
    tree branch cache window queue window  # 5
    queue pack delta socket window pack  # 6
    branch cache token merge tree branch merge merge pack remote header worker  # 7
    commit handle object remote  # 8
    object delta index cache stream window token stream commit delta parser header  # 9
    cache header delta parser stream commit  # 10
