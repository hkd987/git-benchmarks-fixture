    ref remote object stream tree socket blob stream blob remote  # 0
    buffer parser buffer rebase window blob stream ref  # 1
    merge cache stream socket pack branch parser  # 2
    buffer socket pack remote window worker socket queue delta  # 3
    ref handle stream ref token merge merge worker parser index header handle  # 4
    tree stream object delta stream  # 5
    branch cache commit worker blob blob merge tree queue  # 6
    ref header blob socket worker pack parser  # 7
    merge header blob queue blob index  # 8
    tree branch worker pack socket tree socket commit  # 9
    merge queue branch pack  # 10
    delta worker commit handle pack commit  # 11
    merge window index buffer token remote parser branch  # 12
    handle remote object blob commit ref remote remote branch branch buffer stream  # 13
    tree tree queue handle  # 14
    cache commit queue token remote  # 15
    buffer token remote index branch ref handle  # 16
    queue blob ref commit parser cache stream  # 17
    buffer socket parser object merge ref parser worker object delta  # 18
    index buffer window merge token cache worker parser buffer queue object tree  # 19
    token commit cache pack stream  # 20
    buffer ref window tree ref  # 21
    buffer cache delta index blob  # 22
    remote pack buffer queue branch  # 23
    branch worker window pack stream stream worker commit  # 24
    stream parser branch tree  # 25
    window header window worker blob object  # 26
    object window queue token token pack  # 27
    index rebase handle remote socket delta rebase index token  # 28
    window pack ref handle commit commit blob handle remote token object  # 29
    header merge tree tree blob pack branch token index parser cache  # 30
    socket rebase queue buffer merge socket rebase branch queue branch delta  # 31
    index rebase socket worker buffer pack buffer header commit worker cache  # 32
    branch remote handle tree rebase tree pack  # 33
    remote pack queue object index pack parser delta buffer  # 34
    header branch socket window  # 35
    branch object socket stream queue object token tree buffer  # 36
    merge commit stream stream commit  # 37
    pack stream window stream index merge worker remote  # 38
    delta rebase merge worker blob commit buffer socket branch queue  # 39
    branch queue window queue delta blob commit socket rebase  # 40
    commit stream delta rebase worker tree parser  # 41
    socket ref tree token  # 42
    delta header token object parser object branch index token index parser  # 43
    window pack tree delta cache commit handle  # 44
    parser buffer tree parser  # 45
    index handle buffer token cache  # 46
    worker merge merge parser commit header stream  # 47
    cache index object pack queue buffer queue stream worker branch index  # 48
    header commit token merge tree socket remote remote stream queue delta  # 49
    handle header stream socket remote parser object ref worker  # 50
    socket ref handle stream merge worker socket tree remote cache merge object  # 51
    stream blob remote queue buffer worker buffer socket buffer index  # 52
    queue pack cache handle socket header  # 53
    header parser stream token index tree index branch  # 54
    handle branch header queue worker merge blob object commit  # 55
    rebase pack branch rebase index blob window socket branch cache queue  # 56
    ref stream header tree commit queue ref  # 57
    buffer merge tree rebase commit handle queue  # 58
    socket handle stream handle pack delta branch  # 59
    parser worker buffer tree window rebase pack commit ref window cache buffer  # 60
    socket merge token rebase  # 61
    blob window pack pack remote branch  # 62
    socket worker buffer worker index token  # 63
    handle pack worker cache object  # 64
    handle branch tree worker index parser parser header token buffer token delta  # 65
    index delta socket delta pack handle buffer stream blob  # 66
    index worker stream branch buffer header socket token ref stream commit  # 67
    token socket queue pack delta remote header  # 0
    stream ref merge delta object ref pack  # 1
    handle branch handle index header delta object  # 2
    socket token parser header pack  # 3
    header buffer cache commit tree window window  # 4
    branch handle pack window ref window rebase rebase object  # 0
    header socket queue object commit socket token remote handle  # 1
    stream pack tree commit ref token delta parser  # 2
    socket buffer socket socket blob pack blob  # 3
    queue delta delta cache commit window header parser tree rebase  # 4
    header queue worker branch pack object index merge token tree  # 5
    object pack cache worker commit remote blob handle merge  # 6
    object stream index handle handle object socket delta token blob window queue  # 7
    merge commit cache index commit delta branch queue handle delta worker  # 8
    object cache pack delta merge parser stream socket socket ref rebase commit  # 0
    tree token buffer socket socket blob socket socket header  # 1
    buffer window worker delta delta commit commit buffer  # 2
    branch ref pack blob queue tree merge merge rebase blob token  # 3
    merge worker blob index queue  # 4
    remote worker object stream buffer blob token remote stream buffer delta delta  # 5
    pack commit stream handle stream pack merge remote object buffer tree index  # 6
    handle object object queue tree stream handle ref window pack merge rebase  # 7
    header delta header commit index pack merge remote cache merge  # 8
    queue buffer ref token  # 9
    parser index handle rebase header commit blob  # 10
    queue parser tree blob ref  # 11
    handle buffer worker header ref blob  # 12
