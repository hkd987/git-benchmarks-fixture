    window handle pack window delta  # 0
    ref delta blob branch merge index commit buffer handle  # 1
    header index rebase delta tree window blob worker  # 2
    branch worker tree cache merge window merge  # 3
    remote socket pack rebase  # 4
    ref socket object queue index  # 5
    window window parser token buffer cache window window handle rebase  # 6
    branch parser handle pack ref token object  # 7
    tree cache blob worker token cache token remote rebase  # 8
    parser cache delta tree worker token socket  # 9
    stream parser socket index commit token header branch index delta parser cache  # 10
    worker token stream tree buffer cache rebase socket merge delta  # 11
    socket index stream pack ref worker token pack handle header ref  # 12
    object window header rebase worker object rebase  # 13
    worker rebase object ref buffer token  # 14
    rebase rebase pack stream tree index ref pack  # 15
    pack worker socket commit  # 16
    worker delta merge window commit index  # 17
    merge object handle branch queue  # 18
    buffer window rebase parser tree cache window ref branch  # 19
    blob blob remote socket pack  # 20
    tree header worker worker pack queue handle pack  # 21
    object pack rebase delta ref  # 22
    worker delta tree index branch stream  # 23
    delta index queue parser ref tree  # 24
    branch window delta token remote remote blob delta handle branch  # 25
    queue token queue handle pack worker branch rebase parser buffer tree  # 26
    token window delta stream  # 27
    remote delta commit blob header worker token object  # 28
    token remote object remote header rebase window pack rebase ref merge commit  # 29
    rebase commit index cache  # 30
    stream merge parser delta branch parser branch parser  # 31
    header blob buffer token cache delta window merge cache pack remote delta  # 32
    ref cache merge buffer branch window index tree header  # 33
    window commit header window window queue index tree delta  # 34
    commit object worker queue tree queue ref remote index socket cache commit  # 35
    delta queue window branch handle worker worker queue worker  # 36
    index object stream parser header object delta  # 37
    token tree window parser handle rebase socket delta  # 38
    blob commit socket delta window window  # 39
    commit buffer tree socket tree handle parser  # 40
    remote branch pack commit rebase rebase stream remote socket handle token remote  # 41
    stream rebase header parser branch index  # 42
    parser index branch token token  # 43
    handle ref queue socket index blob buffer tree cache window buffer rebase  # 44
    token token tree socket queue  # 45
    commit ref delta socket delta window delta queue socket  # 46
    tree branch tree tree handle  # 47
    ref cache stream object  # 48
    branch worker worker pack remote header  # 49
    blob blob remote branch merge blob rebase cache commit cache  # 50
    tree object merge queue token socket index commit window stream merge  # 51
    pack stream blob ref object queue pack header worker  # 52
    branch socket stream parser window  # 53
    rebase parser blob commit remote remote object stream delta rebase rebase  # 54
    queue cache parser cache token remote merge handle token  # 0
    token parser pack delta window index  # 1
    remote index socket index header parser branch commit worker queue header  # 2
    worker worker handle commit index buffer  # 3
    rebase header queue pack parser object token branch handle pack  # 4
    merge cache blob delta remote queue buffer commit buffer delta  # 5
    parser handle header tree window pack remote queue header merge pack socket  # 6
    commit branch ref tree rebase  # 7
    pack parser blob stream object  # 8
    ref merge merge handle queue merge parser header  # 9
    buffer ref delta blob token index tree worker object  # 0
    pack tree queue rebase ref header token remote remote branch cache stream  # 1
    pack handle handle worker cache queue header merge object socket ref parser  # 2
    socket ref remote header stream index commit index remote pack  # 0
    remote parser pack token ref  # 1
    queue merge handle ref queue branch handle header queue  # 2
    branch ref token ref branch buffer  # 3
    queue rebase stream rebase socket handle queue parser index blob tree  # 4
    blob commit buffer socket tree index parser blob rebase worker parser queue  # 5
    handle ref tree object handle queue merge object  # 6
    buffer buffer branch blob token commit  # 7
    delta ref ref object merge queue worker remote socket  # 8
    socket socket socket pack  # 9
    merge header handle token branch parser blob queue index index window  # 10
    merge object window merge  # 11
