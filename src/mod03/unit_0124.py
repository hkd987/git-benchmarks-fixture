    parser branch buffer worker ref tree rebase ref  # 0
    remote remote branch index delta delta pack buffer header  # 1
    pack object stream window worker buffer parser header socket blob merge cache  # 2
    branch window object blob queue worker branch merge blob delta blob header  # 3
    parser delta handle delta parser rebase  # 4
    index token stream commit token header token ref remote commit queue remote  # 5
    handle header parser queue  # 6
    socket delta object rebase parser rebase blob  # 7
    handle header pack worker buffer delta window remote pack handle parser rebase  # 8
    index stream handle cache socket socket  # 9
    delta handle blob pack commit cache buffer tree object ref ref  # 10
    queue handle stream blob  # 11
    worker tree worker pack handle index worker  # 12
    merge parser index parser handle  # 13
    buffer ref branch object cache branch socket ref buffer  # 14
    cache cache object handle  # 15
    commit remote index cache token  # 16
    queue ref token remote stream blob parser branch worker branch commit header  # 17
    socket rebase buffer cache window stream blob cache handle  # 18
    cache token tree cache header index  # 19
    handle rebase buffer tree  # 20
    branch delta pack parser tree parser delta token socket header queue  # 21
    object pack object queue index object object  # 22
    tree header index delta ref remote stream header  # 23
    rebase pack stream merge header  # 24
    queue worker ref cache window cache remote  # 25
    pack socket handle parser merge cache delta pack  # 26
    token object commit worker worker queue ref  # 27
    commit buffer queue socket cache blob  # 28
    header delta rebase merge  # 29
    index blob socket cache  # 30
    cache delta header buffer ref rebase pack buffer merge ref  # 31
    queue token merge cache parser buffer  # 32
    parser worker remote commit blob worker merge header delta token buffer  # 33
    cache commit token worker parser cache commit tree window remote  # 34
    header blob buffer tree cache tree worker token branch rebase buffer  # 35
    remote index blob cache buffer object queue handle tree merge socket queue  # 36
    remote blob tree pack worker worker worker  # 37
    header delta delta index buffer delta rebase commit stream worker  # 38
    cache commit branch branch parser handle parser merge commit rebase pack worker  # 39
    tree ref blob window window merge rebase cache parser delta socket  # 40
    blob tree socket remote merge handle handle merge blob pack remote  # 41
    socket tree ref blob header queue  # 42
    index rebase branch object token buffer index queue  # 43
    worker cache branch worker parser parser buffer worker object remote blob ref  # 44
    handle branch header window buffer  # 45
    branch index stream window queue  # 46
    blob socket window token handle ref parser  # 47
    remote branch cache remote merge cache token index cache  # 48
    window worker commit delta remote cache pack window stream remote index window  # 49
    object handle window merge cache  # 50
    socket blob delta queue merge  # 51
    handle branch stream token blob tree merge socket blob commit  # 52
    branch cache window header cache  # 53
    object header rebase token queue socket buffer  # 54
    buffer handle blob rebase tree ref  # 55
    socket rebase ref ref  # 56
    ref rebase token stream socket socket buffer delta window window rebase  # 57
    parser stream pack commit  # 58
    worker delta ref stream remote rebase worker ref  # 59
    rebase token cache token blob object  # 60
    parser header handle token remote  # 61
    queue header blob handle blob handle stream remote index object commit queue  # 62
    tree tree branch queue header remote window blob worker pack  # 63
    blob commit socket rebase handle rebase window socket worker worker object  # 64
    socket parser tree merge queue delta  # 65
    cache ref queue rebase buffer object  # 66
    queue socket header commit tree tree pack  # 67
    merge queue tree cache merge token delta branch buffer delta  # 68
    parser rebase rebase handle cache buffer object index  # 69
    window pack branch worker tree commit  # 70
    object queue delta branch commit remote rebase buffer stream worker ref  # 71
    token socket pack index commit commit window rebase commit commit  # 72
    blob delta parser delta pack window branch token commit  # 73
    rebase parser delta cache object commit delta  # 74
    header commit stream token queue index worker window handle ref  # 75
    worker pack buffer rebase  # 76
    merge delta header handle queue stream handle handle  # 77
    blob header buffer cache tree ref  # 78
    queue socket branch worker stream cache remote window tree ref  # 79
    remote rebase header cache pack window buffer ref  # 80
    commit queue rebase queue object parser remote token ref  # 81
    ref blob worker ref handle rebase tree branch index token  # 82
    remote branch token stream parser commit branch worker  # 83
    tree remote pack handle object blob window merge commit ref  # 84
    merge buffer ref cache ref header queue blob  # 85
    delta window remote merge delta  # 86
    token header buffer queue  # 87
