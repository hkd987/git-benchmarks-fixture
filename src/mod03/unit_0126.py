    merge index remote socket branch header parser header tree rebase  # 0
    socket commit commit pack remote worker handle tree worker token merge worker  # 1
    stream cache stream parser stream buffer queue socket ref  # 2
    token handle token object delta remote  # 3
    queue buffer cache ref blob remote cache  # 4
    ref pack buffer parser parser token tree cache socket header  # 5
    tree index ref object tree blob commit commit header parser blob queue  # 6
    branch merge handle handle handle tree handle cache handle tree pack  # 7
    buffer blob handle branch delta cache commit token rebase tree  # 8
    pack cache cache delta index stream worker ref  # 9
    remote rebase buffer rebase socket stream  # 10
    window parser window commit header remote parser pack socket socket parser  # 11
    branch branch handle window  # 12
    parser branch tree commit handle commit window tree  # 13
    branch worker header pack handle  # 14
    commit blob socket buffer buffer header parser header index queue rebase  # 15
    merge token object queue buffer tree socket  # 16
    socket window branch branch blob delta ref  # 17
    cache commit merge window index worker commit header parser parser  # 18
    pack stream pack cache stream  # 19
    window worker buffer index object remote remote index  # 20
    pack commit blob delta cache ref socket handle  # 21
    blob branch handle window  # 22
    pack pack rebase merge delta queue remote index blob  # 23
    header branch pack handle  # 24
    merge stream worker commit header window handle pack commit  # 25
    token object handle commit pack  # 26
    buffer pack buffer queue buffer handle ref handle branch object  # 27
    merge index object cache commit tree tree delta ref queue  # 28
    window blob object commit socket cache stream cache cache  # 29
    commit buffer pack cache index  # 30
    remote window tree header queue object socket parser  # 31
    pack header ref ref branch socket pack  # 32
    delta pack stream handle buffer worker delta cache branch tree window ref  # 33
    header worker header object  # 34
    pack ref pack handle ref index stream token header blob  # 35
    pack branch cache object token handle cache handle  # 36
    delta merge tree delta cache index object index  # 37
    tree branch parser ref parser stream remote stream handle buffer header branch  # 38
    tree rebase ref object pack cache parser parser object  # 39
    worker index commit token commit window handle buffer token window pack ref  # 40
    queue worker handle branch rebase branch queue  # 41
    handle pack parser remote tree rebase cache buffer socket queue  # 42
    socket buffer cache queue socket buffer  # 43
    merge window stream token commit socket stream  # 44
    queue buffer commit branch remote stream  # 45
    merge worker header handle ref  # 46
    pack header tree socket  # 47
    worker header ref parser parser queue stream  # 48
    window header index window blob cache tree pack worker  # 49
    commit blob commit queue tree  # 50
    token remote token buffer window delta parser delta  # 51
    token delta index handle cache merge merge index window  # 52
    index token object pack cache  # 53
    index parser tree remote parser  # 54
    handle handle remote commit blob stream queue merge queue  # 55
    ref commit parser object stream blob tree  # 56
    window merge remote buffer ref buffer  # 57
    stream header index token socket index handle stream  # 58
    remote worker ref cache commit branch  # 59
    window socket buffer index ref token  # 60
    blob ref remote worker delta pack rebase blob rebase object  # 61
    queue buffer handle pack buffer tree object pack  # 62
    parser index token token  # 63
    pack header pack remote branch worker pack stream  # 64
    delta buffer delta stream socket ref stream rebase cache  # 65
    remote delta token handle parser tree  # 66
    rebase commit tree socket tree merge parser worker rebase commit delta  # 67
    blob object worker stream socket token object ref  # 68
    object parser ref remote object  # 69
