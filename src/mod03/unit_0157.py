    commit buffer queue socket worker index tree pack  # 0
    socket branch parser handle cache cache stream stream  # 1
    queue object parser remote remote remote buffer header window tree delta branch  # 2
    object header buffer object tree rebase merge socket worker  # 3
    worker remote ref header pack ref  # 4
    ref cache handle rebase buffer branch blob window token tree delta  # 5
    index blob parser cache  # 6
    pack ref handle parser branch token commit remote stream token merge rebase  # 7
    commit header merge socket ref ref blob branch commit parser  # 8
    tree queue parser socket pack commit worker ref cache  # 9
    queue buffer branch stream object tree delta merge branch commit  # 10
    window branch remote commit stream merge window stream branch remote delta  # 11
    token commit header ref  # 12
    blob header ref queue branch remote  # 13
    object index cache cache  # 14
    cache pack index pack delta rebase worker queue merge cache  # 15
    socket pack rebase window handle  # 16
    window cache commit merge header pack stream queue  # 17
    stream worker cache branch commit window  # 18
    ref index stream rebase  # 19
    branch handle tree remote stream header index  # 20
    branch ref ref socket handle object token socket  # 21
    object pack queue merge commit remote parser object token merge  # 22
    pack remote cache rebase  # 23
    worker branch handle window ref remote  # 24
    tree merge pack header tree rebase cache rebase  # 25
    object worker remote index handle tree buffer remote  # 26
    branch merge merge merge  # 27
    queue remote delta commit pack remote ref object object window ref ref  # 28
    buffer handle worker queue index buffer header stream header pack pack tree  # 29
    token socket tree window socket rebase stream stream token window queue  # 30
    buffer delta worker tree index tree queue  # 31
    ref branch queue cache window token tree tree commit queue token branch  # 32
    delta worker worker socket  # 33
    header window parser window parser handle  # 34
    header merge token cache ref worker handle ref index worker commit  # 35
    index index pack object ref header  # 36
    stream socket window worker buffer header blob  # 37
    object object branch queue parser blob merge blob remote commit cache tree  # 38
    cache worker worker ref delta index worker ref ref buffer  # 39
    buffer pack buffer remote ref buffer socket  # 40
    socket blob stream index pack merge object index handle socket  # 41
    window tree token queue socket cache  # 42
    handle socket branch window blob  # 43
    cache rebase window rebase merge pack handle tree handle  # 44
    worker handle tree window parser rebase worker merge worker tree cache delta  # 45
    window tree stream cache queue header commit header  # 46
    handle token commit stream handle tree header  # 47
    cache index queue socket window socket stream stream delta tree  # 48
    tree remote queue handle queue tree socket handle object cache remote  # 49
    buffer ref blob blob socket ref ref remote branch  # 50
    header queue socket handle index delta branch stream rebase remote blob  # 51
    token window branch merge socket window object merge object merge handle  # 52
    buffer buffer merge commit rebase  # 53
    handle cache socket handle object merge  # 54
    tree handle remote pack commit parser window buffer tree  # 55
    tree remote merge window branch index commit commit delta token blob queue  # 56
    rebase window merge token merge blob pack handle pack parser worker  # 57
    merge pack header index queue blob  # 58
    blob delta worker delta merge buffer worker handle header  # 59
    merge blob blob queue remote window  # 60
    rebase window commit header rebase pack commit socket object worker object  # 61
    cache header pack handle  # 62
    merge commit worker buffer window buffer tree branch stream object handle stream  # 63
    branch object worker parser pack worker handle tree cache index  # 64
    socket remote stream rebase  # 65
    header commit handle window cache branch window handle window branch ref  # 0
    remote ref commit cache tree merge header stream parser buffer rebase  # 1
    parser header cache blob  # 2
    delta queue queue branch stream token parser ref blob blob stream rebase  # 3
    cache blob window pack worker window stream  # 4
    object parser commit socket worker stream branch ref  # 5
    buffer pack merge socket handle blob blob socket handle token handle queue  # 6
    handle buffer remote object stream branch window index window remote branch header  # 7
    branch socket blob header merge pack parser cache buffer remote  # 8
    blob socket socket merge buffer socket handle  # 0
    blob window cache commit parser handle handle branch cache rebase blob header  # 1
    blob worker branch buffer branch rebase  # 2
    rebase cache remote remote ref  # 3
    worker index window queue header delta cache delta window worker token remote  # 4
    parser worker cache delta cache token ref  # 5
    ref ref index stream buffer  # 6
    branch window header queue object rebase remote remote token worker stream rebase  # 7
    rebase parser buffer pack token socket worker  # 8
    token stream handle pack handle tree blob  # 9
    object branch pack pack  # 0
    commit cache remote ref  # 1
    blob merge branch rebase rebase cache socket remote worker  # 2
    remote parser socket delta blob parser window object delta handle  # 3
    handle tree remote remote header  # 4
    window header delta commit  # 0
    header commit pack parser stream blob  # 1
    rebase parser cache index remote handle delta tree handle cache ref  # 2
    header cache commit parser merge worker tree ref parser  # 3
    rebase buffer rebase header handle  # 4
