    ref queue cache buffer pack branch worker socket index window blob ref  # 0
    queue ref branch handle index merge header merge branch window object  # 1
    commit blob branch pack index  # 2
    window header parser window socket  # 3
    pack rebase cache delta branch parser delta delta queue delta worker  # 4
    socket branch buffer object  # 5
    worker header rebase stream socket merge handle parser  # 6
    index stream window stream stream tree stream object header stream commit queue  # 7
    worker handle header worker socket  # 8
    remote merge socket pack handle token  # 9
    handle socket token index buffer tree object queue commit handle  # 10
    ref ref remote token object  # 11
    cache index delta buffer queue header worker ref  # 12
    merge socket token ref handle ref stream  # 13
    index blob index queue commit merge queue worker token pack window  # 14
    tree token tree worker handle remote blob worker index object  # 15
    buffer parser pack delta  # 16
    tree rebase object stream  # 17
    pack object object object index worker ref ref blob pack  # 18
    blob buffer token parser commit stream window object cache parser  # 19
    ref ref handle worker merge socket rebase branch queue rebase handle index  # 20
    stream remote object buffer delta token merge  # 21
    parser commit blob header tree  # 22
    stream buffer rebase commit cache cache rebase worker merge  # 23
    pack window rebase blob branch window commit rebase commit pack  # 24
    blob branch header object buffer header stream  # 25
    blob handle remote merge branch commit merge blob parser branch  # 26
    delta object commit header socket rebase delta worker  # 27
    merge token queue parser stream  # 28
    branch branch cache branch delta merge commit worker commit rebase  # 29
    queue buffer stream index buffer  # 30
    merge window cache socket commit rebase  # 31
    window tree ref cache  # 32
    ref rebase cache pack queue commit stream handle queue delta  # 33
    ref branch merge branch remote socket remote pack branch  # 34
    header pack remote token  # 35
    socket merge worker object  # 36
    pack object remote index  # 37
    delta window commit blob object remote rebase object index  # 38
    pack queue ref cache cache remote queue  # 39
    token delta window buffer token buffer token  # 40
    header ref commit blob window remote pack pack  # 0
    blob blob merge merge window remote  # 1
    buffer commit parser cache commit header  # 2
    ref blob queue socket queue tree pack rebase ref index  # 3
