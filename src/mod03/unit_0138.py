    window commit socket ref ref worker handle queue delta rebase parser socket  # 0
    parser worker worker handle window remote commit ref pack stream index  # 1
    tree cache delta socket delta header queue pack stream merge delta stream  # 2
    branch blob buffer header ref rebase ref parser token pack  # 3
    worker buffer parser socket  # 4
    branch worker parser handle  # 5
    cache cache rebase merge branch  # 6
    pack blob parser remote queue cache merge object handle handle queue queue  # 7
    window cache socket socket pack object index  # 8
    tree stream blob header commit ref  # 9
    index pack buffer handle pack queue window delta merge rebase pack index  # 10
    remote parser index delta handle token commit merge blob object tree  # 11
    object object parser stream remote  # 12
    header cache pack commit tree socket stream pack ref  # 13
    pack cache merge queue merge handle worker buffer token queue  # 14
    token handle remote token index branch index header delta header stream  # 15
    index remote rebase cache  # 16
    cache ref branch merge object branch branch window branch  # 17
    object handle token ref pack commit rebase parser  # 18
    merge window index object delta object index  # 19
    worker blob worker token header stream  # 20
    cache worker parser handle pack pack blob worker  # 21
    header socket pack stream header index worker header  # 22
    socket ref stream tree socket ref queue branch delta socket ref pack  # 23
    remote pack rebase parser blob cache buffer worker buffer socket  # 24
    window handle header remote commit stream worker  # 25
    worker blob branch remote rebase  # 26
    handle remote token tree commit index cache merge  # 27
    header cache cache index object parser queue socket rebase window delta  # 28
    delta branch index stream parser branch  # 29
    object commit branch socket ref merge ref handle index token pack socket  # 30
    handle object blob commit handle rebase remote header tree parser commit rebase  # 31
    queue stream blob stream queue tree buffer buffer cache window delta delta  # 32
    merge branch blob index handle delta remote handle  # 33
    queue queue stream branch ref delta branch stream  # 34
    commit commit header delta  # 35
    token socket queue index blob parser cache token index branch  # 36
    handle ref merge cache worker queue token delta header handle  # 37
    cache delta queue merge index worker merge cache buffer ref ref  # 38
    index object buffer worker buffer parser  # 39
    queue buffer socket index stream pack merge worker  # 40
    cache pack blob index token  # 41
    tree blob handle queue rebase handle socket header  # 42
    tree parser buffer commit window remote blob handle socket  # 43
    pack delta pack parser worker pack  # 44
    remote commit rebase cache  # 45
    parser header merge cache  # 46
    index parser merge stream merge  # 47
    window remote rebase rebase buffer index pack remote socket  # 48
    cache ref worker remote header tree parser stream socket object buffer token  # 49
    stream header object parser rebase ref tree header queue cache rebase  # 50
    tree socket token socket token tree  # 51
    window rebase commit worker handle header blob tree header merge merge  # 52
    ref delta handle commit token queue header stream rebase worker  # 53
    branch buffer object merge ref ref worker queue tree  # 54
    worker worker rebase merge commit  # 55
    delta queue commit commit remote cache queue  # 56
    object tree stream index pack blob queue merge tree  # 57
    object rebase ref cache  # 58
    buffer ref object cache buffer object  # 59
    parser header merge index worker buffer window  # 60
    index merge merge object stream stream index remote header cache  # 61
    stream remote stream index  # 62
    socket cache tree parser blob token commit pack  # 63
    window remote pack tree header buffer rebase queue  # 64
    rebase ref token stream worker socket socket buffer  # 65
    cache pack handle token parser token merge  # 66
    rebase tree delta buffer ref blob  # 67
    handle commit token queue blob branch token  # 68
    queue index delta delta buffer  # 69
    commit object branch parser  # 70
    queue rebase socket remote merge handle tree index worker delta  # 71
    stream parser rebase stream queue blob delta commit  # 72
    header token remote handle delta  # 73
    branch header queue rebase handle pack object rebase branch rebase buffer object  # 74
    worker tree queue stream  # 75
    index token index branch branch  # 76
    queue buffer socket header  # 77
    index socket pack cache branch delta  # 78
    header merge tree token handle merge  # 79
    pack tree branch delta pack cache queue parser  # 80
