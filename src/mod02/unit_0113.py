    queue token rebase delta branch ref pack object remote delta remote rebase  # 0
    handle token stream parser tree index worker commit parser  # 1
    parser socket delta header worker merge rebase buffer parser header rebase  # 2
    remote pack worker remote buffer object  # 3
    ref pack stream index handle  # 4
    tree cache merge index token buffer  # 5
    ref pack header remote delta worker cache socket buffer index  # 6
    stream tree token object  # 7
    blob header merge parser rebase remote  # 8
    queue delta branch queue cache ref stream blob  # 9
    stream blob pack commit merge parser rebase index merge tree  # 10
    socket parser merge delta blob buffer queue cache commit tree  # 11
    header object token cache buffer  # 12
    worker delta token socket commit delta header rebase index parser  # 13
    merge window blob cache parser ref  # 14
    worker header window branch  # 15
    cache blob parser token  # 16
    queue commit cache worker  # 17
    token handle delta buffer window commit header blob worker window cache header  # 18
    handle handle object cache blob object handle object handle  # 19
    cache merge buffer blob cache commit  # 20
    ref ref handle window  # 21
    branch pack buffer object worker commit rebase queue window socket pack token  # 22
    delta stream remote ref  # 23
    delta worker socket merge parser buffer merge stream buffer  # 24
    buffer handle commit socket header rebase tree buffer window cache commit rebase  # 25
    remote parser commit ref worker rebase  # 26
    delta blob window delta window pack  # 27
    commit cache parser cache merge stream tree header token commit  # 28
    blob index tree index header branch cache remote ref socket delta  # 29
    worker token rebase object commit ref window buffer  # 30
    worker delta parser header tree pack window remote tree  # 31
    index worker branch index socket  # 32
    ref window header queue merge rebase branch  # 33
    tree object socket object queue worker  # 34
    buffer pack handle handle token token branch remote buffer branch  # 35
    cache ref header delta  # 36
    blob blob handle index index rebase header socket  # 37
    stream header blob blob pack header ref  # 38
    ref queue delta remote header handle queue window handle pack stream object  # 39
    cache object tree pack token blob remote pack worker  # 40
    index token buffer commit object rebase  # 41
    parser ref handle pack parser ref  # 42
    ref cache worker header branch handle token index object ref stream  # 43
    blob index parser socket index delta tree queue  # 44
    token object rebase parser queue handle worker delta index  # 45
    tree window delta cache  # 46
    handle tree worker blob buffer rebase ref stream  # 47
    cache stream window pack stream  # 48
    handle commit socket blob stream handle  # 49
    blob tree object socket handle cache queue worker index remote parser  # 50
    header merge buffer merge pack parser worker stream remote handle  # 51
    delta worker stream tree handle pack parser  # 52
    socket commit blob socket blob remote  # 53
    blob delta cache header  # 54
    object cache token cache delta pack ref delta  # 55
    cache index buffer worker tree cache branch tree header object delta  # 56
    queue index merge socket worker buffer rebase rebase window stream handle  # 57
    handle buffer branch blob stream rebase queue queue buffer  # 58
    pack window index object parser parser worker socket pack  # 59
    stream socket token remote cache commit cache handle remote branch blob  # 60
    stream merge buffer pack pack token handle branch handle  # 61
    ref object branch buffer header object merge parser queue object ref  # 62
    socket branch window ref remote object cache tree  # 63
    blob header ref pack  # 64
    cache tree commit object  # 65
    buffer rebase buffer object index index stream handle  # 66
    ref index socket queue stream tree  # 67
    index worker window merge remote index blob cache stream stream socket blob  # 68
    index remote index token rebase  # 69
