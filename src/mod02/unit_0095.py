    branch index blob index commit object token buffer remote header merge delta  # 0
    cache socket worker pack token socket ref handle commit index index  # 1
    buffer worker ref merge branch stream delta handle socket commit  # 2
    pack tree commit buffer token token blob  # 3
    rebase cache blob blob  # 4
    blob token socket index window token blob  # 5
    window ref header ref socket ref rebase  # 6
    remote parser cache queue stream window  # 7
    blob remote header token  # 8
    delta tree worker pack merge merge parser pack  # 9
    cache buffer worker rebase handle cache commit commit socket socket  # 10
    socket commit worker socket worker pack buffer worker  # 11
    index delta header remote commit commit rebase stream socket  # 12
    parser header buffer window parser blob token ref  # 13
    socket merge socket stream cache ref cache parser worker merge index  # 14
    delta index object object ref cache rebase commit object  # 15
    branch handle stream buffer parser cache queue window stream socket delta  # 16
    pack blob blob rebase token queue queue  # 17
    cache buffer ref buffer socket  # 18
    socket merge index handle queue  # 19
    ref window rebase pack delta cache  # 20
    token rebase object branch header window  # 21
    rebase ref buffer buffer handle stream stream worker pack  # 22
    socket branch stream token merge index  # 23
    buffer window window rebase queue queue rebase  # 24
    queue handle window cache  # 25
    token tree index buffer remote tree window parser  # 26
    window buffer stream stream index queue  # 27
    cache socket handle pack blob socket index  # 28
    header rebase ref header delta header ref header handle object cache  # 29
    window worker header merge window rebase remote queue  # 30
    token blob window header header merge branch  # 31
    handle blob window header buffer commit commit window queue  # 32
    delta branch socket commit token tree remote commit delta worker queue handle  # 33
    index pack buffer object queue delta branch  # 34
    parser merge commit handle  # 35
    ref socket buffer index  # 36
    queue socket window rebase window queue remote window  # 37
    cache stream rebase stream window rebase pack branch buffer pack ref  # 38
    branch branch parser parser window index blob cache branch ref cache merge  # 39
    remote queue delta commit ref buffer tree worker rebase blob  # 40
    buffer buffer socket remote merge handle commit queue cache  # 41
    stream stream handle tree ref object window branch index header socket window  # 42
    cache header object object token worker socket object remote token header cache  # 43
    object pack object tree pack socket queue  # 44
    branch blob commit parser object ref header object header header  # 45
    window commit window branch header queue window tree  # 0
    pack token window merge queue handle header merge cache window  # 1
    header delta branch object merge rebase token stream worker rebase queue cache  # 2
    worker parser branch parser ref queue window object branch object  # 3
    tree remote pack queue cache window buffer worker worker handle  # 4
    commit pack branch branch delta remote parser buffer ref queue delta  # 5
    stream ref tree branch delta delta socket delta delta  # 6
    tree buffer header commit  # 7
    commit tree token cache worker index stream token header  # 8
    parser blob commit branch object pack window blob handle remote tree  # 9
    remote index cache cache commit queue header rebase rebase rebase remote buffer  # 10
    branch socket window token index pack queue queue cache tree  # 11
