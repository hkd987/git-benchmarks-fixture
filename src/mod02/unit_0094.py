    queue worker buffer remote rebase index cache worker  # 0
    branch merge cache commit branch handle  # 1
    ref header window parser ref worker token  # 2
    header cache buffer window buffer window worker branch ref queue remote object  # 3
    worker window index branch buffer  # 4
    index rebase header worker  # 5
    cache blob blob delta token socket merge queue buffer  # 6
    pack branch merge branch worker stream worker ref remote  # 7
    merge merge remote ref remote  # 8
    remote queue token parser rebase object queue delta parser ref  # 9
    cache remote tree parser branch branch merge parser stream commit rebase handle  # 10
    blob window queue index merge window window window tree stream  # 11
    delta commit buffer worker rebase buffer branch token tree cache  # 12
    socket worker header tree  # 13
    remote token object window handle object delta token index rebase buffer stream  # 14
    remote object handle merge handle commit tree delta delta commit handle ref  # 15
    stream buffer parser blob handle index merge  # 16
    merge object object window index token  # 17
    tree tree commit buffer cache commit pack remote delta  # 18
    blob commit blob socket pack blob parser token remote parser stream  # 19
    object worker stream header header token delta  # 20
    tree worker commit header worker merge object  # 21
    delta parser stream remote cache object rebase merge socket remote stream  # 22
    tree parser blob worker object buffer  # 23
    worker branch commit parser branch window branch ref socket queue  # 24
    blob cache delta blob parser queue window  # 25
    window remote ref buffer window worker rebase  # 26
    object token stream header rebase  # 27
    ref merge queue handle pack index parser worker ref handle handle  # 28
    delta ref pack worker blob window blob remote buffer  # 29
    worker queue branch ref queue delta buffer token commit  # 30
    branch handle branch remote parser token header pack  # 31
    handle cache index token rebase cache rebase merge rebase queue queue  # 32
    buffer window header worker tree worker  # 33
    worker queue token blob  # 34
    merge queue token delta  # 35
    rebase window index object index branch stream window merge remote commit blob  # 36
    merge buffer branch object  # 37
    merge branch buffer merge socket rebase worker stream  # 38
    stream token header buffer queue token pack commit  # 39
    ref worker header commit ref stream  # 40
    delta blob header branch socket  # 41
    pack commit index cache delta  # 42
    cache ref rebase object tree delta parser  # 43
    merge header branch handle pack window delta  # 44
    worker pack rebase handle queue commit handle  # 45
    socket handle socket parser ref buffer branch  # 46
    tree queue merge index branch delta queue window token  # 47
    remote blob tree header index object branch buffer  # 48
    stream cache cache cache  # 49
    index index window commit pack  # 50
    buffer index delta tree blob index ref window pack  # 51
    ref pack commit tree blob socket rebase blob handle blob  # 52
    token token buffer window rebase queue delta queue queue queue token pack  # 53
    branch delta cache blob worker header  # 54
    socket object merge commit tree queue parser window rebase cache  # 55
    header branch buffer header ref branch commit buffer remote stream delta  # 56
    header tree ref commit branch  # 57
