    pack ref merge stream remote tree merge remote queue delta commit  # 0
    object remote handle handle token socket buffer delta socket socket pack  # 1
    header merge index delta  # 2
    ref merge delta ref socket rebase window  # 3
    delta stream token socket pack cache window pack commit  # 4
    queue rebase rebase socket cache remote worker buffer object stream  # 5
    parser blob index ref  # 6
    handle ref pack token remote rebase  # 7
    index delta object rebase token ref handle remote remote parser delta window  # 8
    blob handle tree socket blob object blob cache  # 9
    blob delta token handle pack  # 10
    parser header tree window rebase socket  # 11
    parser buffer remote ref  # 12
    ref handle handle handle blob token token commit tree  # 13
    delta socket window queue  # 14
    window token blob worker tree parser  # 15
    index window cache window worker tree cache worker branch cache  # 16
    pack cache commit pack ref rebase window  # 17
    queue merge token ref  # 18
    socket stream cache delta socket object socket parser  # 19
    worker delta cache rebase buffer  # 20
    blob rebase commit blob buffer rebase header pack commit merge delta tree  # 21
    commit window branch buffer socket ref index handle handle rebase blob tree  # 22
    queue object blob buffer  # 23
    pack ref commit queue cache buffer token tree buffer window parser header  # 24
    socket object object delta stream header buffer delta delta delta branch  # 25
    stream rebase parser pack parser socket socket stream object  # 26
    remote delta header index queue worker socket ref header  # 27
    queue header ref merge header rebase handle ref  # 28
    ref header object parser socket socket  # 29
    token blob tree worker blob index header remote cache remote cache worker  # 30
    queue header remote ref handle remote blob buffer stream object token object  # 31
    socket rebase merge window remote stream window  # 32
    socket stream index worker queue socket branch socket  # 33
    branch object remote pack pack  # 34
    pack socket window header commit worker parser rebase token rebase  # 0
    merge ref worker cache queue parser buffer ref parser remote  # 1
    index commit buffer queue header branch  # 2
    index index object worker  # 3
    parser delta blob remote remote buffer object parser remote blob delta  # 4
    blob remote token commit  # 5
    commit handle cache stream queue window socket delta  # 6
    worker queue blob merge socket blob token header blob  # 7
