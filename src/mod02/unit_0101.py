    stream blob window tree ref stream object object socket stream  # 0
    buffer commit delta blob index rebase  # 1
    stream socket window buffer header commit  # 2
    socket merge header cache blob delta index  # 3
    worker stream blob header  # 4
    buffer window queue socket token buffer object window object branch handle  # 5
    stream rebase pack queue index branch tree rebase  # 6
    ref delta window worker commit stream delta handle  # 7
    handle handle commit queue buffer buffer commit worker header index handle  # 8
    cache worker header index  # 9
    merge socket stream parser pack tree index socket token stream window  # 10
    socket token pack branch  # 11
    remote parser rebase commit commit branch tree parser tree queue  # 12
    token object header buffer commit commit cache queue rebase tree commit handle  # 13
    parser remote blob token object handle cache rebase ref worker merge  # 14
    stream header parser socket delta token remote cache queue rebase  # 15
    commit cache blob branch parser parser rebase header delta object  # 16
    stream parser delta buffer stream worker cache index branch handle token rebase  # 17
    object remote ref worker header delta handle rebase branch handle ref merge  # 18
    window ref stream buffer delta commit header merge pack buffer  # 19
    parser window rebase pack buffer ref socket index token delta  # 20
    tree socket cache rebase stream queue window index stream rebase queue  # 21
    branch queue pack queue header socket blob queue queue socket ref  # 22
    blob queue object blob remote blob parser header window tree  # 23
    object stream window merge ref index index stream rebase commit socket cache  # 24
    delta ref rebase object rebase delta worker rebase commit branch  # 25
    branch pack delta object socket delta queue  # 26
    cache queue index object remote  # 27
    cache queue token worker buffer delta ref blob socket merge index  # 28
    blob index branch parser merge merge worker  # 29
    window window token branch queue blob blob parser rebase header  # 30
    ref cache remote queue index  # 31
    buffer object blob object header  # 32
    parser branch index token window remote  # 33
    branch commit stream remote handle handle  # 34
    object socket ref commit handle  # 35
    merge buffer rebase token worker remote  # 36
    window delta tree queue queue  # 37
    header socket cache delta merge merge blob ref socket  # 38
    blob rebase window rebase tree tree remote  # 39
    stream cache index commit delta cache ref socket window stream  # 40
    token token queue cache queue index header handle  # 41
    header blob delta branch rebase window parser object index worker blob  # 42
    cache tree delta stream pack commit index  # 43
    cache delta remote remote object pack  # 44
    rebase handle rebase merge ref rebase remote rebase stream stream object index  # 45
    handle worker pack worker  # 46
    tree queue stream branch cache queue queue worker pack parser ref  # 47
    buffer token commit object queue queue  # 48
    window tree index buffer branch blob blob  # 49
    commit socket index object object stream tree handle  # 50
    rebase delta header delta token blob branch branch branch  # 51
    token stream remote object object remote blob object blob stream window cache  # 52
    pack cache cache cache token merge delta commit blob queue  # 53
    worker queue worker tree handle index cache  # 54
    queue commit window queue socket branch branch pack parser tree remote  # 55
    handle index queue index branch socket  # 56
    branch delta token parser stream branch stream pack merge  # 57
    tree ref ref token stream tree index window  # 58
    ref rebase merge object branch tree object  # 59
    token merge cache pack window merge  # 60
    commit tree stream buffer merge socket  # 61
    worker header header merge remote buffer buffer  # 62
    token socket index header buffer delta handle parser  # 63
    pack header object blob buffer delta buffer blob remote commit cache  # 64
    pack delta remote buffer merge token remote merge cache index  # 65
    branch delta ref cache  # 66
    blob delta socket pack buffer object  # 67
    pack stream handle window blob branch parser index commit stream  # 68
    pack merge branch blob buffer token parser parser commit cache header pack  # 69
    cache rebase socket rebase queue  # 70
