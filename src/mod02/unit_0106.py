    delta blob tree pack  # 0
    object commit tree cache  # 1
    stream remote stream pack handle cache  # 2
    commit merge stream stream tree ref header socket  # 3
    remote commit rebase blob delta stream  # 4
    commit socket blob worker remote tree  # 5
    buffer remote index merge index queue index commit  # 6
    queue token stream delta  # 7
    cache handle remote commit cache  # 8
    delta remote branch stream delta index pack ref worker handle merge  # 9
    window merge ref cache delta worker rebase socket token commit merge header  # 10
    token socket queue remote token index socket commit buffer pack  # 11
    pack pack merge token queue  # 12
    buffer index token worker cache token  # 13
    queue index rebase delta socket  # 14
    merge tree queue buffer token delta handle tree remote  # 15
    token branch stream header  # 16
    header commit object tree socket  # 17
    socket handle stream ref tree tree commit branch stream  # 18
    pack handle buffer queue tree merge token branch worker buffer  # 19
    blob cache worker merge delta remote header  # 20
    merge handle object header queue pack remote delta delta object  # 21
    stream delta index branch tree pack cache  # 22
    tree buffer object queue token stream pack remote handle token socket  # 23
    tree stream socket tree socket object socket merge  # 24
    cache object stream object branch  # 25
    object ref queue parser worker queue remote handle  # 26
    handle header branch merge ref  # 27
    socket worker header buffer pack blob  # 28
    stream worker merge delta pack  # 29
    queue handle buffer merge index  # 30
    stream handle merge window buffer rebase ref branch cache queue worker blob  # 31
    queue blob remote object handle window window blob object  # 32
    window object branch parser  # 33
    cache blob parser pack  # 34
    rebase delta ref cache pack  # 35
    cache header rebase branch rebase merge worker remote merge index  # 36
    buffer delta header remote socket delta object merge blob  # 37
    commit stream socket ref object buffer worker worker  # 38
    stream worker buffer merge index merge stream commit  # 39
    tree pack pack remote socket stream  # 40
    token ref branch rebase branch commit worker window rebase parser delta  # 0
    cache buffer stream tree pack worker  # 1
    commit worker cache tree stream cache  # 2
    remote handle socket delta  # 3
    tree handle stream tree remote cache tree index token header cache stream  # 4
    socket blob pack ref  # 5
    window object queue socket token  # 6
    queue handle pack blob pack rebase rebase parser buffer  # 7
    branch rebase queue worker pack  # 8
    delta index blob remote remote  # 9
    remote delta handle object blob parser socket token worker tree header merge  # 10
    branch pack tree rebase token header queue parser merge parser  # 0
    cache header merge parser remote pack index object index index  # 1
    header merge worker branch parser window  # 2
    handle pack pack queue cache object remote header  # 3
    ref pack buffer worker token branch  # 4
    stream merge merge ref index rebase branch rebase  # 5
    ref header ref buffer window commit index socket handle ref  # 6
    pack tree window worker  # 7
    index handle cache commit rebase  # 8
    handle object parser handle commit ref buffer header commit token  # 9
    object rebase ref stream branch queue branch branch  # 10
    socket rebase merge window header buffer object object cache stream  # 11
    worker worker rebase merge socket  # 12
    socket index window worker queue remote handle  # 13
    merge pack worker stream buffer index object commit  # 0
    pack index queue index delta buffer handle window worker ref buffer  # 1
    index branch rebase header worker pack tree blob  # 2
    handle socket delta object tree ref token  # 3
    pack rebase rebase header rebase  # 4
    cache index index rebase delta cache commit window window  # 5
    branch ref merge rebase handle object  # 6
