    tree pack ref parser worker commit  # 0
    cache merge stream pack handle delta  # 1
    parser cache window branch worker worker index buffer socket  # 2
    index window socket worker handle merge worker remote blob  # 3
    parser window rebase object index  # 4
    token commit merge remote ref rebase worker socket blob socket buffer  # 5
    cache tree queue buffer rebase pack buffer blob object  # 6
    blob pack remote cache window  # 7
    stream rebase handle ref parser ref  # 8
    tree queue merge parser buffer delta  # 9
    tree header branch cache commit blob  # 10
    object window delta queue parser branch  # 11
    merge worker object merge window queue delta rebase  # 12
    cache queue stream remote queue rebase blob  # 13
    index pack socket socket cache branch worker cache parser  # 14
    worker socket remote stream  # 15
    delta stream delta delta merge branch delta ref  # 16
    blob ref cache index header delta blob object merge  # 17
    window header buffer pack rebase parser  # 18
    rebase stream tree parser pack token socket tree socket branch delta object  # 19
    socket object remote commit commit cache  # 20
    object commit index cache socket parser header ref pack  # 21
    handle remote commit worker handle parser branch socket object token stream worker  # 22
    delta cache branch queue  # 23
    header window worker handle window cache  # 24
    remote branch commit pack worker blob stream  # 25
    parser window queue stream  # 26
    parser branch handle socket buffer worker handle object commit object object handle  # 27
    ref delta tree socket stream  # 28
    rebase buffer parser remote stream queue  # 29
    branch blob handle object merge queue delta tree index  # 30
    socket socket branch remote object token remote delta  # 31
    object buffer pack parser branch buffer queue  # 32
    merge parser token commit delta worker remote handle  # 33
    worker remote rebase socket remote branch queue blob stream index tree  # 34
    pack handle parser queue object remote delta stream merge  # 35
    rebase rebase object parser object branch  # 36
    tree parser commit token header blob cache  # 37
    token pack rebase ref pack token  # 38
    index branch object branch blob rebase stream worker stream stream pack cache  # 39
    worker delta cache commit window delta  # 40
