    branch queue blob token socket queue window remote pack stream window blob  # 0
    commit commit rebase parser blob parser queue queue rebase commit buffer  # 1
    worker object handle handle stream header rebase branch branch  # 2
    pack parser header stream tree token window worker handle remote index  # 3
    pack rebase remote blob  # 4
    blob ref tree header stream ref remote handle merge  # 5
    branch remote blob branch merge window cache worker window stream  # 6
    cache pack merge socket merge object token index index stream queue  # 7
    handle delta socket object object object header remote commit socket delta rebase  # 8
    queue window tree commit  # 9
    window worker parser rebase ref token commit object index buffer blob  # 10
    socket queue stream blob handle index remote  # 11
    pack handle tree handle socket  # 12
    merge index pack index  # 13
    branch buffer ref worker socket delta cache commit buffer ref blob  # 14
    pack remote index delta socket token window pack ref merge pack  # 15
    ref pack blob delta cache cache tree branch ref buffer index tree  # 16
    object handle stream delta rebase parser blob handle object  # 17
    object delta parser parser header header pack tree  # 18
    stream socket merge worker branch delta index rebase  # 19
    object token rebase index index parser socket rebase  # 20
    tree stream worker parser rebase branch queue token queue commit  # 21
    socket object blob remote worker queue rebase queue rebase  # 22
    parser ref socket commit  # 23
    blob socket queue remote pack branch buffer merge object  # 24
    index header blob stream window header branch remote commit  # 25
    socket pack token pack header header  # 26
    header ref commit queue header parser ref  # 27
    cache blob pack index socket merge window parser tree stream ref  # 28
    rebase socket object window merge  # 29
    worker tree blob stream cache parser delta commit cache delta  # 30
    socket index cache pack  # 31
    remote rebase window blob window blob remote index  # 32
    remote pack pack queue header  # 33
    commit ref branch stream header pack cache  # 34
    buffer window index index pack index merge ref cache  # 35
    handle object ref token commit object branch stream header cache rebase window  # 36
    blob parser token object delta header socket parser worker  # 37
    pack pack tree worker  # 38
    token parser object ref cache buffer pack  # 39
    remote window cache commit delta index tree queue window rebase remote merge  # 40
    socket object stream cache stream branch parser merge rebase worker worker  # 41
    pack parser handle commit cache rebase queue commit handle stream  # 42
    header tree socket stream header pack object window delta merge rebase pack  # 43
    remote token branch parser commit remote rebase  # 44
    buffer tree blob remote worker  # 45
    tree blob handle delta cache branch blob  # 46
    ref rebase tree queue queue handle  # 47
    token queue rebase remote stream ref object worker merge  # 48
    worker branch delta token branch handle  # 49
    tree blob parser ref pack stream blob  # 50
    blob cache tree parser branch  # 51
    commit rebase tree object delta header object blob handle window  # 52
    merge worker handle tree pack rebase delta cache ref  # 53
    queue rebase queue stream ref ref parser worker  # 54
    ref object delta blob header queue queue window remote rebase cache  # 55
    ref rebase object commit queue remote token cache window worker  # 56
    blob blob blob socket  # 57
    remote delta branch token handle object  # 58
    remote index blob header parser handle handle index token  # 59
    merge worker rebase delta delta handle remote stream handle parser parser  # 60
    commit window parser ref  # 61
    handle pack buffer header tree pack socket socket branch  # 62
    branch pack index delta handle blob remote remote  # 63
    handle ref parser merge queue window header index object worker handle  # 64
    header rebase object remote parser header  # 65
