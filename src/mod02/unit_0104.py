    parser socket index buffer ref parser  # 0
    buffer blob token tree parser token header object worker merge  # 1
    merge ref header token  # 2
    tree cache index ref socket stream  # 3
    remote delta merge rebase  # 4
    worker header socket queue window  # 5
    index blob worker rebase window parser blob parser ref pack cache  # 6
    index handle socket pack blob merge blob  # 7
    branch header stream buffer remote queue  # 8
    index delta pack buffer handle remote  # 9
    blob stream blob object header cache merge token parser queue cache rebase  # 10
    index commit rebase window socket rebase blob pack ref handle  # 11
    cache ref rebase window remote worker  # 12
    ref index tree tree tree blob handle rebase buffer parser  # 13
    stream token delta buffer index blob branch socket stream index stream buffer  # 14
    cache handle stream delta buffer window queue remote cache remote rebase stream  # 15
    buffer rebase queue token ref ref tree worker index cache header window  # 16
    ref index object merge queue buffer object index handle queue cache socket  # 17
    token buffer stream header merge cache header token pack blob  # 18
    queue delta handle commit merge parser delta ref  # 19
    stream object socket commit  # 20
    parser rebase branch window window remote queue socket index ref  # 21
    merge index ref remote worker handle buffer socket header window delta pack  # 22
    handle branch merge socket object  # 23
    cache blob remote remote commit remote buffer buffer worker cache  # 24
    window stream buffer merge token commit window header object tree  # 25
    socket queue rebase commit ref  # 26
    remote index index cache stream rebase  # 27
    branch cache socket stream header merge worker commit  # 28
    commit commit worker remote queue  # 29
    buffer stream worker blob blob  # 30
    buffer merge object queue object commit handle  # 31
    merge merge cache stream socket rebase cache stream ref tree  # 32
    buffer handle queue tree worker parser cache window index queue branch  # 33
    ref pack merge handle pack ref rebase token pack remote  # 34
    blob object ref handle ref tree  # 35
    object commit window pack pack object  # 36
    stream tree handle cache  # 37
    parser commit buffer tree pack  # 38
    window handle handle tree handle  # 39
    cache remote branch pack branch window  # 40
    index worker remote branch merge blob worker stream queue branch tree branch  # 41
    queue tree socket queue delta remote tree token tree  # 0
    merge tree pack pack token queue merge ref queue buffer  # 1
    socket remote commit header handle branch  # 2
    token blob index worker rebase index branch branch cache branch commit  # 3
    queue queue stream worker object handle ref cache branch index  # 4
    index remote worker queue  # 5
    object rebase cache handle branch ref window parser object socket blob  # 6
    window handle merge stream socket index tree socket socket worker ref  # 7
    pack buffer buffer handle token merge remote commit index handle socket  # 8
    buffer pack index merge rebase stream worker handle remote handle buffer worker  # 9
    handle window object object merge tree token  # 10
    object pack window blob token index  # 11
