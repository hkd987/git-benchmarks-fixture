    parser queue pack socket tree pack  # 0
    delta object parser blob cache socket  # 1
    blob stream ref ref stream rebase commit object  # 2
    queue branch object token blob queue ref tree  # 3
    branch parser rebase rebase parser  # 4
    buffer branch blob handle  # 5
    ref parser worker index index handle object  # 6
    handle object socket object delta buffer  # 7
    index window buffer token tree cache header object  # 8
    buffer window blob object window index  # 9
    cache rebase token stream  # 10
    index worker ref handle  # 11
    remote rebase socket queue object blob  # 12
    ref tree queue remote  # 13
    pack remote worker stream  # 14
    buffer delta object branch parser  # 15
    stream buffer socket merge parser stream ref tree  # 16
    pack blob token socket stream rebase stream tree parser  # 17
    cache object worker worker cache window window handle  # 18
    token tree tree tree queue queue delta tree  # 19
    stream parser socket index queue remote  # 20
    ref remote ref stream stream parser buffer  # 21
    merge queue stream parser merge blob parser queue commit branch  # 22
    cache delta tree ref ref worker ref index window socket remote  # 23
    parser tree branch handle  # 24
    handle pack stream header window blob remote tree queue  # 25
    worker remote token queue index buffer worker  # 26
    socket cache index header ref object tree worker  # 27
    pack queue stream commit queue  # 28
    index buffer header ref remote stream socket index cache remote buffer merge  # 29
    ref merge rebase worker remote token handle ref stream  # 30
    ref stream handle queue remote cache  # 31
    delta ref stream socket rebase  # 32
    object header parser blob buffer token delta branch delta  # 33
    branch delta blob delta parser commit object blob ref window  # 34
    merge object socket index branch socket stream delta worker header token  # 35
    queue delta parser tree buffer rebase  # 36
    cache token commit merge object  # 37
    token tree handle object handle pack delta tree merge index rebase remote  # 38
    ref cache stream rebase  # 39
    window index pack window  # 40
    window token branch remote buffer  # 41
    window buffer worker queue ref token pack  # 42
    cache commit object worker stream  # 43
    cache socket branch parser remote delta rebase remote header pack  # 44
    token worker header parser merge commit branch window ref branch token  # 45
    token stream index cache ref socket  # 46
    pack branch pack commit remote index buffer  # 47
    blob merge worker worker  # 48
    blob parser buffer header handle  # 49
    ref blob merge socket merge queue  # 50
    delta blob parser tree header worker pack worker object socket ref stream  # 51
    stream delta commit pack tree buffer delta token  # 52
    rebase parser commit window merge delta cache buffer  # 53
    worker pack pack delta rebase pack ref handle object  # 54
    remote header object rebase ref  # 55
    token queue blob header pack commit stream window cache pack  # 56
    parser parser cache rebase parser buffer rebase window socket  # 57
    branch cache window ref remote handle cache worker remote  # 58
    object pack queue remote buffer queue queue ref cache index  # 59
    pack token stream buffer branch pack handle rebase cache  # 60
    remote header cache merge parser object token stream index branch  # 61
    pack parser tree token socket object cache delta  # 62
    cache handle blob handle delta worker index  # 63
    branch tree handle stream rebase window branch handle socket parser buffer  # 64
    parser stream cache ref merge ref header  # 65
