    pack remote socket branch pack cache object  # 0
    tree cache index merge blob delta  # 1
    stream commit merge handle commit  # 2
    tree header window queue tree rebase socket  # 3
    pack parser queue index  # 4
    handle stream blob handle index rebase  # 5
    worker ref object merge remote worker delta merge socket  # 6
    buffer header cache index delta merge handle tree ref socket header rebase  # 7
    header window delta token object worker remote  # 8
    object window queue pack  # 9
    stream token handle worker tree socket commit worker object  # 10
    cache branch rebase cache object socket buffer  # 11
    ref rebase ref object delta worker window merge header stream index  # 12
    cache window buffer merge tree merge header  # 13
    remote queue cache delta commit commit window rebase tree stream delta delta  # 14
    blob pack queue buffer delta pack worker parser  # 15
    queue token handle header header token pack commit socket cache header  # 16
    worker header index stream remote token  # 17
    blob object buffer blob index token token socket socket queue  # 18
    object header index object branch  # 19
    parser delta worker remote header worker window parser commit delta merge  # 20
    queue ref cache blob index cache object token merge token ref stream  # 21
    ref index stream buffer buffer object remote index  # 22
    ref object blob delta rebase ref  # 23
    object window branch index window  # 24
    worker cache object handle blob commit index delta  # 25
    token object commit remote remote queue stream buffer merge queue worker window  # 26
    merge rebase index index tree parser buffer  # 27
    token commit tree tree blob pack worker tree delta parser buffer merge  # 28
    handle parser worker handle pack tree token  # 29
    socket queue tree queue buffer object queue  # 30
    buffer ref parser delta ref rebase window stream token remote  # 31
    tree parser blob index window  # 32
    blob index tree window branch parser object remote branch socket  # 33
    window index header object stream tree object cache  # 34
    rebase commit cache buffer  # 35
    window token delta pack  # 36
    object token window merge remote token buffer  # 37
    queue window window pack  # 38
    window ref pack ref cache  # 39
    window token worker socket handle remote handle rebase parser queue  # 40
    rebase merge window index index token stream ref ref stream  # 41
    commit blob window worker stream worker  # 42
    commit pack delta header remote  # 43
    window merge handle stream parser remote window pack blob index ref  # 44
    parser blob commit buffer object worker stream cache worker token index token  # 45
    header header worker blob  # 46
    token window branch window blob pack stream  # 47
    object rebase delta merge token cache header  # 48
    buffer pack buffer delta commit object token token remote blob  # 49
    ref commit worker merge branch parser  # 50
    socket stream object index queue tree handle stream  # 51
    blob branch buffer parser parser window socket branch cache  # 52
    index buffer worker blob handle delta  # 53
    pack delta window merge  # 54
    blob delta pack worker index remote index header  # 55
