    handle header window index object commit buffer  # 0
    token ref object pack socket parser header  # 1
    header cache pack stream header commit queue pack merge token stream window  # 2
    object blob remote index buffer merge cache parser  # 3
    rebase worker remote socket socket header header index cache worker merge tree  # 4
    blob rebase stream cache object  # 5
    parser cache pack queue  # 6
    rebase delta merge ref window  # 7
    header branch queue commit token delta worker queue header  # 8
    object ref handle worker header worker blob handle handle  # 9
    handle rebase queue pack queue rebase parser window parser branch rebase  # 10
    handle index cache delta worker parser tree remote rebase pack handle  # 11
    token header cache tree tree stream branch stream branch tree queue rebase  # 12
    tree worker header window merge header token rebase worker commit stream blob  # 13
    merge delta merge buffer header pack socket token parser stream remote handle  # 14
    remote stream socket stream buffer token parser object  # 15
    ref rebase token rebase branch cache commit ref commit merge  # 16
    token commit remote pack socket parser stream  # 17
    socket commit token remote commit worker socket merge socket buffer  # 18
    queue branch delta rebase worker index blob  # 19
    token stream merge pack parser handle commit merge parser  # 20
    handle remote remote buffer stream window rebase branch header ref  # 21
    stream socket branch buffer  # 22
    socket buffer ref cache header ref handle index header tree queue  # 23
    cache token buffer queue index rebase remote parser stream blob commit  # 24
    token remote remote tree parser worker tree object ref  # 25
    token pack merge blob merge  # 26
    delta ref object object stream token rebase handle handle pack pack tree  # 27
    commit commit socket tree branch merge object queue socket tree delta stream  # 28
    index remote pack worker tree  # 29
    pack parser parser rebase window blob cache parser socket object cache branch  # 30
    queue blob ref ref queue tree commit commit handle ref  # 31
    branch window tree buffer stream cache merge  # 32
    remote tree queue blob socket socket  # 33
    parser commit queue commit parser remote worker pack  # 34
    merge cache queue commit merge branch commit cache buffer  # 35
    cache merge socket stream buffer cache remote socket index window token  # 36
    parser remote index commit socket object window rebase worker  # 37
    queue remote commit merge remote rebase branch remote pack  # 38
    rebase token queue rebase remote  # 39
    delta commit tree parser stream queue branch socket rebase cache blob ref  # 40
    stream parser buffer tree merge merge worker window commit socket blob token  # 41
    object merge delta handle commit ref stream  # 42
    queue remote delta window  # 43
    ref merge remote worker rebase queue worker ref  # 44
    tree ref tree commit merge worker window stream branch index queue  # 45
    token handle remote remote pack ref delta window worker  # 46
    remote rebase remote merge worker buffer  # 47
    buffer handle delta pack branch  # 48
    window branch token token remote  # 49
    branch cache tree index branch index  # 50
    remote handle remote delta window cache  # 51
    stream token header token handle queue tree parser  # 52
    window stream object buffer delta  # 53
    worker handle commit socket header merge  # 54
    tree delta ref remote object merge rebase token  # 55
    object delta parser header index rebase pack socket buffer  # 56
    index merge branch blob handle blob branch merge parser header  # 57
    rebase header rebase merge  # 58
    delta tree object object token cache cache blob queue merge token  # 59
    branch cache queue tree tree queue  # 60
    merge ref cache socket  # 61
    object worker rebase delta merge delta pack delta  # 62
    ref stream header queue queue rebase remote parser merge pack handle remote  # 63
    pack commit branch delta commit index  # 64
    object stream ref parser rebase socket merge  # 65
    blob remote delta delta worker window parser delta token blob index socket  # 66
    queue pack pack parser index buffer cache  # 67
    worker blob cache index blob object buffer pack stream window cache  # 68
    token parser header pack remote ref window handle socket buffer object cache  # 69
    parser delta cache object tree remote tree index blob buffer blob ref  # 70
    header stream rebase ref merge cache pack object cache buffer token header  # 71
    buffer pack rebase handle pack  # 72
    commit header delta ref queue stream rebase  # 73
