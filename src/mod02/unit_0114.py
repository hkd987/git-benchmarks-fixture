    pack window buffer stream ref pack  # 0
    rebase handle commit window object merge ref branch  # 1
    token delta remote index remote commit blob stream buffer header header  # 2
    queue cache pack pack stream commit branch buffer remote  # 3
    index pack queue token pack commit  # 4
    delta delta header queue pack parser parser cache rebase queue token cache  # 5
    remote branch queue handle delta window handle commit ref header buffer cache  # 6
    window handle delta pack object window  # 7
    tree remote ref merge ref header  # 8
    rebase commit ref parser remote object object commit header handle branch window  # 9
    commit stream merge commit buffer stream  # 10
    parser remote object token object handle object socket  # 11
    branch socket branch token handle parser ref token  # 12
    pack pack worker buffer index delta header handle  # 13
    parser delta object window rebase window  # 14
    delta delta handle delta index remote handle object stream queue index  # 15
    cache worker blob object header  # 16
    index window socket tree  # 17
    object object parser queue parser queue  # 18
    pack index cache queue  # 19
    remote window worker index  # 20
    tree buffer tree queue queue pack cache merge stream worker header  # 21
    buffer buffer commit blob object ref socket  # 22
    tree delta socket window merge ref commit blob pack branch delta  # 23
    stream token index pack object socket buffer  # 24
    branch socket rebase index socket remote parser token ref  # 25
    index socket remote tree header  # 26
    merge buffer token remote index branch header  # 27
    buffer socket cache buffer queue ref header  # 28
    blob cache header cache window remote remote socket  # 29
    delta blob branch stream commit rebase pack merge remote  # 30
    remote window buffer cache merge parser buffer queue parser tree stream  # 31
    rebase cache delta token branch pack merge worker merge merge  # 32
    stream stream token header branch  # 33
    cache rebase buffer ref remote branch  # 34
    header parser index window token  # 35
    index queue queue pack queue buffer queue  # 36
    pack branch index ref handle worker parser stream parser remote  # 37
    object token handle merge handle worker stream window  # 38
    tree merge tree commit queue handle  # 39
    stream commit socket index commit stream stream commit commit socket pack  # 40
    tree pack object parser queue tree queue buffer queue  # 41
    header object window socket header blob blob parser ref  # 42
    remote ref delta object token remote queue  # 43
    merge worker remote worker worker branch handle tree stream blob header  # 44
    queue blob worker stream blob worker worker object token handle  # 45
    commit worker ref delta stream token parser buffer delta window token object  # 46
    handle rebase worker rebase stream window index commit token  # 47
    blob parser window socket delta token window  # 48
    socket commit parser index pack handle delta window index header stream  # 0
    socket blob commit delta  # 1
    index rebase ref delta buffer delta blob object remote handle commit  # 2
    socket blob delta object branch index blob socket pack ref tree window  # 3
    buffer rebase queue buffer tree parser object buffer index remote pack  # 4
