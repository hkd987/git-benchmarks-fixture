    pack tree window queue handle commit commit buffer branch  # 0
    socket remote tree cache ref merge cache object header worker queue blob  # 1
    object header token cache cache pack remote socket parser remote  # 2
    worker delta queue ref handle  # 3
    token token handle handle branch  # 4
    stream pack socket tree queue  # 5
    window stream header pack parser remote buffer merge parser buffer ref  # 6
    buffer merge commit header cache parser cache commit header  # 7
    pack blob token object cache  # 8
    cache cache remote buffer parser blob blob  # 9
    handle delta socket commit cache  # 10
    header handle buffer worker token parser header socket merge window window  # 11
    blob ref tree buffer rebase header handle remote remote  # 12
    blob handle remote stream socket pack branch  # 13
    queue merge token stream handle token  # 14
    socket stream token tree  # 15
    merge commit delta pack window remote  # 16
    handle parser object socket rebase handle object commit cache stream  # 17
    index ref remote index token  # 18
    rebase object remote delta index rebase cache commit delta remote index delta  # 19
    buffer socket ref object stream index  # 20
    ref socket buffer queue  # 21
    buffer index tree pack  # 22
    index socket queue delta index index handle merge commit pack  # 23
    merge blob index rebase window header index rebase commit handle  # 24
    socket object blob object branch rebase ref ref rebase ref parser rebase  # 25
    pack object rebase ref blob rebase merge cache blob  # 26
    merge window pack blob merge socket handle window tree index window  # 27
    stream tree index blob rebase header handle object handle ref worker tree  # 28
    delta worker stream ref rebase window  # 29
    token rebase blob socket stream delta queue  # 30
    buffer branch delta buffer index worker blob header window cache  # 31
    object queue ref index delta queue handle buffer stream token header  # 32
    handle socket window delta header ref  # 33
    delta header queue blob token stream worker object commit worker delta branch  # 34
    stream object worker window handle merge worker queue socket buffer worker stream  # 35
    delta parser window handle cache commit queue rebase worker  # 36
    worker token pack index worker pack handle  # 37
    window cache branch queue branch worker stream ref remote  # 38
    window parser socket header stream window tree pack branch pack header branch  # 39
    window parser blob parser handle parser socket handle commit  # 40
    buffer header buffer blob remote parser header object stream  # 41
    blob object header blob tree tree stream handle commit stream token queue  # 42
    parser parser tree object buffer parser token worker blob  # 43
    tree ref parser window rebase socket pack token parser header  # 44
    delta cache window header handle buffer cache  # 45
    worker parser ref socket socket  # 46
    index delta token handle queue commit buffer  # 47
    header blob buffer merge rebase parser delta queue cache  # 48
    branch tree queue delta handle index queue  # 49
    pack worker commit remote tree parser remote socket delta handle index  # 50
    delta buffer token commit remote delta  # 51
    remote token commit blob queue delta socket ref  # 52
    window ref remote token ref object stream cache  # 53
    window ref ref socket merge worker blob branch branch handle handle stream  # 54
    handle queue header branch commit  # 55
    commit buffer delta object tree  # 56
    blob buffer pack index delta stream commit tree buffer  # 57
    handle window window delta remote  # 58
    ref merge branch index pack remote tree index handle  # 59
    tree commit window socket handle header tree  # 60
    parser pack handle queue queue queue pack tree merge tree  # 61
    rebase branch object worker token  # 62
    blob window queue queue token blob header queue window object  # 63
    queue stream blob socket stream socket  # 64
    object pack socket tree delta object delta blob socket object object cache  # 65
    cache remote parser tree merge handle parser ref  # 66
    window parser remote cache parser branch index worker queue  # 67
    index ref blob queue ref token delta parser  # 68
    worker parser branch header parser branch header pack  # 69
    index stream socket ref delta socket delta ref token blob  # 70
    index remote parser worker parser branch buffer header ref  # 71
    rebase parser blob window delta token rebase socket  # 72
    pack object blob header worker parser branch remote remote  # 73
    pack token tree commit handle blob object handle worker socket merge socket  # 74
    index pack rebase token delta branch stream index object window object  # 75
    stream index ref remote queue token merge parser  # 76
    worker socket queue object  # 77
    buffer token parser delta  # 78
