    remote buffer window branch rebase header remote tree cache queue commit  # 0
    rebase parser cache blob branch window  # 1
    parser parser token handle socket commit branch socket ref delta delta  # 2
    merge window ref object window cache merge object window rebase  # 3
    remote commit remote handle handle object ref index pack branch commit  # 4
    handle merge header queue socket window parser object window window worker cache  # 5
    socket object merge pack  # 6
    object rebase object ref pack remote cache commit rebase ref tree  # 7
    delta object parser ref merge parser cache delta header delta  # 8
    blob parser handle header branch handle worker object  # 9
    delta parser handle delta ref ref parser cache buffer cache index token  # 10
    ref object commit token parser branch branch remote commit header branch blob  # 11
    worker branch queue rebase rebase queue index pack  # 12
    delta merge parser pack handle delta worker  # 13
    window queue stream parser pack pack branch  # 14
    worker pack queue tree object remote  # 15
    socket buffer worker parser  # 16
    commit handle blob handle merge index window parser index  # 17
    branch blob tree rebase  # 18
    rebase merge buffer queue branch  # 19
    cache header buffer pack remote merge object queue cache object delta remote  # 20
    commit buffer queue pack merge branch  # 21
    blob stream cache rebase  # 22
    token commit handle tree  # 23
    branch token parser window delta cache buffer blob cache branch ref header  # 24
    header ref header cache queue commit ref ref commit  # 25
    cache commit pack stream stream delta worker commit handle object  # 26
    queue commit tree stream commit cache object remote branch  # 27
    stream worker pack parser buffer buffer buffer ref blob  # 28
    ref blob commit window tree cache  # 29
    buffer handle buffer rebase buffer tree tree cache header handle cache object  # 30
    header cache pack merge  # 31
    tree buffer branch cache object  # 32
    cache window window commit handle tree handle remote index stream commit  # 33
    commit header index token window  # 34
    index cache stream rebase worker object token parser header token index stream  # 35
    tree remote token delta  # 36
    branch header parser remote window queue  # 37
    commit branch cache object ref  # 38
    token handle remote handle queue window stream ref blob  # 39
    delta index token remote  # 40
    tree stream buffer worker window blob remote parser blob  # 41
    remote branch handle queue pack remote stream parser socket window  # 42
    header branch remote tree ref pack buffer blob ref tree buffer  # 43
    ref tree rebase cache blob remote branch rebase cache blob  # 44
    tree object merge delta parser  # 45
    delta branch buffer ref stream queue header header window  # 46
    socket blob cache window merge blob tree cache  # 47
    remote cache buffer handle stream buffer delta remote rebase  # 48
    rebase token handle parser merge  # 49
    window stream ref pack commit queue ref rebase merge  # 50
    blob cache rebase commit  # 51
    cache object window commit pack socket queue branch buffer ref commit  # 52
    object queue rebase token  # 53
    token window ref buffer cache merge blob handle ref token rebase  # 54
    ref pack window pack queue header worker stream remote index stream blob  # 55
    delta branch handle branch merge pack rebase handle remote  # 56
    merge header merge handle merge parser pack merge index pack parser object  # 57
    token commit header delta commit cache  # 58
    commit pack buffer index  # 59
    ref handle parser tree tree worker  # 60
    cache object delta blob window  # 61
    ref delta ref remote ref rebase header tree  # 62
    header index cache index handle  # 63
