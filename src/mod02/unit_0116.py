    pack stream queue blob object ref parser parser tree delta index window  # 0
    pack pack merge socket  # 1
    index merge stream handle  # 2
    merge delta header ref blob  # 3
    tree delta remote worker buffer window remote delta rebase queue socket  # 4
    delta blob commit parser window ref rebase remote object commit merge  # 5
    tree commit branch index object commit ref  # 6
    pack cache rebase ref tree window pack index worker ref parser branch  # 7
    token object queue ref buffer parser pack queue buffer socket object stream  # 8
    object blob queue parser header object  # 9
    stream queue commit remote  # 10
    cache parser worker parser object handle buffer parser socket pack rebase  # 11
    stream merge branch blob handle handle cache stream  # 12
    window queue rebase stream  # 13
    stream buffer worker index cache window token header  # 14
    parser pack delta token header header commit commit delta socket handle stream  # 15
    queue socket worker pack queue token commit merge commit  # 16
    queue index branch object worker branch ref ref  # 17
    ref commit remote blob queue blob parser ref header object  # 18
    merge handle blob branch rebase blob  # 19
    ref cache delta buffer branch cache stream cache stream  # 20
    blob rebase merge pack merge rebase token handle index index branch  # 21
    header merge commit parser object branch blob socket  # 22
    handle pack commit header pack index delta  # 23
    header header delta stream branch remote object header parser buffer blob commit  # 24
    handle remote delta merge cache window merge  # 25
    index ref remote worker  # 26
    commit window buffer commit handle cache header ref window tree ref  # 27
    buffer cache worker handle commit remote merge queue token header socket  # 28
    index object header worker stream cache delta header window index  # 29
    token blob pack tree remote  # 30
    merge cache commit parser remote stream  # 31
    token branch blob parser socket worker worker buffer pack delta cache index  # 32
    blob socket blob header parser handle stream socket header handle  # 33
    branch handle window index stream index ref stream object rebase pack parser  # 0
    parser queue commit buffer worker  # 1
    window token object blob handle cache parser  # 2
