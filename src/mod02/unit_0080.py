    queue merge delta parser object queue ref worker  # 0
    commit blob pack branch queue token worker cache  # 1
    parser cache blob merge worker rebase commit commit window  # 2
    window merge branch buffer queue cache parser cache rebase rebase cache  # 3
    rebase worker socket tree handle cache branch buffer handle cache parser merge  # 4
    worker delta socket socket handle token buffer socket  # 5
    rebase pack delta header socket branch buffer worker parser  # 6
    tree remote pack token  # 7
    branch parser buffer blob blob remote buffer  # 8
    stream branch buffer remote buffer token handle  # 9
    token buffer stream stream buffer  # 10
    rebase commit object merge branch queue rebase token pack object index  # 11
    cache socket blob stream object remote stream queue worker blob  # 12
    ref token cache commit queue handle worker pack parser branch handle  # 13
    tree blob commit commit cache merge commit remote merge window  # 14
    blob ref ref header window merge header socket  # 15
    queue delta pack cache pack handle stream  # 16
    buffer blob index index  # 17
    token blob merge ref rebase  # 18
    blob merge header merge queue tree token  # 19
    token merge handle parser window  # 20
    index index window queue commit pack token  # 21
    window stream remote worker buffer token  # 22
    delta index index handle stream delta token parser branch  # 23
    buffer branch commit tree  # 24
    delta worker stream queue merge  # 25
    parser worker blob stream index remote tree tree token  # 26
    rebase handle window blob  # 27
    header cache blob window ref ref socket window commit  # 28
    rebase branch worker window commit queue queue worker remote handle object object  # 29
    rebase remote token index ref  # 30
    socket pack queue socket buffer rebase index cache index pack  # 31
    blob handle stream header ref cache queue  # 32
    rebase object header branch commit  # 33
    tree ref header commit commit branch pack  # 34
    delta rebase header ref commit commit tree delta window socket branch  # 35
    worker commit worker ref header stream remote pack queue merge buffer  # 0
    parser tree queue socket buffer token parser object  # 1
    socket queue handle token index tree  # 2
    object index branch buffer worker socket parser window worker socket  # 3
    header tree window stream worker  # 4
    branch queue pack token  # 5
    pack queue socket worker merge buffer worker queue ref socket pack  # 6
    merge socket tree stream pack header buffer ref token object  # 7
    rebase object header ref pack parser pack header ref  # 8
