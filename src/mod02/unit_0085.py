    merge buffer stream queue queue tree  # 0
    blob token remote cache index blob socket branch ref queue remote object  # 1
    merge merge index header handle header  # 2
    token queue handle remote window commit  # 3
    socket token stream queue  # 4
    handle tree branch worker index index parser window  # 5
    commit parser buffer cache object commit object  # 6
    window tree index rebase ref tree  # 7
    tree object ref handle remote handle  # 8
    handle remote window cache stream queue worker queue  # 9
    branch queue worker parser token queue worker cache ref  # 10
    pack token buffer buffer cache header  # 11
    buffer parser socket index  # 12
    buffer branch cache header window handle remote object window  # 13
    token window cache stream worker parser delta rebase queue cache socket  # 14
    token tree pack socket stream index worker  # 15
    ref branch window index queue tree header ref rebase  # 16
    worker index worker socket window index queue cache handle socket blob handle  # 17
    blob handle queue merge parser pack cache handle branch buffer commit rebase  # 18
    object stream delta parser handle  # 19
    pack stream delta remote header tree token delta tree object socket  # 20
    stream delta object commit rebase  # 21
    commit blob delta delta header handle pack  # 22
    queue cache token header pack remote  # 23
    commit stream socket tree tree ref  # 24
    rebase rebase merge blob pack tree remote tree worker  # 25
    window worker merge buffer socket  # 26
    delta remote stream worker pack handle cache worker merge handle stream cache  # 27
    pack object branch merge parser token object blob commit queue object  # 28
    remote remote ref blob cache handle tree pack token  # 29
    token parser token pack cache stream ref branch pack  # 30
    pack socket queue object rebase rebase tree stream  # 31
    rebase delta delta token object stream handle cache tree  # 32
    token object index handle branch stream token commit remote pack parser  # 33
    blob cache socket stream buffer header index socket  # 34
    delta parser token socket merge remote window  # 35
    token handle queue tree ref stream window window  # 36
    tree handle remote tree handle queue token branch merge  # 37
    token cache parser header buffer remote tree commit branch blob cache  # 38
    object worker buffer ref  # 39
    cache delta worker handle worker buffer handle socket parser worker index  # 40
    queue ref token handle blob branch tree queue buffer ref  # 41
    tree merge handle buffer branch socket cache object delta commit delta  # 42
    object socket buffer rebase ref blob index worker delta pack branch cache  # 43
    buffer blob parser token branch index pack  # 44
    ref remote header blob worker blob  # 45
    blob window pack merge  # 46
    worker remote pack window window rebase remote commit index commit  # 47
    window token buffer header remote  # 48
    branch rebase parser ref buffer window buffer remote tree handle queue  # 49
    rebase delta cache worker object rebase merge  # 50
    pack rebase object tree delta buffer branch  # 51
    pack pack worker parser window commit worker branch commit queue pack buffer  # 52
    pack branch parser token object stream tree handle cache token parser  # 53
    worker tree parser index pack index queue window  # 54
    merge worker stream branch window pack remote worker  # 55
    branch socket worker header header  # 56
    parser stream token stream window window worker  # 57
    buffer merge window parser window remote worker tree cache  # 58
    parser worker worker blob merge rebase tree cache delta header socket  # 59
    delta token buffer blob branch queue token token ref object  # 60
    blob branch socket commit pack pack ref remote handle object stream worker  # 61
    handle remote blob window delta remote  # 62
    queue buffer window buffer worker worker pack rebase object index remote  # 63
    delta rebase buffer ref  # 64
    commit worker object token parser handle stream merge stream object worker  # 65
    blob buffer delta branch socket queue socket commit tree rebase  # 66
    window token worker stream  # 67
    branch stream handle branch socket buffer token handle rebase  # 68
    queue parser token remote token remote socket cache stream branch object  # 69
    token buffer token rebase ref queue queue buffer delta buffer delta window  # 70
    ref delta token window branch stream rebase pack  # 71
    parser remote rebase handle stream parser pack  # 72
    header socket socket ref pack remote index stream  # 73
    commit index commit socket blob  # 74
    buffer token object cache remote  # 75
    pack stream cache token branch buffer  # 76
    socket token queue index queue merge merge handle queue  # 77
    socket blob buffer stream handle index delta header handle parser header delta  # 78
    parser pack pack commit parser buffer parser rebase  # 79
    commit stream index blob parser object commit pack delta blob parser pack  # 80
    buffer rebase index index ref window pack socket handle  # 81
    delta branch ref socket pack socket index token delta index ref object  # 82
    window cache object handle merge parser branch token parser merge object  # 83
    index commit token socket blob worker window merge queue window stream handle  # 84
    parser handle blob buffer  # 85
    header token object branch handle cache rebase  # 86
    commit socket commit queue socket token delta  # 87
    tree pack buffer rebase branch window branch parser stream window branch token  # 88
    pack rebase parser parser header socket ref token remote tree token  # 89
