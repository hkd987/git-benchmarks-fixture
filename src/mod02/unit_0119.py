    token socket parser index  # 0
    pack delta parser worker pack tree stream cache branch rebase merge object  # 1
    parser stream object commit remote pack blob cache branch index index  # 2
    handle pack stream worker rebase  # 3
    socket delta socket ref token merge socket object  # 4
    commit remote token branch pack socket socket  # 5
    tree index index merge token cache window rebase  # 6
    token header stream socket queue stream  # 7
    commit remote ref rebase index remote stream rebase blob commit socket delta  # 8
    blob header parser cache commit stream object socket index  # 9
    token branch blob rebase ref token ref parser ref parser stream  # 10
    handle socket token tree socket pack stream remote stream queue worker  # 11
    blob queue object delta merge remote rebase cache remote ref merge token  # 12
    window commit remote ref handle rebase  # 13
    window commit queue cache blob tree cache merge  # 14
    cache delta remote rebase parser window index window  # 15
    object window buffer index ref cache token socket  # 16
    index worker socket tree pack delta remote pack branch parser  # 17
    index worker stream socket  # 18
    queue object window header window blob  # 19
    index handle ref tree handle parser  # 20
    pack tree blob remote token delta tree object branch  # 21
    rebase index blob branch cache branch object object cache  # 22
    handle remote token queue blob rebase socket  # 23
    commit stream token worker  # 24
    tree socket tree tree header window index pack blob rebase  # 25
    delta buffer commit merge stream pack cache worker  # 26
    pack token parser header header parser header  # 27
    commit header merge blob remote merge delta commit window branch merge cache  # 28
    merge ref token blob queue  # 29
    delta object rebase remote blob blob rebase  # 30
    socket pack pack socket remote  # 31
    blob tree blob worker header  # 32
    worker branch window stream parser ref queue  # 33
    socket cache ref handle token socket  # 34
    commit cache cache token pack parser ref ref ref worker object  # 35
    handle rebase window token rebase ref  # 36
    index commit window rebase  # 37
    buffer socket commit cache stream rebase object merge stream rebase worker delta  # 38
    socket token parser header pack object parser token merge stream  # 39
    worker rebase stream socket  # 40
    stream cache delta commit merge stream  # 41
    branch queue token object index pack parser window rebase  # 42
    window stream header index socket remote  # 43
    pack window branch index branch  # 44
    header blob buffer window tree parser ref buffer window header blob cache  # 45
    ref object buffer rebase socket blob header worker token stream  # 46
    object commit socket parser stream parser socket header queue  # 47
    commit rebase rebase handle  # 48
    socket tree cache tree tree  # 49
    parser stream ref delta window  # 50
    token object branch commit window  # 51
    window stream handle blob tree socket parser  # 52
    delta branch tree handle header rebase ref worker  # 53
    handle header object object token index blob index pack stream worker ref  # 54
    worker object handle delta tree remote worker  # 55
    handle socket token worker worker merge  # 56
    merge socket stream pack parser delta queue  # 57
    index branch delta buffer handle token merge delta commit  # 58
    window pack merge blob handle window rebase commit buffer socket  # 59
    object ref tree index header blob  # 60
    remote window merge object merge rebase parser  # 61
    ref object worker token object index worker header  # 62
    queue parser index socket  # 63
    token cache pack ref window remote remote  # 64
    parser queue cache delta pack blob handle pack  # 65
    ref socket socket header tree queue ref stream header blob remote  # 66
    merge pack parser delta  # 67
    header branch ref delta cache blob commit branch queue remote object  # 68
    parser stream remote tree header remote object pack merge token merge  # 69
    index token header buffer commit blob tree  # 70
    branch merge pack stream buffer token tree  # 71
    branch socket worker buffer buffer delta window commit delta branch parser  # 72
    delta delta ref object worker blob rebase branch  # 73
    branch token cache ref branch  # 74
    index socket parser worker branch blob header stream delta parser  # 75
    stream buffer stream parser object delta ref token cache rebase handle delta  # 76
    ref socket delta window header queue worker ref pack worker buffer merge  # 77
    tree buffer index parser handle socket object  # 78
    merge token commit object pack window  # 79
    handle token queue pack parser merge tree  # 80
    handle merge handle parser delta object parser token stream window buffer  # 0
    branch pack token parser object tree cache commit delta remote index  # 1
    buffer delta stream ref stream index header  # 2
    blob token blob worker parser  # 3
    socket rebase delta cache token commit object merge delta  # 4
    pack remote socket commit  # 5
    ref worker header blob handle queue socket  # 6
    ref ref worker index delta  # 7
    window delta stream buffer index rebase handle  # 8
    handle worker merge remote remote delta  # 9
    handle buffer merge window index buffer rebase stream stream rebase  # 10
