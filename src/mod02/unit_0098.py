    index remote object socket index window stream branch tree queue commit  # 0
    tree header header pack window ref handle buffer blob pack header buffer  # 1
    branch token handle header buffer rebase window remote  # 2
    queue queue ref remote rebase header ref token stream queue cache tree  # 3
    socket pack commit pack  # 4
    stream socket header blob object object  # 5
    socket remote remote remote socket parser buffer window  # 6
    buffer blob remote handle window  # 7
    token token window blob remote branch window pack header rebase delta  # 8
    socket cache window rebase object delta tree  # 9
    branch merge ref handle stream rebase pack  # 10
    cache tree tree cache rebase tree pack delta cache rebase  # 11
    token remote merge queue merge worker stream token socket stream tree token  # 12
    tree worker branch index header  # 13
    commit stream branch buffer pack index rebase  # 14
    delta blob stream blob token ref buffer worker delta stream blob header  # 15
    parser parser delta stream token  # 16
    window object socket parser queue handle commit  # 17
    blob buffer remote pack delta header merge socket remote object index remote  # 18
    stream buffer token index merge  # 19
    queue object token buffer parser cache object parser rebase object  # 20
    worker worker commit header pack ref delta socket queue commit  # 21
    rebase header ref ref delta handle delta  # 22
    header branch commit delta header blob commit pack stream header  # 23
    pack index ref delta pack worker socket pack  # 24
    tree rebase parser object parser object pack commit merge object token  # 25
    cache ref cache tree remote stream handle  # 26
    commit parser ref merge buffer tree header socket  # 27
    pack branch blob cache delta object rebase handle  # 28
    pack remote header queue ref handle tree  # 29
    merge delta stream index  # 30
    ref tree worker rebase stream  # 31
    cache parser object header object handle branch tree  # 32
    commit pack object index rebase rebase remote socket socket delta  # 33
    parser blob remote buffer merge commit tree  # 34
    parser header remote rebase header merge remote  # 35
    ref remote object rebase cache rebase  # 36
    window branch tree parser merge  # 37
    stream socket tree handle window socket merge worker cache index tree blob  # 38
    queue parser worker object remote ref rebase buffer  # 39
    worker socket tree cache worker  # 40
    delta parser blob commit stream queue object rebase  # 41
    buffer index rebase buffer index  # 42
    remote remote delta socket header blob rebase commit stream window token  # 43
    parser header worker commit handle parser queue socket remote cache  # 44
    object branch queue index merge token socket remote commit ref  # 45
    branch commit parser index header index header parser  # 46
    worker blob token remote ref  # 47
    worker token delta worker  # 48
    commit rebase parser buffer stream buffer window object cache index  # 49
    delta commit queue remote  # 50
    window cache parser ref remote ref remote merge merge queue branch  # 51
    tree handle worker tree socket remote stream ref parser rebase stream tree  # 52
    commit remote rebase handle socket  # 53
    delta tree header remote delta ref stream object  # 54
    tree blob remote commit cache ref worker remote  # 55
    header pack merge delta tree header socket token merge rebase stream token  # 56
    commit cache pack buffer token object stream handle window index queue branch  # 57
    tree remote object token  # 58
    buffer parser worker delta  # 59
    pack tree delta buffer object window index ref parser  # 60
    handle ref worker token  # 61
    blob tree worker merge worker  # 62
    tree cache merge remote merge  # 63
    cache buffer ref handle commit buffer header blob  # 64
    buffer object commit pack pack index  # 65
    object token queue stream stream window merge stream object  # 66
    ref window parser token  # 67
    commit remote commit window commit merge  # 68
    merge window object stream blob socket tree parser commit merge buffer pack  # 69
