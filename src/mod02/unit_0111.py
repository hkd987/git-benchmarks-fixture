    branch token token remote object blob delta stream queue rebase token buffer  # 0
    delta header token buffer worker ref parser index stream queue queue remote  # 1
    queue tree object header tree stream header header branch commit parser  # 2
    header handle branch branch handle  # 3
    buffer object socket delta window parser queue  # 4
    header socket stream object index branch stream  # 5
    window parser buffer parser index remote token token header  # 6
    stream ref ref worker parser cache object object queue stream delta  # 7
    header merge worker ref remote worker branch buffer pack  # 8
    queue object header window stream pack token blob socket handle delta window  # 9
    socket worker commit index socket buffer stream blob header tree pack  # 10
    merge index pack index token buffer remote header  # 11
    cache delta tree token queue  # 12
    index cache header handle stream remote tree token blob handle  # 13
    socket buffer parser object commit token blob merge pack  # 14
    buffer object branch ref merge worker ref worker commit queue header cache  # 15
    rebase token merge branch stream socket pack token ref blob  # 16
    branch header handle cache commit token delta pack  # 17
    cache parser branch remote index commit commit buffer header remote buffer  # 18
    header rebase window cache object blob delta socket commit cache  # 19
    merge merge delta worker token ref pack  # 20
    token socket worker ref pack ref socket token blob merge cache  # 21
    token merge blob tree stream object branch merge  # 22
    commit index worker rebase handle parser blob window remote  # 23
    blob pack ref delta index branch commit ref token pack blob buffer  # 24
    pack socket tree object rebase blob blob  # 25
    worker cache worker object object index delta buffer ref stream token handle  # 26
    window blob delta worker ref header merge stream  # 27
    delta window blob parser index index blob window remote rebase rebase  # 28
    tree merge rebase stream tree worker worker window  # 29
    token index handle worker handle cache token pack delta  # 30
    handle object rebase object tree  # 31
    branch buffer index handle remote worker  # 32
    token object token parser queue  # 33
    remote socket window cache ref index pack pack  # 34
    ref object parser remote buffer index worker rebase branch commit  # 35
    index stream delta rebase merge header worker remote  # 36
    commit commit handle parser worker object merge delta  # 37
    cache merge index delta  # 38
    object queue tree header pack token handle queue stream  # 39
    delta parser index parser token branch buffer cache parser  # 40
    blob window object branch rebase delta  # 41
    index stream index socket parser socket  # 42
    stream branch pack stream delta object commit index index buffer parser  # 43
    blob parser merge handle blob  # 44
    object buffer index queue worker parser socket buffer buffer blob window header  # 45
    window pack branch stream buffer  # 46
    merge tree worker worker window delta ref remote pack ref  # 47
