    blob header token ref parser buffer socket pack stream blob index  # 0
    ref handle blob token cache pack parser handle  # 1
    branch branch pack merge pack rebase worker index remote buffer handle stream  # 2
    blob remote object stream socket worker header delta worker pack  # 3
    buffer parser socket token worker ref tree parser worker index parser stream  # 4
    index delta object ref token parser  # 5
    branch buffer remote parser window  # 6
    merge merge delta pack  # 7
    blob rebase delta delta window  # 8
    header window buffer stream remote worker tree ref merge  # 9
    merge header rebase token cache queue index stream handle  # 10
    buffer buffer window blob  # 11
    commit pack token buffer socket handle socket rebase window header  # 12
    tree remote stream queue branch object worker branch  # 13
    delta commit tree window cache  # 14
    buffer parser cache delta token socket commit branch  # 15
    cache queue pack pack delta buffer  # 16
    queue ref window window object tree socket buffer commit cache  # 17
    commit delta handle stream window worker header  # 18
    token tree delta queue blob ref buffer token  # 19
    object socket rebase queue header  # 20
    rebase handle tree stream tree merge handle pack  # 21
    handle cache delta blob queue window blob commit header ref  # 22
    index stream delta merge header pack remote remote cache worker  # 23
    merge parser header buffer commit remote cache blob  # 24
    blob remote rebase buffer ref tree ref branch commit pack queue  # 25
    header socket index tree pack token blob cache buffer object  # 26
    parser blob commit stream rebase rebase  # 27
    queue remote branch handle blob socket merge pack branch blob  # 28
    window remote cache token cache remote token parser delta rebase ref  # 29
    commit token stream blob  # 30
    delta branch handle rebase parser ref rebase tree object merge branch  # 31
    blob socket pack delta cache stream index header  # 32
    buffer ref queue object object commit queue remote tree object object blob  # 33
    tree window ref queue index stream  # 34
    remote blob header pack commit buffer socket delta blob object merge cache  # 35
    socket socket branch delta token commit blob stream delta worker  # 36
    socket merge index delta ref header worker  # 37
    parser token rebase pack buffer object handle parser parser  # 38
    ref pack ref queue merge object cache window tree  # 39
    buffer merge tree header blob index buffer object pack rebase  # 40
    object worker branch tree remote branch merge commit queue queue worker handle  # 41
    buffer cache window rebase buffer commit ref  # 42
    queue queue parser tree window cache pack stream commit commit worker token  # 43
    queue token rebase rebase remote tree token blob commit token token  # 44
    queue handle index branch object window cache  # 45
    delta cache object cache branch stream branch  # 46
    token tree rebase queue stream index parser tree queue  # 47
    tree buffer parser delta blob delta delta commit  # 48
    cache buffer rebase stream queue remote buffer  # 49
    index cache queue tree  # 50
    worker delta window handle commit header blob cache token parser pack rebase  # 51
    commit delta index socket commit tree ref pack  # 52
    window pack window branch delta delta  # 53
    index branch handle blob buffer token  # 54
    tree handle commit delta rebase commit window merge  # 55
    remote commit socket delta window socket token token commit buffer  # 56
    window tree commit merge handle socket delta  # 57
    queue parser cache token merge delta delta stream  # 58
    blob delta tree commit pack  # 59
    rebase rebase pack blob worker buffer object index worker queue index  # 60
    pack index token token ref buffer pack cache queue  # 61
    window remote tree remote blob object index commit pack header  # 62
    delta remote socket tree remote buffer  # 63
    branch remote tree queue rebase parser index queue stream  # 64
    socket cache ref blob tree  # 65
    ref window cache parser pack delta ref  # 66
    rebase cache token blob  # 67
    buffer worker blob remote tree ref window pack  # 68
    remote cache buffer cache  # 69
    remote merge header blob  # 70
    merge handle ref buffer object socket token header socket blob queue  # 71
    cache object blob branch ref remote cache token delta  # 72
    tree cache buffer socket  # 73
    parser stream header object rebase  # 74
    header index buffer merge stream pack  # 75
    merge header branch cache rebase blob queue delta  # 76
    index branch ref rebase worker ref worker handle cache  # 77
    ref object parser commit delta blob window cache object  # 78
    object pack delta queue header delta handle delta delta pack stream  # 79
    header pack socket blob token  # 80
