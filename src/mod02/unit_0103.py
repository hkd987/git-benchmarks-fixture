    merge header parser queue handle buffer parser stream header object  # 0
    merge worker object queue buffer object index pack header buffer commit token  # 1
    delta stream ref index remote stream cache  # 2
    parser parser pack merge cache pack remote index buffer cache stream  # 3
    queue pack handle header worker commit delta  # 4
    remote index header rebase socket rebase  # 5
    branch commit buffer blob cache window merge blob buffer object branch merge  # 6
    worker tree header handle  # 7
    worker delta handle window commit index window window blob index remote handle  # 8
    stream delta stream blob ref header merge worker commit  # 9
    tree parser pack commit cache handle blob handle blob worker delta  # 10
    parser queue merge queue parser cache index  # 11
    object handle remote queue worker rebase commit window  # 12
    socket worker cache stream ref tree header token remote  # 13
    stream stream ref pack socket index merge ref remote remote ref  # 14
    header delta token handle parser socket window commit index merge  # 15
    commit stream buffer index index worker delta object stream  # 16
    worker rebase token merge rebase pack remote remote branch  # 17
    blob index commit stream  # 18
    delta branch token merge token queue parser object branch  # 19
    parser blob pack delta index  # 20
    cache index parser worker handle token stream commit delta ref handle  # 21
    object header merge blob remote remote socket index object  # 22
    index window commit header ref token  # 23
    handle branch index parser queue delta  # 24
    cache pack ref token commit ref parser  # 25
    header remote blob commit commit blob header object worker worker commit  # 26
    pack commit index object pack object remote  # 27
    tree index socket tree index ref pack remote blob  # 28
    handle rebase parser header parser parser pack stream branch socket delta  # 29
    delta handle socket pack merge worker remote  # 30
    socket handle buffer stream header remote worker parser remote buffer delta  # 31
    delta handle handle merge  # 32
    branch token blob cache cache header  # 33
    index worker ref merge stream tree window socket parser index  # 34
    window queue handle branch  # 35
    blob tree socket blob blob commit socket branch header stream window  # 36
    parser queue worker header merge window  # 37
    socket delta ref handle blob queue handle  # 38
    pack stream pack delta cache socket handle commit branch parser ref cache  # 39
    commit queue cache header socket commit  # 40
    buffer worker parser queue  # 41
    worker worker worker delta pack stream  # 42
    socket parser remote merge blob window cache window  # 43
    ref cache ref rebase socket merge pack commit remote merge branch  # 44
    worker blob commit delta header commit handle merge token  # 45
    window object object merge parser  # 46
    window buffer window object delta cache object  # 47
    pack pack tree window tree object commit commit  # 48
    handle header blob token delta pack stream window stream buffer remote delta  # 49
    tree delta index window socket object parser  # 50
    remote header rebase token remote  # 51
    index rebase branch handle header parser token header rebase window branch worker  # 52
    ref handle cache remote tree cache header rebase  # 53
    window handle buffer merge cache object header token  # 54
    window rebase queue blob stream delta commit commit header queue  # 55
    delta parser ref blob pack cache ref stream queue index remote  # 56
    handle buffer buffer blob blob merge  # 57
    buffer handle token buffer  # 58
    delta remote buffer object token delta merge tree  # 59
    queue rebase parser worker  # 60
    queue ref object buffer handle parser tree stream remote delta  # 61
    stream buffer commit worker  # 62
    handle queue buffer handle pack handle parser delta  # 63
    tree queue queue object token remote  # 64
    commit ref remote stream  # 65
    object socket parser pack  # 66
    token token parser socket branch pack socket blob window worker  # 67
    stream remote commit queue merge  # 68
    window tree pack parser queue commit queue  # 69
    parser index handle cache header worker rebase header  # 70
    socket remote delta merge token ref blob index socket  # 71
    tree rebase worker delta handle ref handle commit stream parser  # 72
    queue delta tree socket index object commit parser object  # 0
    branch tree merge commit  # 1
    window rebase header queue delta  # 2
    token tree buffer branch tree commit object queue  # 3
    worker socket object token tree window object branch object token  # 4
    header remote token buffer  # 5
    commit index queue header worker remote  # 6
    window socket delta worker socket rebase handle window rebase rebase  # 7
    token handle branch rebase merge  # 8
    queue parser tree stream pack  # 9
