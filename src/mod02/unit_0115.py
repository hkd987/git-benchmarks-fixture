    delta ref delta pack handle handle  # 0
    token index pack pack  # 1
    branch stream tree merge object cache window handle blob  # 2
    branch buffer merge branch commit cache ref tree handle blob delta rebase  # 3
    buffer header blob commit tree header handle rebase branch  # 4
    blob commit tree commit queue worker  # 5
    header object remote ref pack rebase object worker buffer pack ref handle  # 6
    header ref delta worker buffer buffer branch socket blob branch delta pack  # 7
    rebase parser delta window queue stream merge delta queue merge  # 8
    buffer token pack merge blob branch stream blob ref ref index  # 9
    queue stream window worker index  # 10
    delta parser blob index  # 11
    merge merge object handle delta rebase socket tree tree  # 12
    socket remote object window window index commit merge buffer worker blob  # 13
    delta handle worker ref header worker parser socket parser cache ref remote  # 14
    header tree pack merge stream stream  # 15
    remote handle commit cache merge buffer stream stream object commit pack  # 16
    handle rebase merge window token header buffer ref rebase remote commit  # 17
    pack remote parser commit  # 18
    header branch tree branch parser remote token worker commit socket window delta  # 19
    merge object tree parser merge stream  # 20
    handle ref index blob delta header header worker parser branch  # 21
    token stream ref cache cache queue cache merge  # 22
    buffer commit tree header rebase pack token remote  # 23
    index buffer header index ref window  # 24
    commit header parser queue tree queue commit blob object delta rebase  # 25
    branch token buffer pack header  # 26
    rebase remote header branch  # 27
    delta header parser token tree  # 28
    ref token remote handle queue queue ref ref  # 29
    handle parser object pack branch socket object blob cache index  # 30
    handle index stream object branch parser token commit rebase index parser  # 31
    ref remote stream buffer ref worker  # 32
    index handle buffer socket blob rebase stream object ref merge token  # 33
    merge index index socket ref header rebase  # 34
    commit remote header buffer tree ref object worker  # 35
    buffer queue queue commit socket stream window handle  # 36
    worker rebase delta worker branch rebase remote merge merge  # 37
    parser header token branch  # 38
    worker object header token worker index ref stream delta blob blob  # 39
    ref queue remote token commit ref blob token handle merge parser token  # 40
    queue rebase queue object stream commit header  # 41
    worker worker token token queue remote parser parser  # 42
    pack stream branch buffer remote cache merge blob  # 43
    queue object commit commit cache worker queue  # 44
    pack commit ref parser commit delta remote tree commit queue worker token  # 45
    handle branch header remote index window  # 46
    merge index handle worker pack rebase queue buffer rebase remote index tree  # 47
    rebase handle handle index stream branch queue  # 48
    token socket queue object  # 49
    parser ref remote handle window blob pack rebase queue window  # 50
    token stream parser header branch commit buffer rebase worker  # 51
    queue ref worker token  # 52
    cache header handle stream worker worker ref  # 53
    branch blob cache remote ref window rebase parser  # 54
    commit blob header merge rebase handle worker stream commit remote cache socket  # 55
    blob parser commit queue  # 56
    cache blob branch tree worker token buffer  # 57
    worker cache branch ref blob  # 58
    socket stream branch worker commit blob cache  # 59
    ref token worker socket remote  # 60
    rebase stream delta parser merge index rebase parser worker cache  # 0
    window commit stream handle rebase pack  # 1
    queue object queue rebase index  # 2
    parser queue ref remote ref parser queue header stream remote buffer  # 3
    buffer pack socket buffer delta commit delta  # 4
    commit parser object cache header index parser  # 5
    pack pack handle socket  # 6
    header handle branch ref index  # 7
    pack index ref rebase commit socket remote rebase pack delta merge window  # 8
    buffer queue header ref stream index  # 9
    rebase header header branch commit  # 10
