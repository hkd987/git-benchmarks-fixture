    merge object buffer ref branch rebase cache  # 0
    queue object worker blob worker  # 1
    blob rebase window remote blob rebase delta handle token  # 2
    buffer rebase commit index delta rebase  # 3
    blob stream handle token header handle pack pack commit ref  # 4
    queue buffer parser branch index ref cache  # 5
    ref branch blob parser pack ref window header parser  # 6
    pack blob token stream cache blob stream stream  # 7
    stream parser blob window cache blob worker pack tree blob  # 8
    tree rebase ref pack branch tree handle token  # 9
    cache window parser cache merge cache  # 10
    parser stream parser queue object header commit socket window delta  # 11
    window rebase ref branch  # 12
    queue object queue token parser merge index blob blob commit header blob  # 13
    delta ref header object pack  # 14
    cache pack handle delta object worker blob worker delta  # 15
    merge remote worker blob parser blob worker tree handle blob  # 16
    worker index blob queue queue socket branch rebase  # 17
    header buffer window rebase  # 18
    handle rebase handle handle token merge rebase worker commit window object  # 19
    token cache commit tree window window rebase buffer socket queue  # 20
    handle object window socket worker merge pack blob socket branch blob  # 21
    handle commit parser commit parser parser tree buffer buffer pack  # 22
    delta parser handle object pack  # 23
    queue delta buffer pack handle commit queue  # 24
    remote cache queue object cache  # 25
    queue tree remote commit remote ref buffer rebase tree window  # 26
    window tree worker commit commit rebase blob delta blob  # 27
    remote delta blob object stream delta window cache remote  # 28
    buffer stream header branch  # 29
    stream token worker buffer rebase rebase  # 30
    rebase delta token token  # 31
    index pack ref parser index  # 32
    commit header remote header header buffer merge queue  # 33
    tree parser worker blob  # 34
    merge tree token tree blob index merge rebase branch delta remote commit  # 35
    tree buffer queue socket rebase buffer  # 36
    handle remote tree token merge object header header  # 37
    pack tree blob pack token buffer pack window commit  # 38
    socket index remote pack  # 39
    buffer header index buffer branch worker commit branch buffer socket  # 40
    branch queue object cache socket  # 41
    parser pack window token token tree buffer buffer  # 42
    remote buffer buffer merge window blob  # 43
    socket blob cache handle blob handle  # 44
    token worker window rebase pack blob handle pack  # 45
    blob cache queue pack parser window  # 46
    ref handle cache header  # 47
    delta handle ref object  # 48
    buffer ref window header tree socket rebase socket ref handle  # 49
    blob window rebase rebase buffer pack object blob remote worker stream worker  # 50
    merge buffer socket header worker queue worker blob header remote  # 51
    token window header header worker  # 52
    buffer stream blob buffer ref buffer worker token merge  # 53
    handle commit rebase queue index remote branch index  # 54
    remote socket rebase merge rebase  # 55
    worker worker index commit object  # 56
    branch stream remote blob window worker index  # 57
    token object delta worker window blob commit  # 58
    object delta parser header buffer  # 59
    delta queue ref socket queue buffer handle worker socket merge  # 60
    object ref worker socket header  # 61
    queue socket socket delta  # 62
    rebase socket rebase handle rebase  # 63
    window pack token ref socket  # 64
    cache cache parser branch index blob commit branch delta ref token  # 65
    ref blob cache delta rebase handle delta remote queue  # 66
    queue ref worker parser index merge window window handle  # 67
    pack token socket token branch object ref parser worker  # 68
    stream window object remote branch worker  # 69
    queue queue pack queue window stream ref blob worker index cache ref  # 70
    pack tree queue branch parser  # 71
    object cache stream tree socket  # 72
    cache token branch ref blob window  # 73
    rebase handle rebase parser index blob worker delta header ref  # 74
    window parser ref window  # 75
    header blob commit socket rebase queue worker  # 76
    handle stream socket cache tree  # 77
    handle buffer rebase handle token handle rebase buffer handle  # 0
    commit tree pack cache object ref commit rebase window blob buffer  # 1
    buffer tree rebase ref blob  # 2
    tree merge tree object  # 3
    buffer pack stream delta pack stream worker delta index remote  # 4
    rebase ref cache commit stream stream stream tree socket  # 0
    rebase object branch ref stream delta window header  # 1
    rebase header blob blob worker  # 2
