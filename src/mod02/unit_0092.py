    header worker ref worker stream branch buffer cache ref delta buffer merge  # 0
    object token commit window tree  # 1
    cache token window remote pack stream socket tree delta  # 2
    parser object index stream handle blob commit window tree cache index pack  # 3
    worker header handle blob  # 4
    remote window rebase worker window  # 5
    rebase branch branch branch buffer rebase tree stream queue  # 6
    delta handle handle remote queue pack buffer remote  # 7
    socket token cache ref queue  # 8
    stream merge rebase blob socket tree  # 9
    pack object socket rebase commit blob pack rebase buffer blob  # 10
    window commit tree pack  # 11
    remote ref buffer token  # 12
    ref stream buffer socket commit window  # 13
    branch cache token header ref  # 14
    blob header commit socket ref branch worker index stream buffer  # 15
    ref delta delta token token handle rebase rebase  # 16
    socket worker token cache branch window ref object window  # 17
    stream ref pack token header merge object pack socket token merge  # 18
    buffer rebase parser cache window stream tree  # 19
    blob header cache rebase token token token commit cache parser parser  # 20
    queue delta object branch  # 21
    socket tree index tree tree index handle header stream  # 22
    branch buffer buffer token tree  # 23
    index parser parser blob cache  # 24
    commit object branch branch window remote queue header  # 25
    object remote ref object index header tree  # 26
    pack ref blob index buffer remote handle  # 27
    stream remote index socket socket object queue remote handle blob  # 28
    socket remote worker worker  # 29
    parser worker remote commit commit handle ref worker buffer  # 30
    rebase ref delta handle stream parser  # 31
    handle blob socket branch branch merge object parser socket cache token worker  # 32
    handle header handle branch merge socket blob branch commit commit window branch  # 33
    object tree delta object  # 34
    queue queue ref socket parser tree commit object worker cache window  # 35
    header socket parser branch handle object socket handle delta socket  # 36
    socket tree buffer stream token stream socket header token  # 37
    blob index remote stream queue buffer commit cache header handle  # 38
    blob parser pack merge cache ref  # 39
    queue socket token branch window delta token pack  # 40
    merge ref pack branch  # 41
    rebase rebase blob worker object buffer object  # 42
    token handle object window header commit tree remote  # 43
    delta commit blob branch stream cache  # 44
    merge blob commit remote pack handle  # 45
    handle buffer ref stream rebase ref header ref pack parser  # 46
    branch buffer commit branch  # 47
    header queue tree tree remote worker tree blob window  # 48
    parser queue handle stream pack object pack queue stream rebase window index  # 49
    stream socket buffer delta stream socket  # 50
    worker socket header object commit cache handle delta  # 51
    rebase worker cache cache  # 52
    parser merge window delta header header window commit worker  # 53
    ref commit merge delta delta worker window merge remote index  # 54
    window handle token header index header object remote queue window commit branch  # 55
    tree tree worker header remote tree header parser blob  # 56
    handle handle object commit rebase branch  # 57
    worker handle handle parser token  # 58
    header buffer token handle blob socket socket header handle  # 59
    window cache window window  # 60
    ref tree pack commit branch tree token remote window  # 61
    tree delta blob ref rebase ref branch ref window cache commit blob  # 62
    window branch parser object handle  # 63
    delta stream rebase rebase worker merge header delta branch blob header  # 64
    buffer commit remote socket branch delta  # 65
    blob queue stream pack rebase tree object pack  # 66
    blob header ref header remote worker stream tree  # 67
    index window cache header index token parser queue worker merge branch  # 68
    socket pack cache stream window rebase commit stream window remote  # 69
    queue queue blob branch remote rebase socket  # 70
    object stream remote ref tree rebase handle token token buffer blob remote  # 71
    ref index handle stream pack commit parser delta cache ref delta  # 72
    ref tree rebase rebase stream  # 73
    object blob remote stream window index object merge index rebase stream  # 74
    handle header pack queue worker parser buffer  # 75
    pack merge delta stream commit  # 76
    handle handle socket cache token  # 77
    socket worker stream handle  # 78
    branch window merge remote  # 79
    remote cache parser stream commit object buffer  # 80
    socket rebase header ref  # 81
    branch branch cache socket queue cache merge handle worker object ref ref  # 82
    branch pack tree header  # 83
    object token stream merge object  # 84
    delta buffer cache delta delta  # 85
    tree header index object merge queue  # 86
    header parser worker branch window queue object index buffer  # 87
