    buffer token handle token stream object merge  # 0
    delta index pack socket parser socket remote  # 1
    object tree pack rebase rebase  # 2
    parser delta delta buffer delta stream object remote  # 3
    stream delta index merge merge remote  # 4
    delta remote ref handle remote stream worker queue  # 5
    buffer index window blob stream token parser blob  # 6
    parser index header queue branch tree index handle cache  # 7
    merge token stream parser object remote handle token queue worker window  # 8
    stream commit queue delta branch window branch ref object window window  # 9
    queue ref window header window tree  # 10
    blob ref handle header  # 11
    socket header queue remote  # 12
    tree token stream object index  # 13
    header cache ref handle token cache object buffer tree delta parser index  # 14
    merge remote delta blob tree parser commit  # 15
    index socket pack queue window token cache cache  # 16
    remote cache header branch token merge buffer remote ref  # 17
    parser ref rebase header object rebase ref remote window ref  # 18
    handle delta pack token blob parser commit parser token handle header  # 19
    window commit ref branch window commit pack worker merge socket token  # 20
    stream cache commit remote delta pack worker commit object stream stream merge  # 21
    queue remote queue branch  # 22
    socket parser window handle handle tree window blob  # 23
    ref branch object delta  # 24
    cache object handle stream rebase ref parser tree blob  # 25
    remote blob buffer rebase stream merge blob delta parser  # 26
    ref stream index parser socket branch  # 27
    merge stream ref queue handle tree window header queue buffer  # 28
    queue merge window window buffer rebase branch blob  # 29
    branch branch merge blob object  # 30
    window handle rebase worker rebase index worker commit window pack  # 31
    window stream index blob  # 32
    index branch token parser buffer object socket blob handle rebase  # 33
    object stream socket remote queue  # 34
    commit queue index tree branch merge commit blob ref parser tree  # 35
    blob delta handle pack header worker remote merge socket blob  # 36
    window branch branch header buffer queue ref queue token cache  # 37
    cache branch cache socket header delta commit commit worker rebase merge stream  # 38
    stream commit header queue header buffer stream object  # 39
    parser commit blob object index handle worker  # 40
    handle queue header cache index commit  # 41
    pack stream rebase merge token handle delta rebase  # 42
    token merge object cache commit ref header socket delta  # 43
    object cache buffer cache object remote commit  # 44
    ref stream parser rebase  # 45
    socket handle commit merge window delta handle window socket  # 46
    socket rebase queue worker  # 47
    delta stream delta buffer queue  # 48
    queue object commit token blob ref commit ref remote token handle  # 49
    object tree window handle remote buffer tree ref stream blob socket branch  # 50
    pack cache pack rebase index handle ref header  # 51
    commit parser pack rebase socket header buffer  # 52
    blob remote pack stream worker buffer remote ref queue object queue buffer  # 53
    cache commit worker pack worker branch object header worker delta index delta  # 54
    window commit stream stream merge delta token socket header index commit pack  # 55
    blob merge worker commit  # 56
    stream socket tree delta socket remote queue pack buffer parser worker buffer  # 57
    window handle parser handle ref header  # 58
    window blob cache token branch queue  # 59
    delta handle window tree window object header  # 60
    queue window worker handle parser parser merge rebase  # 61
    tree handle rebase token header object  # 62
    queue blob handle object branch blob remote token pack tree blob merge  # 63
    remote buffer queue tree  # 64
    commit object remote worker remote blob queue  # 65
    tree object cache merge pack pack index  # 66
    delta header socket buffer stream  # 67
    object tree merge token buffer  # 68
    window window queue socket delta ref  # 69
    handle tree queue branch remote header stream parser token header blob  # 70
    remote ref stream token blob commit socket token  # 71
    merge parser parser index queue merge object object blob queue ref  # 72
    token merge window commit merge index  # 73
    token tree token blob handle delta queue  # 74
    handle index pack handle delta  # 75
    commit window delta pack commit pack commit  # 76
    pack header queue index blob branch window pack branch  # 77
    merge rebase queue pack window worker ref commit index delta  # 78
    stream remote queue branch commit tree  # 0
    remote rebase commit remote merge commit delta queue ref object  # 1
    branch rebase window blob index parser  # 2
    blob socket pack delta  # 3
    rebase worker queue blob tree header commit commit cache tree pack  # 4
    buffer buffer header window object object blob window ref blob  # 5
