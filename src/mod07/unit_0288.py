    ref handle commit worker merge rebase parser  # 0
    window header rebase remote  # 1
    merge object window ref index buffer rebase  # 2
    object buffer merge socket  # 3
    queue branch ref pack  # 4
    tree header worker buffer header  # 5
    stream queue blob parser  # 6
    socket ref blob queue  # 7
    worker token stream stream delta parser ref tree handle cache pack  # 8
    rebase cache blob queue window pack handle remote  # 9
    remote window tree object rebase token object  # 10
    token header queue cache object window  # 11
    tree commit token merge commit handle remote window  # 12
    commit stream merge parser  # 13
    handle token buffer token queue window  # 14
    index pack parser cache worker remote merge commit  # 15
    worker window window queue socket cache  # 16
    buffer branch tree merge window socket stream object commit rebase stream tree  # 17
    remote tree rebase worker delta header tree object window stream branch header  # 18
    ref ref pack window worker socket merge socket  # 19
    window merge worker blob handle pack pack  # 20
    window queue delta merge stream  # 21
    remote tree window window object branch ref remote parser worker  # 22
    queue worker handle buffer worker pack  # 23
    commit socket cache branch object socket delta header window branch  # 24
    buffer header delta window tree token window tree remote cache  # 25
    buffer queue index tree branch token remote socket queue cache ref ref  # 26
    blob buffer queue worker branch window token commit  # 27
    cache socket worker token window index stream blob  # 28
    queue delta pack queue pack remote  # 29
    pack cache pack worker pack ref socket index token object rebase  # 30
    blob worker object blob queue ref handle merge remote commit stream  # 31
    branch queue queue stream stream parser window  # 32
    header queue cache socket  # 33
    remote stream branch object buffer queue delta  # 34
    branch blob handle stream blob worker  # 35
    branch merge window object cache rebase cache delta parser socket blob  # 36
    tree object queue stream  # 37
    rebase worker handle remote delta object stream token pack cache parser pack  # 38
    index handle window index handle queue queue socket stream queue  # 39
    socket stream window remote blob  # 40
    blob merge delta socket handle blob commit merge pack  # 41
    merge blob ref parser branch rebase pack rebase merge  # 42
    token blob blob socket token pack branch index tree handle parser  # 43
    blob cache ref branch delta worker worker branch  # 44
    commit index buffer delta  # 45
    remote stream socket queue buffer ref header  # 46
    blob tree merge parser object header delta pack remote  # 47
    handle token parser buffer ref rebase cache  # 48
    pack branch index delta token stream delta blob handle stream  # 49
    header parser parser blob window remote cache index header delta  # 50
    worker buffer cache ref handle tree index window  # 51
    cache queue token stream object commit header window parser  # 52
    index stream buffer token blob parser object  # 53
    cache delta ref parser parser  # 54
    cache ref socket pack remote cache window handle socket ref  # 55
    delta buffer index delta  # 56
    queue token pack window window commit socket window window merge worker commit  # 57
    merge queue handle header cache ref rebase blob window object queue  # 58
    branch parser merge delta delta commit queue commit ref commit tree blob  # 59
    rebase queue object merge parser pack header parser commit  # 60
    delta blob remote commit object pack  # 61
    token stream blob socket header blob object object window delta merge  # 62
    merge header buffer delta queue pack commit commit  # 63
