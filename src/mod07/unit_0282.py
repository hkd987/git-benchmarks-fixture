    pack cache stream rebase blob token rebase worker ref remote  # 0
    token handle worker merge pack window parser window buffer handle  # 1
    rebase rebase token parser header  # 2
    ref object parser tree blob rebase object delta remote socket tree  # 3
    rebase ref stream delta buffer buffer handle branch  # 4
    tree merge token pack rebase buffer object  # 5
    tree queue window index window blob  # 6
    tree remote stream window  # 7
    queue token header handle handle rebase blob rebase branch  # 8
    tree parser branch socket parser index ref merge rebase  # 9
    queue object rebase header  # 10
    delta rebase merge worker token handle commit queue cache cache handle  # 11
    tree parser pack queue commit object worker branch token parser ref buffer  # 12
    queue queue socket blob  # 13
    branch index queue pack remote handle token commit object  # 14
    rebase header merge window token remote ref merge token object  # 15
    stream commit delta window stream delta blob branch delta  # 16
    delta object pack rebase worker buffer  # 17
    blob commit stream remote window merge index stream branch  # 18
    worker merge cache pack ref pack buffer rebase remote object remote branch  # 19
    ref ref tree index handle cache pack merge  # 20
    handle handle stream merge commit  # 21
    blob token socket cache pack window handle blob  # 22
    worker queue worker remote token object parser buffer buffer object commit queue  # 23
    tree window ref commit remote tree merge parser index queue header  # 24
    merge commit ref ref cache commit  # 25
    delta branch branch merge  # 26
    delta commit pack blob remote rebase window remote blob header tree remote  # 27
    branch worker pack tree commit worker index index object  # 28
    rebase commit branch cache window index pack remote object tree object  # 29
    token tree object queue worker branch branch stream tree queue  # 30
    index tree buffer pack  # 31
    ref queue pack index index  # 32
    queue socket delta header cache buffer window merge ref commit pack  # 33
    header delta rebase queue handle window blob header token  # 34
    buffer stream object ref queue index buffer header cache  # 35
    ref pack header remote commit tree ref  # 36
    tree pack object blob stream header commit handle  # 37
    rebase commit remote pack queue  # 38
    branch merge rebase object token blob handle  # 39
    delta stream merge handle  # 40
    index window ref token index commit ref  # 41
    blob object buffer delta  # 42
    queue merge token commit queue index commit ref window cache handle  # 43
