    object socket branch window worker object handle queue blob  # 0
    object remote token ref buffer pack commit blob commit object header rebase  # 1
    commit worker pack handle cache stream merge rebase token commit tree  # 2
    ref ref branch merge parser pack worker  # 3
    remote parser index header  # 4
    ref pack object blob stream cache pack commit ref delta ref  # 5
    socket header branch buffer index parser index token cache buffer index merge  # 6
    token cache delta blob  # 7
    queue blob ref socket token commit token queue handle cache  # 8
    pack header commit queue worker buffer ref blob cache  # 9
    token rebase blob object merge stream commit blob window  # 10
    handle socket token cache parser commit commit window  # 11
    rebase parser blob header worker  # 12
    ref ref object queue branch worker rebase handle handle branch stream  # 13
    blob tree commit queue  # 14
    ref merge header index delta pack pack  # 15
    index cache merge cache handle parser remote cache parser  # 16
    branch cache queue parser merge cache  # 17
    merge buffer blob handle  # 18
    worker blob window worker handle buffer worker delta  # 19
    header parser worker buffer commit stream  # 20
    delta cache parser stream socket buffer handle delta rebase  # 21
    window socket object queue queue remote window stream index  # 22
    window ref remote window worker header  # 23
    branch commit delta socket object pack parser token  # 24
    rebase ref remote delta  # 25
    buffer parser blob delta worker delta parser cache branch  # 26
    ref buffer index parser stream cache index rebase index tree  # 27
    stream worker worker stream  # 28
    ref branch branch commit tree parser commit delta index  # 29
    stream socket commit window delta remote parser cache  # 30
    index parser stream pack delta  # 31
    rebase buffer merge worker commit socket object blob  # 32
    index delta token merge object commit buffer remote  # 33
    object merge stream branch token tree remote  # 34
    stream stream blob parser object token merge branch window tree index buffer  # 35
    header parser cache pack pack socket  # 36
    ref merge token merge pack worker cache  # 37
    object merge window branch rebase socket branch object index  # 38
    rebase window pack cache ref object ref ref socket  # 39
    window ref ref stream  # 40
    worker index handle cache  # 41
    parser rebase remote index ref parser index blob pack  # 42
    worker cache socket merge worker branch cache  # 43
    queue stream index index worker  # 44
    remote branch remote socket buffer commit rebase index worker index  # 45
    ref header parser stream stream token branch  # 46
    rebase merge rebase handle  # 47
    commit cache buffer tree handle window queue buffer socket remote commit  # 48
    blob queue pack branch stream tree stream rebase worker merge  # 49
    blob blob object branch worker remote queue commit merge branch remote blob  # 50
    cache branch stream blob parser index cache commit buffer rebase  # 51
    token tree buffer cache rebase  # 52
    ref socket blob remote index parser  # 53
    pack remote buffer pack window object ref index token ref cache delta  # 54
    worker blob cache header merge object rebase  # 55
    stream remote window index queue branch header window queue remote  # 56
    worker ref remote ref queue blob index  # 57
    handle index socket delta worker ref pack worker pack delta rebase blob  # 58
    window header index socket parser parser rebase commit token queue  # 59
    worker delta header tree remote window  # 60
    socket index queue tree index window worker commit window branch buffer cache  # 61
    ref cache branch buffer  # 62
    pack remote branch cache stream merge token delta stream buffer  # 63
    remote header buffer buffer blob merge header tree  # 64
    blob tree token object pack merge worker branch commit header index  # 65
    delta remote rebase delta pack stream token ref object tree socket window  # 0
    object ref delta delta pack socket remote  # 1
    branch queue remote pack branch  # 2
    cache stream tree index commit socket buffer tree  # 3
