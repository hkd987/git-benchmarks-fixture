    tree rebase delta socket ref branch tree stream remote  # 0
    worker commit queue remote window pack socket  # 1
    header branch ref commit merge pack blob cache blob remote index worker  # 2
    queue stream commit pack cache merge header queue ref delta rebase commit  # 3
    pack rebase stream token pack parser pack remote header commit commit merge  # 4
    token socket socket buffer  # 5
    worker blob rebase worker cache rebase worker remote window branch ref ref  # 6
    tree rebase token parser window queue buffer token  # 7
    remote branch window commit merge index header  # 8
    header buffer delta remote remote queue tree branch queue tree commit rebase  # 9
    object buffer cache handle pack buffer ref cache  # 10
    worker branch merge index tree cache cache delta parser merge  # 11
    buffer worker blob blob cache  # 12
    socket buffer stream token object  # 13
    object branch remote branch rebase  # 14
    object queue object branch buffer index buffer remote queue object  # 15
    commit stream delta token blob parser parser window rebase  # 16
    rebase object remote token delta remote socket index  # 17
    socket object index ref ref object queue buffer  # 18
    delta parser queue stream queue branch header blob  # 19
    tree merge handle tree token pack cache blob commit branch  # 20
    remote ref buffer worker branch buffer  # 21
    header remote commit rebase handle commit merge remote rebase  # 22
    parser parser socket tree blob stream  # 23
    remote socket ref window tree object cache queue  # 24
    pack stream ref worker index stream rebase merge tree worker cache  # 25
    token buffer header pack pack  # 26
    socket buffer token rebase header worker buffer tree handle object worker worker  # 27
    merge commit pack pack buffer merge merge queue ref commit  # 28
    token queue buffer pack object header commit blob pack parser tree  # 29
    token pack branch delta delta handle stream cache object remote header  # 30
    remote cache remote blob queue buffer handle socket branch  # 31
    buffer cache delta index header queue merge  # 32
    branch buffer socket branch cache commit tree token ref tree merge  # 33
    handle stream queue rebase branch blob  # 34
    cache branch worker queue index worker header remote socket window ref ref  # 35
    merge remote token window  # 36
    queue stream delta buffer  # 37
    tree parser rebase tree index header  # 38
    stream buffer pack remote worker  # 39
    cache rebase queue tree tree stream rebase pack object  # 40
    header index queue object cache tree delta parser pack cache index remote  # 41
    delta buffer commit delta pack parser index ref token  # 42
    commit tree tree pack token stream commit  # 43
    header merge socket parser queue buffer parser commit worker handle branch  # 44
    tree stream buffer commit merge window blob  # 45
    object merge blob ref  # 46
    worker rebase parser ref parser pack stream blob delta worker  # 47
