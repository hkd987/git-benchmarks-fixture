    parser delta buffer socket delta  # 0
    stream stream queue merge merge window buffer ref  # 1
    ref blob token buffer branch  # 2
    pack stream token pack buffer worker object delta stream  # 3
    handle token buffer handle pack header queue ref remote cache delta  # 4
    header window pack handle  # 5
    buffer token handle merge blob commit merge object cache buffer delta index  # 6
    pack token ref tree remote object  # 7
    commit buffer token merge rebase buffer stream pack parser worker blob  # 8
    tree tree rebase stream commit remote  # 9
    socket stream token object handle header worker object queue merge handle worker  # 10
    branch rebase socket parser rebase blob merge stream  # 11
    branch index object ref  # 12
    header queue rebase queue merge socket  # 13
    blob worker buffer queue stream handle worker remote ref socket rebase  # 14
    handle tree cache pack tree buffer delta  # 15
    worker tree object remote ref stream stream branch stream stream token remote  # 16
    stream socket parser index  # 17
    index remote index buffer header handle  # 18
    tree merge parser rebase blob queue parser commit  # 19
    queue handle object branch queue stream delta cache commit cache stream header  # 20
    remote merge rebase ref object index window worker pack remote  # 21
    header handle delta window remote delta token object header worker commit  # 22
    pack handle delta stream cache queue commit branch tree handle delta  # 23
    handle merge header delta worker window ref cache object  # 24
    buffer queue remote cache remote cache socket token queue blob  # 25
    stream index rebase queue  # 26
    rebase index pack socket  # 27
    tree buffer handle blob ref  # 28
    rebase queue tree buffer delta handle remote handle  # 29
    cache cache cache ref handle pack queue handle handle rebase header token  # 30
    delta cache blob commit delta commit stream tree delta branch ref delta  # 0
    socket commit worker queue buffer socket queue object cache merge buffer  # 1
    socket branch token window merge buffer  # 2
    pack handle worker token ref branch token token header  # 3
    merge window socket header tree  # 4
    token remote remote remote  # 5
    parser commit parser commit  # 6
    worker queue ref worker delta rebase branch  # 7
    window queue remote ref  # 8
    parser token socket blob  # 9
    remote header delta worker stream  # 10
