    cache remote rebase window index blob commit worker  # 0
    stream branch branch tree socket object rebase queue index  # 1
    socket merge ref remote commit  # 2
    handle object cache delta pack parser rebase  # 3
    object delta index queue commit blob merge ref index object parser parser  # 4
    pack commit header queue commit header branch  # 5
    tree tree ref queue stream stream object rebase queue  # 6
    header commit stream header token queue  # 7
    object stream branch index object  # 8
    tree remote header branch buffer rebase buffer worker stream token handle stream  # 9
    ref index rebase pack  # 10
    worker handle delta pack ref index window ref queue merge ref index  # 11
    window stream rebase pack stream token header  # 12
    blob token index socket commit window  # 13
    cache blob cache index pack queue  # 14
    commit header remote rebase window buffer  # 15
    pack queue stream cache worker  # 16
    pack commit header delta window tree object queue  # 17
    queue remote queue delta window tree object delta token window buffer  # 18
    header delta socket window tree handle  # 19
    remote parser stream branch tree tree index pack stream remote branch token  # 20
    ref rebase pack branch  # 21
    merge cache handle ref  # 22
    branch ref rebase handle pack merge rebase branch commit  # 23
    window pack window header branch handle merge  # 24
    socket window remote blob header commit parser branch token socket index  # 25
    index queue tree buffer  # 26
    index merge delta header tree buffer  # 27
    parser rebase worker rebase branch token delta rebase  # 28
    stream queue ref index queue stream object tree branch merge  # 29
    parser socket window commit buffer token remote  # 30
    queue worker queue cache worker blob index rebase stream  # 31
    buffer delta queue tree object handle remote delta rebase parser object  # 32
    index branch delta buffer queue parser cache commit token rebase branch merge  # 33
    parser object rebase index branch merge queue queue index rebase  # 34
    blob parser token handle tree  # 35
    cache buffer token cache buffer  # 36
    rebase parser ref blob delta cache  # 37
    token socket cache token branch index stream  # 38
    index buffer pack cache  # 39
    tree header branch ref handle  # 40
    socket worker tree buffer worker parser blob ref token merge  # 41
    queue parser blob merge  # 42
    remote handle window header window delta commit branch index  # 43
    window header object commit merge parser  # 44
    branch merge rebase object  # 45
    remote pack merge ref window commit  # 46
    tree stream cache object parser token object worker header tree handle  # 47
    window ref remote header stream branch worker cache handle handle  # 48
    handle ref handle cache parser commit  # 49
    ref blob token queue ref cache buffer socket  # 50
    stream header parser object cache token  # 51
    stream ref index pack commit parser rebase commit rebase branch handle  # 52
    merge branch index stream window pack header parser blob socket  # 53
    object buffer branch buffer window token  # 54
    pack branch worker tree handle remote commit header delta object tree  # 55
    stream stream ref stream rebase window ref branch merge  # 56
    parser parser queue index pack buffer worker pack  # 57
    cache tree pack pack remote cache commit rebase window  # 58
    branch socket queue tree branch blob pack tree worker rebase delta stream  # 59
    branch socket socket index header remote token window handle  # 60
    delta commit buffer worker tree socket window blob stream  # 61
    worker tree tree commit token header rebase buffer token ref object  # 62
    pack queue header commit merge  # 63
    window socket blob socket parser commit index rebase header merge index  # 64
    merge socket remote stream handle token handle tree rebase  # 65
    handle buffer pack buffer delta branch merge worker object parser object queue  # 66
    blob delta commit cache buffer stream socket commit delta worker stream  # 67
    worker buffer remote index remote remote token blob merge commit  # 68
    tree stream header queue index tree remote tree  # 69
    parser rebase ref handle rebase handle header handle  # 70
    stream index blob window ref header tree handle index  # 71
    index object rebase index  # 72
    ref delta blob buffer blob ref index token handle index branch  # 73
    token index socket commit queue worker branch window  # 74
    merge socket pack pack remote  # 75
    stream cache parser cache queue delta pack socket index commit  # 76
    pack commit blob pack queue delta stream  # 77
    remote remote cache delta stream  # 78
    queue buffer rebase merge commit window window handle  # 79
    header tree window token remote stream worker  # 80
    object stream object worker remote stream rebase  # 81
    merge header merge rebase object blob queue remote object cache buffer  # 82
    object rebase header parser object index remote stream blob cache rebase  # 0
    cache buffer socket socket  # 1
    socket buffer rebase merge worker socket token header index buffer buffer  # 2
    ref worker queue stream ref buffer blob queue buffer  # 3
    window parser index cache handle pack ref branch commit blob cache index  # 4
    index delta merge ref delta commit  # 0
    parser window ref header cache cache index index  # 1
    cache commit stream merge stream queue blob ref handle  # 2
    merge tree stream tree pack merge window merge commit pack merge index  # 3
    cache queue window rebase window remote rebase remote merge  # 4
    header cache token window cache token branch delta delta cache index  # 5
    cache socket merge index worker stream token rebase object  # 6
    blob delta pack commit handle  # 7
