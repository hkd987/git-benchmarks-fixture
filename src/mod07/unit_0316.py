    merge branch branch window index remote blob queue handle branch blob token  # 0
    window blob socket window parser  # 1
    object ref tree stream queue tree tree  # 2
    blob remote remote pack worker blob merge remote buffer pack merge  # 3
    rebase socket blob object handle branch commit commit merge handle rebase window  # 4
    tree worker handle remote ref  # 5
    pack socket socket object window tree token cache  # 6
    socket window stream header handle  # 7
    queue ref branch rebase worker ref delta index queue  # 8
    rebase merge worker object ref queue pack pack rebase buffer merge tree  # 9
    cache remote rebase index merge socket  # 10
    blob handle pack blob window pack stream commit parser tree parser object  # 11
    handle object token token  # 12
    ref rebase cache buffer rebase window socket stream  # 13
    tree tree buffer socket cache  # 14
    buffer handle blob worker socket cache delta queue  # 15
    queue index queue buffer worker index tree header blob cache token handle  # 16
    queue buffer worker remote delta object branch rebase parser stream  # 17
    index pack delta parser branch buffer merge buffer worker rebase  # 18
    handle cache branch parser header remote queue queue ref index buffer  # 19
    pack rebase rebase window index header index worker handle parser  # 20
    rebase parser ref pack stream merge object merge object delta  # 21
    commit merge header handle  # 22
    index header pack branch ref tree tree  # 23
    cache window merge ref commit token window stream handle  # 24
    blob queue pack index delta remote window  # 25
    stream buffer stream handle remote handle cache commit object  # 26
    rebase pack rebase header header delta window remote worker tree  # 27
    header branch remote merge queue  # 28
    worker stream pack commit delta token remote  # 29
    handle blob handle token  # 30
    stream blob parser cache object index parser  # 31
    queue object object cache worker commit delta remote remote parser  # 32
    handle ref blob window rebase worker token handle commit  # 33
    parser tree socket object ref handle merge  # 34
    merge header branch index index blob pack pack  # 35
    socket merge window socket handle pack remote branch  # 36
    cache cache stream rebase tree commit pack commit branch  # 37
    merge ref commit buffer index queue commit buffer window buffer merge  # 38
    pack commit commit pack stream rebase socket  # 39
    cache index delta handle  # 40
    stream commit ref tree ref queue blob header rebase queue merge  # 41
    remote index ref branch buffer cache token queue tree ref ref  # 42
    rebase socket worker queue rebase index pack ref parser index object blob  # 43
