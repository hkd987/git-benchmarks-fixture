    object socket header token branch object stream header queue ref object  # 0
    tree buffer header window pack ref  # 1
    object commit merge object object token ref header blob queue ref rebase  # 2
    tree window socket socket token window worker branch buffer index commit branch  # 3
    remote index parser header delta queue pack index commit merge window  # 4
    pack stream rebase window header  # 5
    socket remote parser window commit window  # 6
    stream tree tree tree branch stream  # 7
    index cache remote parser window socket commit branch object tree stream merge  # 8
    blob queue rebase commit delta remote header ref pack stream object  # 9
    stream branch commit merge handle ref delta  # 10
    window socket object token branch commit remote parser header socket cache  # 11
    queue stream handle object delta header remote parser stream  # 12
    branch cache blob branch socket rebase rebase blob  # 13
    remote object merge header branch  # 14
    merge merge object parser  # 15
    delta parser handle object stream cache socket worker object ref  # 16
    token header delta parser handle object pack object tree object token cache  # 17
    remote ref queue cache index blob  # 18
    socket rebase worker buffer cache stream index  # 19
    tree merge token header  # 20
    merge delta stream handle  # 21
    header tree handle stream blob index queue token pack header buffer delta  # 22
    branch socket pack handle worker commit handle worker  # 23
    rebase window window remote worker index  # 24
    buffer queue pack cache commit socket handle delta object  # 25
    index commit index parser rebase merge queue ref rebase tree  # 26
    stream handle window buffer stream object  # 27
    pack buffer parser remote buffer  # 28
    cache cache object handle commit remote socket token delta  # 29
    remote parser index socket queue header branch token commit  # 30
    remote pack remote handle token  # 31
