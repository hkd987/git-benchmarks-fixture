    parser buffer object pack  # 0
    commit tree socket tree worker queue handle  # 1
    commit rebase buffer worker ref buffer buffer token blob worker object  # 2
    cache queue socket ref stream tree window  # 3
    delta parser remote stream commit socket delta remote  # 4
    blob delta queue cache  # 5
    queue parser socket handle  # 6
    worker header commit token socket  # 7
    delta window object ref remote  # 8
    blob stream worker header stream delta window header socket merge rebase queue  # 9
    queue queue stream blob buffer worker handle remote object  # 10
    buffer buffer buffer buffer socket rebase commit commit handle  # 11
    handle queue index socket handle commit buffer object  # 12
    handle token object commit worker blob pack stream socket  # 13
    pack token token parser  # 14
    token object merge rebase tree remote object blob blob tree header  # 15
    rebase window socket tree merge object parser blob object  # 16
    tree header pack blob index window handle  # 17
    queue pack socket blob rebase parser header worker worker window commit window  # 18
    tree tree object buffer merge index remote  # 19
    parser remote branch buffer queue rebase tree header window branch merge merge  # 20
    tree cache blob cache worker cache token socket delta branch branch handle  # 21
    stream tree worker remote header queue rebase buffer  # 22
    queue blob pack stream object index window blob branch blob worker  # 23
    remote handle remote cache rebase ref object cache  # 24
    header header pack pack header ref queue blob remote index pack  # 25
    ref pack socket branch tree  # 26
    stream remote remote worker  # 27
    handle stream socket cache  # 28
    branch worker buffer merge socket delta branch handle  # 29
    index delta index delta index commit stream header parser ref  # 30
    commit queue header pack rebase stream  # 31
    merge blob handle header parser header handle header object blob stream  # 32
    pack buffer handle remote queue buffer  # 33
    header delta blob buffer socket worker queue window  # 34
    stream buffer cache branch index object commit  # 35
    window pack queue cache commit blob handle stream tree stream pack  # 36
    stream window token remote  # 37
    remote merge stream parser commit handle branch tree remote parser socket  # 38
    rebase ref token queue ref delta  # 39
    queue merge worker branch merge stream blob worker tree token index  # 40
    tree pack worker object  # 41
    parser handle rebase pack  # 42
    queue stream token buffer worker object header cache  # 43
    remote delta rebase remote commit remote object rebase  # 44
    parser pack parser tree cache blob handle  # 45
    ref handle window cache worker blob handle  # 46
    cache cache handle worker object window object ref pack  # 47
    ref header branch parser header remote blob blob delta header  # 48
    merge commit branch stream blob header socket  # 49
    object branch branch index object queue  # 50
    window cache merge branch delta tree header ref blob queue  # 51
    rebase cache queue socket header token parser token remote worker  # 52
    header index handle merge cache merge queue  # 53
    socket remote merge merge pack window queue object  # 54
    token stream tree pack queue blob pack worker  # 55
    handle handle delta tree queue header ref socket  # 56
    rebase index index parser rebase token socket commit worker rebase  # 57
    buffer index ref branch tree token window  # 58
    stream delta merge remote queue stream object remote index merge cache  # 59
    branch queue stream handle parser rebase window index token branch merge  # 60
    rebase ref queue merge  # 61
    rebase object worker queue socket branch tree parser parser window commit  # 62
    blob tree delta worker  # 63
    merge cache parser delta index handle merge blob object commit token  # 64
    stream cache remote token merge  # 65
    window remote commit rebase remote socket delta header merge  # 66
    header object rebase queue  # 67
    cache branch window remote queue index header stream branch  # 68
    merge queue buffer token queue queue cache tree  # 69
    remote parser window queue branch pack delta stream cache worker object  # 70
    header worker token stream handle parser remote handle worker  # 71
    delta handle commit tree branch header rebase worker  # 72
    queue header cache delta window object merge  # 73
    rebase delta tree delta rebase worker cache tree merge stream commit header  # 74
    blob cache remote parser socket worker commit remote commit handle token branch  # 75
    window parser stream delta remote handle token  # 76
    commit stream socket pack merge branch header  # 77
    merge queue buffer handle buffer parser parser  # 78
    object queue handle blob ref branch pack  # 79
    branch queue delta index handle blob queue tree  # 80
    cache buffer delta queue  # 81
    branch stream header socket stream stream token cache  # 82
    handle object ref delta buffer commit pack merge  # 83
    header ref delta socket socket token index token worker parser socket  # 0
    merge window tree object index blob pack branch stream tree rebase merge  # 1
    index stream branch parser index cache  # 2
    tree commit merge index rebase stream remote  # 3
    buffer worker index index tree token pack  # 4
    handle object parser pack tree commit ref  # 5
    parser socket tree tree object remote  # 6
    socket blob header index object remote  # 7
    parser remote branch blob tree ref  # 8
    stream queue queue ref pack commit  # 9
    window pack object rebase tree delta token index commit  # 10
    commit tree index branch object queue pack token index handle  # 11
    tree commit handle index delta branch  # 0
    object delta buffer handle handle cache  # 1
    remote window window delta token token window  # 2
    ref pack delta window cache merge  # 3
    object parser parser token header parser  # 4
    worker pack pack pack tree  # 5
    blob merge handle stream index buffer token  # 6
