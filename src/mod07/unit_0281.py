    cache token ref buffer handle delta remote socket cache  # 0
    buffer window queue stream branch index merge merge  # 1
    token tree parser handle  # 2
    remote blob merge queue token commit  # 3
    ref blob queue commit buffer remote  # 4
    handle stream ref object cache socket rebase object socket header pack window  # 5
    socket worker remote ref delta socket cache buffer branch  # 6
    queue stream stream remote parser index buffer cache merge queue stream commit  # 7
    buffer cache socket socket commit branch  # 8
    index window blob window ref parser  # 9
    parser token blob delta rebase window  # 10
    object socket rebase object window tree remote parser parser merge stream handle  # 11
    commit object branch rebase worker branch tree tree  # 12
    blob blob object object remote queue window parser rebase object buffer  # 13
    cache pack queue cache index worker handle  # 14
    window merge rebase worker buffer blob remote header header buffer window  # 15
    queue worker ref queue pack blob remote branch  # 16
    index commit remote cache blob buffer commit ref ref ref branch  # 17
    index remote branch index index cache socket stream cache rebase pack  # 18
    cache worker remote handle parser pack ref  # 19
    commit window socket window header ref ref  # 20
    pack stream index stream handle socket window merge header handle remote worker  # 21
    buffer branch parser parser handle blob commit blob  # 22
    commit parser socket tree blob commit stream index merge buffer rebase index  # 23
    window buffer header header object queue worker cache tree  # 24
    branch stream commit window window stream worker remote branch stream delta rebase  # 25
    branch socket blob pack queue merge parser branch rebase cache handle delta  # 26
    stream blob branch header remote stream pack pack commit delta tree  # 27
    merge remote tree parser branch branch  # 28
    commit buffer window worker remote parser buffer buffer handle  # 29
    remote buffer branch cache stream queue window rebase  # 30
    socket branch object parser branch pack  # 31
    tree cache worker buffer pack pack remote token handle  # 32
    window token buffer object pack commit object  # 33
    window header cache commit rebase branch index  # 34
    stream branch delta cache blob worker merge pack  # 35
    queue buffer buffer parser tree branch object  # 36
    handle socket commit cache stream index socket stream delta commit socket  # 37
    index stream worker merge remote handle cache worker  # 38
    ref blob parser remote  # 39
    tree object header worker commit stream queue queue delta merge  # 40
    ref token stream remote token  # 41
    socket buffer buffer header pack merge  # 42
    tree object worker handle header socket handle cache header handle parser  # 43
    cache branch delta ref header  # 44
    ref window ref header  # 45
    parser pack handle buffer merge rebase  # 46
    window worker pack blob queue worker window queue rebase cache header  # 47
    window index worker remote branch ref remote  # 48
    delta cache handle delta window socket socket rebase merge blob  # 49
    buffer rebase cache tree stream ref tree branch token  # 50
    tree window object pack cache token pack cache  # 51
    tree window buffer buffer window blob rebase worker index parser handle  # 52
