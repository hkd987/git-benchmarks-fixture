    delta queue queue remote object commit pack delta index worker remote object  # 0
    buffer merge object branch  # 1
    stream blob delta token parser cache stream  # 2
    header pack pack queue pack window commit branch delta branch object  # 3
    remote parser ref parser ref socket  # 4
    socket remote rebase branch worker delta delta ref commit buffer  # 5
    handle queue object commit handle delta index tree header pack  # 6
    merge buffer token merge commit token window object  # 7
    branch stream object buffer window index cache remote handle branch  # 8
    token header remote commit parser object  # 9
    rebase ref pack blob parser  # 10
    index blob rebase pack worker  # 11
    handle cache handle blob object merge pack worker index handle remote buffer  # 12
    window object object buffer branch worker token delta  # 13
    pack pack header socket pack handle  # 14
    window queue worker cache index worker window queue  # 15
    pack cache object window handle pack worker  # 16
    stream stream ref rebase ref rebase delta index worker header  # 17
    index index object stream index token pack ref branch header commit  # 18
    buffer rebase index delta tree rebase cache tree socket blob window  # 19
    cache token commit cache cache  # 20
    parser buffer queue cache blob remote stream  # 21
    branch rebase branch index ref handle index socket stream parser delta  # 22
    commit delta delta socket token tree remote token cache blob  # 23
    ref blob cache handle stream tree header  # 24
    token rebase blob handle commit pack commit worker handle cache buffer parser  # 25
    delta token rebase window window object worker merge queue  # 26
    blob blob object parser blob ref  # 27
    index handle buffer delta  # 28
    queue object rebase delta handle commit window cache buffer cache handle ref  # 29
    delta header buffer merge handle delta header index pack header ref commit  # 30
    cache worker parser tree index  # 31
    remote merge header queue worker commit header object queue handle delta merge  # 32
    queue header parser tree handle handle  # 33
    delta buffer rebase rebase buffer token  # 34
    rebase worker object rebase branch token branch  # 35
    stream object buffer token  # 36
    cache branch socket stream object  # 37
    socket queue token blob object parser  # 38
    blob header cache pack cache cache merge ref stream  # 39
    remote window token parser stream branch remote  # 40
    ref merge ref merge rebase  # 41
    buffer window object buffer delta  # 42
    worker tree tree tree ref stream object cache queue parser header  # 43
    handle index socket window buffer  # 44
    branch index commit header object  # 45
    header blob ref index merge pack worker handle cache  # 46
    index rebase header remote merge tree handle rebase  # 47
    header blob remote buffer index header delta branch object pack  # 48
    token index socket pack tree handle branch remote pack object branch pack  # 49
    branch stream branch buffer commit remote  # 50
    remote index merge object ref delta  # 51
    parser queue worker rebase ref header token blob object  # 52
    worker commit commit rebase merge blob stream header queue header window handle  # 53
    queue worker worker delta buffer remote window stream pack cache cache  # 54
    object stream header buffer  # 55
    handle parser remote stream  # 56
    branch queue tree merge merge buffer object handle branch rebase delta tree  # 57
    stream cache stream merge index object buffer cache stream handle token  # 58
    blob remote branch commit object merge buffer  # 59
