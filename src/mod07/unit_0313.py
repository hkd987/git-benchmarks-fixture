    object merge buffer header commit  # 0
    branch ref socket commit handle merge ref tree window buffer header  # 1
    stream commit rebase handle branch pack pack commit buffer cache  # 2
    stream branch pack window object tree ref token  # 3
    delta commit cache cache rebase queue header worker merge tree  # 4
    branch worker object remote  # 5
    buffer delta ref window pack branch worker buffer handle branch socket delta  # 6
    handle rebase header socket index object token delta tree tree socket tree  # 7
    pack branch socket queue window ref stream token cache window buffer  # 8
    blob handle rebase commit delta ref branch commit window index worker  # 9
    blob commit commit rebase queue buffer  # 10
    pack commit rebase pack window delta header tree remote blob  # 11
    index header remote pack remote delta commit stream delta  # 12
    merge token remote rebase header  # 13
    merge window rebase pack ref header header object delta commit commit  # 14
    cache header remote blob buffer parser queue delta object  # 15
    parser rebase queue pack token  # 16
    cache rebase stream header tree header  # 17
    token rebase ref ref blob object blob blob  # 18
    window tree ref delta buffer  # 19
    handle header stream index tree merge worker worker ref parser  # 20
    window remote tree cache  # 21
    ref queue blob merge buffer queue token  # 22
    buffer handle ref queue pack rebase index handle stream commit  # 23
    tree socket index parser object pack buffer header  # 24
    header stream socket merge socket socket worker handle pack  # 25
    blob delta socket index window pack  # 26
    handle token cache window cache pack  # 27
    header rebase window pack merge object pack socket  # 28
    stream blob handle stream parser socket token cache  # 29
    blob branch ref header index rebase branch index branch stream buffer rebase  # 30
