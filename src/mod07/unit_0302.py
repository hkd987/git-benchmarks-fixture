    delta commit rebase pack commit window  # 0
    branch stream pack ref  # 1
    header branch pack tree commit object  # 2
    object delta object branch  # 3
    worker socket index queue header blob handle buffer index remote  # 4
    cache object commit index object delta object window header worker blob  # 5
    blob ref delta handle pack tree commit  # 6
    stream handle handle cache merge window index worker pack  # 7
    queue object queue window blob worker cache pack queue remote parser remote  # 8
    rebase cache merge socket remote branch handle  # 9
    cache token tree window parser parser  # 10
    socket merge delta parser delta header stream rebase ref socket object  # 11
    blob rebase worker branch object remote rebase blob merge  # 12
    socket branch merge header socket token token  # 13
    token queue branch tree rebase rebase stream blob  # 14
    cache rebase queue delta commit branch  # 15
    socket token merge window handle rebase  # 16
    index delta commit cache handle tree worker buffer stream remote  # 17
    stream branch delta merge header tree handle  # 18
    buffer branch worker token  # 19
    stream tree stream ref branch blob  # 20
    stream header stream buffer index remote header  # 21
    tree tree cache object object window stream  # 22
    object object remote rebase ref index header blob pack rebase window  # 23
    tree buffer branch socket blob  # 24
    handle remote commit window tree  # 25
    worker socket parser commit tree blob handle pack  # 26
    handle pack header handle parser ref worker ref commit token handle handle  # 27
    tree queue commit parser tree  # 28
    queue object remote queue token socket merge queue object index  # 29
    rebase blob socket remote remote branch header socket rebase stream buffer ref  # 30
    queue pack worker remote token index cache token branch object queue parser  # 31
    rebase commit pack commit handle tree token ref stream worker  # 32
    blob handle delta delta branch cache ref index buffer object commit ref  # 33
    merge remote worker merge pack merge rebase  # 34
    tree socket queue buffer stream rebase header commit blob pack  # 35
    buffer remote handle branch ref token  # 36
    object parser tree handle  # 37
    object index ref object rebase worker cache  # 38
    handle socket merge blob  # 39
    handle parser handle ref worker commit tree remote parser object  # 40
    ref tree object worker index tree ref  # 41
    index window cache cache delta tree parser  # 42
    header index merge delta buffer merge delta  # 43
    object socket remote window window  # 44
    branch socket index commit tree commit pack  # 45
    window pack index header pack worker ref ref  # 46
    stream worker merge ref object blob header merge  # 47
    rebase index commit stream token remote rebase handle  # 48
    buffer merge window branch handle commit header rebase merge header commit blob  # 49
    stream blob ref commit merge index blob tree window token rebase commit  # 50
    blob tree worker window index pack queue rebase  # 51
    socket index pack blob remote pack commit header index commit branch  # 52
    remote ref worker ref handle socket merge token header object commit delta  # 53
    pack blob ref header stream branch  # 54
    tree socket index parser queue merge header stream window index  # 55
    tree commit object index ref merge window  # 56
    blob rebase parser tree socket header  # 57
    token queue window remote rebase buffer  # 58
    ref socket handle worker merge socket cache delta header pack commit  # 59
    index delta pack queue header parser object pack rebase delta branch blob  # 60
    object object tree delta  # 61
    blob pack merge rebase remote header worker remote object buffer object object  # 62
    rebase blob blob cache object window socket queue  # 63
    ref ref buffer header  # 64
    branch delta merge index socket parser pack  # 65
    handle queue queue buffer rebase window delta  # 66
    queue object rebase queue object cache object worker header  # 67
    pack buffer token window blob token pack rebase token window blob stream  # 68
    rebase parser token token remote tree stream index  # 69
