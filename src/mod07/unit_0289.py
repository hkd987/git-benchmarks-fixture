    delta worker tree worker handle  # 0
    delta pack delta window tree delta  # 1
    buffer handle buffer header token remote token ref header  # 2
    rebase stream branch buffer commit  # 3
    queue object ref rebase pack commit blob remote  # 4
    header pack commit commit rebase window index handle buffer delta merge  # 5
    worker handle pack merge buffer rebase window branch  # 6
    header pack queue token buffer branch queue parser parser branch header  # 7
    delta buffer object commit object delta rebase  # 8
    cache ref pack tree object cache stream socket  # 9
    stream worker commit parser queue pack cache window handle rebase tree delta  # 10
    tree branch ref object buffer  # 11
    token pack token branch tree window socket branch delta buffer index merge  # 12
    rebase merge index stream window  # 13
    worker token token stream handle delta object header parser handle token window  # 14
    object worker parser buffer merge worker buffer parser stream window blob  # 15
    remote tree rebase token handle merge worker commit buffer header  # 16
    tree rebase stream buffer queue handle object queue window merge branch window  # 17
    queue rebase blob socket queue pack tree queue delta object  # 18
    stream rebase socket branch commit commit merge handle  # 19
    token worker blob remote  # 20
    delta blob ref blob blob merge cache parser rebase worker  # 21
    remote handle socket socket branch remote  # 22
    ref tree buffer pack remote index buffer index worker blob commit  # 23
    token tree remote parser blob token buffer ref object token rebase  # 24
    merge rebase cache object parser  # 25
    merge token socket cache socket ref buffer merge  # 26
    cache stream object blob branch worker  # 27
    pack index branch socket header  # 28
    header index socket cache ref worker tree object branch header  # 29
    commit parser commit object header pack  # 30
    delta delta buffer ref  # 31
    index ref merge window remote index index queue blob branch index  # 32
    blob pack handle remote parser tree commit buffer window queue object  # 33
    pack commit buffer merge  # 34
    parser worker object pack  # 35
    pack worker cache window token branch commit remote remote blob queue object  # 36
    rebase socket merge rebase handle rebase  # 37
