    branch handle object cache delta rebase object stream socket  # 0
    remote blob branch worker delta window header cache index branch branch delta  # 1
    delta remote token delta socket window blob branch merge delta  # 2
    delta remote window header queue  # 3
    object parser ref parser index buffer delta queue object merge  # 4
    merge window handle token ref tree buffer  # 5
    branch cache commit object buffer blob branch  # 6
    cache branch ref tree queue handle merge merge  # 7
    index header parser rebase ref delta  # 8
    blob rebase object remote remote  # 9
    token parser token buffer tree ref socket cache  # 10
    token merge commit commit index header branch stream  # 11
    object worker rebase cache pack  # 12
    stream blob handle cache  # 13
    handle blob queue header  # 14
    rebase remote delta stream merge rebase  # 15
    tree delta index tree index  # 16
    remote header worker blob tree socket  # 17
    blob parser pack blob pack  # 18
    token merge buffer commit object remote  # 19
    worker handle handle commit  # 20
    stream header commit stream parser  # 21
    merge handle tree cache rebase handle worker commit ref handle  # 22
    commit index window object tree token parser header  # 23
    buffer queue cache handle delta branch  # 24
    stream stream remote buffer index parser remote worker  # 25
    parser commit socket header tree tree token socket  # 26
    object window object merge branch  # 27
    tree header handle index token token worker object object rebase stream blob  # 28
    blob parser branch index  # 29
    buffer buffer rebase tree queue parser  # 30
    object stream worker object pack rebase pack blob delta  # 31
    worker delta buffer tree delta token socket commit buffer branch branch pack  # 32
    remote buffer pack window branch  # 33
    ref worker tree branch cache worker parser blob ref  # 34
    commit commit index parser ref header queue tree stream handle  # 35
    delta worker header blob ref  # 0
    buffer window socket worker cache commit header  # 1
    token window rebase buffer  # 2
    buffer remote parser rebase socket  # 3
    branch stream worker remote header token queue  # 4
    cache buffer cache handle rebase ref window  # 5
    merge parser buffer parser socket queue handle parser header  # 6
    object handle rebase branch  # 7
    worker blob socket commit header worker queue branch tree remote  # 8
