    pack remote socket delta window header  # 0
    window object delta index token  # 1
    branch socket ref merge index socket  # 2
    socket queue cache object parser tree  # 3
    header buffer parser pack object cache  # 4
    ref worker tree pack branch branch header queue buffer index stream ref  # 5
    tree object pack object index stream rebase queue cache  # 6
    index object queue window header  # 7
    remote index socket cache ref  # 8
    object remote blob worker index token merge index cache merge header  # 9
    merge socket buffer handle header  # 10
    token socket index queue blob branch worker pack queue token worker  # 11
    pack cache queue window delta tree worker stream  # 12
    queue parser token cache tree commit commit buffer stream  # 13
    handle remote buffer object rebase  # 14
    buffer header socket header token rebase  # 15
    tree cache delta parser header ref cache delta  # 16
    object pack buffer blob rebase object window ref window delta  # 17
    object object worker worker blob buffer index pack rebase token token merge  # 18
    object index branch stream  # 19
    index rebase parser blob socket ref blob stream worker handle  # 20
    header branch blob remote  # 21
    delta window blob buffer window  # 22
    blob worker queue tree parser token handle pack cache  # 23
    remote buffer object commit object queue parser worker cache window buffer blob  # 24
    queue stream token commit tree rebase stream  # 25
    remote pack queue remote pack parser window queue object commit  # 26
    merge commit queue parser cache delta buffer pack commit pack rebase  # 27
    window ref window stream object  # 28
    blob worker commit queue buffer branch commit delta  # 29
    header branch delta object parser handle remote branch parser cache queue  # 30
    rebase stream ref window remote window worker queue  # 31
    index buffer merge merge header  # 32
    handle stream rebase stream tree delta blob pack remote tree queue blob  # 33
    token merge buffer rebase handle queue token object merge stream commit cache  # 34
    handle merge window tree  # 35
    merge parser queue worker remote pack commit handle token cache blob tree  # 36
    merge blob handle window index blob stream stream token stream commit object  # 37
    socket delta window pack window commit  # 38
    window blob delta window window  # 0
    blob cache commit pack commit handle remote  # 1
    token delta window parser socket buffer commit rebase commit cache  # 2
    delta handle window ref delta index delta  # 3
    header worker commit tree object token handle window blob handle buffer stream  # 4
    socket buffer merge parser handle object object handle window  # 5
    merge socket delta commit header  # 6
    ref worker stream blob ref rebase rebase tree tree  # 7
