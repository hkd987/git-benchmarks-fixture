    parser header buffer ref socket merge socket  # 0
    delta branch queue handle remote branch cache token socket queue  # 1
    cache pack stream token  # 2
    socket commit delta socket delta object delta pack branch blob  # 3
    remote rebase object delta branch tree commit header queue delta rebase window  # 4
    branch header merge handle worker index commit  # 5
    queue ref cache merge token rebase header handle tree  # 6
    object object remote worker branch rebase stream index window branch tree  # 7
    branch parser parser worker rebase cache blob handle  # 8
    tree header remote rebase  # 9
    branch handle tree buffer object  # 10
    object stream header header object  # 11
    pack rebase tree tree  # 12
    queue parser buffer ref buffer object pack delta worker handle blob queue  # 13
    pack pack commit cache worker branch pack header socket  # 14
    merge ref worker stream token cache  # 15
    stream handle buffer remote object token tree delta socket commit rebase tree  # 16
    delta cache delta delta blob worker  # 17
    remote tree worker blob cache socket blob delta remote branch ref buffer  # 18
    remote rebase parser ref ref worker cache cache pack index  # 19
    parser cache queue delta branch merge index branch commit parser commit window  # 20
    merge pack buffer remote token queue socket pack commit worker cache branch  # 21
    parser branch ref window rebase  # 22
    worker parser rebase index rebase queue index header delta  # 23
    window remote worker commit parser  # 24
    cache handle blob tree queue remote pack delta header tree handle object  # 25
    header stream merge cache  # 26
    handle ref window remote queue rebase remote handle header  # 27
    remote remote merge token blob remote token index delta buffer  # 28
    index pack rebase ref pack window branch header  # 29
    token index remote queue delta remote token rebase window cache  # 30
    rebase index ref worker ref commit remote rebase object  # 31
    branch branch tree worker cache token handle rebase blob  # 32
    parser queue commit branch remote cache parser tree  # 33
    branch buffer object header stream parser cache socket ref merge  # 34
    worker blob window stream parser ref buffer object worker  # 35
    remote blob rebase parser merge object pack  # 0
    index ref rebase tree worker merge remote  # 1
    object branch ref queue index  # 2
    rebase ref remote parser handle tree handle socket buffer ref worker pack  # 3
    rebase branch handle parser ref remote stream tree blob  # 4
    stream delta ref delta  # 5
    header delta parser stream blob window rebase ref tree merge  # 6
    cache window ref ref handle commit object index  # 7
    commit object pack blob blob branch token socket ref  # 8
    socket merge socket tree remote  # 9
    merge remote header commit rebase branch object worker ref header  # 10
    buffer socket delta window socket parser header tree handle  # 11
    stream commit pack commit commit delta rebase socket  # 12
    buffer stream stream window parser buffer remote commit merge commit window header  # 0
    token socket commit merge queue object parser commit socket pack  # 1
    branch queue handle remote  # 2
    index remote cache remote socket  # 3
    remote rebase branch parser index blob window tree cache object object delta  # 4
    delta branch buffer parser pack ref worker queue  # 5
    worker pack token queue cache cache commit  # 0
    tree handle stream delta  # 1
    merge parser branch worker token header token tree blob handle  # 2
    token header object pack index object header rebase rebase cache handle queue  # 3
