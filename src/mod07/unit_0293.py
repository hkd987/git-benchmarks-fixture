    parser buffer object window delta  # 0
    branch cache cache rebase  # 1
    blob tree ref delta cache object object object index parser  # 2
    delta blob queue header remote blob index parser buffer  # 3
    blob delta stream header remote parser tree  # 4
    header rebase handle remote index merge remote handle  # 5
    header window socket branch cache  # 6
    handle rebase delta object  # 7
    worker window pack socket handle  # 8
    tree blob handle queue parser merge pack header stream queue handle window  # 9
    tree merge index merge remote rebase stream cache  # 10
    object worker remote queue buffer  # 11
    parser buffer commit queue parser rebase merge merge stream buffer header  # 12
    delta window parser window header window token delta handle index  # 13
    blob commit stream tree header buffer commit cache rebase index pack  # 14
    commit object merge buffer stream remote  # 15
    ref pack parser delta parser cache  # 16
    object delta buffer parser stream worker stream window queue worker index  # 17
    tree delta delta tree commit merge queue cache socket delta blob  # 18
    worker blob queue delta tree object delta socket  # 19
    socket pack stream remote blob remote window socket queue  # 20
    handle branch delta tree handle  # 21
    stream queue remote tree tree window socket merge  # 22
    delta delta cache header socket parser commit parser merge handle tree index  # 23
    handle window branch token index  # 24
    blob remote pack merge ref buffer window branch header object remote token  # 25
    parser delta worker pack object object worker rebase window stream header  # 26
    blob stream token ref socket  # 27
    worker index stream cache worker ref commit  # 28
    socket remote parser index commit window tree parser remote worker tree stream  # 29
    window stream socket merge header pack ref token handle tree commit socket  # 30
    header token pack remote queue index cache header commit branch commit buffer  # 31
    parser cache delta socket worker parser queue  # 32
    branch ref pack worker merge rebase queue  # 33
    handle branch index remote token socket merge rebase branch ref index  # 34
    merge worker buffer queue stream buffer branch  # 35
    blob handle delta branch pack commit handle  # 36
    handle header branch socket  # 37
    parser cache handle merge header delta delta  # 38
    socket window merge stream buffer branch tree header ref pack ref  # 39
    blob delta tree window worker queue handle delta parser remote delta ref  # 40
    ref stream branch buffer window blob queue rebase token index ref window  # 41
    blob buffer blob remote window rebase  # 42
    header socket stream queue header cache handle worker header header stream  # 43
    commit merge pack buffer ref buffer stream socket worker remote  # 44
    worker ref object object  # 45
    cache merge window ref handle pack parser buffer index queue commit  # 46
    tree socket index parser handle merge  # 47
    parser commit header buffer cache branch branch branch rebase merge remote handle  # 48
    merge stream pack stream worker tree tree token stream ref cache  # 49
    window socket stream socket  # 50
    rebase blob tree header socket ref pack rebase token parser  # 51
    branch cache queue ref delta ref ref  # 52
    parser pack buffer remote branch cache remote  # 53
    stream handle delta parser pack tree header remote stream header header  # 54
    queue rebase rebase commit queue remote blob object pack  # 55
    buffer branch index queue pack tree socket  # 56
    object tree tree ref object ref queue delta  # 57
    window rebase rebase cache token branch stream cache blob commit  # 58
    commit pack token delta window delta object buffer token remote  # 59
    blob stream window index window  # 60
    index branch rebase token index buffer worker branch  # 61
    cache parser merge cache cache socket branch merge window  # 62
    parser cache index rebase blob stream commit commit  # 63
    merge queue header socket ref handle pack index tree object  # 64
    worker cache socket stream tree  # 65
    merge buffer window queue queue index cache header window index  # 66
    stream pack index blob parser header header cache parser cache token queue  # 67
    handle ref commit commit delta  # 68
    pack token merge token  # 69
    parser tree commit stream remote merge worker cache cache window stream delta  # 70
    blob socket rebase commit window tree merge header ref  # 71
    window handle object stream  # 72
    stream merge blob window stream window commit  # 73
    queue window rebase header stream  # 74
    delta token branch window worker rebase worker merge delta remote blob  # 0
    branch index index socket remote handle buffer commit index token queue stream  # 1
    socket window object stream  # 2
    worker window pack parser tree queue stream socket blob  # 3
    stream remote index worker stream  # 4
    tree tree stream merge parser handle socket blob blob remote  # 5
    delta worker worker pack  # 6
    branch parser parser pack socket header ref index blob worker cache  # 7
