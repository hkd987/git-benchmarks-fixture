    header blob handle rebase socket cache blob blob tree cache  # 0
    stream pack object token stream queue tree handle blob object  # 1
    commit cache index tree delta tree parser blob worker  # 2
    stream ref socket branch branch socket header  # 3
    ref parser blob socket object stream  # 4
    cache parser worker commit pack  # 5
    delta object buffer merge parser  # 6
    index window header tree worker buffer token header commit  # 7
    token pack merge remote ref merge branch blob pack  # 8
    pack index merge merge parser buffer blob tree socket blob  # 9
    parser handle remote blob merge ref merge buffer  # 10
    branch window cache cache queue  # 11
    socket window header window commit  # 12
    rebase index header delta worker stream token queue delta buffer blob remote  # 13
    commit stream pack socket commit branch commit remote pack  # 14
    ref delta tree queue cache token  # 15
    index handle rebase merge buffer token object cache delta object branch  # 16
    cache merge buffer merge token  # 17
    commit queue rebase parser blob ref blob token window parser object  # 18
    cache ref cache cache header commit  # 19
    blob commit parser handle buffer handle delta rebase  # 20
    blob buffer tree token branch buffer socket window rebase  # 21
    remote blob worker cache handle  # 22
    stream remote commit delta  # 23
    cache token header stream ref  # 24
    worker socket cache handle parser commit commit token  # 25
    pack stream blob ref cache index branch window window remote handle  # 26
    queue object delta queue worker remote  # 27
    stream ref tree buffer worker  # 28
    branch object tree stream delta queue object branch  # 29
    header commit index window merge  # 30
    stream queue buffer buffer tree blob object queue worker  # 31
    rebase ref handle buffer merge socket index  # 32
    ref window blob branch ref ref  # 33
    ref merge socket tree buffer blob index index window ref  # 34
    token buffer index branch branch window cache branch  # 35
    queue delta object parser  # 36
    ref ref merge ref index delta pack handle  # 37
    merge stream remote parser queue  # 38
    object pack commit index  # 39
    rebase buffer index remote tree ref rebase worker  # 40
    buffer buffer handle branch handle branch queue rebase parser cache index remote  # 41
    worker object remote socket parser tree commit token  # 42
    worker merge index commit  # 43
    worker delta delta stream header blob commit token  # 44
    window tree merge index token ref parser socket  # 45
    buffer header window parser merge stream  # 46
    tree branch object merge remote  # 47
    commit ref queue worker header merge worker worker window queue  # 48
    ref merge branch merge tree commit stream worker index merge blob buffer  # 49
    pack delta queue rebase delta pack index  # 50
    token worker window token cache parser window  # 51
    buffer merge worker handle branch worker cache  # 0
    remote ref rebase object branch token delta  # 1
    branch stream socket token buffer merge worker worker branch merge  # 2
    blob object remote tree  # 3
    ref ref buffer index queue  # 4
    commit blob window window branch index header token tree socket header tree  # 5
    rebase pack header buffer header  # 6
