    token blob tree object remote queue cache remote tree branch header  # 0
    blob cache merge object window index  # 1
    object socket pack token buffer  # 2
    delta blob parser index socket  # 3
    merge handle commit header  # 4
    pack index index rebase handle object  # 5
    commit pack token stream rebase remote remote delta stream buffer  # 6
    blob tree worker window buffer merge index delta  # 7
    tree object pack merge delta index buffer queue header ref ref window  # 8
    pack socket socket ref pack branch queue branch  # 9
    socket delta merge cache branch delta  # 10
    token buffer tree tree window object worker socket cache buffer merge  # 11
    header tree remote cache queue worker index delta rebase buffer pack parser  # 12
    ref index merge commit cache remote stream rebase handle blob rebase cache  # 13
    object index handle delta object index socket blob window queue ref  # 14
    buffer merge cache index branch token cache branch object blob commit stream  # 15
    cache window tree stream remote pack pack  # 16
    pack remote merge branch buffer branch token  # 17
    handle window cache buffer  # 18
    queue ref socket window commit ref  # 19
    index window queue branch ref ref cache stream index remote commit  # 20
    object rebase parser rebase pack commit token remote commit  # 21
    merge commit cache queue rebase handle delta  # 22
    stream window parser blob  # 23
    queue delta delta ref header window blob header stream rebase blob  # 24
    buffer header merge rebase remote header worker token tree  # 25
    queue commit queue pack cache  # 26
    object index handle rebase blob stream  # 27
    object parser stream worker tree stream pack rebase stream queue parser  # 28
    cache buffer socket rebase object delta header  # 29
    commit parser pack parser ref token parser branch  # 30
    window delta stream merge merge blob window cache window delta merge  # 31
    socket parser cache handle  # 32
    stream blob object header merge branch stream ref tree queue index stream  # 33
    cache window token token buffer buffer queue worker buffer tree pack ref  # 34
    worker index blob cache delta commit buffer token stream handle tree  # 35
    delta header parser queue window worker pack merge header queue stream  # 36
    merge pack remote handle branch remote remote blob tree object  # 37
    handle branch socket header worker remote queue buffer  # 38
