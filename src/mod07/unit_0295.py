    index blob worker handle cache  # 0
    stream handle rebase blob index  # 1
    buffer socket rebase worker merge  # 2
    pack delta worker object ref merge  # 3
    parser worker ref object branch object delta ref remote rebase window  # 4
    branch parser rebase object window merge index remote branch stream stream  # 5
    tree tree merge parser merge header tree merge remote token blob  # 6
    object index rebase index stream pack index remote commit pack parser blob  # 7
    window tree remote buffer rebase worker  # 8
    header header merge socket  # 9
    object index window rebase cache remote  # 10
    cache merge stream blob  # 11
    object token parser pack ref queue queue merge  # 12
    merge delta rebase queue ref ref token  # 13
    handle ref ref header stream worker merge remote  # 14
    token worker socket ref token blob rebase merge delta index socket header  # 15
    parser worker rebase object rebase token header ref  # 16
    rebase queue parser worker index queue token window rebase parser worker  # 17
    delta stream parser blob delta buffer  # 18
    token handle buffer ref window  # 19
    header stream ref delta pack  # 20
    cache buffer stream remote handle cache index commit ref  # 21
    worker stream stream pack queue  # 22
    worker pack blob buffer queue buffer  # 23
    handle object cache merge index  # 24
    rebase cache token parser window remote stream parser tree handle parser buffer  # 25
    window merge cache parser blob header  # 26
    buffer stream delta parser  # 27
    parser merge window rebase remote pack handle tree cache buffer commit cache  # 28
    stream object tree socket handle handle worker  # 29
    index blob tree queue token  # 30
    blob stream cache remote remote index object commit  # 31
    header handle handle ref  # 32
    branch ref handle blob branch object stream tree blob tree  # 33
    blob buffer queue commit blob tree pack ref parser window merge parser  # 34
    worker queue branch merge branch index object object  # 35
    branch window rebase blob window token header merge branch delta window  # 36
    worker parser remote queue parser window buffer parser  # 37
    handle token handle cache buffer window handle remote tree blob  # 38
    commit stream stream index queue handle  # 39
    token buffer worker object  # 40
    remote tree delta pack parser object queue index commit token parser  # 41
    ref branch buffer tree queue pack branch worker  # 42
    parser object cache remote  # 43
    commit pack window queue pack window ref ref index ref delta index  # 44
    ref handle tree delta buffer header ref handle  # 45
    token delta token header ref  # 46
    handle cache parser branch stream tree handle  # 47
    parser index delta branch ref queue  # 48
    ref ref object stream worker merge worker socket  # 49
    window remote branch tree worker  # 50
    branch index stream window branch queue index tree index buffer tree socket  # 51
    worker token ref stream object window token branch worker header ref  # 52
    worker ref socket index socket tree  # 53
    cache handle object ref window branch buffer parser socket commit  # 54
    socket object tree branch  # 55
    cache header object window pack object worker blob  # 56
    blob delta rebase worker  # 57
    tree header buffer ref pack commit handle  # 58
    buffer blob parser ref merge window socket token  # 59
    tree index cache branch  # 60
    parser socket parser merge index parser ref socket  # 61
    remote object handle branch header stream ref stream stream stream tree cache  # 62
