    token handle worker ref rebase merge token queue branch socket  # 0
    remote commit header header index socket header delta  # 1
    pack object worker buffer delta handle stream branch parser  # 2
    header merge parser index merge merge tree  # 3
    ref delta buffer socket index ref queue merge  # 4
    stream worker delta object remote rebase delta branch queue worker  # 5
    token merge commit queue rebase  # 6
    worker rebase delta blob header ref header  # 7
    merge rebase token socket handle commit remote delta socket branch  # 8
    tree merge stream rebase token remote socket handle token  # 9
    window queue branch delta delta worker socket queue worker  # 10
    handle blob parser queue branch stream  # 11
    commit branch merge remote queue pack buffer parser queue delta blob  # 12
    remote blob header token commit object  # 13
    merge header merge merge  # 14
    rebase remote blob queue handle token remote commit branch socket object  # 15
    ref window queue parser ref socket window worker blob tree parser merge  # 16
    object remote merge tree rebase ref blob  # 17
    stream object parser merge index merge branch token buffer  # 18
    delta object stream pack handle ref delta header remote header blob object  # 19
    object worker blob window parser token merge branch merge window queue index  # 20
    blob ref branch remote queue index index handle stream parser rebase  # 21
    index tree tree delta delta blob queue tree remote object  # 22
    rebase handle parser pack delta  # 23
    pack buffer ref commit parser  # 24
    socket worker socket handle remote object remote  # 25
    parser header delta pack blob object parser handle ref  # 26
    rebase stream delta header blob rebase  # 27
    buffer header handle merge merge token blob rebase  # 28
    rebase tree blob stream stream merge blob rebase blob cache  # 29
    stream buffer tree queue branch commit pack token remote window header cache  # 30
    parser worker commit token merge window pack handle branch rebase pack  # 31
    token cache worker commit rebase cache socket stream remote merge header pack  # 32
    remote header tree remote remote remote commit cache tree delta  # 33
    blob header remote delta remote window rebase parser tree commit buffer  # 34
    pack commit object token token tree blob socket handle window ref stream  # 35
    ref handle queue delta buffer socket merge stream  # 36
    blob worker cache delta queue remote buffer  # 37
    tree window header handle worker index pack tree  # 38
    parser header worker ref header branch merge window socket object blob  # 39
    window remote merge merge window parser blob queue delta remote object object  # 40
    handle tree ref parser buffer tree handle pack  # 41
    pack queue cache worker buffer merge merge cache worker  # 42
    stream token rebase ref worker buffer  # 43
    rebase buffer parser index  # 44
    tree object commit ref cache blob queue header delta object  # 45
    buffer branch handle index token rebase worker  # 46
    merge ref token window tree  # 47
    index stream queue parser  # 48
    branch rebase remote merge socket  # 49
    worker parser worker branch  # 50
    header blob rebase remote queue  # 51
    rebase queue handle object rebase stream socket buffer tree parser object socket  # 52
    parser object socket tree remote token tree  # 53
    handle header blob remote blob blob  # 54
    remote object blob commit buffer queue worker ref  # 55
    blob remote queue commit header  # 56
    ref queue ref window branch branch ref queue parser worker merge  # 57
    stream index remote queue header  # 58
    token index merge tree remote queue socket remote parser window  # 59
    stream tree header object  # 60
    delta branch handle tree blob ref worker worker remote  # 61
    pack token header buffer index window socket worker delta object socket header  # 62
    header buffer merge remote cache object merge commit worker remote header ref  # 63
    pack tree window stream delta object remote remote object branch  # 64
    rebase index blob parser merge index token blob worker socket stream tree  # 65
