    rebase stream blob parser queue worker socket header handle  # 0
    index stream delta socket buffer socket commit delta  # 1
    worker blob index ref delta worker commit parser buffer  # 2
    stream cache queue cache merge parser remote remote delta parser  # 3
    rebase queue token remote blob header branch window remote socket  # 4
    socket ref queue tree queue index object header queue  # 5
    pack ref merge blob window stream pack cache blob worker tree merge  # 6
    queue socket branch buffer window remote parser  # 7
    pack ref remote index ref object index  # 8
    blob parser token pack parser delta object object token delta tree  # 9
    object window handle pack merge index commit delta stream handle socket  # 10
    branch pack token merge ref queue index ref handle queue  # 11
    tree delta socket handle queue socket tree tree header branch  # 12
    header queue worker index rebase parser  # 13
    object token stream cache stream cache ref rebase cache header  # 14
    commit blob blob object  # 15
    handle tree socket socket merge ref stream socket worker commit delta delta  # 16
    remote object remote blob ref buffer  # 17
    object window buffer cache pack branch token  # 18
    parser pack branch tree  # 19
    tree parser parser queue blob socket remote handle  # 20
    ref queue remote tree commit remote  # 21
    object parser buffer pack cache branch window cache tree ref branch object  # 22
    object pack ref object object stream delta index remote  # 23
    commit socket index worker merge  # 24
    delta tree stream delta merge commit cache delta stream  # 25
    buffer rebase branch buffer branch stream  # 26
    queue cache commit blob token window buffer remote  # 27
    delta blob pack handle delta queue socket stream header socket pack  # 28
    handle commit index worker object token worker header delta  # 29
    remote tree worker header window remote token  # 30
    merge queue rebase window window pack blob branch header  # 31
    tree object queue index handle token blob  # 32
    header window handle remote queue handle handle delta queue handle pack  # 33
    delta pack queue pack object object worker blob queue window merge worker  # 34
    parser delta rebase handle window  # 35
    object ref ref token buffer buffer remote merge header  # 36
    parser window index queue tree header buffer pack remote ref token pack  # 37
    remote rebase window pack window cache object token worker  # 38
    branch socket tree cache worker handle window merge cache cache object buffer  # 39
    parser window parser window header tree branch commit header queue  # 40
    parser cache worker blob header socket  # 41
    queue queue cache cache remote ref commit branch stream worker  # 42
    commit index buffer header worker token rebase rebase queue rebase index handle  # 43
