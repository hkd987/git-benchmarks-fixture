    handle blob commit handle tree remote queue socket index tree ref commit  # 0
    remote queue token socket branch index worker merge cache pack merge index  # 1
    pack pack pack token header socket branch rebase commit window  # 2
    commit blob token parser ref window blob ref buffer window worker handle  # 3
    index ref socket parser remote blob header window remote stream  # 4
    cache token socket cache commit commit remote delta blob tree token  # 5
    header commit remote rebase stream ref worker branch buffer merge branch object  # 6
    window queue pack delta buffer  # 7
    remote token header remote buffer buffer handle branch  # 8
    object queue ref queue rebase rebase worker tree commit  # 9
    object header merge branch window handle  # 10
    index remote remote object commit worker stream parser  # 11
    rebase handle object blob tree object delta window  # 12
    object tree header cache remote remote rebase cache token pack worker tree  # 13
    worker pack window queue branch window branch handle ref branch  # 14
    rebase blob merge handle  # 15
    object ref window header header object rebase  # 16
    tree delta pack worker pack  # 17
    delta delta handle worker queue buffer  # 18
    remote tree delta window handle rebase rebase header  # 19
    object cache blob index remote rebase ref index tree handle  # 20
    ref delta merge cache delta stream pack buffer  # 21
    handle commit commit cache parser index  # 22
    header header socket header token buffer  # 23
    blob queue window blob queue commit cache  # 24
    parser token merge rebase parser delta handle commit rebase cache stream  # 25
    pack commit token delta  # 26
    ref merge queue parser pack  # 27
    handle stream blob queue branch blob token ref  # 28
    rebase merge stream delta rebase parser buffer socket worker  # 29
    object delta window object index delta buffer  # 30
    queue blob commit tree window parser commit rebase remote  # 31
    object buffer branch remote  # 32
    delta socket rebase merge parser cache socket branch tree window  # 33
    remote delta parser cache cache commit object tree rebase pack stream  # 34
    merge object buffer buffer merge  # 35
    rebase tree queue buffer parser delta pack remote  # 36
    cache header token index remote  # 37
    parser merge rebase delta pack stream delta  # 38
    parser socket pack merge queue window queue delta blob parser  # 39
    buffer socket worker object pack  # 40
    index blob handle parser token delta header blob worker delta header  # 41
    parser socket window tree rebase remote parser  # 42
    header worker merge token socket handle  # 43
    tree worker ref index header parser  # 44
    handle cache branch blob buffer object cache  # 45
    stream delta commit cache tree cache  # 46
    index index commit stream  # 47
    blob rebase queue branch branch ref handle remote ref pack  # 48
    object token ref parser merge token commit branch cache window handle rebase  # 49
    remote delta window queue stream remote index merge object tree queue  # 50
    stream index ref merge worker stream token branch object  # 51
    remote object worker cache buffer  # 52
    worker blob merge socket merge ref window header  # 53
    header delta blob rebase commit tree merge index delta  # 54
    delta tree socket token cache window stream  # 55
    parser window parser parser commit object stream rebase delta ref  # 56
    window remote parser stream merge index window index merge stream  # 57
    rebase handle token parser branch tree branch token  # 58
    worker remote rebase branch window  # 59
    stream blob object commit ref  # 60
    queue handle branch pack parser worker socket queue token cache cache  # 61
    cache index index merge merge queue socket object socket commit worker  # 62
    blob blob merge commit pack  # 63
    handle buffer stream branch stream socket parser object stream commit blob  # 64
    ref worker buffer index  # 65
    cache parser tree worker blob queue pack branch rebase  # 66
    delta queue index buffer tree  # 67
    queue handle socket object queue handle  # 68
    worker buffer object handle commit queue pack blob  # 69
