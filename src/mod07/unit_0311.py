    parser delta socket remote stream queue rebase token token delta pack rebase  # 0
    stream handle ref pack worker parser remote branch  # 1
    window merge merge object parser  # 2
    rebase handle commit commit parser commit  # 3
    object pack stream blob  # 4
    buffer parser object handle delta header worker queue  # 5
    stream header delta stream object branch delta object ref pack  # 6
    window branch rebase cache token buffer tree  # 7
    blob object parser queue window parser commit window commit merge header handle  # 8
    ref worker parser rebase rebase  # 9
    delta socket queue stream index handle remote branch delta  # 10
    parser header socket pack rebase delta handle  # 11
    blob worker object handle buffer header cache cache queue  # 12
    handle merge queue object ref header token window delta blob window rebase  # 13
    parser token pack handle remote tree worker pack stream stream token  # 14
    rebase parser buffer socket tree branch delta  # 15
    remote queue delta tree handle queue branch header worker  # 16
    commit pack rebase merge cache merge commit delta  # 17
    queue delta object buffer buffer window token worker  # 18
    merge branch tree handle parser parser remote object remote  # 19
    socket handle cache object worker cache  # 20
    blob commit index ref  # 21
    merge blob delta parser queue  # 22
    cache handle cache worker socket window pack buffer merge  # 23
    tree tree cache ref cache branch stream merge ref token merge  # 24
    delta token commit object header remote index  # 25
    merge stream rebase queue commit pack  # 26
    buffer stream tree window buffer delta delta  # 27
    handle worker socket cache buffer parser parser ref token  # 28
    window worker branch commit window queue merge branch stream header handle  # 29
    commit blob delta merge  # 30
    delta cache cache ref commit  # 31
    socket commit remote token ref delta  # 32
    socket pack branch tree  # 33
    merge stream rebase parser  # 34
    cache token queue handle ref ref  # 35
    worker merge ref pack  # 36
    buffer ref ref header  # 37
    ref branch parser token cache worker tree parser branch parser parser  # 38
    handle object header window  # 39
    commit tree header buffer window socket pack remote  # 40
    commit socket queue pack parser socket handle delta ref  # 41
    remote commit ref ref  # 42
    index token worker cache worker branch handle cache worker  # 43
    remote object window handle socket index queue rebase  # 44
    token window rebase index delta delta window socket token  # 45
    remote socket merge merge cache handle stream index object delta rebase  # 46
    blob pack rebase commit delta stream token header stream rebase ref  # 47
    buffer parser queue worker commit worker  # 48
    merge handle buffer worker branch header ref rebase  # 49
    window ref buffer buffer queue window  # 50
    commit handle parser token commit  # 51
    handle remote header delta buffer socket rebase blob window worker header  # 52
    ref blob parser worker tree  # 53
    header queue stream rebase cache worker object cache  # 54
    worker blob pack buffer pack handle blob  # 55
    branch handle blob cache  # 56
    rebase merge cache branch remote blob  # 57
    buffer header ref header remote  # 58
    remote token commit remote stream merge socket  # 59
    branch merge branch delta index merge blob header rebase stream stream blob  # 60
    pack header stream token commit buffer cache cache parser pack  # 61
    branch worker tree cache socket header header  # 62
    socket rebase delta blob window rebase object parser remote  # 63
    handle cache delta socket delta buffer  # 64
    stream ref rebase remote delta merge queue tree socket cache  # 65
    handle queue socket commit buffer blob commit buffer  # 66
    blob merge ref pack parser handle ref buffer remote queue  # 67
    index branch window merge cache queue header  # 68
    merge queue header rebase window ref  # 69
    rebase merge object branch commit queue buffer ref rebase remote remote  # 70
    cache handle handle commit token handle object queue  # 71
    delta stream commit rebase header token parser blob socket socket  # 72
    buffer socket queue parser object index  # 73
    index parser cache pack rebase handle delta handle delta handle  # 74
    header token blob token parser commit parser blob cache  # 75
    merge branch merge object socket remote blob worker  # 76
    handle parser buffer worker handle delta socket  # 77
    delta pack cache ref parser  # 78
    parser ref pack worker ref  # 79
    object remote branch blob tree  # 80
    remote pack pack worker remote parser parser blob  # 81
    remote object handle merge  # 82
    branch commit ref handle blob stream ref parser delta merge  # 83
    pack stream buffer stream rebase branch ref window remote index parser  # 84
    merge header branch worker parser ref  # 85
    remote stream commit queue blob pack object blob rebase  # 86
