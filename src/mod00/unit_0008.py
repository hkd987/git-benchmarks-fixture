    pack header rebase handle header token index commit token worker  # 0
    pack remote buffer cache object pack blob window token rebase ref handle  # 1
    header index delta branch stream  # 2
    buffer socket branch cache socket commit index ref index tree pack  # 3
    branch rebase branch blob object stream queue  # 4
    header socket buffer token object tree commit cache  # 5
    merge delta token object blob blob commit remote rebase ref remote remote  # 6
    commit parser socket ref merge  # 7
    handle stream token window worker remote handle tree queue  # 8
    parser remote branch header merge delta merge ref buffer rebase rebase  # 9
    queue stream stream socket socket buffer  # 10
    queue branch window object merge remote queue parser token stream  # 11
    parser merge blob cache branch socket commit blob branch merge  # 12
    tree buffer socket token  # 13
    rebase delta queue window commit merge tree header blob  # 14
    rebase blob token remote header blob socket window pack parser branch commit  # 15
    pack cache object header worker handle ref token parser socket token  # 16
    delta object index cache worker tree index blob token  # 17
    merge commit header header cache commit delta pack window object tree delta  # 18
    cache worker remote queue merge branch delta  # 19
    token handle cache delta commit queue  # 20
    stream socket remote handle  # 21
    remote remote token socket rebase branch  # 22
    blob index commit buffer delta buffer  # 23
    tree index parser tree worker worker commit handle blob queue  # 24
    token stream window commit object rebase delta object index index commit  # 25
    merge token cache ref index  # 26
    commit socket remote pack stream buffer object  # 27
    index queue stream buffer window ref queue window merge remote window  # 28
    parser window queue buffer rebase object tree rebase  # 29
    worker buffer object ref remote remote commit header blob window  # 30
    merge worker merge tree rebase object socket  # 31
    object socket pack token merge  # 32
    stream window cache branch header queue ref ref socket  # 33
    merge socket pack branch  # 34
    blob buffer pack rebase header tree pack handle window rebase tree merge  # 35
    stream worker rebase branch header header buffer branch stream tree delta  # 36
    rebase ref queue pack rebase worker  # 37
    commit blob header token rebase pack handle delta  # 38
    buffer cache delta pack window handle branch index commit remote  # 39
    header merge stream rebase worker stream  # 40
    object ref parser branch ref pack header handle header pack  # 41
    worker merge handle ref branch delta worker buffer commit  # 42
    cache remote pack pack  # 43
    cache header cache parser token blob socket cache queue queue commit token  # 44
    remote worker handle branch worker  # 45
    buffer parser commit blob pack branch queue merge remote handle  # 46
    header pack pack index pack  # 47
    rebase object pack commit queue header parser worker stream parser blob rebase  # 48
    stream queue worker merge buffer token cache delta pack  # 49
    queue socket delta remote index  # 50
    ref pack stream queue index tree socket  # 51
    window merge remote delta cache  # 52
    cache socket stream tree object  # 53
    tree token token buffer parser branch remote ref cache tree index pack  # 54
    buffer rebase parser window socket parser ref  # 55
    tree branch commit window cache handle  # 0
    object remote commit cache parser pack socket  # 1
    parser pack branch stream ref remote cache blob remote worker  # 2
    delta socket token header merge commit worker  # 3
    index tree index pack  # 4
    cache socket object token token token rebase tree  # 5
    socket header ref window tree branch  # 6
    delta branch branch queue stream token handle pack pack  # 7
