    blob parser blob index ref remote window cache token blob handle  # 0
    token handle cache tree tree stream socket merge  # 1
    socket socket object cache index  # 2
    index stream ref worker branch rebase cache header  # 3
    buffer tree tree ref window blob buffer window buffer remote stream  # 4
    queue cache queue remote blob rebase pack remote pack remote worker rebase  # 5
    rebase blob delta header cache tree token window stream branch pack branch  # 6
    pack pack window index blob blob window branch header tree blob parser  # 7
    queue commit commit header index commit token pack  # 8
    blob merge rebase cache stream ref commit rebase buffer token  # 9
    worker cache merge ref pack  # 10
    socket merge branch branch object remote token commit pack object  # 11
    remote blob header handle handle  # 12
    stream ref pack branch  # 13
    buffer rebase header queue pack branch header ref ref object  # 14
    token remote token remote token  # 15
    pack pack tree commit branch header buffer socket window  # 16
    handle socket rebase blob  # 17
    queue queue parser buffer buffer commit queue commit index remote remote  # 18
    pack blob buffer pack  # 19
    stream handle ref parser commit handle remote tree queue ref index  # 20
    worker delta window socket delta ref  # 21
    socket commit merge parser merge worker worker worker parser delta index socket  # 22
    blob header object index  # 23
    rebase tree handle pack delta index stream remote token  # 24
    queue object window cache buffer handle socket  # 25
    object delta queue cache ref ref blob tree token  # 26
    commit tree merge buffer delta stream commit  # 27
    parser token parser parser parser blob  # 28
    worker header remote cache buffer queue header handle tree ref object  # 29
    blob buffer buffer index remote  # 30
    token socket queue delta merge rebase worker buffer delta rebase delta cache  # 31
    queue handle delta ref queue  # 32
    window blob socket cache rebase object  # 33
    buffer tree header worker merge branch cache queue  # 34
    index queue branch stream parser remote token  # 35
    delta commit merge object index object delta  # 36
    worker worker tree queue ref window stream  # 37
    merge branch branch handle index token commit object object  # 38
    queue object token delta rebase header  # 39
    handle cache parser branch buffer ref commit  # 40
    header buffer parser commit stream index pack branch  # 41
    commit object rebase cache branch delta header  # 42
    header socket index stream index blob  # 43
    merge pack worker ref rebase  # 44
    token queue ref stream rebase buffer window rebase token header window  # 45
    queue tree commit token remote remote token remote  # 46
    window index token blob cache  # 47
    merge object buffer branch blob remote window cache worker window  # 48
    tree socket queue stream object  # 49
    socket buffer token parser window tree worker queue stream delta  # 50
    cache pack worker ref delta handle cache pack  # 51
    object ref parser buffer branch branch ref  # 52
    blob stream remote tree commit header blob branch worker branch header header  # 53
    object merge socket pack commit buffer  # 54
    cache delta index ref socket handle stream socket delta token  # 55
    parser blob handle delta delta delta branch handle buffer worker worker delta  # 56
    rebase tree merge blob  # 57
    pack delta parser cache stream queue parser worker  # 58
    cache index cache merge socket remote merge delta index  # 59
    branch queue remote token buffer index  # 60
    queue object cache tree blob object  # 61
    index socket handle token handle pack stream  # 62
    remote stream index socket socket  # 63
    ref parser buffer delta delta token window  # 64
    rebase object remote blob blob header header buffer cache stream pack buffer  # 65
    socket header object merge delta merge blob cache window token worker parser  # 66
    buffer queue commit header blob merge branch cache ref delta commit  # 67
    header tree branch token blob branch rebase remote branch pack stream blob  # 68
    index rebase merge index tree rebase ref window remote  # 69
    object remote object header socket cache object  # 70
    stream window rebase window  # 71
