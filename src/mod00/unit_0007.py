    object window window merge token  # 0
    rebase pack token queue rebase object index pack remote remote branch  # 1
    token object stream cache tree cache  # 2
    rebase tree rebase worker token token  # 3
    token token socket window delta  # 4
    remote cache commit object parser cache blob ref delta merge  # 5
    queue pack index ref delta stream rebase parser  # 6
    buffer merge blob merge queue object blob pack delta token parser  # 7
    blob remote handle rebase remote merge delta blob header  # 8
    buffer handle index branch delta tree buffer  # 9
    token branch remote tree delta pack remote token handle blob  # 10
    rebase parser window window pack index branch window handle object delta token  # 11
    pack delta pack handle commit index index tree blob stream pack remote  # 12
    header index socket socket commit socket  # 13
    token handle socket ref worker token tree header rebase cache commit  # 14
    window queue index parser branch blob socket header  # 15
    parser pack cache worker blob rebase index socket socket  # 16
    blob header merge commit pack ref branch queue queue  # 17
    cache header index object delta queue delta tree blob  # 18
    commit header stream blob  # 19
    buffer pack header buffer  # 20
    cache ref header cache ref buffer socket index stream index blob blob  # 21
    index merge handle delta remote  # 22
    rebase header tree index queue delta rebase object branch worker remote object  # 23
    commit stream handle queue worker object  # 24
    remote merge merge rebase delta parser merge ref window  # 25
    token buffer index rebase token token worker socket blob index  # 26
    queue ref worker queue tree pack  # 27
    worker parser merge merge stream stream queue header branch ref  # 28
    object index blob remote blob header delta  # 29
    commit buffer index index rebase worker buffer ref merge  # 30
    stream object socket token  # 31
    buffer header stream blob object stream ref queue  # 32
    object socket branch merge socket pack worker header handle merge ref commit  # 33
    remote commit window window handle  # 34
    delta worker cache parser  # 35
    delta pack buffer tree token handle tree header token stream commit tree  # 36
    queue blob commit buffer token index buffer  # 37
    pack buffer cache handle remote commit index pack ref remote  # 38
    parser pack parser tree index delta parser handle branch  # 39
    index queue merge buffer handle ref merge  # 40
    stream rebase object header object stream socket blob pack tree merge handle  # 41
    token header window merge handle branch  # 42
    branch pack index rebase index  # 43
    rebase rebase rebase buffer merge  # 44
    parser blob merge ref token  # 45
    queue rebase handle tree remote merge  # 46
    worker window socket header branch cache worker header branch  # 47
    rebase buffer worker object pack window  # 48
    delta branch header delta  # 49
    ref pack handle handle queue stream commit object header  # 50
    tree branch handle pack ref delta worker index rebase merge socket object  # 51
    commit stream object stream  # 52
    index queue ref cache tree window parser commit cache  # 53
    queue object merge queue  # 54
    window socket object window commit  # 55
    pack pack index token buffer worker pack object merge ref socket  # 56
    socket rebase merge merge window merge worker  # 57
    ref object remote branch pack blob token header handle remote window worker  # 58
    worker delta pack buffer delta  # 59
    merge window handle stream token stream parser queue handle object index blob  # 60
    rebase parser object worker worker ref cache  # 61
    queue buffer rebase worker socket merge branch worker pack  # 62
    window rebase remote object buffer buffer header socket stream pack  # 63
    buffer queue pack pack handle socket index handle  # 64
    object socket stream object pack socket branch stream token worker  # 65
    pack tree header queue handle commit tree object  # 66
