    parser commit object handle worker ref tree rebase  # 0
    header window window branch delta blob tree parser branch  # 1
    delta cache handle merge  # 2
    commit branch remote token handle  # 3
    tree merge stream tree ref stream  # 4
    parser socket cache socket ref index pack token buffer cache  # 5
    object object blob blob handle queue object queue ref worker ref  # 6
    window commit commit object index  # 7
    remote buffer handle header commit pack merge commit token queue socket  # 8
    handle pack handle handle index merge cache  # 9
    branch rebase stream buffer pack worker stream delta object  # 10
    tree ref token delta rebase queue stream queue object  # 11
    handle merge index token branch header commit branch  # 12
    parser cache token remote  # 13
    token header handle rebase blob header queue  # 14
    header blob ref remote rebase worker delta handle  # 15
    tree queue queue socket window delta blob pack tree branch object  # 16
    socket blob handle header handle  # 17
    queue object blob window index queue buffer header queue rebase merge tree  # 18
    queue merge worker buffer  # 19
    header delta object delta commit object remote worker handle cache branch token  # 20
    cache rebase cache remote pack delta delta merge buffer index  # 21
    rebase parser parser pack rebase rebase pack parser socket merge object queue  # 22
    index queue index rebase  # 23
    rebase header blob stream header delta pack commit object merge index  # 24
    branch merge header pack socket index queue window remote blob socket  # 25
    header cache window cache delta window commit  # 26
    token ref cache delta  # 27
    socket remote handle header token header ref commit blob  # 28
    branch header merge cache header blob window token handle  # 29
    buffer commit index token  # 30
    handle commit blob remote ref buffer parser  # 31
    pack cache socket commit window window index handle blob  # 32
    commit branch token tree token object remote pack  # 33
    rebase parser delta merge  # 34
    worker buffer window queue queue handle buffer buffer commit stream pack  # 35
    pack stream ref remote worker token merge socket parser  # 36
    delta pack cache rebase branch worker commit tree queue merge  # 37
    socket object merge tree token  # 38
    object socket worker remote rebase handle stream token pack socket  # 39
    queue delta index token buffer merge  # 40
    index delta worker blob tree  # 41
    tree header queue branch ref tree  # 42
    window pack object window buffer ref merge stream  # 43
    stream rebase rebase socket commit merge cache handle commit  # 44
    handle queue token stream queue merge header worker commit  # 45
    tree pack cache commit branch blob index branch handle  # 46
    parser window parser token buffer stream  # 47
    window pack object cache buffer  # 48
    buffer merge rebase worker header tree parser commit merge worker  # 49
    header commit cache buffer stream buffer blob header index  # 50
    handle branch socket delta rebase commit delta index token  # 51
    index remote pack token token rebase header token commit delta tree index  # 52
    object queue ref remote socket merge delta commit queue branch tree  # 53
    window worker pack cache window blob tree buffer merge tree rebase blob  # 54
    index parser token ref socket ref worker socket worker window pack  # 55
    rebase remote queue delta token delta token ref queue  # 56
    queue object merge branch rebase cache  # 57
