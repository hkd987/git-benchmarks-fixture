    header blob rebase stream  # 0
    commit header merge token merge  # 1
    header branch socket stream  # 2
    index ref token delta object buffer token window handle window  # 3
    branch object window parser handle  # 4
    remote window stream socket rebase  # 5
    stream cache ref cache buffer queue blob queue  # 6
    blob remote merge socket branch ref handle  # 7
    handle commit queue pack commit index parser token handle header token  # 8
    blob blob remote ref socket  # 9
    parser index handle rebase merge cache object  # 10
    tree ref remote ref ref window worker token index tree index buffer  # 11
    merge rebase index header stream rebase token delta index  # 12
    worker ref header rebase blob handle merge window pack object  # 13
    stream delta object handle object  # 14
    window branch handle buffer delta socket branch index token  # 15
    token queue commit commit header  # 16
    socket tree token queue worker merge  # 17
    socket index index header token index merge window rebase header blob  # 18
    parser branch parser cache merge merge tree  # 19
    buffer window worker worker commit socket header object window worker branch  # 20
    token blob stream socket buffer queue stream  # 21
    cache blob worker queue socket cache pack merge header  # 22
    buffer tree token buffer  # 23
    window blob buffer cache merge tree delta token handle handle  # 24
    remote parser stream object remote remote index window buffer index  # 25
    window blob rebase branch blob merge queue rebase stream  # 26
    object window rebase branch buffer stream buffer header  # 27
    header tree header buffer object delta ref commit commit  # 28
    header queue merge rebase object stream worker worker buffer  # 29
    queue buffer commit branch parser commit token remote delta tree blob remote  # 30
    pack token header worker pack header stream tree stream object remote buffer  # 31
    queue commit rebase commit rebase merge cache  # 32
    ref worker token queue socket queue parser blob remote worker  # 33
    cache ref window handle worker tree commit cache ref socket parser handle  # 34
    commit delta worker branch  # 35
    parser rebase worker commit delta token  # 36
    worker token window pack worker cache  # 37
    blob stream remote parser branch cache remote stream cache header  # 38
    blob object ref rebase index merge  # 39
    handle socket index rebase rebase rebase handle commit stream stream  # 40
    commit merge blob index  # 41
    worker parser delta pack pack index object cache  # 42
    token remote rebase merge  # 43
    object window object ref socket blob window delta branch branch buffer  # 44
    tree object cache worker header delta socket  # 45
    branch rebase cache cache object commit index index socket blob blob  # 46
    cache pack commit handle object worker handle handle rebase delta  # 47
    index queue handle parser object tree cache rebase  # 48
    pack branch socket cache handle object delta pack handle parser  # 49
    queue object parser header token rebase worker window  # 50
    token pack rebase object rebase parser branch queue handle handle window rebase  # 51
    delta worker merge header tree header stream rebase merge object buffer  # 52
    remote branch blob merge  # 53
    handle blob buffer handle object header cache tree socket tree queue  # 54
    index worker token socket remote tree parser buffer queue parser branch  # 55
    stream remote commit blob token tree branch window queue  # 56
    object cache cache window branch  # 57
    ref pack socket handle merge tree window tree commit commit  # 58
    rebase socket worker index merge  # 59
    buffer delta socket index  # 60
    rebase parser cache header token socket worker worker index  # 61
    blob remote remote branch rebase buffer ref object delta token branch ref  # 62
    delta merge window buffer window  # 0
    cache stream remote pack buffer  # 1
    worker handle cache object header branch pack token  # 2
    worker pack rebase tree cache token parser  # 3
    branch window window buffer pack handle pack  # 4
    tree ref branch window queue cache delta remote token rebase  # 5
    tree token delta delta tree queue branch rebase remote ref index branch  # 6
    delta object rebase buffer  # 7
    queue cache cache token blob pack worker blob merge rebase index  # 8
    commit tree branch delta delta token rebase worker header window  # 9
    ref queue remote window handle queue commit merge handle  # 10
    delta commit blob parser buffer ref  # 11
    tree handle window blob stream delta branch handle parser remote  # 12
    tree blob merge ref ref handle parser delta buffer  # 13
    parser handle worker rebase pack buffer commit stream  # 14
    header tree worker object branch  # 0
    commit header index pack branch  # 1
    parser index handle branch handle queue delta queue delta index buffer socket  # 2
    header parser pack remote header stream remote merge  # 3
    parser delta commit socket rebase parser header branch merge pack  # 4
    delta remote object queue window remote delta merge merge window  # 5
    handle socket token queue  # 6
    object cache cache queue  # 7
