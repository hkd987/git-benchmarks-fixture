    tree delta object index header  # 0
    object ref rebase index object worker tree worker tree worker stream window  # 1
    branch header stream handle blob window index object delta branch queue commit  # 2
    commit socket tree delta tree cache object  # 3
    queue header parser worker socket remote handle rebase delta commit handle branch  # 4
    commit rebase stream cache rebase branch remote parser remote commit  # 5
    window tree header rebase header merge handle header parser cache  # 6
    blob pack rebase commit delta branch blob window remote  # 7
    worker blob remote rebase tree ref blob remote index merge window  # 8
    buffer pack stream remote merge worker window parser queue ref delta  # 9
    delta remote queue tree buffer parser handle tree  # 10
    merge buffer stream remote merge commit delta  # 11
    blob buffer pack commit cache queue parser pack tree stream  # 12
    branch ref object cache parser rebase merge cache parser  # 13
    buffer tree header commit remote  # 14
    handle stream object delta queue queue blob  # 15
    merge header delta remote merge parser cache tree window window merge tree  # 16
    socket ref branch worker token  # 17
    ref token window tree ref  # 18
    header commit window cache handle worker object handle stream  # 19
    header socket rebase header index stream delta stream object handle pack worker  # 20
    merge object stream commit object buffer  # 21
    cache worker cache socket branch token remote window branch worker buffer  # 22
    ref pack merge delta pack worker token remote window commit commit blob  # 23
    merge index stream queue header pack window stream header  # 24
    cache parser header merge commit delta socket token worker commit  # 25
    header stream rebase token socket index buffer rebase stream  # 26
    commit tree remote delta object buffer merge cache worker index handle  # 27
    cache queue parser header stream pack parser branch  # 28
    branch index blob delta window rebase tree queue  # 29
    token queue object header token cache ref commit  # 30
    tree worker worker pack handle  # 31
    tree tree rebase tree token  # 32
    index merge commit tree stream parser queue worker  # 33
    ref delta token header stream stream buffer  # 34
    buffer branch branch merge  # 35
    socket socket socket pack buffer stream object blob queue ref remote parser  # 36
    delta token delta remote index worker index  # 37
    commit cache index remote socket  # 38
    object index ref socket stream window ref ref socket  # 39
    ref worker delta token socket ref delta commit index handle merge  # 40
    blob handle window rebase  # 41
    socket stream merge token delta commit window blob  # 42
    cache branch parser buffer pack delta queue window handle blob  # 43
    blob commit tree buffer worker tree object remote index branch cache  # 44
    merge rebase socket ref remote remote  # 45
    cache stream merge ref index worker buffer commit queue stream  # 46
    index header header token rebase merge remote merge header delta delta tree  # 47
    commit tree commit rebase ref remote blob  # 48
    buffer merge rebase header remote commit cache merge queue buffer branch  # 49
    tree cache merge token parser blob delta  # 50
    socket index merge stream cache index token stream worker pack tree worker  # 51
    branch delta object worker  # 52
    window queue commit rebase rebase queue cache  # 53
    merge token parser delta stream token  # 54
    header queue token window pack blob header socket stream tree  # 55
    buffer index blob socket object handle stream remote blob merge queue  # 56
    branch remote window window merge branch worker rebase handle tree tree cache  # 57
    rebase window pack cache  # 58
    blob queue branch rebase worker tree  # 59
    delta tree commit delta  # 60
    buffer object parser token rebase ref branch cache remote rebase handle  # 61
    object merge blob object window  # 62
    tree ref stream queue cache stream merge remote pack merge stream  # 63
    stream queue window buffer blob branch commit index cache ref queue  # 64
    delta ref branch queue tree commit cache cache merge stream  # 65
    stream remote blob merge tree socket  # 66
    stream branch parser queue  # 67
    remote index remote commit merge branch rebase merge window  # 68
    socket cache socket branch rebase handle socket buffer commit window branch  # 69
    merge token branch index  # 0
    cache queue cache socket commit window  # 1
    cache object commit delta object window remote delta merge  # 2
    header rebase branch parser blob rebase  # 3
    window branch buffer object pack commit  # 4
    delta parser ref header delta socket header object header  # 5
    cache tree pack ref index merge merge  # 6
    parser parser commit commit  # 7
    index queue handle buffer parser  # 8
    remote buffer remote handle branch  # 9
