    stream token token tree window branch queue  # 0
    remote index window cache branch tree tree  # 1
    token window socket header  # 2
    stream token merge cache delta  # 3
    merge cache socket cache delta blob  # 4
    queue merge commit stream  # 5
    delta parser header index ref object token handle rebase socket  # 6
    header ref branch buffer ref merge blob merge  # 7
    merge remote blob remote stream stream blob merge  # 8
    remote window merge merge  # 9
    buffer delta stream window  # 10
    rebase object blob cache merge tree parser  # 11
    rebase queue ref blob worker merge delta  # 12
    rebase merge handle socket commit pack buffer worker  # 13
    ref object pack stream blob socket delta socket object  # 14
    object worker buffer window commit delta worker delta buffer index pack object  # 15
    buffer object buffer blob tree branch tree ref pack queue  # 16
    cache object queue commit tree stream remote window  # 17
    parser remote rebase token parser delta blob  # 18
    tree pack branch rebase object tree window handle object delta worker  # 19
    parser worker tree token buffer cache socket token tree remote  # 20
    blob remote remote tree pack header merge  # 21
    commit stream index object socket object window index branch branch merge  # 22
    queue handle branch handle remote merge pack tree rebase  # 23
    ref buffer header pack object  # 24
    ref worker blob object parser blob  # 25
    stream blob object cache worker worker  # 26
    queue commit index blob ref  # 27
    ref socket token tree buffer cache  # 28
    token token buffer delta merge pack rebase queue  # 29
    header header stream tree  # 30
    index branch handle object remote index rebase delta token merge  # 31
    delta stream delta index  # 32
    token merge delta parser  # 33
    worker buffer pack stream  # 34
    token cache socket header header tree index tree header object queue rebase  # 35
    merge cache index index parser stream  # 36
    pack header object commit buffer token branch window  # 37
    remote buffer blob pack merge token  # 0
    worker blob handle parser delta token worker parser object header token buffer  # 1
    object ref socket remote socket  # 2
    delta remote blob commit socket buffer worker  # 3
