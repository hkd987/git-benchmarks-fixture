    window merge ref socket merge cache cache pack delta index  # 0
    pack pack pack commit delta handle worker worker parser remote stream  # 1
    merge cache merge handle window remote pack object buffer window commit parser  # 2
    delta stream handle token parser token worker merge delta stream  # 3
    index buffer socket blob  # 4
    handle branch branch index object pack  # 5
    handle header object blob parser header header tree token remote  # 6
    index commit stream buffer worker merge parser header  # 7
    remote handle remote tree socket object queue merge blob header parser  # 8
    queue ref remote worker  # 9
    buffer token socket merge  # 10
    remote remote branch window handle worker rebase cache window delta queue rebase  # 11
    merge socket worker merge branch cache ref handle handle object header  # 12
    header pack parser remote remote parser index window  # 13
    delta header tree worker socket socket socket object merge  # 14
    token blob window ref token stream buffer  # 15
    index cache ref delta header handle delta worker  # 16
    tree header merge ref merge token buffer  # 17
    socket ref rebase parser blob queue blob rebase  # 18
    blob parser object parser index header delta tree handle  # 19
    header commit rebase parser  # 20
    index ref rebase worker buffer remote  # 21
    parser queue handle buffer window  # 22
    header parser object blob socket blob stream  # 23
    blob header merge window socket buffer  # 24
    cache object object delta queue commit stream handle  # 25
    branch pack buffer index cache rebase rebase object merge  # 26
    pack blob parser commit header blob remote ref  # 27
    parser blob index pack buffer stream ref ref token merge  # 28
    commit handle delta branch blob  # 29
    ref cache index cache header branch socket socket token commit token ref  # 30
    remote queue worker buffer socket window window  # 31
    stream delta stream rebase  # 32
    cache stream parser delta buffer commit queue pack branch cache  # 33
    header pack window cache socket tree  # 34
    remote queue token header rebase window handle delta ref queue index  # 35
    parser socket tree rebase index  # 36
    ref header index handle tree cache queue buffer  # 37
    blob cache commit worker remote worker window merge object  # 38
    socket delta index branch header merge  # 39
    token ref pack blob branch rebase buffer object pack commit  # 40
    socket branch remote index index parser ref queue  # 41
    stream socket stream cache  # 42
    object merge socket branch index buffer stream pack blob pack window commit  # 43
    index queue cache merge index object header ref window delta  # 44
    pack queue merge merge remote remote remote branch buffer buffer  # 45
    blob rebase index delta window delta object tree commit  # 46
    object header blob cache ref remote socket branch  # 47
    window commit object handle window index commit delta remote  # 48
    worker worker rebase ref rebase  # 49
    branch cache worker branch socket header worker parser  # 50
    blob delta tree parser token cache queue ref token  # 51
    pack queue pack blob buffer socket rebase  # 52
    pack queue remote cache pack token commit stream tree  # 53
