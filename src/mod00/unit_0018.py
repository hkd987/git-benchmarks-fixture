    tree buffer remote buffer  # 0
    merge cache merge queue pack queue tree remote ref queue buffer header  # 1
    rebase parser tree buffer  # 2
    worker window pack token  # 3
    worker rebase token socket delta queue  # 4
    stream handle index stream tree stream stream token branch  # 5
    handle header handle parser commit window worker pack parser queue commit  # 6
    rebase stream index queue stream stream handle object queue branch remote worker  # 7
    merge window branch merge stream object  # 8
    parser rebase object blob parser handle rebase merge parser header commit tree  # 9
    ref delta index buffer window stream index header tree  # 10
    stream pack cache socket window tree blob header ref blob queue blob  # 11
    delta branch commit index delta parser branch  # 12
    ref header blob buffer pack ref remote object  # 13
    token token remote stream commit parser tree commit branch branch index handle  # 14
    delta remote token rebase pack tree  # 15
    stream ref rebase branch cache token ref pack stream handle  # 16
    pack branch rebase pack pack  # 17
    rebase worker delta socket remote parser cache index remote worker  # 18
    window delta blob cache header  # 19
    socket window branch parser rebase header handle socket rebase blob blob socket  # 20
    merge branch pack parser rebase handle pack pack  # 21
    object buffer queue worker socket handle queue  # 22
    token cache branch tree index tree  # 23
    delta buffer tree token buffer cache parser remote  # 24
    cache stream window stream blob queue  # 25
    pack rebase token merge pack token commit tree handle  # 26
    window tree handle worker index parser object pack socket  # 27
    branch pack object rebase parser branch window handle token branch ref buffer  # 28
    rebase handle tree ref ref  # 29
    merge merge blob stream window  # 30
    tree object delta tree tree window queue cache  # 31
    remote delta stream commit index merge handle object worker blob remote  # 32
    token tree index delta ref buffer buffer cache parser merge branch  # 33
    header delta tree handle window tree blob pack buffer branch ref  # 34
    branch merge delta commit  # 35
    worker remote tree buffer tree  # 36
    handle blob object stream handle cache queue merge token  # 37
    blob object header handle cache delta buffer delta delta handle  # 38
    handle socket window tree window socket worker ref  # 39
    tree token ref queue merge  # 40
    parser token pack token socket rebase pack socket delta parser parser  # 41
    cache remote socket stream window queue blob commit ref  # 42
    ref pack queue token  # 43
    remote ref rebase index buffer  # 44
    blob delta object tree token  # 45
    branch buffer queue worker worker queue rebase queue cache remote tree index  # 46
    pack commit stream parser branch  # 47
    buffer tree worker commit header commit merge token  # 48
    rebase buffer object queue stream ref index merge index  # 49
    pack blob stream object ref  # 50
    socket token token rebase parser parser worker commit tree  # 51
    blob cache header header  # 52
    ref cache cache header branch window blob tree blob queue  # 53
    token blob parser delta stream remote handle branch window  # 54
    queue socket pack window socket  # 55
    commit window remote stream  # 56
    window socket socket pack stream token window object header  # 57
    ref window header pack cache commit header  # 58
    token buffer token blob  # 59
    queue ref merge cache blob handle remote stream queue parser  # 60
    worker parser parser queue index blob window index handle window object  # 61
    worker remote worker buffer object  # 62
    buffer buffer delta index stream ref buffer handle  # 63
    header blob cache blob merge cache parser index handle socket worker queue  # 0
    remote queue index queue token socket stream  # 1
    rebase tree ref socket cache cache ref ref parser token  # 2
    cache window branch ref buffer object rebase remote queue  # 3
    handle delta ref header queue remote tree handle  # 4
    commit ref parser socket header branch handle parser window queue handle  # 5
    token remote blob merge pack tree header worker merge branch  # 6
