    window token handle cache commit buffer worker parser  # 0
    token window index ref token  # 1
    socket object pack queue commit remote index index  # 2
    delta rebase pack merge rebase window worker  # 3
    blob socket remote tree blob commit ref merge handle blob queue buffer  # 4
    cache window blob remote delta buffer blob  # 5
    pack parser object handle tree buffer  # 6
    window stream header handle branch  # 7
    index rebase handle stream  # 8
    pack blob cache socket socket pack blob handle parser cache branch  # 9
    stream cache queue blob cache worker header queue index socket handle  # 10
    queue socket stream index  # 11
    stream handle stream stream window index pack worker pack ref blob  # 12
    header header parser window worker pack rebase remote commit blob window  # 13
    commit queue tree pack pack cache remote remote pack  # 14
    delta index token queue worker buffer merge remote commit delta  # 15
    commit stream buffer remote buffer queue commit  # 16
    merge header token cache handle blob token  # 17
    window parser cache header cache window rebase delta stream tree  # 18
    buffer socket stream branch delta index object pack tree parser object  # 19
    worker token handle delta parser worker pack index  # 20
    ref worker worker index buffer cache  # 21
    handle ref pack branch commit  # 22
    rebase branch parser object pack  # 23
    parser ref commit index buffer pack delta  # 24
    header window tree socket window delta handle parser  # 25
    parser cache delta stream object  # 26
    handle branch stream header blob  # 27
    delta pack tree commit index ref cache commit cache cache window window  # 28
    ref tree object header queue buffer worker index blob delta index object  # 29
    parser handle remote socket parser socket handle  # 30
    window rebase parser merge pack merge queue queue window ref handle  # 31
    commit cache ref tree rebase index branch remote  # 32
    merge merge object window rebase rebase ref  # 33
    merge ref rebase object header tree remote  # 34
    delta ref remote index header  # 35
    remote delta worker worker rebase tree tree handle tree remote  # 36
    buffer tree token cache object object merge cache  # 37
    blob stream blob object buffer socket window header pack  # 38
    buffer stream object rebase handle queue  # 39
    stream delta rebase token merge object worker remote ref  # 40
    header header remote worker queue tree branch queue  # 41
    tree window cache ref  # 42
    pack delta delta pack pack commit token rebase worker  # 43
    ref worker buffer handle  # 44
    header queue commit token worker queue parser  # 45
    blob header merge buffer blob token parser remote tree delta buffer parser  # 46
    pack pack queue ref socket branch stream  # 47
    blob ref index commit window queue window remote merge stream  # 48
    header ref merge header  # 49
    header pack handle merge remote window handle parser delta header  # 50
    queue window header delta parser merge commit rebase blob handle queue  # 51
    window handle pack index worker remote ref delta  # 52
    pack object worker parser socket parser index delta  # 53
    index delta header socket delta  # 54
    merge blob delta branch object object object queue stream header  # 55
    ref merge buffer commit merge  # 56
    rebase branch remote rebase worker rebase object cache cache worker index queue  # 57
    token object branch parser delta  # 58
    queue buffer window merge tree worker cache rebase object branch window  # 59
    header ref window remote rebase index header object cache  # 60
    branch window queue socket tree handle ref object ref window  # 61
    pack rebase stream remote parser blob branch object  # 62
    buffer handle queue token index pack cache pack  # 63
    commit token socket socket merge worker blob remote window buffer socket  # 64
    remote blob socket tree socket delta  # 65
    stream object window buffer stream commit queue index window  # 66
    tree queue parser parser buffer worker merge header delta delta  # 67
    window buffer commit rebase tree window blob window handle  # 68
    queue delta commit commit socket remote tree token  # 69
    buffer object pack token handle delta parser  # 70
    object ref rebase stream parser cache  # 71
    queue ref remote pack ref delta cache  # 72
    blob cache handle socket object delta commit merge  # 73
    merge pack worker object object index merge  # 74
    rebase branch ref blob parser branch delta token pack pack  # 75
    delta blob commit rebase pack branch parser queue remote remote  # 76
    object tree token handle blob tree branch handle  # 77
    window pack handle cache delta index object cache stream pack  # 78
    queue socket stream object tree object tree queue blob  # 79
    token stream index index worker index ref stream handle stream  # 80
    socket window object worker remote parser commit branch merge object  # 81
