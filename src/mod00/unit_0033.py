    merge window pack delta  # 0
    commit pack queue rebase rebase stream tree socket handle rebase  # 1
    rebase tree merge handle parser handle branch parser token token index handle  # 2
    cache commit stream cache  # 3
    blob header merge blob socket ref parser ref merge stream delta  # 4
    rebase blob remote object queue tree window window token token  # 5
    handle parser commit window stream handle blob pack queue branch handle socket  # 6
    buffer branch worker stream queue remote  # 7
    blob cache delta socket pack buffer pack token object tree  # 8
    window header ref merge window tree header cache  # 9
    parser ref token header ref cache stream index handle pack  # 10
    tree rebase socket pack commit  # 11
    socket stream socket remote parser branch  # 12
    cache merge token index  # 13
    blob cache branch object window index worker parser token blob delta header  # 14
    window parser blob worker  # 15
    queue parser delta window blob buffer queue ref object  # 16
    buffer object merge branch handle window pack pack blob  # 17
    token stream rebase token merge index  # 18
    ref tree queue token blob  # 19
    window object merge buffer commit rebase remote  # 20
    worker merge ref handle index window queue buffer object  # 21
    socket ref window buffer object  # 22
    stream stream stream token handle blob  # 23
    stream socket token worker parser remote blob  # 24
    handle rebase window parser parser token rebase ref rebase blob index  # 25
    commit stream pack ref handle object blob object  # 26
    delta index branch cache object  # 27
    rebase tree commit pack  # 28
    rebase pack parser pack branch blob handle  # 29
    socket pack remote rebase buffer queue commit header pack remote header socket  # 30
    parser cache tree tree worker socket  # 31
    commit parser branch token header index commit header queue commit  # 32
    worker index queue cache branch cache rebase pack worker token commit  # 33
    parser handle tree socket object index header window stream rebase handle  # 34
    stream ref commit branch merge queue tree  # 35
    token cache ref branch merge ref cache blob object socket delta  # 36
    blob buffer worker delta  # 37
    blob socket merge object merge queue  # 38
    blob index delta ref branch  # 39
    cache pack object token  # 40
    pack queue remote stream rebase delta delta  # 41
    cache token queue object stream branch rebase  # 42
    cache commit header token queue pack socket merge token handle  # 43
    worker window remote remote handle  # 44
    tree token pack object worker commit remote rebase delta  # 45
    handle object delta delta cache  # 46
    remote pack rebase handle parser  # 47
    socket token header index  # 48
    pack buffer delta header stream delta commit branch socket tree cache  # 49
    socket handle token remote window remote window pack remote handle branch merge  # 50
    delta index tree delta merge rebase blob branch  # 51
    buffer parser ref header index pack queue  # 52
    pack parser tree tree parser remote object object delta index stream buffer  # 53
    remote remote commit merge merge rebase delta token queue index window  # 54
    header ref window rebase remote pack remote rebase  # 55
