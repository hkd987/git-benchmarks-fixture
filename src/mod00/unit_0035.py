    delta object token merge rebase socket  # 0
    queue cache handle index blob delta remote ref merge index socket  # 1
    pack buffer token socket header stream commit  # 2
    buffer buffer commit header cache index worker delta parser ref pack  # 3
    queue object pack stream parser tree socket blob  # 4
    parser branch branch window queue stream parser remote blob  # 5
    token header ref delta index buffer  # 6
    ref window buffer parser delta rebase ref worker  # 7
    pack rebase worker window header branch merge rebase blob  # 8
    ref merge remote cache worker tree index ref token rebase  # 9
    ref parser worker queue blob blob stream header object merge queue  # 10
    header socket queue worker cache cache blob token blob remote  # 11
    queue stream merge handle remote object blob buffer header queue  # 12
    index window queue remote blob worker remote socket token merge  # 13
    header queue remote remote tree delta tree pack pack worker stream blob  # 14
    header socket worker branch token ref  # 15
    buffer branch branch token tree handle socket pack parser index token handle  # 16
    buffer delta delta buffer socket branch tree cache cache token  # 17
    header ref buffer remote branch buffer buffer parser remote pack  # 18
    handle ref rebase worker branch pack worker pack stream worker branch  # 19
    pack worker branch merge buffer queue header blob rebase delta token  # 20
    index buffer token cache object branch commit blob token token  # 21
    commit stream object rebase merge cache blob  # 22
    window delta token object ref window pack pack ref remote object stream  # 23
    worker worker merge delta cache index token  # 24
    tree blob rebase object handle token rebase worker cache header window buffer  # 25
    cache index index tree index worker branch cache token index object pack  # 26
    commit header stream worker pack  # 27
    remote blob parser cache remote buffer window cache token queue tree buffer  # 28
    handle remote remote parser socket parser window  # 29
    cache header commit handle socket tree queue blob  # 30
    blob blob parser merge handle header branch tree tree blob pack  # 31
    queue tree rebase blob stream  # 32
    index cache tree blob  # 33
    cache blob header delta delta buffer handle pack merge  # 34
    parser pack worker tree pack  # 35
    delta handle index buffer remote rebase stream object parser merge token  # 36
    branch worker ref worker index socket blob stream stream header pack  # 37
    remote token commit window ref worker  # 38
    worker branch ref branch pack pack token  # 39
    stream tree socket header  # 40
    window pack commit branch token buffer index  # 41
    ref stream tree parser object socket worker ref tree tree token  # 42
    header object rebase remote queue header object parser worker handle delta socket  # 43
    tree branch window queue tree branch merge worker token buffer handle  # 44
    buffer remote rebase header cache pack queue branch commit  # 45
    pack queue pack delta object remote worker  # 46
    tree stream remote parser buffer object buffer header socket  # 47
    parser window branch token socket cache  # 48
    remote branch commit branch  # 49
    index cache buffer remote handle commit blob tree object tree blob remote  # 50
    commit worker blob merge parser queue stream blob socket ref window ref  # 51
    window window ref remote socket pack stream tree buffer buffer index  # 52
    remote buffer pack blob branch tree parser index pack worker  # 53
    handle pack blob remote  # 54
    tree ref branch delta branch  # 55
    pack blob socket object delta  # 56
    ref remote rebase pack merge delta token remote  # 57
    commit handle commit handle socket parser  # 58
    buffer remote queue header blob  # 59
    index commit object commit  # 60
    worker window commit worker handle header remote merge merge  # 61
    blob remote token delta  # 62
    merge index object branch merge remote remote cache token blob queue pack  # 63
    object commit index commit branch merge blob buffer socket commit index  # 64
    buffer rebase index cache ref remote buffer stream index branch  # 65
    ref worker handle ref index token branch worker  # 66
    branch token buffer rebase header buffer token merge buffer rebase blob  # 67
    merge commit delta buffer branch token blob buffer  # 68
    rebase merge handle handle worker tree index  # 69
    token window window commit  # 70
    tree pack remote tree stream cache socket  # 71
    parser token queue pack socket ref ref commit  # 72
    remote worker header token stream stream socket handle remote  # 73
    cache parser socket parser remote  # 74
    commit merge tree merge remote token ref  # 75
    index buffer queue header worker blob worker ref  # 76
    stream tree delta cache header token blob commit  # 77
    remote blob parser blob index delta branch  # 78
    socket remote tree blob cache index socket merge  # 79
    ref delta remote delta buffer commit object remote parser index worker  # 80
    branch delta blob rebase header tree queue  # 81
    buffer rebase commit pack cache object queue cache queue cache branch  # 0
    ref branch header worker  # 1
    commit ref header merge blob ref object socket branch branch token header  # 2
    pack buffer branch worker remote blob cache header handle cache tree  # 3
