    header branch branch parser rebase stream queue blob index worker ref  # 0
    header merge remote header handle pack socket  # 1
    index handle worker blob buffer  # 2
    rebase queue merge branch worker window commit object  # 3
    socket buffer cache merge branch queue worker window window index window  # 4
    tree delta merge window queue worker rebase remote object cache rebase queue  # 5
    header tree handle stream  # 6
    queue stream header header cache tree  # 7
    merge blob ref token parser handle delta object cache  # 8
    delta token queue commit parser blob  # 9
    object cache header header pack object window  # 10
    worker index ref parser header  # 11
    queue window blob rebase delta cache header  # 12
    token socket object socket cache  # 13
    parser stream socket cache commit blob merge header commit handle delta pack  # 14
    header buffer pack blob buffer cache index  # 15
    tree commit index socket delta object header token  # 16
    index header header delta queue blob token merge object token commit  # 17
    remote cache cache tree  # 18
    pack pack socket worker tree handle handle rebase stream token  # 19
    blob queue stream remote token worker handle commit pack token queue  # 20
    header delta cache ref tree remote pack  # 21
    merge ref socket rebase commit object socket object cache merge queue  # 22
    pack handle handle worker worker  # 23
    window queue index window pack commit tree merge  # 24
    parser queue worker merge merge branch object  # 25
    window socket token worker object tree header buffer cache header  # 26
    tree remote object token tree merge socket object token  # 27
    stream branch header header ref header blob ref merge index  # 28
    delta commit token window worker worker rebase merge branch  # 29
    buffer object branch queue window rebase  # 30
    object merge remote tree cache blob merge stream pack branch  # 31
    socket rebase merge socket ref token handle header  # 32
    stream commit cache blob merge delta ref  # 33
    buffer handle header object queue branch parser remote commit buffer  # 34
    socket branch token stream  # 35
    header parser token tree queue socket  # 36
    handle queue blob stream header buffer worker  # 37
    delta window remote worker pack object rebase  # 38
    header pack index worker delta remote pack  # 39
    index commit merge merge merge queue header merge buffer ref  # 40
    tree worker pack buffer buffer delta tree window  # 41
    index pack worker delta ref stream tree socket buffer  # 42
    tree handle blob index remote stream buffer commit rebase header index  # 43
    token window stream blob queue ref stream parser socket cache stream  # 44
    parser header ref remote delta pack cache stream  # 45
    object token branch window merge object tree parser merge  # 46
    pack delta tree parser merge cache object header blob remote  # 47
    stream buffer rebase ref window  # 48
    rebase stream tree parser stream stream buffer blob socket buffer handle merge  # 49
    window worker branch handle socket  # 50
    branch parser header object worker window worker remote delta merge  # 51
    socket parser stream index commit  # 52
    tree stream delta commit blob merge branch parser rebase merge  # 53
    merge queue stream blob cache cache header queue commit  # 54
    index remote index remote pack blob token merge header worker token  # 55
    index remote queue window  # 56
    header object blob blob rebase  # 57
    rebase branch remote window delta  # 58
    ref ref worker branch parser  # 59
    branch buffer token token object delta index branch ref commit parser  # 60
    branch blob parser parser tree socket delta merge pack remote  # 61
    index parser queue parser buffer window  # 62
    object queue handle index handle socket queue  # 63
    blob cache index worker socket  # 64
    rebase ref tree merge stream  # 65
    tree ref object rebase remote remote object socket tree  # 66
    header cache window merge token pack delta  # 67
    buffer delta queue queue remote  # 68
    branch header rebase queue  # 69
