    buffer blob blob blob parser pack worker  # 0
    blob pack parser worker branch buffer rebase branch object merge  # 1
    index delta pack buffer object tree worker index cache remote handle merge  # 2
    branch merge window header index merge pack commit queue rebase window  # 3
    queue index pack queue ref object index token queue ref cache  # 4
    tree rebase blob handle merge header index token buffer ref blob token  # 5
    rebase tree ref blob  # 6
    queue index header tree pack pack header  # 7
    socket blob token commit commit pack tree window ref  # 8
    stream branch tree socket stream queue pack  # 9
    buffer tree pack worker pack worker  # 10
    parser handle delta blob buffer rebase  # 11
    socket blob stream header commit socket buffer socket window rebase  # 12
    handle cache pack header token commit window worker  # 13
    merge delta socket remote parser buffer merge  # 14
    token object object commit rebase cache queue blob  # 15
    cache stream buffer header tree commit tree rebase worker handle  # 16
    merge stream queue commit header object rebase branch window header rebase window  # 17
    socket branch remote delta branch blob rebase delta ref  # 18
    parser delta branch queue worker token commit blob cache  # 19
    pack worker branch worker  # 20
    blob parser window header branch remote socket ref stream handle buffer pack  # 21
    queue parser worker handle branch cache pack tree queue handle pack  # 22
    pack handle socket stream tree cache pack index cache handle  # 23
    buffer queue blob tree branch handle object queue blob remote parser ref  # 24
    token token handle remote object branch handle ref commit window object  # 25
    commit handle tree object tree commit branch handle object pack tree cache  # 26
    socket branch blob buffer commit remote blob stream handle object commit commit  # 27
    header queue commit commit window worker index blob  # 28
    blob commit delta buffer buffer window  # 29
    parser ref parser header header pack cache  # 30
    rebase header worker queue worker remote index handle commit queue object window  # 31
    stream cache handle stream blob pack remote handle rebase ref header  # 32
    socket socket worker buffer socket blob token branch rebase window merge  # 33
    rebase queue commit worker buffer header buffer queue cache index stream  # 34
    commit commit remote stream delta  # 35
    pack queue rebase delta commit branch pack buffer cache  # 36
    ref queue token pack blob object  # 37
    merge merge branch blob  # 38
    queue parser pack tree window buffer handle  # 0
    pack tree tree delta handle stream socket merge rebase handle  # 1
    buffer ref pack buffer tree pack tree rebase handle  # 2
    buffer token buffer blob index merge stream object blob queue  # 3
    object rebase parser blob buffer object  # 4
    remote socket buffer index branch window commit token  # 0
    object blob queue tree handle parser tree socket blob  # 1
    tree parser ref stream queue worker  # 2
    header commit tree blob header queue window index ref socket  # 3
    rebase buffer pack rebase ref handle cache stream header pack blob commit  # 4
    blob cache rebase blob cache branch  # 5
    buffer ref handle tree socket blob ref parser token window buffer  # 6
    worker branch window merge ref socket worker delta rebase pack header  # 7
    merge remote blob cache branch tree rebase pack commit object branch  # 8
    merge socket object remote socket header socket tree ref parser  # 9
    handle buffer pack delta handle cache index socket header commit delta stream  # 10
    parser cache queue queue token index merge merge  # 11
    token parser socket remote handle worker branch branch object delta header  # 12
