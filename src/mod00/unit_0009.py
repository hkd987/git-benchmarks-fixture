    cache remote stream cache header index handle token blob parser queue pack  # 0
    parser stream handle header tree token remote ref  # 1
    cache commit ref cache buffer buffer  # 2
    token index window branch parser rebase socket  # 3
    header merge merge socket worker tree  # 4
    merge header buffer blob handle  # 5
    blob header branch queue rebase rebase blob object object blob  # 6
    parser header window tree stream  # 7
    delta worker socket queue  # 8
    blob ref cache merge stream window handle  # 9
    merge token worker ref  # 10
    tree index object merge token  # 11
    blob stream queue cache tree  # 12
    index socket parser object ref ref  # 13
    worker worker rebase parser queue merge rebase remote object worker merge  # 14
    queue token token index header queue  # 15
    tree delta parser remote parser rebase branch window  # 16
    queue window handle stream window blob  # 17
    parser pack parser token parser stream window stream header ref branch  # 18
    index ref tree rebase delta  # 19
    delta object worker merge delta handle  # 20
    branch blob stream stream commit commit  # 21
    parser pack remote branch window socket  # 22
    socket handle parser queue worker blob  # 23
    window tree worker header token pack remote delta  # 24
    buffer merge token pack handle token tree remote pack ref  # 25
    worker branch delta remote stream stream cache header stream branch  # 26
    delta cache object token commit token object queue remote window commit blob  # 27
    token header worker tree parser  # 28
    commit ref handle header header parser  # 29
    cache token branch buffer blob window header cache window ref commit  # 30
    ref cache rebase header window object rebase  # 31
    remote object buffer stream  # 32
    remote ref handle ref remote delta tree index tree buffer  # 33
    window buffer socket ref  # 34
    pack remote pack stream delta commit rebase worker branch  # 35
    commit parser queue cache blob  # 36
    window token object window stream  # 37
    stream commit worker tree merge socket  # 38
    tree socket queue merge branch ref blob commit token rebase  # 39
    buffer rebase window stream delta cache merge remote  # 40
    buffer blob token blob commit parser token stream commit  # 41
    blob stream delta object index queue socket tree branch stream remote worker  # 42
    pack token remote socket buffer pack parser worker index buffer header buffer  # 43
    token object queue tree worker stream delta parser buffer stream buffer parser  # 44
    parser parser ref token queue remote pack  # 45
    merge handle commit blob header token remote worker remote  # 46
    handle parser index blob ref object stream index index blob  # 47
    commit stream handle object object blob handle ref token cache  # 48
    buffer rebase delta header object window cache  # 49
    token window handle blob pack  # 50
    merge rebase rebase object buffer pack token branch index  # 51
    tree buffer tree pack delta parser window commit ref  # 52
    ref object window delta window parser index socket buffer handle blob blob  # 53
    header cache index queue queue stream  # 54
    worker queue stream ref tree window  # 55
    cache handle delta cache stream index delta header rebase object branch  # 56
    queue header window queue token branch worker rebase merge stream  # 57
    commit pack worker merge tree merge  # 58
    token remote handle blob commit object rebase merge  # 59
    ref object stream queue buffer rebase ref cache index window token  # 60
    rebase ref index header window blob window index merge worker object  # 61
    queue commit buffer remote  # 62
    object worker rebase merge index stream parser header stream blob worker branch  # 63
    remote stream delta delta index cache token commit ref token ref  # 64
    worker worker blob delta  # 65
    ref pack ref worker object handle worker merge tree  # 66
    ref delta commit token window tree stream blob index window tree buffer  # 67
    cache pack ref stream pack remote  # 68
    ref token tree object index remote delta socket commit parser  # 69
    rebase delta socket cache delta stream header merge socket handle  # 70
    ref rebase object socket object parser  # 71
