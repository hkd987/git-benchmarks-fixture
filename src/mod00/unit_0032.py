    blob socket handle commit token object  # 0
    token parser remote queue window handle blob rebase token window token rebase  # 1
    parser window window blob parser branch  # 2
    pack parser header blob tree remote header object header delta  # 3
    window token window token index branch  # 4
    worker commit remote worker token handle tree socket remote queue remote  # 5
    stream remote delta cache worker  # 6
    handle rebase buffer socket delta ref queue stream index header tree  # 7
    ref object remote cache token  # 8
    remote rebase branch tree handle rebase pack commit  # 9
    rebase object parser remote stream  # 10
    cache buffer window index index commit ref blob header stream cache  # 11
    token parser rebase blob delta object ref object  # 12
    delta stream blob window worker queue branch token pack  # 13
    tree parser cache socket  # 14
    window ref handle rebase merge  # 15
    buffer object queue socket stream  # 16
    token merge delta header delta blob blob pack socket remote  # 17
    header buffer delta parser commit ref rebase buffer merge handle pack  # 18
    worker parser queue header  # 19
    queue branch ref queue commit socket branch  # 20
    queue cache window token worker  # 21
    handle window index window window rebase token index window commit header  # 22
    rebase header buffer header handle  # 23
    window merge cache commit rebase worker socket header buffer remote remote blob  # 24
    tree object merge queue handle merge buffer header remote  # 25
    cache delta pack rebase worker commit blob pack stream stream tree merge  # 26
    commit cache index ref commit tree header remote header  # 27
    parser handle cache object pack delta pack  # 28
    tree token index blob socket queue remote index ref  # 29
    merge socket worker pack pack worker blob rebase window handle tree  # 30
    remote commit ref merge handle parser commit token handle rebase  # 31
    ref stream delta branch window parser ref rebase  # 32
    merge window branch delta blob  # 33
    merge queue stream ref branch merge delta tree object branch  # 34
    socket remote blob queue token branch delta pack object  # 35
    blob queue index delta  # 0
    handle delta rebase blob queue socket window object parser  # 1
    handle pack blob worker remote handle blob  # 2
    parser pack remote remote delta worker blob branch  # 3
    ref socket handle worker header object header branch commit ref buffer  # 4
    buffer merge merge worker object queue handle token index cache rebase  # 5
    tree delta queue handle index socket buffer commit queue  # 6
    ref index tree ref worker stream ref  # 7
    branch index socket index merge tree index rebase remote header blob  # 8
    pack buffer index object queue header window delta parser header queue token  # 9
