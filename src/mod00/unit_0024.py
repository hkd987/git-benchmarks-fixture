    remote header branch buffer buffer rebase stream rebase cache  # 0
    ref pack rebase buffer window remote window rebase window  # 1
    branch branch rebase remote commit rebase buffer buffer stream socket  # 2
    remote window merge token buffer  # 3
    remote ref header handle parser parser branch merge remote  # 4
    queue ref handle merge window pack header  # 5
    remote remote rebase index handle parser queue  # 6
    parser rebase stream handle buffer commit index index  # 7
    worker stream rebase blob  # 8
    merge index stream remote token delta buffer pack remote token  # 9
    delta header parser blob window stream window parser delta header handle  # 10
    index merge worker buffer blob rebase object buffer worker rebase remote  # 11
    buffer worker header handle remote index ref stream merge  # 12
    worker queue remote delta tree blob commit ref remote window branch buffer  # 13
    index delta delta remote branch ref  # 14
    socket queue index blob delta pack rebase handle handle branch header  # 15
    ref handle pack queue window buffer ref remote remote tree pack  # 16
    buffer token header parser commit  # 17
    window handle pack delta socket queue rebase token  # 18
    stream queue stream token blob cache header queue index index remote handle  # 19
    object token ref handle cache  # 20
    index stream ref pack handle parser  # 21
    commit commit window rebase  # 22
    header header index commit stream  # 23
    remote branch cache parser worker socket handle object branch ref buffer worker  # 24
    branch window ref blob token rebase commit  # 25
    queue handle queue commit socket pack index queue buffer index commit commit  # 26
    stream blob rebase remote worker token merge socket pack blob stream queue  # 27
    worker commit parser stream remote stream  # 28
    ref socket window buffer remote branch socket branch merge  # 29
