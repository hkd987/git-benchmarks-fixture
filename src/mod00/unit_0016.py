    index blob stream object tree header index socket pack remote pack  # 0
    header window blob index branch blob stream handle  # 1
    merge token token index pack pack  # 2
    token buffer rebase parser commit handle stream merge  # 3
    queue socket window parser ref delta buffer blob window buffer  # 4
    window stream window buffer ref socket  # 5
    merge index ref header delta merge  # 6
    cache delta worker buffer socket pack queue remote queue cache rebase  # 7
    queue worker rebase blob index object queue blob token pack  # 8
    tree remote parser buffer pack  # 9
    socket cache worker parser object tree  # 10
    socket window tree worker queue  # 11
    worker header cache blob tree header token  # 12
    merge commit token commit  # 13
    buffer index window rebase ref cache buffer  # 14
    rebase stream buffer blob window queue queue rebase queue ref  # 15
    buffer buffer delta stream branch token header token token stream window  # 16
    index header object rebase window header socket worker rebase window index queue  # 17
    delta tree handle blob queue index remote queue remote handle  # 18
    blob index parser stream queue  # 19
    worker worker stream rebase handle cache token  # 20
    tree queue queue rebase cache  # 21
    handle handle index queue remote commit branch cache commit object remote  # 22
    object remote branch parser remote rebase parser index  # 23
    rebase window object merge blob pack remote blob blob pack queue socket  # 24
    commit queue queue pack token delta delta  # 25
    window index ref delta stream cache buffer remote queue commit tree  # 26
    pack handle header object socket merge stream object parser tree handle  # 27
    header ref window worker ref header merge stream delta delta  # 28
    handle queue object merge socket  # 29
    header commit parser rebase index header buffer buffer remote stream rebase  # 30
    parser blob cache token  # 31
    index buffer ref tree queue remote object header window  # 32
    delta socket ref buffer ref parser blob window buffer  # 33
