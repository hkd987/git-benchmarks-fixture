    cache delta queue ref  # 0
    blob cache buffer ref handle delta commit parser token  # 1
    cache handle blob window token header delta  # 2
    blob pack socket pack remote token window object rebase index buffer  # 3
    worker parser worker socket rebase rebase queue blob  # 4
    worker index handle buffer buffer object pack buffer  # 5
    rebase blob remote handle commit  # 6
    worker stream tree tree handle header merge  # 7
    queue queue parser object token  # 8
    branch commit commit socket merge branch ref parser delta  # 9
    socket tree cache ref  # 10
    handle stream stream socket index ref commit  # 11
    tree object rebase commit parser header rebase socket token socket delta  # 12
    merge window tree remote window cache  # 13
    commit worker worker blob stream  # 14
    token socket window merge tree stream  # 15
    queue cache window stream queue tree window header window  # 16
    merge merge ref branch cache stream queue header  # 17
    buffer tree ref token merge index parser parser pack window  # 18
    token index parser remote rebase commit buffer object window branch worker  # 19
    object delta queue delta header token queue stream window merge socket branch  # 20
    delta object cache tree index commit buffer stream object  # 21
    header merge pack stream window queue remote cache remote ref  # 22
    rebase socket socket header parser tree queue remote rebase index parser  # 23
    parser commit ref rebase delta merge parser header  # 24
    object remote delta object blob tree remote  # 25
    blob stream parser worker blob handle queue  # 26
    queue object index index header header  # 27
    delta index socket branch  # 28
    commit delta buffer stream stream remote object  # 29
    worker rebase header merge blob cache object  # 30
    merge socket queue rebase worker commit stream tree  # 31
    tree ref merge ref blob token delta cache commit  # 32
    buffer window handle pack token pack token index handle  # 33
    branch commit parser handle blob  # 34
    socket handle buffer socket  # 35
    worker window header ref merge index blob parser socket merge delta  # 36
    merge queue token header stream merge stream object delta  # 37
    socket handle pack tree parser ref window blob delta worker handle  # 38
    buffer socket branch merge cache window  # 39
    header object worker buffer socket  # 40
