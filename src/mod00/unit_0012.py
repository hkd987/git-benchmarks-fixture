    object object ref header index cache  # 0
    delta queue parser header cache tree parser queue cache stream handle worker  # 1
    rebase blob header handle window buffer delta remote socket parser stream  # 2
    branch header commit merge token buffer token queue queue cache merge handle  # 3
    ref header branch rebase blob commit stream  # 4
    buffer stream remote worker buffer index ref  # 5
    buffer parser worker header ref blob handle ref index buffer socket token  # 6
    socket tree merge ref object remote header socket socket commit parser  # 7
    commit tree blob remote blob  # 8
    socket queue stream token remote parser rebase commit socket  # 9
    worker token token ref branch delta window worker object handle delta stream  # 10
    remote handle blob stream commit window buffer object header tree  # 11
    ref ref object stream commit cache token worker  # 12
    pack cache ref token token stream commit handle window index header branch  # 13
    queue commit index merge object  # 14
    socket pack stream ref merge ref queue  # 15
    index socket delta header  # 16
    ref socket stream branch buffer  # 17
    blob remote commit commit rebase delta rebase buffer parser  # 18
    rebase window rebase window header ref socket commit  # 19
    commit index merge index blob blob header token tree ref  # 20
    token worker token remote socket header branch worker  # 21
    delta window tree rebase object  # 22
    delta worker branch object  # 23
    worker object socket blob commit queue socket header  # 24
    buffer object stream branch  # 25
    queue blob parser cache branch  # 26
    index handle index tree window socket object ref token  # 27
    header token blob stream index merge remote merge buffer ref ref branch  # 28
    branch parser delta queue rebase commit remote socket rebase rebase rebase  # 29
    parser branch branch queue object stream  # 30
    ref ref commit remote queue cache tree stream  # 31
    merge object handle stream queue token index worker remote remote window remote  # 32
    pack header remote ref blob token  # 33
    cache blob delta header blob handle pack rebase pack cache token buffer  # 34
    socket stream token parser merge worker  # 35
    window branch queue queue  # 36
    socket commit handle delta  # 37
    branch tree worker worker blob remote worker branch branch window  # 38
    queue pack merge branch commit rebase window merge  # 39
    socket cache socket rebase socket parser branch  # 40
    stream index pack parser merge object  # 41
    index merge cache merge worker  # 42
    cache tree cache window tree socket parser header socket merge parser handle  # 43
    commit blob merge rebase index branch ref blob ref commit handle queue  # 44
    header pack object rebase object header index window  # 45
    handle worker worker delta cache worker tree ref object index blob buffer  # 46
    socket ref header handle queue tree cache index header rebase buffer blob  # 47
    window pack merge window stream index parser remote queue pack socket  # 48
    parser handle blob blob  # 49
    index token window branch blob token window index queue pack rebase  # 50
    window rebase socket buffer window blob rebase  # 51
    header parser token window buffer pack  # 52
    parser header pack commit object tree branch buffer  # 53
    worker commit header branch window buffer branch header tree stream buffer cache  # 54
    tree commit commit rebase window object socket remote  # 55
    ref merge stream merge delta blob  # 56
    blob index ref handle ref pack remote buffer  # 57
    worker cache rebase parser tree commit delta index ref stream header  # 58
    worker handle tree buffer commit  # 59
    queue remote object parser index remote  # 60
    stream window object header token index worker rebase  # 0
    worker cache remote queue branch queue merge branch  # 1
    ref object stream window merge merge tree object index socket socket cache  # 2
