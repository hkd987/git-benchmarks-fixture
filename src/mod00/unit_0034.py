    buffer header stream rebase blob header rebase handle parser  # 0
    remote window pack branch handle  # 1
    blob window buffer commit merge worker remote queue pack merge pack  # 2
    ref window cache blob stream branch merge  # 3
    worker pack stream rebase blob handle index delta branch  # 4
    cache tree delta rebase rebase  # 5
    merge header object pack commit delta merge branch handle pack  # 6
    cache token branch parser handle queue merge branch merge buffer commit  # 7
    commit worker index remote stream cache merge  # 8
    buffer socket delta handle blob  # 9
    handle delta delta token buffer stream merge blob header delta worker  # 10
    remote remote index buffer index object  # 11
    blob parser ref buffer index socket pack worker handle worker token  # 12
    merge commit socket header remote  # 13
    socket ref rebase buffer window pack commit rebase handle  # 14
    buffer commit worker token cache branch  # 15
    socket buffer commit merge buffer window stream branch token ref  # 16
    parser buffer delta index stream remote rebase index header delta stream rebase  # 17
    index object token worker worker rebase cache  # 18
    parser merge tree buffer branch token header  # 19
    handle stream pack handle token  # 20
    buffer parser queue commit header branch handle branch  # 21
    worker queue pack pack stream object pack stream socket blob  # 22
    delta buffer token rebase ref rebase socket worker token  # 23
    window parser window socket header handle window window blob socket  # 24
    parser pack object parser branch parser socket  # 25
    queue tree worker window rebase buffer rebase  # 26
    commit buffer parser worker parser remote buffer stream buffer remote blob window  # 27
    stream branch merge remote parser rebase buffer socket buffer tree  # 28
    blob cache token token pack buffer ref index token  # 29
    header ref object delta  # 30
    remote rebase handle pack pack object blob cache  # 31
    branch blob buffer merge pack buffer object worker token  # 32
    merge queue delta header worker  # 33
    stream token delta remote commit parser blob object token stream handle ref  # 34
    branch merge remote handle stream commit stream tree handle header branch  # 35
    pack object stream pack window  # 36
    remote token buffer blob  # 37
    index buffer blob rebase rebase merge cache  # 38
    handle tree queue commit rebase merge object  # 39
    buffer branch header index delta rebase worker  # 40
    branch socket merge index window  # 41
    socket object commit socket cache index  # 42
    object stream socket token worker cache commit object parser  # 43
    blob queue token index  # 44
    ref cache token parser window worker commit buffer pack  # 45
    branch pack branch worker blob parser commit delta commit index  # 46
    tree branch rebase ref commit queue remote socket handle  # 47
    pack ref handle pack cache window rebase  # 48
    stream worker index token worker buffer ref worker stream index  # 49
    ref queue buffer socket handle stream object header socket object delta  # 50
    stream cache branch queue remote token remote merge rebase window window  # 51
    merge tree queue index window object pack cache rebase index commit index  # 52
    delta tree socket remote pack buffer worker header pack commit ref worker  # 53
    cache rebase pack remote rebase socket tree remote  # 54
    header remote rebase merge  # 55
    rebase cache merge parser remote worker delta merge delta  # 56
    token window commit socket socket stream header branch branch ref  # 57
    merge cache delta window ref merge remote  # 58
    tree header remote buffer tree object token token  # 59
    window tree rebase tree header object header worker  # 60
    worker merge commit stream merge index window buffer blob object  # 61
    pack stream branch delta  # 62
    object remote stream branch object pack queue pack window  # 63
