    branch merge window window index merge branch  # 0
    token tree buffer window branch merge commit  # 1
    rebase tree branch merge remote  # 2
    blob parser commit stream  # 3
    parser blob index socket blob tree parser rebase stream  # 4
    stream queue delta token cache index branch  # 5
    remote pack parser cache worker index index  # 6
    rebase header handle worker branch socket tree socket token queue  # 7
    tree stream buffer header handle token  # 8
    tree ref ref merge handle rebase rebase buffer cache window  # 9
    window index parser blob  # 10
    queue blob remote buffer worker merge socket  # 11
    tree worker remote token header rebase merge  # 12
    handle stream buffer blob socket commit branch stream socket  # 13
    token commit blob remote cache handle commit index delta token cache  # 14
    merge pack pack index stream handle  # 15
    handle branch ref parser pack blob  # 16
    object rebase window buffer  # 17
    queue parser parser rebase header remote pack  # 18
    token object branch worker rebase socket handle  # 19
    pack queue branch object blob remote index  # 20
    commit object worker delta blob remote remote branch branch socket buffer  # 21
    stream header window delta buffer parser merge remote socket cache  # 22
    buffer queue pack token index socket cache queue  # 23
    commit parser pack index rebase tree parser cache header  # 24
    cache rebase stream object  # 25
    pack merge index tree socket remote delta object stream blob window  # 26
    object delta window index window rebase  # 27
    window tree rebase queue buffer branch blob buffer window window worker delta  # 28
    cache merge socket blob  # 29
    tree buffer stream merge index  # 30
    branch token stream blob pack queue buffer  # 31
    window header delta header stream merge token worker blob queue  # 32
    parser merge header cache delta branch delta queue rebase remote  # 33
    handle parser index parser token socket ref rebase object branch  # 34
    object pack cache buffer socket tree branch window remote  # 35
    pack window delta buffer  # 36
    index object remote queue handle rebase socket  # 37
    branch remote commit queue delta rebase stream remote cache commit pack object  # 38
    branch pack stream handle token stream  # 39
    blob commit object window tree parser buffer window rebase parser queue  # 40
    commit remote blob pack merge rebase handle blob remote socket window  # 41
    merge ref remote header commit  # 42
    stream token rebase queue queue queue merge window parser pack  # 43
    pack window branch handle object buffer ref object window tree rebase remote  # 44
    queue merge window rebase socket buffer cache  # 45
    worker object delta stream remote tree branch worker cache handle  # 46
    remote ref window index stream cache commit  # 47
    cache delta token blob worker rebase  # 48
    branch object index window blob stream handle window ref pack  # 49
    worker cache stream cache  # 50
    remote worker parser parser rebase handle object rebase ref  # 51
    queue header cache blob index stream header worker  # 52
    cache window blob tree merge  # 53
    pack worker parser token header header merge  # 54
    token handle queue parser  # 55
    buffer stream token commit object blob object buffer  # 56
    pack tree pack pack cache parser  # 57
    header tree buffer delta parser token branch commit  # 58
    buffer commit merge cache index blob remote worker remote rebase rebase parser  # 59
    parser delta parser commit parser tree  # 60
    remote window rebase merge branch window window branch rebase header tree  # 61
    merge tree window window cache cache ref index window  # 62
    handle buffer cache delta object window ref object ref rebase tree socket  # 63
    window rebase index socket header queue delta window queue  # 64
    rebase object socket stream  # 65
    queue handle remote tree remote  # 66
    buffer stream parser merge token delta cache ref remote stream socket  # 67
    object commit merge index  # 68
    buffer stream handle blob tree cache blob handle  # 69
    worker stream ref header cache token blob parser queue  # 70
    socket branch window buffer queue buffer parser  # 71
    ref pack cache header window  # 72
    token branch cache buffer index worker remote handle  # 73
    index socket commit delta window merge  # 74
    object worker blob handle blob merge branch stream  # 75
    token stream remote blob delta blob tree parser buffer cache parser worker  # 76
    parser tree queue tree  # 77
    parser pack delta worker worker commit  # 78
    delta cache tree rebase  # 79
