    remote pack merge worker blob  # 0
    remote queue pack index remote header header object ref socket  # 1
    ref merge merge merge  # 2
    parser parser commit ref buffer handle window queue remote rebase branch rebase  # 3
    object rebase branch token window  # 4
    object merge cache parser object delta parser window rebase  # 5
    parser socket handle ref token  # 6
    socket tree remote socket stream window  # 7
    socket remote token parser cache pack object object remote merge header  # 8
    delta handle merge worker window blob header delta cache merge blob ref  # 9
    commit buffer parser worker rebase socket window window  # 10
    buffer branch merge stream index window merge merge index header cache  # 11
    socket buffer commit blob  # 12
    tree rebase token pack  # 13
    worker merge header index queue commit remote blob ref  # 14
    commit token handle pack index  # 15
    rebase queue token cache  # 16
    worker socket object delta object ref worker tree window  # 17
    buffer ref tree delta ref worker worker  # 18
    delta delta parser index handle pack socket window queue worker  # 19
    buffer handle ref buffer handle token parser remote branch commit cache  # 20
    blob ref cache ref cache merge object worker  # 21
    merge stream ref ref queue buffer blob object  # 22
    header handle handle parser index  # 23
    buffer ref window stream ref pack delta branch buffer stream  # 24
    remote index commit delta buffer buffer  # 25
    rebase window commit index cache blob window tree ref handle ref  # 26
    header socket tree queue  # 27
    delta tree merge stream header window pack  # 28
    socket remote remote socket stream index delta index ref  # 29
    cache remote parser parser  # 30
    commit blob token ref  # 31
    remote commit header merge pack delta branch  # 32
    blob header window parser index queue index branch  # 33
    ref remote buffer rebase blob cache  # 34
    queue stream window index handle commit  # 35
    remote index index merge  # 36
    blob token header ref tree object parser parser branch branch  # 37
    cache remote window cache ref branch cache cache queue commit pack pack  # 38
    index handle token rebase handle index ref branch rebase window  # 39
    stream tree window branch index worker worker handle index commit  # 40
    handle cache buffer queue worker pack socket pack cache  # 41
    token delta index rebase  # 42
    rebase handle handle merge token header commit remote  # 43
    cache parser object object buffer rebase ref  # 44
    buffer worker pack tree queue branch  # 45
    worker tree window rebase window handle blob  # 46
    blob object tree handle rebase commit token tree queue object parser index  # 47
    pack window merge pack window parser branch  # 48
    object tree commit commit buffer pack cache handle pack token buffer ref  # 49
    buffer commit stream parser tree blob socket header handle merge  # 50
    blob window commit header token  # 51
    merge pack delta buffer blob cache socket remote  # 52
    object delta object buffer cache socket stream parser header stream parser worker  # 53
    parser commit stream token  # 54
    commit window merge object branch index rebase  # 55
    handle delta blob index object stream worker socket token tree  # 56
    commit commit remote queue header  # 57
    rebase ref stream object socket handle pack ref remote cache cache  # 58
    socket remote cache ref queue  # 59
    tree pack branch merge  # 60
    branch header header remote rebase cache token token merge header object  # 61
    cache commit branch parser buffer blob remote window object remote remote object  # 62
    delta index ref window header cache queue socket worker blob  # 63
    parser parser index blob worker blob object branch pack parser delta  # 64
    tree index buffer pack  # 65
    index delta cache token parser cache buffer handle merge  # 66
    blob remote buffer header ref branch token  # 67
    merge object object index handle stream remote blob queue token delta pack  # 68
    worker remote tree merge  # 69
    pack parser commit stream ref token remote stream  # 70
    object rebase blob delta  # 71
    object buffer merge rebase token delta remote  # 72
    object index merge index blob pack object pack ref tree socket commit  # 73
    commit commit blob stream object merge tree blob pack ref index merge  # 74
    delta commit merge index handle ref header parser blob commit rebase  # 75
    tree index blob commit worker cache  # 76
    token delta token buffer stream  # 77
    queue delta stream cache commit worker token buffer  # 78
    pack stream queue cache header  # 79
    object token token remote branch tree  # 80
    tree pack pack rebase ref  # 81
    object delta socket ref index blob tree  # 82
    commit commit stream stream index queue cache blob stream token  # 0
    token window parser window branch  # 1
    worker remote cache handle header merge socket pack branch  # 2
    ref commit commit merge rebase header  # 3
    worker tree handle worker ref delta branch parser worker socket  # 4
    delta object stream rebase parser merge  # 5
    branch tree stream stream remote rebase window object delta queue merge  # 6
    remote header branch stream socket rebase cache pack ref blob  # 7
    socket index window cache cache remote header  # 8
    ref buffer token cache handle merge index token token socket rebase index  # 9
    pack header cache object index tree socket socket token  # 10
    merge pack buffer pack  # 11
