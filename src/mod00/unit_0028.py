    window remote window delta header  # 0
    window rebase tree buffer buffer pack pack buffer cache worker parser  # 1
    commit commit commit tree branch worker  # 2
    stream buffer commit blob branch blob tree parser  # 3
    socket handle blob pack pack ref rebase commit  # 4
    delta index pack handle socket  # 5
    parser delta branch pack blob pack token worker queue queue  # 6
    pack tree window handle  # 7
    blob index object ref worker merge token commit branch handle remote branch  # 8
    header tree queue remote cache  # 9
    delta parser ref window blob cache  # 10
    remote header index branch object token worker ref branch  # 11
    window remote ref worker buffer token socket header window queue blob worker  # 12
    remote remote window merge cache rebase  # 13
    merge stream parser handle commit remote  # 14
    window stream branch buffer branch parser merge buffer parser socket blob  # 15
    tree cache merge commit delta  # 16
    socket merge parser handle header ref header  # 17
    branch blob stream stream delta handle  # 18
    token blob token ref commit queue index  # 19
    handle remote parser object  # 20
    buffer rebase index tree merge buffer  # 21
    blob blob header remote rebase cache remote ref  # 22
    socket socket token commit rebase window  # 23
    branch object commit merge delta  # 24
    pack buffer ref worker index branch stream worker buffer worker pack  # 25
    commit blob delta ref object pack delta buffer object remote branch  # 26
    pack rebase buffer queue socket blob buffer rebase stream  # 27
    ref window tree buffer tree header merge commit pack merge object queue  # 28
    branch cache rebase delta worker socket remote  # 29
    token delta commit header  # 30
    pack buffer socket window queue ref handle cache rebase  # 31
    socket window header queue worker blob header delta tree ref header object  # 32
    object commit delta stream handle window branch delta  # 33
    worker branch merge blob token handle commit buffer rebase worker queue  # 34
    handle tree commit ref object cache header index object queue handle  # 35
    window socket pack stream delta delta header object handle queue object index  # 36
    branch delta rebase delta commit  # 37
    window window ref ref  # 38
    buffer ref merge token pack merge worker rebase remote buffer header queue  # 39
    queue token worker parser tree socket blob parser  # 40
    socket commit parser window token object worker header tree  # 41
    queue object index branch object  # 42
    handle rebase branch cache  # 43
