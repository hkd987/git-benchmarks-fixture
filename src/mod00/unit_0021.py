    merge commit index object window blob delta stream handle socket buffer buffer  # 0
    pack pack socket commit blob worker remote  # 1
    header commit queue remote merge branch index rebase  # 2
    worker token queue object pack parser  # 3
    blob parser buffer parser cache buffer ref token token  # 4
    tree branch blob handle cache window header ref index blob header token  # 5
    cache buffer blob parser window cache cache object stream  # 6
    queue index worker queue queue branch commit stream handle pack header parser  # 7
    parser header queue queue  # 8
    header handle handle token  # 9
    branch index branch queue stream token object merge  # 10
    token header object object  # 11
    handle ref tree delta tree header remote  # 12
    object parser remote queue token merge tree delta delta blob queue  # 13
    commit socket blob cache rebase cache parser delta  # 14
    window tree cache remote window socket commit rebase token remote  # 15
    ref object cache stream commit ref socket merge remote  # 16
    handle rebase delta commit socket commit merge  # 17
    worker header remote socket token worker delta  # 18
    commit blob index parser object tree blob handle stream merge cache socket  # 19
    queue window branch branch buffer object parser queue worker branch header  # 20
    commit queue merge delta remote stream buffer index merge merge cache  # 21
    worker merge ref commit  # 22
    header ref commit handle ref commit merge header rebase index  # 23
    token queue delta buffer rebase ref pack stream parser  # 24
    queue window window handle queue socket delta parser token window object index  # 25
    merge queue commit queue stream rebase window remote remote  # 26
    worker worker ref blob blob object delta  # 27
    pack handle worker handle tree token token parser rebase  # 28
    branch remote rebase stream  # 29
    tree cache token queue queue queue index queue  # 30
    queue handle delta delta worker worker worker tree tree parser  # 31
    object blob stream cache merge rebase index handle header queue  # 32
    ref handle stream merge buffer stream window cache ref  # 33
    index buffer remote queue object  # 34
    rebase socket socket token token socket tree token ref  # 35
    window object blob commit worker blob tree queue  # 36
    commit commit commit object cache buffer object delta delta cache rebase tree  # 37
    blob index worker window buffer handle handle merge ref token  # 38
    socket queue cache socket ref  # 39
    worker queue remote commit  # 40
    object branch index window token  # 41
    pack parser stream pack remote remote stream  # 42
    ref branch merge buffer index buffer  # 43
    branch commit rebase handle branch object  # 44
    ref pack pack buffer object object socket handle ref branch delta  # 45
    stream cache delta token header header  # 46
    pack window parser pack commit merge commit branch worker blob ref tree  # 47
    window commit handle handle remote merge stream  # 48
    delta pack branch handle  # 49
    token queue blob header  # 50
    parser stream window worker  # 51
    token queue window token ref handle branch  # 52
    parser buffer remote cache queue remote commit window  # 53
    blob tree tree socket index commit token branch token object  # 54
    cache branch rebase handle delta remote header queue stream pack commit remote  # 55
    branch index ref branch queue rebase stream object  # 56
    handle ref buffer merge  # 57
    socket stream header remote branch ref rebase object buffer branch object  # 58
    index parser blob blob  # 59
    worker blob pack queue  # 60
    delta rebase parser commit buffer handle object queue rebase  # 61
    header buffer parser ref handle branch remote window  # 62
    commit parser header header rebase index rebase branch branch stream  # 63
    commit remote handle merge  # 64
    blob token cache parser pack ref cache buffer  # 65
    pack commit token remote remote  # 66
    cache object commit queue window buffer object object index window socket token  # 67
    index delta window cache merge  # 68
    handle token parser object tree handle header socket tree delta  # 69
    stream commit index handle  # 70
    token stream buffer worker cache  # 71
    ref rebase remote socket window window blob pack worker ref  # 72
    merge index merge rebase  # 73
    delta token rebase merge  # 74
    branch parser commit tree  # 75
    remote tree commit index worker branch  # 76
    window buffer queue queue  # 77
    worker buffer worker window handle blob tree merge socket ref  # 78
    remote blob cache remote stream branch merge socket cache  # 79
    delta window delta rebase worker  # 80
    buffer pack handle remote blob ref tree buffer object remote rebase  # 81
    merge socket window handle delta header delta blob tree  # 0
    window tree stream commit tree blob ref stream tree blob cache tree  # 1
    blob socket queue worker pack stream ref handle  # 2
    index buffer blob token buffer blob cache  # 3
    window pack rebase merge buffer token worker queue socket commit merge window  # 0
    rebase cache parser parser  # 1
    branch buffer rebase worker header tree  # 2
    branch stream index buffer token  # 3
