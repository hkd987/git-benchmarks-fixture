    commit blob worker index socket branch socket  # 0
    tree rebase window rebase socket header  # 1
    window remote cache branch object buffer remote blob stream index pack merge  # 2
    header commit stream token tree header cache queue stream socket  # 3
    object rebase ref ref tree parser tree worker queue  # 4
    branch object parser remote buffer blob token worker blob cache  # 5
    header handle cache rebase socket  # 6
    branch cache merge buffer handle commit stream object cache commit token delta  # 7
    token index commit blob cache commit parser ref  # 8
    handle branch worker rebase  # 9
    ref tree cache buffer stream cache buffer merge pack  # 10
    socket branch rebase index index handle  # 11
    blob index token commit handle pack rebase blob parser remote  # 12
    buffer tree queue header queue socket pack pack index worker  # 13
    delta blob header rebase pack stream token  # 14
    remote ref rebase handle merge branch window  # 15
    commit object parser branch buffer socket branch index branch ref  # 16
    buffer socket merge ref handle  # 17
    cache object blob worker header tree object tree  # 18
    tree blob parser branch worker stream  # 19
    window window socket worker merge commit parser stream  # 20
    branch merge remote blob header buffer handle queue remote  # 21
    window blob commit socket index  # 22
    blob buffer socket merge merge branch index pack cache object pack  # 23
    socket pack object index window ref blob window  # 24
    tree stream tree branch cache buffer ref tree stream commit tree  # 25
    buffer tree index worker commit tree queue  # 26
    socket token commit delta header window object delta window commit delta  # 27
    header delta handle rebase buffer worker rebase stream rebase pack branch  # 28
    token pack queue cache pack pack tree index rebase cache  # 29
    token pack socket handle object merge ref token queue  # 30
    queue parser header header parser handle socket  # 31
    pack header merge delta blob object worker handle parser worker object  # 32
    blob blob socket token ref window token index header  # 33
    cache branch object buffer blob queue object index parser buffer  # 34
    header branch queue stream buffer blob queue  # 35
    merge tree merge header window  # 36
    remote commit delta parser window rebase commit cache branch window  # 37
    delta commit handle stream queue socket header object remote merge  # 38
    cache merge branch stream window cache rebase merge commit object  # 39
    remote worker remote socket commit buffer worker buffer  # 40
    window cache rebase parser handle stream parser blob branch  # 41
    window ref header header rebase delta stream blob commit parser window  # 42
    pack pack handle ref token token remote blob  # 43
    blob window buffer commit cache delta tree socket remote  # 44
    window buffer branch merge  # 45
    header pack ref header worker window branch window  # 46
    socket window pack cache  # 47
    branch header tree ref worker queue  # 48
    socket pack branch window branch stream buffer  # 49
    branch header handle worker stream ref index parser commit cache  # 50
    stream parser object token pack window  # 51
    pack rebase index ref buffer handle index index object stream cache buffer  # 52
    delta handle buffer token blob cache buffer ref tree token socket  # 53
    commit queue remote queue object commit pack socket  # 54
    queue ref ref merge merge cache queue delta delta queue  # 55
    object object rebase index branch buffer parser remote ref index queue tree  # 56
    worker commit commit worker cache object  # 57
    ref branch rebase queue commit tree branch commit remote token  # 58
    header tree cache ref cache socket tree delta socket stream stream  # 59
    header stream remote branch stream token header  # 60
    queue index commit object merge handle header object  # 61
    delta token token remote ref parser token parser ref parser header  # 62
    index queue window index commit ref buffer stream  # 63
    ref header rebase index ref window merge rebase window token  # 64
    worker ref buffer commit window parser branch  # 65
    object merge rebase object rebase ref blob window delta token token  # 66
    pack delta buffer remote worker socket  # 67
    pack header delta merge commit branch  # 68
    merge merge rebase parser buffer buffer stream object  # 69
    token delta index index  # 70
    branch commit cache cache queue buffer header pack pack pack cache  # 71
