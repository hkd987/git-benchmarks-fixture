    remote blob handle blob delta tree  # 0
    handle header stream socket buffer merge header parser branch  # 1
    remote pack commit token ref socket  # 2
    commit object header queue ref remote  # 3
    branch tree ref merge merge index cache  # 4
    blob delta cache stream index index merge delta  # 5
    pack pack buffer commit rebase blob  # 6
    header socket buffer header tree remote branch pack tree  # 7
    cache header commit delta header parser  # 8
    stream object worker delta commit socket token socket merge  # 9
    branch delta index parser  # 10
    object rebase ref worker worker handle branch delta tree merge remote stream  # 11
    tree index rebase branch rebase rebase worker rebase buffer ref token  # 12
    ref delta merge delta stream  # 13
    socket socket stream window socket buffer commit window ref branch  # 14
    window window socket stream token merge parser window header worker  # 15
    pack pack header merge tree delta handle commit parser cache branch  # 16
    worker remote object window token tree header delta  # 17
    object handle buffer cache  # 18
    socket stream tree stream delta stream  # 19
    delta worker remote index  # 20
    cache pack delta commit  # 21
    stream commit pack branch token pack blob remote remote token branch  # 22
    token token socket stream delta delta object window stream tree stream  # 23
    blob remote parser branch tree worker delta index cache  # 24
    object branch stream tree commit  # 25
    commit pack token buffer window object buffer stream commit socket handle  # 26
    index header handle parser window buffer stream buffer blob rebase tree  # 27
    delta delta branch worker handle buffer worker window delta pack buffer stream  # 28
    blob rebase ref cache object stream  # 29
    token tree queue rebase index queue commit buffer tree rebase stream socket  # 30
    rebase remote queue stream blob object stream  # 31
    remote worker cache rebase  # 32
    rebase token buffer blob header remote remote object tree delta delta  # 33
    token commit branch socket merge cache header remote merge socket  # 34
    queue index handle buffer stream cache pack header ref remote parser buffer  # 35
    pack cache stream buffer object  # 36
    queue rebase blob handle object blob index merge token delta socket window  # 37
    worker handle window token window buffer  # 38
    commit worker pack window worker remote blob header socket handle token  # 39
    branch buffer token socket  # 40
    queue header window parser  # 41
    queue commit blob ref ref branch delta stream  # 42
    header handle cache rebase header delta  # 43
    window ref window parser header object cache worker header stream socket branch  # 44
    index blob commit queue branch  # 45
    header rebase parser parser buffer rebase branch delta header window  # 46
    stream blob token window buffer commit handle  # 47
    stream queue branch socket blob header pack commit delta worker  # 48
    stream handle window object buffer socket handle header object ref index  # 49
    parser ref tree socket  # 50
    merge blob cache object index merge parser stream merge  # 51
    remote index window object handle branch window worker object handle  # 52
    buffer queue token pack merge stream  # 53
    queue delta token merge index worker parser merge branch index tree delta  # 54
    merge queue handle parser worker blob ref blob blob buffer  # 55
    index cache remote remote remote queue ref window tree header  # 56
    ref token worker ref stream object pack cache  # 57
    socket parser worker queue object commit ref worker index pack socket token  # 58
    handle delta delta merge remote handle queue window ref remote remote  # 59
    worker remote header tree commit buffer tree header index header pack pack  # 60
    index worker commit merge delta rebase ref header merge rebase token  # 61
