    branch pack ref pack  # 0
    commit cache socket cache token  # 1
    worker worker rebase merge queue buffer buffer header header merge object stream  # 2
    token branch parser buffer pack  # 3
    handle handle ref index blob rebase window socket index  # 4
    pack blob parser merge stream  # 5
    index delta socket token queue delta tree  # 6
    remote parser worker window parser index blob  # 7
    parser parser handle blob handle worker delta index cache  # 8
    queue index buffer blob merge worker  # 9
    parser pack merge stream  # 10
    object remote blob blob cache rebase window blob  # 11
    merge ref handle stream  # 12
    merge commit commit index remote ref delta  # 13
    merge tree queue worker socket branch window socket object  # 14
    buffer stream branch ref window cache  # 15
    branch remote commit ref delta  # 16
    pack delta blob window worker  # 17
    delta stream cache cache index tree header parser socket  # 18
    header tree handle buffer window index commit  # 19
    stream socket merge header index pack ref ref ref object index blob  # 20
    parser stream header rebase stream  # 21
    commit parser blob socket socket pack handle header blob commit header object  # 22
    handle socket socket socket token buffer branch  # 23
    delta rebase branch handle ref rebase remote index object  # 24
    branch object pack delta branch handle remote object worker handle  # 25
    handle index merge cache tree  # 26
    handle pack merge branch remote worker rebase stream window token  # 27
    merge buffer token window tree merge  # 28
    object blob commit buffer queue object remote commit blob commit remote  # 29
    cache token buffer remote merge socket delta  # 30
    rebase tree pack delta worker header object commit delta object header  # 31
    cache queue branch queue  # 32
    parser token token stream object  # 33
    remote window ref rebase object pack  # 34
    ref tree blob queue remote delta  # 35
    index handle parser handle stream  # 36
    tree handle parser delta stream buffer socket index delta merge  # 37
    object remote header index blob header socket handle window parser parser merge  # 38
    queue remote pack ref commit cache  # 39
    blob delta cache object cache buffer socket parser  # 40
    rebase branch header object merge handle index tree delta window rebase ref  # 41
    stream worker buffer stream  # 42
    branch header header window branch tree index tree rebase ref  # 43
    worker handle pack index handle buffer blob  # 44
    buffer tree token commit  # 0
    object queue token handle rebase index remote buffer blob object buffer  # 1
    queue ref worker socket branch commit stream  # 2
    buffer buffer index parser cache commit commit object worker merge socket  # 3
    object merge parser token window header queue handle stream  # 4
    buffer socket object header ref  # 5
    pack object buffer stream socket  # 6
    merge window object rebase  # 7
    merge commit pack queue blob tree  # 0
    pack queue handle window token commit  # 1
    object worker header window object branch worker ref tree window  # 2
    rebase pack token pack  # 3
    worker tree ref pack remote  # 4
    rebase header handle token parser pack delta rebase merge branch worker worker  # 5
    socket remote tree cache  # 6
