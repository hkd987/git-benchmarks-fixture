    token parser object object ref object token delta  # 0
    token remote parser cache handle index merge pack handle  # 1
    cache pack delta remote  # 2
    header header pack ref pack blob  # 3
    token delta ref parser blob queue rebase  # 4
    socket index token index parser rebase ref header branch delta header  # 5
    buffer remote ref blob pack commit stream socket object  # 6
    window tree delta blob  # 7
    parser handle worker pack branch window  # 8
    commit parser worker commit remote token  # 9
    pack handle rebase blob  # 10
    token merge delta handle index worker parser commit commit  # 11
    pack buffer branch ref token header socket cache commit index header remote  # 12
    object tree header blob stream  # 13
    object tree worker remote socket blob token remote merge  # 14
    worker socket tree header buffer blob queue queue  # 15
    socket buffer rebase rebase worker ref socket parser token rebase branch handle  # 16
    blob object ref handle queue handle index merge branch  # 17
    token tree ref socket buffer buffer socket  # 18
    pack header object commit cache blob header buffer branch  # 19
    index stream rebase pack merge  # 20
    window queue index object rebase  # 21
    tree worker handle token  # 22
    worker object blob buffer pack cache stream  # 23
    pack delta delta rebase worker  # 24
    merge stream blob cache pack branch token rebase socket  # 25
    socket object merge socket merge  # 26
    merge index header header window index object  # 27
    pack socket tree stream  # 28
    ref merge merge worker handle rebase stream buffer delta cache branch  # 29
    commit buffer cache cache stream  # 30
    worker remote branch socket blob branch branch branch branch socket rebase cache  # 31
    header blob token tree remote merge  # 32
    token branch merge tree cache parser header rebase stream tree  # 33
    buffer tree pack queue window tree rebase merge window branch socket  # 34
    worker queue socket buffer remote object token header  # 35
    window window merge index  # 36
    blob rebase socket merge header rebase index token object cache  # 37
    pack remote queue branch rebase rebase stream commit handle branch tree buffer  # 38
    merge blob worker remote branch branch merge buffer index cache remote  # 39
    commit tree queue merge queue handle merge  # 40
    handle rebase socket queue commit index merge index  # 41
    blob tree rebase queue worker remote cache buffer branch blob parser  # 42
    ref stream remote merge rebase index tree parser  # 43
    merge blob stream remote delta remote socket  # 44
    ref object object blob window ref branch blob  # 45
    rebase buffer delta blob tree pack object cache token  # 46
    object tree index tree tree tree cache delta  # 47
    remote branch delta stream parser pack merge tree index  # 48
    delta remote ref commit handle window buffer  # 49
    pack rebase remote handle parser index remote rebase worker index token ref  # 50
    ref token header parser branch pack token  # 51
    merge header commit pack parser remote  # 52
    worker ref header token rebase object blob merge worker merge  # 53
    buffer index pack blob handle index index remote  # 54
    merge handle queue parser object parser parser token worker merge  # 55
    window ref merge parser tree remote branch blob commit queue header socket  # 56
    object parser remote socket index pack ref delta tree rebase header parser  # 57
    buffer ref ref buffer buffer blob header remote stream merge merge worker  # 58
    commit worker pack commit branch handle rebase socket stream pack pack header  # 59
    header tree pack delta cache delta token worker object ref header pack  # 60
    window merge blob parser merge object  # 61
    index object remote commit parser ref pack  # 62
    header branch header merge parser merge delta buffer blob socket tree  # 63
    pack worker handle token  # 64
    buffer token queue stream merge branch delta merge tree queue stream  # 65
    stream pack merge delta window parser  # 66
    window delta handle tree rebase stream remote  # 67
    buffer commit parser object  # 68
    remote remote socket ref window rebase window tree  # 69
    stream stream index blob delta  # 70
    handle cache index pack  # 71
    commit ref tree blob token tree ref worker cache delta  # 72
    handle index ref remote branch  # 73
    worker rebase object branch ref stream commit parser  # 0
    handle merge ref window buffer object buffer window  # 1
    branch ref queue socket pack rebase rebase socket remote rebase branch  # 2
    blob pack remote buffer header parser delta branch blob index index  # 3
