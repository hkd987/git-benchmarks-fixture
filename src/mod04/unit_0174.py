    tree delta index merge commit tree commit rebase blob blob  # 0
    index ref index stream handle cache token object tree  # 1
    delta merge rebase parser object blob token handle tree index stream  # 2
    rebase index tree index pack worker branch socket index rebase  # 3
    remote merge remote object buffer token tree handle queue index  # 4
    index remote merge index commit remote commit  # 5
    window merge buffer parser  # 6
    window commit remote queue queue delta index token remote index  # 7
    ref cache blob window blob token tree buffer  # 8
    window tree parser remote pack commit pack tree socket token  # 9
    index worker ref queue window buffer  # 10
    queue worker object delta merge worker remote handle  # 11
    blob tree branch ref delta branch  # 12
    commit blob buffer rebase socket commit index socket socket handle tree object  # 13
    tree worker delta pack merge tree ref  # 14
    parser remote rebase parser socket  # 15
    buffer remote queue merge remote delta cache blob buffer worker object  # 16
    commit buffer parser rebase  # 17
    tree socket buffer buffer handle blob pack merge window commit queue token  # 18
    rebase rebase rebase pack remote queue socket  # 19
    worker commit object socket worker header object delta  # 20
    commit index socket pack object socket window parser delta  # 21
    merge object window queue stream pack token index  # 22
    index handle merge window stream object branch remote merge  # 23
    queue commit cache merge header header object handle  # 24
    pack ref cache token ref pack  # 25
    ref worker worker socket  # 26
    worker remote branch stream ref pack  # 27
    branch merge socket branch commit index index worker delta parser  # 28
    merge window buffer cache header cache  # 29
    remote delta pack index merge socket ref blob handle socket  # 30
    queue stream merge worker  # 31
    queue worker socket commit token token tree rebase socket  # 32
    buffer merge worker buffer parser header parser header parser socket object worker  # 33
    header index delta buffer tree index pack  # 34
    ref commit token stream token buffer index pack  # 35
    token cache buffer index branch index token token token parser socket  # 36
    ref pack blob pack handle  # 37
    branch token stream handle merge commit  # 38
    delta worker window index queue token buffer merge  # 39
    socket index branch merge parser  # 40
    merge handle parser window handle stream token header cache  # 41
    worker buffer handle ref index object pack  # 42
    header queue delta blob  # 43
    merge commit buffer ref pack  # 44
    queue commit worker worker cache  # 45
    object header tree header object remote merge buffer socket buffer queue token  # 46
    ref stream object rebase  # 47
    object remote cache remote handle cache cache blob socket parser  # 48
    buffer queue stream worker parser branch branch  # 49
    header branch pack header queue rebase stream rebase  # 50
    cache blob object header commit queue tree  # 51
    pack delta queue blob ref pack  # 52
    remote window token socket remote merge token window commit branch  # 53
    parser index rebase rebase index commit commit ref index remote header buffer  # 54
    tree window delta buffer worker header parser  # 55
    index stream pack object branch branch  # 56
    pack blob ref parser parser cache window object blob index  # 57
    commit buffer delta window window queue stream header worker  # 58
    pack worker header socket token  # 59
    queue blob remote pack token  # 60
    parser stream object blob tree  # 61
    delta socket socket commit blob commit socket merge window commit handle merge  # 62
    token index worker merge queue delta ref queue pack buffer  # 63
    blob stream header blob handle handle blob merge  # 64
    delta pack remote handle rebase cache header tree blob header  # 65
    handle handle stream ref buffer blob branch  # 66
    socket parser index handle delta commit object  # 67
    window rebase commit token tree delta socket buffer tree parser  # 68
    commit remote queue delta queue queue commit buffer cache window parser  # 69
    blob rebase stream window header buffer delta queue ref socket  # 70
    stream object merge object stream blob worker object index parser  # 0
    delta rebase tree tree  # 1
    buffer parser branch tree handle header delta queue  # 2
    window pack index parser  # 3
    pack parser rebase queue rebase rebase socket merge pack branch merge cache  # 4
    header object merge token  # 5
    pack delta parser window index queue  # 6
    pack cache header delta window handle token  # 7
    ref buffer header ref buffer commit buffer commit tree handle ref branch  # 8
    parser branch rebase parser object worker commit parser branch ref commit  # 9
    merge handle blob tree branch handle  # 10
