    token branch index index window index socket  # 0
    index worker handle rebase pack commit worker tree tree remote ref  # 1
    commit cache socket index socket stream tree cache tree rebase token worker  # 2
    delta tree window socket  # 3
    parser commit cache queue worker branch delta rebase queue merge remote object  # 4
    rebase queue pack rebase commit buffer  # 5
    object pack commit branch branch merge header  # 6
    commit rebase object delta header window parser  # 7
    blob index merge ref branch window merge delta ref index socket  # 8
    commit header blob queue  # 9
    worker blob worker blob commit pack socket branch remote buffer buffer commit  # 10
    ref rebase cache blob window socket cache object delta object worker  # 11
    object commit header cache queue blob handle rebase socket token tree  # 12
    tree window remote queue handle handle window token  # 13
    ref socket socket merge blob tree pack remote branch queue remote token  # 14
    parser worker remote stream  # 15
    commit cache object delta index window socket pack  # 16
    blob handle merge branch branch branch pack window parser  # 17
    queue buffer rebase worker socket object remote parser  # 18
    commit delta cache merge blob buffer window socket merge  # 19
    tree object cache parser tree window worker index object merge  # 20
    cache branch ref worker cache  # 21
    worker merge socket rebase remote socket  # 22
    stream commit token pack worker index parser commit ref buffer  # 23
    parser index object index ref token pack ref stream  # 24
    handle ref socket token merge window tree object delta  # 25
    parser handle blob ref delta merge delta remote pack delta  # 26
    parser cache window parser  # 27
    remote merge rebase ref tree commit buffer rebase object buffer  # 28
    object tree worker cache parser  # 29
    rebase delta parser worker delta object parser merge commit  # 30
    socket commit rebase worker blob tree commit cache parser commit pack branch  # 31
    buffer tree worker window socket delta cache blob token worker handle  # 32
    queue socket delta merge delta stream object tree pack  # 33
    delta header parser rebase cache branch  # 34
    queue rebase handle worker index merge merge  # 35
    ref stream queue header window socket socket  # 36
    remote handle object rebase  # 37
    index tree worker index cache commit cache worker worker  # 38
    rebase commit window object window rebase cache handle commit worker token remote  # 39
    worker handle tree commit branch commit header stream blob token worker queue  # 40
    delta header branch remote tree remote blob commit token commit  # 41
    header queue queue remote header header delta commit buffer stream  # 42
    ref parser delta blob parser object  # 43
    cache stream ref index tree queue buffer stream branch  # 44
    index object branch object header tree queue remote rebase branch  # 45
    parser parser parser header  # 46
    socket queue commit pack object merge branch queue pack  # 47
    ref header tree remote object blob object stream socket buffer ref remote  # 48
    merge token buffer window rebase index token cache  # 49
    cache index queue object handle cache pack tree window token  # 50
    object blob commit buffer cache merge token object rebase token object blob  # 51
    socket cache socket blob window worker window  # 52
    rebase queue commit worker merge pack handle commit  # 53
    rebase rebase tree remote token socket token blob header  # 54
    blob buffer delta buffer merge merge delta worker  # 55
    commit commit window buffer token window cache branch object merge  # 56
    rebase queue branch stream index  # 57
    handle queue window window queue worker token  # 58
    blob token buffer pack buffer blob  # 59
    rebase handle rebase tree header commit ref delta  # 60
    header ref rebase blob window pack  # 61
    pack blob ref tree  # 62
    branch tree handle index  # 63
    commit token cache queue queue tree commit  # 64
    tree queue worker object handle  # 65
    queue queue merge handle parser  # 66
    branch socket handle queue  # 67
    tree stream parser branch index remote remote cache tree  # 68
    handle buffer ref commit header buffer object parser  # 69
    delta index window delta window blob  # 70
    handle merge window object ref ref parser window buffer commit handle rebase  # 71
    commit delta ref pack commit socket stream pack remote socket  # 72
    cache object merge pack  # 73
