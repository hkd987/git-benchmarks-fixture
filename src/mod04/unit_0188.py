    stream index tree index cache branch worker cache delta  # 0
    pack rebase blob rebase pack ref ref index commit delta  # 1
    buffer commit buffer commit window handle tree pack buffer parser  # 2
    handle header ref pack window  # 3
    socket tree handle parser tree worker blob token commit commit header  # 4
    stream remote parser delta remote tree commit parser header pack object  # 5
    window socket socket branch queue cache rebase worker socket token ref  # 6
    worker handle parser rebase handle object tree stream  # 7
    rebase token worker handle  # 8
    remote window token parser  # 9
    handle rebase window token blob worker window delta blob tree  # 10
    ref worker branch ref socket buffer buffer rebase queue worker  # 11
    pack worker queue header pack parser header  # 12
    merge commit window index rebase  # 13
    header buffer index tree  # 14
    socket object socket socket branch  # 15
    window token branch worker remote header socket branch handle token window cache  # 16
    remote blob index tree buffer parser handle worker commit stream  # 17
    stream tree worker index cache blob parser  # 18
    commit header blob commit  # 19
    socket blob index index token tree rebase parser remote  # 20
    handle buffer cache token queue  # 21
    queue token index queue queue  # 22
    buffer merge socket handle object header cache worker  # 23
    queue branch tree ref socket branch buffer merge tree  # 24
    buffer commit handle branch commit header blob tree stream delta  # 25
    header window delta merge tree blob pack worker delta  # 26
    cache commit window parser pack branch buffer stream object  # 27
    queue index token delta branch object queue commit window index branch pack  # 28
    index worker delta queue header parser index stream token  # 29
    commit rebase pack buffer cache  # 30
    branch branch object commit tree stream remote parser delta branch  # 31
    parser tree buffer header cache worker merge worker  # 32
    ref handle branch handle window  # 33
    worker object branch delta delta rebase cache window remote object  # 34
    remote remote worker stream buffer window index delta worker commit  # 35
    header header tree stream parser rebase header  # 36
    blob merge ref token object header queue token  # 37
    object tree merge queue rebase rebase  # 38
    merge ref parser tree  # 39
    pack commit pack object  # 40
    remote commit parser commit  # 41
    delta ref tree remote queue blob commit tree  # 42
    branch ref window header index worker branch socket stream header ref  # 43
    commit remote header tree blob commit parser stream  # 44
    object pack remote socket socket handle token rebase blob window delta stream  # 45
    handle socket window handle header blob  # 46
    delta ref rebase buffer stream cache  # 47
    queue blob socket commit stream pack object branch  # 48
    delta parser index token worker  # 49
    index object commit token index delta branch rebase branch index  # 50
    token window object worker buffer handle window remote buffer object  # 51
    index delta blob handle  # 52
    window window token worker delta queue worker buffer tree token merge  # 53
    token remote handle cache window stream parser  # 54
    object token remote index rebase tree merge ref handle window object delta  # 55
    delta socket window worker delta merge  # 56
    object object parser blob branch index socket  # 57
    commit socket worker rebase blob  # 58
    delta remote buffer branch token  # 59
    tree token buffer worker rebase ref socket handle stream  # 60
    queue queue handle token parser token buffer socket  # 61
    tree worker pack remote handle parser ref remote worker delta token object  # 62
    parser worker window cache  # 63
    object window cache blob remote pack remote worker token blob remote buffer  # 64
    ref delta branch parser socket merge remote  # 65
    remote delta rebase socket buffer remote parser  # 66
    branch handle index remote rebase socket  # 67
    index stream tree window socket ref  # 68
    cache buffer worker index handle  # 69
    blob rebase blob pack cache  # 70
    commit queue remote token  # 71
    buffer queue blob queue blob  # 72
    pack buffer blob stream branch  # 73
