    delta worker buffer tree rebase index handle  # 0
    buffer remote parser header  # 1
    object cache handle parser buffer handle pack buffer ref  # 2
    worker cache blob cache commit token header merge worker socket  # 3
    parser rebase rebase ref delta token index  # 4
    cache stream merge commit delta object  # 5
    blob buffer tree token token blob remote socket merge  # 6
    pack merge cache token worker  # 7
    ref token ref queue remote commit commit branch  # 8
    index remote object index socket blob handle index index index  # 9
    pack token stream pack handle worker merge delta ref  # 10
    worker stream merge merge worker object token branch parser queue worker  # 11
    worker tree rebase stream  # 12
    socket cache remote blob worker buffer pack parser commit blob  # 13
    index socket queue handle tree pack  # 14
    ref tree index buffer rebase  # 15
    index worker worker buffer handle rebase branch queue  # 16
    branch tree blob merge  # 17
    stream token merge blob buffer  # 18
    tree window cache buffer index parser  # 19
    commit parser worker handle merge merge cache header blob index index  # 20
    header remote delta tree token branch cache  # 21
    token blob merge index stream handle branch stream socket worker stream header  # 22
    pack blob remote pack tree handle blob blob  # 23
    commit tree handle rebase  # 24
    buffer handle tree buffer index window worker object stream socket tree  # 25
    pack merge merge parser commit index object socket window handle  # 26
    remote handle tree handle queue tree buffer ref  # 27
    stream delta remote index stream index parser  # 28
    handle token pack index remote branch object parser remote window delta  # 29
    handle stream object window pack remote buffer cache index queue parser buffer  # 30
    blob parser commit window queue handle  # 31
    parser queue branch worker  # 32
    token handle merge pack object cache queue window header window  # 33
    cache stream object commit branch rebase tree stream tree handle object parser  # 34
    parser remote index blob pack ref blob pack header  # 35
    delta pack parser merge token  # 36
    rebase object tree merge blob branch token handle cache delta object rebase  # 37
    header remote delta pack ref cache token worker parser object rebase cache  # 38
    rebase worker cache branch  # 39
    cache queue branch token  # 40
    parser merge handle merge remote queue index branch token branch rebase ref  # 41
    commit object delta stream buffer blob queue merge token branch window branch  # 42
    tree buffer queue buffer branch pack socket commit parser delta worker  # 43
    header tree commit commit ref header  # 44
    remote socket tree buffer  # 45
    tree remote parser stream delta rebase object blob  # 46
    pack tree token parser merge parser blob handle worker  # 47
    cache blob token delta index merge  # 48
    pack header pack worker parser pack tree parser token stream stream  # 49
    branch stream cache ref parser stream buffer worker  # 50
    cache worker branch window delta  # 51
    rebase delta handle header commit stream remote worker parser  # 52
    parser index branch token branch socket ref cache  # 53
    rebase parser rebase header remote  # 54
    stream remote object branch index header index token tree index index  # 55
    rebase stream window commit remote pack stream buffer parser object merge commit  # 56
    rebase worker worker cache branch parser queue worker handle handle commit branch  # 57
    merge queue token parser parser merge  # 58
    branch handle queue commit socket cache stream header socket  # 59
    buffer object remote rebase header socket buffer cache remote branch object  # 60
    blob ref blob queue  # 61
    branch blob cache queue rebase blob handle buffer delta blob parser object  # 62
    tree header stream handle window blob header ref ref commit commit token  # 63
