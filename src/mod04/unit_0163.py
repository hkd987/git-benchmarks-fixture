    pack object handle blob branch handle tree cache merge object parser  # 0
    remote token rebase tree commit window tree cache cache stream  # 1
    blob header ref queue stream stream rebase header commit  # 2
    tree stream cache blob index queue branch blob stream header  # 3
    buffer header object handle branch window window remote header ref tree stream  # 4
    blob delta cache remote cache buffer blob rebase tree  # 5
    worker remote tree merge pack handle ref  # 6
    header buffer parser delta ref socket  # 7
    tree buffer buffer parser parser queue parser token commit merge  # 8
    rebase window header remote stream queue  # 9
    tree socket index tree  # 10
    queue rebase commit window  # 11
    rebase merge stream rebase rebase window socket window ref blob  # 12
    socket buffer index header remote queue socket commit remote remote index buffer  # 13
    socket buffer header queue parser commit index tree window buffer delta window  # 14
    delta ref socket handle object window stream pack commit cache worker ref  # 15
    tree blob queue queue socket token worker merge  # 16
    tree object merge parser token queue header tree  # 17
    worker index header queue tree rebase header token window queue blob header  # 18
    ref queue rebase pack delta handle  # 19
    worker rebase branch cache index socket queue commit tree token merge handle  # 20
    pack handle pack remote blob commit socket stream  # 21
    ref header rebase buffer rebase queue worker tree index stream buffer parser  # 22
    parser stream parser branch socket blob merge  # 23
    queue parser commit tree pack handle window commit  # 24
    cache handle object handle stream worker commit remote tree  # 25
    object index delta handle token commit stream rebase token commit  # 26
    queue blob header handle cache socket ref stream commit  # 27
    blob commit object pack delta queue token blob token cache socket buffer  # 28
    socket header stream delta parser branch buffer header remote object branch  # 29
    token parser tree pack ref token  # 30
    ref token handle socket pack pack buffer handle window  # 31
    object queue tree object  # 32
    tree blob commit ref queue rebase parser merge  # 33
    token object ref token rebase queue window merge branch merge window  # 34
    object header buffer commit socket tree remote  # 35
    cache ref merge window rebase window object cache  # 36
    socket token header object ref index merge object  # 37
    remote worker branch merge handle rebase remote header merge queue window  # 38
    queue socket object branch stream header ref  # 39
    buffer worker object object index header branch blob index branch worker  # 40
    handle queue delta handle worker buffer queue  # 41
    window stream remote remote delta handle  # 42
    blob stream parser tree commit rebase  # 43
    tree branch ref blob stream parser  # 44
    branch object header pack commit cache queue  # 45
    commit socket branch blob cache queue branch queue  # 46
    window index index socket  # 47
    parser socket token window delta worker remote worker worker pack merge pack  # 48
    cache socket remote window remote commit window merge socket header ref  # 49
    stream header remote blob ref index commit index merge tree branch  # 50
    object handle object commit buffer branch parser object rebase object  # 51
    branch pack window queue  # 52
    remote parser cache buffer object pack delta branch token parser handle  # 53
    token merge remote pack  # 54
    blob handle rebase cache remote parser object  # 55
    cache handle rebase merge  # 56
    handle blob header buffer blob cache token  # 0
    index merge delta branch header buffer remote parser ref commit worker rebase  # 1
    socket rebase token delta delta remote window index ref pack queue  # 2
    window branch remote token stream worker delta socket worker  # 3
    socket stream rebase remote ref worker pack  # 4
