    cache remote ref rebase delta parser header tree stream branch object  # 0
    merge stream parser pack blob remote merge stream token remote branch  # 1
    remote ref parser header tree commit parser remote object index  # 2
    blob ref object blob rebase merge tree branch delta blob commit  # 3
    rebase ref parser tree handle index tree buffer stream remote tree  # 4
    blob handle remote merge handle pack merge buffer token  # 5
    socket pack branch header delta  # 6
    worker socket queue tree blob cache stream cache buffer ref delta buffer  # 7
    delta parser buffer worker rebase window object header commit cache cache  # 8
    token merge handle commit index rebase header window delta  # 9
    handle rebase index token parser merge  # 10
    merge handle commit rebase cache remote merge blob  # 11
    commit buffer commit commit window ref commit object index index  # 12
    buffer rebase remote pack token commit  # 13
    tree buffer index index token remote parser merge  # 14
    socket queue buffer remote header  # 15
    pack stream branch pack socket blob  # 16
    header branch window buffer handle branch token ref window delta  # 17
    ref stream remote worker ref window socket tree index buffer object cache  # 18
    delta socket delta worker socket pack  # 19
    cache token parser stream stream  # 20
    pack commit header merge parser remote  # 21
    handle tree blob window buffer rebase  # 22
    object rebase handle cache index token handle merge handle  # 23
    parser delta remote ref index pack stream  # 24
    merge ref buffer handle tree index token  # 25
    cache worker merge parser header queue remote  # 26
    socket stream handle window socket queue object window index remote remote merge  # 27
    object blob branch window parser remote blob token worker parser socket  # 28
    queue rebase object stream pack rebase buffer header commit pack token  # 29
    token rebase window index window tree branch commit header buffer stream  # 30
    stream window window socket  # 31
    ref cache cache remote buffer tree delta ref  # 32
    tree window socket merge stream queue  # 33
    pack buffer parser commit merge token pack commit index delta delta  # 34
    parser commit pack pack window ref handle  # 35
    branch ref parser stream stream ref merge worker parser  # 36
    header stream handle commit ref worker queue merge token  # 37
    rebase merge buffer rebase window queue token window tree tree delta buffer  # 38
    delta queue rebase worker merge pack  # 39
    tree remote index socket worker blob branch ref  # 40
    delta worker commit tree blob window  # 41
    object ref branch queue socket pack token commit pack rebase socket socket  # 42
    ref socket stream commit blob parser commit merge  # 43
    ref header parser merge commit  # 44
    worker parser worker object  # 45
    tree ref stream socket branch rebase merge pack branch worker stream  # 0
    token delta merge worker worker header branch  # 1
    token merge buffer queue remote cache ref blob handle  # 2
    delta queue commit buffer commit rebase commit token handle ref tree  # 3
    token rebase pack index queue token cache queue  # 4
    parser handle ref handle handle queue socket  # 5
    stream remote cache worker  # 6
    tree tree delta window branch rebase cache  # 0
    tree handle stream window ref  # 1
    queue buffer stream queue token remote  # 2
    stream rebase merge rebase buffer stream object rebase socket  # 3
    delta window commit window commit pack handle header socket  # 4
    index remote object buffer cache tree header  # 5
    delta merge token pack delta object ref handle window socket cache tree  # 6
