    ref ref cache token rebase commit blob socket stream socket  # 0
    ref commit handle commit blob commit merge branch  # 1
    remote pack index buffer socket rebase commit merge queue  # 2
    remote delta rebase header merge rebase delta parser  # 3
    pack delta cache object queue handle merge buffer branch tree  # 4
    rebase handle window merge branch pack index handle parser  # 5
    handle header ref window object window branch parser header rebase cache  # 6
    stream stream stream commit token index blob rebase pack delta buffer branch  # 7
    handle merge cache handle merge blob pack  # 8
    handle object worker queue buffer handle token pack merge  # 9
    ref ref handle blob branch  # 10
    remote buffer header blob index remote worker buffer token merge  # 11
    branch queue stream remote token delta  # 12
    buffer parser merge rebase socket tree delta queue  # 13
    parser window socket worker socket  # 14
    token rebase handle header object worker buffer stream  # 15
    handle socket rebase blob pack blob parser window merge pack  # 16
    delta ref ref token commit index branch header remote  # 17
    handle handle socket worker window token cache delta blob token header merge  # 18
    merge handle header header header window blob ref header parser branch stream  # 19
    tree stream remote remote window  # 20
    remote buffer blob index merge  # 21
    commit queue tree stream  # 22
    stream stream handle index worker  # 23
    token parser header branch blob header  # 24
    header ref pack worker window socket window blob queue merge  # 25
    queue commit socket rebase parser cache commit buffer tree  # 26
    tree delta pack pack window rebase pack stream  # 27
    commit rebase queue window tree index  # 28
    index remote ref tree socket branch stream rebase queue blob tree object  # 29
    handle header ref ref handle remote cache token socket queue queue  # 30
    worker commit buffer pack header stream ref commit buffer branch  # 31
    pack socket header merge pack header pack delta rebase queue  # 32
    handle index delta delta rebase delta handle worker  # 33
    worker worker rebase queue merge pack token index token socket cache pack  # 34
    header queue blob cache rebase object rebase commit remote pack queue  # 35
    token rebase object queue pack cache buffer cache token pack token queue  # 36
    parser merge branch queue rebase pack tree object cache tree  # 37
    socket tree cache commit blob index handle handle index  # 38
    window delta branch remote worker tree object object merge pack header branch  # 39
    token header handle commit  # 40
    window object socket remote stream merge merge  # 41
    worker pack window header remote window remote pack cache stream  # 42
    blob tree merge cache pack index commit parser buffer stream commit  # 43
    handle index commit index ref merge rebase object object tree pack blob  # 44
    header blob queue delta buffer token merge pack branch  # 45
    branch header stream object ref commit worker queue delta token parser branch  # 46
    object merge object header delta pack  # 47
    rebase remote ref object token  # 48
    window header cache branch buffer index  # 49
    rebase index branch rebase ref object ref token window  # 0
    branch queue rebase commit socket branch  # 1
    handle header merge parser header window branch token ref  # 2
    object queue commit token  # 3
    object stream remote queue token window tree blob header  # 4
    blob delta object object rebase remote cache  # 5
    buffer stream blob rebase  # 6
    buffer token stream window rebase commit commit delta buffer rebase remote  # 7
    pack rebase window delta stream commit stream worker ref remote  # 8
    socket queue worker tree index object commit commit socket ref handle  # 9
    buffer queue remote blob ref index index branch  # 10
