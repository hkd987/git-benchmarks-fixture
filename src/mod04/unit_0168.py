    pack delta header remote header blob delta handle commit  # 0
    header socket commit commit  # 1
    token tree object commit pack window index parser socket handle  # 2
    merge object pack queue cache rebase ref merge commit window  # 3
    commit cache buffer token stream worker worker delta  # 4
    token handle object delta  # 5
    worker handle worker blob handle rebase token parser object  # 6
    branch handle tree rebase  # 7
    rebase buffer pack pack blob remote merge stream cache delta tree  # 8
    commit blob worker parser worker handle  # 9
    ref worker rebase ref  # 10
    queue index commit tree merge  # 11
    tree remote queue stream blob index buffer blob buffer branch  # 12
    worker socket delta index socket object buffer  # 13
    parser window window tree parser pack token commit commit buffer queue  # 14
    window blob index buffer blob header rebase stream commit queue stream  # 15
    socket delta ref cache object blob socket index cache  # 16
    handle merge buffer pack rebase commit socket worker window stream  # 17
    queue token worker cache index  # 18
    remote header pack buffer queue parser blob remote  # 19
    buffer token tree stream branch  # 20
    window parser rebase commit queue rebase handle  # 21
    worker window delta stream token pack pack  # 22
    tree token commit commit header blob rebase parser commit queue parser  # 23
    buffer delta socket object branch  # 24
    queue blob remote branch ref index stream blob queue remote buffer  # 25
    header remote buffer delta window  # 26
    stream merge worker socket header ref window header  # 27
    delta window queue tree cache  # 28
    branch object parser index commit socket commit rebase remote  # 29
    cache queue worker delta blob  # 30
    tree object pack blob  # 31
    token socket branch stream token  # 32
    object stream handle ref ref commit  # 33
    remote pack parser rebase pack queue stream window  # 34
    cache cache queue cache parser blob delta  # 35
    pack window pack delta  # 36
    pack index blob delta  # 37
    buffer header remote merge stream object  # 38
    rebase merge header token worker pack commit branch window  # 39
    merge socket handle worker index rebase object parser pack merge  # 40
    cache index tree commit branch socket worker commit stream queue  # 41
    merge rebase object stream pack token delta  # 42
    cache worker commit commit handle commit queue  # 43
    handle socket handle worker ref parser worker tree cache buffer  # 44
    index handle index branch  # 45
    token pack remote rebase blob buffer ref tree branch commit remote rebase  # 46
    token buffer stream merge delta merge  # 47
    socket cache merge header remote handle worker stream  # 48
    ref commit queue socket parser commit tree  # 49
    index stream cache index  # 50
    stream buffer buffer object buffer remote index delta index remote  # 51
    queue window tree token remote worker parser object token  # 52
    commit socket worker worker queue handle delta index worker worker branch  # 53
    merge token delta handle cache merge object pack ref stream branch  # 54
    window commit blob commit token buffer token merge object tree remote  # 55
    socket ref buffer socket cache remote header pack  # 56
    token tree ref object tree merge  # 57
    handle branch tree branch delta handle delta  # 58
    header branch branch pack handle queue rebase delta blob  # 59
    parser header worker parser socket branch window cache pack buffer object pack  # 60
    index header merge stream object queue worker  # 61
    rebase branch cache parser branch window commit  # 62
    object branch commit token parser  # 63
    rebase cache socket remote header token ref index remote window commit cache  # 64
    remote buffer pack remote delta cache blob blob window header cache  # 65
    remote index index delta worker pack index queue pack  # 66
    token delta worker stream delta index stream  # 67
    object commit object token object commit pack buffer  # 68
    rebase window merge blob buffer stream rebase queue merge socket  # 69
    delta buffer tree cache handle socket merge pack header socket object  # 70
    tree blob header branch branch tree delta stream commit pack  # 71
    ref socket blob branch tree parser handle stream  # 72
    header parser queue token remote branch remote ref  # 73
    buffer branch commit object cache merge cache worker queue token queue  # 74
    handle cache branch delta stream tree commit stream remote object cache  # 0
    parser tree socket commit token rebase stream socket remote socket cache  # 1
    window stream commit blob stream remote remote delta merge cache blob  # 2
    handle rebase pack rebase tree token token header ref index rebase  # 3
    token blob stream commit stream ref  # 4
    index token handle socket branch ref  # 5
    token stream blob token  # 6
