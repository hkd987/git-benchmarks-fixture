    socket buffer remote cache pack buffer branch delta  # 0
    remote delta index parser merge remote object commit queue window  # 1
    tree parser commit queue handle index  # 2
    pack stream socket branch handle handle merge branch socket  # 3
    ref index token branch parser  # 4
    object ref worker buffer commit parser worker delta  # 5
    queue remote window token  # 6
    pack object object object token buffer rebase  # 7
    index queue remote handle queue  # 8
    object token window remote queue tree branch buffer commit tree queue parser  # 9
    ref rebase cache cache stream stream  # 10
    blob stream commit worker window  # 11
    header commit object cache rebase blob pack rebase header pack cache blob  # 12
    commit token stream index tree header branch merge branch token header token  # 13
    handle handle index rebase merge remote parser queue buffer  # 14
    queue stream header index index tree pack worker cache  # 15
    handle handle stream delta delta object rebase  # 16
    merge index cache remote branch remote  # 17
    handle merge stream cache merge index  # 18
    token socket object token window handle branch remote queue stream  # 19
    commit window commit stream cache worker socket token buffer ref  # 20
    socket commit parser stream rebase  # 21
    branch tree commit commit ref merge ref stream object socket index  # 22
    socket commit pack handle buffer blob ref object stream tree  # 23
    window tree merge merge buffer  # 24
    blob header commit worker pack socket socket rebase queue  # 25
    cache merge tree branch header window pack remote queue index  # 26
    stream cache parser stream socket  # 27
    buffer worker remote object index parser ref cache cache handle  # 28
    ref stream branch blob commit queue queue branch stream  # 29
    buffer delta blob rebase branch object commit commit token token object  # 30
    window pack rebase stream window token worker remote header pack  # 31
    queue handle index object delta parser tree  # 32
    object object token queue token delta object  # 33
    worker cache parser stream token handle handle index window queue rebase  # 34
    queue object socket window commit token rebase commit socket blob  # 35
    cache object tree worker merge cache delta remote parser token merge  # 36
    header worker remote socket blob rebase parser handle branch window  # 37
    blob rebase delta delta merge object  # 38
    worker buffer socket buffer socket blob object index stream queue  # 39
    rebase parser cache merge commit object ref commit worker  # 40
    object remote buffer window queue window window  # 41
    header window tree buffer pack tree  # 42
    buffer socket index remote merge socket rebase merge window blob socket  # 43
    delta ref commit cache remote queue ref merge commit merge  # 44
    index queue delta worker merge window header branch merge worker stream window  # 45
    token parser rebase commit socket delta window  # 46
    pack parser merge buffer merge merge merge remote rebase remote  # 47
    cache index ref merge branch queue window tree socket window window  # 48
    window handle parser header parser header  # 49
    parser blob token handle buffer window cache tree handle index  # 50
    stream merge socket worker object handle window  # 51
    cache branch blob ref branch worker branch remote  # 52
    handle delta handle merge tree header  # 53
    tree index cache merge branch worker  # 54
    ref tree token worker blob commit window cache buffer queue tree cache  # 55
    socket merge index cache rebase handle worker header delta  # 56
    blob delta cache branch queue remote rebase pack  # 57
    buffer delta window buffer  # 58
    remote stream window window socket delta object index tree rebase cache  # 59
    stream header header blob blob socket remote object blob cache  # 60
    window remote remote stream branch buffer window tree commit header token  # 61
    socket index header delta ref remote socket pack  # 62
    handle blob delta index branch  # 63
    index index branch queue cache window queue tree delta object cache index  # 64
    ref parser index parser ref header  # 65
    branch header object handle branch rebase  # 66
    pack branch buffer socket tree  # 67
    parser object socket handle commit  # 68
    token socket socket branch index branch parser window remote  # 69
    socket blob window commit window handle  # 70
    handle ref index window parser  # 71
    commit object merge cache handle object cache index cache  # 72
    cache remote pack delta header rebase pack stream window  # 73
    socket worker stream socket object pack queue socket index worker index  # 0
    merge object window stream buffer merge remote object  # 1
    merge rebase commit window header rebase commit remote pack blob  # 2
    index delta header object window stream buffer tree handle queue  # 3
