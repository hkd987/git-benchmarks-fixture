    stream remote blob buffer token socket  # 0
    ref cache blob stream cache  # 1
    cache delta parser window ref object window delta pack socket cache buffer  # 2
    header pack delta blob socket window stream  # 3
    branch remote window rebase window index rebase buffer object  # 4
    blob worker buffer commit index merge remote  # 5
    index index buffer commit branch commit rebase header tree  # 6
    merge token worker remote rebase merge tree window rebase  # 7
    parser parser buffer buffer ref socket branch cache blob object token  # 8
    rebase queue blob parser socket commit window token  # 9
    worker index token window stream tree buffer pack header header token ref  # 10
    cache handle token handle remote  # 11
    blob branch pack tree header window socket pack window commit index stream  # 12
    stream header object rebase parser rebase object blob commit cache commit rebase  # 13
    worker tree handle branch header cache worker index branch blob handle socket  # 14
    stream socket branch buffer remote header stream remote merge pack tree  # 15
    blob remote window stream  # 16
    parser tree parser worker worker remote pack commit queue object tree stream  # 17
    socket stream queue cache parser queue  # 18
    parser tree ref blob handle tree rebase buffer  # 19
    blob tree branch stream window  # 20
    index rebase cache ref object worker socket ref socket stream parser branch  # 21
    cache merge stream commit queue ref stream window ref socket queue  # 22
    cache handle header token buffer tree  # 23
    commit branch blob header blob buffer  # 24
    cache header blob worker delta header  # 25
    token worker worker handle parser window socket rebase index handle cache queue  # 26
    delta rebase cache blob ref window stream object  # 27
    token cache socket remote header handle buffer header rebase buffer worker handle  # 28
    pack socket queue worker  # 29
    ref parser merge object commit socket stream blob commit  # 30
    parser pack stream delta queue  # 31
    branch token pack delta socket branch cache stream queue stream token ref  # 32
    commit handle branch branch pack remote delta rebase  # 33
    handle buffer blob queue object window commit commit  # 34
    token tree stream parser merge pack handle object object  # 35
    token socket ref index worker object index ref  # 36
    ref index delta merge buffer queue queue parser buffer  # 37
    merge window token remote merge header blob stream pack tree  # 38
    queue blob stream parser cache tree index worker queue  # 39
    handle cache window parser branch  # 40
    commit remote handle blob branch index pack ref worker header  # 41
    cache handle stream token  # 42
    cache blob pack window merge stream handle tree delta  # 43
    socket cache handle delta  # 44
    worker header queue pack commit rebase token buffer stream branch socket remote  # 45
    object handle token parser  # 46
    handle parser blob header queue token merge delta index buffer rebase  # 47
    worker token merge stream  # 48
    header header commit queue merge merge parser remote buffer delta  # 49
    remote ref ref tree parser window  # 50
    delta merge cache pack stream header header index handle parser commit ref  # 51
    token blob buffer header pack token merge object remote  # 52
    pack window header window handle index blob index delta parser window delta  # 53
    queue ref token handle stream socket window  # 54
    header token branch buffer remote tree socket ref worker delta  # 55
    parser window remote delta worker socket index socket stream rebase delta  # 56
    stream merge branch header index queue pack pack cache cache token  # 57
    socket commit header ref header window parser commit handle  # 58
    worker cache handle pack header delta branch socket  # 59
    header worker blob commit stream token token cache index cache  # 60
    socket stream remote remote branch token token cache  # 61
    merge parser branch rebase ref queue handle token  # 62
    object pack cache tree ref  # 63
    handle stream branch token worker handle queue socket object socket blob  # 64
    handle delta worker merge remote branch remote  # 65
    parser socket blob header buffer pack header commit ref cache  # 66
