    header header worker index branch stream cache  # 0
    merge socket blob blob merge tree  # 1
    header delta handle queue buffer  # 2
    window worker merge blob buffer window  # 3
    handle header remote merge token  # 4
    cache branch handle parser index remote  # 5
    queue remote pack branch  # 6
    parser worker merge merge cache ref handle index  # 7
    socket delta tree queue worker delta  # 8
    window handle cache header worker token object remote index window branch blob  # 9
    token socket token handle socket token  # 10
    header token ref header branch socket blob  # 11
    index branch stream cache  # 12
    stream tree buffer queue commit socket window worker  # 13
    index branch header parser  # 14
    handle header header merge cache parser branch stream delta  # 15
    pack worker handle header commit delta blob handle delta ref object index  # 16
    stream window rebase tree merge parser rebase object queue  # 17
    merge pack cache merge  # 18
    header worker parser cache index tree branch tree window token buffer rebase  # 19
    index token commit handle index ref rebase handle token header  # 20
    queue delta parser branch tree ref rebase branch socket  # 21
    object header token object  # 22
    commit branch commit merge ref token buffer blob header socket  # 23
    remote header object handle blob cache commit pack handle socket  # 24
    ref cache header handle buffer ref header remote cache tree rebase  # 25
    handle parser delta token remote buffer queue object worker worker merge parser  # 26
    buffer commit queue remote tree  # 27
    token object window buffer rebase  # 28
    tree socket buffer branch pack commit tree header socket  # 29
    stream cache handle handle ref remote blob delta header  # 30
    window object branch cache branch delta handle commit rebase  # 31
    buffer handle header stream socket blob merge blob tree branch queue parser  # 32
    queue object parser delta stream remote handle worker queue pack  # 33
    ref branch buffer cache object merge stream worker ref  # 34
    ref object queue socket index worker header  # 35
    window parser delta queue merge object window rebase  # 0
    pack delta worker tree rebase worker worker tree window tree pack  # 1
    buffer parser merge commit object buffer rebase rebase cache index socket  # 2
    stream parser queue rebase  # 3
    ref tree tree pack worker pack window  # 4
    branch token delta tree header  # 0
    handle index tree header commit  # 1
    window socket cache ref index socket  # 2
    queue branch stream merge cache queue delta socket  # 3
    branch blob header merge handle blob tree window  # 4
    remote socket parser remote  # 5
    socket tree remote ref remote delta ref  # 6
    index object parser cache handle window index branch parser  # 7
    rebase buffer queue remote blob buffer object merge  # 8
    buffer delta remote worker  # 9
    blob parser commit branch window buffer  # 10
    window window delta stream socket object commit header branch  # 11
    pack tree buffer index header pack queue ref branch remote remote  # 12
