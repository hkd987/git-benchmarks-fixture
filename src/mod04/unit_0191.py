    merge worker token socket pack pack header delta  # 0
    stream branch rebase ref header  # 1
    index header rebase handle tree queue token cache worker pack header  # 2
    worker cache buffer socket window handle buffer worker object  # 3
    pack merge index branch  # 4
    tree token buffer queue tree rebase handle ref remote  # 5
    socket handle delta socket  # 6
    merge branch remote stream  # 7
    branch header merge delta  # 8
    worker branch branch window  # 9
    commit queue buffer socket ref object socket merge  # 10
    stream parser socket delta pack delta object  # 11
    window handle header blob ref blob cache merge cache delta index commit  # 12
    header worker branch header merge parser tree rebase index object  # 13
    token pack rebase ref  # 14
    delta header buffer remote header branch parser remote cache  # 15
    worker cache index rebase ref tree merge header commit  # 16
    header pack merge header  # 17
    header header socket cache rebase cache pack header object  # 18
    window branch object pack buffer remote  # 19
    worker blob pack stream index commit  # 20
    queue token tree header socket buffer handle branch  # 21
    window blob tree merge parser branch header index object index delta stream  # 22
    token pack window tree cache queue header stream window index index  # 23
    object token socket merge tree commit token  # 24
    blob branch worker cache header branch delta stream worker pack  # 25
    ref index rebase header  # 26
    header delta rebase remote delta window blob header commit worker parser ref  # 27
    stream handle index commit window buffer worker stream branch  # 28
    index buffer socket branch  # 29
    handle buffer delta branch tree branch  # 30
    window commit blob rebase  # 31
    delta queue delta buffer pack  # 32
    blob pack rebase merge parser stream parser blob stream handle cache pack  # 33
    index merge delta branch queue  # 34
    token ref socket handle header stream pack ref  # 35
    stream blob parser cache blob pack window cache pack worker  # 36
    cache blob window commit buffer cache tree ref tree commit handle token  # 37
    tree worker blob cache index tree worker socket queue merge ref worker  # 38
    tree remote socket ref handle blob ref ref delta queue remote  # 39
    handle branch rebase handle blob index commit token  # 40
    parser rebase blob stream branch cache header token object  # 41
    blob parser window remote merge  # 42
    branch ref token cache parser tree  # 43
    buffer object ref delta queue ref ref object window blob queue window  # 44
    window stream parser ref worker index handle cache  # 45
    remote window branch stream stream remote delta parser buffer rebase  # 46
    stream index merge delta stream worker  # 47
    token pack handle branch worker  # 48
    worker branch token handle cache  # 49
    object ref merge index window queue window blob blob index  # 50
    token remote rebase header parser queue window  # 51
    tree parser tree queue parser  # 52
    object commit pack ref delta  # 53
    cache rebase stream pack cache object remote tree  # 54
    remote socket token header  # 55
    index socket index worker blob object rebase  # 56
