    branch remote worker tree cache index  # 0
    branch parser rebase header rebase handle delta pack queue pack merge commit  # 1
    header header queue handle pack rebase delta ref object commit queue  # 2
    commit delta handle worker delta queue pack buffer  # 3
    pack object stream branch cache blob handle commit parser object delta commit  # 4
    cache ref stream token handle remote commit delta branch parser  # 5
    token tree stream rebase socket socket window stream blob parser  # 6
    parser buffer socket branch worker index  # 7
    pack cache queue cache queue pack  # 8
    ref window token socket worker remote  # 9
    token blob handle buffer commit  # 10
    queue worker index commit cache pack branch handle delta delta  # 11
    merge socket delta stream commit  # 12
    rebase queue rebase pack index stream socket  # 13
    buffer remote stream pack socket header  # 14
    delta stream parser parser token worker buffer  # 15
    branch commit worker window cache delta ref cache stream worker queue  # 16
    buffer parser stream object worker  # 17
    rebase queue worker queue merge buffer remote index socket index  # 18
    delta ref commit pack queue  # 19
    cache token cache handle  # 20
    cache handle socket handle  # 21
    parser object merge parser queue buffer socket  # 22
    token ref object stream object cache delta  # 23
    delta index ref commit handle delta branch index stream ref tree index  # 24
    ref parser index socket index  # 25
    buffer queue blob stream commit delta pack ref stream socket  # 26
    stream socket merge buffer buffer stream merge  # 27
    rebase merge queue object pack merge branch stream  # 28
    object pack stream ref commit buffer  # 29
    header parser pack branch branch  # 30
    ref index blob queue header remote rebase branch index buffer rebase  # 31
    cache ref commit socket delta object rebase  # 32
    cache header ref worker  # 33
    pack window object tree ref commit parser handle stream  # 34
    buffer cache header rebase index  # 35
    blob commit rebase tree delta  # 36
    token delta parser delta object rebase token header header pack merge window  # 37
    buffer object buffer delta branch commit commit token cache  # 38
    merge delta branch window token  # 39
    cache ref ref worker stream handle delta  # 40
