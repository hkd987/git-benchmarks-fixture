    parser object blob worker remote merge stream  # 0
    stream buffer rebase ref header remote blob stream blob cache  # 1
    cache token parser window header index commit pack stream  # 2
    delta blob pack stream token handle token remote handle parser merge parser  # 3
    rebase delta cache rebase branch token delta  # 4
    branch commit blob ref  # 5
    handle stream queue stream object handle tree header pack commit  # 6
    ref remote blob tree header stream handle  # 7
    index handle pack commit  # 8
    pack index queue pack ref pack ref buffer index branch socket buffer  # 9
    socket cache buffer rebase stream tree blob rebase parser parser object token  # 10
    tree header remote stream parser rebase stream blob cache object commit  # 11
    queue object commit token blob remote merge stream merge buffer rebase index  # 12
    stream index tree worker  # 13
    worker branch rebase parser tree index branch index branch index branch window  # 14
    socket stream worker stream remote remote object window object branch ref pack  # 15
    handle branch window commit handle branch rebase ref worker ref remote  # 16
    handle header header parser buffer socket parser cache branch stream  # 17
    object header parser merge commit rebase worker  # 18
    buffer remote parser merge  # 19
    blob blob worker delta blob pack rebase stream  # 20
    object tree worker commit  # 21
    pack merge socket header remote buffer  # 22
    handle tree ref worker object  # 23
    handle header index remote index window rebase branch  # 24
    remote stream branch ref header  # 25
    handle merge stream token cache worker socket ref buffer  # 26
    ref commit blob buffer buffer handle handle delta  # 27
    index handle delta stream queue tree  # 28
    object tree cache header rebase  # 29
    queue branch merge handle parser worker parser blob merge header buffer  # 30
    branch commit stream branch tree pack delta rebase queue worker rebase  # 31
    stream socket branch header handle tree index merge remote rebase index  # 32
    ref object token socket index branch  # 33
    buffer stream pack buffer parser queue  # 34
    delta window merge pack branch header ref index rebase blob  # 35
    object cache ref branch  # 36
    tree delta merge remote queue object pack  # 37
    cache blob ref token window  # 38
    token handle queue merge handle stream delta  # 39
    remote merge ref commit index header remote rebase socket object pack rebase  # 40
    handle queue socket branch cache stream branch  # 41
    pack remote socket commit token header buffer tree pack  # 42
    worker ref remote blob  # 43
    window index socket blob pack queue queue ref buffer parser queue  # 44
    blob delta socket cache object merge token buffer token handle  # 45
    blob pack blob cache ref merge commit buffer blob branch  # 46
    branch handle buffer rebase cache  # 47
    parser rebase rebase object commit worker  # 48
    branch cache object rebase token remote buffer  # 49
    parser token cache commit merge  # 50
    queue remote header object header tree handle stream remote token  # 51
    handle remote parser header  # 52
    remote ref tree window object header tree socket  # 53
    delta index commit worker blob pack handle commit window merge  # 54
    parser tree pack merge remote  # 55
    tree commit window merge buffer socket merge index  # 56
    parser socket queue cache  # 57
    branch stream header queue index stream header socket socket  # 58
    remote buffer queue remote header parser delta buffer object parser queue commit  # 59
    socket queue cache stream pack header blob pack remote  # 60
    merge merge socket token window parser object  # 61
    worker rebase header socket  # 62
    window parser branch object cache object buffer merge delta  # 63
    tree parser delta parser tree delta token worker parser commit commit  # 64
    merge queue pack window queue tree  # 65
    parser blob token index tree buffer remote pack branch merge tree handle  # 66
