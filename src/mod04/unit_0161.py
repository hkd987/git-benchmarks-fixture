    queue token commit socket object cache  # 0
    buffer handle buffer ref object socket  # 1
    socket blob token ref delta  # 2
    queue blob object pack rebase window commit buffer  # 3
    blob parser tree worker branch parser branch parser  # 4
    merge buffer token token object parser parser  # 5
    branch remote delta cache ref delta delta token  # 6
    rebase buffer socket ref  # 7
    handle object queue blob stream  # 8
    rebase branch handle parser object  # 9
    tree blob window window pack  # 10
    worker object rebase token token delta  # 11
    cache handle index branch buffer commit commit ref queue token  # 12
    branch buffer remote stream socket token header commit socket  # 13
    ref index socket parser cache window object tree pack merge  # 14
    cache handle delta remote socket  # 15
    socket tree token tree parser stream parser delta parser window token  # 16
    remote remote pack handle  # 17
    blob pack object remote token handle pack branch ref delta worker queue  # 18
    buffer index blob ref object remote object tree buffer  # 19
    socket ref token window commit token header tree window commit  # 20
    blob queue socket tree  # 21
    index branch tree blob merge parser window pack  # 22
    merge queue buffer cache parser buffer  # 23
    handle handle index token  # 24
    token commit tree branch ref worker  # 25
    rebase stream window buffer blob delta  # 26
    rebase index stream header cache  # 27
    header ref index branch header commit  # 28
    queue ref blob tree token worker rebase buffer rebase worker  # 29
    ref handle ref stream handle cache handle rebase handle buffer cache queue  # 30
    header stream window branch tree header commit header  # 31
    cache remote parser socket token window commit  # 32
    window tree worker stream ref tree tree cache  # 33
    handle object handle cache stream commit delta branch ref merge  # 34
    ref handle worker token blob parser handle index socket handle object buffer  # 35
    delta socket object index queue delta socket index handle index  # 36
    token tree header worker remote socket buffer queue  # 37
    ref object token handle blob header queue worker worker handle branch merge  # 38
    rebase rebase window header tree tree queue buffer window remote branch remote  # 39
    commit handle merge pack token remote  # 40
    stream worker window header cache stream  # 41
    tree rebase branch cache token buffer merge  # 42
    handle rebase stream cache window handle buffer  # 43
    worker worker rebase header tree  # 44
    index pack merge socket pack parser index ref buffer  # 45
    parser handle buffer cache  # 46
    remote header parser window stream tree ref parser pack worker  # 47
    ref branch window tree queue socket  # 48
    socket tree blob object  # 49
