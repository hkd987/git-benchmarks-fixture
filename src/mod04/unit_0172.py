    cache rebase buffer rebase handle object blob commit  # 0
    token branch index remote token object delta merge cache stream remote worker  # 1
    token header parser socket header  # 2
    window remote buffer branch  # 3
    ref cache delta pack socket merge commit worker buffer  # 4
    buffer merge merge ref  # 5
    token socket commit rebase blob stream pack rebase object tree  # 6
    ref merge buffer token tree delta  # 7
    socket queue rebase ref  # 8
    header cache merge rebase header index tree  # 9
    window parser pack queue commit object branch window worker  # 10
    token branch cache rebase index tree pack handle commit  # 11
    branch window remote pack stream object socket  # 12
    branch pack delta window  # 13
    token buffer tree delta ref window ref pack stream worker ref  # 14
    object header branch queue queue parser  # 15
    tree tree stream ref delta index queue window branch object queue window  # 16
    object index index object merge blob stream merge socket  # 17
    buffer blob token index commit parser cache  # 18
    rebase tree buffer socket object socket  # 19
    merge pack parser pack tree pack ref remote header header header object  # 20
    rebase pack ref blob token branch parser branch pack remote  # 21
    header pack object handle window pack ref  # 22
    object pack header parser handle stream  # 23
    buffer parser stream queue  # 24
    index merge remote token window ref delta window window pack index socket  # 25
    object queue blob buffer branch token commit blob header commit  # 26
    merge buffer buffer parser rebase rebase stream stream object index parser  # 27
    token handle merge parser  # 28
    worker index index window queue delta  # 29
    buffer commit tree object blob blob commit handle  # 30
    header cache delta stream queue pack queue header  # 31
    branch remote token token handle pack  # 32
    socket handle pack remote  # 33
    cache pack header queue pack delta token delta delta parser  # 34
    rebase window ref blob queue object queue merge queue rebase  # 35
    pack buffer stream header  # 36
    window pack merge branch index stream cache  # 37
    merge merge parser merge object  # 38
    branch index pack rebase  # 39
    object stream header buffer handle window buffer cache  # 40
    branch tree pack header handle remote blob  # 41
    blob object header merge object token index socket parser window  # 42
    blob token cache index pack rebase window tree remote header remote socket  # 43
    tree window remote buffer worker header pack  # 44
    branch merge rebase pack tree tree queue window  # 45
    ref window stream token tree queue stream pack queue window  # 46
    stream window queue remote  # 47
    rebase tree queue parser token branch socket commit  # 48
    index delta tree object branch parser header handle buffer header socket  # 49
    cache worker delta header delta remote rebase  # 50
    delta pack branch window cache pack branch delta worker rebase delta socket  # 51
    cache token tree tree cache header tree  # 52
    parser token commit ref header header cache stream socket queue  # 53
    ref socket branch socket pack object  # 54
    worker merge cache branch  # 55
    object branch merge merge parser token pack  # 56
    buffer header index merge tree  # 57
    worker commit commit tree pack object rebase remote stream  # 58
    buffer handle merge index queue token  # 59
    rebase merge socket commit index  # 60
