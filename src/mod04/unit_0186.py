    cache pack cache commit cache object parser handle  # 0
    handle token remote index index cache  # 1
    ref token branch handle index tree window  # 2
    index cache stream buffer pack token worker handle object token index  # 3
    rebase cache object remote branch token handle cache  # 4
    cache buffer delta header token handle blob handle blob handle remote  # 5
    delta tree buffer window socket queue cache rebase remote worker queue  # 6
    token branch header handle header  # 7
    worker tree socket parser token  # 8
    delta blob token parser rebase pack handle merge commit  # 9
    ref cache blob window stream stream blob handle handle commit  # 10
    worker blob index handle rebase object tree queue  # 11
    cache branch worker ref tree  # 12
    header header window index merge stream stream commit tree header merge  # 13
    merge pack delta remote tree object handle socket token  # 14
    header socket socket handle window parser pack rebase cache index  # 15
    socket cache window rebase branch branch buffer window queue commit worker  # 16
    rebase commit blob queue handle header tree object tree branch stream  # 17
    merge object parser token handle delta object pack ref token object queue  # 18
    pack cache handle window header buffer  # 19
    tree buffer delta window queue  # 20
    token window queue commit index  # 21
    commit buffer worker header ref  # 22
    tree worker object handle token ref cache  # 23
    blob pack ref index blob parser window object header delta delta pack  # 24
    pack index ref handle object  # 25
    blob tree pack remote cache stream handle tree blob object blob  # 26
    pack tree remote window rebase index tree index commit pack object  # 27
    queue branch token pack merge remote handle merge rebase index merge  # 28
    parser ref handle branch rebase index queue blob parser parser buffer commit  # 29
    window buffer stream window  # 30
    tree header handle worker delta delta index socket branch tree delta window  # 31
    parser queue handle rebase parser token  # 32
    queue rebase queue index token merge blob parser worker worker  # 33
    socket branch parser index remote blob cache branch stream ref  # 34
    parser delta ref merge cache rebase delta index  # 35
    window object token blob  # 36
    blob queue token window buffer buffer tree worker blob cache  # 37
    index remote blob window branch worker  # 38
    queue worker worker header socket ref handle commit window delta index worker  # 39
    index merge tree remote  # 40
    branch commit rebase stream merge window window handle buffer ref socket socket  # 41
    header branch cache worker worker pack ref remote  # 42
    header merge parser handle window object socket object handle remote branch worker  # 43
    remote rebase parser blob remote  # 44
    ref queue socket header  # 45
    handle commit branch branch commit  # 46
    object queue branch worker buffer buffer blob index socket  # 47
    cache commit blob handle commit socket delta buffer ref stream  # 48
    token blob commit index worker branch stream  # 49
    delta branch handle branch window rebase buffer token  # 50
    index blob blob parser stream ref delta pack blob delta delta  # 51
    window blob blob header buffer socket  # 52
    window object worker window index commit remote commit delta merge  # 53
    branch worker queue stream header pack worker queue window index  # 54
    ref parser window parser buffer merge parser  # 55
    token window stream queue tree remote  # 56
    parser commit remote blob token  # 57
    tree index merge token stream branch merge delta commit  # 58
    blob pack token ref handle  # 59
    blob delta delta socket index window merge index pack  # 60
    rebase remote commit parser queue branch header rebase blob remote  # 61
    commit socket worker commit tree pack header commit  # 62
    header stream commit object parser worker handle  # 63
    cache parser blob delta remote socket tree delta ref socket  # 64
    remote parser branch queue worker pack delta worker  # 65
    buffer rebase pack queue object handle pack remote  # 66
    stream handle stream pack handle cache token handle worker blob handle handle  # 67
    ref merge tree remote worker header window cache commit index header  # 68
    rebase cache cache remote object worker blob delta handle delta  # 69
    socket parser merge token index object index ref window index  # 70
    parser socket ref parser object token pack handle cache blob queue delta  # 71
    merge commit handle worker parser blob delta handle token header socket token  # 72
    tree blob blob buffer remote delta buffer delta delta rebase parser  # 73
