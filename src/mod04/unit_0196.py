    window object cache queue object handle  # 0
    branch worker tree window commit cache tree blob ref worker commit index  # 1
    token stream blob tree pack object index token  # 2
    branch branch header parser cache  # 3
    merge pack parser buffer parser  # 4
    buffer cache window commit buffer  # 5
    handle handle cache merge stream pack remote pack buffer object  # 6
    remote pack handle queue stream cache object window rebase window  # 7
    merge merge stream index buffer tree commit worker  # 8
    socket remote delta worker header  # 9
    socket branch branch parser header header stream  # 10
    delta header stream pack rebase queue blob delta queue  # 11
    commit socket ref remote  # 12
    stream buffer queue branch ref parser queue  # 13
    header window header index blob tree delta  # 14
    stream ref ref object cache merge merge header commit merge header token  # 15
    handle parser window object cache worker ref socket buffer ref buffer worker  # 16
    worker socket token buffer blob window commit worker merge merge pack buffer  # 17
    worker token buffer parser header rebase  # 18
    commit parser window stream delta stream delta tree  # 19
    commit index worker parser stream ref delta stream remote delta stream pack  # 20
    stream worker window branch window stream queue token stream worker pack stream  # 21
    token ref tree delta socket stream token ref  # 22
    cache stream rebase buffer object blob index rebase  # 23
    socket handle pack blob queue pack parser index parser object queue socket  # 24
    parser handle socket remote delta pack object commit header  # 25
    window header rebase buffer pack socket  # 26
    worker branch stream token branch window  # 27
    token worker stream rebase merge header worker token  # 28
    socket queue branch delta  # 29
    queue rebase worker handle cache commit tree worker commit queue queue delta  # 30
    token rebase remote buffer token socket remote handle  # 31
    commit stream delta buffer remote object tree branch stream  # 32
    object commit header window index commit stream  # 33
    worker tree stream window index stream handle blob object header  # 34
    tree buffer socket cache merge stream index blob merge queue remote stream  # 35
    tree commit stream delta merge rebase window queue  # 36
    index branch cache token branch handle parser ref  # 37
    rebase stream socket stream  # 38
    remote tree ref cache ref parser commit stream  # 39
    rebase handle header cache queue pack merge window window rebase  # 40
    buffer blob pack stream window buffer queue  # 41
    header object commit object index  # 0
    handle worker stream blob ref merge branch delta stream  # 1
    index branch handle branch  # 2
    remote window rebase tree header queue stream branch blob stream branch  # 3
    blob socket pack branch delta object parser  # 4
    window merge ref pack  # 5
    rebase tree remote worker remote buffer socket parser  # 6
    header rebase delta stream rebase queue ref worker  # 7
    buffer remote pack worker  # 8
    index handle parser token token commit ref header index token token  # 9
    buffer stream commit object ref merge branch buffer header  # 10
