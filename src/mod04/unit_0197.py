    ref handle merge pack tree merge buffer merge blob stream  # 0
    commit merge rebase index stream header header index blob index ref object  # 1
    parser socket branch object pack  # 2
    window parser queue ref remote branch pack  # 3
    remote socket token commit  # 4
    delta stream window index  # 5
    delta commit rebase pack handle  # 6
    ref stream rebase tree remote parser ref token index  # 7
    index pack index buffer tree commit object header cache  # 8
    worker stream token buffer  # 9
    socket blob index handle handle parser window socket  # 10
    index parser pack blob stream ref socket  # 11
    buffer queue header handle cache  # 12
    parser delta commit token  # 13
    worker queue window cache stream handle  # 14
    pack cache blob pack object worker socket blob index window pack  # 15
    index stream socket cache token  # 16
    token token parser token remote worker handle  # 17
    branch remote ref pack  # 18
    stream remote ref window stream pack remote tree branch tree  # 19
    buffer tree merge window rebase socket token  # 20
    cache branch blob rebase  # 21
    stream queue stream remote header handle queue buffer cache queue  # 22
    header queue object tree handle header socket blob parser window  # 23
    branch header pack parser commit commit branch buffer commit worker  # 24
    remote branch header worker pack delta rebase handle commit  # 25
    parser ref token index  # 26
    window remote parser header socket buffer cache parser  # 27
    cache ref worker ref commit buffer token ref tree commit ref  # 28
    pack object rebase token header  # 29
    branch handle merge commit pack delta handle parser blob  # 30
    commit rebase object parser index rebase worker stream window  # 31
    stream handle header handle socket  # 32
    remote object socket buffer remote object worker ref  # 33
    queue handle rebase window cache  # 34
    index merge header delta cache delta socket window  # 35
    cache branch ref cache stream blob parser  # 36
    ref merge window socket header  # 37
    token pack window tree pack  # 38
    commit branch blob socket stream rebase commit object worker commit queue  # 39
    window commit handle merge header header header tree handle branch socket worker  # 40
    index queue branch stream buffer delta index cache blob  # 41
    ref stream parser branch remote remote window commit buffer  # 42
    index rebase rebase tree cache parser delta delta token object delta  # 43
    ref header remote buffer merge  # 44
    delta tree tree index pack token handle socket buffer branch object cache  # 45
    queue merge handle pack socket delta queue cache remote object tree  # 46
    stream remote pack header delta socket handle  # 47
    stream header delta ref window pack tree  # 48
    ref tree delta delta delta header merge ref header cache  # 49
    pack object header buffer pack parser pack window stream delta pack window  # 50
    merge commit cache queue cache remote window index socket token ref worker  # 51
    queue commit parser rebase  # 52
    index socket delta pack tree socket token cache tree remote  # 53
    header header header commit cache rebase handle  # 54
    commit buffer branch pack branch delta pack  # 55
    socket parser ref parser window delta buffer branch remote blob token queue  # 56
    branch delta rebase blob  # 57
    branch index parser pack rebase queue buffer worker blob  # 58
    parser merge remote object delta header  # 59
    tree index remote pack merge pack cache rebase commit rebase parser index  # 60
    parser remote delta commit  # 61
    merge ref tree delta token rebase  # 62
