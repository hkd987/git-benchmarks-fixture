    branch parser buffer tree queue token remote window object remote socket  # 0
    buffer pack rebase header token ref index pack ref header parser  # 1
    delta commit delta remote tree token stream cache stream object delta header  # 2
    worker commit blob merge buffer blob blob tree  # 3
    buffer commit ref socket commit ref  # 4
    buffer object delta object ref parser  # 5
    queue socket cache blob commit  # 6
    socket remote rebase remote queue merge object merge parser  # 7
    queue delta window index buffer stream  # 8
    blob handle index stream cache parser  # 9
    merge window buffer window tree index worker blob remote cache  # 10
    queue index blob header  # 11
    socket handle window worker  # 12
    tree parser worker ref token merge object ref header  # 13
    queue tree remote index branch socket delta blob delta  # 14
    blob cache parser cache branch commit rebase buffer stream worker delta  # 15
    object blob handle worker socket stream worker  # 16
    handle cache cache cache cache object token cache handle delta  # 17
    stream worker remote parser token cache index token stream object tree  # 18
    branch remote ref branch header  # 19
    handle merge buffer handle tree blob window window  # 20
    index remote socket delta remote buffer cache token  # 21
    object stream parser blob stream branch delta delta tree  # 22
    merge queue index worker commit stream commit index window socket index commit  # 23
    object commit queue token stream tree token header  # 24
    stream cache branch remote  # 25
    header window queue delta delta pack queue  # 26
    window pack merge object parser pack queue commit remote  # 27
    handle cache delta tree merge branch commit blob delta parser object  # 28
    object socket commit commit delta handle buffer branch index handle  # 29
    cache object stream merge  # 30
    branch rebase token rebase merge  # 31
    cache branch index header ref blob merge socket  # 32
    parser handle stream stream  # 33
    socket commit index merge tree cache window buffer parser  # 34
    window blob remote ref token token header pack index pack pack  # 35
    header commit cache window  # 36
    socket socket object rebase window remote index  # 37
    commit worker cache cache commit pack ref ref commit cache socket  # 38
    handle queue header buffer index ref buffer buffer  # 39
    header rebase parser cache  # 40
    queue blob branch rebase ref header stream socket socket tree queue  # 41
    delta delta cache pack remote commit ref index commit stream socket  # 42
    buffer object buffer worker socket  # 43
    delta commit object parser commit blob object buffer  # 44
    header handle handle object header  # 45
    object header rebase parser commit merge blob branch cache header branch delta  # 46
    buffer socket commit cache blob header rebase delta  # 47
    cache window buffer tree rebase  # 48
    index parser parser blob remote delta ref ref delta tree blob stream  # 49
    remote header tree window ref blob blob object cache  # 50
    rebase rebase branch header parser pack delta buffer delta cache  # 51
    stream handle header commit queue queue ref branch  # 52
    stream blob branch rebase socket window token blob cache  # 53
    socket socket commit rebase window blob  # 54
    socket tree worker blob queue stream  # 55
    stream socket remote buffer worker token tree remote handle rebase  # 56
    pack stream header header rebase merge header commit stream  # 57
    blob index queue socket pack  # 58
    worker header commit commit index token branch socket cache header tree tree  # 59
    tree window window delta blob  # 60
    pack object handle tree delta handle handle cache  # 61
    ref object socket object cache cache pack  # 62
    tree merge worker window cache tree remote merge header  # 63
    tree cache queue token handle window cache buffer branch stream blob  # 64
    queue rebase blob parser merge socket header cache stream commit stream  # 65
    branch cache stream commit worker pack parser cache  # 66
    ref pack pack blob queue branch socket tree branch rebase branch index  # 67
    commit object handle buffer cache commit ref header handle window window  # 68
