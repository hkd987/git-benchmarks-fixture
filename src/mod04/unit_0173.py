    window worker socket socket commit commit worker stream blob handle token header  # 0
    blob commit worker ref index header queue header tree handle  # 1
    socket merge token branch rebase pack parser tree  # 2
    worker header queue merge handle header  # 3
    object handle cache commit queue pack object pack handle handle  # 4
    merge rebase handle parser queue pack object socket parser rebase socket queue  # 5
    socket parser commit window  # 6
    merge index blob handle ref branch worker  # 7
    merge worker pack token branch  # 8
    pack remote window pack rebase token window window parser remote socket  # 9
    worker pack queue index merge window socket buffer commit remote  # 10
    pack branch parser socket ref cache queue ref  # 11
    window tree index tree queue stream queue delta token  # 12
    cache window branch queue  # 13
    object parser header worker window remote tree token handle commit cache  # 14
    cache pack rebase blob object object queue window  # 15
    stream token pack queue worker  # 16
    remote worker remote tree index buffer window object  # 17
    handle socket rebase window tree socket worker rebase token socket handle ref  # 18
    blob blob header worker ref  # 19
    delta branch rebase parser tree stream handle index branch buffer tree  # 20
    tree tree handle pack stream tree buffer commit cache commit  # 21
    object cache token socket handle cache index remote commit cache branch stream  # 22
    pack delta stream delta  # 23
    branch blob merge worker branch stream queue branch buffer delta  # 24
    remote object token pack tree tree object stream socket  # 25
    branch object cache merge object queue cache object pack ref pack queue  # 26
    handle index delta parser rebase  # 27
    ref index stream socket rebase pack handle tree  # 28
    window remote socket token buffer token worker stream object cache  # 29
    token tree blob blob token ref stream parser pack queue branch  # 30
    ref token ref tree token index token remote handle socket  # 31
    remote pack buffer queue  # 32
    branch ref socket header  # 33
    ref worker tree stream remote delta buffer branch commit commit rebase branch  # 34
    rebase object object header index handle socket index header stream window queue  # 35
    buffer parser socket buffer buffer ref branch pack worker tree  # 36
    parser worker remote token index stream  # 37
    merge stream parser worker window  # 38
    socket pack commit branch tree  # 39
    cache buffer cache rebase commit  # 40
    queue buffer object rebase socket window branch rebase merge window delta stream  # 41
    pack cache parser queue header remote stream handle handle worker  # 42
    socket parser blob worker handle queue tree  # 43
    remote pack pack rebase delta window index handle parser cache  # 44
    ref remote queue header ref pack  # 45
    branch index rebase tree window merge worker delta index index  # 46
    stream merge worker remote blob queue  # 47
    blob socket queue rebase delta rebase object branch remote  # 48
    delta merge tree pack worker  # 49
    commit merge worker merge  # 50
    parser pack ref object stream tree delta  # 51
    header token tree branch queue index cache parser pack  # 52
    token ref buffer buffer branch rebase tree  # 53
    stream stream buffer token index worker handle  # 54
    blob ref worker object branch ref  # 55
    blob window rebase blob  # 56
    merge buffer pack blob  # 57
    token merge pack blob pack object pack object remote delta  # 58
    object remote merge tree branch blob parser  # 59
    blob buffer pack pack remote cache cache branch cache commit buffer  # 60
    object handle token branch object stream parser window object cache branch  # 61
    token merge buffer merge parser delta cache queue blob branch header queue  # 62
    worker header window index branch object cache merge  # 63
    commit parser ref token delta header queue index pack header merge  # 64
    handle handle commit queue branch remote token index  # 65
    handle window window ref socket socket  # 66
    branch header token stream object ref branch window rebase cache header remote  # 67
    tree cache branch branch delta rebase tree rebase rebase token  # 68
    worker tree worker stream rebase  # 0
    ref remote merge tree parser rebase commit pack commit merge ref ref  # 1
    pack rebase cache socket merge tree ref object rebase stream  # 2
    object delta cache tree stream ref branch index cache  # 3
    merge branch tree socket socket  # 4
    socket socket remote worker commit handle  # 5
    blob window remote token merge index window  # 6
    object blob rebase stream ref blob pack  # 7
    tree buffer delta header parser index handle  # 8
    branch index remote commit worker branch rebase delta worker branch  # 9
    parser worker remote rebase window window parser token branch cache parser  # 10
    object cache window remote  # 11
    branch blob ref ref index worker index window buffer parser branch  # 12
    cache commit delta ref ref handle cache queue delta  # 0
    ref handle token token delta merge stream token pack window commit tree  # 1
    cache ref handle branch handle parser worker cache handle merge window delta  # 2
    remote cache blob cache rebase pack stream merge blob  # 3
    window cache pack parser blob  # 4
    token queue handle cache commit merge header branch header socket buffer  # 5
    commit remote worker pack  # 6
    remote object delta cache buffer tree object stream remote  # 7
    socket window queue parser pack index commit header handle  # 8
    stream remote tree token remote rebase object token buffer queue merge remote  # 9
    parser handle parser cache window worker remote window parser  # 10
    window index object ref  # 11
    header header commit socket tree object queue handle parser queue buffer  # 12
    buffer header header commit remote  # 13
    handle index tree socket delta  # 14
