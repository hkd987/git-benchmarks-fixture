    buffer blob blob pack ref merge ref ref  # 0
    tree window queue object header merge commit commit  # 1
    buffer socket parser commit window parser ref worker window handle token object  # 2
    index rebase pack ref pack token tree pack commit delta  # 3
    branch handle rebase queue  # 4
    ref rebase parser socket  # 5
    rebase blob index window delta  # 6
    queue queue remote pack object  # 7
    buffer rebase handle window header parser remote  # 8
    index blob tree socket tree stream queue handle  # 9
    stream token ref parser buffer delta  # 10
    blob header worker header  # 11
    worker worker remote token header window parser parser token socket  # 12
    socket remote commit commit tree  # 13
    worker stream remote cache stream tree token object  # 14
    index header token remote tree  # 15
    worker branch header stream ref stream cache  # 16
    header rebase rebase commit  # 17
    window tree remote header parser handle ref rebase blob window  # 18
    branch rebase index delta stream header cache cache rebase index index index  # 19
    worker blob rebase stream window branch remote worker buffer  # 20
    stream commit delta tree  # 21
    branch ref socket remote worker  # 22
    commit worker blob merge buffer index header handle branch stream  # 23
    tree worker ref cache queue cache ref rebase  # 24
    object merge handle index parser blob buffer handle stream remote cache  # 25
    socket remote header object socket object buffer delta  # 26
    blob header blob commit commit commit pack index delta socket worker  # 27
    handle stream queue delta tree  # 28
    object window object stream ref  # 29
    handle worker tree token stream worker window cache parser remote  # 30
    merge token window handle index pack tree queue  # 31
    commit rebase pack branch header handle handle remote remote delta cache  # 32
    rebase window pack rebase merge socket queue branch stream handle stream  # 33
    header token delta delta tree index  # 34
    blob buffer queue commit parser commit merge  # 35
    rebase pack rebase socket object cache parser branch merge branch  # 36
    branch queue handle worker blob parser tree socket object window  # 37
    window branch ref blob tree socket handle  # 38
    window token window stream parser pack  # 39
    blob buffer branch object worker remote header merge object buffer blob pack  # 40
    ref queue rebase index header ref rebase  # 41
    cache window index branch stream queue remote remote pack branch buffer rebase  # 42
    window commit token ref buffer socket  # 43
    socket index rebase delta commit header delta ref header parser buffer buffer  # 44
    tree tree buffer parser socket remote worker ref header  # 45
    branch parser stream buffer worker  # 46
    commit blob merge window handle socket commit buffer index merge  # 47
    object queue rebase socket socket merge object ref blob rebase commit cache  # 48
    token socket blob window tree parser blob pack  # 49
    delta socket index object cache  # 50
    window stream worker commit object socket  # 51
    object buffer header blob window socket merge window ref token  # 52
    commit commit parser pack branch delta  # 53
    token parser stream header blob commit queue pack remote delta worker rebase  # 54
    buffer rebase buffer token remote token object pack header ref index  # 55
    blob worker stream token object index remote branch index stream index  # 56
    header blob blob rebase header branch window blob branch  # 57
