    branch object index buffer  # 0
    delta blob buffer buffer  # 1
    object parser merge remote parser  # 2
    worker pack pack ref pack rebase buffer worker ref  # 3
    buffer parser commit parser merge handle  # 4
    commit token stream worker socket  # 5
    window remote parser branch delta tree parser parser  # 6
    stream cache stream tree buffer  # 7
    parser cache branch index queue tree object token remote handle index  # 8
    branch window worker ref index tree branch delta ref pack  # 9
    index window remote tree parser  # 10
    socket tree rebase header cache rebase  # 11
    ref remote index rebase merge branch commit  # 12
    delta tree worker branch ref stream header  # 13
    buffer delta remote blob rebase index pack buffer pack delta  # 14
    rebase tree pack socket socket queue delta header queue token  # 15
    parser socket token remote stream socket socket queue handle index socket  # 16
    tree rebase blob cache socket handle delta token socket delta  # 17
    stream queue delta remote tree blob commit token ref queue delta merge  # 18
    pack branch commit blob commit remote window blob token  # 19
    object header index delta branch tree remote delta parser header parser socket  # 20
    tree worker queue object remote cache  # 21
    tree window branch socket branch tree remote pack commit pack commit worker  # 22
    index pack remote cache window header parser queue index rebase window rebase  # 23
    tree token handle commit handle header  # 24
    queue header worker socket tree socket remote branch rebase index  # 25
    queue window blob socket queue commit  # 26
    rebase header socket ref token  # 27
    header parser index cache cache branch rebase handle remote  # 28
    remote merge buffer header worker object token  # 29
    handle delta worker window parser token stream  # 30
    tree pack ref delta header window blob token pack queue header cache  # 31
    cache rebase merge rebase queue tree token index blob  # 32
    cache delta worker merge pack pack  # 33
    handle worker merge commit worker merge remote blob header remote object  # 34
    index tree token parser  # 35
    header stream commit queue branch  # 36
    pack delta stream parser  # 37
    blob commit commit window object stream ref merge delta header merge  # 38
    tree branch commit token object  # 39
    blob handle branch tree branch header pack blob index  # 40
    token window pack rebase stream worker  # 41
    delta token merge cache branch remote  # 42
    delta buffer object cache header worker  # 43
    parser remote parser pack delta index remote  # 44
    merge buffer remote worker cache ref rebase window cache  # 45
    token queue blob header merge header buffer merge header  # 46
    object queue remote stream cache socket header branch buffer stream  # 47
    merge token object header object rebase buffer worker rebase object branch pack  # 48
    merge ref header header header object commit token window worker parser  # 49
    handle rebase index merge token delta commit  # 50
    delta stream worker object header queue parser socket buffer  # 51
    header worker blob cache merge  # 52
