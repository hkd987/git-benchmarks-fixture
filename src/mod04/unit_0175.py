    stream header handle rebase cache worker delta remote delta token pack tree  # 0
    queue ref window rebase blob  # 1
    handle window commit header header  # 2
    index stream ref branch delta merge blob buffer index branch remote commit  # 3
    buffer stream index stream commit remote branch  # 4
    branch pack delta tree remote  # 5
    token commit parser object rebase  # 6
    stream window token delta ref blob index socket remote socket  # 7
    delta header buffer cache socket handle worker  # 8
    merge worker ref tree blob commit merge remote pack remote  # 9
    pack branch socket window  # 10
    handle handle object object object handle branch pack cache index tree ref  # 11
    header rebase queue queue window rebase blob window header  # 12
    cache window buffer queue worker delta branch queue blob tree socket window  # 13
    tree index token cache parser index branch merge object  # 14
    object tree token commit commit remote ref queue handle blob handle stream  # 15
    commit ref ref header parser pack commit window  # 16
    socket merge window buffer stream parser  # 17
    queue commit handle socket blob blob parser token cache queue  # 18
    remote cache parser parser branch header rebase token blob object  # 19
    index cache header socket  # 20
    handle queue worker remote  # 21
    remote index ref ref stream buffer  # 22
    queue index remote pack  # 23
    commit queue stream branch merge socket tree pack  # 24
    queue header branch tree remote ref  # 25
    cache parser window remote parser blob merge cache token blob window  # 26
    remote branch branch index branch index branch parser window merge stream header  # 27
    stream blob header rebase delta  # 28
    branch tree merge blob parser socket ref  # 29
    blob window pack cache ref  # 30
    remote queue blob socket  # 31
    commit header token socket socket blob worker tree ref worker branch socket  # 32
    pack delta tree window  # 33
    commit queue rebase pack cache  # 34
    parser index commit parser stream rebase header token  # 35
    blob token queue tree parser remote object object object  # 36
    token socket index queue window queue blob socket cache object index socket  # 37
    window rebase socket object remote socket token parser rebase socket parser  # 38
    branch pack stream socket commit parser index  # 39
    buffer index buffer object queue buffer socket cache  # 40
    remote buffer branch header commit tree socket ref parser socket blob header  # 41
    buffer commit rebase rebase token merge  # 42
    object commit delta commit pack index blob branch  # 43
    branch worker queue tree tree merge queue  # 44
    handle remote handle worker  # 45
    index rebase commit rebase  # 46
    tree handle commit stream worker socket branch  # 47
    rebase object token window socket pack token ref  # 48
    worker handle branch socket socket parser object tree queue buffer buffer  # 49
    object merge ref header remote tree cache branch handle merge window merge  # 50
    token handle ref rebase commit rebase rebase socket token  # 51
    index blob queue handle token socket buffer pack buffer parser  # 52
    worker header socket handle token  # 53
    token branch object header merge tree  # 54
    queue handle index window ref commit socket  # 55
    delta branch stream worker pack worker  # 56
    remote parser rebase pack parser  # 57
    rebase tree buffer branch worker blob buffer commit socket index blob  # 58
    handle branch rebase header object merge delta commit ref pack  # 59
    window branch merge token handle worker tree blob parser pack commit  # 60
    index token rebase token remote commit pack  # 0
    pack stream index commit cache  # 1
    header buffer tree commit branch header handle tree commit cache branch  # 2
    handle merge parser delta  # 3
