    token buffer socket rebase commit pack socket rebase merge object  # 0
    commit parser window window  # 1
    handle object pack cache handle  # 2
    handle remote delta tree window handle worker queue header buffer  # 3
    socket commit pack buffer token commit remote  # 4
    header token blob merge rebase commit commit  # 5
    token pack stream parser parser socket buffer merge remote blob cache  # 6
    merge blob delta rebase delta window header remote stream commit  # 7
    cache object index object buffer blob remote socket  # 8
    cache pack commit commit worker remote buffer index socket tree  # 9
    merge rebase commit index header parser worker stream handle delta handle token  # 10
    blob commit branch remote cache socket parser ref window stream delta  # 11
    remote commit rebase delta token handle parser parser rebase rebase socket  # 12
    rebase socket commit branch ref handle handle parser buffer rebase  # 13
    rebase remote socket index  # 14
    merge handle buffer blob commit rebase pack branch  # 15
    stream ref index branch socket commit pack ref  # 16
    socket remote header worker pack parser stream cache tree  # 17
    index worker merge commit object delta object merge commit stream  # 18
    worker commit buffer cache  # 19
    worker buffer ref token index rebase delta pack  # 20
    parser handle commit parser stream delta pack  # 21
    delta handle socket index window blob buffer index handle  # 22
    cache object socket handle merge rebase  # 23
    window queue parser object pack header blob header commit handle index  # 24
    ref stream handle worker worker handle  # 25
    worker ref parser delta index ref object remote merge  # 26
    rebase object stream stream ref branch tree parser  # 27
    blob pack merge tree header queue remote index queue blob merge remote  # 28
    ref parser pack cache object delta buffer object buffer  # 29
    stream delta object socket index pack buffer remote delta ref delta index  # 30
    branch buffer pack buffer handle worker stream parser window token merge buffer  # 31
    rebase ref commit commit blob header  # 32
    remote handle stream worker buffer queue worker  # 33
    tree window parser blob stream stream socket rebase delta  # 0
    socket rebase object branch stream token buffer ref cache worker rebase window  # 1
    worker remote parser handle rebase remote header socket  # 2
    index object blob rebase buffer header  # 3
    remote ref parser blob parser object  # 4
    cache merge cache header cache  # 5
    rebase pack worker stream merge  # 6
