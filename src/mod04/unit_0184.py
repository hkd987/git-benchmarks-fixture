    pack blob token header  # 0
    pack remote header pack blob cache blob socket cache stream branch  # 1
    commit parser pack rebase cache index remote handle ref token commit  # 2
    commit header parser index socket delta  # 3
    object socket header commit header cache rebase  # 4
    stream remote handle buffer merge worker socket stream remote buffer merge branch  # 5
    remote delta worker token object  # 6
    queue socket branch cache branch header blob delta rebase remote  # 7
    parser object blob token tree tree  # 8
    queue tree handle socket  # 9
    index queue commit merge worker remote handle ref queue  # 10
    stream ref index queue  # 11
    queue pack buffer queue worker remote buffer token blob  # 12
    tree queue branch rebase buffer buffer  # 13
    merge tree index parser handle parser remote pack blob  # 14
    cache ref token pack cache  # 15
    blob queue tree queue worker ref token object merge branch rebase blob  # 16
    tree commit socket commit buffer rebase ref  # 17
    pack token branch window parser merge stream blob index branch  # 18
    cache handle handle tree delta socket parser header pack  # 19
    cache header pack pack worker blob cache stream tree rebase window  # 20
    pack token stream worker remote buffer blob worker parser  # 21
    stream worker rebase object commit  # 22
    window object handle branch parser header merge remote commit  # 23
    remote cache cache window stream remote commit branch worker remote  # 24
    header branch tree header pack token blob queue  # 25
    blob handle header branch token handle parser queue  # 26
    commit window merge pack merge commit handle  # 27
    queue buffer index parser queue  # 28
    remote worker remote handle  # 29
    header delta ref queue branch branch  # 30
    socket header object blob header remote socket queue parser tree  # 31
    branch pack delta window queue header queue worker merge handle  # 32
    pack tree handle cache cache cache delta header branch  # 33
    pack remote pack rebase  # 34
    commit merge window window stream blob window remote rebase  # 35
    delta object buffer pack buffer commit index cache stream  # 36
    parser merge token object window index merge queue ref branch pack  # 37
    cache stream merge branch rebase stream rebase header worker token queue  # 38
    buffer tree tree queue parser token parser  # 39
    branch token merge index header parser  # 40
    worker socket token worker window header commit worker  # 41
    parser tree window index parser buffer worker index  # 42
    window commit parser tree ref commit window object  # 43
    worker index token pack buffer window window object blob blob merge  # 44
    pack commit parser commit window handle worker pack handle remote commit  # 45
    queue handle pack tree delta pack header stream window rebase  # 46
    stream index cache merge rebase parser socket  # 47
    merge commit handle token stream blob token remote header worker commit  # 48
    buffer cache buffer index commit rebase socket index  # 49
    blob remote rebase delta window rebase worker  # 50
    stream remote window index object stream  # 51
    parser merge window stream buffer worker pack buffer handle ref cache handle  # 52
    cache buffer socket index queue  # 53
    handle window remote index parser rebase rebase  # 54
    token index window token window socket index delta cache rebase  # 55
    rebase tree socket remote  # 56
    object stream pack delta parser commit socket window branch socket  # 57
    merge stream buffer delta  # 58
    pack pack merge remote socket window tree  # 59
    worker branch handle remote commit parser object token handle  # 60
    remote worker blob branch buffer rebase object stream socket  # 61
    parser ref handle buffer token commit  # 62
    commit branch window buffer token cache index  # 63
    merge window buffer remote delta blob  # 64
    pack stream index worker token ref object blob  # 65
    object header parser handle remote commit remote commit queue  # 66
    index stream delta delta delta window token socket  # 67
    tree index branch index tree header parser delta pack handle  # 68
    stream remote cache queue window cache  # 69
    token pack branch rebase remote tree worker index remote object  # 70
    ref ref window worker object commit window commit delta object  # 71
    delta cache commit ref buffer header queue stream ref  # 72
    header delta delta queue delta worker  # 73
    queue blob branch tree commit stream queue  # 74
    parser window handle remote cache tree parser queue  # 75
    merge ref rebase blob header queue worker  # 76
    window stream socket index ref  # 77
    pack object stream socket blob  # 78
    worker token queue window token  # 79
