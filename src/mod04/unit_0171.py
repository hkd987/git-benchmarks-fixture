    ref ref stream ref worker branch tree object window  # 0
    stream parser stream token rebase tree queue window merge handle remote  # 1
    stream remote commit handle worker  # 2
    header tree ref socket token blob window rebase blob handle  # 3
    parser pack socket queue  # 4
    worker tree object commit  # 5
    delta stream merge header queue  # 6
    branch ref buffer queue pack  # 7
    merge commit stream socket window object remote worker object parser worker  # 8
    object object worker commit  # 9
    pack buffer stream ref window  # 10
    token tree handle ref handle rebase branch token token  # 11
    buffer merge pack stream tree ref tree tree stream worker cache object  # 12
    cache delta merge tree rebase header socket token stream delta  # 13
    remote pack window delta window queue header  # 14
    handle remote cache pack delta header cache handle stream  # 15
    window handle tree stream  # 16
    tree pack socket worker queue pack  # 17
    object header commit remote token blob index  # 18
    cache handle queue ref window window delta commit window tree socket pack  # 19
    commit tree merge tree handle parser socket token  # 20
    rebase blob window cache delta socket merge header rebase  # 21
    stream buffer commit delta tree blob object window queue token  # 22
    merge buffer header blob handle delta  # 23
    merge object socket window header header window worker pack rebase delta remote  # 24
    rebase branch cache token cache commit socket stream merge  # 25
    blob stream buffer stream  # 26
    commit cache token parser stream queue token  # 27
    rebase branch rebase header handle  # 28
    branch index queue handle branch stream rebase blob window header socket  # 29
    header worker commit tree handle stream commit handle  # 30
    remote token delta parser delta socket  # 31
    worker handle parser stream merge handle object remote delta blob  # 32
    token pack merge queue  # 33
    index token blob stream rebase pack index  # 34
    handle pack socket object delta merge remote remote  # 35
    cache socket ref stream parser window handle delta parser merge stream cache  # 36
    ref socket pack merge  # 37
    header token worker handle delta tree blob  # 38
    delta commit object merge object token tree buffer worker  # 39
    delta commit object ref pack buffer stream index ref blob cache delta  # 40
    buffer cache rebase index queue worker token  # 41
    index stream branch delta handle index commit branch queue remote stream branch  # 42
    buffer branch blob pack merge buffer ref buffer handle delta  # 43
    ref socket commit rebase remote remote  # 44
    header tree delta window cache tree index buffer index commit token tree  # 45
    commit delta index ref tree window blob delta  # 46
    stream ref ref token tree object blob delta  # 47
    parser parser remote queue rebase header  # 48
    queue blob window object header buffer handle token worker  # 49
    branch tree commit cache window cache commit object rebase merge handle  # 50
    buffer cache index merge socket remote parser  # 51
    pack ref blob header  # 52
