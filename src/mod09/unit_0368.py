    handle cache branch tree index socket handle pack worker blob stream tree  # 0
    worker ref buffer blob rebase pack cache  # 1
    branch handle window header rebase  # 2
    handle commit commit socket socket parser stream object window parser header worker  # 3
    window pack ref socket token token cache handle  # 4
    cache parser cache parser handle socket tree index header worker queue  # 5
    stream worker buffer commit queue index buffer rebase delta socket  # 6
    branch buffer object rebase socket queue tree pack remote  # 7
    ref pack object branch branch merge delta merge pack window parser  # 8
    header commit header tree remote remote branch window rebase  # 9
    window queue ref ref queue remote parser object  # 10
    rebase worker remote tree window tree  # 11
    object merge commit worker  # 12
    token token merge window branch  # 13
    handle delta handle socket window window index  # 14
    pack index parser commit parser stream window commit header remote blob  # 15
    socket blob parser tree window delta commit  # 16
    ref header delta tree remote header commit commit  # 17
    object object buffer cache object commit merge pack object  # 18
    cache queue worker stream window index handle  # 19
    pack parser remote commit handle pack window blob blob window  # 20
    cache token queue token handle token  # 21
    header blob cache cache commit header buffer  # 22
    rebase queue branch delta pack index ref commit  # 23
    index blob stream header blob queue index  # 24
    buffer object blob handle index window branch commit commit stream stream buffer  # 25
    buffer delta pack parser ref pack window object index object buffer tree  # 26
    remote object stream tree  # 27
    object stream ref blob pack stream index cache parser commit  # 28
    header window blob parser token index  # 29
    queue buffer object queue remote pack socket pack queue branch  # 30
    tree window rebase token buffer tree  # 31
    merge buffer tree delta cache parser remote parser queue  # 32
    object parser object buffer  # 33
    commit delta remote worker ref object handle branch  # 34
    parser merge index handle rebase ref worker token  # 35
    ref socket index ref ref branch  # 36
    delta remote worker pack tree socket blob handle  # 37
    object stream remote socket merge object tree rebase tree  # 38
    commit buffer object window blob commit ref tree cache  # 39
    cache rebase object header delta stream delta window object  # 40
    queue blob worker remote pack parser worker queue index remote window merge  # 41
    ref remote remote pack rebase queue  # 42
    blob stream tree token branch handle stream branch  # 43
    merge pack commit blob ref stream parser  # 44
    worker object stream buffer  # 45
    ref rebase header header parser delta remote socket tree socket  # 46
    parser object blob window cache worker token  # 47
    ref stream blob merge branch index handle branch header handle stream handle  # 48
    merge buffer tree index buffer object remote commit delta token  # 49
    blob rebase buffer pack rebase  # 50
    parser branch worker queue  # 51
    header queue handle parser handle merge parser header window delta  # 52
    header merge parser header token  # 53
    stream ref buffer header parser handle window rebase  # 54
