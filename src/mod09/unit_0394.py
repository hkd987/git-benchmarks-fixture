    remote ref ref object parser queue ref handle  # 0
    index stream window buffer index tree queue merge stream cache ref  # 1
    queue tree tree remote token merge blob queue stream queue rebase queue  # 2
    object handle blob queue cache socket cache merge  # 3
    commit window socket handle branch  # 4
    pack object worker parser delta cache socket blob header index queue  # 5
    token index cache worker delta index tree header commit handle cache  # 6
    merge header blob token token  # 7
    pack pack window object worker branch index token header token  # 8
    object handle stream delta cache remote window blob  # 9
    handle queue parser stream worker  # 10
    cache merge tree object  # 11
    parser remote rebase token token queue  # 12
    buffer tree tree object remote stream  # 13
    delta header parser handle window stream queue index socket header window buffer  # 14
    stream queue queue delta index worker  # 15
    tree commit queue queue pack window index  # 16
    tree tree queue commit queue queue pack socket  # 17
    rebase token queue rebase tree blob header  # 18
    stream rebase parser cache blob window commit blob remote rebase object  # 19
    tree queue object queue token index token  # 20
    delta blob header merge tree cache token cache tree ref worker  # 21
    object handle tree cache  # 22
    buffer branch buffer object buffer window token  # 23
    queue worker cache blob  # 24
    worker branch index delta cache ref blob socket header queue ref commit  # 25
    cache blob parser index remote header buffer token  # 26
    index delta socket merge cache remote  # 27
    ref stream pack cache branch object queue buffer  # 28
    commit token ref token header token token  # 29
    merge token handle rebase worker blob worker queue window tree remote  # 30
    socket window parser parser cache window ref window  # 31
    branch object commit ref index header  # 32
    queue pack tree parser  # 33
    tree blob object rebase token branch worker parser cache tree cache  # 34
    rebase token header worker rebase delta object branch tree  # 35
    index delta token index socket parser tree rebase delta cache  # 36
    remote delta tree stream window remote blob queue socket token  # 37
    stream branch buffer blob branch socket delta parser ref  # 38
    remote socket ref delta  # 39
    ref parser socket token cache remote pack blob  # 40
    window branch tree ref header parser tree  # 41
    window header worker token  # 42
    blob rebase object tree branch pack delta object  # 43
    ref stream ref socket stream queue  # 44
    window queue worker pack cache window parser ref branch  # 45
    remote merge handle blob handle delta parser ref merge rebase rebase handle  # 46
    remote object blob index merge commit blob commit remote  # 47
    queue handle stream blob  # 48
    tree stream branch commit pack tree parser blob  # 49
    cache commit branch branch  # 50
    blob blob window parser pack tree  # 51
    blob delta merge parser branch stream remote merge buffer worker worker  # 52
    remote cache blob header worker rebase delta ref  # 53
    commit stream index ref delta cache buffer handle worker index handle worker  # 54
    ref buffer token parser ref handle  # 55
    commit rebase socket ref tree pack commit queue  # 56
    tree header rebase remote rebase tree remote merge branch tree  # 57
    token pack header blob commit header blob buffer  # 58
    object handle token buffer  # 59
    delta delta merge tree buffer window worker window blob header rebase window  # 60
    blob queue window cache handle commit worker cache window  # 61
    queue tree worker delta ref parser merge blob blob blob delta blob  # 62
    delta object object parser handle blob index token token pack  # 63
    cache rebase rebase blob commit commit ref queue index index ref cache  # 64
    cache ref handle stream socket merge ref rebase cache merge branch object  # 65
    merge worker ref merge delta  # 66
    commit commit object queue merge token cache socket object  # 67
    cache header token handle handle worker handle rebase socket remote  # 68
    commit socket blob index worker header blob parser rebase tree worker  # 69
    worker blob index index window merge stream pack worker header header  # 70
    queue index handle header handle handle  # 71
    blob tree queue rebase branch delta delta remote pack tree  # 72
    stream buffer delta header handle header  # 73
    pack token commit object queue handle delta stream commit stream window buffer  # 74
    pack delta parser stream  # 75
    handle cache cache buffer rebase stream socket branch worker worker  # 76
    socket ref token socket tree parser window tree  # 77
