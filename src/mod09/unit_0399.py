    header delta token header tree tree cache cache queue object cache  # 0
    pack queue merge buffer merge window tree header blob  # 1
    ref socket parser blob  # 2
    remote ref token handle window token  # 3
    cache merge ref parser blob pack object handle tree tree  # 4
    header tree token delta  # 5
    index pack blob handle blob branch window blob handle commit commit commit  # 6
    stream token worker delta token  # 7
    index index handle branch cache  # 8
    object branch ref worker index commit delta buffer  # 9
    handle stream merge header blob  # 10
    index worker window parser commit  # 11
    rebase socket index delta branch rebase delta worker stream handle rebase  # 12
    pack buffer stream cache buffer ref rebase merge token delta blob pack  # 13
    blob window remote handle cache delta handle window tree commit  # 14
    parser branch blob object handle remote buffer ref handle stream handle remote  # 15
    tree handle delta handle delta branch index socket rebase buffer remote queue  # 16
    worker worker stream remote buffer window index parser tree worker merge blob  # 17
    socket object pack index queue merge delta index commit object  # 18
    remote handle branch object worker delta pack ref object  # 19
    branch cache delta cache object handle ref socket socket header rebase parser  # 20
    delta branch ref rebase worker blob parser window worker merge token handle  # 21
    handle ref tree blob remote  # 22
    tree worker delta socket commit socket window parser pack parser  # 23
    window rebase token cache merge blob worker index remote header stream  # 24
    merge index token buffer tree  # 25
    header token rebase delta merge queue remote queue ref  # 26
    index blob pack socket blob tree remote branch merge  # 27
    rebase commit index parser buffer pack  # 28
    rebase index stream blob object token  # 29
    header worker object stream buffer token branch  # 30
    queue delta remote socket worker ref header handle merge pack  # 31
    parser merge socket index ref queue merge  # 32
    socket stream socket header worker branch window parser ref  # 33
    cache header pack parser ref window  # 0
    tree worker blob stream parser remote  # 1
    parser merge window header tree remote stream window parser token  # 2
    blob merge merge pack blob blob ref buffer ref rebase blob remote  # 3
