    pack window buffer pack commit header handle parser  # 0
    ref blob queue buffer token remote  # 1
    worker object object worker merge  # 2
    buffer merge rebase parser buffer merge branch index ref ref remote index  # 3
    delta index delta token delta worker buffer  # 4
    header delta rebase header remote  # 5
    commit ref window cache pack window  # 6
    stream merge ref worker object branch object  # 7
    commit pack handle branch header header pack  # 8
    blob tree queue handle merge cache  # 9
    window header delta branch index  # 10
    delta pack stream delta remote cache stream cache branch branch queue commit  # 11
    branch branch cache rebase delta commit  # 12
    header cache worker cache ref pack cache socket  # 13
    branch merge delta pack pack buffer ref header parser index branch  # 14
    tree handle window object remote stream rebase window  # 15
    cache header pack delta ref tree socket ref  # 16
    index queue commit commit blob merge stream tree index object branch worker  # 17
    blob parser token pack stream delta  # 18
    parser socket parser stream merge token parser rebase  # 19
    header socket object rebase branch blob commit parser cache window  # 20
    queue blob buffer remote buffer cache header  # 21
    object token tree pack buffer parser  # 22
    branch remote rebase merge window stream window index stream commit  # 23
    buffer remote object branch pack ref delta delta window worker parser tree  # 24
    token parser stream queue ref buffer  # 25
    tree blob token index buffer header header  # 26
    pack window token queue delta socket delta window queue  # 27
    token cache merge ref header pack socket  # 28
    buffer queue tree header window  # 29
    delta socket token header window ref socket tree socket ref  # 30
    ref pack window ref delta ref window index rebase  # 31
    window stream ref token merge worker window branch header stream merge  # 32
    socket merge handle worker ref branch merge delta token token  # 33
    cache blob tree object window delta handle queue stream window  # 34
    delta stream tree rebase delta socket window token  # 35
    window pack branch delta parser  # 36
    header header stream object merge commit parser parser  # 37
    socket socket index remote cache header blob  # 38
    merge remote rebase remote  # 39
    pack commit delta stream blob delta  # 40
    delta index cache remote handle header merge ref branch index  # 41
    worker buffer header socket object delta cache socket stream queue blob window  # 42
    buffer socket token parser  # 43
    token queue pack index merge object tree queue delta pack blob queue  # 44
    ref blob token tree queue buffer  # 45
    socket buffer token handle blob cache  # 46
    remote commit worker ref cache ref stream queue socket queue blob socket  # 47
    merge remote blob queue queue cache delta remote tree merge delta window  # 48
    parser merge header stream blob ref object queue queue buffer worker delta  # 49
    commit socket token parser stream  # 50
    token cache merge window  # 51
    buffer index merge blob merge remote tree delta object  # 52
    parser commit pack ref queue object  # 53
    branch queue cache stream delta token pack queue branch window delta  # 54
    pack parser delta branch  # 55
    delta window queue window  # 56
    pack delta cache stream buffer worker window delta  # 57
    handle queue parser parser delta handle  # 58
    buffer commit parser cache tree index buffer queue buffer object rebase queue  # 59
    ref commit token ref index object index merge token blob remote  # 60
    ref queue parser window remote  # 61
    handle remote buffer rebase ref worker header delta merge handle worker  # 62
    handle window rebase window token  # 63
    worker worker rebase worker handle remote blob parser socket  # 64
    ref token merge remote handle  # 65
    token pack object delta header parser stream ref  # 66
    worker token pack branch queue queue delta ref tree window handle cache  # 67
    index rebase queue remote blob  # 68
    header rebase token delta buffer rebase  # 69
    header header parser cache object stream branch worker buffer remote  # 70
    merge cache worker parser handle header worker cache token  # 71
    socket worker object parser window tree tree stream  # 72
    stream window remote delta commit  # 73
    remote delta blob merge index header window header delta socket buffer  # 0
    parser parser buffer stream socket stream index  # 1
    merge object handle index  # 2
    blob pack window blob rebase branch pack rebase parser buffer branch tree  # 3
    blob cache commit header queue parser merge cache header parser window worker  # 4
    index merge parser worker index  # 5
    queue handle socket stream ref  # 6
    stream stream parser buffer handle  # 7
    window ref remote branch handle branch tree commit merge  # 8
    queue commit object blob  # 0
    window object merge branch header delta remote  # 1
    pack buffer token object ref remote queue worker  # 2
    cache handle cache branch merge object merge token token index  # 3
    blob window queue parser ref parser socket  # 4
    index token buffer merge buffer index handle  # 5
    buffer worker worker cache socket token worker  # 6
    branch object index queue blob worker header delta delta merge window  # 7
    blob ref blob commit stream rebase branch token blob header  # 8
    delta rebase window handle object object handle header parser rebase  # 9
    queue socket handle handle tree queue worker queue commit  # 0
    buffer tree queue handle cache  # 1
    parser merge cache branch commit ref delta merge queue merge parser window  # 2
    parser delta blob index ref token remote pack handle  # 3
