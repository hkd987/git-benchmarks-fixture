    buffer ref header worker commit delta merge socket queue rebase token  # 0
    remote parser tree commit index ref ref queue header header header ref  # 1
    socket parser ref window buffer tree window  # 2
    buffer ref worker cache merge worker parser window commit header  # 3
    token rebase queue socket window rebase token commit buffer object handle cache  # 4
    parser buffer socket remote branch handle remote  # 5
    delta cache remote index  # 6
    commit rebase blob parser remote  # 7
    stream merge pack parser queue pack remote object worker blob worker rebase  # 8
    remote branch index remote  # 9
    queue ref socket remote tree handle branch blob parser rebase  # 10
    delta stream worker header buffer pack remote  # 11
    token ref queue delta buffer token worker socket rebase ref  # 12
    remote remote merge parser parser cache queue  # 13
    cache object socket object  # 14
    rebase rebase object pack delta merge buffer window index parser  # 15
    ref ref delta stream index rebase queue delta window index remote  # 16
    blob blob remote socket pack token window socket socket pack  # 17
    branch merge ref window handle token delta socket rebase  # 18
    queue index rebase delta parser delta handle worker rebase worker  # 19
    blob commit tree handle queue merge cache commit window rebase socket stream  # 20
    blob window socket branch remote socket blob branch branch buffer merge  # 21
    cache buffer rebase index window branch  # 22
    object commit tree cache  # 23
    handle cache index parser stream blob header pack object  # 24
    index object object merge delta worker  # 25
    commit buffer commit stream merge socket pack index parser rebase worker  # 26
    cache stream buffer buffer branch window ref stream buffer queue rebase  # 27
    rebase token branch rebase cache index stream merge ref index  # 28
    cache object blob cache stream window tree pack parser  # 29
    window window merge merge parser object  # 30
    blob merge parser object index  # 31
    cache remote branch ref parser token buffer parser stream header  # 32
    header queue buffer handle  # 33
    branch queue branch commit remote header header commit tree  # 34
    blob tree branch branch stream object commit index stream tree cache  # 35
    index pack tree blob token index token delta index window cache cache  # 36
    header merge blob commit cache rebase  # 37
    stream blob token object window pack delta remote rebase delta parser stream  # 38
    worker socket commit delta tree  # 39
    socket parser stream parser socket parser pack handle parser branch buffer pack  # 40
    buffer cache branch window buffer worker stream branch blob cache  # 41
