    delta delta window worker tree rebase branch token  # 0
    buffer worker tree cache  # 1
    object commit socket header ref stream merge delta window blob  # 2
    rebase branch object merge queue  # 3
    delta tree window remote ref  # 4
    header stream rebase object commit tree header tree worker tree cache  # 5
    object pack rebase parser pack stream token parser commit token  # 6
    queue socket commit cache buffer merge  # 7
    rebase token queue worker stream window  # 8
    cache window queue blob remote window delta tree merge merge stream  # 9
    window socket cache delta tree  # 10
    merge branch ref delta buffer token  # 11
    index worker pack cache delta object  # 12
    delta ref tree tree handle merge window token token  # 13
    queue cache merge blob parser tree  # 14
    buffer window handle tree worker  # 15
    queue worker index header ref  # 16
    tree worker rebase buffer handle window header delta tree branch  # 17
    buffer merge header index header window  # 18
    handle stream branch tree tree buffer buffer worker commit cache parser stream  # 19
    pack commit branch delta branch remote commit queue cache stream  # 20
    commit token buffer pack ref pack parser branch ref commit  # 21
    commit index window index object blob remote ref object window handle  # 22
    parser handle tree merge object object ref pack  # 23
    queue parser object rebase pack socket pack ref queue buffer buffer pack  # 24
    merge parser stream pack  # 25
    blob merge parser object rebase pack parser  # 26
    queue merge token merge handle  # 27
    branch token header buffer rebase stream token socket stream blob delta  # 28
    object socket rebase queue merge branch worker worker rebase pack delta  # 29
    delta stream delta delta blob worker buffer  # 30
    parser object worker commit socket index branch socket  # 31
    index blob commit branch worker commit merge buffer ref  # 32
    worker ref parser socket  # 33
    queue worker header header object merge delta queue pack  # 34
    ref stream remote blob delta handle  # 35
    blob window queue branch cache  # 36
    buffer object tree delta  # 37
    buffer blob remote delta ref branch socket  # 38
    token worker object merge header  # 39
    socket buffer cache tree queue handle window  # 40
    tree pack index window pack cache commit blob handle remote worker socket  # 41
    merge cache pack buffer commit delta queue socket object worker  # 42
    index remote token tree object  # 43
    commit remote cache index cache blob blob worker ref  # 44
    branch remote commit ref  # 45
    handle pack remote parser  # 46
    queue header ref buffer buffer pack  # 47
    commit queue buffer parser remote queue pack socket stream token  # 48
    ref socket worker socket worker  # 49
    parser remote cache rebase  # 50
    merge window token stream tree merge rebase window delta pack  # 51
    ref window rebase branch  # 52
    cache stream ref object buffer queue delta window branch commit  # 53
    window merge branch cache remote ref window ref commit  # 54
    blob stream index remote handle socket  # 55
    window commit buffer token handle tree  # 56
    delta handle cache branch tree worker pack ref header pack index queue  # 57
    cache pack handle cache pack queue pack window index token  # 58
    header rebase object rebase queue socket blob  # 59
