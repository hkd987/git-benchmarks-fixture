    window buffer socket object  # 0
    parser commit header token handle object header blob socket cache  # 1
    parser buffer blob token socket window  # 2
    queue buffer window tree header pack cache buffer merge index index cache  # 3
    cache commit parser tree merge index blob stream socket commit  # 4
    stream stream rebase queue socket pack pack token socket  # 5
    handle worker rebase buffer index index buffer socket  # 6
    merge window buffer remote pack pack commit window worker  # 7
    object token branch window tree merge commit  # 8
    window parser object parser index cache  # 9
    pack parser remote header header buffer token tree branch  # 10
    branch header socket branch worker worker socket  # 11
    blob object blob remote handle token  # 12
    socket worker rebase merge merge index cache token ref commit header branch  # 13
    buffer handle buffer socket merge remote commit merge merge worker tree object  # 14
    merge tree worker rebase socket header delta buffer  # 15
    header parser stream cache  # 16
    ref tree merge delta commit tree remote delta handle worker rebase blob  # 17
    commit window pack branch worker  # 18
    blob branch commit parser  # 19
    remote window stream buffer socket ref tree blob  # 20
    delta branch rebase cache branch parser ref  # 21
    worker token socket socket  # 22
    worker stream parser cache cache merge  # 23
    branch pack merge index parser parser worker worker tree ref handle  # 24
    remote handle pack queue pack  # 25
    window parser window delta tree stream merge rebase socket socket stream  # 26
    header index commit parser buffer worker  # 27
    socket worker handle delta merge cache delta blob remote delta  # 28
    worker cache socket branch blob handle merge index stream object  # 29
    cache window blob commit window window delta commit pack tree merge  # 30
    parser object worker tree delta parser token ref tree  # 31
    parser delta parser worker token socket  # 32
    blob worker branch worker queue  # 33
    stream buffer object queue rebase token delta commit  # 34
    token rebase buffer pack index blob handle socket delta  # 35
    remote socket tree rebase  # 36
    buffer pack delta worker pack parser  # 37
    blob buffer delta stream ref ref buffer tree remote object socket object  # 38
    commit queue header socket handle merge merge index  # 39
    delta rebase handle ref pack  # 40
    socket tree window buffer  # 41
    pack tree handle stream commit  # 42
    handle commit branch ref commit delta  # 43
    handle tree index token stream cache merge parser  # 44
    ref blob index blob object token  # 45
    index window header handle merge remote delta cache delta  # 46
    delta worker cache token merge token stream index remote worker  # 47
    branch object token blob header branch parser handle rebase header worker  # 48
    index token parser delta buffer window  # 49
    buffer cache blob rebase window queue buffer merge token  # 50
    queue socket buffer cache  # 51
    socket buffer window tree token rebase stream handle  # 52
    branch socket object queue merge stream handle  # 53
    handle parser pack commit buffer merge stream  # 54
    token rebase window merge header commit buffer header worker window  # 55
    merge commit socket token delta commit parser cache rebase blob delta  # 56
    parser rebase index parser branch remote merge stream handle  # 57
    commit pack token socket index tree header branch stream cache cache  # 58
    rebase branch window parser rebase cache socket stream remote  # 59
    object token delta buffer parser rebase blob object ref token  # 60
    tree rebase header header  # 61
    queue handle tree object branch index window object  # 62
    header ref parser window object remote  # 63
    cache window ref tree  # 64
