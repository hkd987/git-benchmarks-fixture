    worker object header branch buffer pack tree remote delta  # 0
    stream rebase blob rebase object worker  # 1
    tree object parser delta header  # 2
    blob rebase index parser remote worker token worker  # 3
    stream branch merge cache tree tree object rebase  # 4
    blob merge ref socket ref branch worker blob queue commit  # 5
    delta branch merge ref rebase rebase token rebase merge  # 6
    index index parser pack  # 7
    queue socket branch window merge queue stream  # 8
    socket delta index object  # 9
    buffer branch delta parser tree parser remote socket blob  # 10
    merge index tree rebase object object branch tree queue  # 11
    window rebase object branch tree socket window  # 12
    cache token tree token socket tree queue stream ref  # 13
    pack queue blob socket remote delta object  # 14
    header stream branch cache buffer pack object blob stream  # 15
    blob object queue pack  # 16
    rebase rebase parser queue cache ref branch window commit  # 17
    pack ref commit blob stream ref stream token cache tree ref  # 18
    token worker ref socket  # 19
    window blob queue parser pack window merge delta commit parser blob parser  # 20
    stream ref remote ref tree merge ref  # 21
    ref remote merge pack worker parser  # 22
    blob pack branch merge merge token queue worker window delta  # 23
    pack socket tree cache cache header commit commit pack tree branch object  # 24
    buffer blob buffer delta socket merge window pack index  # 25
    cache remote merge pack rebase window token cache  # 26
    branch rebase tree window  # 27
    tree parser branch window header branch blob  # 28
    queue window rebase object object token merge  # 29
    queue merge commit pack parser merge ref socket cache  # 30
    merge commit index worker rebase token socket token buffer  # 31
    commit ref queue delta tree worker commit header  # 32
    handle tree pack delta index token blob header parser  # 33
    worker blob queue blob branch  # 34
    branch pack branch commit  # 35
    parser cache stream pack tree ref  # 36
    commit cache index remote  # 37
    token window ref merge object queue rebase blob socket  # 38
    header token index index object  # 39
    header merge handle object delta branch socket  # 40
    rebase commit queue rebase worker buffer handle token window commit rebase  # 41
    cache pack buffer index index window buffer  # 42
    remote token tree header remote queue handle object pack  # 43
    header cache object queue  # 44
    buffer queue ref cache worker  # 45
    header tree commit index cache handle buffer buffer blob  # 46
    socket cache branch header commit commit queue index window  # 47
    pack parser pack header index merge blob object  # 48
    queue buffer branch blob remote rebase header delta blob pack stream ref  # 49
    header worker merge pack token token token delta delta window  # 50
    commit tree object delta parser cache tree header pack branch  # 51
    branch tree stream cache token object queue header tree token  # 52
    tree cache rebase handle  # 53
    parser ref header socket worker handle buffer object queue window tree  # 54
    window pack remote remote stream stream branch rebase token commit merge worker  # 55
    delta buffer merge ref token buffer queue header blob object stream  # 56
    pack object ref worker delta index branch header token buffer ref  # 57
    merge index blob worker object rebase header parser object header token queue  # 58
    remote token remote stream tree socket commit ref  # 59
    commit buffer parser header merge tree token commit handle merge parser  # 60
    worker blob window socket object merge stream commit parser remote branch stream  # 61
    queue index object parser pack tree commit window index buffer worker queue  # 62
    pack commit window queue remote remote  # 63
    remote handle object branch  # 0
    delta commit commit queue pack parser socket delta merge pack delta  # 1
    token tree rebase blob window  # 2
    merge object branch index commit delta object  # 0
    socket pack window token stream queue header  # 1
    index tree window parser commit worker index token tree worker  # 2
    parser tree parser remote window  # 3
    socket branch parser index blob  # 4
    rebase index handle object queue tree handle ref object ref queue  # 5
    stream stream socket tree queue parser blob handle worker worker  # 6
    remote parser worker rebase token remote socket pack handle socket delta  # 7
    ref object index index object worker window header handle tree stream  # 8
