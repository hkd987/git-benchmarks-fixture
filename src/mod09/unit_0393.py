    delta ref buffer index commit parser tree  # 0
    parser pack object tree stream commit parser object  # 1
    cache commit ref buffer rebase stream socket  # 2
    token rebase remote remote ref  # 3
    index rebase worker rebase worker  # 4
    socket tree tree worker socket window cache window header token  # 5
    index worker ref pack handle header window tree worker handle object worker  # 6
    stream pack window stream worker tree token header tree pack ref  # 7
    header index socket worker token object blob index pack cache blob merge  # 8
    stream buffer worker socket commit  # 9
    object parser index remote tree object queue worker socket merge  # 10
    index pack remote merge stream cache index buffer delta ref delta  # 11
    socket branch socket ref window tree  # 12
    header blob worker queue blob  # 13
    handle object merge index  # 14
    token blob parser queue  # 15
    remote object blob remote handle branch stream worker cache merge  # 16
    parser branch window merge remote cache header blob stream worker  # 17
    buffer tree merge commit tree blob delta merge index handle socket cache  # 18
    buffer merge socket pack  # 19
    window object header header buffer queue pack queue rebase handle  # 20
    stream handle commit rebase branch blob worker tree worker socket tree  # 21
    buffer commit header token index remote parser stream  # 22
    tree token worker branch  # 23
    worker blob socket index blob  # 24
    branch socket rebase index handle socket merge buffer socket commit  # 25
    remote remote blob pack pack cache buffer blob rebase parser buffer  # 26
    commit queue parser merge tree queue handle  # 27
    branch header queue object cache  # 28
    branch branch commit pack queue merge header pack queue blob parser header  # 29
    pack window ref socket header delta handle  # 30
    ref cache index delta delta remote stream  # 31
    handle merge index merge remote merge  # 32
    tree remote header blob pack worker branch rebase header commit commit  # 33
    cache tree remote queue merge remote  # 34
    header handle socket delta buffer window header branch window tree header branch  # 35
    socket rebase rebase parser index delta branch token delta worker  # 36
    branch cache stream delta merge tree handle  # 37
    branch ref ref buffer commit rebase cache merge socket delta  # 38
    commit stream index merge  # 39
    object index object handle  # 40
    queue branch branch ref  # 41
    merge queue socket delta  # 42
    branch object blob header buffer commit cache commit  # 43
    token cache worker index token  # 44
    delta pack object window remote cache window branch handle queue ref tree  # 45
    branch index socket merge header remote blob cache branch parser  # 46
    cache delta header pack blob token commit  # 47
    handle handle header delta  # 48
    commit token object pack rebase parser  # 49
    handle merge handle buffer socket merge parser index  # 50
    delta commit delta pack remote stream blob token handle rebase  # 51
    delta blob queue worker parser window object ref index token ref  # 52
    cache commit cache handle delta queue ref window token  # 53
    window tree header ref buffer window remote cache merge queue worker commit  # 54
    branch index window ref merge socket ref  # 55
    pack rebase index pack socket merge  # 56
    handle commit commit parser rebase worker object window  # 57
    branch index merge token stream  # 58
    pack handle ref delta commit handle  # 59
    tree parser buffer index stream cache pack merge index  # 60
    rebase index tree tree queue commit delta commit rebase delta remote  # 61
    window remote blob branch header remote pack window delta object blob  # 62
    stream tree branch merge index window queue pack stream remote  # 63
    window queue socket blob socket rebase  # 64
    commit pack token index delta  # 65
    parser tree header delta stream parser  # 66
    commit merge commit header blob remote worker buffer pack rebase tree  # 67
    object remote token branch delta pack object queue pack parser handle  # 68
    branch pack token commit  # 69
    blob window handle commit rebase cache handle  # 70
    header blob rebase merge buffer merge cache  # 71
    commit socket merge header token  # 72
    queue token merge queue merge window  # 73
    ref commit parser tree merge cache header index  # 74
    queue tree tree ref token socket tree remote object token  # 75
    rebase pack cache object index index commit blob  # 76
    merge pack blob rebase stream remote  # 77
    worker tree window cache  # 78
