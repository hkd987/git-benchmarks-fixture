    object buffer rebase pack buffer socket stream token rebase  # 0
    rebase pack stream branch index ref queue stream tree delta token  # 1
    commit remote commit token window rebase buffer handle tree token queue queue  # 2
    worker delta socket queue queue token cache tree cache  # 3
    branch buffer token worker queue parser commit object socket remote handle  # 4
    queue socket ref remote stream header index object buffer socket handle index  # 5
    buffer worker remote ref index token branch index tree parser cache  # 6
    index delta rebase rebase blob token delta queue  # 7
    cache remote queue cache index worker delta rebase pack token pack  # 8
    rebase socket merge rebase rebase remote index tree handle stream token branch  # 9
    buffer remote branch merge merge commit parser index commit  # 10
    handle blob pack queue delta merge blob buffer  # 11
    object branch buffer branch stream index delta ref  # 12
    rebase tree pack handle branch handle token  # 13
    stream commit ref ref worker commit pack  # 14
    buffer handle token index worker object buffer  # 15
    cache tree parser worker  # 16
    queue cache socket object socket ref rebase rebase stream  # 17
    branch blob socket parser window pack cache window object object handle commit  # 18
    delta queue buffer token delta handle rebase window  # 19
    delta blob handle commit header  # 20
    rebase header tree header branch buffer remote cache object  # 21
    parser ref rebase rebase worker header merge window  # 22
    commit cache stream index  # 23
    object stream queue worker tree rebase socket  # 24
    merge header buffer tree socket branch merge ref parser object socket  # 25
    worker commit handle token index queue blob  # 26
    index pack token worker pack stream ref index stream  # 27
    merge socket header remote index  # 28
    branch token object branch queue queue worker socket ref branch  # 29
    window blob branch delta pack rebase  # 30
    queue merge ref queue  # 31
    socket tree pack remote tree merge merge  # 32
    tree parser pack cache branch remote  # 33
    tree worker rebase queue remote tree object stream handle token parser blob  # 34
    token remote blob header worker queue  # 35
    index header parser index branch header window cache pack blob  # 36
    handle handle parser commit header rebase branch tree remote tree queue  # 37
    stream parser pack header cache socket token index object index worker object  # 38
    remote remote rebase parser rebase  # 39
    socket delta socket worker index worker handle parser rebase worker delta index  # 40
    commit queue commit queue merge ref queue tree commit  # 41
    commit stream cache header commit handle remote rebase worker  # 42
    stream queue socket merge commit buffer index handle pack window worker ref  # 43
    pack remote remote remote ref header  # 44
    header merge buffer window commit object queue object rebase  # 45
    remote parser delta worker commit worker header token parser buffer socket socket  # 46
    object queue object parser worker  # 47
    blob buffer handle ref queue handle pack window branch  # 48
    commit ref buffer queue header handle buffer rebase tree tree rebase merge  # 49
    blob merge cache header merge worker object merge window object commit socket  # 50
    index parser buffer worker commit pack merge queue branch queue  # 51
    pack socket parser blob worker blob  # 52
    blob tree window tree merge cache buffer rebase  # 53
    branch buffer buffer queue  # 54
    pack worker index commit cache blob merge  # 55
    rebase pack index parser object merge header worker parser window pack queue  # 56
    merge tree buffer parser delta merge  # 57
    pack queue delta queue branch delta pack delta remote commit token  # 58
    delta header commit header cache tree object buffer rebase  # 59
    cache parser pack index merge merge queue rebase tree merge cache window  # 60
    blob commit object stream pack tree blob commit pack object handle cache  # 61
    header rebase token window queue commit socket queue token worker delta socket  # 62
    token header queue pack header socket rebase socket pack index cache header  # 63
    commit branch parser index delta header tree ref delta tree pack  # 64
    tree object buffer token tree stream stream blob remote  # 65
