    window header parser handle socket token index stream  # 0
    commit stream token object commit parser parser index  # 1
    pack branch token branch remote token remote parser handle  # 2
    cache blob blob commit tree delta worker worker parser token handle stream  # 3
    merge cache tree stream handle worker socket stream  # 4
    header remote queue socket  # 5
    rebase blob queue merge queue object  # 6
    tree worker branch socket handle index worker worker  # 7
    parser stream merge tree rebase branch rebase rebase stream worker window  # 8
    header header stream worker blob branch header delta cache cache parser token  # 9
    commit ref branch cache tree rebase socket blob delta parser delta  # 10
    buffer branch pack pack handle worker index worker object worker parser object  # 11
    tree ref merge index  # 12
    delta window ref socket remote header blob cache delta parser tree branch  # 13
    token rebase token worker parser stream header handle pack  # 14
    header header header index  # 15
    delta buffer rebase token merge stream  # 16
    delta object blob branch commit tree rebase token  # 17
    cache ref rebase branch object socket  # 18
    window ref index index blob handle  # 19
    object ref queue index cache commit delta stream token buffer header ref  # 20
    handle parser branch socket handle token branch handle cache queue cache  # 21
    window socket blob worker buffer window merge branch  # 22
    rebase delta delta handle cache header header window object  # 23
    pack ref branch delta  # 24
    handle delta buffer queue socket header cache stream tree token branch rebase  # 25
    blob rebase handle handle tree commit  # 26
    rebase parser index tree index worker object index commit cache  # 27
    remote merge object rebase queue buffer object blob queue cache  # 28
    stream handle commit buffer buffer branch cache  # 29
    token delta object window commit token cache cache  # 30
    remote stream object token rebase pack delta blob worker  # 31
    merge buffer remote rebase merge remote merge window rebase remote  # 32
    header cache window token blob queue index stream branch rebase stream commit  # 33
    ref index pack remote buffer token cache  # 34
    delta ref ref remote socket parser tree delta socket ref window handle  # 35
    index remote queue parser  # 36
    merge blob tree object token handle object window branch rebase rebase tree  # 37
    parser index commit merge  # 38
    branch token tree delta parser tree worker header  # 39
    socket object queue branch  # 40
    socket parser object rebase worker  # 41
    commit branch window merge socket  # 42
    index commit object pack parser ref blob  # 43
    token rebase blob delta remote rebase buffer remote cache delta queue rebase  # 44
    pack remote header pack window worker stream object header object  # 45
    object index index rebase cache cache pack ref buffer delta  # 46
    ref index worker branch commit delta blob pack ref window parser tree  # 47
    handle index pack header  # 48
    handle delta delta ref stream stream object delta delta  # 49
    commit remote blob token worker token  # 50
    blob queue queue remote delta socket tree stream  # 51
    remote tree commit ref blob blob remote parser tree delta cache socket  # 52
    tree merge tree remote commit worker rebase worker ref  # 53
    socket pack index blob stream branch queue buffer queue ref  # 54
    token remote window delta object buffer blob token handle  # 55
    queue queue buffer merge object delta blob tree worker  # 56
    stream socket pack index blob window merge stream rebase object  # 57
    window parser branch ref delta blob window  # 58
    ref parser handle tree object cache  # 59
    blob worker remote ref buffer remote queue handle blob object  # 60
    index handle blob buffer  # 61
    rebase header object worker token rebase ref blob stream delta delta  # 62
    blob branch remote parser queue handle token  # 63
    object stream header queue object blob index header blob handle window worker  # 64
    blob buffer stream pack cache handle branch token object  # 65
    pack remote ref remote ref  # 66
    header tree branch stream queue delta commit window branch blob header handle  # 67
    handle merge ref object socket blob parser  # 68
    header socket stream index handle merge stream window merge  # 69
    pack stream queue token window stream  # 70
    stream tree worker socket worker handle token token  # 71
    ref pack tree handle ref object header queue parser rebase queue  # 72
    tree commit blob stream  # 73
    index index branch ref stream window  # 74
    cache tree object stream header branch commit ref  # 75
    header merge rebase branch stream ref  # 76
    merge tree commit ref remote  # 77
