    ref parser queue rebase ref delta remote parser merge rebase token window  # 0
    buffer tree index window branch tree merge object socket index tree  # 1
    merge queue ref rebase  # 2
    parser handle stream window remote tree ref  # 3
    window index tree buffer handle  # 4
    branch object header window pack buffer index queue remote queue index  # 5
    queue remote delta commit queue remote rebase queue  # 6
    commit rebase ref stream cache branch object merge rebase  # 7
    cache buffer rebase remote window delta token ref queue handle  # 8
    merge tree object header handle queue  # 9
    delta parser pack queue merge handle token handle ref pack rebase  # 10
    pack rebase token ref parser rebase cache  # 11
    delta commit blob pack cache object  # 12
    object commit ref object socket  # 13
    ref object ref merge commit index token delta token ref parser header  # 14
    object index commit delta merge tree ref  # 15
    queue window rebase header delta delta commit  # 16
    pack rebase remote remote tree  # 17
    pack socket remote window remote token buffer cache  # 18
    remote cache token rebase token cache handle tree  # 19
    stream object tree pack rebase handle pack commit socket pack  # 20
    worker queue index header handle merge ref  # 21
    pack blob commit queue window parser worker queue  # 22
    pack token token merge handle cache index object worker  # 23
    window blob buffer object blob pack buffer merge cache queue ref socket  # 24
    merge rebase window socket  # 25
    index parser blob worker  # 26
    commit header socket branch index handle stream token remote remote cache  # 27
    blob worker header merge index stream tree commit buffer  # 28
    blob remote remote token parser tree  # 29
    delta commit cache buffer  # 30
    handle remote index ref branch queue commit object  # 31
    pack queue parser branch rebase remote  # 32
    merge window socket worker object index index  # 33
    branch merge delta merge commit queue  # 34
    worker rebase header queue stream remote ref buffer remote merge  # 35
    remote commit header index blob tree pack buffer blob commit  # 36
    object handle worker stream cache ref token rebase blob delta commit  # 37
    branch remote token tree window handle buffer tree queue stream delta index  # 38
    token cache ref rebase tree merge socket  # 39
    pack handle index tree socket commit buffer buffer worker  # 40
    handle parser blob object socket  # 41
    handle socket header stream  # 42
    cache ref delta merge index header branch rebase index stream  # 43
    remote worker ref window branch ref  # 44
    handle object header parser object  # 45
    header stream delta window worker socket delta handle socket delta  # 46
    buffer socket parser delta buffer delta branch tree  # 47
    ref merge handle ref window handle  # 48
    blob tree worker object pack ref  # 49
    handle merge ref cache parser window rebase cache stream header buffer  # 50
    tree token cache commit index pack index  # 51
    blob stream token window pack queue branch window ref cache delta stream  # 52
    ref tree tree handle object buffer parser cache delta buffer  # 53
    merge parser commit socket handle delta tree remote  # 54
    delta tree pack cache index buffer queue  # 55
    remote parser merge remote object merge blob header  # 56
    stream stream window merge  # 57
    cache pack window token blob parser parser handle parser index  # 58
    handle ref token header  # 59
    window queue commit merge object worker header blob stream  # 60
    buffer buffer socket queue merge ref queue branch cache pack  # 61
    ref window handle worker blob cache ref rebase window rebase stream  # 62
    branch remote header pack socket index handle tree buffer window  # 63
    object index worker delta ref  # 64
    remote handle window pack handle blob tree tree  # 65
