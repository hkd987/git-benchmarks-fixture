    handle worker worker object object branch ref  # 0
    stream branch buffer rebase worker  # 1
    socket rebase blob pack cache branch  # 2
    merge remote blob delta rebase branch stream  # 3
    tree index merge delta delta stream buffer commit  # 4
    merge token worker merge delta buffer tree socket object delta window merge  # 5
    rebase handle header handle socket index commit delta  # 6
    blob delta handle commit tree handle delta header pack window buffer  # 7
    blob blob index handle delta delta rebase  # 8
    handle ref pack queue buffer index window pack rebase merge  # 9
    delta ref handle token commit merge  # 10
    branch worker rebase branch buffer cache token parser  # 11
    window token remote pack merge rebase cache  # 12
    handle merge merge worker remote parser branch window token cache index  # 13
    commit remote socket queue token tree index buffer handle commit rebase  # 14
    buffer cache window token remote worker header pack index window rebase  # 15
    queue header commit branch index ref socket object worker  # 16
    socket commit branch worker blob queue window cache worker cache pack  # 17
    delta branch ref tree object window remote stream  # 18
    merge window buffer object worker branch branch header remote  # 19
    window header cache ref parser rebase  # 20
    commit index parser window object queue rebase branch window index buffer merge  # 21
    remote commit blob stream queue cache queue worker buffer stream handle  # 22
    buffer ref blob stream rebase handle blob token index commit  # 23
    worker window worker handle token cache parser ref parser delta  # 24
    remote parser parser socket stream  # 25
    branch window stream merge ref worker tree commit  # 26
    blob pack object buffer socket header handle token  # 27
    queue object object token tree socket pack  # 28
    ref window parser branch delta branch queue  # 29
    worker cache tree handle  # 30
    delta socket pack tree commit worker parser header  # 31
    ref queue queue index cache tree object delta remote  # 0
    rebase commit rebase branch buffer token  # 1
    worker window token remote rebase handle worker pack header ref queue blob  # 2
    commit header remote tree handle index commit ref  # 3
    queue branch socket merge socket buffer object handle  # 4
    commit blob branch parser blob window remote stream delta handle header stream  # 5
    worker cache socket worker buffer queue commit index blob  # 6
    queue worker delta merge  # 0
    pack blob window stream delta stream  # 1
    stream token queue branch token cache  # 2
    object queue header rebase buffer socket commit worker blob merge blob window  # 3
    branch queue parser parser stream window blob token  # 4
    tree rebase window buffer window rebase tree handle  # 5
