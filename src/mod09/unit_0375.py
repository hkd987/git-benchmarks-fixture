    buffer window object cache pack cache rebase index index parser handle queue  # 0
    parser merge cache commit branch  # 1
    parser merge branch stream buffer cache rebase delta  # 2
    blob branch handle index remote worker commit socket delta  # 3
    pack blob parser buffer blob  # 4
    header cache branch pack handle queue commit cache parser  # 5
    cache branch pack remote parser stream branch token socket delta ref  # 6
    socket parser commit branch token delta cache  # 7
    delta ref header tree blob token buffer pack remote commit  # 8
    queue window branch parser queue socket pack commit  # 9
    merge window ref handle rebase window socket parser branch header  # 10
    index handle worker stream merge parser merge branch ref remote buffer  # 11
    index header cache branch ref delta worker merge  # 12
    merge remote window delta queue commit buffer socket object window cache ref  # 13
    pack stream window token blob buffer branch  # 14
    delta token commit token ref socket buffer header header worker stream buffer  # 15
    window rebase object handle cache handle remote pack delta  # 16
    branch blob socket rebase merge tree socket stream  # 17
    object delta socket remote header remote token token token  # 18
    object merge queue remote socket socket handle blob stream index worker  # 19
    handle commit branch blob queue token branch socket header  # 20
    window queue socket pack ref index handle object cache socket index remote  # 21
    cache socket blob worker pack cache commit window queue header buffer token  # 22
    object ref queue stream handle handle worker merge queue handle  # 23
    worker parser index remote  # 24
    worker remote ref rebase pack token delta ref  # 25
    queue rebase buffer stream  # 26
    handle object rebase tree window  # 27
    cache queue remote pack merge merge delta  # 28
    branch tree ref parser worker buffer socket queue  # 29
    parser branch stream delta  # 30
    cache socket pack queue  # 31
    window cache worker header window object object delta delta object  # 32
    stream remote cache stream index pack blob object parser rebase  # 33
    remote rebase branch ref pack  # 34
    handle commit window tree queue header queue  # 35
    token handle queue pack queue ref cache merge rebase index tree  # 36
    cache window handle cache stream pack  # 37
    token index handle rebase ref  # 38
    object object pack header remote blob queue header parser window window  # 39
    stream parser index branch  # 40
    blob remote parser delta handle ref window  # 41
    token stream token delta branch object commit queue tree  # 42
    rebase pack worker remote delta ref window  # 43
    buffer parser stream tree merge delta header window  # 44
    object window ref blob commit object tree  # 45
    commit merge cache socket  # 46
    branch handle ref delta handle socket tree  # 47
    delta pack index stream merge parser remote cache handle cache handle  # 48
    blob merge object ref index stream remote parser tree ref queue remote  # 49
    worker remote handle branch worker commit blob merge stream queue  # 50
    blob parser rebase tree handle branch parser merge commit branch rebase  # 51
    tree header header header queue delta ref delta socket pack  # 52
    parser queue ref rebase  # 53
    worker merge header window token rebase stream  # 54
    parser cache cache queue ref commit queue buffer blob  # 55
    delta buffer object handle delta tree index delta blob window  # 56
    merge window header socket stream stream delta  # 57
    cache ref tree queue merge pack buffer  # 58
    blob window buffer delta socket object delta  # 59
    delta buffer parser commit tree queue  # 60
    tree queue pack header cache cache commit  # 61
    queue parser stream window queue branch merge queue blob cache delta token  # 62
    remote object ref token header commit socket window ref queue rebase index  # 63
    queue token pack tree blob token blob  # 64
    queue buffer header tree blob merge tree merge pack branch window  # 65
    merge remote merge token queue merge  # 66
    cache tree parser token remote delta commit header object queue buffer stream  # 67
    tree header handle header cache header  # 68
    header rebase buffer socket header stream merge ref branch socket  # 69
    socket blob blob pack header stream  # 70
    index queue buffer blob  # 71
    header queue parser merge  # 72
    object worker remote cache worker rebase socket cache worker merge worker  # 73
    object token tree merge buffer handle parser  # 74
