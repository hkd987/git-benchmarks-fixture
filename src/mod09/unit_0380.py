    buffer blob parser branch parser header remote parser  # 0
    remote socket merge buffer index cache tree queue rebase blob delta  # 1
    index branch handle rebase buffer tree header queue  # 2
    branch parser index remote branch cache parser token socket index token parser  # 3
    object commit socket blob pack worker  # 4
    parser ref branch parser merge cache tree pack worker branch index rebase  # 5
    delta remote header cache branch pack header commit header  # 6
    parser window rebase pack rebase header header tree parser buffer window  # 7
    cache remote pack token index index branch index tree merge worker rebase  # 8
    merge index stream pack object commit commit window  # 9
    socket window window buffer queue blob tree delta worker pack parser  # 10
    window parser buffer ref  # 11
    window stream commit cache blob socket  # 12
    ref tree handle handle rebase cache worker parser token parser ref  # 13
    rebase branch buffer handle remote  # 14
    handle socket header socket header  # 15
    branch remote ref object blob blob handle  # 16
    rebase socket worker merge branch  # 17
    merge parser index parser header  # 18
    handle object merge object  # 19
    buffer handle pack queue buffer blob  # 20
    branch header remote queue token buffer object cache cache branch window  # 21
    merge worker handle handle token socket branch socket socket  # 22
    index token blob token object token cache  # 23
    remote object branch queue object ref ref remote cache blob branch  # 24
    queue token blob delta commit remote header header parser  # 25
    window token buffer blob  # 26
    token index header delta window window window branch worker token token remote  # 27
    commit stream remote branch queue commit header  # 28
    window merge remote delta delta stream object merge tree branch  # 29
    blob tree tree rebase queue  # 30
    stream object header socket worker token delta  # 31
    parser queue socket parser stream  # 32
    remote handle object stream merge  # 33
    socket handle branch pack  # 34
    stream branch socket header tree token queue delta cache  # 35
    commit delta merge tree pack remote parser  # 36
    worker index socket tree token queue header window  # 37
    object window cache ref index socket commit  # 38
    delta handle token commit ref branch remote remote tree branch rebase  # 39
    parser object blob index commit object delta blob buffer remote  # 40
    header index ref window object object window commit header socket index  # 41
    ref parser worker window branch object socket cache stream index  # 42
    rebase stream socket rebase queue  # 43
    ref blob branch object object rebase blob header stream rebase blob token  # 44
    commit stream cache rebase index token tree  # 45
    window handle token buffer worker token  # 46
    cache parser remote token commit object  # 47
    ref socket blob pack object index  # 48
    ref rebase rebase pack branch remote token handle window branch  # 49
    object token tree remote rebase commit token pack cache worker cache blob  # 50
    merge tree pack ref buffer commit queue  # 51
    branch socket cache merge pack blob socket socket object delta handle  # 52
    tree ref window pack window rebase socket buffer rebase parser object queue  # 53
    tree index rebase commit handle window handle index queue commit rebase cache  # 54
    object rebase socket rebase header remote header  # 55
    socket cache commit socket token  # 56
    token merge buffer header window buffer stream parser cache remote commit  # 57
    branch merge window branch cache parser header rebase  # 58
    socket rebase token rebase index handle parser rebase branch  # 59
    rebase buffer rebase branch rebase commit  # 60
    worker cache token blob merge index token worker  # 61
    worker cache object token pack handle rebase parser queue commit handle merge  # 62
    socket index socket delta cache  # 63
    queue header commit delta stream token cache worker rebase  # 64
    socket commit index pack cache ref merge worker handle worker  # 65
    branch index cache token handle socket parser cache token token  # 66
    stream branch queue index delta ref merge stream  # 67
    stream stream queue delta delta  # 68
    branch delta header socket commit stream blob  # 69
    header delta merge stream blob header stream  # 70
    buffer parser header stream commit blob rebase index socket delta  # 71
    worker socket window stream handle worker index ref handle remote window  # 72
    cache remote tree commit delta rebase object commit index stream ref  # 73
    ref handle header branch parser stream delta queue socket window  # 74
    window header tree branch blob commit commit socket rebase  # 75
    pack rebase header object branch ref cache token window stream blob  # 76
    handle parser cache blob branch cache index ref  # 77
    remote delta ref tree delta  # 78
    parser tree socket ref branch index  # 79
    delta branch buffer object delta delta worker cache parser commit queue  # 80
    rebase window object commit object commit index tree merge blob remote  # 81
    pack header remote ref  # 82
