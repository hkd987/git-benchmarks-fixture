    buffer index ref blob cache object  # 0
    index parser delta rebase ref  # 1
    window rebase header merge stream token handle parser delta  # 2
    ref buffer remote queue queue  # 3
    header window ref object cache handle token queue index stream  # 4
    queue blob commit pack blob pack remote branch parser handle token  # 5
    rebase handle parser header worker  # 6
    handle blob header token  # 7
    ref rebase merge window cache branch  # 8
    tree commit index object queue  # 9
    header header token tree delta blob  # 10
    index worker rebase tree cache worker branch remote cache branch queue handle  # 11
    index ref token header delta window  # 12
    parser ref stream merge object rebase queue  # 13
    delta pack remote socket queue  # 14
    handle stream stream handle rebase handle pack delta worker handle handle window  # 15
    commit ref merge ref index  # 16
    handle socket remote stream window socket object tree pack stream  # 17
    delta remote queue delta pack stream remote remote index tree cache  # 18
    blob handle pack branch window rebase  # 19
    merge window buffer header socket  # 20
    header merge index index worker commit rebase stream queue header stream rebase  # 21
    stream buffer worker delta ref  # 22
    pack token worker token cache parser object tree object header stream stream  # 23
    commit object delta merge  # 24
    delta rebase remote socket worker object  # 25
    tree index branch index rebase ref merge  # 26
    token object index branch buffer index object blob ref  # 27
    queue socket index socket stream queue tree header branch delta rebase parser  # 28
    commit worker header branch socket remote merge cache parser tree  # 29
    parser handle cache pack rebase rebase object socket token pack token  # 30
    delta ref stream buffer stream pack delta token branch handle  # 31
    index parser cache object index  # 32
    ref parser socket rebase window merge token worker window queue ref rebase  # 33
    header delta delta remote header rebase  # 34
    parser commit handle blob ref parser  # 35
    tree queue stream worker buffer merge  # 36
    object commit tree parser socket  # 37
    blob handle ref parser commit tree blob  # 38
    header buffer index pack blob blob delta queue merge ref index  # 39
    pack commit ref branch branch window merge object merge parser  # 40
    buffer buffer rebase blob  # 41
    queue delta blob window worker  # 42
    tree queue blob branch  # 0
    index delta rebase handle header stream index cache blob  # 1
    stream socket remote queue tree worker parser  # 2
    pack ref header socket pack merge socket object blob header ref  # 3
    rebase delta branch stream ref rebase cache  # 4
    delta worker queue blob stream rebase header  # 5
    token object merge socket commit window handle tree commit  # 6
    worker merge remote merge  # 7
    commit buffer cache buffer object remote pack blob buffer pack delta ref  # 8
    commit remote rebase pack socket  # 9
    parser remote cache header token remote parser pack remote delta merge handle  # 10
    object index merge socket stream blob cache remote worker buffer header  # 11
    commit branch queue pack handle merge remote  # 12
