    handle buffer worker token index buffer object  # 0
    queue commit remote merge object branch  # 1
    pack handle worker tree  # 2
    object rebase tree cache ref pack pack delta branch  # 3
    queue blob branch queue window queue  # 4
    merge blob token cache buffer blob delta branch blob tree  # 5
    commit stream rebase tree  # 6
    branch handle queue index index  # 7
    blob merge index delta remote object  # 8
    window token parser branch token handle handle rebase  # 9
    stream queue delta branch window  # 10
    worker header cache ref socket window branch branch branch  # 11
    index cache delta handle branch  # 12
    worker token worker index token  # 13
    tree window merge stream stream object header worker  # 14
    blob token cache window cache blob commit  # 15
    object merge handle rebase header object parser index remote  # 16
    index blob header socket rebase stream  # 17
    socket pack stream stream object handle token index object handle index index  # 18
    pack blob handle header delta token ref branch worker  # 19
    blob merge parser blob stream stream merge merge branch  # 20
    remote object object delta delta header token socket remote  # 21
    branch commit tree header merge blob merge worker token delta rebase  # 22
    handle pack delta window  # 23
    ref worker header ref token pack  # 24
    stream worker ref socket delta object blob index handle parser  # 25
    blob worker delta handle cache rebase header merge ref cache  # 26
    pack token parser index merge ref remote remote  # 27
    commit handle commit index  # 28
    window handle pack window blob queue commit token  # 29
    remote worker token commit socket pack pack commit delta stream  # 30
    ref socket header commit rebase socket remote queue worker queue  # 31
    cache window header tree buffer rebase queue  # 32
    index worker remote socket merge handle ref index socket header  # 33
    remote ref window rebase buffer parser remote buffer blob socket  # 34
    object socket ref socket window  # 35
    merge rebase handle worker pack index delta handle commit rebase  # 36
    queue queue parser index  # 37
    socket queue rebase ref  # 38
    object ref cache worker cache index buffer window  # 39
    blob pack index object delta header commit  # 40
    token buffer tree pack branch  # 41
    blob blob object queue  # 42
    branch socket window branch token handle blob tree commit delta cache  # 43
    ref queue remote merge commit rebase blob  # 44
    queue delta socket merge merge  # 45
    buffer pack branch commit buffer commit commit blob buffer rebase  # 46
    commit pack delta index buffer commit remote delta  # 47
    stream merge index rebase index commit merge blob cache commit stream tree  # 48
    index cache queue parser token  # 49
    pack cache remote header commit  # 50
    buffer tree commit buffer stream  # 51
    rebase buffer buffer window header tree header header handle  # 52
    header branch worker queue remote token rebase token worker rebase  # 53
    cache rebase index stream  # 54
    queue tree buffer handle ref tree parser  # 55
    cache blob rebase socket ref object window object delta stream  # 56
    commit queue delta index branch  # 57
    tree object queue pack  # 58
    delta commit remote stream  # 59
    tree pack socket branch object  # 60
    token parser commit cache remote delta queue object merge  # 61
    merge branch buffer branch window tree index delta  # 62
    remote index commit pack queue blob ref socket  # 63
    object ref socket merge  # 64
    buffer socket rebase header  # 65
    object socket merge queue merge ref ref  # 66
    socket socket worker worker remote branch cache  # 67
    buffer rebase delta tree  # 68
    delta worker buffer rebase window  # 69
    header rebase worker rebase socket stream header token token buffer  # 70
    token stream delta remote commit buffer tree ref  # 71
    delta window header index parser commit tree header merge branch socket  # 72
    buffer index merge rebase remote  # 73
    stream header worker window ref commit branch blob tree  # 74
    branch ref object handle delta stream  # 75
    window worker remote socket merge rebase stream rebase index  # 76
    queue queue tree branch  # 77
    pack rebase pack remote rebase header rebase delta handle index stream commit  # 78
    remote commit object parser  # 79
    queue header window rebase commit window buffer tree remote  # 80
    tree parser tree header tree delta  # 81
    delta index window object commit buffer merge branch cache header ref  # 82
    stream merge pack parser blob pack object token window  # 83
    index queue stream ref window buffer index cache token remote remote commit  # 84
    header queue buffer cache parser branch pack merge index buffer worker  # 85
    buffer window rebase stream object tree cache delta header  # 86
