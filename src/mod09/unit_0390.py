    stream branch handle parser header commit ref object queue buffer header parser  # 0
    parser object remote window ref branch token socket tree merge  # 1
    stream delta remote branch handle index  # 2
    ref handle cache tree tree tree ref delta delta  # 3
    ref stream object parser handle blob  # 4
    commit worker object cache parser worker object index  # 5
    delta tree index cache remote worker  # 6
    index buffer socket worker pack stream  # 7
    cache delta rebase header header ref  # 8
    ref pack tree window stream pack worker  # 9
    token parser blob merge handle rebase worker branch  # 10
    merge merge pack ref ref stream delta merge buffer token window  # 11
    rebase rebase stream header handle token buffer  # 12
    tree pack pack commit index queue worker worker window  # 13
    remote buffer tree branch blob buffer header merge tree socket  # 14
    ref branch buffer tree commit token ref socket queue  # 15
    cache cache branch ref rebase window rebase  # 16
    cache window branch tree remote ref pack handle merge window tree blob  # 17
    handle parser remote socket handle stream object  # 18
    socket commit branch delta rebase object  # 19
    merge ref pack handle index stream token commit worker  # 20
    ref token branch handle handle ref ref delta token queue socket  # 21
    remote blob rebase commit rebase worker  # 22
    token branch socket object handle object  # 23
    tree tree socket delta cache queue commit commit branch  # 24
    ref rebase stream token  # 25
    object tree branch token token ref rebase merge token  # 26
    tree parser socket merge blob pack  # 27
    merge token commit buffer pack  # 28
    handle token remote socket tree  # 29
    ref index tree branch  # 30
    object socket worker queue stream blob  # 31
    index commit window handle merge pack worker  # 32
    index window header header cache window index commit commit tree  # 33
    parser queue stream blob commit window handle ref parser  # 34
    queue parser handle queue tree  # 35
    ref buffer cache header stream remote index commit stream handle  # 36
    header commit buffer ref  # 37
    header delta cache cache  # 38
    socket buffer pack tree rebase merge cache pack parser stream  # 39
    stream index window merge  # 40
    branch branch worker worker tree token  # 41
    stream commit stream branch object ref pack pack blob parser header rebase  # 42
    buffer worker queue remote object token  # 43
    header branch merge worker worker delta window window worker merge  # 44
    header object window object commit merge parser queue remote socket index tree  # 45
    socket object stream queue index merge  # 46
    blob merge merge tree tree blob handle blob  # 47
    buffer token queue remote rebase  # 48
    branch commit tree parser ref buffer delta blob branch tree blob  # 0
    branch tree worker cache delta  # 1
    delta merge blob pack  # 2
    pack ref tree rebase tree pack blob stream commit pack worker  # 3
    delta commit rebase delta worker parser parser commit  # 4
    commit tree merge ref branch rebase tree index merge cache stream  # 5
    token tree commit window buffer  # 6
    merge object queue ref  # 7
    token handle worker handle object rebase parser socket index cache rebase pack  # 8
