    index header blob remote buffer blob tree blob merge queue  # 0
    buffer queue window socket tree blob object window queue  # 1
    token socket pack cache rebase parser  # 2
    handle window queue queue merge branch buffer window branch buffer  # 3
    worker buffer window pack rebase pack token merge token cache queue  # 4
    ref ref remote index socket object window index stream delta window  # 5
    queue delta remote tree commit tree delta tree index socket  # 6
    rebase merge ref buffer rebase header commit  # 7
    merge handle pack header index remote cache  # 8
    remote index object token delta pack socket buffer worker queue  # 9
    remote handle worker merge merge rebase window delta socket blob queue  # 10
    object ref queue header commit object merge worker stream branch  # 11
    object worker delta tree  # 12
    stream handle remote stream queue index  # 13
    socket commit worker socket handle buffer delta  # 14
    handle delta remote delta delta pack  # 15
    token header stream merge index merge  # 16
    buffer token window worker delta remote token window commit  # 17
    queue index buffer cache commit branch  # 18
    parser blob header cache commit  # 19
    branch stream rebase worker delta window socket rebase worker worker header pack  # 20
    socket ref object object  # 21
    merge rebase pack remote  # 22
    parser buffer handle handle cache header tree tree worker  # 23
    handle pack branch object  # 24
    rebase queue worker stream token handle  # 25
    commit cache branch token commit parser remote  # 26
    ref parser queue ref branch blob queue  # 27
    token tree cache socket  # 28
    blob object delta handle header queue parser remote buffer  # 29
    stream remote stream queue worker object commit socket rebase  # 30
    delta pack delta blob tree queue stream parser queue cache rebase commit  # 31
    blob pack stream buffer pack index handle  # 32
    ref header header stream pack delta stream branch  # 33
    window window object merge rebase worker branch branch header  # 34
    rebase token cache delta index  # 35
    stream window branch blob queue tree pack worker  # 36
    rebase stream merge rebase ref commit header commit rebase socket  # 37
    token remote branch blob  # 38
    cache merge index branch rebase queue stream delta  # 39
    merge ref token index remote worker branch object stream  # 40
    handle remote object buffer tree socket buffer  # 41
    cache parser socket pack header  # 42
    buffer delta branch token rebase buffer ref socket token parser parser merge  # 43
    rebase index ref remote parser header cache blob branch  # 44
    stream buffer stream commit pack  # 45
    index token remote header commit  # 46
    delta ref stream branch merge ref branch pack buffer socket remote  # 47
    delta window commit index window parser stream delta cache pack pack  # 48
    commit header delta header  # 49
    queue merge pack branch  # 50
    header worker ref handle handle token  # 51
    queue stream token rebase  # 52
    socket pack token remote rebase pack  # 53
    remote token tree index  # 54
    buffer tree pack queue index  # 55
    rebase header queue tree  # 56
    buffer tree delta token buffer buffer pack rebase index buffer worker  # 57
    merge socket commit commit ref stream header index tree stream  # 58
    window ref remote cache remote stream cache stream  # 59
    delta handle rebase rebase index token  # 60
    tree parser stream rebase object index  # 61
    branch queue parser stream buffer queue ref branch tree cache branch  # 62
    pack branch buffer stream  # 63
    remote parser cache blob handle header pack parser buffer rebase  # 64
    tree ref rebase commit worker commit stream merge object rebase window  # 65
    object index commit cache queue window  # 66
    tree remote window blob header blob cache handle pack delta  # 67
    tree queue header buffer buffer socket stream object  # 68
    index worker delta delta cache tree delta  # 69
    rebase object ref tree stream socket remote branch  # 70
    token buffer socket handle index stream parser worker queue queue  # 71
    rebase socket token object tree  # 72
    worker object pack branch delta cache socket worker merge worker ref worker  # 73
