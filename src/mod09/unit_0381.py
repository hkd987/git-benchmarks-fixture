    buffer blob index worker ref  # 0
    ref socket token tree worker stream rebase worker object pack index  # 1
    branch cache header cache commit token stream header delta remote merge remote  # 2
    rebase handle header worker  # 3
    cache remote stream merge parser stream branch stream parser rebase handle rebase  # 4
    worker index header window index index index header stream  # 5
    worker ref merge header delta delta merge worker blob  # 6
    commit parser remote branch object buffer buffer  # 7
    remote index merge rebase merge  # 8
    header queue tree remote buffer window commit ref branch remote buffer queue  # 9
    branch worker blob token stream handle tree token queue cache  # 10
    stream socket header token index delta pack commit commit  # 11
    merge parser commit queue  # 12
    handle socket buffer stream buffer object parser stream delta cache window buffer  # 13
    remote tree worker commit merge handle ref  # 14
    rebase header delta blob remote handle merge parser commit socket parser worker  # 15
    token handle cache merge tree index branch ref header tree  # 16
    token remote buffer parser merge window socket parser header  # 17
    stream tree socket parser queue index stream parser object worker  # 18
    handle window buffer pack tree index buffer parser token  # 19
    pack window ref stream object rebase queue worker  # 20
    socket delta ref index  # 21
    tree delta stream delta tree handle ref  # 22
    pack index blob parser remote  # 23
    merge branch blob ref stream buffer delta queue tree header rebase worker  # 24
    delta buffer branch delta buffer remote branch parser queue rebase merge  # 25
    queue socket stream window handle  # 26
    index window merge blob branch token remote object blob pack  # 27
    index stream rebase token handle  # 28
    commit ref index queue cache stream  # 29
    delta worker token parser blob object token merge  # 30
    parser parser queue cache remote worker  # 31
    remote worker buffer object stream queue window queue remote object stream rebase  # 32
    index ref ref ref handle handle blob  # 33
    pack socket worker queue parser merge parser merge remote  # 34
    blob window worker header ref merge cache queue socket  # 35
    socket commit remote tree blob pack token delta  # 36
    pack handle branch window buffer  # 37
    socket merge branch blob  # 38
    branch branch index cache socket tree ref buffer tree blob  # 39
    socket object queue ref window  # 40
    socket merge handle index blob blob rebase merge branch commit window  # 41
    delta worker branch socket rebase socket parser socket  # 42
    rebase ref blob ref rebase object  # 43
    pack tree buffer object socket branch  # 44
    stream merge stream queue delta branch worker header branch ref ref  # 45
    socket ref rebase worker buffer cache pack blob branch object delta  # 46
    token buffer object delta header  # 47
    object tree token queue  # 48
    token socket rebase handle remote  # 49
    object queue token buffer object index token blob window  # 50
    blob token worker header socket token index queue handle  # 51
    index header ref parser index commit pack blob merge queue remote  # 52
    merge stream rebase queue index rebase token blob socket blob branch delta  # 53
    queue queue tree token pack handle tree token blob parser  # 54
