    parser cache rebase remote socket header tree merge worker tree parser queue  # 0
    remote object object commit  # 1
    window merge handle header remote stream stream worker header ref  # 2
    worker branch tree commit handle cache cache ref  # 3
    token buffer merge remote pack header socket  # 4
    branch token delta blob tree merge delta delta rebase handle rebase parser  # 5
    remote queue delta merge parser ref remote delta stream rebase  # 6
    header tree tree queue buffer buffer rebase worker merge stream delta blob  # 7
    token buffer blob queue buffer merge  # 8
    pack stream parser delta handle commit handle buffer index header  # 9
    cache header window delta branch rebase ref index ref index  # 10
    pack window index header ref socket tree index  # 11
    tree cache delta cache index queue handle rebase remote  # 12
    window parser window object index token  # 13
    handle cache delta branch buffer pack cache worker socket tree pack  # 14
    pack parser object merge delta  # 15
    socket queue token stream ref  # 16
    header worker ref delta merge cache commit cache ref  # 17
    stream tree parser ref cache pack cache  # 18
    index tree branch remote merge  # 19
    handle worker buffer ref branch commit parser token socket parser stream buffer  # 20
    commit socket header token  # 21
    merge buffer stream commit  # 22
    merge blob ref window  # 23
    stream ref object pack worker parser index commit handle cache handle pack  # 24
    rebase queue stream buffer rebase stream socket merge handle parser rebase token  # 25
    worker token branch object branch buffer handle token buffer  # 26
    merge delta blob header branch remote  # 27
    pack token merge pack commit pack token buffer handle queue buffer  # 28
    pack window handle socket object branch object commit parser  # 29
    index buffer tree header commit header worker blob handle worker  # 30
    pack blob tree cache object merge blob window  # 31
    stream stream cache index  # 32
    token handle rebase delta delta  # 33
    handle branch merge handle parser index  # 34
    token commit parser queue buffer index stream ref pack remote ref  # 35
    buffer worker blob handle merge pack cache queue merge tree remote remote  # 36
    token index merge ref parser commit window parser commit  # 0
    worker stream window index queue socket  # 1
    ref merge handle object stream  # 2
    buffer branch parser stream  # 3
    remote tree token tree parser blob branch remote parser  # 4
    worker buffer parser merge buffer merge buffer pack index  # 5
    queue handle token window branch rebase commit commit blob parser remote branch  # 6
