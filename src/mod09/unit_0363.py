    stream pack delta pack branch window buffer buffer window object buffer commit  # 0
    merge index index parser ref worker  # 1
    ref tree index header buffer window rebase delta rebase window commit branch  # 2
    tree parser tree worker delta socket handle queue rebase rebase index  # 3
    parser merge stream object index branch  # 4
    worker commit commit header cache index object handle  # 5
    ref tree window token pack  # 6
    delta rebase handle object buffer  # 7
    object remote buffer handle  # 8
    parser cache object worker delta blob handle ref index rebase token token  # 9
    handle commit rebase buffer delta  # 10
    window pack branch socket object pack  # 11
    buffer cache tree branch  # 12
    window cache object blob window pack  # 13
    commit socket queue ref parser queue tree handle object socket  # 14
    cache token object parser  # 15
    pack remote rebase window  # 16
    parser stream blob window tree tree tree header handle  # 17
    remote object token cache window pack  # 18
    ref branch commit header buffer header commit commit parser socket worker header  # 19
    cache ref branch stream merge cache ref ref tree object queue index  # 20
    token queue socket stream ref buffer rebase pack index  # 21
    socket buffer token handle token stream window handle commit  # 22
    queue tree handle object rebase commit header socket  # 23
    object index tree ref object parser  # 24
    tree buffer ref cache index  # 25
    buffer handle branch parser header  # 26
    ref pack token handle queue rebase handle token token blob object  # 27
    branch parser queue header pack object pack handle cache  # 28
    window rebase index buffer token pack delta  # 29
    socket buffer tree buffer  # 30
    worker index tree commit handle object rebase delta stream  # 31
    worker delta stream buffer delta socket parser  # 32
    delta stream merge delta token token remote rebase  # 33
    handle window worker rebase handle handle branch pack header branch queue  # 34
    queue rebase branch buffer worker pack delta ref window window pack  # 35
    parser remote header queue index branch stream stream  # 36
    rebase remote buffer blob pack branch delta header buffer rebase object  # 37
    blob queue cache buffer window  # 38
    pack worker socket handle blob blob remote  # 39
    buffer merge branch queue remote rebase  # 40
    handle header tree index handle cache rebase  # 41
    blob delta ref branch stream object  # 42
    delta remote remote queue buffer tree  # 43
    object tree token socket token index token remote  # 44
    index object handle remote cache commit merge branch pack object  # 45
    handle handle remote branch socket token token rebase  # 46
    queue stream header commit parser remote rebase stream  # 47
    tree buffer queue pack socket rebase object queue object  # 48
    tree socket rebase queue merge remote rebase header  # 49
    queue worker window commit parser stream cache window window  # 50
    commit header header commit ref ref  # 51
    queue token handle cache  # 52
    ref window handle stream index  # 53
    branch object buffer merge branch delta branch buffer queue  # 54
    queue commit parser socket buffer delta  # 55
    parser ref cache parser token cache window object parser handle stream pack  # 56
    handle buffer stream window handle socket token blob token commit  # 57
    commit delta socket worker handle stream  # 58
    socket merge stream queue delta delta token stream index pack tree  # 59
    merge delta blob socket object socket merge branch rebase  # 60
    pack commit object object header tree socket blob cache  # 61
    index rebase stream object merge header remote handle worker  # 0
    ref object token ref buffer branch  # 1
    rebase delta token token  # 2
    buffer parser window branch header blob  # 3
    blob commit branch delta parser tree handle commit tree remote pack header  # 4
    commit index ref pack worker stream  # 5
    parser object socket socket ref  # 6
    branch token index token token token tree buffer index worker token token  # 7
    tree socket merge branch header window branch index token  # 8
    cache queue branch worker  # 9
    rebase stream index buffer stream index queue  # 10
    object remote handle object object pack header pack merge  # 11
    pack buffer buffer cache rebase  # 12
