    handle rebase commit object header buffer pack buffer  # 0
    delta pack queue header branch index pack  # 1
    window object rebase stream handle parser object token socket delta blob blob  # 2
    queue branch tree socket worker  # 3
    header remote handle header token  # 4
    cache parser handle delta socket queue  # 5
    delta ref remote blob remote stream object tree window  # 6
    ref remote stream commit socket ref ref branch  # 7
    window pack header ref branch index stream rebase commit  # 8
    merge header ref tree window  # 9
    token window delta token ref branch  # 10
    object pack delta ref  # 11
    remote window token worker stream handle window rebase worker cache delta blob  # 12
    queue object socket token ref token parser delta header token worker  # 13
    token delta blob queue merge  # 14
    stream merge branch tree socket delta socket remote header stream tree  # 15
    commit commit rebase stream index tree commit buffer worker stream stream  # 16
    stream blob queue cache token parser object worker  # 17
    delta index remote object delta pack  # 18
    stream socket branch socket commit socket rebase index merge  # 19
    buffer buffer buffer buffer worker buffer worker handle branch stream  # 20
    remote stream token index window  # 21
    object tree header cache  # 22
    socket tree object branch pack merge stream cache  # 23
    ref index tree stream socket window stream pack  # 24
    parser pack commit blob  # 25
    parser pack remote worker  # 26
    ref worker tree tree queue commit remote  # 27
    socket stream socket queue pack remote  # 28
    header token blob ref object queue  # 29
    token worker remote socket ref rebase remote token token rebase  # 30
    tree handle cache buffer index socket index tree worker  # 31
    worker pack remote index header pack  # 32
    worker token pack index object pack token stream token  # 33
    buffer delta index buffer parser worker cache merge  # 34
    queue handle parser object  # 35
    remote remote branch rebase rebase socket  # 36
    stream cache buffer rebase cache buffer tree ref  # 37
    token queue header window header handle ref blob stream parser commit  # 38
    remote queue object merge delta stream rebase commit handle handle merge socket  # 39
    index ref handle merge ref window token  # 40
    delta parser branch commit queue  # 41
    merge remote pack merge  # 42
    object cache parser stream branch worker ref tree object rebase cache  # 43
    object pack header rebase branch blob tree  # 44
    blob merge rebase object index cache ref window  # 45
    queue socket commit token rebase header cache index  # 46
    token index stream blob worker worker commit buffer  # 47
    object merge blob tree queue stream worker window tree ref index  # 48
