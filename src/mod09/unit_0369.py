    tree header tree socket queue ref rebase branch worker stream stream  # 0
    queue queue socket socket pack ref object ref stream window  # 1
    blob queue delta cache blob blob  # 2
    index socket queue queue object index remote header blob handle  # 3
    remote remote buffer header cache merge cache pack worker  # 4
    object object commit socket ref stream header handle handle blob object merge  # 5
    remote merge stream buffer remote  # 6
    branch merge queue handle buffer socket remote  # 7
    branch worker buffer socket cache handle  # 8
    parser buffer tree window blob merge merge  # 9
    handle rebase header delta handle object pack stream commit blob branch  # 10
    blob token handle rebase branch handle  # 11
    window object index buffer handle socket merge token pack ref  # 12
    window remote stream stream delta parser socket  # 13
    commit cache blob cache index socket pack  # 14
    token remote ref queue socket ref object worker delta index tree  # 15
    token socket cache object  # 16
    index branch blob merge rebase rebase cache header window  # 17
    blob ref commit queue parser rebase handle delta  # 18
    remote merge object object delta pack window parser cache  # 19
    stream remote commit worker stream remote socket socket handle blob blob  # 20
    cache worker socket merge blob cache queue merge  # 21
    commit header token branch rebase token queue index worker rebase window handle  # 22
    header parser queue rebase socket token object rebase handle queue delta merge  # 23
    token rebase queue socket pack stream pack  # 24
    blob ref tree blob token merge remote socket queue ref  # 25
    buffer window pack branch buffer parser cache remote branch parser  # 26
    socket worker socket handle  # 27
    cache commit commit branch worker  # 28
    merge tree token pack socket  # 29
    branch index cache handle buffer  # 30
    socket remote commit commit ref queue delta ref object  # 31
    branch object pack ref parser merge pack cache buffer rebase queue blob  # 32
    branch merge cache pack ref merge  # 33
    token header pack index buffer queue tree tree worker remote blob commit  # 34
    stream index branch queue  # 35
    delta window handle buffer delta tree branch branch ref worker buffer header  # 36
    object handle tree stream cache handle  # 37
    buffer blob rebase rebase handle remote buffer commit buffer header blob  # 38
    pack worker parser cache index  # 39
    header branch ref merge parser buffer handle stream  # 40
    parser worker merge token pack queue queue header branch queue commit  # 41
    object pack merge ref handle queue  # 42
    buffer token stream worker header cache handle commit tree index socket  # 43
    blob cache worker stream delta worker parser tree queue stream remote  # 44
    object header queue buffer cache handle merge socket parser queue  # 45
    blob tree header parser window blob stream token delta blob  # 46
    pack merge ref rebase ref stream header  # 47
    merge socket header buffer pack ref index remote rebase object  # 48
    window branch cache handle rebase stream blob merge merge index  # 49
    commit ref branch socket pack remote queue handle pack remote queue  # 50
    window ref token socket handle index merge  # 51
    window queue buffer worker queue merge delta window socket  # 52
    queue pack queue socket index rebase commit pack parser worker stream  # 53
    token window header delta branch remote ref ref parser  # 54
    header ref header remote rebase queue branch ref  # 55
    branch buffer parser parser rebase branch window merge  # 56
    merge rebase commit delta blob blob window header  # 57
    handle delta queue buffer stream object blob  # 58
    tree delta socket object rebase branch worker handle parser  # 59
    remote blob stream handle socket commit rebase parser tree  # 60
    worker index buffer tree delta parser delta worker merge branch  # 61
    stream header buffer ref token window blob branch  # 62
    buffer merge queue merge ref rebase handle queue header token merge  # 63
    remote handle remote index remote remote  # 64
    queue header merge ref  # 65
    blob worker buffer tree delta index queue buffer index index worker  # 66
    index object handle queue merge handle commit parser handle parser branch pack  # 67
    remote commit pack token object queue  # 68
    buffer branch stream merge rebase index queue remote socket rebase remote token  # 69
    commit token commit rebase parser commit index pack pack worker tree pack  # 70
    blob delta token handle header token stream cache worker worker stream blob  # 71
    socket tree queue index token  # 72
    merge delta merge buffer commit object index  # 73
    window object blob index branch pack cache pack object header  # 74
    handle header cache header index cache worker pack  # 75
    window cache delta pack queue remote branch object stream cache stream  # 76
    buffer buffer merge index window branch index pack rebase handle blob delta  # 77
