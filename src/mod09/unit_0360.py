    remote branch merge buffer pack index handle queue blob  # 0
    stream merge branch branch ref delta cache pack socket token  # 1
    buffer parser buffer socket ref pack stream stream queue  # 2
    worker socket remote merge handle rebase queue window socket commit remote delta  # 3
    worker stream pack commit token worker index handle  # 4
    remote token window queue branch merge blob handle buffer tree commit  # 5
    object object merge pack rebase handle  # 6
    object ref commit blob  # 7
    branch queue worker buffer tree tree buffer parser  # 8
    tree remote delta pack header index ref delta object object  # 9
    rebase index cache socket token worker pack  # 10
    handle parser pack blob stream tree  # 11
    blob header handle branch socket worker  # 12
    index rebase cache token stream object commit merge worker token pack  # 13
    queue index branch delta  # 14
    ref remote blob tree  # 15
    socket socket blob stream ref merge window stream  # 16
    rebase commit window ref blob tree worker parser header  # 17
    worker rebase cache stream ref  # 18
    object buffer delta token handle  # 19
    blob socket token stream window pack commit ref worker index window rebase  # 20
    ref delta rebase cache stream blob object  # 21
    rebase pack socket cache delta pack object commit blob rebase blob pack  # 22
    blob token branch stream  # 23
    stream branch socket ref parser commit delta commit queue header window  # 24
    remote pack header buffer token queue index  # 25
    delta branch stream handle parser worker header rebase  # 26
    tree tree delta handle handle stream branch  # 27
    object pack stream window window socket  # 28
    handle handle parser handle header  # 29
    branch parser object ref blob remote cache blob  # 30
    object commit object parser tree  # 31
    socket tree object parser header delta window  # 32
    blob socket socket tree queue header queue handle parser header parser tree  # 33
    delta index merge socket parser stream window  # 34
    remote blob stream tree  # 35
    tree ref commit socket parser queue merge  # 36
    index header commit ref  # 37
    parser merge buffer rebase object cache buffer worker cache  # 38
    parser buffer branch branch handle  # 39
    buffer rebase index parser ref handle buffer  # 40
    delta index header branch commit parser rebase parser worker  # 41
    branch handle index remote delta branch token pack header  # 42
    handle queue cache pack handle worker socket buffer token  # 43
    remote parser stream window remote handle  # 44
    ref token token commit parser pack buffer window remote token  # 45
    parser buffer socket header delta merge handle stream cache object commit token  # 46
    branch commit rebase parser window header commit handle remote rebase rebase  # 47
    cache parser window token object branch pack object handle object rebase object  # 48
    blob object commit blob worker stream  # 49
    merge rebase blob window worker cache buffer tree remote remote worker  # 50
    rebase queue index delta remote branch header commit ref remote index socket  # 51
    stream pack queue handle socket pack stream branch  # 52
    window handle rebase pack socket  # 53
    worker tree worker worker merge index branch stream queue cache  # 54
    queue delta handle queue header buffer blob buffer branch handle socket  # 55
    header queue commit delta blob index rebase rebase queue buffer buffer  # 56
    header header remote commit commit window ref  # 57
    commit token blob object  # 58
    buffer stream header header branch tree token pack handle handle branch  # 59
    parser window blob stream  # 60
    remote pack token rebase  # 61
    tree branch ref rebase commit commit tree branch socket  # 62
    parser ref delta handle window buffer handle  # 63
    socket queue blob object rebase cache delta header object stream delta index  # 64
    token tree pack object  # 65
    queue queue index remote worker  # 66
    queue window tree parser  # 67
    parser index worker rebase  # 68
    buffer stream socket tree object handle socket  # 69
    stream header remote window pack object branch cache stream stream socket commit  # 70
    queue rebase ref parser branch queue index merge  # 71
    token queue token cache rebase delta tree window  # 72
    stream worker ref index delta branch  # 73
    token index index parser parser queue pack socket handle  # 74
    object worker branch cache handle  # 75
    socket delta cache cache worker queue handle token buffer blob index object  # 76
    worker token queue token commit blob remote  # 77
    header remote buffer index handle header tree header object commit  # 78
    pack blob branch stream rebase commit stream  # 79
