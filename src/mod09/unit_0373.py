    branch worker index branch remote blob remote  # 0
    remote parser rebase buffer commit cache header  # 1
    blob index object window socket queue  # 2
    commit stream window worker cache handle commit queue worker buffer  # 3
    stream header buffer stream index  # 4
    ref parser pack tree remote header socket branch  # 5
    ref window remote blob buffer header socket queue buffer ref queue queue  # 6
    remote branch token worker branch pack worker window handle blob  # 7
    parser handle branch branch parser delta stream worker merge header remote  # 8
    commit remote queue queue queue delta token handle  # 9
    remote cache commit rebase window ref blob ref index token  # 10
    cache cache token merge parser handle handle blob queue  # 11
    handle remote branch index  # 12
    header delta ref rebase blob worker worker header queue socket  # 13
    socket remote rebase remote pack tree token pack  # 14
    blob index pack queue header blob merge ref header tree remote queue  # 15
    delta index worker merge blob index socket cache socket  # 16
    object worker ref worker object branch  # 17
    queue delta window handle tree rebase window  # 18
    worker rebase window socket index socket ref worker object ref window ref  # 19
    stream index tree handle ref  # 20
    branch header object delta merge header buffer worker  # 21
    rebase branch cache socket token worker object window buffer token  # 22
    stream stream handle delta worker index  # 23
    index cache cache remote cache socket merge  # 24
    parser blob token commit socket blob index rebase  # 25
    header blob remote delta stream stream pack  # 26
    handle blob buffer object worker branch parser  # 27
    index window remote token object cache parser delta index token  # 28
    socket worker handle stream  # 29
    pack worker commit ref index stream  # 30
    worker stream queue commit blob stream  # 31
    branch worker handle socket worker rebase header commit stream pack pack  # 32
    rebase worker header stream rebase merge branch handle merge index object token  # 33
    header branch pack tree  # 34
    object queue worker parser cache delta ref  # 35
    stream header tree window object index commit merge handle token parser  # 36
    header buffer blob remote commit queue parser ref stream cache object  # 37
    delta object delta remote worker token worker  # 38
    commit commit commit socket pack stream socket handle rebase queue rebase  # 39
    rebase object cache handle object header rebase stream parser  # 40
    buffer token buffer ref  # 0
    parser rebase tree cache branch ref ref branch worker  # 1
    cache cache socket handle  # 2
    blob window ref buffer window merge  # 3
    pack tree ref merge ref worker blob queue window  # 4
    rebase worker tree cache remote token index  # 5
    commit delta tree index header  # 6
    remote object object commit index parser tree parser  # 7
    delta remote pack cache pack window pack remote index object pack queue  # 8
    object index socket blob queue token  # 9
    remote parser parser tree blob blob blob window parser merge object  # 10
    token worker window remote  # 11
    remote pack pack pack  # 0
    handle parser tree socket token merge object socket commit  # 1
    token worker window cache blob  # 2
    window branch delta blob queue merge blob token  # 3
    token branch buffer ref merge window merge cache  # 4
    merge token rebase pack window handle merge object stream  # 5
    rebase token parser tree remote index object stream parser  # 6
    branch object blob object blob  # 7
    branch socket pack tree header stream delta remote buffer commit handle  # 8
    commit cache object window cache window rebase pack commit window  # 9
    tree token ref worker delta branch  # 10
    cache pack object token parser buffer socket stream cache pack worker  # 11
    commit window branch parser stream blob header merge header merge rebase branch  # 12
    queue handle merge index socket object object handle  # 0
    handle header queue object delta branch cache branch stream remote  # 1
    tree commit worker header branch blob buffer commit header  # 2
    merge header rebase index header delta  # 3
    remote branch branch ref handle  # 4
    window handle branch handle token window object cache  # 5
    delta branch token index handle  # 6
    delta token commit stream object merge index index  # 7
    header cache worker stream  # 8
    index token buffer header remote buffer socket pack queue  # 9
    commit worker index handle buffer  # 10
    stream blob stream remote buffer header commit index object parser  # 11
    object handle window queue buffer object stream socket rebase commit blob handle  # 12
    header header window header buffer handle tree header socket socket  # 13
    worker socket commit queue tree remote merge queue  # 14
    delta token index commit remote merge remote delta blob delta socket cache  # 0
    worker remote queue parser  # 1
    handle socket parser pack token header  # 2
    tree socket socket remote pack branch pack queue blob header remote  # 3
    ref buffer pack window delta pack blob  # 4
