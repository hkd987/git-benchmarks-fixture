    handle parser token object merge pack merge merge  # 0
    queue buffer window delta  # 1
    queue ref stream cache queue remote branch buffer remote stream delta header  # 2
    header socket delta pack index cache socket remote stream  # 3
    merge remote worker buffer branch tree token index merge  # 4
    socket token parser header token ref delta remote header  # 5
    blob buffer worker remote  # 6
    rebase queue window remote rebase commit  # 7
    blob parser tree buffer remote  # 8
    token rebase pack queue header parser blob header pack tree handle  # 9
    commit token commit worker queue stream window  # 10
    cache pack cache buffer handle parser commit tree branch worker cache  # 11
    cache ref header token  # 12
    header commit window pack token handle stream  # 13
    index socket pack ref object merge socket  # 14
    object tree tree merge  # 15
    cache tree index header pack object queue rebase window branch  # 16
    queue commit delta blob socket index window  # 17
    rebase index commit tree socket worker buffer queue rebase tree rebase  # 18
    remote object branch stream delta branch remote branch object  # 19
    queue branch branch parser remote remote ref rebase  # 20
    window handle window stream tree ref socket branch  # 21
    handle tree blob object handle index object object stream tree stream  # 22
    socket socket rebase pack delta queue  # 23
    cache window blob rebase cache  # 24
    remote stream delta object remote cache commit handle  # 25
    handle pack remote merge remote merge rebase branch tree pack window  # 26
    token header remote delta pack cache header merge window socket branch buffer  # 27
    token branch worker stream worker branch  # 28
    queue blob ref remote remote parser buffer cache object branch index socket  # 29
    commit stream stream delta delta tree commit window commit  # 30
    header rebase remote branch index socket worker object cache blob buffer stream  # 31
    merge ref ref handle commit  # 32
    queue cache parser pack token worker window  # 33
    cache branch pack stream worker  # 34
    cache handle window stream branch merge remote  # 35
    token header ref delta stream  # 36
    queue buffer pack blob buffer  # 37
    pack parser delta header blob rebase queue socket window  # 38
    branch token worker window object blob token  # 39
    pack queue token delta rebase worker buffer pack  # 40
    merge index queue window delta blob ref ref commit  # 41
    token worker tree window commit pack branch handle queue branch  # 42
    blob object queue blob handle pack object object ref token  # 43
    stream blob remote tree token parser cache remote merge handle handle  # 44
    object buffer token blob tree queue branch worker tree parser delta commit  # 45
    commit remote cache branch ref  # 46
    blob buffer window stream token branch token socket branch  # 47
    handle stream branch remote merge remote  # 48
    buffer merge header merge  # 49
    pack remote token socket delta  # 50
    branch ref queue index ref branch token merge  # 51
    window worker socket ref stream stream stream rebase buffer ref  # 52
    header token handle buffer commit branch remote handle blob index socket delta  # 53
    delta window parser branch worker cache  # 54
    commit rebase tree branch tree merge commit stream merge  # 55
    token queue branch rebase remote ref blob header header parser  # 56
    delta cache index queue handle commit  # 57
    index branch stream cache remote merge queue handle branch merge delta stream  # 58
    blob commit parser blob worker delta remote  # 59
    stream ref cache token remote buffer tree index ref  # 60
    socket parser header handle blob index  # 61
    branch queue buffer merge parser ref token  # 62
    remote index remote window socket blob  # 63
    delta header rebase branch parser buffer cache merge ref commit queue commit  # 64
    branch header branch handle  # 65
    window rebase window rebase index queue delta branch buffer header parser ref  # 66
    blob remote header buffer object delta token object merge worker  # 67
    object header cache stream pack header header index object  # 68
    commit window remote socket buffer token rebase queue cache buffer  # 69
