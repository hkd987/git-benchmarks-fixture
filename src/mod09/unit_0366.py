    queue object tree cache delta merge pack object  # 0
    pack pack cache handle pack merge delta  # 1
    buffer pack worker commit rebase buffer parser remote remote index  # 2
    tree handle delta cache tree stream window object stream tree  # 3
    token queue rebase commit branch socket cache window object header  # 4
    branch buffer rebase rebase cache socket remote worker delta header  # 5
    header delta stream parser socket blob rebase  # 6
    stream index queue commit index remote tree buffer  # 7
    commit socket commit queue  # 8
    header blob pack socket cache remote remote remote  # 9
    buffer branch queue parser blob header tree window object remote worker  # 10
    merge stream parser buffer branch  # 11
    queue buffer socket object blob  # 12
    commit blob parser header token rebase  # 13
    socket merge branch ref stream queue  # 14
    pack commit window token index buffer delta  # 15
    window tree parser rebase blob  # 16
    queue stream buffer delta window commit parser token cache window buffer commit  # 17
    queue parser ref tree  # 18
    merge cache remote merge cache window token worker tree handle blob  # 19
    queue index queue queue cache remote handle  # 20
    pack window handle header tree worker socket token pack stream  # 21
    object remote cache blob window window parser  # 22
    stream token buffer token  # 23
    window rebase worker cache ref  # 24
    socket branch parser commit rebase  # 25
    commit delta rebase remote  # 26
    tree branch object rebase rebase pack  # 27
    parser rebase index object blob parser buffer pack cache  # 28
    pack branch remote token remote buffer commit merge header stream  # 29
    token worker buffer branch parser delta  # 30
    token rebase header commit rebase queue merge object window branch index handle  # 31
    header merge buffer window buffer commit index pack parser  # 32
    socket tree socket cache pack rebase commit token header window  # 33
    rebase blob stream window queue  # 34
    socket ref queue rebase worker rebase parser cache queue handle delta  # 35
    handle branch parser tree pack remote  # 36
    ref cache remote window worker pack rebase  # 37
    parser tree stream cache parser object ref commit merge parser index branch  # 38
    index tree ref branch pack window stream queue pack ref  # 39
    parser rebase merge worker header cache buffer rebase cache  # 40
    handle handle commit worker  # 41
    pack blob queue tree rebase buffer header token  # 42
    rebase commit handle buffer token remote blob ref index delta remote  # 43
    token cache token stream cache ref stream merge delta stream  # 44
    header tree worker socket ref pack stream merge queue buffer  # 45
    ref rebase index header queue merge remote  # 46
    stream buffer branch object ref branch  # 47
    cache index index window cache token stream index ref merge  # 48
    remote tree ref tree socket delta  # 49
    merge ref branch stream header header rebase delta object queue  # 50
    parser pack socket index pack stream  # 51
    pack object commit merge stream socket cache ref object queue handle  # 52
    object remote tree queue header stream cache queue socket index parser  # 53
    commit buffer token handle  # 54
    object merge ref parser ref merge buffer rebase stream window tree blob  # 55
    buffer token token commit  # 56
    parser branch commit branch parser worker tree commit branch branch handle blob  # 57
    socket tree queue window handle remote cache index handle  # 58
    remote rebase queue token buffer  # 59
    window branch window blob buffer  # 60
    queue commit index rebase  # 61
    buffer parser merge buffer handle blob token parser  # 62
    stream commit merge remote pack merge handle branch pack blob object tree  # 63
    window merge branch buffer pack delta commit  # 64
    queue index cache stream remote object  # 65
    stream blob merge tree worker rebase window merge  # 66
    tree queue queue blob window branch branch queue window token  # 67
    worker handle branch worker object parser tree buffer socket remote  # 68
    commit commit remote remote stream window remote parser pack rebase index worker  # 69
    ref stream object delta index buffer  # 70
    delta socket parser blob parser rebase cache worker buffer  # 71
    worker parser queue delta  # 72
    object worker parser window parser pack merge handle  # 73
    object socket rebase index object index  # 74
    stream window tree buffer header ref pack blob worker branch delta  # 75
    queue delta parser token stream stream handle  # 76
    handle remote blob branch handle commit handle stream merge merge  # 77
