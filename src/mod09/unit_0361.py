    stream index object rebase cache commit cache object  # 0
    merge window handle delta rebase branch  # 1
    worker ref object worker  # 2
    blob handle buffer remote merge buffer ref index handle header  # 3
    socket delta token parser  # 4
    object blob header merge token pack tree  # 5
    cache tree queue token delta blob handle rebase object  # 6
    object blob queue object index window buffer delta  # 7
    blob blob ref rebase stream ref commit delta stream merge buffer  # 8
    worker index queue pack delta header tree header window index handle  # 9
    cache cache window parser token commit blob header  # 10
    stream index tree merge header parser worker tree branch parser branch  # 11
    buffer handle remote delta window index buffer rebase socket window object delta  # 12
    socket pack socket buffer pack ref buffer  # 13
    commit parser commit queue token cache merge remote socket window object  # 14
    branch parser commit pack handle token delta stream  # 15
    rebase index cache remote window remote branch delta branch queue index token  # 16
    socket merge branch socket window handle window index cache  # 17
    header buffer socket parser object blob blob worker handle stream buffer  # 18
    token ref blob blob header  # 19
    window window token tree remote buffer rebase header buffer branch commit  # 20
    delta delta header cache tree rebase merge worker cache ref pack  # 21
    blob blob blob blob token worker stream header  # 22
    stream delta header queue  # 23
    branch buffer remote delta token commit index stream ref  # 24
    merge buffer ref commit parser object header pack merge blob  # 25
    ref branch ref parser object stream tree  # 26
    object parser buffer pack buffer worker window pack  # 27
    window cache token blob socket handle rebase ref cache buffer  # 28
    merge token remote header tree commit object queue parser buffer  # 29
    token merge rebase branch  # 30
    ref ref remote commit queue stream queue queue rebase handle  # 31
    token ref index branch remote  # 32
    remote tree ref queue pack handle token  # 33
    delta blob blob rebase queue  # 34
    branch index delta worker pack delta socket cache window branch buffer  # 35
    commit delta delta blob tree window merge  # 36
    object header stream worker  # 37
    ref branch socket index  # 38
    index index blob merge header  # 39
    remote rebase branch parser branch window worker  # 40
    parser parser pack pack branch remote  # 41
