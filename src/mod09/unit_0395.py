    blob object blob index queue pack parser merge queue tree  # 0
    stream queue pack merge window worker worker buffer queue tree  # 1
    stream header buffer handle parser blob header handle blob cache commit  # 2
    merge pack merge window merge token  # 3
    stream branch header rebase  # 4
    tree delta merge commit branch parser header blob merge  # 5
    tree merge object branch socket delta window tree index  # 6
    index rebase rebase remote ref token commit token queue  # 7
    header token remote branch header queue socket parser queue  # 8
    window parser stream tree queue index rebase window buffer branch merge  # 9
    handle branch index merge tree handle rebase branch  # 10
    ref stream rebase handle  # 11
    token parser buffer commit remote  # 12
    parser buffer branch merge remote  # 13
    blob tree handle object window stream token  # 14
    rebase commit handle socket  # 15
    merge commit worker rebase  # 16
    stream commit object index header delta cache index  # 17
    remote delta cache worker branch ref stream blob cache blob  # 18
    stream stream cache cache commit stream rebase object branch merge  # 19
    delta parser pack window pack rebase branch merge  # 20
    socket stream token socket header merge tree  # 21
    commit pack merge pack  # 22
    object queue token ref window worker object  # 23
    handle pack token commit cache stream window remote commit branch  # 24
    queue tree object object socket  # 25
    worker buffer object branch worker stream delta pack stream token queue  # 26
    index handle merge handle index ref  # 27
    delta blob worker stream pack  # 28
    commit commit object stream rebase cache handle window handle  # 29
    commit header header blob index handle window tree pack  # 30
    token handle remote handle object  # 31
    pack worker remote object header  # 32
    index socket parser parser  # 33
    remote token cache commit buffer blob delta window ref buffer tree  # 34
    branch stream ref queue buffer window worker commit delta rebase cache tree  # 35
    parser buffer commit socket remote object branch merge handle stream window  # 36
    delta rebase ref delta branch commit socket token cache blob index object  # 37
    buffer queue buffer rebase blob queue  # 38
    queue parser object commit pack  # 39
    rebase cache parser blob  # 40
    blob window buffer rebase handle delta queue rebase commit  # 41
    delta pack parser ref commit stream token queue remote blob blob queue  # 42
    parser object parser merge pack delta buffer blob delta worker blob  # 43
    ref branch commit worker object commit handle  # 44
    merge queue worker merge ref delta blob buffer delta commit blob  # 45
    rebase cache parser parser worker  # 46
    socket tree worker handle pack merge delta worker  # 47
    ref stream ref window cache merge buffer handle  # 48
    window header remote index stream cache queue header delta parser branch  # 49
    parser merge worker index header merge  # 50
    stream header pack tree cache object ref token tree pack tree window  # 51
    blob remote token token cache  # 52
    handle delta ref tree commit object tree  # 53
    tree token cache rebase index rebase  # 54
    buffer object header delta buffer socket parser ref merge  # 55
    token delta parser pack header cache header ref tree cache queue rebase  # 0
    branch window ref handle  # 1
    commit index handle window socket commit handle worker tree  # 2
    commit parser delta rebase pack ref queue socket queue  # 3
    object buffer ref blob commit branch window object object remote handle pack  # 4
