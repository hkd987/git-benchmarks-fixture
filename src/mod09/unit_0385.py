    tree buffer buffer buffer index header header parser cache  # 0
    socket delta index commit  # 1
    index tree window stream buffer ref parser blob index token branch  # 2
    commit socket tree pack rebase blob parser rebase rebase buffer  # 3
    branch token commit header token parser handle buffer buffer tree stream  # 4
    token rebase stream branch worker  # 5
    queue commit stream object pack index branch  # 6
    rebase blob window ref commit ref ref blob cache rebase rebase cache  # 7
    parser branch blob pack queue parser ref cache window  # 8
    socket header handle token ref  # 9
    stream parser ref remote branch parser object remote commit cache stream remote  # 10
    tree ref commit handle window cache stream queue merge parser commit ref  # 11
    handle queue object delta header window cache worker blob buffer  # 12
    header branch cache worker merge window socket  # 13
    tree tree remote parser stream stream stream merge tree  # 14
    socket worker rebase merge ref merge handle branch rebase  # 15
    worker cache merge object pack  # 16
    handle tree handle worker remote parser object rebase socket buffer remote object  # 17
    commit parser blob worker buffer pack pack tree  # 18
    buffer buffer remote remote  # 19
    commit queue stream blob branch remote index ref  # 20
    pack worker rebase stream token remote  # 21
    commit tree object remote window cache stream  # 22
    index tree pack window branch pack  # 23
    token ref ref merge ref merge token worker  # 24
    buffer stream index pack ref  # 25
    object token index branch merge blob queue window header ref window  # 26
    blob socket blob remote cache ref rebase queue  # 27
    window branch delta window index tree worker commit stream  # 28
    ref index token handle header header socket  # 29
    tree window worker buffer pack cache  # 30
    remote worker delta parser commit queue buffer cache delta parser branch index  # 31
    stream commit stream token commit tree rebase object  # 32
    index window parser pack index  # 33
    tree remote handle parser object worker merge worker token queue  # 34
    buffer buffer ref socket parser socket parser index worker parser  # 35
    blob merge blob remote object remote  # 36
    token commit stream index pack rebase queue remote  # 37
    blob pack remote object header header tree index  # 38
    pack object ref header header commit cache blob remote blob  # 39
    commit blob merge ref ref delta  # 40
    tree header ref stream queue rebase  # 41
    delta token header token branch header blob delta remote  # 42
    tree handle pack token remote parser token header  # 43
    branch header branch commit  # 44
    merge tree branch header cache ref commit worker merge ref header socket  # 45
    queue stream tree delta parser tree  # 46
    queue socket stream tree merge commit  # 47
    merge stream queue index branch delta blob ref object parser token  # 48
    branch handle tree blob merge worker  # 49
    remote buffer handle stream remote ref stream delta  # 50
    header stream rebase merge stream  # 51
    token ref window remote header header blob  # 52
    merge worker ref merge  # 53
    handle parser token buffer ref blob  # 54
    socket worker token cache  # 55
    window socket queue object worker ref rebase  # 56
    commit pack commit delta tree  # 57
    commit delta worker object rebase  # 58
    branch rebase queue cache commit cache remote queue handle pack pack  # 59
    branch buffer socket pack cache ref buffer cache window  # 60
    parser merge cache header buffer index ref worker delta  # 61
    ref socket branch blob object branch socket token branch remote delta object  # 62
    commit parser object cache index remote delta  # 63
    socket socket window socket  # 64
    cache socket cache token stream parser branch  # 65
    branch worker pack window commit parser blob header rebase header header window  # 66
    tree buffer commit merge parser pack  # 67
    remote buffer rebase rebase token window header branch ref  # 68
    tree socket cache ref cache tree object  # 69
    branch object socket remote index token ref  # 70
    rebase worker commit merge commit token token token ref delta stream  # 71
    tree worker branch queue remote ref header cache token token blob  # 72
    rebase handle header socket  # 73
    ref queue queue buffer object blob queue branch blob pack pack  # 74
    delta socket merge window handle blob commit tree merge ref index worker  # 75
    ref cache worker index object delta parser blob  # 76
    branch token remote handle pack  # 77
    merge tree index delta handle pack commit worker buffer  # 78
    merge stream branch worker index merge cache object  # 79
    index commit window ref ref buffer buffer ref  # 80
    socket ref delta token branch queue queue  # 81
    socket pack socket worker merge worker parser socket window  # 82
