    branch handle branch commit object tree  # 0
    merge index blob header delta delta  # 1
    object handle index queue tree index queue merge buffer merge  # 2
    remote token object worker remote blob  # 3
    branch delta branch queue  # 4
    token branch handle buffer window tree blob socket  # 5
    remote buffer tree buffer blob pack branch branch stream  # 6
    remote queue handle remote object token queue  # 7
    buffer remote index window handle blob  # 8
    handle window token handle worker merge blob  # 9
    object tree stream header rebase window cache window object buffer socket remote  # 10
    delta tree tree handle rebase token handle object parser  # 11
    rebase index header rebase queue header  # 12
    pack remote delta object tree  # 13
    delta ref ref index delta window tree handle  # 14
    commit pack window window pack branch stream merge  # 15
    delta index queue tree  # 16
    parser delta tree window ref pack socket handle queue queue branch socket  # 17
    index delta delta handle index  # 18
    socket commit queue cache ref ref commit branch branch token worker  # 19
    header index ref cache  # 20
    object worker remote commit branch object object stream branch rebase queue  # 21
    remote cache parser parser worker handle stream header queue queue  # 22
    branch queue branch commit merge handle buffer worker tree  # 23
    delta parser commit pack parser socket remote  # 24
    socket object handle blob token window socket object queue window rebase  # 25
    index commit window token stream branch window blob ref  # 26
    socket rebase pack header queue rebase parser buffer branch  # 27
    tree commit tree header object window header merge rebase worker worker remote  # 28
    queue stream commit buffer index buffer  # 29
    queue header handle object header rebase buffer  # 30
    token stream ref ref branch delta  # 0
    merge socket branch token blob queue  # 1
    tree parser socket tree stream handle pack commit parser pack worker merge  # 2
    buffer buffer rebase delta stream stream  # 3
    object rebase tree parser  # 4
    blob rebase delta token delta worker cache pack blob stream cache stream  # 5
    stream blob blob rebase commit delta buffer ref cache  # 6
    socket remote tree rebase tree  # 7
    buffer commit buffer handle commit  # 8
    header buffer worker merge delta index window rebase delta  # 9
