    worker window blob buffer index window ref tree ref stream ref stream  # 0
    index stream handle ref pack  # 1
    rebase socket stream parser worker buffer blob worker token worker remote commit  # 2
    buffer branch merge object handle remote delta cache blob cache buffer delta  # 3
    pack remote parser commit socket ref header socket socket index  # 4
    header object buffer blob index merge index window window pack pack  # 5
    tree stream stream socket handle token  # 6
    remote token branch object index socket commit worker blob  # 7
    stream parser header queue object delta window tree remote  # 8
    commit blob object pack rebase object index rebase  # 9
    stream commit index queue handle  # 10
    branch merge worker tree buffer worker ref  # 11
    ref queue commit buffer tree blob token pack  # 12
    handle branch socket remote delta handle  # 13
    handle pack ref delta token parser merge window  # 14
    branch window delta commit cache delta rebase  # 15
    pack worker rebase queue blob remote blob window remote  # 16
    merge token header pack socket ref token  # 17
    window delta header worker rebase object  # 18
    merge pack parser branch socket token merge rebase handle cache window  # 19
    handle header delta window blob pack remote parser object  # 20
    pack remote queue index ref ref rebase remote pack blob token branch  # 21
    tree parser worker object window rebase blob worker  # 22
    parser header branch blob rebase  # 23
    handle handle header stream commit object stream cache socket commit ref parser  # 24
    ref delta buffer tree  # 25
    merge merge parser handle pack delta token rebase  # 26
    cache stream cache token delta object queue commit commit ref  # 27
    handle header tree blob handle tree socket remote commit  # 28
    rebase buffer buffer merge  # 29
    parser queue buffer branch remote merge object queue  # 30
    header merge merge window rebase blob  # 31
    queue socket delta delta socket  # 32
    queue object tree remote ref  # 33
    token index socket parser  # 34
    branch worker ref branch tree cache token rebase delta  # 35
    token object window branch tree blob merge socket header pack header  # 36
    cache socket buffer worker token token tree object tree window  # 37
    rebase remote rebase pack  # 38
    index blob remote index window  # 39
    tree cache pack blob stream  # 40
    queue commit delta cache index socket tree window  # 41
    tree queue merge pack cache  # 42
    token parser stream socket queue header parser  # 43
    header queue window queue  # 44
    header delta stream tree stream remote  # 45
    ref branch branch handle cache remote remote branch delta window header  # 46
    commit object queue header  # 47
    worker ref cache remote  # 48
    token parser queue tree socket  # 49
    commit header ref ref parser tree header index header index handle ref  # 50
    branch handle socket tree socket index token rebase queue object branch worker  # 51
    branch buffer merge rebase parser  # 52
    socket index cache cache stream header token window delta  # 53
    pack cache branch handle commit rebase  # 54
    token handle merge remote ref stream  # 55
    stream worker worker rebase parser index worker delta buffer blob delta  # 56
    cache remote branch worker handle  # 57
    queue cache blob queue  # 58
    window object header delta worker blob commit merge socket  # 59
    stream delta branch header window blob  # 60
    blob tree handle object commit buffer rebase ref ref parser  # 61
    worker token handle worker commit delta cache  # 62
    window stream window cache object  # 63
    socket remote branch socket tree commit window window  # 64
    tree token tree buffer delta remote socket window queue cache buffer token  # 65
    merge stream parser pack window branch index merge remote rebase token delta  # 0
    blob blob window parser token worker branch  # 1
    buffer branch header stream tree buffer  # 2
    buffer commit socket object socket delta rebase socket  # 3
    parser merge parser queue parser token  # 4
    header delta object index  # 0
    blob remote tree stream  # 1
    queue commit branch commit socket branch  # 2
    window remote token worker token  # 3
    tree socket branch token token object blob remote socket  # 4
    window commit socket index branch ref  # 5
    remote header branch delta branch remote token ref  # 0
    commit pack remote worker  # 1
    buffer blob blob handle ref handle handle blob rebase header  # 2
    branch queue object index  # 3
    parser object pack header stream remote socket parser pack pack  # 4
    cache token parser parser handle  # 5
    pack worker index header  # 6
    branch window commit socket ref  # 7
