    queue token remote stream commit rebase parser  # 0
    cache cache delta worker rebase merge remote rebase ref  # 1
    stream token delta pack window worker object stream blob branch worker  # 2
    remote stream tree socket object remote  # 3
    merge buffer socket blob branch parser header commit cache  # 4
    merge token merge branch buffer ref header remote  # 5
    delta queue parser index remote  # 6
    header worker tree token object header delta rebase token buffer  # 7
    merge stream blob merge blob ref buffer handle header merge  # 8
    tree window handle remote buffer  # 9
    blob merge rebase commit object ref parser cache commit merge merge  # 10
    commit commit queue cache header header buffer object buffer delta buffer worker  # 11
    socket rebase merge handle worker delta  # 12
    ref ref commit blob rebase parser tree index commit token branch stream  # 13
    token commit queue window pack handle rebase branch commit window worker  # 14
    pack header header queue pack parser remote  # 15
    branch handle branch stream rebase  # 16
    cache tree cache pack remote  # 17
    handle merge rebase window cache queue header branch  # 18
    queue stream tree remote token window commit merge window socket  # 19
    remote branch handle remote blob ref socket  # 20
    cache object worker worker tree buffer commit stream cache merge cache  # 21
    token remote pack stream tree parser rebase cache cache merge  # 22
    socket remote header merge cache blob rebase worker  # 23
    cache tree queue header pack  # 24
    ref header token commit queue rebase buffer  # 25
    tree ref delta stream cache delta  # 26
    merge worker blob tree delta merge tree buffer parser merge buffer  # 27
    branch cache socket merge index token merge delta window parser branch  # 28
    branch ref branch buffer cache worker worker token index window stream  # 29
    queue socket branch pack parser cache blob handle ref stream  # 30
    worker window cache commit window socket rebase socket worker worker  # 31
    cache blob parser buffer delta cache remote handle object merge blob worker  # 0
    queue stream tree object cache tree socket rebase  # 1
    socket index pack branch  # 2
    branch parser object object  # 3
    worker commit stream parser delta socket branch branch branch blob pack header  # 4
    handle socket index pack pack delta  # 5
    stream queue header tree buffer tree blob queue window blob queue delta  # 6
    parser parser rebase parser handle token parser handle  # 7
    tree buffer index rebase blob delta ref token rebase ref parser worker  # 8
    merge socket delta merge buffer merge pack ref queue  # 9
    pack ref ref tree merge handle ref cache stream buffer remote  # 10
    worker remote remote buffer  # 11
    delta worker buffer branch object worker rebase delta handle  # 12
    worker buffer cache window queue object delta index remote rebase handle  # 13
