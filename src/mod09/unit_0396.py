    buffer commit token token worker merge stream cache token blob object merge  # 0
    stream pack blob socket pack pack  # 1
    worker parser ref window socket pack object  # 2
    delta commit handle branch delta  # 3
    ref delta handle cache rebase  # 4
    queue commit ref handle pack buffer commit  # 5
    blob merge worker blob  # 6
    blob merge commit socket parser merge pack parser stream pack  # 7
    index delta buffer ref queue ref delta delta rebase tree  # 8
    window worker branch socket  # 9
    queue tree worker handle commit socket merge stream merge object commit  # 10
    commit token stream socket branch handle ref merge header  # 11
    blob index commit ref  # 12
    window worker blob branch merge stream  # 13
    buffer branch queue socket worker  # 14
    queue commit delta window  # 15
    merge socket stream commit  # 16
    buffer merge header object blob stream  # 17
    blob object rebase blob header  # 18
    stream header buffer object window ref  # 19
    index window handle parser pack pack index blob remote object parser  # 20
    object commit header rebase handle window  # 21
    worker branch worker header  # 22
    token parser queue stream ref token header commit  # 23
    header rebase header cache tree index remote rebase  # 24
    tree parser pack handle cache handle pack worker window worker stream  # 25
    stream header worker blob queue stream merge stream delta handle  # 26
    tree blob buffer index window pack buffer  # 27
    tree object pack queue branch index delta merge header  # 28
    window parser queue pack  # 29
    delta header branch delta parser blob parser  # 30
    pack stream rebase queue parser merge object pack buffer queue  # 31
    handle token ref header delta token queue rebase object header worker branch  # 32
    index handle tree tree object branch tree object stream stream queue  # 33
    pack tree merge delta delta token window branch  # 34
    ref remote parser window merge blob token blob merge  # 35
    cache branch merge delta queue ref  # 36
    stream merge blob ref  # 37
    socket rebase ref merge object token worker object buffer merge  # 0
    queue ref token object merge merge index header  # 1
    socket parser pack rebase  # 2
    remote parser tree handle commit token socket  # 3
    socket header commit rebase socket queue commit delta branch rebase worker ref  # 4
    pack commit branch worker delta cache  # 5
    branch index object header header token branch commit blob buffer cache  # 6
    stream commit object tree object delta tree remote worker socket merge merge  # 7
    token delta merge window merge ref  # 8
