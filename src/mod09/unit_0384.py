    pack window object queue window token worker parser buffer  # 0
    branch object index object merge cache blob object index  # 1
    delta parser window handle branch pack pack rebase  # 2
    token merge socket token window pack  # 3
    blob blob merge object token remote header object blob handle stream blob  # 4
    socket rebase branch window handle branch object  # 5
    rebase buffer worker socket token stream worker window rebase stream buffer handle  # 6
    window object merge tree merge ref branch  # 7
    worker delta worker stream index worker handle parser  # 8
    object handle parser window tree socket handle  # 9
    commit handle object queue pack parser commit handle parser commit buffer  # 10
    parser merge handle merge remote header delta  # 11
    stream pack index blob tree buffer stream index window blob  # 12
    worker ref window pack queue handle  # 13
    object branch rebase socket remote  # 14
    remote tree delta stream cache index  # 15
    parser commit merge blob token pack ref branch  # 16
    object merge stream socket merge token index tree cache merge stream tree  # 17
    stream index token window token  # 18
    cache commit object worker worker delta worker tree  # 19
    pack rebase rebase rebase object worker window handle remote delta  # 20
    handle pack index pack object index ref cache worker branch  # 21
    merge branch ref index window pack blob  # 22
    stream token cache worker commit branch rebase delta worker  # 23
    tree worker remote cache buffer queue index worker delta pack socket  # 24
    delta parser tree ref  # 25
    rebase index commit delta blob queue buffer commit blob merge tree object  # 26
    merge token token buffer merge stream worker  # 27
    cache ref ref blob merge queue  # 28
    buffer branch commit rebase socket socket pack rebase tree  # 29
    object rebase commit commit header  # 30
    tree buffer stream socket  # 31
    worker merge queue object remote handle socket buffer socket merge branch  # 32
    delta pack pack window branch buffer  # 33
    ref handle worker ref remote header stream merge blob cache ref  # 34
    header buffer branch remote  # 35
