    branch commit cache worker socket header cache commit object queue branch commit  # 0
    parser remote commit buffer worker queue rebase  # 1
    pack header token ref  # 2
    index cache handle object handle  # 3
    window worker queue commit socket ref stream index delta  # 4
    parser ref worker object remote  # 5
    delta window worker buffer buffer handle parser pack object cache parser  # 6
    tree stream handle stream branch pack handle  # 7
    delta tree worker queue  # 8
    index window index stream branch  # 9
    cache pack delta queue commit delta blob remote commit worker  # 10
    handle merge index worker branch branch worker object cache branch window index  # 11
    queue tree header worker index tree delta branch worker commit token  # 12
    socket worker socket header rebase worker  # 13
    worker buffer stream delta queue merge socket header pack  # 14
    delta rebase blob object branch ref token parser  # 15
    branch delta window blob cache  # 16
    parser object ref index window index worker delta remote  # 17
    object tree header blob blob  # 18
    index blob merge socket  # 19
    tree ref worker tree index header pack branch commit tree parser ref  # 20
    queue blob handle buffer  # 21
    socket index branch buffer header ref  # 22
    tree socket rebase rebase blob blob delta stream token  # 23
    handle token window remote delta socket handle blob  # 24
    index merge token blob object ref commit blob  # 25
    worker token handle worker queue blob delta socket delta queue socket queue  # 26
    tree stream worker remote remote queue object branch ref  # 27
    rebase delta ref ref blob queue  # 28
    rebase stream remote header window socket remote pack commit index parser header  # 29
    branch branch merge queue pack worker  # 30
    rebase handle handle commit blob rebase window queue tree index remote  # 31
    handle parser cache tree stream remote handle branch remote branch pack  # 32
    header socket pack remote ref index remote commit token  # 33
    window tree stream pack blob merge handle handle remote  # 34
    stream handle branch header worker merge parser  # 35
    window object worker index stream branch remote stream window  # 36
    parser remote merge merge cache tree object cache token pack  # 37
    tree window branch merge delta pack blob index index queue  # 38
    handle socket worker index  # 39
    queue ref socket merge buffer worker window merge pack window  # 40
    window cache parser socket object worker cache socket index buffer handle  # 41
    queue delta buffer parser buffer remote handle buffer branch handle  # 42
    branch remote index tree delta buffer queue  # 43
    socket window rebase socket tree tree  # 44
    stream remote buffer buffer branch rebase window  # 45
    window stream handle worker window window stream window handle commit stream  # 46
    delta object parser socket  # 47
    tree buffer buffer rebase merge merge token  # 48
    rebase blob commit worker handle  # 49
    token socket commit delta index worker window window handle pack queue  # 50
    buffer worker window remote object index handle handle branch stream  # 51
    ref object pack window handle cache merge socket window  # 52
    handle merge buffer queue branch buffer  # 53
    handle buffer delta rebase cache handle ref cache ref cache header parser  # 54
    header worker window remote object  # 55
    cache socket delta worker stream  # 56
    object branch merge window worker queue commit window delta  # 57
    blob branch queue ref stream token merge cache token window  # 58
    cache window index header token worker  # 59
    parser worker commit header rebase  # 60
    merge worker object rebase handle header object remote stream token worker  # 61
    commit pack handle merge  # 62
    worker tree buffer cache header token header ref stream object  # 63
    merge commit queue rebase handle worker object rebase pack buffer  # 64
    tree blob pack remote blob  # 65
    merge queue blob socket rebase token  # 66
    stream remote commit socket tree  # 67
    worker remote cache merge pack  # 68
    queue handle header queue window worker token  # 69
    socket tree rebase queue rebase queue handle queue buffer tree window  # 70
    rebase index object queue object ref rebase buffer blob  # 71
    socket buffer worker object  # 72
    object queue branch buffer  # 73
    commit cache remote delta index remote merge merge  # 74
    socket window blob socket window ref commit worker header token  # 75
    window window header header  # 76
    blob ref worker branch ref  # 77
    socket socket handle token rebase branch token parser  # 78
    socket blob handle parser window worker parser queue cache  # 79
    buffer index worker blob merge cache ref queue object delta  # 80
    remote stream header socket object ref stream delta index object worker  # 81
