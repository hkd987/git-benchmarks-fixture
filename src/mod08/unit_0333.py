    blob token socket window ref pack parser object rebase remote object  # 0
    rebase remote remote blob  # 1
    token blob parser worker header ref buffer ref  # 2
    cache handle remote commit merge remote cache rebase window blob delta commit  # 3
    rebase buffer stream ref buffer merge object stream handle  # 4
    delta stream pack worker object stream  # 5
    cache ref merge pack token branch handle parser parser window ref queue  # 6
    buffer cache stream cache ref cache delta remote queue branch  # 7
    delta blob tree branch window commit rebase  # 8
    ref socket merge window tree  # 9
    stream remote branch remote window rebase worker  # 10
    merge branch socket ref ref window cache buffer blob worker  # 11
    ref parser merge blob cache stream tree pack rebase merge queue socket  # 12
    window ref blob commit header  # 13
    window delta buffer window merge branch object commit header  # 14
    delta pack blob stream header  # 15
    parser handle branch handle branch header socket worker worker object  # 16
    remote cache branch object delta pack parser parser worker window  # 17
    pack token header header  # 18
    tree tree header pack worker remote blob socket  # 19
    index window stream header remote window rebase pack queue window  # 20
    delta handle delta blob stream object header handle stream  # 21
    branch token merge token parser merge worker object tree stream token  # 22
    buffer object ref cache socket tree handle window ref socket worker  # 23
    merge remote index parser blob header  # 24
    cache parser stream merge stream buffer token delta rebase  # 25
    index handle stream object socket remote commit commit stream handle  # 26
    ref queue object tree  # 27
    handle window index window  # 28
    pack blob parser worker header blob blob  # 29
    header pack header merge index worker object merge queue pack pack  # 30
    branch index stream rebase window rebase  # 31
    cache pack rebase branch  # 32
    delta ref socket pack  # 33
    parser tree branch handle object header handle remote delta  # 34
    handle commit commit index blob tree branch queue object  # 35
    stream merge branch pack  # 36
    socket blob parser parser merge  # 37
    tree blob index header object handle ref parser remote tree  # 38
    socket remote remote token worker worker branch remote  # 39
    remote buffer delta parser rebase socket pack buffer index tree merge branch  # 40
    socket cache header ref  # 41
    stream ref socket window parser branch window rebase  # 42
    tree window tree buffer tree parser rebase rebase index  # 43
    socket branch branch delta pack branch parser index  # 44
    socket merge commit index cache header  # 45
    delta worker delta index buffer tree branch commit  # 46
    parser worker delta merge branch rebase branch remote tree worker  # 47
    stream buffer object parser commit buffer index socket rebase buffer  # 48
    stream pack merge delta merge parser index branch ref commit tree socket  # 49
    pack buffer blob socket cache socket parser buffer index blob socket  # 50
    header header tree stream header branch  # 51
    ref branch object remote buffer buffer merge worker  # 52
    handle stream commit handle tree buffer blob blob stream object  # 53
    blob buffer worker queue  # 54
    index pack socket ref cache window blob socket  # 55
    blob tree worker blob blob stream delta commit stream  # 56
    cache tree handle rebase pack  # 57
    rebase merge buffer ref index merge delta window  # 58
    rebase blob commit parser stream ref handle handle stream parser delta ref  # 59
    ref token rebase merge index commit  # 60
    parser worker pack token index remote tree delta  # 61
    object parser worker branch token  # 62
    delta worker remote pack commit  # 63
    header rebase cache pack commit commit rebase delta  # 64
    handle cache cache buffer window commit stream queue delta  # 65
    window object delta cache delta stream window commit buffer tree tree  # 66
    merge rebase index delta  # 67
    queue handle socket header blob blob tree cache window commit rebase delta  # 68
    remote ref token queue parser blob  # 69
    merge queue buffer parser stream tree socket branch stream token  # 70
