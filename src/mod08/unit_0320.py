    delta window object branch  # 0
    parser commit pack object commit tree stream  # 1
    object blob handle ref rebase index  # 2
    pack rebase cache rebase buffer delta rebase remote  # 3
    window token worker cache  # 4
    handle blob remote rebase window tree  # 5
    socket parser parser object socket blob  # 6
    stream worker blob socket cache handle  # 7
    blob header merge object rebase  # 8
    commit socket object window  # 9
    commit branch delta handle blob blob ref delta token cache  # 10
    token header stream merge branch  # 11
    pack window stream window pack parser ref  # 12
    blob handle remote pack commit  # 13
    stream worker delta ref cache buffer merge commit worker  # 14
    blob tree buffer ref stream ref remote tree queue queue  # 15
    pack cache worker index delta  # 16
    pack ref tree object  # 17
    branch pack stream branch rebase pack cache delta token tree branch  # 18
    blob parser queue token handle merge object queue window  # 19
    token header buffer worker worker blob object ref index  # 20
    rebase queue handle remote remote worker  # 21
    blob handle worker merge delta rebase blob  # 22
    index blob parser delta delta cache cache window  # 23
    object tree buffer socket rebase queue token stream ref handle  # 24
    handle rebase parser branch  # 25
    cache stream branch commit index token ref  # 26
    socket rebase stream merge cache pack parser tree queue cache handle  # 27
    delta buffer rebase window ref delta worker delta index  # 28
    tree ref header rebase queue index header delta socket tree  # 29
    stream queue pack object token parser cache queue  # 30
    header cache window worker rebase  # 31
    rebase cache commit parser branch worker remote pack merge blob index window  # 32
    object ref tree token queue token stream socket queue  # 33
    commit handle socket tree handle stream object  # 34
    handle index delta rebase socket socket  # 35
    pack pack handle pack blob blob buffer rebase  # 36
    merge handle object window pack token pack header  # 37
    pack header socket blob handle ref  # 38
    object parser blob token rebase delta index token window stream tree  # 39
    blob cache cache merge branch window handle delta branch  # 40
    rebase merge delta ref merge merge object socket pack  # 41
    window stream ref rebase remote token  # 42
    stream queue object tree window remote pack worker parser blob worker header  # 43
    pack cache delta cache rebase buffer header  # 44
    worker delta tree tree object tree cache commit pack rebase blob socket  # 45
    index remote window remote header blob stream  # 46
    tree buffer buffer queue object tree blob index  # 47
    branch token window merge object tree worker  # 48
    object window tree remote  # 49
    ref branch window rebase stream parser merge window buffer rebase delta cache  # 50
    socket cache window socket cache pack ref  # 51
    branch rebase stream merge token ref  # 52
    handle token buffer merge buffer index  # 53
    object tree socket worker handle socket merge token index header  # 54
    header queue object object commit  # 55
    pack queue branch merge stream token  # 56
    commit queue delta remote tree window pack socket socket window header tree  # 57
    header delta remote pack  # 58
    branch token stream cache tree merge index merge index cache  # 59
    index handle merge delta header header merge  # 60
    worker stream blob pack buffer delta stream ref stream header  # 61
    queue header token index ref branch commit queue header  # 62
    blob branch merge branch object merge stream stream merge buffer  # 63
    worker cache token buffer remote parser handle parser remote  # 64
    token blob header index handle stream remote  # 65
    handle index buffer window cache rebase  # 66
    branch ref merge buffer header cache worker ref  # 67
    delta stream remote delta header delta branch window stream  # 68
    header delta parser ref window buffer socket  # 69
    index buffer stream index  # 70
    header blob ref rebase object token merge  # 71
    queue token worker delta commit commit index index commit handle index  # 72
    pack commit tree ref socket cache worker queue window worker  # 73
    handle token delta socket  # 74
    parser remote object branch cache  # 75
    queue socket stream stream socket delta handle worker  # 76
    handle rebase header rebase window index  # 77
    pack window socket stream queue header rebase queue  # 78
    header window merge stream branch window buffer window ref pack  # 79
    handle remote merge window blob commit merge queue ref  # 80
    parser rebase pack delta window handle  # 81
    queue ref worker merge  # 82
    index blob socket commit handle window  # 83
    window pack cache remote blob blob remote branch  # 84
