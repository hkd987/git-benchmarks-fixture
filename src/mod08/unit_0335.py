    object socket ref merge handle parser parser parser pack branch merge commit  # 0
    rebase blob ref stream ref  # 1
    header cache token cache ref worker window buffer  # 2
    merge buffer stream ref tree cache  # 3
    pack delta cache worker remote object branch token  # 4
    parser stream parser merge  # 5
    branch remote merge delta queue object commit remote  # 6
    branch header rebase pack window parser  # 7
    worker blob window window queue rebase window  # 8
    token rebase branch stream object cache token  # 9
    worker delta buffer pack token rebase parser blob object index  # 10
    worker blob buffer buffer commit ref window object branch delta index  # 11
    header object socket window parser tree tree handle delta socket  # 12
    window token handle tree token header merge parser  # 13
    parser cache window tree merge tree index token  # 14
    index stream merge commit delta header header commit handle merge  # 15
    worker blob branch index ref stream token branch index  # 16
    branch token window pack header merge worker rebase commit cache  # 17
    token blob queue ref buffer object ref index branch cache buffer  # 18
    index remote window header socket header rebase cache  # 19
    header rebase commit branch cache worker ref  # 20
    socket commit tree delta cache  # 21
    tree index merge worker window queue  # 22
    index rebase worker cache header pack remote buffer header header socket  # 23
    blob tree token queue cache cache parser index  # 24
    rebase cache merge header object handle  # 25
    commit token window token socket stream  # 26
    header object blob socket header  # 27
    rebase rebase object pack token  # 28
    handle index blob handle  # 29
    window stream merge cache object parser  # 30
    worker rebase cache parser ref commit buffer  # 31
    stream merge merge delta tree window buffer merge  # 32
    remote remote cache cache delta  # 33
    object worker blob buffer buffer  # 34
    remote object ref cache branch blob  # 35
    commit window delta commit index parser header  # 36
    buffer worker queue blob commit merge tree handle  # 37
    object cache handle delta token header buffer object merge index  # 38
    branch ref queue stream buffer tree pack stream ref queue  # 39
    blob index handle stream  # 40
    remote remote window branch socket queue queue token window index remote  # 41
    cache index delta object buffer  # 42
    buffer pack window delta blob  # 43
    pack token token commit window  # 44
    header header buffer blob ref  # 45
    delta stream stream cache cache object remote pack delta  # 46
    rebase queue pack cache rebase buffer window pack buffer header  # 47
    merge blob merge remote socket remote branch pack index cache pack index  # 48
    object header token rebase socket parser commit blob stream  # 49
    worker remote tree token handle cache token merge  # 50
    branch delta header header cache stream tree queue worker  # 51
    commit queue remote rebase handle cache  # 52
    commit cache branch delta  # 53
    object pack index tree  # 54
    index branch merge blob remote header remote stream branch blob  # 55
    stream commit buffer cache branch stream parser socket cache delta object  # 56
    merge socket socket stream tree pack object buffer pack rebase ref worker  # 57
    worker blob merge tree commit handle tree  # 58
    pack ref window stream index cache  # 59
    tree cache socket parser  # 60
    merge rebase tree cache pack tree worker  # 61
    token queue branch worker queue rebase  # 62
    token parser object window window header  # 63
    buffer socket merge remote branch header delta ref queue remote merge merge  # 64
    branch parser tree tree  # 65
    window merge remote pack cache queue object merge delta rebase branch  # 66
    pack handle worker parser  # 67
    stream commit handle window blob parser remote rebase ref header  # 68
    commit ref socket index worker index rebase token window blob  # 69
