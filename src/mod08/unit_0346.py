    index buffer stream cache index tree ref rebase branch queue commit  # 0
    branch buffer queue buffer socket pack  # 1
    pack stream remote socket cache  # 2
    commit worker branch stream rebase tree token pack object handle blob socket  # 3
    remote worker pack object socket token remote worker delta  # 4
    parser token cache rebase blob merge window branch delta stream remote  # 5
    handle queue header ref remote header worker socket ref  # 6
    worker commit socket handle  # 7
    rebase buffer parser queue token cache delta  # 8
    pack remote ref object worker  # 9
    commit window header buffer buffer rebase queue stream handle handle commit  # 10
    stream socket branch delta branch  # 11
    token worker commit commit tree remote buffer  # 12
    buffer socket buffer delta branch handle pack stream parser window header  # 13
    delta buffer handle header socket branch handle  # 14
    branch header cache tree handle ref  # 15
    pack token queue stream  # 16
    index ref commit parser merge index blob buffer  # 17
    ref object commit tree parser stream ref ref cache header remote branch  # 18
    blob pack index delta header buffer delta  # 19
    commit token worker stream commit cache index  # 20
    commit branch handle remote pack ref cache remote branch index queue parser  # 21
    header buffer ref queue pack window  # 22
    cache tree commit blob ref  # 23
    parser pack rebase commit queue handle rebase buffer stream header buffer  # 24
    pack remote worker handle pack blob  # 25
    parser commit pack parser token cache  # 26
    blob rebase stream pack  # 27
    object blob token queue worker buffer parser ref parser socket  # 28
    ref commit pack merge  # 29
    cache worker merge rebase socket pack header remote merge  # 30
    cache handle window window object stream  # 31
    stream branch socket parser parser blob branch buffer  # 32
    handle window header index  # 33
    worker parser merge socket handle ref worker socket rebase pack blob index  # 34
