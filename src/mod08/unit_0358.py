    branch parser merge cache delta token handle object pack token  # 0
    merge socket commit stream rebase remote header merge token  # 1
    window window worker index  # 2
    queue merge tree stream parser parser buffer  # 3
    object handle buffer ref delta pack parser pack index buffer  # 4
    parser socket ref branch queue rebase tree  # 5
    stream queue remote pack object ref window delta index pack header token  # 6
    pack ref pack commit delta branch  # 7
    delta object window merge queue index socket  # 8
    header rebase worker commit queue worker  # 9
    stream window token index object commit object rebase queue stream token tree  # 10
    object tree window pack handle merge merge cache  # 11
    buffer blob token rebase window token commit ref buffer  # 12
    branch token worker delta rebase rebase worker delta delta token commit  # 13
    delta worker branch pack parser buffer branch cache buffer pack blob  # 14
    blob socket remote remote blob index  # 15
    delta buffer window tree index branch merge commit stream queue cache  # 16
    stream delta parser parser rebase window window branch commit merge blob  # 17
    rebase cache window window window token object commit  # 18
    merge worker commit blob window remote delta  # 19
    remote merge handle parser pack socket pack header  # 20
    handle branch stream queue index ref merge tree commit branch stream worker  # 21
    stream token object branch queue header window pack index merge header  # 22
    object tree rebase merge  # 23
    ref commit worker delta socket commit rebase  # 24
    delta queue stream window rebase  # 25
    ref buffer delta tree socket branch pack ref  # 26
    worker window commit index window commit handle index rebase parser buffer  # 27
    token delta window object queue  # 28
    stream merge commit remote cache worker cache socket  # 29
    commit ref merge object tree socket cache remote handle delta pack  # 30
    header queue commit token stream cache  # 31
    branch object pack worker branch socket  # 32
    ref index handle cache  # 33
    worker buffer worker branch object header  # 34
    header worker parser blob stream  # 35
    parser blob worker tree rebase stream handle delta blob socket  # 36
    tree blob window tree header commit object parser index header parser  # 37
    worker buffer socket rebase parser ref object remote  # 38
    commit tree header buffer queue index branch worker branch commit object  # 39
    commit socket socket delta remote  # 40
    stream header rebase branch index pack rebase handle index delta  # 41
    parser socket pack commit queue branch  # 42
    token handle blob commit branch  # 43
    stream handle queue pack  # 44
    ref worker socket object window object branch  # 45
    stream stream remote token worker cache cache buffer pack token header  # 46
    pack delta socket token delta remote remote  # 47
    buffer header tree pack  # 48
    tree object socket delta merge rebase  # 49
    tree pack parser branch rebase remote stream remote handle  # 50
    header queue rebase ref  # 51
    worker remote socket tree  # 52
    merge merge cache object worker parser handle handle commit header buffer delta  # 53
    merge remote token stream  # 54
    delta branch worker branch header rebase stream stream buffer  # 55
    header delta object merge  # 56
    branch worker token commit rebase branch cache blob  # 57
    stream delta header buffer parser branch delta header commit stream token  # 58
    header queue rebase branch  # 0
    queue pack merge commit object blob header  # 1
    tree rebase parser pack window pack merge buffer queue branch window worker  # 2
    tree token commit cache merge pack worker  # 3
    commit object token socket delta handle  # 4
    header pack stream delta  # 5
    socket parser delta remote socket delta rebase queue tree header parser delta  # 6
    handle buffer index rebase cache  # 7
    commit object object stream cache rebase branch commit queue rebase  # 8
    branch pack parser window queue  # 9
    merge window tree commit  # 0
    ref branch pack buffer merge commit blob  # 1
    worker ref commit buffer parser tree header object stream  # 2
    blob merge stream buffer merge worker index tree tree  # 3
    header remote handle handle header  # 4
    rebase parser rebase token worker token buffer rebase parser  # 5
    token merge delta delta ref buffer queue remote delta header token index  # 6
    branch commit window queue delta  # 7
