    header tree tree handle tree buffer worker commit worker parser delta  # 0
    token ref worker remote header  # 1
    window window buffer token rebase token header cache buffer worker  # 2
    header object rebase remote queue cache  # 3
    buffer pack tree window object blob object  # 4
    token object parser socket object rebase  # 5
    handle rebase cache buffer remote  # 6
    cache queue window worker rebase worker socket tree cache merge  # 7
    header queue index cache commit branch window  # 8
    cache delta merge socket ref worker  # 9
    branch handle index handle stream socket header token stream  # 10
    commit worker token tree pack object pack buffer parser handle  # 11
    handle window index header cache worker pack delta parser  # 12
    remote buffer buffer cache blob socket rebase token  # 13
    buffer cache merge header blob delta  # 14
    commit stream socket rebase queue delta tree parser  # 15
    rebase index merge cache stream tree token header tree index index  # 16
    delta pack delta object handle token remote rebase worker stream buffer  # 17
    remote blob queue token worker handle header queue rebase merge rebase  # 18
    parser pack remote ref worker  # 19
    parser stream object index buffer merge pack window pack  # 20
    pack object remote worker queue queue  # 21
    delta window merge parser pack rebase rebase buffer branch token cache blob  # 22
    cache socket blob stream delta branch merge buffer  # 23
    token blob branch delta delta  # 24
    object rebase branch blob tree  # 25
    ref worker index branch parser object index  # 26
    socket merge window cache worker token token index  # 27
    buffer cache pack branch  # 28
    rebase branch cache pack stream  # 29
    parser handle token branch index tree ref delta commit ref  # 30
    worker merge stream delta queue object stream token cache  # 31
    branch handle pack stream stream commit pack stream buffer queue handle  # 32
    merge blob rebase commit branch queue window window pack index pack  # 33
    worker branch blob rebase queue  # 34
    handle pack handle stream stream handle worker tree  # 35
    commit buffer token blob tree socket pack tree pack  # 36
    parser socket socket handle object socket merge cache stream  # 37
    tree cache index index merge index header handle branch  # 38
    queue window merge socket socket index branch worker  # 39
    delta parser branch stream merge delta branch parser  # 40
    token worker stream window window socket  # 41
    blob cache object commit commit window ref index  # 42
    index rebase tree socket remote worker header parser object remote window buffer  # 43
    pack window worker queue merge stream worker blob remote  # 44
    pack merge remote blob tree ref token cache window handle blob worker  # 45
    commit ref queue branch tree handle rebase worker parser  # 0
    stream stream socket tree socket remote handle rebase worker  # 1
    pack object index commit remote blob tree merge  # 2
    tree remote delta commit branch blob socket window header  # 3
