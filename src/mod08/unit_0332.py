    cache parser queue stream merge branch token rebase token index  # 0
    buffer buffer token buffer stream window index buffer  # 1
    parser delta tree ref merge merge parser  # 2
    queue buffer ref tree tree index token cache remote worker queue delta  # 3
    remote ref pack branch  # 4
    buffer stream header pack  # 5
    cache worker window pack cache stream  # 6
    delta delta merge commit window object index socket worker handle object  # 7
    delta tree worker commit index header handle header remote header  # 8
    parser delta pack rebase delta worker  # 9
    queue parser delta socket handle handle merge token buffer pack tree  # 10
    worker delta tree token object socket tree object index token index buffer  # 11
    stream pack ref branch delta socket worker cache buffer rebase window tree  # 12
    socket blob delta merge parser header index stream  # 13
    pack socket remote ref buffer pack buffer object index remote object  # 14
    pack object stream parser object merge index socket  # 15
    merge token stream rebase tree socket buffer  # 16
    handle object merge remote branch blob window buffer pack cache  # 17
    object handle handle stream ref window delta  # 18
    worker cache pack pack worker delta pack socket  # 19
    ref socket queue delta object blob blob token cache queue  # 20
    object worker rebase commit token pack pack handle cache rebase remote  # 21
    remote cache token socket index queue rebase  # 22
    stream cache handle stream parser  # 23
    object object socket pack header branch buffer merge index handle  # 24
    merge commit ref worker pack  # 25
    delta stream buffer merge worker queue cache tree header stream worker  # 26
    buffer pack window merge blob branch branch header ref ref remote  # 27
    delta merge rebase object object  # 28
    token object handle worker socket queue commit  # 29
    buffer delta worker worker commit  # 30
    branch branch blob tree branch buffer socket blob  # 31
    header index token header commit  # 32
    queue index merge socket header header rebase  # 33
    object ref token ref remote  # 34
    index merge ref pack handle blob commit remote  # 0
    parser blob cache worker  # 1
    queue blob buffer stream merge buffer index merge handle  # 2
