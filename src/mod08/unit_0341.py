    handle socket blob handle  # 0
    tree branch handle cache merge handle cache token index token  # 1
    delta ref parser token  # 2
    object pack delta token buffer cache merge index  # 3
    object merge delta tree object  # 4
    socket object rebase cache cache handle pack object socket queue pack  # 5
    commit remote pack merge socket parser pack queue merge  # 6
    index merge worker socket blob delta stream object blob remote  # 7
    rebase delta stream socket merge  # 8
    blob socket window handle tree blob worker  # 9
    handle branch parser blob cache window header handle rebase tree commit handle  # 10
    stream merge header cache queue cache handle commit pack blob cache index  # 11
    stream remote tree remote  # 12
    blob header stream worker object  # 13
    header merge branch handle remote cache parser  # 14
    queue window merge window stream commit worker handle stream pack rebase  # 15
    stream socket commit cache  # 16
    stream object merge parser queue queue branch  # 17
    header index blob merge header commit  # 18
    ref worker branch merge branch object cache  # 19
    remote worker blob rebase cache tree token cache  # 20
    socket header tree merge worker window header index stream buffer  # 21
    parser header index blob handle stream header  # 22
    tree buffer pack stream worker  # 23
    commit socket parser remote worker handle  # 24
    pack stream pack merge parser worker header  # 25
    object parser header worker blob parser cache buffer handle ref rebase  # 26
    cache queue stream parser merge blob  # 27
    ref window queue window  # 28
    socket ref branch commit stream buffer buffer ref  # 29
    socket remote delta cache handle blob tree rebase header rebase  # 30
    tree queue ref pack queue socket stream ref tree ref socket cache  # 31
    object index socket remote branch queue ref remote delta  # 32
    queue rebase token pack socket token cache cache window merge handle  # 33
    tree parser pack rebase object  # 34
    token tree merge rebase remote  # 35
    rebase queue ref remote  # 36
    delta header worker worker merge merge merge window index remote object  # 37
    remote remote token commit commit object index  # 38
    window rebase commit delta delta branch  # 39
    handle object remote remote window  # 40
    ref remote blob header delta token queue token header cache commit buffer  # 41
    stream window header buffer branch rebase tree merge blob header  # 42
    queue tree stream merge remote handle cache index window blob  # 43
    handle window buffer ref  # 44
    stream tree delta handle  # 45
    tree rebase merge worker delta token window object handle tree worker  # 46
    window parser socket remote index blob header rebase queue index  # 47
    delta stream socket tree parser window header socket delta merge worker  # 48
    queue handle remote window merge commit handle handle stream handle remote buffer  # 49
    handle tree queue object remote token rebase  # 50
    token stream ref commit worker cache tree queue  # 51
    commit window blob merge handle tree  # 52
    object window header branch stream rebase token  # 53
    blob merge commit buffer remote delta  # 54
    buffer object merge stream merge token blob socket buffer socket index rebase  # 55
    worker rebase index queue delta worker rebase window pack worker ref  # 56
    socket commit index branch blob ref branch queue merge parser rebase  # 57
    worker index delta index cache commit cache cache  # 58
    parser socket cache ref merge delta blob parser pack window header  # 59
    blob window stream object rebase worker object token buffer branch  # 0
    merge commit handle socket branch cache ref tree socket  # 1
    token parser queue pack token merge queue index  # 2
    rebase remote stream ref cache remote ref socket socket object  # 3
    remote cache buffer window delta header ref commit tree  # 4
    branch rebase pack commit  # 5
