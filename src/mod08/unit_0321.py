    merge delta window token object buffer delta token blob pack socket buffer  # 0
    object commit pack rebase  # 1
    buffer stream object parser object pack commit cache  # 2
    parser parser ref buffer index merge pack  # 3
    tree socket merge worker socket parser blob queue window object cache  # 4
    window rebase queue commit pack rebase blob index worker  # 5
    delta cache socket delta pack branch delta cache  # 6
    handle ref parser rebase  # 7
    ref object token socket branch cache buffer  # 8
    parser commit index remote window  # 9
    socket window delta object commit object stream ref remote pack remote commit  # 10
    commit index commit window handle worker ref  # 11
    merge parser remote pack tree blob worker token buffer cache cache  # 12
    delta worker handle pack cache delta object pack cache window token pack  # 13
    rebase index tree socket parser parser index index header socket header  # 14
    stream object index tree delta header pack index branch commit parser rebase  # 15
    token rebase blob branch handle buffer parser  # 16
    header parser ref pack pack object merge queue handle  # 17
    delta cache blob parser remote parser buffer pack header index header  # 18
    worker remote worker ref handle parser ref blob remote token socket blob  # 19
    ref queue handle worker blob queue window queue rebase handle token rebase  # 20
    tree stream object remote  # 21
    worker merge cache worker delta  # 22
    token worker tree stream header remote ref parser token blob  # 23
    cache commit remote object commit parser  # 24
    commit worker merge merge window header blob remote cache blob  # 25
    remote token remote worker  # 26
    cache ref stream rebase pack  # 27
    blob cache queue socket delta tree  # 28
    merge tree merge cache index  # 29
    queue commit pack buffer header rebase worker tree blob commit handle  # 30
    cache branch socket remote branch header  # 31
    blob window parser queue buffer commit branch worker window merge rebase  # 32
    pack pack socket cache object window ref pack index remote  # 33
    index delta window window handle merge delta rebase tree pack commit  # 34
    object pack token remote worker worker  # 35
    tree buffer parser remote delta header object ref  # 36
    stream rebase remote merge  # 37
    object worker commit delta window commit object rebase cache ref  # 38
    index blob index branch pack worker socket tree  # 39
    pack queue tree merge  # 40
    stream queue ref token stream tree remote cache stream window commit stream  # 41
    delta rebase tree queue branch stream  # 42
    cache tree handle ref tree index  # 43
    rebase buffer worker queue parser worker  # 44
    object stream header stream  # 45
    handle delta branch cache buffer header worker  # 46
    queue index stream socket commit index object delta  # 47
    queue remote buffer buffer parser  # 48
    socket rebase rebase buffer token stream header branch delta  # 49
    tree window remote cache object tree  # 50
    token tree parser rebase blob commit queue header blob commit pack  # 51
    tree buffer pack remote merge tree  # 52
    window token remote tree tree pack  # 53
    pack stream queue blob queue handle  # 54
    index stream blob handle merge branch rebase pack stream ref  # 55
    object object commit commit commit blob header window header pack socket  # 56
    merge remote commit merge blob ref stream parser  # 57
    blob commit token token socket  # 58
    tree cache ref parser socket delta  # 59
    delta header pack rebase token stream socket branch worker stream commit blob  # 60
    worker stream pack cache delta queue  # 61
    worker tree remote handle branch object delta  # 62
    blob token blob queue window socket branch ref  # 63
    tree stream worker queue delta buffer  # 64
    parser ref index buffer buffer cache cache window rebase branch socket  # 65
    merge handle ref tree delta worker handle index queue merge tree parser  # 66
    queue index object object branch socket pack blob  # 67
    worker window buffer merge branch window window stream stream  # 68
    ref buffer commit ref header worker rebase tree  # 69
    parser buffer merge parser  # 70
    buffer pack cache window index  # 71
    worker window rebase worker window worker object  # 72
    branch pack stream stream object index header header parser  # 73
    branch index tree pack index rebase ref object queue delta branch buffer  # 74
    pack header index handle merge branch merge  # 75
    index header commit blob  # 76
    handle object buffer remote remote object window pack delta socket  # 77
    ref header ref blob tree window queue delta stream cache queue rebase  # 78
    handle handle handle rebase stream stream index socket index  # 79
    commit ref merge buffer buffer rebase index socket  # 80
    parser token commit handle commit cache  # 81
