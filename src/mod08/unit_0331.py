    index remote token rebase  # 0
    delta queue tree blob queue  # 1
    header worker worker worker cache index object queue object commit  # 2
    worker branch queue blob branch  # 3
    index merge buffer token  # 4
    merge pack parser cache blob ref delta  # 5
    cache stream header parser rebase commit worker socket  # 6
    delta delta remote commit rebase stream index  # 7
    object delta queue pack blob  # 8
    merge tree merge commit merge parser index ref window  # 9
    stream index merge header parser object buffer tree header pack object socket  # 10
    queue commit blob header commit merge header  # 11
    stream merge rebase branch queue worker token branch cache commit  # 12
    object parser window buffer buffer window index  # 13
    delta handle parser queue socket handle  # 14
    tree commit commit merge commit rebase  # 15
    commit branch remote object cache queue socket queue  # 16
    commit merge rebase rebase delta object ref parser ref  # 17
    worker header merge handle parser blob queue  # 18
    parser commit delta tree cache remote cache window index object blob rebase  # 19
    blob commit worker object buffer window pack  # 20
    object header branch index delta index socket  # 21
    parser worker cache commit queue token merge commit  # 22
    commit commit merge token token handle index delta  # 23
    worker socket cache commit index blob delta branch  # 24
    token stream ref buffer commit remote parser handle parser ref delta socket  # 25
    object delta cache merge parser worker rebase buffer  # 26
    window branch token merge window remote buffer queue  # 27
    ref commit commit merge remote delta cache rebase pack merge  # 28
    rebase branch socket window socket pack commit remote buffer index stream  # 29
    handle branch stream stream commit ref  # 30
    handle buffer blob index object worker object tree queue  # 31
    tree blob pack token handle remote merge parser  # 32
    index header index parser cache delta  # 33
    header blob rebase handle socket pack pack cache header remote  # 34
    remote worker commit tree commit merge rebase blob commit rebase  # 35
    blob rebase commit window pack stream socket buffer socket stream parser  # 36
    blob index window queue stream rebase ref handle delta  # 37
    merge remote blob socket queue  # 38
    ref parser parser commit queue cache token handle object  # 39
    delta worker window queue merge stream tree delta rebase header worker  # 40
    buffer cache delta delta pack parser delta blob  # 41
    rebase delta remote worker pack  # 42
    rebase tree rebase tree ref token queue parser pack index socket  # 43
    blob rebase index header parser object worker remote remote  # 44
    index cache window branch window socket index handle parser ref  # 45
