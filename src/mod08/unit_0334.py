    commit ref pack delta socket header blob stream cache  # 0
    socket stream delta merge remote  # 1
    merge queue index parser index tree index cache pack cache  # 2
    cache commit socket worker cache header stream commit handle commit  # 3
    commit branch merge pack token socket worker object delta worker  # 4
    buffer remote pack buffer commit stream commit blob socket ref object merge  # 5
    stream handle worker remote pack object delta socket remote buffer handle remote  # 6
    handle queue remote token remote buffer blob tree stream index remote  # 7
    stream stream token rebase parser commit pack socket rebase merge delta worker  # 8
    branch handle worker object queue buffer merge delta handle header  # 9
    socket pack worker pack blob branch socket object delta commit token delta  # 10
    index tree header blob buffer branch remote index worker tree merge  # 11
    worker stream object buffer merge cache blob blob rebase cache token remote  # 12
    object parser socket delta tree object buffer object branch object handle  # 13
    tree delta socket parser socket token blob  # 14
    socket buffer remote blob buffer rebase worker handle handle delta merge rebase  # 15
    merge rebase window parser handle merge ref handle  # 16
    rebase window cache object token stream worker blob blob token  # 17
    ref cache branch worker stream handle remote remote remote commit  # 18
    handle remote pack rebase cache  # 19
    pack cache tree ref pack parser parser  # 20
    index remote object parser window pack index remote delta  # 21
    window buffer window index socket socket parser ref commit ref  # 22
    blob pack socket remote handle  # 23
    window token branch merge blob blob remote branch  # 24
    delta commit window token buffer blob cache ref commit commit token  # 25
    tree rebase ref delta  # 26
    header buffer handle token rebase parser ref socket header  # 27
    rebase index buffer cache remote socket merge remote rebase rebase header  # 28
    buffer blob merge stream worker delta  # 29
    worker buffer parser header handle object commit queue  # 30
    queue handle cache ref worker buffer pack stream  # 31
    index handle token handle ref  # 32
    commit object index buffer  # 33
    blob commit worker parser buffer  # 34
    window parser delta window rebase stream  # 35
    handle index remote delta pack handle handle remote object queue  # 36
    ref delta merge delta stream delta  # 37
    worker token window queue  # 38
    pack ref ref window queue window rebase header parser blob header  # 39
    index cache merge delta pack window pack cache cache  # 40
    branch worker branch ref queue  # 41
    branch stream commit queue branch cache  # 42
    handle socket cache header buffer parser merge queue token queue index  # 43
    branch index tree remote ref  # 44
    token socket commit ref window socket  # 45
    worker buffer parser merge queue token index pack  # 46
    ref handle socket ref stream socket token  # 47
    header ref queue window buffer rebase worker index cache stream  # 48
    branch pack rebase stream  # 49
    cache socket delta buffer ref merge tree ref ref cache merge  # 50
    handle header handle pack socket buffer stream socket window queue worker header  # 51
    window parser delta merge merge handle object merge merge buffer  # 52
    parser blob worker remote cache cache delta buffer  # 53
    cache index stream header  # 54
    blob commit branch buffer delta parser stream header token remote  # 55
    index socket token buffer buffer blob pack buffer blob  # 56
    blob pack delta parser  # 57
    header tree stream cache stream  # 58
    window merge window parser delta buffer rebase index stream commit  # 59
    merge window parser ref cache tree tree pack window rebase ref  # 60
    branch socket header pack merge socket handle handle  # 61
    header object remote tree branch token  # 62
    buffer remote stream worker cache  # 63
    queue socket remote handle  # 64
