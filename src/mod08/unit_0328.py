    worker buffer delta tree commit worker merge token parser index remote object  # 0
    worker buffer header remote  # 1
    object delta stream pack  # 2
    delta rebase worker socket merge header  # 3
    commit queue parser buffer delta commit tree ref parser buffer queue  # 4
    queue ref header buffer commit buffer  # 5
    remote branch merge socket merge header blob remote window tree commit tree  # 6
    rebase tree handle tree remote handle header  # 7
    object worker delta handle rebase  # 8
    branch ref pack index object handle branch worker ref header branch  # 9
    cache window pack parser cache commit blob header  # 10
    worker handle ref window  # 11
    cache cache parser cache buffer stream object commit tree ref  # 12
    stream pack cache window token cache queue handle  # 13
    pack tree cache object handle branch index blob cache  # 14
    branch object object remote object tree  # 15
    window commit socket parser blob stream ref rebase blob window  # 16
    header token cache remote tree pack rebase parser branch delta branch  # 17
    commit pack parser blob header queue ref stream buffer object  # 18
    token remote tree buffer queue queue rebase remote cache delta cache remote  # 19
    stream header buffer ref token window window delta  # 20
    pack queue header commit  # 21
    socket cache commit delta  # 22
    socket socket stream buffer commit buffer parser tree header object  # 23
    handle ref pack worker queue delta socket handle socket object index delta  # 24
    handle stream commit object header branch stream handle socket remote commit worker  # 25
    branch header cache window pack  # 26
    token index parser parser index  # 27
    parser tree socket stream header window  # 28
    handle stream merge delta tree token branch  # 29
    cache queue parser cache pack stream worker rebase parser ref  # 30
    branch token token commit worker header parser pack queue  # 31
    header pack commit branch delta  # 32
    buffer queue remote index  # 33
    tree header buffer window rebase remote header remote tree buffer  # 34
    rebase queue merge merge  # 35
    branch socket blob remote remote commit commit window  # 36
    socket parser header token delta worker  # 37
    delta queue delta object worker cache merge worker rebase  # 38
    ref object merge pack  # 39
    index merge socket stream blob branch merge  # 40
    rebase object token stream ref blob parser token  # 41
    tree delta socket queue tree worker  # 42
    index pack pack delta  # 43
    blob cache branch tree  # 44
    socket index socket object token  # 45
    header branch window branch socket header buffer token  # 46
    ref ref commit index  # 47
    cache window parser pack cache  # 48
    object parser blob remote stream object  # 49
    window object merge index  # 50
    buffer stream buffer pack queue rebase parser header socket  # 51
    object window blob buffer  # 52
    remote ref rebase blob  # 53
    branch window rebase commit remote  # 54
    index header remote token  # 55
    parser window token branch header  # 56
    commit index merge object index handle cache header socket commit branch pack  # 57
    parser token tree blob header remote header  # 58
    buffer delta parser tree rebase window window remote header  # 59
    cache token pack queue index rebase  # 60
    branch cache delta header remote queue pack ref  # 61
    rebase branch buffer merge remote remote rebase  # 62
    commit pack ref object handle  # 63
    tree merge worker merge cache worker  # 64
    cache branch object index ref rebase window ref rebase rebase delta parser  # 65
    token pack worker socket buffer index buffer delta blob cache  # 66
    object merge stream tree tree object window stream ref  # 67
    ref socket branch index tree delta worker branch index parser buffer  # 68
    ref worker merge header delta worker cache queue stream branch  # 69
    object handle tree commit blob parser merge token queue socket socket  # 70
