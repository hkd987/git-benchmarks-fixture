    ref rebase remote remote socket socket worker ref pack  # 0
    stream object delta remote branch  # 1
    commit blob commit queue stream object socket header  # 2
    rebase delta branch parser worker worker pack socket parser token  # 3
    buffer branch parser window commit rebase buffer buffer queue  # 4
    header branch ref commit ref header window rebase stream  # 5
    parser blob parser handle pack index remote  # 6
    token merge commit blob rebase socket token token  # 7
    tree merge worker object  # 8
    header queue stream remote object tree worker delta remote commit  # 9
    stream object tree ref pack remote index header pack  # 10
    worker handle header remote blob ref handle worker  # 11
    stream token tree parser rebase  # 12
    window branch rebase tree tree object handle rebase token  # 13
    queue stream merge pack merge handle pack buffer commit  # 14
    stream worker queue header token ref blob  # 15
    token merge index pack rebase queue pack  # 16
    branch ref ref worker header remote header commit pack parser header  # 17
    window tree buffer socket socket window handle stream  # 18
    socket buffer queue merge token cache rebase  # 19
    index parser token remote  # 20
    commit ref index merge merge remote parser handle cache parser remote worker  # 21
    pack object merge commit queue token delta pack  # 22
    queue handle merge socket window branch index commit object  # 23
    object cache tree socket buffer index stream remote socket index  # 24
    pack tree blob object worker ref queue socket delta handle socket merge  # 25
    socket blob ref remote  # 26
    parser index queue socket rebase stream index  # 27
    stream remote buffer stream  # 28
    index worker merge index worker  # 29
    index tree socket rebase parser tree object buffer  # 30
    token cache rebase tree  # 31
    header remote object blob parser tree  # 32
    branch cache index rebase ref parser  # 33
    merge buffer rebase cache merge socket parser remote token socket parser object  # 34
    window pack stream remote handle  # 35
    window window object queue window window socket ref delta delta stream object  # 36
    commit buffer parser handle parser parser commit queue token branch buffer window  # 0
    buffer delta token ref tree window tree tree token token ref buffer  # 1
    object merge pack remote blob pack parser branch header blob rebase  # 2
    socket window tree token blob  # 0
    queue blob parser worker tree stream buffer object blob remote header  # 1
    header parser rebase socket header queue stream tree queue buffer handle rebase  # 2
    pack commit rebase header handle buffer commit stream token  # 3
    buffer parser header token  # 4
    delta branch pack stream socket  # 5
    blob socket socket socket window pack buffer  # 6
    remote rebase blob header index index merge worker rebase cache  # 7
    delta ref merge index blob remote header blob delta index  # 8
    tree remote parser token handle  # 9
