    socket handle window stream socket handle window branch ref ref stream  # 0
    window blob cache cache queue queue pack ref object branch socket  # 1
    ref tree commit buffer branch stream remote pack  # 2
    cache ref socket socket window commit branch cache index  # 3
    queue buffer delta blob pack cache window pack buffer handle index socket  # 4
    rebase delta blob parser tree token window worker merge buffer parser  # 5
    queue socket remote blob queue stream  # 6
    cache window index buffer branch merge merge  # 7
    handle window tree merge blob tree handle pack queue commit  # 8
    token remote rebase ref  # 9
    tree stream socket token rebase window commit worker  # 10
    worker ref blob object object index header worker stream rebase  # 11
    handle tree queue remote handle  # 12
    socket buffer commit handle  # 13
    index rebase tree commit worker header worker stream socket  # 14
    remote worker stream stream token branch  # 15
    index socket socket branch ref header cache  # 16
    merge cache commit index tree delta merge buffer commit parser branch  # 17
    socket stream queue index tree delta  # 18
    header index tree buffer branch queue index buffer commit stream remote parser  # 19
    delta cache parser object cache  # 20
    ref socket parser blob ref object commit index cache  # 21
    index cache window token commit merge remote worker  # 22
    pack cache parser rebase rebase delta blob merge  # 23
    cache parser commit buffer delta header remote socket  # 24
    tree cache parser delta token index merge tree index queue tree parser  # 25
    socket ref buffer handle ref pack stream tree  # 26
    commit rebase worker parser tree parser  # 27
    token stream blob window merge handle token tree pack worker  # 28
    rebase branch tree parser buffer queue stream ref rebase handle  # 29
    header buffer buffer branch ref delta blob merge delta handle handle pack  # 30
    branch parser delta index parser parser queue ref handle delta blob header  # 31
    commit token worker window  # 32
    socket remote window parser index branch blob commit queue socket token  # 33
    blob cache commit delta ref blob stream blob remote stream remote  # 34
    ref object rebase worker pack ref rebase parser token remote rebase blob  # 35
    index handle rebase header ref worker blob branch delta pack cache handle  # 36
    worker blob pack object cache merge tree blob worker object buffer pack  # 37
    header queue branch worker socket window cache stream ref index  # 38
    blob blob blob queue worker remote tree header commit branch commit token  # 39
    delta stream remote rebase  # 40
    stream delta parser handle handle header header header tree pack worker  # 41
    branch rebase branch cache delta merge  # 42
    handle commit object remote parser branch handle remote  # 43
    queue worker delta window blob worker blob  # 44
    token merge remote blob commit queue socket blob index  # 45
    object queue stream socket commit parser pack header  # 46
    worker ref cache object  # 47
    ref object remote parser header handle parser ref  # 48
    stream blob parser buffer blob handle  # 49
    object parser rebase handle header  # 50
    rebase header socket buffer token handle object index parser header  # 51
    tree buffer remote socket branch token  # 52
    socket object blob blob branch merge handle socket socket worker window worker  # 53
    buffer blob queue merge parser merge index branch commit delta object handle  # 54
    buffer ref socket window remote worker worker delta window  # 55
    handle socket tree socket index commit  # 56
    object remote window window  # 57
    token parser delta cache merge index  # 58
    blob buffer token delta buffer handle buffer ref socket header merge handle  # 59
    index commit queue tree merge stream merge handle  # 60
    commit index worker branch pack window object commit worker commit  # 61
    tree index rebase worker index socket worker stream parser parser worker blob  # 62
    token merge tree window  # 63
    buffer pack parser rebase blob  # 64
    ref rebase queue blob branch  # 65
    worker token window remote  # 66
    pack pack pack delta buffer commit worker token worker  # 67
    object branch parser token token delta  # 68
    tree rebase remote rebase delta branch parser window cache header parser  # 69
    stream delta window cache stream remote queue merge  # 70
    index ref blob stream branch commit blob parser cache  # 71
    tree ref header rebase buffer delta token delta token socket index  # 72
    branch queue buffer tree pack merge buffer tree stream  # 73
    delta delta cache index object rebase stream  # 74
    object remote cache branch delta cache stream  # 75
    tree header blob object header remote remote  # 76
    tree pack delta handle ref header  # 77
    queue delta tree worker worker blob  # 78
    stream blob object object delta branch pack merge stream buffer index branch  # 79
    window object branch remote rebase delta pack handle  # 80
