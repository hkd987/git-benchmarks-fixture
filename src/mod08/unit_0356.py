    buffer rebase buffer ref branch header window  # 0
    queue pack queue header stream object token  # 1
    delta rebase merge cache ref index header handle header socket  # 2
    queue branch handle cache token  # 3
    parser token buffer merge parser socket remote queue blob handle token  # 4
    remote commit branch parser buffer branch branch index  # 5
    commit rebase handle delta branch header object  # 6
    pack pack stream remote tree cache tree pack cache pack worker  # 7
    object remote rebase index window object worker commit handle parser  # 8
    delta queue token branch branch handle rebase parser commit buffer handle worker  # 9
    header rebase object blob token rebase commit object  # 10
    worker blob window delta  # 11
    rebase socket ref window token parser commit  # 12
    cache window socket window index merge rebase merge ref  # 13
    blob header commit window buffer cache stream token tree blob  # 14
    token worker rebase token merge  # 15
    cache window delta remote merge delta pack window rebase window  # 16
    object parser handle buffer  # 17
    delta object handle index token stream stream  # 18
    ref queue header buffer header blob token worker  # 19
    blob stream queue queue index merge socket queue  # 20
    parser parser tree rebase token tree commit  # 21
    rebase socket merge index blob  # 22
    socket tree parser stream ref index  # 23
    pack queue commit window commit merge socket  # 24
    queue cache buffer blob header branch object handle merge socket tree commit  # 25
    pack blob buffer commit cache queue pack merge ref header window  # 26
    ref blob socket ref merge rebase cache cache  # 27
    rebase commit cache worker delta  # 28
    handle window blob queue pack tree parser window worker  # 29
    rebase rebase index socket queue  # 30
    blob cache buffer header pack pack pack queue index  # 31
    index blob buffer buffer commit  # 32
    header object stream delta  # 33
    commit index merge queue worker commit worker window  # 34
    worker pack merge buffer window stream  # 35
    merge remote pack branch index merge delta  # 36
    branch delta blob pack cache window queue rebase remote  # 37
    rebase worker delta window ref socket stream window socket  # 38
    rebase ref cache merge  # 39
    header delta stream socket pack commit remote commit  # 40
    stream tree index parser header blob queue  # 41
    branch object tree token commit blob delta window  # 42
    remote branch handle delta token merge socket ref  # 43
    window blob commit rebase tree branch  # 44
    worker buffer socket ref  # 45
    blob header pack ref cache handle delta token pack window  # 46
    handle blob token parser handle window worker  # 0
    cache buffer index commit stream tree window stream ref header stream  # 1
    cache socket buffer remote handle tree cache window buffer worker pack  # 2
    handle branch token cache handle ref worker rebase rebase merge cache parser  # 3
    buffer rebase worker handle blob tree delta token rebase stream  # 4
    token buffer index handle tree handle branch cache merge merge worker worker  # 5
    worker header pack remote queue worker pack rebase token  # 6
    index queue token worker branch merge buffer delta window window index  # 7
    index delta stream window buffer socket blob handle commit header  # 8
