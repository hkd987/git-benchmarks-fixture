    object parser window index header delta handle buffer blob object  # 0
    worker ref rebase socket tree object token index pack rebase  # 1
    commit header object buffer worker branch window window branch pack pack  # 2
    token queue blob object header merge object object  # 3
    queue commit window branch  # 4
    blob worker delta header rebase window window  # 5
    merge handle cache window blob token queue tree index rebase  # 6
    window cache window delta handle buffer branch ref  # 7
    delta blob object merge socket worker parser parser blob cache  # 8
    blob queue ref remote stream  # 9
    queue object merge buffer header pack delta ref blob merge blob  # 10
    commit index delta object parser  # 11
    merge worker object delta  # 12
    socket queue cache tree blob remote window handle branch parser  # 13
    queue tree buffer parser header  # 14
    handle commit pack cache delta window window worker object ref queue  # 15
    worker buffer token delta cache delta  # 16
    blob stream tree window branch branch branch window  # 17
    remote buffer remote blob worker tree queue worker merge queue handle buffer  # 18
    merge buffer worker tree remote blob  # 19
    object ref window queue  # 20
    queue commit blob queue remote merge socket tree commit handle  # 21
    commit cache buffer branch tree  # 22
    stream buffer delta header header handle header  # 23
    rebase handle socket header ref commit token tree cache buffer queue handle  # 24
    stream buffer delta rebase token pack parser pack header parser stream  # 25
    blob commit socket socket worker index merge  # 26
    queue pack ref stream worker cache parser remote buffer index delta  # 27
    remote header branch remote cache tree header stream queue stream delta  # 28
    token header rebase pack rebase branch queue queue window branch queue  # 29
    parser blob commit buffer  # 30
