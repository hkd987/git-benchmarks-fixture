    merge commit remote queue commit delta tree index delta  # 0
    remote object buffer worker  # 1
    rebase pack parser stream header buffer remote index commit  # 2
    socket token delta branch merge blob stream ref ref token token tree  # 3
    commit object merge branch rebase tree queue cache  # 4
    buffer socket token index remote buffer remote queue  # 5
    parser branch handle handle delta window stream  # 6
    stream rebase branch branch handle commit ref socket branch  # 7
    header header rebase window merge pack branch pack  # 8
    remote header token worker  # 9
    rebase object index buffer worker worker remote delta commit socket  # 10
    buffer buffer merge queue header remote header rebase remote pack delta parser  # 11
    merge buffer window buffer handle ref rebase worker handle worker  # 12
    commit pack index index buffer  # 13
    object handle header token stream parser handle delta buffer parser header delta  # 14
    object token delta pack token merge pack stream buffer buffer worker worker  # 15
    branch index socket delta token tree object  # 16
    queue blob token window buffer buffer rebase ref cache index parser  # 17
    delta tree blob worker blob delta tree index header socket remote blob  # 18
    header blob blob queue delta blob remote socket object pack socket cache  # 19
    stream delta rebase merge stream tree queue  # 20
    pack socket window object ref object rebase  # 21
    socket header cache commit  # 22
    rebase branch index ref delta commit rebase cache pack  # 23
    stream socket remote cache index commit handle branch tree rebase window object  # 24
    index commit parser cache stream queue buffer  # 25
    rebase token merge remote index delta socket window socket commit pack stream  # 26
    index branch socket rebase parser parser pack queue cache  # 27
    token parser branch remote  # 28
    object remote handle rebase window  # 29
    index pack object commit branch object  # 30
    parser blob tree remote buffer header worker  # 31
    stream socket tree blob rebase rebase pack socket pack pack  # 32
    worker object ref tree index cache blob cache ref tree  # 0
    worker blob cache delta token remote tree  # 1
    pack blob branch window delta tree header  # 2
    rebase header parser delta object pack branch handle commit  # 3
    object socket cache window header rebase handle worker  # 4
    commit queue object header socket socket  # 5
    blob merge tree parser window queue pack blob  # 6
    rebase buffer token object parser socket window token  # 7
    buffer commit delta buffer object parser ref queue  # 8
    handle remote branch handle window delta buffer merge pack buffer  # 9
    buffer buffer index pack token queue object commit  # 10
    parser object object token window merge merge socket token  # 11
    queue socket cache socket buffer tree stream ref pack  # 12
