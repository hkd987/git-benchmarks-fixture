    merge rebase stream stream rebase ref handle blob parser socket token merge  # 0
    commit remote cache delta buffer blob delta merge merge buffer branch  # 1
    cache socket merge stream index remote cache pack buffer branch header worker  # 2
    worker queue object handle ref remote queue  # 3
    index pack index window buffer blob remote index merge stream blob rebase  # 4
    header cache tree delta stream object buffer  # 5
    object delta index tree parser delta socket delta delta object  # 6
    branch branch stream socket commit rebase pack token delta queue index worker  # 7
    socket handle branch object worker tree rebase  # 8
    index rebase rebase socket socket socket  # 9
    pack commit token queue stream  # 10
    branch delta ref rebase branch  # 11
    handle handle delta remote window cache worker object  # 12
    window merge parser pack header object token commit token tree merge  # 13
    branch merge merge worker  # 14
    handle object commit rebase ref  # 15
    remote parser rebase rebase window header branch remote  # 16
    window index object delta index handle  # 17
    stream socket worker pack buffer commit tree index  # 18
    tree rebase handle window queue worker socket remote handle token worker  # 19
    remote worker remote blob  # 20
    object token token index commit delta window  # 21
    handle remote object index cache stream  # 22
    blob handle socket commit commit token worker pack blob object  # 23
    parser header stream socket parser tree token  # 24
    tree remote tree object rebase  # 25
    branch parser handle commit tree socket buffer  # 26
    window buffer stream queue queue token commit cache  # 27
    rebase index commit socket cache window tree  # 28
    remote tree commit blob  # 29
    stream remote object handle  # 30
    worker tree worker rebase object rebase commit rebase  # 31
    window queue parser socket merge commit  # 32
    socket tree socket remote worker ref socket  # 33
    blob ref blob merge header ref  # 34
    commit index parser buffer cache ref  # 35
    header rebase blob header object remote buffer cache branch ref pack  # 36
    socket buffer socket buffer queue tree  # 37
    object window delta index object commit commit stream  # 38
    token stream worker queue stream  # 39
    window cache stream window header handle header rebase  # 40
    branch ref ref ref tree rebase ref rebase  # 41
