    commit blob object window merge object merge  # 0
    branch token rebase token branch object tree socket queue  # 1
    socket blob rebase token queue buffer merge parser stream queue  # 2
    token delta blob token parser token token rebase  # 3
    token socket delta queue socket remote tree merge handle parser object  # 4
    branch delta object remote window buffer commit stream delta  # 5
    handle remote delta blob tree header delta  # 6
    merge pack remote commit header handle parser tree commit  # 7
    header ref ref parser handle index worker queue branch  # 8
    stream tree worker cache commit socket cache ref socket  # 9
    branch pack socket tree queue queue ref  # 10
    buffer object commit merge socket remote pack index  # 11
    branch pack header window parser window socket window pack queue blob  # 12
    worker pack buffer stream cache blob handle pack blob rebase tree parser  # 13
    blob parser cache delta ref header blob  # 14
    ref merge stream rebase queue parser window  # 15
    tree queue index commit blob ref  # 16
    worker queue remote branch tree buffer parser stream queue parser socket handle  # 17
    worker queue object socket worker header commit commit window stream remote  # 18
    stream commit object commit  # 19
    token ref delta handle branch blob pack  # 20
    delta tree header pack branch object merge handle  # 21
    index ref cache stream commit merge  # 22
    handle stream cache parser handle blob window queue  # 23
    tree queue queue pack window pack  # 24
    remote merge object header handle delta rebase  # 25
    index delta commit commit  # 26
    remote socket token parser delta rebase branch branch remote  # 27
    index socket rebase object  # 28
    branch tree commit merge blob tree index rebase pack commit  # 29
    blob blob token header stream  # 30
    rebase buffer merge window cache ref  # 31
    rebase blob tree cache pack token merge remote delta pack remote  # 32
    ref delta parser commit parser remote pack delta  # 33
    token rebase buffer merge window cache handle merge remote  # 34
    buffer worker branch handle delta index cache blob cache queue  # 35
    cache delta queue index buffer rebase rebase ref index buffer queue tree  # 36
    pack pack socket tree remote cache window handle socket  # 37
    worker merge stream branch parser window worker  # 38
    merge object window branch object remote worker remote  # 39
    merge stream header handle delta token delta index delta  # 40
    buffer queue stream object  # 41
    stream pack socket blob header worker worker  # 42
    header parser token blob header tree cache token merge  # 43
    merge token pack commit commit parser blob delta worker window pack  # 44
