    tree token buffer index handle index socket tree remote merge worker  # 0
    object branch buffer remote tree rebase pack tree token handle  # 1
    commit cache token merge commit worker buffer window parser index handle merge  # 2
    window queue delta commit merge remote  # 3
    delta header blob delta remote queue socket handle blob  # 4
    window remote buffer merge header  # 5
    queue buffer window object worker tree handle delta cache  # 6
    commit window stream commit  # 7
    tree pack window header pack  # 8
    socket window blob commit object blob rebase buffer blob tree object merge  # 9
    merge blob rebase tree token branch token buffer pack cache commit parser  # 10
    remote rebase commit blob tree  # 11
    remote socket handle cache ref blob commit cache  # 12
    queue rebase window pack  # 13
    delta index branch token index pack buffer queue remote delta remote rebase  # 14
    handle remote parser cache rebase handle stream handle queue  # 15
    rebase pack remote remote index worker blob rebase branch worker parser  # 16
    remote index window parser  # 17
    stream window window rebase  # 18
    blob socket cache branch tree cache  # 19
    token queue queue token index parser rebase  # 20
    stream window index header  # 21
    queue tree remote remote merge rebase index  # 22
    parser remote handle queue handle header worker ref token  # 23
    window token token pack object rebase object remote handle token index commit  # 24
    ref parser token socket commit blob worker buffer header remote  # 25
    delta branch parser buffer  # 26
    stream parser delta object header rebase stream worker object parser queue merge  # 27
    ref tree rebase tree commit ref remote merge pack remote  # 28
    commit cache index merge stream ref object parser handle branch  # 29
    rebase queue index header parser stream buffer tree remote  # 30
    token buffer tree branch  # 31
    parser tree queue rebase rebase window token  # 32
    token queue tree merge window branch remote remote window delta  # 33
    worker token token tree token parser branch delta commit  # 34
    handle ref header cache blob window object ref object delta cache remote  # 35
    branch window object tree header branch pack delta  # 36
    header buffer remote index blob branch header delta header  # 37
    worker window parser delta  # 38
    stream socket header tree worker commit merge parser parser commit rebase  # 39
    branch commit parser socket queue queue handle queue handle delta window queue  # 40
    rebase queue cache remote token ref index  # 41
    queue header token token ref  # 42
    stream socket rebase window merge handle index worker  # 43
    ref commit ref remote stream cache merge stream  # 44
    parser remote remote remote  # 45
    token token commit stream handle socket ref remote  # 46
    worker worker remote handle blob remote socket parser branch ref  # 47
    commit remote branch buffer  # 48
    delta rebase worker delta stream ref commit worker remote token  # 49
    window worker window parser merge  # 50
    ref token header ref socket ref  # 51
    queue index handle socket socket  # 52
    token stream remote pack window tree delta socket tree  # 53
    blob window worker header pack pack delta blob branch merge worker header  # 54
    buffer queue tree branch blob blob queue ref cache branch object blob  # 55
    cache buffer blob queue worker  # 56
    ref pack socket branch pack socket socket ref branch object pack  # 57
    merge socket ref worker  # 58
    blob cache delta pack  # 59
    parser queue remote handle remote worker worker buffer  # 60
    worker worker merge blob  # 61
    branch worker worker handle rebase header index parser buffer  # 62
