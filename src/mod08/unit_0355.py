    header remote buffer merge handle window  # 0
    buffer token index rebase stream blob header worker index merge tree  # 1
    ref delta socket delta merge delta  # 2
    buffer merge header commit handle commit buffer commit cache branch merge  # 3
    remote window header commit pack worker  # 4
    cache token commit commit header delta  # 5
    ref queue merge header merge pack header tree  # 6
    commit blob commit token parser rebase handle ref header cache tree worker  # 7
    merge index index queue cache header merge commit token header  # 8
    blob ref merge worker branch cache merge index rebase index object rebase  # 9
    branch handle merge socket queue rebase  # 10
    stream header merge tree rebase index remote blob socket header  # 11
    merge stream header queue queue handle pack handle  # 12
    remote remote window remote branch blob socket remote window tree worker socket  # 13
    handle pack remote blob token  # 14
    blob cache parser cache stream blob delta header socket remote ref delta  # 15
    buffer rebase queue tree cache parser index rebase buffer header rebase queue  # 16
    index worker blob socket merge parser buffer  # 17
    remote tree token pack object  # 18
    object pack branch queue cache window commit header  # 19
    stream remote window header window remote parser rebase object delta  # 20
    socket remote commit index buffer delta parser  # 21
    commit socket merge token tree  # 22
    object stream header pack commit buffer header cache rebase queue  # 23
    window ref header window  # 24
    object window header token stream cache  # 25
    buffer merge object index pack  # 26
    pack commit socket token merge delta branch  # 27
    queue ref ref blob pack rebase object rebase header window  # 28
    pack socket branch branch commit commit index stream tree socket  # 29
    tree blob handle ref delta  # 30
    queue cache ref commit commit remote branch pack commit  # 31
    header stream tree token blob token handle  # 32
    blob pack tree branch header socket object branch delta commit  # 33
    object token pack branch header stream object object token object  # 34
    parser tree stream token stream pack  # 35
    cache socket parser stream handle  # 36
    queue buffer merge socket socket tree pack  # 37
    socket remote queue token  # 38
    stream remote handle stream tree blob  # 39
    cache index tree rebase socket tree queue commit  # 40
    remote remote parser pack  # 41
