    remote delta stream rebase queue header  # 0
    index delta tree merge  # 1
    token parser ref rebase parser queue  # 2
    header rebase socket token remote ref stream buffer handle branch remote parser  # 3
    blob ref tree object  # 4
    branch object parser stream handle cache worker remote rebase stream socket worker  # 5
    header pack pack ref handle remote merge header window  # 6
    socket pack branch pack remote worker delta buffer buffer queue  # 7
    window ref stream cache stream index ref stream  # 8
    ref commit remote index window blob stream cache blob ref  # 9
    handle cache handle pack handle tree remote cache delta cache pack remote  # 10
    handle parser merge blob buffer handle socket index token socket rebase stream  # 11
    delta stream merge remote delta remote queue window branch index  # 12
    object handle rebase rebase blob  # 13
    blob buffer commit socket window queue pack  # 14
    header socket tree index pack remote  # 15
    rebase pack rebase commit stream pack index window index cache  # 16
    object queue header queue index  # 17
    object stream buffer worker token rebase handle blob worker cache merge handle  # 18
    tree cache index delta queue blob ref pack tree ref  # 19
    commit header remote object handle pack parser token tree rebase object buffer  # 20
    buffer token header blob branch object token  # 21
    tree token stream pack  # 22
    merge blob blob remote stream  # 23
    worker stream handle window remote worker  # 24
    merge token merge remote pack index window pack  # 25
    merge merge delta merge stream rebase delta  # 26
    blob handle index tree commit queue ref handle ref  # 27
    blob blob commit queue branch  # 28
    socket handle worker header worker index buffer branch delta parser  # 29
    pack merge delta token parser object token header branch  # 30
    tree index blob stream cache handle pack commit index  # 31
    object worker handle window  # 32
    delta window cache delta queue merge branch window header merge blob  # 33
    index handle ref merge blob handle queue tree buffer  # 34
    queue blob branch object socket queue socket worker socket branch parser rebase  # 35
    worker parser parser handle cache queue tree  # 36
    worker blob buffer pack pack handle pack delta  # 37
    delta blob worker delta merge object branch commit  # 38
    index cache header queue header commit blob remote ref blob cache  # 39
    branch commit queue parser rebase stream  # 40
    cache merge pack branch index blob  # 41
    token parser ref branch queue remote ref index ref  # 42
    tree cache pack tree header stream delta  # 43
    remote header header pack socket rebase remote index token  # 44
    delta index branch index rebase remote parser rebase  # 45
    queue socket branch merge parser handle  # 46
    merge socket stream tree queue parser  # 47
    parser parser branch rebase queue delta ref socket delta cache  # 48
    index branch header index stream parser tree merge parser blob  # 49
    rebase tree worker remote object ref socket delta  # 50
    object worker merge ref worker window index commit worker parser  # 51
    ref rebase header buffer delta remote rebase remote rebase index ref header  # 52
    blob handle buffer branch  # 53
