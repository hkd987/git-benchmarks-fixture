    worker remote parser branch  # 0
    stream object header commit  # 1
    blob window handle cache stream token rebase remote  # 2
    queue commit worker parser parser rebase queue blob index parser blob  # 3
    index commit token window rebase pack ref blob cache handle header  # 4
    branch handle socket remote parser branch index cache token  # 5
    cache index socket pack delta stream header buffer  # 6
    handle parser blob socket commit  # 7
    buffer socket header window  # 8
    cache handle object parser pack branch  # 9
    rebase tree socket handle merge branch remote tree header cache  # 10
    delta queue pack commit stream rebase socket cache rebase  # 11
    merge tree delta index  # 12
    remote buffer header cache buffer blob cache remote rebase buffer header  # 13
    cache parser rebase queue object pack header object branch commit token ref  # 14
    commit commit cache index cache  # 15
    token index delta blob branch queue  # 16
    socket index window parser token queue remote queue  # 17
    token index cache handle index queue remote merge remote handle stream commit  # 18
    cache commit parser pack window stream header  # 19
    remote socket delta remote worker socket remote tree blob  # 20
    parser window rebase branch window delta  # 21
    window branch handle window header  # 22
    remote index merge header index object window tree rebase branch  # 23
    queue cache socket cache window blob rebase buffer index  # 24
    commit cache index worker ref stream commit queue  # 25
    window worker pack object  # 26
    socket handle worker stream header commit rebase remote  # 27
    window ref header ref  # 28
    token index tree ref parser delta header  # 29
    window blob object object remote header rebase merge delta object pack  # 30
    pack rebase window buffer  # 31
    tree commit delta header worker branch  # 32
    index pack remote delta token commit remote socket handle cache  # 33
    handle branch rebase remote object rebase buffer merge handle branch header  # 34
    rebase cache window token token queue commit commit window branch stream  # 35
    commit rebase tree buffer remote worker  # 36
    pack ref stream pack stream  # 37
    tree queue index index header commit blob object cache worker  # 38
    remote object index tree object tree tree object header pack branch pack  # 39
    cache token index header ref delta worker  # 40
    stream tree window header blob handle window index rebase blob blob socket  # 41
    socket merge parser parser pack tree remote pack ref delta token  # 42
    socket commit worker index index  # 43
    delta delta object worker  # 44
    stream ref cache ref socket branch object commit blob merge window  # 45
    ref worker delta buffer rebase merge handle worker tree header ref object  # 46
    header handle queue queue parser token parser worker queue branch  # 47
    commit queue commit stream branch  # 48
    buffer index handle delta  # 49
    ref socket parser commit  # 50
    worker cache buffer header blob rebase blob rebase worker  # 51
    buffer merge header header token cache worker cache pack rebase socket  # 52
    socket commit header index window queue handle index buffer rebase parser  # 53
    header rebase window merge blob handle cache cache remote  # 54
    parser branch stream merge stream queue cache  # 55
    delta stream worker ref object queue worker header queue object  # 56
    blob handle tree handle branch object remote queue merge object  # 57
    parser delta socket object delta header tree buffer cache ref merge tree  # 58
