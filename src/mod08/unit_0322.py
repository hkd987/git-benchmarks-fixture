    queue token parser blob cache stream  # 0
    pack blob tree blob commit header socket stream ref queue  # 1
    blob parser merge socket window object ref commit parser index  # 2
    blob handle object buffer  # 3
    delta socket blob blob header queue object ref window parser queue tree  # 4
    object object worker branch cache blob pack pack  # 5
    remote pack socket remote pack queue header stream tree tree merge cache  # 6
    token parser buffer handle pack branch window parser pack header delta ref  # 7
    remote cache token socket stream stream rebase worker  # 8
    rebase pack index tree branch branch tree token worker  # 9
    cache buffer parser stream tree delta index socket index ref blob  # 10
    buffer window header commit worker socket blob blob delta ref  # 11
    merge ref pack blob delta cache queue window  # 12
    header delta tree parser delta header socket ref pack  # 13
    branch buffer branch window worker commit parser window stream delta header  # 14
    stream buffer parser delta object header token buffer object delta branch  # 15
    delta token token merge header  # 16
    branch commit object object header  # 17
    merge header window window rebase  # 18
    window blob window rebase rebase  # 19
    index socket buffer tree queue commit remote  # 20
    commit rebase header handle cache parser rebase object index merge  # 21
    header commit header object handle worker  # 22
    branch pack worker object buffer index remote socket object index  # 23
    parser stream buffer delta parser window object index worker  # 24
    merge pack index delta delta  # 25
    stream pack parser tree tree  # 26
    index tree worker header object parser remote handle window branch remote  # 27
    tree parser window merge ref tree ref window  # 28
    stream blob merge index object window remote handle parser pack socket commit  # 29
    parser buffer commit remote commit index index buffer object remote  # 30
    token rebase handle blob token handle delta header socket rebase parser window  # 31
    delta tree object ref object index  # 32
    merge blob blob stream header token queue branch rebase stream worker tree  # 33
    queue queue buffer ref token merge tree window tree  # 34
    parser token merge index commit delta ref socket commit worker  # 35
    queue buffer queue pack buffer delta pack delta delta token queue  # 36
    buffer parser pack rebase  # 37
    remote branch buffer stream pack tree delta commit cache cache  # 38
    delta delta window remote  # 39
    tree commit window ref worker handle object index delta parser  # 40
    blob window buffer worker delta index ref rebase stream worker  # 41
    cache cache token window worker header index stream socket buffer window commit  # 42
    cache queue buffer header buffer header socket branch  # 43
    header queue header blob delta  # 44
    stream merge queue delta branch parser header tree branch buffer header branch  # 45
    object pack merge parser blob  # 46
    branch window delta token pack header branch  # 47
    token blob buffer handle object blob header parser  # 48
    window handle blob commit object merge tree worker  # 49
    socket blob queue parser pack handle  # 50
    rebase queue token buffer pack  # 51
    branch cache queue window  # 52
    queue delta pack index commit commit remote handle  # 53
    queue object tree remote window branch index buffer handle  # 54
    delta token rebase parser queue socket socket object pack remote cache blob  # 55
    buffer rebase object blob tree queue cache  # 56
    worker worker cache branch ref socket header remote  # 57
    branch pack rebase header merge index delta window  # 58
    branch rebase commit rebase token header pack  # 59
    parser index object pack window queue branch token token remote  # 60
    tree commit rebase tree merge remote blob  # 61
    remote cache window buffer commit delta token  # 62
    queue branch delta pack header delta token  # 63
    branch parser handle blob commit remote parser object index handle window parser  # 64
    token worker branch merge  # 65
    remote delta worker window index buffer token branch token tree stream socket  # 66
    commit window handle rebase handle ref branch  # 67
    cache queue parser queue blob pack  # 68
    parser worker branch ref commit ref  # 69
    rebase object index worker queue cache tree pack  # 70
    delta blob merge tree branch commit merge rebase buffer worker branch  # 71
