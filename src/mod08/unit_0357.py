    stream header branch branch cache ref buffer object blob object queue header  # 0
    window header index stream socket  # 1
    remote rebase rebase rebase buffer delta header branch blob socket  # 2
    window blob blob stream  # 3
    header rebase merge commit object  # 4
    branch socket buffer commit object worker pack blob queue  # 5
    index commit ref window buffer stream merge tree remote branch blob commit  # 6
    token merge token header merge stream merge blob parser remote delta  # 7
    cache queue cache rebase queue  # 8
    window stream token pack delta merge buffer  # 9
    tree worker stream window merge  # 10
    parser remote blob rebase remote ref index  # 11
    ref tree queue queue merge tree header merge cache  # 12
    buffer token cache window buffer tree worker object header cache tree window  # 13
    handle parser stream buffer  # 14
    branch remote token tree object window  # 15
    branch pack blob merge tree object branch cache tree index cache  # 16
    ref buffer merge token header  # 17
    cache tree pack merge token rebase delta handle object blob rebase delta  # 18
    window socket rebase parser stream token index index  # 19
    token branch queue commit branch buffer queue  # 20
    stream index socket stream pack handle stream blob  # 21
    worker index queue merge branch header header header  # 22
    worker delta delta parser stream stream cache tree  # 23
    branch window header worker handle cache index rebase socket  # 24
    token socket index socket object worker rebase buffer  # 25
    ref blob tree remote remote pack merge merge buffer object  # 26
    commit tree object ref stream blob window socket  # 27
    commit commit delta blob  # 28
    blob tree header stream rebase token branch tree parser tree stream  # 29
    branch cache handle rebase cache cache token  # 30
    delta queue cache buffer buffer  # 31
    socket ref stream worker ref branch rebase stream index pack  # 32
    index blob cache delta stream worker blob commit  # 33
    pack merge buffer parser remote commit parser blob  # 34
    rebase socket worker socket parser token tree  # 35
    ref remote blob header rebase index buffer window pack  # 36
    socket index object cache worker tree rebase tree window  # 37
    delta rebase ref socket socket  # 38
    handle pack commit remote delta rebase handle rebase merge parser merge  # 39
