    index object ref socket  # 0
    token token branch tree merge commit delta parser window queue commit  # 1
    commit handle cache parser ref stream token ref queue remote worker  # 2
    merge ref object pack index pack cache object merge handle cache  # 3
    branch ref queue index window handle header worker rebase buffer  # 4
    cache stream socket queue buffer worker commit ref ref  # 5
    commit handle stream stream branch remote  # 6
    delta parser blob socket commit branch branch parser stream  # 7
    tree socket pack index stream socket merge pack worker object blob  # 8
    parser remote handle branch pack blob buffer window queue worker  # 9
    object merge window index rebase worker worker blob  # 10
    blob branch handle buffer buffer queue handle worker socket remote object  # 11
    parser index token rebase branch queue socket branch window commit  # 12
    blob merge buffer handle commit ref queue branch buffer window  # 13
    worker header queue branch object worker delta index worker socket delta  # 14
    ref buffer worker tree tree blob parser  # 15
    merge window handle socket stream worker worker buffer token object window branch  # 16
    queue handle queue merge queue handle parser delta  # 17
    buffer worker stream pack window  # 18
    window handle cache stream ref header  # 19
    index branch merge parser token object delta rebase delta parser commit  # 20
    remote buffer index delta merge stream ref  # 21
    object tree socket object token cache pack tree stream stream stream socket  # 22
    cache buffer rebase rebase buffer blob buffer tree  # 23
    window window remote header delta commit stream blob worker window socket  # 24
    object merge remote index  # 25
    window index index buffer remote cache pack  # 26
    pack object merge remote delta socket cache branch delta queue socket tree  # 27
    window header object queue blob socket rebase  # 28
    index token header stream worker token handle handle window  # 29
    token header merge blob worker remote socket  # 30
    buffer handle cache header rebase pack token commit  # 31
    queue ref token stream socket socket commit worker  # 32
    commit branch token cache handle parser tree rebase handle socket index stream  # 33
    socket handle window merge blob branch cache delta worker remote pack stream  # 34
    worker window merge commit object token commit commit queue delta index  # 35
    handle index commit delta commit remote  # 36
    rebase object tree rebase handle stream buffer pack worker  # 37
    object token commit socket window commit branch rebase socket queue handle ref  # 38
    delta index token commit worker commit delta tree header  # 39
    object merge merge parser socket merge index merge  # 40
    window header remote parser  # 41
    header tree rebase blob merge rebase  # 42
    token blob branch socket buffer pack buffer ref stream  # 43
    tree worker remote handle rebase cache blob queue buffer  # 44
    branch ref buffer worker branch rebase object  # 45
    merge delta object rebase commit worker commit remote worker stream  # 46
    remote parser queue pack ref  # 47
    merge token handle rebase worker buffer window index tree socket index commit  # 48
    rebase window pack blob tree merge token object worker queue window  # 49
    rebase ref blob blob  # 50
    blob worker window parser ref queue stream worker buffer ref  # 51
    remote merge socket header  # 52
    merge rebase ref buffer queue stream commit commit index commit merge  # 53
    buffer delta merge commit branch queue  # 54
    delta blob socket index window index stream handle queue index ref  # 0
    remote commit index socket delta index  # 1
    blob buffer token index cache delta parser pack queue queue  # 2
    socket parser token tree object queue delta commit delta index header buffer  # 3
    tree stream stream object buffer blob remote pack buffer  # 4
    queue tree branch buffer object commit pack branch object  # 5
    socket cache delta parser queue tree index queue cache window cache queue  # 6
    worker branch index tree tree worker socket remote remote merge  # 7
    delta buffer branch delta socket  # 8
    index cache rebase delta window branch stream remote  # 9
    parser socket worker remote remote commit  # 10
