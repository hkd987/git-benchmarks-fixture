    worker stream commit token token stream tree header branch socket  # 0
    header stream stream pack buffer parser header  # 1
    tree header commit pack queue tree queue window tree cache queue  # 2
    stream socket tree blob  # 3
    stream tree queue object ref commit ref  # 4
    stream socket queue window blob object header delta tree  # 5
    window merge cache handle  # 6
    pack ref index worker branch rebase ref header token queue remote queue  # 7
    pack object tree cache branch stream queue rebase tree delta  # 8
    stream parser ref blob header window blob handle remote remote stream  # 9
    header handle commit rebase tree branch  # 10
    blob queue rebase buffer stream socket object delta rebase header  # 11
    token index merge remote window header handle window  # 12
    stream commit queue delta blob  # 13
    handle merge cache tree token stream blob queue socket buffer rebase  # 14
    ref delta object commit  # 15
    parser token blob branch object handle pack  # 16
    commit ref branch header blob merge branch tree tree cache blob  # 17
    handle index cache delta branch  # 18
    pack header worker rebase cache token pack  # 19
    ref remote parser branch ref tree rebase parser worker object window  # 20
    ref cache stream worker index branch  # 21
    parser window rebase rebase socket index branch stream index rebase socket  # 22
    rebase header pack tree queue blob token branch cache index  # 23
    tree remote cache commit branch handle  # 24
    remote cache queue rebase blob header  # 25
    socket worker header buffer socket rebase token  # 26
    commit rebase blob buffer worker ref handle merge stream rebase branch  # 27
    handle delta buffer merge merge rebase queue delta cache  # 28
    pack buffer rebase socket merge stream handle rebase delta blob cache remote  # 29
    handle rebase header remote branch object commit  # 30
    object rebase blob index handle socket ref  # 31
    pack header rebase queue  # 32
    worker token parser ref tree buffer buffer  # 33
    rebase window rebase buffer merge worker worker remote handle worker  # 34
    ref window queue commit  # 35
    rebase socket index object rebase cache  # 36
    rebase object socket window  # 37
    commit tree handle index queue stream  # 38
    queue rebase queue buffer handle remote remote  # 39
    header blob handle pack  # 40
    socket window rebase pack rebase delta window queue  # 41
    stream merge worker delta  # 42
    object ref tree socket blob remote  # 43
    buffer stream blob tree  # 44
    header pack header remote rebase buffer branch remote parser window remote  # 45
    index remote delta queue queue  # 46
    queue cache window merge pack stream window remote window index  # 47
    cache handle queue merge ref pack window pack buffer remote token  # 48
    handle tree header object  # 49
    header delta merge stream remote remote  # 50
    ref commit delta buffer parser remote pack tree cache header  # 51
    merge token ref rebase buffer cache  # 52
    token commit index rebase handle rebase  # 53
    socket commit stream cache ref merge worker  # 54
    delta delta branch branch rebase rebase cache window worker socket  # 55
    socket branch blob blob window index remote tree branch  # 56
    cache commit window rebase rebase parser buffer object  # 57
    header handle socket merge ref stream token blob pack header ref token  # 58
    stream object merge buffer handle parser rebase window buffer object buffer cache  # 59
    token merge stream handle blob stream object handle buffer worker delta  # 60
    branch pack buffer socket queue rebase  # 61
    pack cache handle queue ref token window  # 62
