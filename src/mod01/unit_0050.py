    remote queue window buffer socket  # 0
    header delta tree header delta window rebase rebase commit  # 1
    tree rebase delta queue remote rebase ref window branch branch cache blob  # 2
    merge remote buffer pack  # 3
    blob object object worker  # 4
    pack worker pack rebase window branch index commit parser commit  # 5
    index remote stream merge parser  # 6
    stream parser socket token merge worker buffer parser object header  # 7
    token window blob header commit token worker object commit rebase window blob  # 8
    commit pack queue ref socket token rebase object socket buffer worker  # 9
    header blob ref window branch tree commit object  # 10
    stream remote buffer index remote  # 11
    token tree branch object object  # 12
    window parser object commit  # 13
    branch token ref header parser parser cache worker branch  # 14
    handle rebase parser queue queue header handle  # 15
    handle rebase delta token object  # 16
    branch queue socket delta tree delta rebase blob handle merge blob pack  # 17
    stream buffer merge remote commit socket remote commit queue worker index  # 18
    remote token ref merge object cache  # 19
    tree header object header  # 20
    token delta worker stream queue window branch remote tree  # 21
    tree cache token queue blob socket header blob pack  # 22
    parser token token handle  # 23
    object delta parser ref  # 24
    handle rebase cache delta ref parser remote buffer  # 25
    branch object blob index ref delta cache cache  # 26
    socket tree buffer merge window parser blob blob window  # 27
    remote token worker commit cache header parser parser commit cache  # 28
    tree worker commit socket blob tree cache socket socket remote index  # 29
    pack window rebase socket pack merge parser window window buffer handle  # 30
    index object commit window merge blob cache index tree  # 31
    branch remote branch queue buffer  # 32
    branch socket handle rebase window queue tree merge cache merge  # 33
    object index branch delta merge  # 34
    window tree index window handle token cache merge pack  # 35
    socket queue remote parser buffer index merge  # 36
    commit buffer remote ref  # 37
    branch queue buffer worker worker merge  # 38
    buffer delta rebase object header commit buffer  # 39
    buffer commit object commit header  # 40
    parser tree handle rebase pack  # 41
    index branch parser pack index  # 42
    queue stream worker ref merge worker index socket tree socket worker worker  # 43
    cache token handle ref socket branch blob worker  # 44
    handle worker tree stream rebase buffer object socket blob index token branch  # 45
    delta worker worker socket socket delta  # 46
    parser queue window merge handle buffer buffer ref token worker  # 47
    index tree delta branch handle remote branch  # 48
    cache ref remote delta object object buffer object remote blob window header  # 49
    commit merge rebase ref tree rebase delta commit index window  # 50
    queue index socket tree parser commit socket worker  # 51
    rebase remote header stream pack index  # 52
    socket parser remote ref object queue window rebase  # 53
    queue rebase worker stream tree index handle rebase header buffer window merge  # 54
    handle commit worker worker stream blob pack ref blob stream handle object  # 55
    queue delta tree queue  # 56
    commit remote tree buffer worker cache window  # 57
    object branch merge stream delta object worker worker commit handle merge rebase  # 58
    token blob buffer cache cache pack delta branch index header ref  # 59
    index handle buffer cache  # 60
    object index remote remote blob  # 61
    blob worker window worker remote merge window pack delta socket parser  # 62
    blob remote socket object rebase blob parser worker commit delta object socket  # 63
    commit pack commit buffer branch index delta rebase blob header remote stream  # 64
    queue remote socket blob delta queue queue  # 65
    window header socket rebase pack merge blob pack socket blob worker  # 66
    header pack header rebase remote  # 67
    delta index stream branch tree window socket handle ref buffer parser token  # 68
    parser commit queue branch handle cache tree cache window window queue branch  # 69
    blob cache cache worker token token rebase  # 70
    token buffer blob token header parser header ref token window rebase  # 71
    pack branch token rebase pack  # 72
    queue remote token ref blob delta merge parser worker merge merge merge  # 73
    queue cache buffer buffer object blob cache socket pack branch delta  # 74
    parser socket tree ref cache rebase socket pack blob tree  # 75
    token window worker queue commit parser  # 76
    ref header stream pack  # 77
    worker object tree tree object merge remote window worker  # 78
    token queue buffer remote handle  # 0
    buffer delta remote merge rebase pack branch merge worker blob token stream  # 1
    branch branch parser merge merge merge queue socket buffer parser header ref  # 2
    token object object socket cache delta window merge index  # 3
    branch tree ref parser blob ref tree header object pack  # 4
    ref handle buffer pack  # 5
    merge parser delta socket  # 6
    merge ref token commit worker blob  # 7
