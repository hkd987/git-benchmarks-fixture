    buffer delta blob parser commit index  # 0
    parser pack merge handle token token pack window merge stream  # 1
    queue socket worker stream rebase pack worker worker merge window  # 2
    ref merge worker header tree  # 3
    window blob remote index parser ref index remote queue merge  # 4
    parser object commit parser token parser index object worker  # 5
    commit buffer token blob window merge  # 6
    index worker commit worker socket  # 7
    buffer cache queue header header ref window window remote merge  # 8
    queue window cache branch cache parser window ref cache worker cache  # 9
    queue remote handle tree branch handle ref blob blob token merge delta  # 10
    index tree worker handle  # 11
    merge header parser buffer header header header  # 12
    pack window window worker queue branch  # 13
    delta commit buffer ref index  # 14
    parser blob window cache buffer handle  # 15
    tree merge token merge handle socket commit index  # 16
    object branch socket buffer worker header  # 17
    delta buffer tree parser tree worker token merge handle remote cache  # 18
    delta window pack remote handle parser handle queue ref  # 19
    merge merge merge delta buffer queue header parser index  # 20
    tree header branch tree rebase commit  # 21
    token stream blob token worker window pack token  # 22
    stream rebase delta stream index branch cache socket delta remote queue parser  # 23
    parser branch stream header delta worker  # 24
    stream parser handle object worker index  # 25
    header queue delta token parser cache branch worker  # 26
    socket cache blob header tree worker handle blob header branch header  # 27
    handle parser header queue parser parser socket  # 28
    remote cache tree merge ref socket parser  # 29
    delta token commit handle branch rebase  # 30
    header remote branch index token cache cache queue queue buffer branch blob  # 31
    tree object branch remote branch worker  # 32
    queue token rebase object merge ref tree tree ref handle queue cache  # 33
    handle tree index header commit  # 34
    parser worker queue index header blob stream queue delta rebase pack socket  # 35
    parser blob header worker  # 36
    index rebase buffer remote handle socket index buffer socket ref tree queue  # 37
    rebase tree pack pack handle index object  # 38
    branch window handle socket commit index  # 39
    cache rebase window worker  # 40
    merge pack merge commit  # 41
    rebase pack remote parser tree branch  # 42
    queue rebase remote tree  # 43
    branch blob stream ref token remote header pack  # 44
    ref worker header window handle rebase remote cache  # 45
    window cache socket delta object tree commit tree  # 0
    buffer blob rebase cache pack tree ref branch index merge index delta  # 1
    queue parser cache queue index buffer commit token buffer  # 2
    ref handle merge delta blob merge delta commit buffer blob  # 3
    rebase worker header queue buffer ref delta token  # 4
