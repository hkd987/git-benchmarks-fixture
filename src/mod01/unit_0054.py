    header ref worker window  # 0
    commit index window window merge header buffer pack blob  # 1
    handle rebase blob buffer branch  # 2
    header tree token tree window merge commit  # 3
    pack ref merge branch ref queue window cache blob rebase parser  # 4
    stream branch worker socket ref merge buffer  # 5
    header token pack branch  # 6
    header object object buffer header merge handle buffer socket tree socket tree  # 7
    rebase queue stream header buffer  # 8
    token merge header index header pack delta  # 9
    delta ref stream ref header blob blob cache token remote pack  # 10
    socket token index remote commit handle buffer  # 11
    queue buffer handle token parser token socket delta index tree delta header  # 12
    tree cache socket handle  # 13
    buffer ref queue delta  # 14
    remote queue branch pack handle blob  # 15
    window worker worker tree worker delta remote commit window token merge  # 16
    header blob branch object commit cache rebase window token rebase rebase index  # 17
    socket buffer window blob pack tree buffer merge ref header queue token  # 18
    cache handle socket commit object token socket tree socket header blob socket  # 19
    branch pack ref window handle window cache token queue parser  # 20
    branch ref window object commit cache worker queue remote queue parser  # 21
    worker index token delta branch  # 22
    buffer socket socket ref  # 23
    delta window remote queue parser token cache worker token queue  # 24
    index rebase queue window worker delta pack  # 25
    stream socket object stream  # 26
    header merge tree merge worker rebase remote  # 27
    buffer buffer delta pack socket socket object blob delta  # 28
    index tree header queue tree object object window index socket pack remote  # 29
    commit queue branch worker index  # 30
    commit tree stream ref header object handle header  # 31
    handle object tree cache  # 32
    cache object window branch commit  # 33
    object index index socket stream  # 34
    header tree blob stream commit token window ref socket header  # 35
    index branch rebase header index cache blob  # 36
    index blob commit remote socket stream index  # 37
    parser blob merge parser buffer cache token merge  # 38
    parser window window commit pack object commit rebase object  # 39
    index blob merge tree  # 40
    queue tree remote delta worker socket header parser remote  # 41
    commit socket merge rebase  # 42
    merge rebase queue handle ref blob commit object  # 43
    stream token stream merge socket window rebase  # 44
    stream ref worker remote buffer branch header branch remote  # 45
    stream rebase remote object worker parser merge commit token  # 46
    stream cache branch tree rebase worker socket  # 47
    window object worker cache worker index rebase  # 48
    index blob delta remote worker pack worker buffer index handle  # 0
    merge commit remote header token  # 1
    pack buffer remote token parser buffer commit  # 2
    pack object branch delta  # 3
    commit stream rebase socket handle handle parser rebase branch stream  # 4
    object buffer branch tree window token index commit socket delta rebase  # 5
    buffer worker token object  # 6
