    queue worker pack pack worker blob tree parser buffer  # 0
    socket worker ref merge parser branch  # 1
    delta index window merge cache worker ref merge  # 2
    ref delta branch buffer cache branch delta tree blob window commit  # 3
    cache header remote queue cache handle handle remote stream  # 4
    remote worker stream parser  # 5
    delta branch remote queue commit handle  # 6
    pack cache parser queue object token window worker parser worker  # 7
    worker pack remote socket queue token socket window pack window handle  # 8
    remote token blob object  # 9
    index object blob buffer worker parser parser delta  # 10
    queue parser window tree worker rebase window tree tree  # 11
    worker commit cache cache  # 12
    branch cache pack index delta window rebase stream ref  # 13
    queue branch tree index window object pack  # 14
    socket stream header header parser cache parser pack branch token pack  # 15
    parser delta queue buffer stream  # 16
    buffer tree worker rebase stream parser  # 17
    cache delta object queue handle window worker  # 18
    worker window blob window remote branch header remote socket branch  # 19
    window header commit handle socket token queue object window  # 20
    object stream window branch pack token queue object socket  # 21
    branch object stream worker queue blob window  # 22
    index rebase window tree remote buffer ref commit branch branch merge  # 23
    header delta buffer remote buffer  # 24
    window cache handle merge token  # 25
    window delta stream window  # 26
    parser object delta queue rebase cache token index window  # 27
    rebase tree branch token pack delta handle queue  # 28
    queue token pack object  # 29
    branch commit header delta window socket  # 30
    branch stream tree parser stream  # 31
