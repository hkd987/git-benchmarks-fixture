    token branch rebase commit parser worker parser worker merge header delta ref  # 0
    commit buffer blob token token rebase index queue pack parser  # 1
    parser blob object blob window parser window cache parser handle  # 2
    cache pack ref socket tree merge cache  # 3
    parser object rebase merge object  # 4
    commit window blob window stream ref object socket socket  # 5
    token socket ref buffer rebase remote blob rebase blob  # 6
    header tree merge index ref commit cache tree pack worker pack  # 7
    object index commit queue header tree index blob  # 8
    merge index socket ref token worker tree worker handle  # 9
    window tree ref object header  # 10
    cache branch branch merge branch merge  # 11
    tree parser handle object  # 12
    delta merge index worker tree cache  # 13
    ref cache delta pack remote branch  # 14
    pack cache index tree cache  # 15
    buffer queue ref object stream merge socket window rebase window  # 16
    cache parser token blob  # 17
    ref queue rebase parser parser branch ref delta  # 18
    index remote branch delta cache  # 19
    branch branch header queue  # 20
    pack queue queue parser socket  # 21
    buffer merge parser socket index tree queue index object  # 22
    merge cache tree queue tree delta queue object window  # 23
    socket index tree queue worker stream window  # 24
    index handle handle delta token branch stream socket ref parser header blob  # 25
    buffer buffer branch pack  # 26
    worker cache stream delta index rebase ref rebase  # 27
    remote remote blob token index  # 28
    stream cache delta rebase delta window token  # 29
    buffer header worker ref blob  # 30
    header header token header object cache buffer header  # 31
    merge pack queue delta index tree rebase tree token socket branch tree  # 32
    tree tree object pack delta remote merge  # 33
    header cache queue stream pack header delta ref  # 34
    delta ref pack worker remote parser window  # 35
    pack pack blob socket blob rebase parser object pack rebase token window  # 36
    merge socket window delta window stream delta pack merge tree index rebase  # 37
    blob blob ref object stream  # 38
    buffer token index delta branch delta stream merge worker rebase blob  # 39
    index delta blob object commit socket cache object handle index  # 40
    handle worker blob delta socket blob commit merge worker window cache  # 41
    blob branch remote merge stream merge worker index merge header handle socket  # 42
    queue cache socket delta blob socket branch delta merge ref branch  # 43
    commit merge rebase parser header commit handle rebase  # 44
    index socket tree tree token delta header handle branch queue socket  # 45
    commit window index blob  # 46
    parser pack handle buffer stream branch tree header cache tree worker  # 47
    worker stream pack parser  # 48
    queue tree buffer blob merge cache commit branch blob tree object  # 49
    token cache worker merge delta cache index pack buffer rebase cache ref  # 50
    index branch parser index window worker parser window tree delta  # 51
    rebase remote queue blob delta stream socket  # 52
    blob merge merge queue ref  # 53
    buffer queue queue index handle worker  # 54
    ref stream merge stream queue delta merge remote index  # 55
    handle worker parser queue blob remote branch delta buffer pack index  # 56
    rebase queue token merge token queue  # 57
    branch queue object index  # 58
    merge socket branch blob blob socket buffer queue stream stream branch  # 59
    token pack buffer stream delta merge blob commit ref token parser  # 60
    header merge ref worker delta cache header parser branch  # 61
    commit branch branch merge cache  # 62
