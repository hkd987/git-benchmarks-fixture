    branch parser window window window  # 0
    stream merge buffer header tree token merge  # 1
    rebase window queue buffer tree buffer rebase object header index  # 2
    branch header rebase remote token parser parser worker delta merge queue stream  # 3
    index merge object handle pack worker index  # 4
    handle header tree rebase remote worker  # 5
    parser branch branch blob handle  # 6
    handle header pack socket blob cache queue branch object  # 7
    cache object object stream ref  # 8
    tree branch handle branch commit index  # 9
    blob stream commit index ref worker handle  # 10
    token object tree token ref  # 11
    buffer parser header cache index pack buffer  # 12
    branch commit stream branch branch  # 13
    handle header handle tree object cache  # 14
    buffer ref queue remote  # 15
    handle parser branch handle queue branch  # 16
    rebase branch buffer header blob  # 17
    delta header stream handle cache ref index socket blob buffer remote  # 18
    queue token commit queue branch handle commit rebase stream buffer  # 19
    blob index tree token pack ref rebase cache  # 20
    handle merge buffer delta cache rebase delta object  # 21
    branch socket parser delta delta branch remote pack window queue handle tree  # 22
    cache socket window socket pack remote remote tree worker parser worker token  # 23
    remote index buffer tree cache branch tree commit window  # 24
    queue index merge object index commit index  # 25
    socket socket socket queue tree header buffer index buffer queue branch  # 26
    index worker index queue  # 27
    pack worker rebase cache  # 28
    remote index socket rebase  # 29
    index pack delta header worker window  # 30
    header ref stream pack object index token header window tree worker  # 31
    index commit blob index stream  # 32
    queue merge stream rebase buffer index branch cache remote parser ref  # 33
    ref header token token header merge tree ref rebase parser  # 34
    rebase commit ref handle blob parser header ref  # 35
    ref window worker queue  # 36
    socket header tree handle queue worker window index merge merge index worker  # 37
    blob cache branch branch cache tree  # 38
    parser branch branch object branch ref worker remote pack index  # 39
    pack delta queue blob ref  # 40
    commit rebase socket merge remote ref  # 41
    queue object delta ref worker cache tree worker remote  # 42
    handle remote window socket queue pack cache parser  # 43
    token merge token parser tree blob cache merge  # 44
    index buffer window remote worker blob merge header socket stream branch pack  # 45
    handle socket merge ref parser  # 46
    buffer branch merge merge rebase window  # 47
    queue cache token socket branch worker  # 48
    branch delta remote remote queue blob  # 49
    index window window stream rebase window pack object pack tree  # 50
    window handle queue stream queue remote  # 51
    stream blob commit header token window commit stream ref ref object  # 52
    queue stream worker stream cache cache commit token  # 53
    stream pack header header  # 54
    commit header handle pack ref window merge remote merge  # 55
    commit delta parser header branch pack index window object ref  # 56
    object worker tree commit commit tree blob queue window rebase  # 57
    branch cache token blob queue worker remote delta header cache branch  # 58
    blob cache token merge rebase window branch  # 59
    delta socket index remote rebase ref branch token  # 60
    pack header worker cache cache remote  # 61
    commit delta branch branch handle socket merge worker parser header blob index  # 62
    pack pack object merge  # 63
    remote branch parser delta token window socket object cache window  # 64
    branch index parser parser stream delta handle  # 65
    remote pack stream handle pack index blob pack socket delta cache blob  # 66
    delta parser cache window  # 67
    tree branch header commit branch commit socket ref object  # 68
    socket pack pack commit window socket queue branch blob cache remote delta  # 69
    index blob ref header object socket commit  # 70
    buffer delta queue merge ref parser tree commit rebase queue handle  # 71
    handle tree blob tree branch  # 72
    merge window token pack delta  # 73
    blob parser stream socket cache  # 74
    window window rebase delta object tree object  # 75
    merge pack object tree  # 76
    commit token token token object index commit token remote  # 77
    parser remote remote tree tree header delta token  # 78
    remote cache remote remote merge remote tree merge merge object  # 79
    socket blob worker ref  # 80
    queue stream ref merge  # 81
