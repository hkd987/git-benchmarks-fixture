    buffer cache pack commit stream pack index token delta object stream  # 0
    header rebase worker branch branch header queue remote  # 1
    handle tree merge commit parser remote branch queue header pack header parser  # 2
    window rebase object tree socket commit index queue  # 3
    rebase remote pack delta parser tree branch stream stream rebase socket index  # 4
    object queue rebase queue parser worker index window socket window  # 5
    handle queue remote cache  # 6
    cache object pack blob cache token rebase token index commit pack index  # 7
    stream rebase socket delta socket ref merge branch cache  # 8
    rebase object window tree  # 9
    stream queue handle tree pack pack parser queue tree object merge parser  # 10
    token window header commit blob  # 11
    ref worker token socket rebase handle  # 12
    queue handle rebase delta index window  # 13
    token stream header queue worker delta pack parser handle commit  # 14
    index cache blob cache index  # 15
    cache index delta buffer index header tree merge  # 16
    remote socket header merge stream queue queue cache merge  # 17
    window commit queue worker  # 18
    header socket worker rebase delta rebase handle  # 19
    worker merge queue cache  # 20
    cache handle header cache socket delta header  # 21
    delta delta header ref tree blob blob buffer socket delta  # 22
    merge ref tree token delta  # 23
    pack delta rebase index token worker queue handle header remote buffer  # 24
    pack handle cache ref object buffer  # 25
    handle header pack index ref index header worker object  # 26
    remote parser delta header cache object parser  # 27
    token delta blob cache tree tree  # 28
    parser header worker branch token buffer branch buffer tree  # 29
    ref blob index delta ref object object stream  # 30
    delta branch header window parser index branch object rebase window remote  # 31
    window remote branch handle queue handle stream tree worker  # 32
    tree token delta tree tree queue buffer commit  # 33
    ref rebase merge ref socket handle cache rebase delta rebase socket window  # 34
    window window worker socket tree worker blob window  # 35
    delta rebase object queue branch handle token delta cache pack pack stream  # 36
    queue header stream socket delta delta ref handle  # 37
    remote token token object  # 38
    delta socket ref blob handle handle blob stream remote stream  # 39
    tree socket delta remote blob tree  # 40
    window remote handle cache  # 41
    ref branch token blob delta buffer  # 42
    object blob rebase blob delta object delta blob  # 43
    remote buffer object header socket  # 44
    window cache commit parser queue socket parser header merge  # 45
    parser buffer object rebase ref  # 46
    object handle blob worker parser pack tree index commit tree cache  # 47
    tree buffer index branch handle token branch socket pack queue tree branch  # 48
    stream queue pack delta header window branch  # 49
    pack stream merge index worker blob ref  # 50
    commit tree cache commit cache token  # 51
    parser pack queue delta worker cache commit tree worker ref branch socket  # 52
    blob cache stream buffer branch window  # 53
    parser pack remote parser remote blob worker stream  # 54
    handle pack merge commit ref stream merge handle ref ref  # 55
    buffer stream handle parser pack  # 56
