    commit remote commit token commit handle parser tree socket stream remote  # 0
    cache tree queue remote delta token buffer delta  # 1
    handle cache socket buffer stream parser queue cache pack parser  # 2
    merge window header commit socket header ref merge buffer  # 3
    cache cache commit object token worker stream buffer  # 4
    handle pack window socket index branch parser delta index remote  # 5
    token blob parser blob ref tree merge header merge index  # 6
    handle commit pack cache rebase blob pack commit tree  # 7
    buffer handle header object remote  # 8
    merge token object queue  # 9
    token merge pack worker pack remote merge tree stream worker socket  # 10
    parser pack pack cache header buffer socket token branch buffer ref worker  # 11
    socket header parser delta cache stream worker ref branch queue  # 12
    handle buffer index handle header remote  # 13
    queue stream remote delta socket object delta parser blob parser  # 14
    header blob branch branch commit index tree remote commit remote commit  # 15
    queue handle window pack  # 16
    object handle token branch object rebase  # 17
    buffer rebase parser window blob index cache  # 18
    remote ref cache index remote merge socket  # 19
    branch remote handle cache remote handle object worker worker commit  # 20
    parser parser index branch branch queue tree rebase stream blob  # 21
    commit branch buffer ref window cache object buffer  # 22
    blob merge buffer window delta  # 23
    index rebase worker socket object cache parser tree socket parser stream remote  # 24
    buffer buffer token parser socket branch  # 25
    delta commit remote blob blob buffer  # 26
    window queue object rebase queue worker buffer  # 27
    pack queue worker delta worker merge blob  # 28
    ref tree queue worker handle buffer ref pack cache  # 29
    handle ref merge buffer  # 30
    buffer remote object stream remote commit delta blob handle delta object rebase  # 31
    handle index pack tree object commit index  # 32
    header rebase header header buffer remote stream  # 33
    index branch branch delta index queue  # 34
    object rebase token delta header delta delta blob  # 35
    header pack socket header remote  # 36
    header commit queue queue index  # 37
    merge index object ref pack tree window parser token  # 38
    index header merge delta tree cache stream  # 39
    header handle ref remote token object ref  # 40
    ref merge stream queue queue  # 41
    buffer cache branch blob queue branch stream rebase remote window  # 42
    index worker branch handle  # 43
    tree queue stream token tree worker delta pack  # 44
    handle branch stream index  # 45
    tree window header remote parser index tree object worker ref remote  # 46
    window handle handle pack remote index  # 47
    object socket blob buffer token delta merge blob tree  # 0
    socket cache token remote header handle cache blob branch  # 1
    window delta queue tree buffer stream buffer pack  # 2
    socket object remote pack window  # 3
    stream rebase buffer buffer pack handle  # 4
    token index blob header blob rebase cache commit tree  # 5
    window blob commit stream worker parser  # 6
    stream pack parser delta  # 7
    tree tree header blob  # 8
