    token tree branch parser object socket pack socket  # 0
    cache index pack cache  # 1
    header tree socket remote window merge index  # 2
    rebase object pack worker index window merge object object  # 3
    pack stream blob queue blob index handle  # 4
    branch cache stream pack rebase handle parser  # 5
    branch worker blob remote remote handle  # 6
    token header socket ref  # 7
    handle ref token rebase branch branch blob delta worker stream  # 8
    buffer token delta object pack branch delta  # 9
    buffer tree remote object header stream parser  # 10
    handle worker remote worker token queue merge merge worker  # 11
    branch object blob rebase tree worker header  # 12
    cache remote pack tree token tree socket index cache queue  # 13
    token pack cache merge  # 14
    rebase blob worker handle tree index remote  # 15
    commit token window parser ref rebase ref window object  # 16
    ref branch parser stream rebase buffer merge buffer pack handle ref window  # 17
    stream buffer token object handle rebase queue index merge header  # 18
    delta queue branch ref branch delta queue queue rebase queue  # 19
    remote token ref worker merge commit object  # 20
    merge branch token object cache remote ref header merge  # 21
    stream index remote ref worker socket  # 22
    index pack remote header worker worker  # 23
    tree tree handle socket cache token ref header tree header  # 24
    buffer index branch worker  # 25
    rebase tree worker parser remote window index cache socket object blob pack  # 26
    commit handle header pack branch ref header header parser socket  # 27
    object header handle branch merge  # 28
    blob parser blob delta cache socket header queue branch window merge  # 29
    rebase ref cache blob header cache  # 30
    branch pack branch object ref queue parser buffer rebase  # 31
    rebase window ref merge index commit cache commit worker worker pack  # 32
    parser index commit worker commit ref index tree window tree queue handle  # 33
    merge rebase tree socket merge buffer queue merge  # 34
    object merge buffer stream parser  # 35
    buffer pack blob cache ref queue parser stream stream  # 36
    window ref ref cache queue header remote merge ref  # 37
    stream rebase object object branch window cache  # 38
    blob header commit cache ref object window index cache ref merge  # 39
    commit cache token blob window window ref delta parser ref  # 40
    commit ref branch handle pack socket tree  # 41
    cache tree header merge tree handle branch merge blob object queue queue  # 42
    socket socket token parser pack object header remote index token  # 43
