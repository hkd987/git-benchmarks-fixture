    worker socket delta merge tree  # 0
    remote index buffer parser ref window branch rebase buffer handle  # 1
    parser handle branch blob window rebase buffer object token token socket remote  # 2
    socket token branch blob index tree  # 3
    tree stream socket commit header remote window parser queue worker commit rebase  # 4
    merge merge pack pack  # 5
    branch branch cache queue rebase queue handle queue header delta merge cache  # 6
    remote window cache cache commit commit header window token blob worker  # 7
    remote blob delta index handle buffer delta stream socket worker parser  # 8
    parser commit merge object blob parser cache object header remote  # 9
    token socket stream cache  # 10
    merge delta pack queue handle tree index queue  # 11
    token ref commit delta branch socket merge  # 12
    stream cache index token window remote  # 13
    remote commit rebase pack queue cache branch object header ref token  # 14
    branch object ref handle branch socket remote delta  # 15
    window window commit commit rebase pack ref buffer socket socket remote merge  # 16
    stream rebase merge parser stream tree socket worker cache index  # 17
    pack cache remote tree handle cache  # 18
    rebase branch branch delta blob tree stream cache queue  # 19
    cache queue rebase pack socket token handle buffer buffer index token  # 20
    blob header window stream delta  # 21
    commit queue blob blob window rebase branch  # 22
    pack tree delta merge branch cache buffer tree token tree  # 23
    ref tree branch delta index cache tree window queue  # 24
    tree pack window socket window stream merge tree  # 25
    header cache rebase ref branch buffer rebase cache worker parser worker rebase  # 26
    delta socket parser pack cache handle  # 27
    rebase parser buffer cache parser  # 28
    window ref token pack handle ref commit  # 29
    stream window queue token window token merge tree tree ref  # 30
    delta branch handle delta delta handle header pack commit object commit window  # 31
    socket queue handle index worker  # 32
    delta ref ref header remote merge  # 33
    handle object token commit window parser queue ref pack index object window  # 34
    stream parser delta queue rebase token parser  # 35
    pack blob cache queue handle handle stream object header index  # 36
    cache window stream blob stream remote remote parser merge branch  # 37
    handle branch branch handle merge  # 38
    worker buffer window delta queue ref parser pack  # 39
