    cache window stream worker remote delta stream window commit  # 0
    blob stream index buffer  # 1
    commit worker header branch  # 2
    worker stream index merge  # 3
    merge cache buffer buffer queue tree tree  # 4
    buffer buffer handle header ref handle  # 5
    blob remote index merge remote stream stream  # 6
    header token index rebase token index commit queue pack remote  # 7
    header branch cache merge delta window buffer token token token header index  # 8
    worker handle handle object object commit  # 9
    parser merge rebase socket delta  # 10
    pack blob object tree tree stream buffer  # 11
    buffer pack remote index buffer token tree  # 12
    branch rebase tree commit stream remote pack  # 13
    stream handle commit delta stream index header  # 14
    index rebase token ref remote  # 15
    cache ref object object blob index tree  # 16
    blob token remote token worker worker header worker blob index header buffer  # 17
    worker socket ref queue token ref  # 18
    window ref branch branch index ref token object  # 19
    socket queue branch delta handle queue ref  # 20
    cache worker pack tree  # 21
    window window delta merge  # 22
    ref stream index queue worker  # 23
    stream worker merge buffer  # 24
    cache token token socket cache parser branch queue  # 25
    handle handle worker token  # 26
    socket window blob queue object branch rebase object cache merge rebase ref  # 27
    buffer object token blob token blob delta socket  # 28
    ref queue object blob queue worker commit remote  # 29
    remote socket handle delta window buffer pack blob header  # 30
    branch stream queue window tree index  # 31
    worker index socket pack ref buffer delta cache tree cache queue  # 32
    worker remote queue queue ref socket  # 33
    window header rebase branch parser header  # 34
    pack pack stream worker  # 35
    stream window buffer cache header worker blob remote  # 36
    ref stream object pack token stream commit  # 37
    token blob header header  # 38
    rebase index queue window pack commit merge object commit  # 39
    parser object branch merge blob handle handle index token rebase index  # 40
    object queue stream parser remote stream index ref remote object  # 41
    blob blob object object  # 42
    merge commit buffer window parser socket parser remote window socket parser  # 43
    tree queue commit queue socket blob cache remote queue index branch  # 44
    blob tree stream buffer header parser delta delta branch blob commit  # 45
    remote token merge delta branch stream  # 46
    worker pack tree socket ref worker object buffer ref handle object  # 47
    tree tree object parser merge window  # 48
    branch delta token pack blob socket window window remote index buffer  # 49
    commit cache remote buffer buffer ref remote remote  # 50
    queue branch buffer delta pack index delta merge rebase cache merge queue  # 51
    token ref blob commit parser  # 52
    remote remote commit pack worker  # 53
    handle stream pack cache window pack object object socket remote queue  # 54
    pack object blob worker  # 55
    remote window handle handle ref commit rebase handle socket buffer queue  # 56
    delta remote window blob socket rebase buffer index token tree commit branch  # 57
    window rebase buffer branch ref  # 58
    parser ref tree worker blob branch socket header commit cache ref  # 59
    socket delta token merge pack cache buffer  # 60
    stream socket stream blob queue rebase pack  # 61
    header object header stream pack tree window index delta  # 62
    socket delta handle object commit  # 63
    handle header blob index  # 64
    stream window rebase parser  # 65
    tree ref cache window index stream header commit pack buffer remote header  # 66
    handle window worker buffer  # 67
    pack index branch tree buffer parser cache  # 68
    token ref token ref delta buffer blob stream branch stream index header  # 69
    stream token merge remote rebase stream buffer index cache queue handle  # 70
    rebase commit header buffer  # 71
    branch tree remote object tree branch queue remote tree buffer  # 72
    index pack token window rebase socket stream ref rebase queue token  # 73
    object blob cache handle branch remote tree queue stream index delta  # 74
    merge tree remote ref ref queue stream tree commit  # 75
    delta commit queue index window buffer handle remote  # 76
    object socket pack index stream index blob  # 77
    delta blob tree tree pack  # 78
    header commit handle queue delta queue tree socket queue buffer delta  # 79
    rebase ref token ref  # 80
    ref object pack stream branch rebase parser cache tree  # 81
    handle merge commit rebase socket buffer branch commit object  # 82
    object object ref window branch socket cache parser merge blob handle  # 0
    rebase stream window parser socket header handle rebase ref  # 1
    index rebase commit buffer socket branch tree ref commit cache merge  # 2
    window stream parser merge buffer ref branch tree cache merge cache stream  # 3
    ref worker branch stream handle delta delta cache token queue  # 4
    merge blob queue object buffer tree header pack branch window buffer  # 5
    blob pack blob commit rebase pack commit blob parser  # 6
    branch commit buffer tree  # 7
    commit stream pack handle socket parser ref pack index window window header  # 8
    cache tree rebase buffer token worker  # 9
    stream window blob cache remote window  # 10
    window header socket blob branch token buffer  # 11
    queue socket delta cache merge header remote  # 12
