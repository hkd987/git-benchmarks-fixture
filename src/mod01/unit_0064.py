    delta ref index delta window handle pack  # 0
    tree rebase commit buffer handle object blob worker window ref stream pack  # 1
    worker header blob stream buffer token socket  # 2
    blob pack header cache  # 3
    token worker socket token ref delta rebase commit object parser merge  # 4
    branch rebase blob token cache handle object ref delta index cache branch  # 5
    tree stream commit merge worker  # 6
    remote branch socket stream parser commit header socket  # 7
    blob branch branch blob merge blob ref socket queue object tree handle  # 8
    blob rebase window header remote remote handle handle blob header merge tree  # 9
    worker window commit buffer merge pack blob handle object queue  # 10
    queue buffer socket tree ref index ref token merge  # 11
    rebase delta socket parser merge index blob  # 12
    commit tree cache queue commit ref window buffer parser delta  # 13
    parser token pack blob merge index header parser blob pack  # 14
    parser worker remote handle cache window  # 15
    queue object remote cache queue stream object cache  # 16
    parser header worker window pack  # 17
    object merge parser rebase commit rebase remote token rebase parser queue window  # 18
    header stream ref merge stream rebase buffer blob handle  # 19
    remote blob commit pack stream cache parser merge buffer object ref  # 20
    tree header rebase delta cache merge queue  # 21
    remote tree delta buffer socket parser worker  # 22
    socket object window merge buffer socket commit token token cache pack  # 23
    window socket cache handle pack worker handle worker stream  # 24
    queue index buffer cache window tree branch header pack ref pack  # 25
    buffer cache branch delta pack ref pack worker index branch blob tree  # 26
    parser blob branch queue blob rebase parser  # 27
    header stream pack header window ref branch window branch queue pack  # 28
    worker merge blob token branch  # 29
    buffer blob remote ref  # 30
    handle remote header pack commit tree branch  # 31
    buffer commit worker buffer  # 32
    rebase cache queue socket token parser merge  # 33
