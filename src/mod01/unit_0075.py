    blob remote merge token worker socket worker window  # 0
    branch tree branch commit buffer rebase cache header  # 1
    ref delta delta index branch cache header rebase  # 2
    cache handle object tree token  # 3
    handle queue rebase index branch header buffer token  # 4
    pack socket index window rebase rebase socket buffer cache pack  # 5
    handle stream commit ref buffer rebase blob object  # 6
    window stream token window ref  # 7
    blob object queue parser  # 8
    socket queue worker window window commit buffer  # 9
    commit cache worker tree parser pack token merge handle buffer commit parser  # 10
    handle delta parser handle buffer merge tree header  # 11
    blob token delta cache parser worker queue object queue remote branch delta  # 12
    socket buffer window tree index tree blob token handle parser commit header  # 13
    parser merge token window ref window rebase  # 14
    header socket delta merge merge handle rebase header object  # 15
    header rebase branch handle handle stream token cache socket  # 16
    index window window window commit queue blob buffer  # 17
    index buffer buffer stream rebase stream  # 18
    commit token blob merge blob queue rebase handle tree handle  # 19
    merge remote worker ref merge index handle buffer cache ref  # 20
    branch rebase token remote  # 21
    header ref branch merge tree  # 22
    ref rebase delta buffer  # 23
    merge window buffer queue commit buffer remote  # 24
    handle token blob stream commit delta socket buffer  # 25
    ref rebase worker object  # 26
    index remote parser cache blob buffer handle cache  # 27
    buffer token rebase token token token object rebase handle  # 28
    tree stream window queue index cache header object commit index parser window  # 29
    blob cache rebase queue handle header object remote token object object commit  # 30
    rebase buffer buffer socket  # 31
    queue worker tree delta tree window  # 32
    window tree merge remote parser cache  # 33
    cache parser remote ref socket tree remote remote remote tree  # 34
    worker commit tree tree buffer  # 35
    rebase blob cache buffer header remote worker socket cache  # 36
    index branch pack object  # 37
    pack delta commit delta ref buffer parser cache  # 38
    pack stream socket header branch header stream blob handle object  # 39
    socket worker commit queue queue pack pack ref header object blob  # 40
    buffer buffer handle object handle pack delta  # 41
    index socket branch rebase cache  # 42
    queue rebase window cache buffer  # 43
    object commit tree object handle socket header window queue index  # 44
    token pack blob merge merge handle worker rebase stream pack  # 45
    commit stream pack cache merge tree parser parser window merge buffer  # 46
    token blob remote object branch index handle queue commit pack rebase queue  # 47
    pack cache pack socket window object  # 48
    remote delta commit delta remote worker socket queue handle merge header  # 49
    branch token socket delta ref buffer object token pack worker pack  # 50
