    index cache worker tree rebase ref window stream cache  # 0
    stream ref parser object parser header  # 1
    remote pack parser object header stream commit stream delta index  # 2
    pack merge handle commit blob cache buffer tree cache header  # 3
    socket object socket stream cache  # 4
    handle delta buffer cache  # 5
    pack parser delta delta merge commit ref branch  # 6
    token buffer socket worker header header  # 7
    worker blob socket worker stream buffer worker  # 8
    queue pack merge buffer branch  # 9
    worker stream socket merge tree buffer rebase stream buffer buffer commit  # 10
    stream commit rebase blob blob object merge header  # 11
    blob delta object cache token merge delta header  # 12
    pack header stream stream commit remote cache index  # 13
    commit buffer blob window header  # 14
    rebase stream buffer index stream queue token  # 15
    delta remote buffer tree  # 16
    handle socket commit tree merge pack worker cache handle  # 17
    index buffer cache commit merge header tree buffer cache stream header  # 18
    remote stream delta commit  # 19
    socket window cache cache merge queue cache rebase branch  # 20
    buffer pack worker token handle pack delta  # 21
    socket remote queue ref object object  # 22
    commit buffer blob remote socket object worker blob delta commit parser  # 23
    handle cache ref delta branch blob branch object pack delta ref  # 24
    merge window delta commit ref  # 25
    object cache token stream remote stream socket object cache  # 26
    branch pack token branch worker cache rebase cache tree rebase pack  # 27
    buffer handle remote merge worker socket header pack parser worker buffer  # 28
    tree header rebase header rebase blob cache  # 29
    delta object merge worker  # 30
    delta rebase queue merge ref  # 31
    queue worker stream header commit tree worker object remote queue delta remote  # 32
    pack parser branch branch pack token  # 33
    commit merge window stream rebase  # 34
    remote handle parser branch rebase object handle index rebase  # 35
    header remote merge object blob stream blob pack window  # 36
    parser rebase stream handle branch parser rebase branch queue  # 37
    index stream branch cache tree stream branch queue header buffer delta handle  # 38
    object branch commit object socket socket header  # 39
    worker header pack socket  # 40
    handle object commit queue  # 41
    cache merge stream handle delta stream window worker ref rebase tree rebase  # 42
    buffer queue merge remote branch cache  # 43
    stream delta queue socket header header  # 44
    commit commit object handle queue  # 45
    tree delta buffer delta index tree  # 46
    index stream queue window stream  # 47
    token index buffer buffer window  # 48
    socket buffer object tree blob branch handle rebase  # 49
    delta tree blob socket worker merge ref header handle worker  # 50
    parser branch socket cache worker parser remote  # 51
    header header stream remote parser  # 52
    buffer tree cache stream buffer handle ref parser merge commit window pack  # 53
    window branch remote parser worker tree socket ref  # 54
    parser header tree parser buffer delta branch blob commit  # 55
    queue branch header commit blob stream window cache tree queue header  # 56
    header tree worker rebase worker buffer  # 57
    header remote cache commit handle socket pack stream window  # 58
    parser handle queue header blob worker header stream  # 59
    pack object queue buffer parser ref remote remote socket  # 60
    delta worker buffer ref ref branch delta stream  # 61
    socket index socket socket object token worker worker merge merge delta blob  # 62
    stream window token parser window worker object buffer worker worker  # 63
    socket index tree index merge commit ref handle blob rebase window  # 64
    rebase handle handle commit cache branch queue  # 65
    worker delta merge socket cache ref socket parser  # 66
    index delta cache stream queue blob stream object branch buffer  # 67
    merge stream branch index header buffer delta cache  # 68
    token cache socket tree worker remote index queue ref token ref pack  # 69
    token window worker commit worker delta header remote blob merge commit buffer  # 70
    ref handle commit pack commit  # 71
    window ref stream worker header handle socket delta index branch parser  # 72
    remote parser index tree window  # 73
    window index object index tree pack window worker  # 74
    merge stream buffer header window  # 75
    blob cache handle tree ref cache tree window window remote queue  # 76
    pack pack window ref window commit window  # 77
    queue merge stream stream index pack tree  # 78
    branch commit buffer merge index index queue parser rebase parser blob index  # 79
    socket token token delta commit worker token queue merge  # 80
