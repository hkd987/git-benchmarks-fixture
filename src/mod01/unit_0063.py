    cache handle header cache blob socket cache stream queue buffer delta remote  # 0
    object rebase window cache ref header socket handle queue  # 1
    tree object blob header cache branch  # 2
    remote object handle ref handle delta buffer worker blob stream cache rebase  # 3
    worker queue handle blob  # 4
    remote token window header merge socket  # 5
    merge socket header buffer handle worker index window ref  # 6
    stream object remote stream index parser parser socket rebase queue delta  # 7
    index cache window queue tree socket  # 8
    parser worker header object blob  # 9
    rebase rebase header rebase branch cache commit branch socket branch handle  # 10
    rebase object blob rebase  # 11
    tree cache cache object remote buffer window tree  # 12
    worker branch parser buffer header parser branch ref  # 13
    commit object branch buffer worker worker delta worker stream stream token cache  # 14
    pack ref parser object queue tree merge object delta tree  # 15
    branch tree blob pack queue ref stream  # 16
    index header handle remote queue index  # 17
    window stream blob remote merge header socket cache ref  # 18
    rebase token tree token handle delta  # 19
    pack queue cache branch object merge  # 20
    buffer parser tree cache delta  # 21
    delta branch socket merge  # 22
    cache handle cache buffer blob window ref object commit stream  # 23
    queue handle commit delta delta queue blob stream tree merge rebase header  # 24
    merge stream window handle branch pack delta  # 25
    pack cache handle token branch pack buffer header  # 26
    blob branch cache worker merge index cache window buffer  # 27
    ref delta stream worker pack queue handle buffer stream index  # 28
    remote pack delta branch handle tree stream object delta remote object parser  # 29
    pack header merge window delta object parser handle merge  # 30
    tree delta socket token rebase cache worker window  # 31
    commit cache commit header stream rebase delta parser commit window queue  # 32
    cache token buffer buffer parser window blob  # 33
    cache object ref merge parser handle socket handle commit cache remote  # 34
    stream queue parser ref pack worker buffer  # 35
    queue pack parser commit buffer cache window  # 36
    buffer stream window pack delta token  # 37
    blob pack tree token object index parser cache parser header worker  # 38
    parser header socket object branch buffer ref pack parser  # 39
    blob stream worker pack object worker worker worker tree  # 40
    token pack rebase socket rebase delta blob object  # 41
    token object delta object merge merge handle object stream tree  # 42
    header socket socket buffer header object parser token rebase buffer delta  # 43
    object window branch queue worker object branch queue stream blob  # 44
    commit delta commit handle  # 45
    stream handle cache branch ref delta index  # 46
    delta ref parser index pack object index handle header queue parser pack  # 47
    ref queue blob handle buffer merge delta merge rebase branch  # 48
    socket delta handle branch delta ref socket queue merge index remote  # 49
    worker token rebase blob socket handle  # 50
    handle queue remote commit pack stream  # 51
    object remote pack header pack remote cache  # 52
    object merge worker ref stream worker handle handle blob blob blob  # 53
    window rebase stream cache index ref socket cache cache  # 54
    ref token branch commit branch ref index remote ref header stream rebase  # 55
    tree stream remote window remote cache pack stream delta  # 56
    ref buffer ref pack header ref branch token object worker worker  # 57
    branch stream socket parser token header buffer cache cache cache queue  # 58
    buffer cache delta handle token socket parser branch cache  # 59
    stream ref buffer token stream blob handle header merge parser  # 60
    object header index cache commit  # 61
