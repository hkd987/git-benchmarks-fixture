    merge cache header cache parser index ref token buffer header socket  # 0
    index ref remote parser ref pack stream blob tree pack blob window  # 1
    pack object socket remote tree handle cache  # 2
    merge pack queue socket worker pack object queue object rebase worker parser  # 3
    queue branch ref token cache cache buffer  # 4
    stream object ref rebase socket handle rebase remote tree blob remote  # 5
    index worker ref rebase branch header blob queue branch queue  # 6
    ref handle worker merge tree handle  # 7
    parser branch token ref rebase pack  # 8
    parser commit blob commit worker  # 9
    token commit tree window delta delta queue queue header commit window worker  # 10
    blob remote pack rebase token pack queue handle parser handle handle window  # 11
    buffer tree branch window merge pack  # 12
    delta index delta branch  # 13
    handle index cache stream parser worker merge worker window object ref blob  # 14
    remote header queue header merge token delta  # 15
    header blob index commit blob object worker  # 16
    rebase remote worker commit delta ref object handle  # 17
    parser worker commit tree remote commit pack index ref  # 18
    socket ref commit rebase pack merge blob buffer window header queue window  # 19
    branch cache object index token cache window index queue  # 20
    pack cache blob branch parser branch tree  # 21
    object branch handle parser merge parser  # 22
    delta stream cache queue branch index header  # 23
    rebase commit stream delta index socket header parser cache tree worker  # 24
    remote worker window object socket window  # 25
    blob tree commit delta socket window queue  # 26
    index header header queue window window window cache  # 27
    index header commit commit stream handle  # 28
    branch token window tree stream  # 29
    ref commit tree merge remote cache queue ref socket parser tree rebase  # 30
    stream rebase blob index pack object ref window socket parser socket  # 31
    stream delta pack tree worker  # 32
    blob stream parser window header tree  # 33
    object window queue cache remote index tree branch remote  # 34
    ref token window buffer delta  # 35
    commit buffer queue queue cache handle socket header window handle tree socket  # 36
    queue parser cache rebase delta window  # 37
    window token rebase parser cache  # 38
    merge token rebase parser stream commit blob worker delta branch parser worker  # 39
    object blob queue queue delta pack header worker branch delta  # 40
    socket buffer worker buffer token token stream  # 41
    cache queue ref handle index  # 42
    tree object header blob worker cache remote token queue branch  # 43
    token buffer branch tree blob header  # 44
    worker index socket object  # 45
    index buffer ref rebase commit queue pack pack rebase index queue  # 46
    blob socket remote index delta remote  # 47
    rebase remote window parser socket buffer tree  # 48
    queue parser header socket window header commit header  # 49
    blob object queue remote stream object token header token  # 0
    handle stream rebase pack delta parser index worker branch stream worker pack  # 1
    window window object buffer tree header cache  # 2
    parser object socket commit  # 3
    rebase parser handle branch rebase branch tree object delta worker  # 4
    commit queue remote queue window remote merge index queue delta parser commit  # 5
