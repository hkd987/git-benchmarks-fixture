    commit queue parser ref parser  # 0
    delta cache ref pack branch pack header object pack object  # 1
    window pack object cache worker commit handle  # 2
    worker rebase object window stream pack stream worker parser  # 3
    handle ref tree stream  # 4
    index handle pack handle queue socket object  # 5
    header index branch header worker handle cache remote commit  # 6
    parser rebase branch cache rebase header commit remote blob worker pack  # 7
    merge branch worker queue remote commit remote  # 8
    index rebase delta socket handle header handle commit commit  # 9
    cache parser stream object pack branch cache  # 10
    header object commit queue blob buffer handle worker buffer  # 11
    socket tree buffer merge delta cache stream handle blob branch  # 12
    token stream pack object queue merge remote commit ref cache stream  # 13
    window blob queue ref ref queue  # 14
    rebase blob pack rebase delta ref stream cache branch window queue parser  # 15
    index pack index commit worker window cache blob merge tree  # 16
    delta header pack token  # 17
    rebase merge queue merge commit index queue parser tree  # 18
    stream commit ref queue socket blob merge blob delta header pack  # 19
    handle blob branch remote  # 20
    merge pack object remote remote cache  # 21
    remote token cache queue pack object queue  # 22
    socket stream header pack handle tree  # 23
    parser blob worker remote stream worker index  # 24
    ref merge window header header socket delta  # 25
    token queue worker buffer branch window blob blob queue tree  # 26
    remote worker socket delta token token socket  # 27
    buffer remote parser stream delta queue delta rebase  # 28
    cache blob handle window remote queue pack parser window worker cache  # 29
    merge ref branch object  # 30
    buffer stream object queue remote commit header remote  # 31
    commit index header blob  # 32
    remote remote window queue commit pack worker delta header socket  # 33
    tree header blob pack delta cache remote token stream remote ref worker  # 34
    index stream queue header  # 35
    remote tree blob token socket commit socket pack window  # 36
    pack cache queue merge buffer queue commit  # 37
    index merge branch parser cache commit remote branch header tree index stream  # 38
    index stream window index queue  # 39
    merge merge remote worker buffer delta  # 40
    stream merge rebase header remote remote  # 41
    branch worker merge ref tree pack delta buffer rebase  # 42
    index ref blob branch parser merge window object pack handle branch  # 43
    cache cache blob delta rebase pack header branch worker worker socket  # 44
    delta header blob ref socket  # 45
    handle queue merge remote index tree header  # 46
    socket pack branch handle branch commit worker cache token  # 47
    blob ref commit delta blob rebase  # 48
    stream tree socket index cache queue queue token parser tree  # 49
    window token buffer branch buffer  # 50
