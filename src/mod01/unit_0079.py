    commit worker index socket  # 0
    commit remote stream window index tree delta ref worker handle ref  # 1
    stream parser buffer index merge socket branch index token  # 2
    worker blob token blob worker rebase handle index blob  # 3
    object ref buffer rebase merge ref buffer delta window queue  # 4
    ref tree buffer commit socket header cache blob cache index ref ref  # 5
    buffer ref header blob  # 6
    object merge branch commit buffer remote queue tree delta  # 7
    window buffer queue worker blob  # 8
    pack tree header commit ref queue handle ref ref  # 9
    worker window index branch tree window  # 10
    tree tree socket delta handle merge handle handle index branch branch tree  # 11
    queue merge branch remote ref rebase socket tree socket  # 12
    remote queue window header blob pack queue  # 13
    handle blob token buffer parser  # 14
    tree remote tree rebase socket handle worker  # 15
    index tree commit branch rebase handle blob merge commit  # 16
    branch merge tree socket blob blob parser worker delta remote index  # 17
    window handle commit cache cache merge object socket token branch  # 18
    queue rebase worker buffer branch  # 19
    pack stream token parser cache object queue handle tree  # 20
    parser pack handle socket index blob pack commit branch rebase merge  # 21
    remote handle blob header ref queue index branch object commit handle  # 22
    branch blob blob stream branch worker tree stream remote merge cache  # 23
    token delta branch socket branch delta pack  # 24
    worker token object header  # 25
    cache cache pack socket index ref  # 26
    blob queue remote remote pack index  # 27
    merge worker handle parser  # 28
    pack worker pack parser  # 29
    window rebase remote index  # 30
    object queue worker socket buffer socket delta header merge index window  # 31
    pack remote handle worker commit object buffer stream pack stream  # 32
    remote queue tree ref ref handle token socket blob buffer  # 33
    cache commit worker rebase object socket handle window  # 34
    commit index cache merge cache ref delta rebase header rebase  # 35
    worker buffer header worker object handle buffer delta ref blob  # 36
    handle delta blob rebase token worker  # 37
    blob blob merge object handle pack index window worker remote branch delta  # 38
    object parser socket buffer stream cache parser commit handle  # 39
    object queue handle worker tree window token window rebase  # 40
    stream branch handle cache commit cache parser token rebase handle  # 41
