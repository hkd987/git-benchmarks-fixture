    ref rebase blob socket branch tree token ref commit merge rebase  # 0
    object cache remote parser cache index  # 1
    blob socket socket remote merge merge  # 2
    header pack tree worker  # 3
    tree token commit delta header commit rebase object socket queue merge merge  # 4
    tree delta delta buffer pack rebase buffer pack stream  # 5
    window index buffer queue branch handle index branch  # 6
    parser token cache blob parser index header parser object token object  # 7
    parser token tree stream queue  # 8
    cache index index object token pack queue commit worker  # 9
    merge index index token worker object ref blob merge stream queue worker  # 10
    pack socket socket socket merge ref cache merge token window queue branch  # 11
    worker socket cache header socket  # 12
    rebase token rebase queue rebase merge blob window commit  # 13
    window header merge merge buffer  # 14
    rebase parser window token worker pack buffer worker index  # 15
    header token delta token delta  # 16
    cache parser buffer blob  # 17
    parser delta socket pack index parser  # 18
    buffer delta ref tree remote remote stream rebase branch  # 19
    index socket merge delta queue buffer ref cache tree pack worker  # 20
    ref parser parser tree blob branch remote  # 21
    blob buffer index buffer token  # 22
    index rebase handle branch pack merge socket parser parser remote object header  # 23
    token socket rebase index cache worker token ref  # 24
    token stream merge merge  # 25
    stream socket parser index stream parser stream ref pack  # 26
    token pack window socket  # 27
    cache cache rebase worker  # 28
    buffer remote parser commit object window parser stream tree  # 29
    remote buffer merge header branch pack stream rebase index object index  # 30
    cache parser buffer rebase object header  # 31
    stream blob object worker rebase  # 32
    object blob window handle worker queue index parser  # 33
    token object cache remote blob remote stream  # 34
    stream queue blob parser buffer token window remote queue pack parser  # 35
    header merge branch header queue handle ref merge ref header  # 36
    worker delta handle delta  # 37
    socket tree rebase token commit header ref object  # 38
    pack pack ref blob blob token branch worker  # 39
    token tree merge remote ref cache index  # 40
    object object blob merge handle blob index pack branch pack rebase  # 41
    index worker tree token pack ref cache blob delta window object  # 42
    stream delta delta buffer cache header commit stream ref worker  # 43
    merge object merge parser stream merge tree branch merge header pack  # 44
    cache commit rebase delta header worker worker delta delta buffer worker  # 45
    token pack object ref commit cache header object  # 46
    token buffer delta pack window socket queue remote delta buffer parser handle  # 47
    delta window rebase handle window delta remote  # 48
    header queue remote rebase stream cache remote rebase tree blob index tree  # 49
    commit stream header worker stream  # 50
    window branch blob cache buffer commit ref rebase branch token  # 51
    worker delta stream handle commit handle branch  # 52
    socket object token remote worker stream remote  # 53
    remote parser cache parser queue socket branch  # 54
    buffer object worker parser remote delta handle rebase commit  # 55
    queue header object socket  # 56
    socket ref handle rebase blob branch handle window window commit pack index  # 57
    delta pack token buffer header window cache  # 58
    handle socket index delta  # 59
    header handle header worker header socket remote cache queue object branch  # 60
    cache index window handle merge  # 61
    handle window worker pack  # 62
    branch delta window buffer merge handle ref handle  # 63
    token header merge rebase handle index object merge stream  # 64
    remote header blob handle stream buffer rebase object ref ref  # 65
    token index cache object remote  # 66
    merge stream cache ref  # 67
    object cache window parser  # 68
    tree index remote queue socket merge  # 69
    token pack merge object  # 70
    parser ref branch header object ref  # 71
    socket socket branch branch worker buffer  # 72
    merge token commit commit buffer delta ref tree cache cache  # 73
    blob cache commit queue  # 74
    handle header rebase rebase pack index worker ref header ref tree  # 75
