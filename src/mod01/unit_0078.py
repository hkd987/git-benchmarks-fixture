    queue remote commit branch buffer worker commit branch rebase window queue delta  # 0
    parser merge buffer header cache queue  # 1
    index handle queue blob header socket worker rebase  # 2
    object blob cache token window parser  # 3
    buffer queue handle worker token  # 4
    object delta cache window commit blob stream remote  # 5
    worker remote merge token  # 6
    stream window remote object worker  # 7
    cache ref socket blob buffer cache pack buffer  # 8
    rebase header tree queue parser handle pack buffer branch header commit socket  # 9
    rebase token worker parser object window stream cache index branch worker worker  # 10
    header token delta index queue  # 11
    object blob handle cache handle pack buffer stream parser worker  # 12
    rebase commit token branch window cache  # 13
    index cache ref merge  # 14
    handle merge socket rebase branch index socket blob rebase  # 15
    ref parser buffer worker remote tree parser  # 16
    parser index remote rebase queue worker branch header blob  # 17
    worker tree stream buffer branch handle cache buffer index commit  # 18
    pack queue window window window branch merge ref buffer object parser cache  # 19
    buffer parser rebase worker window  # 20
    merge tree token buffer branch socket queue  # 21
    ref merge merge delta branch buffer  # 22
    queue parser stream rebase buffer token object ref delta  # 23
    window socket remote object ref index object rebase  # 24
    worker rebase worker rebase rebase pack blob object tree  # 25
    parser window rebase handle object header header ref blob index token  # 26
    commit worker parser branch tree commit blob socket worker  # 27
    queue delta remote delta index cache  # 28
    window merge delta rebase pack worker ref queue buffer ref token index  # 29
    object parser handle window commit parser branch header commit  # 30
    branch stream merge window buffer  # 31
    pack window queue rebase branch header window index  # 32
    buffer object commit parser buffer  # 33
    window window parser stream  # 34
    queue stream object window socket ref tree  # 35
    window queue merge rebase object  # 36
    window header parser buffer tree  # 37
    buffer stream pack remote pack rebase  # 38
    index parser parser blob merge blob blob socket pack handle delta  # 39
    pack tree tree tree index ref handle cache branch queue remote  # 40
    commit cache object delta index handle ref window object  # 41
    pack token index header delta index commit  # 42
    handle window pack parser merge  # 43
    pack blob blob ref blob queue handle header handle blob socket header  # 44
    remote parser socket rebase blob socket  # 45
    tree tree branch socket merge pack blob  # 46
    object cache worker object buffer handle branch token worker token  # 47
    buffer merge branch token worker queue blob header parser queue index  # 48
    window cache buffer queue window remote handle token cache  # 49
    rebase tree handle worker socket blob rebase socket object remote stream  # 50
    rebase delta worker header delta stream tree header  # 51
    token object stream rebase cache handle tree queue rebase buffer  # 52
    stream delta cache branch ref  # 53
    parser socket worker commit remote branch buffer delta worker object parser  # 54
    cache delta branch commit ref handle  # 55
    socket handle tree header ref handle pack queue branch object rebase  # 56
