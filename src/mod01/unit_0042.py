    stream worker rebase object token parser cache  # 0
    index queue index tree commit merge cache  # 1
    queue commit commit parser  # 2
    commit socket ref tree  # 3
    stream commit worker parser  # 4
    worker tree object delta merge window delta token delta  # 5
    merge socket token stream blob parser branch pack  # 6
    branch index parser remote handle queue window branch stream ref socket  # 7
    tree tree branch pack header stream buffer worker cache  # 8
    pack remote cache delta  # 9
    queue queue pack socket  # 10
    index buffer worker socket worker header queue branch commit cache merge  # 11
    token blob merge ref parser branch object stream ref  # 12
    token socket object parser commit socket window blob branch index delta  # 13
    rebase ref socket object token  # 14
    handle merge cache cache branch pack blob merge  # 15
    worker index index header cache delta handle queue commit socket pack delta  # 16
    cache branch window branch blob cache worker  # 17
    object parser commit object header object index delta socket commit  # 18
    ref parser commit parser socket pack  # 19
    worker socket ref queue tree  # 20
    index queue cache branch cache commit parser parser header  # 21
    remote rebase merge rebase socket pack pack  # 22
    cache window parser handle remote window token  # 23
    header pack header cache commit window  # 24
    header commit parser blob tree index ref commit merge tree  # 25
    handle cache worker tree stream commit blob rebase stream  # 26
    tree token worker remote ref rebase remote worker token ref  # 27
    worker branch tree cache header queue buffer remote  # 28
    rebase header blob branch rebase rebase ref buffer commit socket cache queue  # 29
    merge cache remote tree delta object branch socket socket index merge header  # 30
    object remote buffer buffer delta branch cache socket  # 31
    token queue worker window rebase header header  # 32
    pack stream delta ref header tree delta object pack remote  # 33
    pack blob token window ref parser token token blob branch ref  # 34
    object header token pack stream index tree  # 35
    parser blob worker tree buffer token  # 36
    parser stream worker commit commit tree cache socket merge ref token header  # 37
    branch worker queue socket index worker  # 38
    object commit rebase queue rebase worker merge object object stream  # 39
    token commit pack handle  # 40
    pack queue queue delta index remote buffer worker  # 41
    branch header stream parser pack socket commit merge tree  # 42
    delta parser tree header handle ref ref  # 43
    blob ref header socket parser header stream index  # 44
    parser worker cache remote cache queue token token blob worker commit merge  # 45
    socket cache socket worker commit  # 46
    remote queue remote tree pack  # 47
    ref delta token branch socket commit merge index  # 48
    socket index parser merge remote commit stream index  # 49
    stream token tree socket branch remote remote socket parser  # 50
    cache handle buffer rebase worker handle window remote buffer  # 51
    cache object commit branch cache handle worker queue header  # 52
    buffer queue queue delta handle queue parser rebase  # 53
    delta worker tree queue stream  # 54
    buffer index commit socket socket index blob delta  # 55
    delta commit index worker  # 56
    branch stream ref blob  # 57
    window ref tree branch worker handle tree delta  # 58
    buffer delta socket object  # 59
    parser tree window branch window merge rebase socket commit  # 60
    index queue worker remote rebase commit  # 61
    merge commit socket socket remote header window ref cache  # 62
    tree handle buffer handle window worker delta commit tree  # 63
    window worker tree stream socket blob cache  # 64
    branch tree remote branch merge token object  # 65
    delta buffer parser delta object token window ref stream socket pack  # 66
    pack delta rebase token cache branch commit blob blob branch socket merge  # 67
    worker socket merge handle rebase handle window object  # 68
    merge header header cache handle parser rebase rebase delta cache tree commit  # 69
    stream tree header index delta cache pack socket  # 70
    object pack cache ref blob queue  # 71
    worker ref object header token tree worker object blob  # 72
    ref blob window branch worker token worker object ref remote commit  # 73
    commit handle worker ref  # 74
    handle index ref queue commit socket stream window header cache  # 75
    queue token queue cache tree worker  # 76
