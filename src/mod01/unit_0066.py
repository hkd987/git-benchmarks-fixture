    handle branch header merge  # 0
    pack header delta pack cache rebase index pack  # 1
    token buffer rebase window  # 2
    handle ref ref rebase  # 3
    delta tree ref cache branch index ref  # 4
    delta cache worker commit  # 5
    token queue queue window commit merge worker queue commit delta ref branch  # 6
    branch worker handle remote blob queue cache cache  # 7
    rebase stream ref object object token commit branch queue  # 8
    blob commit merge queue tree tree remote queue  # 9
    blob merge branch cache branch buffer handle  # 10
    index queue index merge branch worker index header  # 11
    stream token queue blob  # 12
    token object blob commit cache merge rebase header delta delta handle rebase  # 13
    stream delta handle pack token cache rebase  # 14
    remote worker pack pack token blob token remote commit  # 15
    commit remote tree tree object buffer commit queue stream  # 16
    rebase window ref queue blob parser window pack object handle merge merge  # 17
    rebase remote socket index commit parser blob branch  # 18
    window merge worker pack  # 19
    branch commit pack header  # 20
    object pack window worker tree parser  # 21
    queue blob branch branch merge delta header object  # 22
    parser handle remote worker buffer rebase merge remote worker  # 23
    handle blob buffer object tree  # 24
    delta worker tree index socket delta window remote  # 25
    object handle buffer token handle tree socket pack  # 26
    index stream rebase rebase blob  # 27
    worker token blob index merge cache  # 28
    worker cache merge pack cache token buffer ref socket worker pack  # 29
    ref branch rebase tree index delta object buffer socket buffer index stream  # 30
    token merge rebase queue cache index tree remote blob queue index socket  # 31
    remote index delta window rebase socket tree blob cache socket  # 32
    socket stream ref blob handle  # 33
    rebase cache object commit worker buffer socket stream object cache  # 34
    parser worker branch delta socket  # 35
    worker handle cache header rebase buffer window index pack commit commit  # 36
    worker merge commit delta queue token pack  # 37
    cache ref stream worker window buffer tree socket  # 38
    blob pack socket merge header object worker rebase worker tree branch pack  # 39
    remote parser merge branch merge delta commit ref  # 40
    commit parser pack parser index cache index  # 41
    token commit remote blob  # 42
    queue delta blob queue blob commit header commit branch merge  # 43
    rebase ref index window branch object ref  # 44
    index worker branch merge header delta header blob buffer  # 45
    tree worker cache handle commit  # 46
    merge socket buffer header merge commit handle parser branch handle queue  # 47
    stream queue index parser  # 48
    branch worker cache stream cache index remote index delta rebase window remote  # 49
    window handle index merge commit commit rebase buffer stream token window commit  # 50
    token pack queue merge ref cache header rebase handle token  # 51
    remote remote remote header rebase commit handle delta ref cache delta tree  # 52
    parser rebase handle queue object branch branch socket  # 53
    object pack merge object object index commit  # 54
    stream header index buffer blob queue commit pack object index  # 55
    pack window token token token cache commit  # 56
    parser stream parser window parser queue worker window index delta object  # 57
    merge merge remote header merge  # 58
    merge stream merge socket  # 59
    blob pack window window window merge index index parser buffer branch  # 60
    handle buffer branch index window socket  # 61
