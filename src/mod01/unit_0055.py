    stream cache branch window cache token  # 0
    index pack tree merge delta cache worker branch window queue parser  # 1
    window pack ref socket delta remote pack commit index  # 2
    worker socket worker header cache  # 3
    socket socket pack window branch stream window branch  # 4
    stream blob ref index tree socket  # 5
    worker stream remote socket token commit blob token window socket index header  # 6
    merge handle ref merge delta window window  # 7
    ref header parser cache socket worker tree token delta  # 8
    branch cache delta merge handle ref commit queue window tree queue  # 9
    cache token queue tree tree merge blob object stream  # 10
    parser parser handle delta  # 11
    queue tree queue queue socket socket queue branch  # 12
    header window parser buffer rebase  # 13
    handle branch remote blob  # 14
    cache queue queue branch pack window socket object pack token pack buffer  # 15
    stream object socket queue remote cache window token token socket  # 16
    queue cache parser queue stream socket parser pack tree worker  # 17
    commit worker remote delta parser branch stream  # 18
    commit parser blob pack header stream pack parser index pack window pack  # 19
    remote token handle queue worker  # 20
    token tree delta index worker worker remote commit  # 21
    queue blob ref worker delta  # 22
    remote rebase buffer index delta handle token  # 23
    parser tree worker ref pack socket index tree merge cache socket  # 24
    cache commit rebase remote merge object branch  # 25
    header queue merge branch commit delta header branch commit remote index blob  # 26
    merge pack header ref blob pack index remote parser socket stream blob  # 27
    object blob cache cache index worker pack queue object branch  # 28
    blob handle handle rebase ref cache window worker object ref  # 29
    tree token tree buffer branch  # 30
    header index handle delta commit window  # 31
    pack ref merge worker blob tree pack object worker delta queue delta  # 32
    remote token rebase rebase remote handle index buffer header cache commit  # 33
    ref stream cache merge delta ref window worker  # 34
    queue commit worker object delta queue worker tree rebase cache  # 35
    blob remote worker ref remote stream token object  # 36
    remote tree pack index pack buffer index pack queue branch index  # 37
    cache merge blob merge  # 38
    cache commit rebase buffer pack branch delta window  # 39
    handle rebase worker token stream ref  # 40
    cache queue token buffer  # 41
    ref queue cache commit merge stream object  # 42
    commit branch branch blob blob  # 43
    commit buffer socket rebase socket window blob remote pack buffer delta queue  # 44
    remote stream pack stream token branch commit object buffer window  # 45
    queue rebase tree socket merge handle stream rebase pack branch queue  # 46
    rebase blob stream ref index handle buffer pack commit buffer merge object  # 47
    cache blob socket ref pack socket blob merge pack object window remote  # 48
    cache cache rebase rebase object handle cache queue blob socket merge  # 49
