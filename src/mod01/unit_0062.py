    queue remote queue commit handle delta index parser window remote header  # 0
    branch merge rebase merge delta cache pack  # 1
    merge object index token  # 2
    pack token token rebase worker cache  # 3
    delta header header commit index header  # 4
    remote branch index tree header index  # 5
    blob token stream commit blob worker branch pack socket delta  # 6
    delta pack stream window delta socket cache buffer merge index ref parser  # 7
    blob merge pack queue merge rebase branch socket stream pack  # 8
    header object remote blob blob handle branch commit  # 9
    window window stream stream header merge object queue index  # 10
    index socket merge remote rebase remote stream window handle  # 11
    index blob socket socket queue ref  # 12
    stream handle queue ref object token object  # 13
    ref object commit blob blob remote window index object  # 14
    remote handle delta queue pack rebase tree parser remote delta token  # 15
    worker ref socket handle header remote  # 16
    ref branch merge header  # 17
    merge buffer worker remote delta rebase  # 18
    cache blob object pack buffer cache  # 19
    handle stream socket stream token branch  # 20
    tree queue token parser parser window merge token rebase worker queue  # 21
    stream buffer cache window  # 22
    rebase tree socket tree queue index stream cache blob header cache stream  # 23
    object worker remote pack object stream queue pack object worker parser  # 24
    handle socket blob socket token delta tree cache  # 25
    object blob worker cache remote tree  # 26
    ref stream delta commit socket handle token delta  # 27
    token commit delta blob header  # 28
    stream buffer queue buffer pack index pack  # 29
    parser branch merge stream header  # 30
    remote tree window header object  # 31
    parser index merge parser merge blob blob stream tree  # 32
    header object token rebase worker pack worker merge parser merge socket  # 33
    stream branch handle parser delta  # 34
    cache blob socket tree buffer parser queue worker tree  # 35
    tree remote index ref pack token index stream  # 36
    token ref queue handle delta rebase cache blob queue object buffer cache  # 37
    handle branch socket cache  # 38
    rebase pack parser handle worker merge  # 39
    token queue cache cache cache remote  # 40
    branch branch handle window merge pack delta blob buffer blob buffer  # 41
    pack cache rebase buffer commit socket token remote queue  # 42
    token remote index delta buffer parser  # 43
    socket stream parser merge cache rebase stream object  # 44
    index index pack commit token worker merge header commit blob stream delta  # 45
    buffer index remote branch parser handle  # 46
    stream remote ref merge handle index stream  # 47
    rebase worker token stream socket remote socket buffer socket  # 48
    header worker branch ref  # 49
    parser object handle token window remote pack worker merge merge  # 50
    queue blob blob handle stream blob pack token object stream handle tree  # 51
    buffer rebase object token  # 52
    branch object commit buffer ref worker delta  # 53
    buffer remote pack ref ref  # 54
    parser branch delta delta parser window handle  # 55
    rebase merge parser parser  # 56
