    index socket header ref branch delta  # 0
    handle branch stream object ref pack socket tree cache ref tree  # 1
    window index socket delta remote buffer cache tree cache window socket commit  # 2
    rebase parser cache socket ref buffer stream  # 3
    merge remote branch merge  # 4
    merge worker header buffer window remote handle merge ref  # 5
    header stream stream handle blob  # 6
    window window pack window queue buffer stream branch queue  # 7
    socket object worker tree merge rebase pack pack socket buffer  # 8
    parser buffer socket cache token window queue token merge tree  # 9
    buffer window object object object blob commit blob cache parser  # 10
    buffer pack ref window remote handle rebase branch window merge blob  # 11
    window remote branch object handle remote queue handle merge commit object window  # 12
    index header merge token rebase token handle commit buffer object queue socket  # 13
    blob parser queue parser stream  # 14
    branch branch token remote rebase branch cache commit remote merge ref tree  # 15
    index delta merge delta  # 16
    cache commit object stream blob token object commit parser ref  # 17
    tree ref delta delta header merge header index blob  # 18
    token cache handle parser handle ref rebase  # 19
    handle delta remote pack buffer buffer ref index worker stream branch window  # 20
    worker queue socket commit rebase  # 21
    index branch delta object pack token cache cache  # 22
    header stream branch cache tree delta stream pack header  # 23
    cache worker worker pack  # 24
    remote rebase merge token socket parser window header rebase window object commit  # 25
    token token worker stream delta commit  # 26
    branch handle stream header merge token queue  # 27
    handle cache cache cache object merge merge handle delta  # 28
    rebase ref token stream rebase queue parser window queue index object  # 29
    delta ref object parser  # 30
    commit remote merge header rebase parser tree parser tree rebase  # 31
    rebase header header queue index index  # 32
    object rebase blob cache  # 33
    token ref remote pack worker object  # 34
    stream branch cache buffer branch handle parser pack blob merge  # 35
    merge pack tree merge  # 36
    branch worker rebase stream  # 37
    handle ref socket worker header worker token  # 38
    cache header window delta  # 39
    socket cache window stream worker worker  # 40
    remote worker branch worker index socket queue cache cache token  # 41
    header delta commit window header buffer commit rebase index header queue queue  # 42
    rebase header tree object window ref queue ref buffer token queue  # 43
    commit index header ref parser index rebase  # 44
    merge token index worker merge ref buffer header commit header  # 45
    object branch remote merge buffer rebase token index  # 46
    object socket merge commit cache queue cache pack parser socket  # 47
    parser delta tree worker rebase  # 48
    tree delta socket index stream token branch  # 49
    window tree header merge cache delta parser socket commit ref  # 50
    blob socket rebase merge index socket token object buffer  # 51
    remote parser blob tree merge  # 52
    rebase tree tree pack pack remote remote header rebase token parser  # 53
    commit cache worker cache tree  # 54
    delta pack object header branch socket cache header rebase  # 55
    header rebase handle object buffer rebase handle window object token parser tree  # 56
    merge pack rebase branch buffer  # 57
