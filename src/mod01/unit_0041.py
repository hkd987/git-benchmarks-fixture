    rebase queue handle queue handle cache blob pack remote pack worker index  # 0
    commit remote header ref merge rebase object handle delta pack index queue  # 1
    object token blob worker commit  # 2
    worker parser object parser tree rebase remote handle ref branch delta  # 3
    worker buffer remote buffer pack delta commit tree token header socket rebase  # 4
    parser window remote rebase merge merge buffer queue header pack  # 5
    worker branch token index merge blob commit  # 6
    socket ref handle tree buffer queue delta  # 7
    buffer queue handle branch tree  # 8
    token buffer blob worker stream  # 9
    index object buffer worker ref delta remote ref queue queue buffer delta  # 10
    buffer token header branch stream blob  # 11
    object handle remote blob delta  # 12
    token object buffer window stream worker object socket ref  # 13
    tree commit blob handle window token tree branch pack worker merge  # 14
    buffer pack worker token tree  # 15
    parser token tree queue worker  # 16
    buffer object blob merge  # 17
    header pack index rebase socket buffer branch stream remote ref branch index  # 18
    buffer parser pack delta worker  # 19
    pack buffer window queue stream remote merge window pack buffer  # 20
    token parser branch delta remote pack cache  # 21
    handle merge remote pack pack header remote token header queue socket worker  # 22
    rebase branch merge header stream blob delta branch  # 23
    parser cache queue rebase worker stream stream handle blob socket merge  # 24
    delta token index token tree ref worker  # 25
    stream pack window parser  # 26
    buffer pack header handle handle window merge socket branch index worker merge  # 27
    handle window buffer remote parser ref  # 28
    remote ref cache remote  # 29
    stream cache window header  # 30
    delta index pack header socket index pack blob parser tree  # 31
    tree ref socket rebase queue tree blob  # 32
    token token buffer queue object buffer buffer blob parser worker blob  # 33
    remote rebase window handle worker queue buffer delta object ref  # 34
    header branch cache ref  # 35
    blob branch index rebase queue queue delta worker cache queue remote token  # 36
    ref index rebase socket token merge header socket  # 37
    merge branch branch token queue handle parser blob ref stream  # 38
    buffer index blob handle header ref rebase  # 39
    tree commit stream handle index ref tree  # 40
    stream tree rebase worker commit index tree delta cache ref  # 41
    queue tree cache tree remote commit stream merge tree  # 42
    ref index tree branch tree handle token header worker header parser handle  # 43
    rebase delta remote header cache socket remote  # 44
    remote blob ref merge index ref handle blob pack worker tree  # 45
    header window merge header rebase cache remote socket header  # 46
    buffer token worker branch delta stream queue pack branch branch  # 47
    remote index pack index queue branch  # 48
    branch stream delta stream header worker parser object header commit queue  # 49
    pack ref parser queue parser branch socket  # 50
    rebase branch worker blob index stream  # 51
    index object token cache pack rebase worker window  # 52
    index commit ref worker window remote  # 53
    window parser handle commit branch parser object branch branch rebase parser  # 54
    stream handle queue blob pack remote commit header  # 55
    buffer pack socket object queue  # 56
    token parser header merge delta token  # 57
    blob tree blob parser blob remote delta handle header tree  # 58
