    parser parser cache blob object socket cache pack window  # 0
    handle ref header stream rebase tree branch parser delta parser  # 1
    pack tree rebase tree branch  # 2
    pack handle socket merge handle token delta tree pack  # 3
    branch stream ref window merge parser socket parser  # 4
    parser commit rebase handle commit tree queue object handle  # 5
    window delta remote parser token header token queue stream index cache ref  # 6
    branch tree buffer remote ref delta tree  # 7
    worker object header buffer branch  # 8
    header worker cache worker  # 9
    remote cache blob token pack worker queue pack parser window stream  # 10
    index worker merge tree window  # 11
    cache token socket remote pack merge merge token object tree token  # 12
    merge delta delta token index  # 13
    window stream pack worker handle queue parser stream  # 14
    handle parser handle branch token token window rebase index rebase  # 15
    rebase index token buffer remote blob tree remote  # 16
    socket worker header merge delta worker delta cache index buffer delta  # 17
    buffer ref object token  # 18
    stream commit header object handle rebase delta delta tree rebase ref window  # 19
    worker tree handle stream buffer cache worker commit parser  # 20
    commit worker pack index header worker queue socket rebase stream cache  # 21
    header delta handle parser  # 22
    ref stream delta queue stream object  # 23
    handle token buffer pack rebase pack  # 24
    ref merge queue delta remote pack stream blob object rebase  # 25
    token token remote rebase  # 26
    worker buffer stream merge stream queue tree window handle  # 27
    token index buffer handle pack  # 28
    merge stream queue worker buffer commit  # 29
    cache token worker parser window index worker branch tree pack handle  # 30
    header window merge pack remote worker header branch  # 31
    blob worker header parser delta  # 32
    cache token handle object queue rebase remote delta window remote blob stream  # 33
    parser socket remote tree merge  # 34
    blob token object pack header branch rebase blob  # 35
    window window index stream pack header header rebase socket remote  # 36
    merge handle token buffer header buffer merge commit window  # 37
    handle handle window handle handle blob token  # 38
    worker object blob commit buffer queue delta parser buffer  # 39
    pack tree merge handle stream parser remote remote parser index  # 40
    worker branch buffer pack merge blob  # 41
    buffer merge stream merge socket  # 42
    stream stream ref commit header commit object branch window window window ref  # 43
    handle delta index commit delta socket socket buffer  # 44
    tree parser header rebase  # 45
    buffer queue ref header window blob worker delta window remote queue index  # 46
    window rebase merge object branch socket  # 47
    parser worker delta worker header header blob  # 48
    index socket index window buffer remote stream buffer blob parser  # 49
    socket tree pack object  # 50
    cache object cache window  # 51
    worker object token queue commit  # 52
    handle header buffer queue handle remote ref buffer cache  # 53
    buffer rebase socket pack cache merge buffer branch blob handle delta  # 54
    stream token object cache worker object parser remote rebase socket stream  # 55
    parser pack queue window header token queue buffer buffer  # 56
    queue window window ref stream worker header merge remote ref stream handle  # 57
    merge handle pack cache pack token blob parser worker delta buffer stream  # 58
    worker commit object cache delta socket pack parser object header  # 59
    window socket index buffer handle rebase queue object  # 60
    window handle tree worker tree handle object handle rebase  # 61
    ref branch worker blob header parser queue ref index ref branch index  # 62
    socket parser delta header merge pack queue queue object  # 63
    index buffer index delta socket commit commit tree index branch  # 64
    window stream remote stream index queue blob cache commit merge  # 65
    rebase pack window pack blob buffer index queue merge remote  # 66
    remote token tree pack socket  # 67
    merge worker delta window worker  # 68
    object remote handle worker socket merge blob object delta branch index socket  # 69
    handle parser queue cache object  # 70
    socket rebase rebase blob branch merge blob commit index object  # 71
    merge commit ref object header cache remote buffer commit stream  # 72
    index header window branch worker token remote queue pack worker branch remote  # 73
    socket stream buffer rebase cache commit cache ref remote window stream  # 74
    cache token cache socket  # 75
    buffer worker remote handle  # 76
    merge delta rebase cache buffer cache  # 77
    merge remote merge ref buffer token cache stream remote delta  # 78
    object window rebase socket cache buffer rebase queue  # 79
    tree branch socket socket commit  # 80
    index socket worker remote  # 81
    tree worker handle merge window stream ref  # 82
