    pack rebase index stream branch handle  # 0
    queue handle cache worker blob buffer rebase  # 1
    window branch stream commit  # 2
    cache tree blob delta stream branch index commit queue commit commit  # 3
    token delta commit stream commit window window stream pack merge object delta  # 4
    branch socket header delta parser  # 5
    header cache blob ref queue  # 6
    parser tree buffer delta merge commit queue header remote  # 7
    index delta delta handle blob tree blob remote handle  # 8
    tree remote commit header pack window remote delta  # 9
    socket parser socket socket worker rebase object merge socket delta object  # 10
    socket delta stream merge header index worker index index  # 11
    object handle branch blob token  # 12
    handle pack remote token ref handle pack worker  # 13
    blob commit commit blob delta window window  # 14
    index blob cache rebase blob handle window ref commit  # 15
    remote queue object branch window object object  # 16
    ref blob socket handle commit index queue parser  # 17
    commit tree socket stream commit socket header remote cache token handle pack  # 18
    stream token rebase parser parser delta socket pack delta merge rebase  # 19
    stream pack buffer socket object rebase queue handle commit  # 20
    handle socket stream tree delta remote  # 21
    tree socket pack window handle window  # 22
    object parser ref header ref remote object tree window merge worker blob  # 23
    ref parser token merge header worker worker delta branch handle worker buffer  # 24
    tree token token queue worker commit merge remote  # 25
    handle branch blob merge  # 26
    header parser index tree rebase blob header  # 27
    branch window worker pack rebase blob merge blob blob remote parser  # 28
    blob stream commit pack  # 29
    delta remote cache tree remote stream ref handle socket handle buffer  # 30
    delta merge remote header commit  # 31
    queue handle cache cache token branch rebase tree remote handle object header  # 32
    header remote window index commit cache commit worker socket token branch  # 0
    branch index window branch stream handle token ref handle  # 1
    cache header blob tree tree  # 2
    blob branch tree commit token  # 3
    pack pack ref buffer cache blob tree branch rebase window  # 4
    queue buffer index cache tree index delta queue  # 5
    merge index branch handle index  # 6
    token delta merge buffer socket token parser commit window rebase remote branch  # 7
    parser stream worker cache pack merge window blob token window  # 0
    ref parser queue parser handle index merge ref ref  # 1
    tree merge object pack worker blob cache remote object rebase token delta  # 2
