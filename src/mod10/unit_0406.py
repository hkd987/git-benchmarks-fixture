    pack buffer socket index object branch parser cache delta  # 0
    object delta remote worker commit branch branch  # 1
    socket ref delta window buffer token  # 2
    branch commit rebase buffer cache commit socket socket  # 3
    worker handle blob rebase merge pack remote token  # 4
    cache socket remote worker rebase handle ref rebase  # 5
    parser buffer token worker pack stream stream blob tree worker  # 6
    delta blob commit tree  # 7
    parser cache queue merge cache header delta pack object header object  # 8
    worker object worker index header header socket socket cache merge index worker  # 9
    index token branch branch index  # 10
    index pack window merge blob  # 11
    header buffer window index buffer merge queue tree branch branch stream  # 12
    commit cache stream ref remote parser handle merge window object window merge  # 13
    remote blob delta window queue object header token index ref branch buffer  # 14
    stream stream ref socket pack  # 15
    pack object index remote header token stream pack  # 16
    parser header window header index handle branch  # 17
    blob header index worker token worker stream merge  # 18
    worker merge buffer stream socket tree  # 19
    buffer token delta rebase  # 20
    object buffer cache socket object remote  # 21
    header stream index queue handle object window queue merge  # 22
    rebase window queue window delta merge queue  # 23
    blob rebase branch window tree remote parser stream  # 24
    tree branch buffer commit commit buffer cache handle header buffer socket queue  # 25
    cache rebase worker buffer branch parser branch  # 26
    tree token queue delta branch merge header pack header rebase window queue  # 27
    object merge handle commit parser object parser blob socket  # 28
    queue merge parser index blob token pack handle token merge  # 29
    remote socket token worker rebase token branch remote handle  # 30
    delta handle delta delta  # 31
    window rebase token remote window tree socket header  # 32
    buffer tree delta blob header object buffer delta cache token  # 33
    branch socket stream branch  # 34
    handle ref tree buffer cache buffer index  # 35
    handle branch socket pack branch pack rebase  # 36
    delta remote pack worker worker index window index ref delta tree remote  # 37
    cache token commit branch merge commit cache index commit object  # 38
    worker queue header commit branch header commit ref ref pack commit cache  # 39
    header header ref worker  # 40
    tree pack object tree window header stream  # 41
    object branch window buffer  # 42
    ref parser worker tree merge stream commit object  # 43
    handle merge worker socket branch blob  # 44
    buffer index branch index pack stream  # 45
    stream blob cache pack tree blob object handle socket token buffer merge  # 46
    branch object queue window branch window  # 47
    blob token merge rebase tree socket commit delta cache object  # 48
    merge index delta worker remote  # 49
    cache stream queue rebase ref  # 50
    queue parser remote socket header object branch  # 51
    merge cache index blob  # 52
    pack stream ref delta buffer stream delta  # 53
    rebase commit merge remote socket branch socket header window tree blob  # 54
    remote rebase cache window rebase delta token ref window tree handle cache  # 55
    commit branch commit merge branch handle buffer ref stream socket socket worker  # 56
    socket window rebase branch branch  # 57
    index cache rebase buffer handle index parser  # 58
    branch delta buffer header buffer buffer queue index object blob token merge  # 59
    worker stream handle ref rebase  # 60
    stream delta commit object header  # 61
    buffer index cache branch blob blob handle header worker object cache  # 62
    header worker rebase object tree cache  # 0
    branch tree worker worker remote stream remote pack remote buffer tree token  # 1
    blob branch socket branch rebase  # 2
    header ref pack delta  # 3
