    queue object delta stream index rebase merge index handle remote window  # 0
    branch stream worker ref ref ref blob  # 1
    object parser branch merge  # 2
    window cache merge remote worker  # 3
    socket buffer socket commit cache branch delta branch merge token pack index  # 4
    tree handle object worker tree buffer ref queue branch commit commit  # 5
    queue pack index branch buffer index  # 6
    stream object handle pack worker queue cache commit token  # 7
    parser token queue branch token commit window token handle  # 8
    rebase token handle pack socket ref object cache  # 9
    blob socket handle blob remote blob buffer delta  # 10
    merge cache rebase tree object tree token index  # 11
    cache worker blob object blob worker pack parser index  # 12
    parser header commit socket merge merge stream parser  # 13
    buffer buffer rebase cache handle token pack token socket handle merge pack  # 14
    remote ref token cache remote pack remote buffer queue stream socket window  # 15
    pack queue parser branch queue remote  # 16
    rebase token blob branch branch cache header branch blob queue  # 17
    cache blob index header queue ref stream stream blob ref buffer worker  # 18
    stream parser branch header commit handle delta token buffer cache  # 19
    socket queue branch ref queue  # 20
    header worker worker delta parser blob worker stream  # 21
    branch object branch cache cache cache window tree merge socket blob  # 22
    merge merge merge branch cache  # 23
    queue ref socket stream token cache buffer token stream  # 24
    remote queue handle stream token tree socket cache rebase pack index index  # 25
    handle pack cache parser index rebase object commit socket index buffer  # 26
    pack worker remote commit delta blob branch branch tree object ref  # 27
    pack remote tree queue worker remote stream index parser stream  # 28
    object delta buffer index header token branch object  # 29
    commit remote ref worker socket blob rebase parser  # 30
    stream stream rebase queue object index worker index ref  # 31
