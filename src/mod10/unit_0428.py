    queue tree merge object header worker ref token tree socket window ref  # 0
    handle header blob merge delta  # 1
    commit commit rebase handle buffer ref pack  # 2
    header index socket remote ref ref pack parser delta pack  # 3
    header remote commit header queue commit stream pack token header index pack  # 4
    handle blob pack queue commit queue rebase stream delta commit merge  # 5
    parser window delta rebase  # 6
    parser buffer worker window merge window  # 7
    ref stream rebase stream index pack  # 8
    stream window buffer queue cache pack buffer ref  # 9
    queue header tree pack merge socket object token remote delta remote  # 10
    queue commit tree rebase delta handle buffer header merge worker delta merge  # 11
    remote blob index parser ref  # 12
    tree blob rebase merge branch object buffer header object  # 13
    buffer index delta stream queue remote queue queue rebase  # 14
    stream delta ref token handle queue  # 15
    rebase blob token object stream commit index queue stream  # 16
    object index buffer token worker index socket handle buffer rebase commit  # 17
    blob delta token delta remote merge merge blob tree  # 18
    rebase socket queue delta socket stream stream  # 19
    ref worker stream commit queue parser queue object  # 20
    merge cache tree delta buffer rebase remote parser blob header  # 21
    parser parser object stream header  # 22
    stream stream remote parser ref  # 23
    handle parser merge window commit cache blob socket blob  # 24
    worker header pack token stream ref ref remote token merge  # 25
    branch header rebase merge  # 26
    ref stream branch blob ref token socket worker  # 27
    pack parser remote delta branch cache index object socket  # 28
    index pack remote buffer ref parser branch  # 29
    rebase branch window index parser buffer  # 30
    rebase stream branch remote object ref token delta  # 31
    worker pack cache merge merge tree handle worker branch blob delta parser  # 32
    worker worker header worker object  # 33
    remote buffer ref branch  # 34
    queue socket rebase queue window header worker header  # 35
    queue delta blob rebase tree token stream  # 36
    tree rebase ref handle index token stream  # 37
    pack cache remote tree worker ref object tree stream tree blob window  # 38
    remote token object remote window buffer token socket worker  # 39
    header buffer parser token branch handle merge buffer branch buffer blob  # 40
    branch buffer merge parser worker handle delta window pack branch blob  # 41
    buffer handle blob ref commit branch rebase socket  # 42
    cache worker tree parser  # 43
    delta pack remote header header  # 44
    remote handle buffer parser branch socket socket merge  # 45
    socket tree ref merge merge cache worker merge branch worker object  # 46
    commit stream tree index cache  # 47
    object header branch ref branch index remote queue rebase header header branch  # 48
    window tree cache handle socket handle cache tree  # 49
    header cache handle worker ref  # 50
    blob queue blob window stream buffer  # 51
    worker header blob handle  # 52
    socket tree pack merge socket  # 53
    rebase stream ref socket index window blob buffer  # 54
    blob merge worker remote handle commit window  # 55
    object remote tree ref tree tree pack worker blob socket  # 56
    worker branch branch branch ref handle branch worker object buffer  # 57
    pack pack parser branch commit  # 58
    merge parser pack commit remote token  # 59
    rebase cache object tree  # 60
    stream cache cache parser index cache ref  # 61
    worker queue branch parser window pack  # 62
    tree buffer index buffer index parser tree rebase  # 63
    branch object pack parser ref blob branch stream branch merge  # 64
    remote merge merge blob  # 65
    blob remote branch buffer remote index  # 66
    rebase handle blob buffer commit queue index delta  # 67
    socket queue ref merge remote token  # 68
    stream cache parser blob branch tree token tree merge  # 69
    parser cache window ref queue stream rebase tree worker buffer index  # 70
    ref handle stream delta blob delta buffer  # 71
    delta remote parser object cache socket cache socket window rebase  # 72
    pack object blob buffer ref  # 73
    rebase delta delta object buffer index commit cache  # 74
    cache branch stream stream handle branch index merge index tree  # 75
    window index cache cache remote stream rebase stream queue  # 76
    cache token index remote socket ref object branch commit  # 77
