    window parser commit buffer tree parser pack object socket  # 0
    socket socket header socket object socket handle parser rebase handle queue  # 1
    ref buffer ref remote parser remote header pack  # 2
    branch handle handle merge tree remote index socket token queue  # 3
    stream pack header socket  # 4
    remote remote rebase socket buffer header token rebase ref remote ref  # 5
    cache remote branch branch branch rebase queue remote handle buffer  # 6
    ref remote buffer commit delta tree branch object index handle header  # 7
    window tree worker worker commit  # 8
    ref parser remote stream branch header queue index token branch delta object  # 9
    merge remote handle cache tree commit rebase remote handle header  # 10
    header socket index token window token token object handle  # 11
    rebase branch queue branch window buffer queue header handle ref worker tree  # 12
    index socket cache remote  # 13
    delta delta socket window merge branch header socket delta remote commit handle  # 14
    blob header parser stream buffer ref queue commit socket token  # 15
    ref pack queue queue worker object cache branch  # 16
    socket merge cache buffer commit cache  # 17
    window merge ref window handle pack cache blob object queue  # 18
    cache window blob index merge delta buffer ref header  # 19
    header rebase ref buffer merge  # 20
    window remote delta token index  # 21
    token parser merge merge parser remote worker socket index parser object  # 22
    buffer worker tree buffer blob  # 23
    blob delta rebase socket ref token stream  # 24
    delta remote remote rebase  # 25
    header object handle branch remote  # 26
    cache socket header blob ref pack  # 27
    parser tree tree ref index merge commit index branch remote pack ref  # 28
    header merge merge token  # 29
    handle window delta worker  # 30
    tree queue tree merge remote stream window blob commit index socket stream  # 31
    cache stream blob pack pack  # 32
    blob header token branch parser tree buffer header header window stream stream  # 33
    header remote delta socket queue socket remote object header  # 34
