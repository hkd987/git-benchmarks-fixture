    socket window index commit  # 0
    tree rebase rebase merge  # 1
    handle stream queue tree parser  # 2
    branch window index blob header ref pack remote  # 3
    cache branch branch blob tree cache object index merge commit header token  # 4
    index worker handle cache token socket socket buffer  # 5
    buffer tree socket merge ref buffer  # 6
    header handle worker worker delta stream remote ref  # 7
    parser blob queue handle parser parser object merge handle delta  # 8
    buffer handle pack window queue buffer  # 9
    stream delta object header handle worker cache worker queue  # 10
    tree index queue blob socket pack cache parser header  # 11
    tree token token buffer stream merge token  # 12
    index commit cache tree  # 13
    delta object rebase blob merge merge remote rebase  # 14
    commit pack socket pack delta stream branch tree tree socket rebase  # 15
    header queue merge queue parser merge tree window header tree remote pack  # 16
    handle cache worker rebase worker ref merge  # 17
    pack stream rebase worker index  # 18
    buffer tree commit blob buffer object ref queue  # 19
    token tree worker buffer cache  # 20
    buffer worker delta buffer stream window token merge stream token tree  # 21
    worker window parser cache queue worker socket parser stream  # 22
    tree token worker queue tree window stream merge  # 23
    header blob window index parser blob buffer ref token  # 24
    object index rebase index blob header stream commit  # 25
    worker window queue delta handle buffer stream branch tree handle tree  # 26
    delta window remote blob  # 27
    merge token branch index header header buffer queue object blob merge  # 28
    cache window handle window token  # 29
    queue delta merge window  # 30
    rebase commit parser parser stream blob buffer index  # 31
    window handle commit commit header delta commit blob handle buffer  # 32
    buffer remote branch object merge stream queue socket  # 33
    branch cache object token  # 34
    rebase tree index ref remote ref tree  # 35
    pack object rebase branch pack token commit ref worker commit parser ref  # 36
    queue window handle worker object stream  # 37
    index rebase merge queue socket socket object pack  # 38
    ref socket buffer header parser commit remote header token  # 39
    merge index token blob pack object worker  # 40
    ref tree delta merge remote object branch cache delta commit  # 41
    blob queue cache header rebase delta index token commit ref handle  # 42
    socket stream header merge buffer worker window queue remote token header  # 43
    ref buffer cache blob socket  # 44
    token remote queue pack token blob ref index pack tree parser  # 45
    object pack branch stream delta  # 46
    delta remote cache stream  # 47
    window buffer delta tree window handle pack handle  # 48
    buffer token worker blob merge  # 49
    parser tree index pack worker token stream branch stream  # 50
    queue socket header pack stream remote parser tree handle parser  # 51
    stream handle stream window index rebase commit merge header queue merge stream  # 52
    worker ref buffer delta stream pack queue rebase socket socket  # 53
    window ref queue stream commit buffer stream branch rebase token branch delta  # 54
    token commit stream commit remote stream pack handle socket index  # 55
    delta token window window  # 56
    parser handle rebase header token worker pack branch stream  # 57
    queue merge branch socket buffer cache commit  # 58
    commit parser remote ref commit parser merge worker worker stream token  # 59
    worker commit branch handle parser pack header cache  # 0
    buffer queue branch tree ref branch pack queue merge buffer  # 1
    index object branch header pack  # 2
    rebase blob merge token  # 3
    worker blob pack stream  # 4
    queue tree parser commit blob branch blob delta remote  # 0
    worker header object socket delta tree ref blob  # 1
    header worker delta index blob merge object  # 2
    merge tree blob ref blob  # 3
    queue branch merge token handle window commit index stream  # 4
    cache window socket queue pack handle stream delta blob merge  # 5
    rebase parser remote worker commit window token token cache pack  # 6
    queue delta worker handle delta token index  # 7
    merge parser pack index stream  # 8
    object index commit rebase merge handle cache branch  # 9
