    remote handle buffer remote merge delta handle  # 0
    socket buffer branch worker rebase  # 1
    blob ref blob cache  # 2
    header object header object  # 3
    socket window branch worker  # 4
    window token token header commit  # 5
    pack cache merge remote merge ref object object parser cache header  # 6
    ref object parser commit  # 7
    window remote rebase rebase ref ref merge rebase  # 8
    queue blob token object header delta  # 9
    cache ref pack delta branch cache  # 10
    header tree remote blob index delta blob ref cache token  # 11
    remote window rebase tree  # 12
    branch commit buffer worker blob delta handle remote worker  # 13
    cache cache queue buffer  # 14
    window handle delta window window window parser  # 15
    rebase delta index window tree token window  # 16
    window index header worker blob  # 17
    cache stream merge socket rebase window header worker  # 18
    rebase remote handle buffer branch cache stream worker  # 19
    worker buffer stream object token  # 20
    handle delta stream remote cache object rebase buffer token window cache header  # 21
    queue index cache parser rebase ref commit worker rebase header  # 22
    blob cache index blob branch delta  # 23
    delta delta merge window delta commit socket index commit  # 24
    header socket tree cache header merge header index queue rebase  # 25
    blob buffer cache index index  # 26
    worker handle buffer branch blob merge queue worker branch window buffer parser  # 27
    remote blob handle window merge cache stream parser tree  # 28
    merge ref tree commit header blob parser  # 29
    buffer remote header commit  # 30
    handle window header rebase remote rebase cache handle queue  # 31
    queue queue socket cache commit queue token merge delta object delta parser  # 32
    header commit index buffer worker delta merge  # 33
    branch merge ref window cache handle  # 34
    socket parser rebase remote index commit  # 35
    index ref stream delta object token ref delta  # 36
    ref tree queue commit stream queue  # 37
    queue queue queue merge header buffer parser merge  # 38
    handle parser merge cache socket worker rebase parser  # 39
    blob merge rebase delta branch delta tree window  # 40
    ref worker worker merge  # 41
    merge window tree header  # 42
    parser rebase worker blob pack tree handle remote rebase  # 43
    cache stream socket index tree socket object pack branch tree ref  # 44
