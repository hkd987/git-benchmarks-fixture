    ref index socket pack pack  # 0
    merge parser stream handle blob tree worker object object  # 1
    object header object queue window socket branch rebase merge queue  # 2
    buffer merge stream rebase branch delta  # 3
    branch branch token rebase  # 4
    branch worker handle tree  # 5
    delta parser stream tree commit delta blob branch socket  # 6
    blob token blob stream buffer index branch index queue index parser pack  # 7
    stream merge cache queue rebase blob window merge commit remote ref  # 8
    remote stream header commit blob cache header tree  # 9
    stream remote pack parser handle header handle remote index index  # 10
    index buffer cache cache stream handle blob index worker  # 11
    ref delta parser socket  # 12
    queue window object queue stream window header cache branch branch cache pack  # 13
    commit branch pack token commit  # 14
    worker index token index worker window blob object buffer header  # 15
    queue token header socket delta merge token parser stream window  # 16
    branch token commit parser ref object tree pack  # 17
    buffer merge remote stream rebase parser  # 18
    remote remote socket cache cache  # 19
    queue remote rebase blob  # 20
    parser commit token rebase tree branch merge header index merge tree  # 21
    blob object parser header tree token cache remote header  # 22
    parser header window index socket tree pack  # 23
    branch object parser ref rebase merge commit tree branch  # 24
    handle merge handle blob header merge pack  # 25
    pack ref ref worker handle blob index handle branch  # 26
    tree stream worker merge tree pack tree commit delta branch pack branch  # 27
    branch queue parser commit  # 28
    token ref delta parser ref delta blob  # 29
    buffer buffer window rebase  # 30
    cache pack pack socket socket header cache  # 31
    tree stream buffer parser parser index socket cache blob commit remote  # 32
    stream handle rebase branch  # 33
    stream index tree worker buffer buffer parser index header queue header  # 34
    header merge cache object token rebase branch index remote commit header cache  # 35
    stream merge worker blob cache remote index parser window stream delta token  # 36
    queue token worker index  # 37
    worker cache queue header commit pack queue ref cache socket delta  # 38
    socket pack merge ref  # 39
    queue tree worker handle stream branch  # 40
    parser worker tree cache  # 41
    cache blob header buffer handle blob merge object handle stream  # 42
    cache blob cache blob object  # 43
    worker delta window queue blob  # 44
    commit commit parser ref socket commit queue parser ref cache delta  # 45
    delta window delta delta token pack remote token blob buffer object  # 46
    pack pack handle header socket rebase pack rebase  # 47
    branch window branch token index branch buffer queue handle index stream remote  # 48
    index pack worker rebase worker token commit index parser stream window  # 49
    parser token tree object object tree header cache  # 50
    ref merge token object header blob parser stream delta cache stream parser  # 51
    header window remote commit handle window merge worker buffer header worker pack  # 52
    blob delta token buffer index index branch buffer blob merge merge buffer  # 53
    socket commit merge stream  # 54
    delta ref ref object tree parser token handle cache  # 55
    tree window ref index worker handle  # 56
    delta index delta header  # 57
    pack blob branch stream queue window ref  # 58
    commit delta stream index object handle buffer token delta pack remote stream  # 59
    index buffer queue merge blob stream pack cache tree delta commit stream  # 60
    buffer index commit parser  # 61
    branch rebase buffer delta  # 62
    header token branch buffer  # 63
    handle window handle header rebase  # 64
    branch tree socket object header token branch buffer worker object  # 65
    token socket stream queue merge commit branch  # 66
    index cache worker rebase rebase handle index commit rebase cache  # 67
    buffer queue delta parser parser pack handle merge worker branch buffer  # 68
    handle ref object socket queue handle cache socket merge blob worker buffer  # 69
    blob token ref rebase  # 70
    object buffer parser merge  # 71
    remote blob socket blob  # 72
    delta branch worker buffer ref  # 73
    merge merge object remote queue delta  # 74
    tree pack worker queue header tree  # 75
    socket merge window tree  # 76
    ref merge worker rebase branch commit remote parser handle  # 77
    queue index window tree  # 0
    branch ref stream blob cache socket socket rebase parser index socket  # 1
    delta remote delta delta parser queue header pack window commit branch  # 2
    ref token merge handle tree worker  # 3
    header branch window buffer socket window blob header blob  # 4
    merge rebase tree queue  # 5
    ref window socket branch blob blob branch window tree buffer token delta  # 6
    merge object parser tree parser cache commit handle window socket  # 7
    commit worker queue remote blob  # 8
    buffer socket buffer queue queue window  # 9
    tree queue buffer buffer ref window header tree object  # 10
    tree rebase worker worker cache window  # 11
    token header commit ref pack remote  # 12
