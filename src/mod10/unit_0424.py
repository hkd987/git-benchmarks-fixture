    cache tree index tree header socket pack delta header pack rebase  # 0
    ref socket worker stream blob worker window rebase socket cache  # 1
    token remote merge tree blob ref index  # 2
    window worker queue remote parser delta token branch buffer object  # 3
    queue commit buffer delta queue handle  # 4
    socket pack worker queue queue  # 5
    token token branch header  # 6
    index handle socket token window  # 7
    rebase ref worker queue index blob token branch  # 8
    ref object socket remote remote buffer buffer window remote socket parser ref  # 9
    blob socket commit handle queue tree token merge ref blob worker  # 10
    blob branch tree branch header commit branch merge buffer  # 11
    handle worker merge parser index token queue  # 12
    object commit commit ref window buffer worker branch  # 13
    parser window socket header delta token buffer merge stream  # 14
    index object branch token object ref  # 15
    queue token queue cache merge branch  # 16
    worker blob window worker socket commit index cache merge branch  # 17
    commit worker parser remote  # 18
    remote handle token delta index object token  # 19
    blob rebase rebase stream tree tree object ref queue handle  # 20
    token remote header merge window worker branch merge rebase remote header  # 21
    handle stream cache merge stream header parser handle socket rebase queue index  # 22
    rebase worker index header header  # 23
    commit ref merge blob handle  # 24
    pack parser commit cache window socket remote branch worker stream commit ref  # 25
    index queue cache commit rebase buffer rebase remote  # 26
    handle header stream stream blob handle cache  # 27
    remote token remote merge rebase token  # 28
    remote remote parser cache rebase token  # 29
    handle tree token parser commit worker index handle  # 30
    delta queue ref token tree handle rebase cache header ref  # 31
    delta delta worker branch ref parser queue commit rebase object remote merge  # 32
    pack commit remote object parser blob header queue index pack window handle  # 33
    remote merge window merge parser delta index socket  # 34
    header remote blob commit socket commit  # 35
    worker queue queue ref delta  # 36
    delta tree remote merge buffer pack header parser buffer queue  # 37
    stream socket tree socket  # 38
    worker ref window parser buffer buffer  # 39
    index merge ref tree  # 40
    remote parser merge buffer window  # 41
    cache delta parser merge header token token stream pack remote worker handle  # 42
    ref blob pack socket window object window  # 43
    blob blob blob worker tree token cache  # 44
    commit delta tree parser queue remote token remote token cache commit parser  # 45
    ref cache cache buffer branch remote stream buffer handle buffer queue parser  # 46
    pack merge window handle merge buffer stream object branch  # 47
    pack stream pack rebase remote cache queue  # 48
