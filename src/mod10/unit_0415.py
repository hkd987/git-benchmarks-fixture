    object cache merge branch handle blob cache merge commit stream socket  # 0
    pack blob tree merge cache remote object pack tree worker stream  # 1
    token cache header remote stream  # 2
    tree ref worker window index object branch  # 3
    cache ref remote socket queue tree ref handle index  # 4
    index merge commit merge rebase header cache  # 5
    remote stream commit blob pack handle socket buffer tree rebase socket  # 6
    window parser parser index token  # 7
    handle branch cache token tree  # 8
    window tree pack buffer  # 9
    socket parser cache pack cache token queue window  # 10
    remote index rebase tree worker index worker index tree rebase  # 11
    buffer socket worker tree token object buffer commit  # 12
    worker parser merge window worker object branch  # 13
    buffer pack index index commit  # 14
    rebase cache tree rebase pack queue index  # 15
    merge buffer handle delta merge blob parser window rebase index pack buffer  # 16
    delta delta buffer buffer queue stream parser index branch queue rebase  # 17
    rebase tree blob blob tree branch header header index queue  # 18
    token commit buffer parser branch  # 19
    delta header delta window header delta pack stream  # 20
    ref window branch blob object parser remote cache branch  # 21
    branch ref remote blob handle parser socket pack socket blob remote  # 22
    handle parser token header worker queue parser parser header  # 23
    ref object merge blob object worker  # 24
    handle pack token window merge tree parser  # 25
    commit merge handle object window remote index rebase handle merge  # 26
    blob commit blob header pack token window  # 27
    pack blob merge header header index cache stream window pack  # 28
    remote commit queue ref token token delta pack merge ref  # 29
    index rebase object token socket cache  # 30
    pack cache remote merge parser window  # 31
    object blob index cache parser stream object object branch  # 32
