    handle rebase branch branch remote index handle parser handle commit delta pack  # 0
    buffer token token object cache handle  # 1
    window queue worker commit queue handle rebase stream index queue window  # 2
    buffer window object queue socket  # 3
    commit header pack buffer  # 4
    window socket commit socket rebase socket  # 5
    worker cache header object  # 6
    stream parser remote socket ref merge parser buffer ref stream buffer cache  # 7
    stream worker ref cache socket tree index remote object merge  # 8
    index header commit blob  # 9
    window pack header stream cache parser window index remote pack  # 10
    pack tree blob queue rebase pack blob delta commit  # 11
    stream rebase socket ref parser tree header buffer index stream remote buffer  # 12
    token worker cache socket  # 13
    tree socket buffer index worker queue delta ref object  # 14
    object index merge worker header delta worker queue header buffer  # 15
    stream socket remote cache window header commit worker  # 16
    buffer parser cache blob merge parser handle queue parser remote  # 17
    rebase header commit index parser window  # 18
    object buffer commit tree remote queue  # 19
    window token header stream queue worker delta remote  # 20
    token tree handle blob  # 21
    pack ref buffer worker token commit parser blob worker  # 22
    rebase parser tree window buffer branch commit tree commit blob buffer  # 23
    index handle tree token ref  # 24
    branch queue handle blob header commit worker socket stream ref rebase  # 25
    branch object cache index window tree handle commit remote parser  # 26
    buffer tree pack socket blob ref pack header rebase parser index header  # 27
    socket commit socket remote cache  # 28
    buffer worker commit token header delta index delta parser  # 29
    delta delta cache cache queue delta ref commit header worker ref  # 30
    buffer tree delta branch commit  # 31
    rebase delta delta index delta socket index merge  # 32
    stream socket buffer merge stream remote  # 33
    delta cache index object object  # 34
    ref tree blob index delta rebase object cache  # 35
    index blob handle header buffer tree cache merge worker token token  # 36
    ref tree cache delta branch object commit ref cache merge  # 37
    tree ref commit window object ref parser delta object commit socket  # 38
    worker rebase ref branch pack  # 39
    ref delta worker window worker  # 40
    pack cache tree commit parser  # 41
    commit pack pack handle object  # 42
    tree object delta window branch delta cache branch window blob  # 43
    queue index parser cache  # 44
    queue merge socket index delta branch parser parser pack ref remote stream  # 45
    commit token tree buffer  # 46
    rebase stream queue ref rebase handle worker parser parser handle socket worker  # 47
    stream parser parser rebase rebase branch socket rebase pack parser remote handle  # 48
    cache parser socket stream  # 49
    buffer ref tree window pack  # 50
    token cache cache parser object commit pack blob blob delta  # 51
    remote rebase pack tree parser worker worker socket rebase merge pack  # 52
    socket stream blob pack object merge rebase blob cache merge buffer commit  # 53
    header socket header pack commit parser queue rebase rebase commit  # 54
    tree header queue delta pack  # 55
    object buffer queue remote  # 56
    tree window parser remote ref token stream  # 57
    remote remote tree window remote socket delta worker blob  # 58
    branch parser pack handle handle parser worker  # 59
    object remote window delta commit parser stream worker cache stream queue  # 60
    worker commit remote remote pack queue pack tree delta remote window queue  # 61
    header rebase queue delta tree branch  # 62
    handle delta object pack  # 63
    delta handle commit tree rebase parser header stream parser stream  # 64
    queue commit delta token remote object remote token header pack parser object  # 65
    branch pack commit socket token buffer branch  # 66
    worker socket blob window blob token merge socket worker parser merge queue  # 67
    stream remote index handle header window  # 0
    tree index stream handle commit cache queue  # 1
    stream tree worker blob blob merge  # 2
    remote queue socket window ref socket  # 3
    blob stream cache merge pack branch index queue window header tree  # 4
    window ref queue remote rebase rebase tree cache queue  # 5
    parser queue blob ref merge  # 6
    token window delta merge queue header stream commit commit remote  # 7
    pack tree blob delta cache  # 8
