    socket queue pack delta buffer pack remote  # 0
    rebase worker cache commit stream cache delta parser blob queue  # 1
    handle worker parser index merge tree socket object  # 2
    parser branch merge ref header  # 3
    token merge object queue parser commit branch delta blob  # 4
    commit commit pack worker worker rebase pack  # 5
    header ref blob branch object pack branch blob header parser handle  # 6
    index stream remote handle buffer socket delta index socket cache stream parser  # 7
    token rebase socket commit parser buffer  # 8
    handle ref header buffer  # 9
    window token remote window cache index cache queue handle  # 10
    merge blob delta window handle rebase delta rebase token  # 11
    worker tree socket ref  # 12
    queue remote branch tree buffer branch queue header blob blob  # 13
    delta parser index socket delta branch merge blob buffer  # 14
    window ref remote pack  # 15
    tree remote pack handle queue ref remote token window pack  # 16
    merge rebase merge queue parser socket stream  # 17
    pack window parser blob socket parser buffer  # 18
    token pack index tree  # 19
    branch window rebase tree window ref buffer socket object  # 20
    worker buffer parser tree branch pack object parser header worker  # 21
    object tree commit object buffer commit tree cache  # 22
    blob buffer window socket  # 23
    commit token merge header  # 24
    branch index remote tree worker rebase queue remote buffer blob window token  # 25
    worker token cache handle header ref parser handle stream buffer worker ref  # 26
    window blob merge cache window blob buffer tree merge object header branch  # 27
    handle buffer cache object tree  # 28
    window token handle blob rebase delta handle branch blob handle  # 29
    blob header buffer buffer parser queue object merge tree socket  # 30
    header token header buffer merge parser cache tree tree branch cache stream  # 31
