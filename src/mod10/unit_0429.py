    remote cache commit object commit buffer handle commit blob branch delta branch  # 0
    blob window buffer delta parser parser parser  # 1
    delta branch parser socket branch index commit cache socket  # 2
    tree queue branch socket window queue  # 3
    blob delta stream token buffer pack  # 4
    window worker ref header queue queue merge token ref parser stream  # 5
    rebase object buffer handle  # 6
    pack socket buffer branch branch cache handle index commit buffer  # 7
    index handle token pack rebase handle delta branch window index socket pack  # 8
    buffer buffer pack tree parser blob index remote tree queue buffer index  # 9
    rebase delta window object  # 10
    index merge commit header remote  # 11
    cache commit buffer queue pack token queue branch ref header merge  # 12
    parser pack parser cache  # 13
    branch ref remote object index buffer remote  # 14
    buffer window buffer object merge remote remote tree  # 15
    index merge commit token rebase header ref token handle buffer window parser  # 16
    ref buffer commit socket stream socket pack token branch merge parser tree  # 17
    cache buffer header parser handle blob queue index  # 18
    index delta token remote commit stream object worker worker  # 19
    cache window stream commit  # 20
    buffer index object index branch merge stream  # 21
    parser parser blob index socket ref ref queue window cache  # 22
    parser socket header merge buffer delta branch header index  # 23
    remote rebase buffer socket queue ref merge header queue window  # 24
    delta token remote rebase rebase token window index cache index  # 25
    stream queue buffer ref handle remote blob delta  # 26
    ref delta delta branch window  # 27
    socket buffer ref tree remote merge commit token object cache  # 28
    index commit delta window token ref index commit token queue  # 29
    delta socket commit worker cache commit blob object  # 30
    socket branch blob socket index window queue merge commit  # 31
    commit remote cache socket stream ref index object blob cache tree  # 32
    parser delta socket handle handle header token socket index buffer  # 33
    worker tree remote buffer ref  # 34
    socket index index blob object merge delta header merge commit  # 35
    worker object buffer blob header parser rebase queue tree index  # 36
    parser worker worker rebase window rebase rebase cache ref  # 37
    worker cache stream blob handle header remote branch  # 38
    tree commit remote header blob delta window tree handle commit  # 39
    header socket handle handle branch parser branch pack header index worker  # 40
    cache socket index delta window  # 41
    merge rebase parser window window commit window merge ref rebase token  # 42
    stream branch index blob header remote  # 43
    socket token token stream branch pack queue rebase  # 44
    header parser merge blob  # 45
    buffer remote merge parser blob handle cache blob rebase remote  # 46
    delta socket window header object object tree remote commit  # 47
    worker token header cache remote queue commit token header  # 48
    branch rebase buffer header pack commit remote branch  # 49
    parser index remote tree  # 50
    queue stream index tree parser branch socket branch  # 51
    object branch window cache ref queue delta index  # 52
    pack worker ref token parser branch  # 53
    header index socket ref tree object buffer delta parser handle  # 54
    branch window stream window queue pack pack merge branch  # 0
    token queue remote ref  # 1
    queue parser branch delta branch merge  # 2
    blob tree branch index buffer handle worker  # 3
    object header object commit rebase  # 4
    pack merge handle blob token ref blob tree header  # 5
