    blob delta rebase delta commit socket tree buffer delta handle  # 0
    commit header buffer rebase worker header queue worker stream queue token  # 1
    merge commit object stream delta  # 2
    object commit ref socket ref branch parser rebase  # 3
    merge blob worker socket  # 4
    ref window token branch delta blob cache stream object stream remote merge  # 5
    branch cache cache socket parser token  # 6
    cache remote socket cache  # 7
    handle socket worker stream window object cache  # 8
    parser parser rebase stream branch  # 9
    cache blob stream rebase worker merge  # 10
    socket window blob index merge token cache buffer merge parser buffer  # 11
    index commit window worker header parser header parser socket merge delta ref  # 12
    rebase stream header queue  # 13
    index remote branch ref object window handle handle branch queue index  # 14
    rebase socket token delta socket header handle buffer remote  # 15
    rebase token object commit ref branch index  # 16
    ref stream delta branch blob ref  # 17
    socket index branch cache window socket branch queue object window header  # 18
    merge window handle cache  # 19
    ref ref ref buffer rebase branch tree delta queue commit delta  # 20
    tree delta queue buffer handle queue stream socket merge branch window  # 21
    queue index token delta header  # 22
    token object ref cache merge token branch cache ref cache  # 23
    branch window parser stream rebase cache merge branch worker branch  # 24
    window remote pack handle token merge buffer cache  # 25
    object handle rebase object header index merge handle pack buffer delta  # 26
    object cache rebase buffer blob tree branch stream rebase  # 27
    merge rebase commit token rebase buffer handle cache queue parser  # 28
    remote buffer queue branch pack queue stream rebase stream commit  # 29
    merge parser branch tree merge queue  # 30
    header index object remote tree stream  # 31
    object object cache merge socket buffer  # 32
    socket header commit ref header token window commit  # 33
    buffer index branch merge socket merge buffer merge  # 34
    remote blob parser remote rebase ref rebase  # 35
    blob ref stream object handle queue worker stream  # 36
    tree queue handle queue rebase blob commit branch pack object header buffer  # 37
    pack cache queue queue  # 38
    tree stream commit queue commit delta stream handle  # 39
    merge delta cache tree cache commit cache blob rebase  # 40
    buffer branch header queue header index token ref handle remote  # 41
    token ref remote tree cache socket blob ref ref  # 42
    worker buffer parser rebase object pack window queue queue worker parser remote  # 43
    branch remote commit delta remote remote ref commit window  # 44
    queue branch worker object stream stream merge window blob token handle  # 45
    ref parser cache pack pack branch stream  # 46
    handle queue commit handle token buffer socket rebase merge ref header buffer  # 47
    queue ref tree rebase ref remote remote worker socket pack  # 48
    blob header parser header remote socket merge index tree  # 49
    object stream blob remote branch ref token index remote blob token index  # 50
    ref ref rebase object header  # 51
    header token parser object ref buffer merge handle handle socket handle index  # 52
    delta worker remote worker buffer  # 53
    buffer branch branch index tree commit handle buffer index delta  # 54
    buffer socket remote commit parser remote buffer window  # 55
    index rebase commit token tree handle delta delta  # 56
    token handle worker merge token merge branch object  # 57
    parser queue ref rebase tree object pack handle  # 58
    cache token tree token delta stream parser merge remote ref index stream  # 59
    commit buffer blob socket  # 60
    handle token queue delta handle remote window  # 61
    rebase handle delta blob cache tree window  # 62
    token index blob merge merge socket  # 63
    stream remote cache socket index branch ref remote  # 64
    merge merge blob commit  # 65
