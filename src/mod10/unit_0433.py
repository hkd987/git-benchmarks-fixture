    index socket delta window worker  # 0
    cache index commit object handle object object window branch queue  # 1
    rebase queue parser token tree handle queue ref  # 2
    token pack window object blob header worker object socket  # 3
    parser token token socket  # 4
    window merge tree ref queue pack  # 5
    parser ref ref token pack handle  # 6
    queue token ref stream branch queue blob  # 7
    rebase ref ref index commit rebase cache ref rebase handle  # 8
    commit rebase queue merge window  # 9
    handle handle parser token object pack cache worker window handle index window  # 10
    window cache token cache header delta merge merge socket  # 11
    commit token merge index commit  # 12
    merge pack token stream buffer  # 13
    header pack branch socket ref parser remote token  # 14
    socket branch pack window rebase  # 15
    object cache tree parser index  # 16
    worker tree handle window parser object branch merge blob object rebase  # 17
    delta tree remote stream header remote  # 18
    window pack handle branch  # 19
    cache parser queue buffer handle tree remote token worker rebase token  # 20
    pack parser tree stream header commit  # 21
    index buffer delta cache ref remote index commit  # 22
    worker branch socket window window  # 23
    ref object socket worker token cache  # 24
    window cache pack blob  # 25
    delta blob remote remote rebase delta stream rebase queue socket parser  # 26
    queue rebase tree remote  # 27
    cache delta blob worker remote  # 28
    blob socket socket buffer object worker stream header queue  # 29
    socket cache window buffer blob buffer socket socket worker  # 0
    parser rebase token queue stream object  # 1
    blob branch object branch index parser index cache branch  # 2
    window stream cache blob parser token worker commit remote buffer stream  # 3
    stream remote queue handle  # 4
    buffer buffer parser socket socket object handle rebase  # 5
    worker branch pack socket header delta pack  # 6
