    cache worker commit cache header pack buffer merge blob worker window  # 0
    cache tree queue index buffer stream buffer cache buffer buffer queue  # 1
    header index delta ref merge window pack buffer cache queue  # 2
    ref pack merge header ref  # 3
    header blob parser token  # 4
    branch index branch window pack branch socket  # 5
    buffer branch token token index merge window blob index  # 6
    handle ref object parser window object parser rebase blob buffer  # 7
    ref branch window commit index token ref branch remote merge token parser  # 8
    queue commit worker worker header stream handle branch header parser cache buffer  # 9
    buffer header stream tree blob  # 10
    pack merge merge stream rebase remote index token rebase stream parser  # 11
    pack object remote remote blob  # 12
    pack queue pack cache stream socket  # 13
    delta stream window buffer header delta cache buffer  # 14
    socket index object socket index  # 15
    cache stream object branch commit branch handle branch pack  # 16
    token window merge branch window ref rebase queue remote cache  # 17
    commit token ref pack window token stream header ref blob stream handle  # 18
    pack token rebase ref cache buffer window commit buffer  # 19
    commit cache token token parser branch header header object stream delta object  # 20
    index buffer branch commit token window object branch token index delta merge  # 21
    window rebase remote ref ref socket index  # 22
    branch ref token tree handle tree window commit index queue blob  # 23
    header token remote buffer  # 24
    buffer header index commit delta token buffer cache delta window  # 25
    token window delta rebase socket  # 26
    queue object object merge ref merge object  # 27
    delta worker token rebase window ref socket branch merge pack  # 28
    window remote object branch worker worker window parser token cache  # 29
    stream parser queue ref queue commit header token commit  # 30
    remote queue ref ref worker pack  # 31
    queue index merge branch branch tree remote remote ref cache rebase stream  # 32
    token handle object queue object handle commit queue merge handle index  # 33
    handle buffer handle socket ref worker socket  # 34
    token remote pack rebase token merge cache parser parser  # 35
    cache stream commit socket tree  # 36
    ref commit object commit remote blob commit header parser buffer parser  # 37
    object object queue commit worker  # 38
    cache blob rebase remote window remote object worker rebase cache branch ref  # 39
    token commit pack delta header worker  # 40
    queue window token window buffer socket handle parser buffer tree object  # 41
    pack index ref remote commit  # 42
    branch index cache pack blob pack branch tree tree stream buffer  # 43
    rebase object handle object tree queue handle token blob  # 44
    cache window cache merge branch  # 45
    cache queue remote index merge parser pack handle delta worker window  # 46
    delta parser remote tree object  # 47
    cache rebase buffer cache remote token blob socket ref  # 48
    rebase cache queue parser object branch commit header buffer remote rebase parser  # 49
    parser pack token rebase object ref socket stream merge  # 50
    handle rebase ref ref merge token merge branch object ref  # 51
    object worker pack pack rebase branch queue cache socket cache merge tree  # 52
    token stream remote tree token tree ref  # 53
    worker window merge object header branch delta window rebase commit parser  # 54
    parser queue socket socket  # 55
    rebase window index buffer index token pack blob commit delta blob delta  # 56
    stream rebase token pack header socket buffer parser  # 57
    buffer commit delta queue  # 58
    parser handle window remote remote merge queue parser worker remote merge  # 59
    parser worker header blob tree merge token header buffer ref  # 60
    remote socket index buffer  # 61
    worker parser merge merge blob remote stream remote remote pack  # 62
    blob cache buffer queue commit index ref parser socket parser pack  # 63
    tree index index window  # 64
    worker handle worker handle rebase merge commit window worker  # 65
    object delta commit token merge stream handle worker socket  # 66
    buffer remote handle ref  # 67
    handle branch ref pack parser header stream rebase token queue buffer  # 68
    rebase commit delta object pack rebase commit ref header  # 69
    tree buffer merge commit merge  # 70
    commit branch buffer buffer ref rebase cache queue index pack socket blob  # 71
    worker pack header handle queue queue rebase remote cache socket  # 72
    remote branch blob ref branch merge remote handle queue window  # 73
    ref ref blob merge object stream pack  # 74
    header handle header branch branch remote handle commit delta pack  # 75
    socket commit ref blob merge  # 76
    window branch delta rebase queue worker  # 77
    blob socket commit queue remote window token index  # 78
    pack handle stream window handle worker blob delta object ref object  # 79
    socket ref cache merge blob  # 80
    stream ref branch ref object token buffer socket  # 81
    buffer object remote buffer cache stream socket queue branch delta tree tree  # 82
    parser buffer header merge index merge blob remote queue rebase branch commit  # 83
    blob window window object remote branch remote  # 84
    header header blob socket index branch parser worker object  # 85
    ref header object merge merge remote  # 86
    header buffer delta cache merge  # 87
    commit branch delta object rebase queue queue rebase delta object  # 88
