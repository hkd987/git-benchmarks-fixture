    queue handle worker window blob  # 0
    handle rebase ref socket  # 1
    object blob index socket  # 2
    queue pack socket blob index queue tree  # 3
    branch pack commit commit object delta tree index token  # 4
    object ref token token tree socket object worker tree object index worker  # 5
    tree commit remote handle window parser worker header tree handle  # 6
    ref rebase queue ref worker blob worker object window  # 7
    index token remote window stream worker ref token object buffer rebase  # 8
    parser index parser delta window tree blob cache cache  # 9
    socket merge token handle object pack branch  # 10
    merge worker stream ref parser handle parser socket object queue cache  # 11
    parser index header blob index queue commit handle token  # 12
    rebase cache delta header window ref socket index stream object tree  # 13
    branch handle rebase object handle worker socket index handle ref tree header  # 14
    rebase socket blob merge  # 15
    rebase queue pack ref window blob  # 16
    remote remote rebase cache socket commit header tree ref  # 17
    ref object token ref stream merge worker handle queue token branch object  # 18
    token worker socket rebase queue parser index parser window queue  # 19
    stream parser blob stream token remote queue delta branch delta cache rebase  # 20
    cache object commit merge socket  # 21
    pack delta blob rebase ref buffer worker  # 22
    delta object blob buffer header worker  # 23
    parser stream rebase ref  # 24
    merge worker token branch merge buffer queue buffer  # 25
    merge buffer remote pack commit commit merge pack  # 26
    blob tree pack queue delta  # 27
    window token cache pack ref pack remote handle pack blob  # 28
    object blob parser stream pack parser  # 29
    delta ref object header socket socket token pack queue merge queue token  # 30
    parser queue object object buffer  # 31
    commit token token cache remote branch window socket handle  # 32
    queue ref ref index queue parser header delta window  # 33
    worker commit branch rebase worker  # 34
    tree object buffer ref parser token  # 35
    merge token stream cache socket delta token commit  # 36
    remote header pack ref delta socket handle pack pack queue tree socket  # 37
    parser socket delta queue  # 38
    branch worker commit merge queue object  # 39
    blob index object handle rebase commit delta branch branch delta delta cache  # 40
    rebase header cache ref parser merge queue cache  # 41
    index merge object worker  # 42
    socket stream window socket branch rebase stream parser index  # 43
    merge index worker commit blob  # 44
    index branch ref queue remote stream stream worker blob token branch stream  # 45
    merge handle socket delta token queue delta merge index delta parser  # 46
    token index worker handle  # 47
    socket worker token pack branch index handle window index handle  # 48
    window pack header socket queue  # 0
    merge ref pack merge worker parser commit  # 1
    handle socket index merge handle remote tree token  # 2
    queue socket index rebase parser  # 3
    handle token remote header window token branch cache worker header  # 4
    commit rebase token object worker window object queue window remote branch pack  # 5
    branch buffer delta commit remote merge rebase pack rebase remote worker object  # 6
