    cache stream window worker socket handle object delta  # 0
    pack commit commit index token queue blob  # 1
    object index parser header  # 2
    header pack queue blob cache remote ref  # 3
    window cache rebase buffer remote pack tree rebase cache  # 4
    commit blob pack cache branch pack  # 5
    window tree parser ref window object socket index blob cache blob window  # 6
    cache ref queue blob worker handle  # 7
    token socket commit token window stream rebase queue worker blob branch rebase  # 8
    commit socket parser branch ref blob delta  # 9
    rebase header cache index parser rebase remote object  # 10
    object rebase ref pack blob index  # 11
    branch index commit queue ref ref ref delta remote token merge  # 12
    ref window parser parser handle queue header object worker window pack  # 13
    worker buffer window object handle socket pack branch queue ref  # 14
    header commit socket ref rebase index stream worker  # 15
    stream branch token branch worker branch blob cache  # 16
    tree blob merge object branch socket  # 17
    index cache merge rebase delta delta worker tree ref object parser delta  # 18
    parser commit worker rebase parser  # 19
    blob header handle index worker blob socket handle header merge branch  # 20
    branch pack queue rebase  # 21
    remote window remote socket worker rebase index tree worker queue header  # 22
    pack stream blob rebase merge socket pack token blob  # 23
    header stream merge socket buffer commit token worker  # 24
    cache worker header stream blob header branch  # 25
    blob index window branch cache window rebase index header object  # 26
    delta token buffer index object socket remote commit window delta  # 27
    handle queue delta token socket rebase rebase rebase  # 28
    merge cache stream buffer branch rebase  # 29
    merge parser queue header queue rebase commit rebase worker  # 30
    socket window ref buffer delta blob  # 31
    rebase socket rebase header queue tree ref cache rebase rebase delta rebase  # 32
    pack merge handle stream remote handle worker rebase worker worker remote branch  # 33
    pack index cache handle worker commit pack object socket token socket handle  # 34
    token window commit stream parser ref stream commit object  # 35
    ref merge remote queue worker rebase  # 36
    parser rebase commit remote commit token  # 37
    header ref parser queue blob rebase header  # 38
    rebase cache branch worker header socket blob remote remote stream rebase  # 39
    cache index window ref token merge pack pack cache window tree  # 40
    token index socket commit queue rebase tree delta handle tree  # 41
