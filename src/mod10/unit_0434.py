    handle ref queue pack stream cache ref token window  # 0
    tree queue tree delta commit delta  # 1
    parser stream parser stream blob branch token window blob delta rebase  # 2
    merge window delta blob rebase header header index socket cache socket socket  # 3
    socket handle socket header  # 4
    branch token delta token index  # 5
    window parser rebase cache token  # 6
    delta token cache socket token branch  # 7
    merge pack worker tree rebase  # 8
    ref token index queue  # 9
    cache header tree commit handle queue socket  # 10
    handle stream token buffer token rebase object header remote rebase rebase parser  # 11
    cache remote stream parser object blob handle rebase remote socket  # 12
    token pack cache remote cache branch remote  # 13
    delta object remote parser window merge object delta header pack cache  # 14
    index object token pack object  # 15
    token window object buffer merge  # 16
    branch handle remote delta stream object commit ref socket  # 17
    branch window handle object blob  # 18
    tree cache worker merge tree buffer handle token pack queue window parser  # 19
    remote parser object pack cache window merge branch commit branch  # 20
    socket rebase branch delta  # 21
    header parser cache stream handle tree tree stream  # 22
    ref buffer commit tree rebase index  # 23
    parser cache window rebase token handle delta  # 24
    tree worker ref queue window handle ref remote remote rebase token stream  # 25
    window buffer rebase object rebase merge pack pack remote worker rebase ref  # 26
    stream branch pack merge socket parser parser delta commit worker  # 27
    socket remote token buffer object token tree blob parser commit handle parser  # 28
    index rebase branch commit header rebase index cache tree  # 29
    commit remote object parser object blob handle commit  # 30
    index object pack header parser index stream window pack window pack remote  # 31
    worker stream object tree token queue window commit buffer delta  # 32
    stream stream delta handle queue buffer remote handle  # 33
    handle merge tree delta parser handle tree index delta buffer  # 34
    cache merge branch blob buffer branch remote tree stream tree index  # 35
    remote buffer object object buffer index worker worker merge worker  # 36
    header commit handle remote stream header object  # 37
    object commit rebase rebase parser tree cache ref tree delta token object  # 38
    token commit buffer header buffer blob socket handle header merge handle socket  # 39
    delta commit parser delta ref parser  # 40
    queue index delta worker object header cache  # 41
    window socket stream token worker object index blob token parser stream ref  # 42
    worker queue stream blob tree remote delta cache index tree cache  # 43
    object object window object index header queue parser delta queue rebase  # 44
    rebase remote worker socket ref ref commit commit remote index index  # 45
    worker ref worker worker cache blob commit branch remote delta object commit  # 46
    commit delta branch socket  # 47
    stream cache object token rebase window ref  # 48
    pack cache index handle buffer  # 49
    parser handle object index object socket handle  # 50
    queue index handle commit commit cache  # 51
    commit branch stream parser pack stream remote remote commit queue  # 52
    socket delta tree commit tree pack buffer header commit  # 53
    delta commit parser remote ref object ref  # 54
    tree cache pack queue index buffer  # 55
