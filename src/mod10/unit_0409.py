    branch delta queue cache cache window stream merge branch handle remote  # 0
    window tree pack stream parser token ref branch  # 1
    window handle header tree branch merge  # 2
    commit delta remote blob queue cache pack window window delta pack branch  # 3
    buffer merge merge object queue token socket remote commit  # 4
    token blob rebase remote tree pack pack branch parser stream  # 5
    window merge handle handle header commit window stream stream commit rebase  # 6
    remote buffer ref delta merge delta worker pack token token remote  # 7
    cache queue token blob token worker  # 8
    object object rebase tree merge handle delta queue blob blob  # 9
    queue queue merge tree index tree socket worker merge buffer blob cache  # 10
    commit pack pack pack ref cache worker  # 11
    worker stream window worker window worker object queue  # 12
    pack header blob ref  # 13
    delta buffer blob blob window tree header commit index  # 14
    index commit handle pack remote  # 15
    stream cache ref commit rebase tree merge worker  # 16
    ref window commit object delta handle blob tree  # 17
    merge ref cache parser  # 18
    branch object branch worker rebase object token  # 19
    remote branch tree buffer worker rebase index delta cache cache blob  # 20
    ref branch header token socket commit socket tree  # 21
    object token handle blob handle stream header tree merge parser stream  # 22
    ref commit token cache rebase header blob commit delta  # 23
    buffer window queue merge commit delta tree  # 24
    remote cache merge remote parser socket queue header index  # 25
    merge queue rebase pack queue  # 26
    window delta ref branch  # 27
    tree socket token branch queue branch  # 28
    stream handle index object pack branch remote window stream buffer token header  # 29
    window tree object commit delta header pack token index  # 30
    blob merge socket delta  # 31
    index cache tree delta commit branch buffer ref merge index merge cache  # 32
    queue stream tree stream parser  # 33
    window stream tree cache header branch socket ref socket socket  # 34
    merge commit pack stream remote object pack handle merge tree  # 35
    commit delta blob parser  # 36
    handle rebase window worker window cache  # 37
    buffer index rebase handle index remote queue parser parser  # 38
    delta buffer handle buffer worker queue cache  # 39
    window object cache delta tree index delta header  # 40
    branch token handle rebase pack blob  # 41
    commit socket parser branch commit socket index  # 42
    queue commit stream object  # 43
    token merge socket rebase parser parser window worker parser parser  # 44
    stream tree queue pack tree branch token stream pack object queue  # 45
    window queue commit object rebase tree header branch  # 46
    rebase ref blob object worker branch cache handle  # 47
    merge delta pack merge pack stream commit  # 48
    stream object branch object stream parser worker  # 49
    delta ref worker token token commit tree index merge queue parser handle  # 50
    rebase index remote blob stream  # 51
    pack token socket socket merge  # 52
    delta window branch token  # 53
    tree remote cache handle remote queue tree parser  # 54
    remote remote pack stream token pack ref socket branch index queue tree  # 55
    index socket commit blob cache object pack rebase tree  # 56
    queue token remote socket ref window rebase commit  # 57
    buffer window remote handle branch  # 58
    buffer object window ref rebase tree blob token index  # 59
    token merge stream token ref rebase window parser blob object  # 60
    buffer index merge pack branch buffer queue socket object remote header  # 61
    branch merge header ref token branch  # 62
    branch socket handle handle cache queue queue worker header  # 63
    worker pack parser delta worker handle ref  # 64
    ref socket remote index queue handle index delta blob object tree header  # 65
    tree header rebase index index  # 66
    parser rebase rebase delta window queue parser stream cache  # 67
