    window tree tree object header buffer blob worker  # 0
    stream parser window remote cache token ref rebase queue worker blob  # 1
    buffer tree ref worker blob  # 2
    queue object branch worker  # 3
    window index remote window rebase index buffer header object  # 4
    branch merge header pack window worker token queue  # 5
    pack delta rebase socket queue handle window token  # 6
    index delta delta branch cache index object rebase remote  # 7
    blob buffer socket blob handle window index tree cache stream parser queue  # 8
    merge buffer index token queue queue cache delta pack  # 9
    remote delta window header window remote worker cache header  # 10
    branch token tree merge token blob  # 11
    object buffer worker stream branch window queue stream parser commit stream blob  # 12
    remote merge ref commit stream  # 13
    pack index pack tree object worker worker  # 14
    socket cache cache delta rebase handle stream  # 15
    cache queue buffer tree rebase cache branch rebase  # 16
    token buffer merge merge handle ref rebase header pack branch delta rebase  # 17
    tree socket commit index merge delta header  # 18
    header header buffer branch merge tree  # 19
    merge merge worker window cache socket commit delta rebase ref socket stream  # 20
    branch cache commit blob header  # 21
    delta pack tree remote index buffer rebase worker rebase window worker  # 22
    pack queue worker buffer token ref header rebase stream blob handle buffer  # 23
    handle token ref pack  # 24
    header worker token stream socket  # 25
    object ref object rebase object  # 26
    worker branch object cache token blob worker merge  # 27
    ref queue ref handle header remote ref cache handle ref branch delta  # 28
    remote remote ref rebase  # 29
    header token cache header pack ref commit parser cache tree object index  # 30
    handle commit object socket index  # 31
    rebase parser object parser rebase buffer socket remote stream pack  # 32
    token delta handle buffer queue socket ref tree ref handle rebase rebase  # 33
    merge blob token parser index token index object tree  # 34
    rebase queue rebase merge window cache parser  # 35
    queue object header socket handle ref queue window remote  # 36
    cache socket socket ref buffer  # 37
    socket tree delta index cache tree cache merge rebase handle index  # 38
    tree index tree delta ref header merge buffer merge rebase cache  # 39
    branch blob queue parser cache pack window commit  # 40
    object pack token socket stream ref  # 41
    pack pack token cache stream handle parser rebase handle ref remote queue  # 42
    worker tree blob index rebase  # 43
    remote parser socket token buffer buffer  # 44
    token queue window queue token window  # 45
    socket remote header parser object socket branch queue object worker merge merge  # 46
    commit window cache handle object commit  # 47
    blob cache window pack worker token delta ref cache  # 48
    object index tree delta socket token socket object cache  # 49
    socket handle branch object cache buffer  # 50
    token cache delta window window index token  # 51
    worker parser commit branch pack delta  # 52
    pack blob handle pack  # 53
    buffer header index cache handle  # 54
    remote window window handle commit socket cache cache cache stream  # 55
    index header worker ref index window token rebase  # 56
    delta commit merge header  # 57
    object stream tree object delta worker tree commit remote  # 58
    parser tree worker buffer token commit parser cache index merge object  # 59
    blob remote commit rebase window buffer blob delta delta pack stream  # 60
    index handle header worker object  # 61
    cache merge object ref window rebase index parser ref branch remote handle  # 62
    parser branch socket header parser blob merge blob  # 63
    cache cache stream tree pack object cache buffer blob delta queue  # 64
    blob branch socket buffer header stream rebase tree handle index  # 65
    delta index rebase queue stream handle pack  # 66
    token token blob buffer worker rebase commit blob  # 67
    delta rebase window object  # 68
    cache worker buffer remote stream rebase blob pack token  # 69
    pack socket commit pack header rebase merge  # 70
    handle handle handle window merge  # 71
    parser stream socket buffer  # 72
    cache worker token cache stream delta stream blob pack rebase  # 73
    worker token worker branch  # 74
    ref buffer buffer parser header remote remote worker merge ref window  # 75
    parser branch handle delta cache queue index  # 76
    blob parser token tree  # 77
    remote delta queue branch pack  # 78
    blob parser delta handle  # 79
    cache ref stream rebase  # 80
    socket ref cache delta  # 81
    branch header stream header blob object merge merge  # 82
