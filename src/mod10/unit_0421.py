    rebase cache window window object socket token commit stream merge handle ref  # 0
    commit remote object pack socket pack socket merge window  # 1
    token handle worker tree ref delta worker index tree token  # 2
    buffer commit tree blob buffer branch object cache handle index  # 3
    merge ref header socket blob socket header  # 4
    remote branch socket remote branch cache parser rebase blob  # 5
    object pack handle delta handle blob stream buffer rebase index ref  # 6
    parser tree pack branch socket index branch stream rebase object delta remote  # 7
    blob worker parser branch  # 8
    pack buffer window commit queue header window handle tree pack header  # 9
    header index token window  # 10
    tree header delta token merge ref remote handle delta  # 11
    remote branch header queue buffer delta rebase  # 12
    blob merge commit branch worker parser object header remote worker remote object  # 13
    branch socket stream branch window stream branch  # 14
    handle index handle token stream handle worker branch cache remote branch  # 15
    pack blob merge cache object remote tree commit merge merge  # 16
    ref window delta branch tree handle worker tree stream tree socket ref  # 17
    parser delta socket delta rebase parser  # 18
    commit object handle pack header handle rebase ref tree handle  # 19
    merge ref commit ref delta token index blob  # 20
    delta queue object remote object window window worker blob stream merge ref  # 21
    remote ref handle queue  # 22
    merge merge index header index  # 23
    branch index pack rebase  # 24
    token worker cache worker parser cache  # 25
    worker cache parser handle handle commit merge pack parser blob tree header  # 26
    window delta buffer merge remote queue  # 27
    pack worker parser object pack index  # 28
    pack cache parser parser remote remote handle buffer header merge token window  # 29
    header cache window buffer ref handle handle merge  # 30
    index socket merge ref branch header cache object header  # 31
    ref merge tree branch rebase window tree blob ref  # 32
    delta worker tree tree blob remote delta ref worker  # 33
    object parser branch merge pack parser  # 34
    queue blob delta rebase buffer delta  # 35
    index socket branch object  # 36
    merge ref token ref header  # 37
    cache object rebase queue window blob  # 38
    index delta cache commit index stream header  # 39
    rebase queue rebase rebase remote token tree delta stream stream  # 40
    buffer socket rebase ref window index parser buffer commit commit delta parser  # 41
    merge object ref pack object stream  # 42
    window parser pack handle  # 43
    handle merge token commit  # 44
    buffer header ref queue token delta  # 45
