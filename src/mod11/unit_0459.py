    handle branch token pack remote commit blob worker cache  # 0
    buffer buffer index merge blob remote handle  # 1
    buffer rebase tree blob index  # 2
    branch stream ref commit index buffer header window object blob queue cache  # 3
    blob remote parser ref merge socket merge rebase object pack  # 4
    delta parser blob remote object index  # 5
    rebase ref header cache socket delta rebase queue commit  # 6
    merge commit remote blob ref merge rebase pack buffer header ref token  # 7
    blob object pack parser worker merge header stream ref buffer commit  # 8
    token queue remote pack buffer  # 9
    delta delta merge branch parser  # 10
    stream blob rebase token parser  # 11
    remote cache window worker index merge object blob object window  # 12
    queue remote token socket remote pack blob index window blob  # 13
    stream worker token pack branch branch ref cache  # 14
    branch socket delta tree rebase pack parser object  # 15
    buffer window remote window merge socket  # 16
    socket window parser handle index ref buffer commit  # 17
    socket branch buffer window remote window queue pack  # 18
    stream handle merge socket rebase  # 19
    object pack index pack pack index blob  # 20
    stream merge index merge pack merge pack socket object window  # 21
    token branch buffer ref branch merge pack merge  # 22
    cache index tree merge handle stream commit pack  # 23
    parser stream object pack object  # 24
    parser object delta tree buffer ref  # 25
    delta cache parser merge cache rebase parser queue  # 26
    handle socket stream cache commit parser rebase rebase  # 27
    token socket socket token  # 28
    object stream delta header stream ref queue header delta queue commit pack  # 29
    object cache branch blob rebase rebase delta window window stream  # 30
    buffer remote socket cache window  # 31
    delta ref blob blob merge header index branch parser blob rebase handle  # 32
    tree rebase stream merge blob tree buffer window  # 33
    handle blob delta ref merge stream socket merge ref remote  # 34
    stream queue worker buffer stream merge token window stream pack  # 35
    header socket blob pack object commit tree blob worker merge pack index  # 36
    commit token delta commit cache  # 37
    ref pack branch merge socket blob blob worker  # 38
    header worker socket queue header parser stream queue blob object  # 39
    window socket object remote  # 40
    header header rebase index buffer commit queue worker window buffer worker  # 41
    token pack branch buffer remote merge queue stream delta delta  # 42
    stream stream object queue window header commit  # 43
    tree queue stream delta handle token remote cache worker  # 44
    pack buffer rebase header cache token blob pack tree  # 45
    worker delta worker branch queue  # 46
    cache tree branch branch parser index handle cache index delta tree rebase  # 47
    stream pack socket ref blob buffer blob delta object handle  # 48
    pack tree branch blob header remote rebase  # 49
    handle delta commit blob token parser tree index header header pack token  # 50
    object commit parser header pack delta  # 51
    ref worker object blob commit  # 52
    socket token pack object buffer delta handle commit  # 53
    socket index window token ref worker header pack  # 54
    buffer token parser cache queue  # 55
    remote window header worker worker header  # 56
    socket delta branch parser  # 57
    stream parser delta blob object queue merge  # 58
    handle handle cache delta tree delta tree pack delta socket buffer  # 59
    pack index delta stream queue index worker queue cache blob remote  # 60
    merge socket object rebase  # 61
    branch stream remote cache merge delta worker worker rebase pack parser  # 62
    index window cache pack object pack tree window blob cache header  # 63
    object cache object object remote pack delta rebase worker branch blob  # 64
    handle merge socket rebase stream branch blob header tree object  # 65
    rebase stream delta object pack window handle worker  # 66
    rebase socket parser window token cache pack socket object window delta delta  # 67
    pack worker header tree worker  # 68
    handle socket parser buffer remote merge socket handle branch handle blob  # 69
    stream merge stream window  # 70
    remote object buffer object worker  # 71
    delta parser queue buffer handle branch ref cache commit cache index  # 72
    queue queue header queue object  # 73
    tree handle tree header  # 74
    rebase branch index ref window merge queue object tree  # 75
    header pack branch buffer pack ref blob queue index  # 76
    worker pack parser token  # 77
    tree handle ref buffer branch commit  # 78
    token pack buffer remote window stream tree  # 79
    handle rebase index handle merge  # 80
    branch ref pack merge merge index queue handle parser queue stream blob  # 81
    index rebase socket blob  # 82
