    parser tree worker ref token cache merge token queue merge  # 0
    token tree handle worker  # 1
    cache parser socket object window rebase window buffer tree tree  # 2
    tree delta socket object handle commit delta remote queue object worker cache  # 3
    pack tree stream rebase pack parser object window ref  # 4
    handle token tree parser remote token  # 5
    stream cache ref window pack buffer commit queue window queue  # 6
    socket handle blob stream  # 7
    delta parser blob branch pack cache pack merge token token index rebase  # 8
    merge worker handle stream index merge token remote token  # 9
    cache header window parser object handle rebase object worker cache  # 10
    stream commit commit rebase index merge  # 11
    ref stream merge header socket worker branch header window blob  # 12
    parser socket cache index window handle worker blob  # 13
    pack header pack index branch object ref parser remote pack  # 14
    delta pack rebase index worker stream merge window  # 15
    remote queue object blob branch stream branch  # 16
    object tree commit parser  # 17
    pack stream index pack tree  # 18
    window header pack header header queue buffer  # 19
    handle tree blob commit  # 20
    header index rebase socket remote  # 21
    ref handle handle cache header header window object  # 22
    blob object worker ref parser buffer tree merge cache blob delta  # 23
    handle ref index window worker merge header header blob buffer window index  # 24
    handle stream stream commit pack object  # 25
    socket parser parser commit remote rebase pack worker worker index window  # 26
    object stream remote queue socket delta index rebase  # 27
    socket buffer delta remote cache blob cache commit buffer handle  # 28
    window handle stream branch cache buffer worker cache socket queue object  # 29
    ref object object buffer ref ref handle  # 30
    delta header merge handle merge object parser blob window  # 31
    token remote delta parser rebase buffer branch blob socket rebase delta  # 32
    branch header tree handle remote worker  # 33
    rebase stream remote socket branch object commit blob rebase  # 34
    object handle index index tree handle branch socket pack  # 35
    object pack token delta delta remote tree branch worker token  # 36
    branch header stream pack queue cache tree socket buffer tree  # 37
    worker index commit object remote worker worker merge  # 38
    pack parser merge commit socket parser header stream token parser  # 39
    branch handle index parser  # 40
    pack stream window header  # 41
    blob pack stream commit branch tree handle socket pack  # 42
    rebase stream object worker index token stream commit handle pack parser  # 43
    window branch commit pack cache blob rebase worker tree header ref cache  # 44
    object ref delta token worker delta buffer worker index ref  # 45
    buffer worker window commit cache  # 46
    delta rebase stream ref blob  # 47
    commit pack buffer socket socket ref window handle pack handle  # 48
    delta stream commit object  # 49
    buffer index pack merge socket remote token  # 50
    pack object queue buffer queue merge token buffer pack  # 51
    cache queue delta worker  # 52
    cache handle queue delta socket socket worker window branch  # 53
    delta remote branch socket handle parser token worker pack token delta worker  # 54
    object queue handle stream window token index pack  # 55
    tree buffer stream header pack object token worker handle tree parser rebase  # 56
    handle buffer worker merge tree queue worker queue remote branch socket  # 57
    window pack window object pack  # 58
    delta buffer commit buffer  # 59
    socket commit token worker window tree blob commit  # 60
    cache index parser ref pack token index  # 61
    handle merge merge object stream handle parser branch  # 62
    queue blob socket merge branch queue pack cache queue buffer  # 63
    worker pack remote object stream rebase  # 64
    cache buffer remote parser window token queue  # 65
    handle buffer remote object index pack  # 66
    ref queue buffer tree handle handle token pack queue stream commit header  # 67
    cache worker socket merge commit index object parser window token window  # 68
    token tree cache tree branch  # 69
    pack handle parser buffer merge  # 70
    stream cache remote stream pack  # 71
    ref cache worker merge parser blob commit merge blob window handle  # 72
    socket branch blob header branch pack socket header worker token buffer  # 73
    token worker handle window merge stream object object index socket  # 74
    tree buffer worker object handle delta ref socket merge blob pack  # 75
    blob pack cache remote blob blob header ref blob header index  # 76
    remote commit stream delta stream branch  # 77
    blob tree window stream queue socket buffer worker blob cache  # 78
    merge token remote index stream socket socket window branch remote  # 79
    token object blob handle ref handle  # 80
    index commit stream pack  # 81
    cache buffer pack window header handle commit rebase ref window commit  # 82
    token merge queue object socket worker blob index  # 83
    remote queue object stream stream  # 84
    handle worker parser worker handle merge  # 0
    stream rebase stream stream merge index pack  # 1
    pack header commit parser remote buffer merge remote rebase  # 2
    rebase queue rebase ref socket cache  # 3
    blob ref remote parser rebase stream object object token window commit merge  # 4
    rebase cache commit buffer rebase buffer index rebase socket cache worker merge  # 5
    token handle remote queue merge buffer  # 6
