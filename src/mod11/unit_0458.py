    blob blob stream window blob blob blob  # 0
    pack ref blob remote socket delta queue parser buffer blob  # 1
    cache blob rebase remote stream stream parser socket delta window tree  # 2
    pack ref handle blob header rebase delta tree  # 3
    window window buffer token cache object  # 4
    queue ref rebase header token parser commit cache  # 5
    socket remote queue socket handle  # 6
    blob remote merge token branch socket pack worker parser index  # 7
    header queue worker pack parser handle stream delta  # 8
    parser token object worker  # 9
    handle handle tree remote object pack worker parser header pack branch commit  # 10
    blob delta remote delta  # 11
    remote merge parser tree  # 12
    index parser stream header token commit  # 13
    blob rebase object index commit pack  # 14
    commit ref blob rebase header buffer tree  # 15
    blob tree remote blob ref remote index stream parser  # 16
    tree commit handle stream object branch ref object  # 17
    blob handle rebase buffer stream queue buffer ref ref buffer buffer remote  # 18
    index cache window header pack branch  # 19
    cache branch buffer commit blob stream socket  # 20
    pack socket queue socket socket ref remote worker worker branch remote worker  # 21
    delta branch buffer queue object pack blob  # 22
    ref remote branch delta queue socket stream merge queue cache ref window  # 23
    commit merge merge object socket stream worker ref  # 24
    remote delta commit commit pack  # 25
    tree ref cache handle handle delta blob queue  # 26
    handle window window token buffer blob  # 27
    remote pack worker remote buffer index remote worker branch  # 28
    queue pack socket delta remote parser header remote window merge queue  # 29
    remote header token object  # 30
    rebase handle rebase rebase stream pack branch  # 31
    index merge queue delta token queue remote stream window pack blob  # 32
    pack handle header socket blob worker  # 33
    pack delta object blob window  # 34
    queue merge commit cache window cache index token ref tree delta buffer  # 35
    blob branch ref cache  # 36
    object blob object object handle tree parser rebase token object  # 37
    index socket ref ref  # 38
    rebase delta queue worker tree parser stream blob merge stream delta token  # 39
    blob branch pack pack delta branch stream object socket rebase ref socket  # 40
    worker window token index object stream branch rebase token ref  # 41
    remote token window handle token  # 42
    commit tree handle queue index branch buffer window  # 43
    object commit merge index rebase commit handle branch window  # 44
    cache header rebase window commit tree delta object token handle merge  # 45
    cache blob remote socket stream stream worker blob remote blob  # 46
    window rebase rebase worker branch  # 47
    stream stream rebase handle ref cache index  # 48
    worker delta commit object header blob  # 49
    stream rebase object cache commit header worker cache header socket  # 50
    window cache handle branch blob  # 51
