    branch branch merge rebase tree worker socket merge header pack worker  # 0
    pack object index handle merge tree pack  # 1
    index token worker handle queue merge buffer branch blob handle  # 2
    index queue window blob ref  # 3
    tree merge commit window rebase rebase delta socket merge object branch worker  # 4
    window pack queue delta parser queue window pack tree remote stream blob  # 5
    header object header buffer remote header token handle remote socket  # 6
    token blob token cache merge index  # 7
    socket token merge index cache rebase socket index buffer pack  # 8
    remote tree remote queue merge delta index  # 9
    rebase pack merge blob pack commit handle branch pack blob commit tree  # 10
    index socket commit tree stream ref token queue delta handle cache remote  # 11
    remote tree object object delta ref token handle parser ref queue parser  # 12
    queue branch commit commit commit worker remote stream object ref branch  # 13
    window rebase pack index branch cache tree parser object delta worker cache  # 14
    delta queue handle remote handle pack  # 15
    pack window worker branch commit cache delta buffer  # 16
    window token token rebase stream parser branch  # 17
    parser object pack pack tree delta remote index index stream worker  # 18
    worker index branch object rebase  # 19
    index ref handle header handle object header  # 20
    cache index buffer parser  # 21
    branch commit queue rebase index queue socket  # 22
    cache worker parser buffer rebase rebase rebase merge remote remote token window  # 23
    pack handle commit delta rebase queue remote blob delta remote queue rebase  # 24
    ref window pack index remote  # 25
    delta commit socket remote socket  # 26
    rebase header cache blob handle worker remote socket pack cache token delta  # 27
    delta socket queue commit handle index  # 28
    object socket queue commit window commit queue token remote  # 29
    merge branch handle worker worker branch socket queue blob index  # 30
    branch ref cache stream  # 31
    delta pack rebase remote merge header worker ref socket window parser  # 32
    queue window blob index branch blob buffer object  # 33
    remote remote pack object handle  # 34
    index object token rebase  # 35
    queue worker branch branch window tree object  # 36
    handle object buffer branch stream  # 37
    queue merge delta delta header rebase cache  # 38
    rebase header object branch queue rebase token stream branch parser rebase socket  # 39
    remote handle worker merge tree blob cache buffer parser commit ref parser  # 40
    window commit commit ref ref  # 41
    parser branch header tree  # 42
    header tree commit worker  # 43
    blob worker object stream ref commit  # 44
    commit header buffer merge remote queue pack  # 45
