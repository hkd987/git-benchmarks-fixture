    index header branch stream commit parser ref rebase object  # 0
    cache pack handle rebase socket merge header pack  # 1
    commit commit stream commit index pack blob  # 2
    cache window tree parser worker object rebase header  # 3
    tree worker buffer socket delta socket parser remote index  # 4
    ref blob remote worker stream  # 5
    buffer queue stream queue merge token branch stream rebase remote  # 6
    object socket branch worker window  # 7
    buffer blob blob branch parser buffer blob delta worker ref  # 8
    remote tree queue buffer merge buffer rebase window merge  # 9
    queue commit delta header index ref rebase blob blob merge blob pack  # 10
    branch remote commit handle blob delta pack cache  # 11
    token buffer delta queue queue buffer tree index index header  # 12
    remote branch stream rebase rebase  # 13
    merge socket object blob index queue commit merge branch commit index  # 14
    rebase stream remote window buffer branch  # 15
    pack socket delta token  # 16
    queue socket worker tree remote blob remote socket stream  # 17
    token rebase worker stream ref stream stream  # 18
    ref header object index  # 19
    branch tree index tree rebase parser  # 20
    ref ref pack worker socket tree  # 21
    window commit parser handle index commit commit index stream commit merge  # 22
    object window header ref blob  # 23
    object delta tree blob parser branch merge worker  # 24
    header rebase commit stream worker stream commit object queue delta remote  # 25
    merge index index worker parser delta  # 26
    branch queue window parser parser stream merge merge merge window socket  # 27
    object buffer handle remote branch parser stream socket merge  # 28
    rebase queue branch ref worker header delta ref  # 29
    window index worker index pack commit worker branch header cache  # 30
    parser index ref token buffer  # 31
    stream worker pack window ref branch window  # 32
    merge window index worker delta parser merge object  # 33
    tree tree index parser buffer blob  # 34
    stream cache socket branch rebase remote  # 35
    index parser rebase token tree header header commit  # 36
    queue delta window ref rebase index remote remote remote socket header commit  # 37
    pack worker buffer merge worker commit  # 38
    blob ref remote merge merge blob queue queue pack object pack tree  # 39
    header cache worker parser socket parser blob  # 40
    buffer tree delta commit index parser commit branch socket branch tree  # 41
    ref delta rebase blob queue pack parser stream  # 42
    merge delta window window delta window window rebase remote commit  # 43
    rebase worker handle queue stream buffer  # 44
    socket socket cache window  # 45
    index object pack branch window queue cache  # 46
    queue worker index stream socket worker branch commit stream parser handle  # 47
    worker remote token commit commit rebase rebase handle branch parser  # 48
    ref handle worker handle  # 49
    index pack window tree commit merge queue remote tree delta queue header  # 50
    ref tree token header window  # 51
    commit merge window merge delta remote commit queue parser blob  # 52
    parser remote ref cache merge tree parser ref parser header  # 53
    parser index handle cache socket merge window blob delta tree  # 54
    pack token branch window worker remote header stream  # 55
    cache queue parser delta tree parser index  # 56
    parser buffer rebase parser window parser queue handle  # 57
    worker rebase blob queue header blob header token index header  # 58
    ref token remote queue merge socket handle worker header blob delta handle  # 59
    tree queue commit token ref rebase handle rebase handle worker  # 60
    worker header blob queue rebase merge rebase rebase tree tree parser  # 61
    parser cache handle token buffer  # 62
    merge queue pack remote cache socket queue parser buffer socket  # 63
    delta object tree window  # 64
    cache pack delta cache window tree pack stream handle socket  # 65
    worker window worker handle worker branch commit  # 66
    pack token cache delta token index header commit delta branch object  # 67
    object socket pack socket tree  # 68
    merge token window header delta  # 69
    queue pack handle branch tree ref object rebase window parser token  # 70
    buffer rebase blob window window delta cache index branch queue  # 71
    ref pack queue queue branch pack rebase parser  # 72
    stream window object buffer buffer merge buffer token worker merge remote tree  # 73
    window ref header delta  # 74
    pack header ref queue merge window worker remote token token rebase  # 75
    token header delta cache rebase merge commit merge parser buffer  # 76
    branch handle index tree handle header remote stream  # 77
    commit handle socket tree token index rebase delta branch  # 78
    delta handle blob merge queue queue cache rebase index merge  # 79
    tree cache token queue socket index blob buffer parser buffer buffer commit  # 80
    commit remote pack queue  # 81
    pack stream ref socket ref ref queue  # 82
    header remote socket delta handle socket queue blob object stream  # 83
    parser blob stream worker commit rebase commit rebase rebase  # 84
    commit socket delta rebase merge cache token stream stream  # 85
