    buffer merge window pack header tree stream token rebase handle  # 0
    handle worker header merge object index index socket object queue branch stream  # 1
    delta blob commit buffer delta token parser buffer token  # 2
    tree blob pack blob delta remote  # 3
    branch buffer branch tree parser ref socket remote  # 4
    branch index handle index object handle buffer header token handle delta  # 5
    object queue stream stream queue commit  # 6
    remote delta buffer delta tree stream  # 7
    cache window handle parser queue remote token delta cache remote blob worker  # 8
    delta header blob object  # 9
    stream object queue socket worker cache token  # 10
    worker cache ref handle remote delta tree object object handle rebase tree  # 11
    worker handle parser window buffer merge index index remote  # 12
    worker index socket branch queue token window branch handle handle  # 13
    tree header index window index socket  # 14
    merge blob commit blob handle ref header worker token remote  # 15
    delta object stream queue socket branch remote stream buffer buffer  # 16
    remote rebase handle branch tree remote cache token stream cache tree header  # 17
    object handle pack rebase tree merge delta window parser pack token  # 18
    window worker parser buffer merge commit blob header buffer cache buffer tree  # 19
    header index pack commit cache header ref tree blob socket  # 20
    ref token tree delta pack queue header cache stream rebase commit  # 21
    object blob header delta buffer socket cache socket remote delta pack  # 22
    window token socket object  # 23
    blob buffer blob tree header cache header buffer stream  # 24
    header handle handle token delta delta parser  # 25
    blob blob token parser worker  # 26
    handle header stream branch pack blob blob  # 27
    merge remote delta pack remote queue blob branch rebase branch merge  # 28
    object handle pack window  # 29
    stream buffer merge stream branch queue  # 30
    index merge commit remote queue  # 31
    socket remote pack rebase worker token blob index handle  # 32
    buffer socket pack rebase rebase object cache  # 33
    tree index cache index buffer cache  # 34
    branch handle handle tree socket rebase tree  # 35
    merge header delta cache handle merge ref socket branch cache branch pack  # 36
    pack tree rebase ref object buffer header buffer ref socket merge worker  # 37
    handle index pack commit pack delta index  # 38
    merge stream object queue branch merge  # 39
    index stream window buffer pack merge header  # 40
    blob branch handle parser ref  # 41
    token merge queue ref rebase  # 42
    ref queue delta object branch socket  # 43
    pack socket remote buffer remote cache index  # 44
    rebase socket delta cache  # 45
    branch worker buffer window delta socket  # 46
    buffer tree buffer cache commit socket delta worker queue index buffer  # 47
    socket delta object remote ref branch tree  # 48
    merge tree header pack blob index  # 49
    window rebase buffer header  # 50
    socket commit index socket tree object tree delta queue object index  # 51
    pack rebase branch branch buffer window remote merge token index  # 52
    cache stream token commit ref cache cache object blob  # 53
    rebase queue worker header pack commit worker cache  # 54
