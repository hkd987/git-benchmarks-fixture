    handle delta pack socket  # 0
    socket token object remote  # 1
    blob pack object ref commit rebase remote merge window delta queue ref  # 2
    blob delta socket merge cache branch ref pack socket parser  # 3
    merge pack queue ref pack token token blob parser branch  # 4
    handle ref remote window parser parser merge  # 5
    remote queue worker worker  # 6
    tree buffer socket buffer  # 7
    commit parser handle handle remote index  # 8
    index socket delta parser branch rebase  # 9
    cache window socket token window rebase ref blob stream ref  # 10
    remote socket buffer branch buffer remote queue worker stream  # 11
    handle branch stream ref  # 12
    socket merge delta tree tree buffer branch remote stream pack header  # 13
    branch object remote queue blob ref ref header commit buffer commit delta  # 14
    branch branch window stream cache delta  # 15
    index ref merge remote window rebase socket queue remote tree  # 16
    branch window merge rebase remote ref worker  # 17
    rebase cache worker parser  # 18
    header handle commit tree remote tree handle  # 19
    rebase pack cache queue buffer handle delta  # 20
    cache rebase token object remote  # 21
    blob token remote queue queue merge index buffer worker queue  # 22
    cache branch buffer tree blob queue header merge stream worker index  # 23
    blob merge header handle worker window ref remote  # 24
    window ref token header merge token buffer merge socket parser parser delta  # 25
    remote socket delta blob object queue tree blob pack tree commit ref  # 26
    blob header pack remote buffer cache blob commit delta index index rebase  # 27
    rebase buffer parser pack token blob tree socket tree ref token delta  # 28
    tree cache handle cache object  # 29
    stream index blob cache socket  # 30
    object token stream cache commit rebase worker token socket window  # 31
    tree blob remote parser object ref  # 32
    blob socket parser handle  # 33
    parser queue pack socket rebase token  # 34
    delta merge branch stream blob pack token buffer header cache  # 35
    index ref buffer socket window ref queue  # 36
    delta remote blob blob  # 37
    remote parser remote ref object cache commit branch token stream pack parser  # 38
    buffer remote buffer tree  # 39
    tree queue commit blob rebase blob buffer header stream stream  # 40
    delta pack branch blob branch index pack pack worker merge blob  # 41
    merge token header ref stream header object  # 42
    tree worker merge object ref queue buffer window merge ref object  # 43
    object parser buffer commit parser object object delta handle commit  # 44
    branch queue handle parser queue commit  # 45
    window pack pack buffer window cache token token rebase window remote  # 46
    merge remote socket pack handle rebase socket parser  # 47
    ref socket remote rebase pack rebase delta socket stream  # 48
    token handle merge header token  # 49
    token object delta object pack ref buffer blob tree remote branch remote  # 50
    window worker pack delta blob cache window delta blob tree branch header  # 51
    pack worker delta socket window remote socket cache header header index remote  # 52
    commit remote remote handle object rebase worker  # 53
    tree header delta tree handle remote ref merge index parser  # 54
    cache handle handle cache cache remote object blob  # 55
    handle socket worker token cache parser header cache parser  # 56
    pack cache window handle stream buffer index buffer commit worker  # 57
    blob rebase tree buffer cache  # 58
    cache index pack object header window ref buffer header buffer blob socket  # 59
    token tree stream worker delta window index stream  # 60
    merge socket tree token handle ref window handle  # 61
    branch pack worker delta delta tree object tree  # 62
    index tree buffer handle socket ref buffer  # 63
    merge parser socket worker stream blob remote header blob object ref tree  # 64
    delta queue branch socket  # 65
    header commit pack delta ref  # 66
    handle rebase index blob commit commit worker  # 67
    stream rebase socket rebase branch worker branch worker  # 68
    handle rebase commit parser  # 69
    queue buffer blob blob token token tree merge  # 70
    ref queue handle handle buffer ref delta remote stream socket delta pack  # 71
    rebase delta branch branch  # 72
    queue remote buffer socket merge buffer window commit rebase queue stream  # 73
    rebase ref window buffer stream ref buffer commit branch  # 74
    branch window rebase rebase ref  # 75
    object commit queue merge queue index token blob cache cache handle  # 76
    blob object delta branch buffer queue header remote branch parser  # 77
    token commit index index pack delta index queue object tree pack ref  # 78
    token socket blob tree ref window  # 79
    ref object window window worker  # 80
    pack object queue window ref socket header tree window  # 0
    window merge delta pack token branch object index window  # 1
    commit queue header tree remote object worker token parser cache blob  # 2
    remote tree token index object  # 3
    handle rebase commit window  # 4
    worker header merge index handle stream remote socket ref  # 5
    worker pack rebase pack  # 6
    window handle rebase object blob  # 7
    index branch token rebase buffer token stream token window  # 8
    buffer queue merge worker window token pack branch header socket merge  # 9
    index rebase queue remote branch object blob tree parser parser  # 10
    pack parser handle buffer token rebase  # 11
    merge delta handle commit buffer socket parser object remote blob  # 12
    stream rebase ref cache remote  # 13
