    delta index rebase branch parser cache blob blob merge  # 0
    cache index worker ref branch rebase tree handle  # 1
    pack queue token blob buffer  # 2
    commit ref rebase cache branch merge object blob  # 3
    header buffer handle remote remote pack object  # 4
    worker token rebase cache branch window merge  # 5
    tree parser merge blob rebase worker  # 6
    delta index pack rebase tree merge stream remote  # 7
    handle queue index blob handle branch parser window  # 8
    token object tree cache  # 9
    worker rebase window pack  # 10
    parser blob window cache rebase parser worker buffer delta  # 11
    token token ref delta object remote delta remote socket object  # 12
    stream pack merge queue stream remote index cache branch object rebase  # 13
    branch ref window branch pack parser  # 14
    queue remote pack object token cache parser queue buffer  # 15
    queue commit header header socket remote parser socket buffer buffer token window  # 16
    queue ref ref stream rebase ref window branch ref ref parser commit  # 17
    branch remote cache ref  # 18
    branch blob ref socket token queue token pack  # 19
    rebase rebase commit merge stream token branch  # 20
    index handle cache blob parser  # 21
    merge tree buffer queue  # 22
    remote index buffer cache  # 23
    pack buffer rebase merge branch worker object  # 24
    window token stream header socket header  # 25
    index blob worker ref  # 26
    window commit worker header blob remote merge worker  # 27
    blob object handle commit worker delta socket parser pack buffer  # 28
    cache branch worker ref stream token buffer stream index  # 29
    queue socket rebase parser buffer  # 30
    branch rebase blob parser blob object buffer pack commit  # 31
    parser cache blob socket stream token worker ref socket parser commit window  # 32
    ref buffer branch header pack cache object tree handle ref remote  # 33
    remote object socket socket cache object token handle ref handle merge  # 34
    parser handle pack queue ref object merge branch queue worker header pack  # 35
    stream parser cache merge socket tree index  # 36
    rebase delta object index object tree parser queue object socket remote  # 37
    commit worker tree parser branch commit stream buffer header header  # 38
    merge ref handle commit pack merge queue worker handle  # 39
    pack commit rebase socket handle worker window tree parser  # 40
    window cache ref commit pack worker rebase  # 41
    queue cache index cache stream buffer stream pack  # 42
    tree blob index cache stream parser index worker  # 43
    window window header blob queue parser delta commit token buffer  # 44
    ref cache parser queue pack merge worker socket  # 45
    token merge commit ref cache merge worker object remote index index tree  # 46
    handle window buffer header merge parser commit token index socket cache buffer  # 47
    token ref handle token branch merge stream  # 48
    header queue cache window remote rebase delta delta queue delta  # 49
    rebase commit commit rebase stream handle  # 50
    index ref remote token handle  # 51
    rebase commit queue cache ref index header window stream ref index  # 52
    buffer buffer buffer header handle tree branch queue header  # 53
    stream ref pack commit stream object  # 54
    rebase index parser blob index token branch  # 55
    stream handle tree parser socket handle stream queue cache delta blob window  # 56
    object commit handle commit parser index worker window merge queue socket  # 57
