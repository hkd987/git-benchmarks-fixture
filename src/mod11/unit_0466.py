    merge parser window token tree tree handle index header header merge remote  # 0
    cache parser handle handle worker tree blob cache queue  # 1
    object socket remote ref header pack delta tree  # 2
    rebase window buffer token commit rebase delta index rebase commit  # 3
    merge handle worker window pack index ref commit pack  # 4
    buffer rebase pack pack tree merge  # 5
    merge rebase handle commit ref parser queue handle  # 6
    header commit delta ref commit  # 7
    delta ref handle tree tree socket delta  # 8
    socket remote stream branch delta branch  # 9
    buffer worker remote ref parser blob stream remote remote ref buffer  # 10
    token worker branch header rebase pack pack blob commit  # 11
    header header index handle  # 12
    ref remote queue buffer index worker delta branch token tree  # 13
    buffer parser pack blob handle worker buffer branch cache ref worker buffer  # 14
    handle branch cache token blob index rebase  # 15
    pack remote merge cache queue tree handle  # 16
    index socket ref cache  # 17
    stream handle tree handle queue commit delta blob  # 18
    ref pack index tree socket pack merge window  # 19
    commit rebase rebase merge index tree queue index stream parser object delta  # 20
    stream branch object header delta ref  # 21
    header queue header buffer pack object socket rebase  # 22
    header pack parser buffer  # 23
    stream queue rebase window object object stream socket index queue parser cache  # 24
    queue cache tree queue blob ref commit delta worker tree  # 25
    parser parser pack buffer queue tree token commit merge queue  # 26
    rebase cache index parser index pack rebase worker blob  # 27
    parser token index object tree  # 28
    worker commit cache pack delta parser merge commit rebase  # 29
    window merge pack tree  # 30
    stream stream remote rebase token remote worker tree blob window  # 31
    parser token delta stream ref index branch worker queue commit  # 32
    parser header worker worker handle socket commit header blob commit buffer  # 33
    branch remote tree socket ref queue rebase token  # 34
    window tree header object object ref  # 35
    delta branch buffer rebase token object rebase worker  # 36
    stream socket index merge token commit  # 37
    stream buffer merge index  # 38
    parser queue worker socket worker branch index branch cache socket queue  # 39
    cache rebase stream commit socket stream token token remote cache delta  # 40
    blob cache queue stream token pack  # 41
    handle object token parser handle merge  # 42
    buffer stream rebase queue commit remote queue worker stream window header index  # 43
    token index object buffer remote index branch branch  # 44
    ref branch tree branch handle ref remote token header delta header token  # 45
    cache commit index cache parser pack buffer cache merge cache  # 46
    buffer token cache branch pack branch rebase header parser tree  # 47
    object tree merge stream socket cache blob handle  # 48
    stream blob remote header  # 49
    object window token token handle window buffer token socket  # 50
    buffer remote header queue window index  # 51
    merge rebase queue worker handle  # 52
    token socket ref buffer socket  # 53
    parser delta header queue object object stream remote rebase  # 54
    merge parser index delta token parser  # 55
    merge pack parser branch worker remote object ref pack ref ref token  # 56
    object ref buffer remote token header  # 57
    handle handle queue buffer worker token merge header merge  # 58
    blob rebase worker header rebase pack parser commit rebase socket tree  # 59
    queue token token worker rebase commit cache  # 60
    delta window stream queue header stream socket rebase commit tree rebase window  # 61
    ref stream pack token worker  # 62
    socket remote merge cache stream  # 63
    token branch token ref remote worker object  # 64
    delta branch merge pack remote commit pack socket window pack object  # 65
    branch object tree buffer worker  # 66
    socket cache stream parser delta  # 67
    merge merge tree rebase cache  # 0
    token index handle cache commit stream index branch token  # 1
    window header delta branch delta  # 2
    rebase branch merge queue tree  # 3
    index window worker header delta queue buffer ref object window  # 4
    pack ref commit merge branch window socket index window window window  # 0
    queue object handle socket worker delta handle handle  # 1
    parser remote worker commit tree rebase blob worker blob blob token rebase  # 2
    stream window index window queue handle  # 3
    index tree rebase rebase commit  # 4
    stream buffer tree delta blob header tree blob buffer index branch remote  # 5
    window worker queue worker index cache cache ref ref rebase  # 6
    window delta tree merge rebase  # 7
    index ref parser blob tree branch tree token merge rebase parser tree  # 0
    object header commit rebase pack token blob branch  # 1
    object delta socket window  # 2
    queue merge rebase commit merge stream handle rebase  # 3
    index tree commit remote cache branch stream window handle cache tree pack  # 4
    token merge stream index object queue pack commit cache rebase  # 5
    commit handle window header object delta tree remote cache ref  # 6
    rebase index cache token buffer  # 7
    parser handle pack parser worker  # 8
    branch stream object queue token  # 9
    parser window worker object parser worker branch buffer handle queue blob commit  # 10
    parser header token token stream remote rebase  # 0
    rebase delta buffer queue rebase socket stream index  # 1
    delta index stream socket buffer parser  # 2
    socket buffer handle index merge object buffer stream stream handle  # 3
    token rebase commit blob handle  # 4
