    parser delta cache object parser  # 0
    commit socket window token  # 1
    parser parser buffer parser commit blob delta window blob  # 2
    stream commit delta blob ref window header  # 3
    pack handle stream blob cache rebase handle pack index queue cache worker  # 4
    rebase index handle cache branch index delta tree pack  # 5
    worker pack worker header index rebase header  # 6
    handle queue branch rebase handle queue index tree ref stream cache rebase  # 7
    token cache header stream  # 8
    remote blob stream buffer handle socket object  # 9
    header blob merge tree stream blob object rebase token token  # 10
    object buffer blob window merge object  # 11
    token token blob parser token parser parser object  # 12
    queue index stream merge queue index rebase queue stream ref  # 13
    window remote socket tree worker parser  # 14
    tree cache parser branch  # 15
    buffer blob token index merge tree delta socket worker cache  # 16
    worker socket cache ref buffer cache header  # 17
    buffer remote socket branch header ref index  # 18
    cache remote rebase buffer merge index blob  # 19
    parser queue socket header ref rebase token stream branch cache stream stream  # 20
    window socket buffer blob index queue index stream cache branch stream  # 21
    ref stream worker commit branch stream  # 22
    parser remote socket index delta stream tree  # 23
    branch commit header tree queue ref window stream  # 24
    pack window cache worker ref tree ref buffer blob cache  # 25
    queue window delta index queue index  # 26
    index header pack commit window rebase parser parser index parser  # 27
    ref commit tree socket window header  # 28
    queue pack token branch queue  # 29
    handle ref merge delta merge parser ref merge pack index  # 30
    worker blob handle merge worker pack cache  # 31
    commit tree index remote queue index parser cache remote rebase worker window  # 32
    merge merge object token cache stream object queue delta tree header  # 33
    socket branch queue commit token  # 34
    token merge worker pack parser header delta socket merge cache  # 35
    commit tree blob token worker object  # 36
    token pack blob remote index  # 37
    object window parser branch worker worker  # 38
    socket header delta handle buffer stream merge header rebase blob blob  # 39
    socket worker worker token worker tree cache rebase delta queue branch token  # 40
    delta queue commit remote header index cache blob token token parser  # 41
    remote parser merge parser queue token handle parser queue worker worker  # 42
    token delta branch object  # 43
    rebase stream ref header handle remote object tree  # 44
    window token parser rebase window  # 45
    commit index tree window branch tree header window  # 46
    blob branch buffer commit tree parser merge branch merge socket blob  # 47
    buffer blob tree delta stream worker parser header token token queue  # 48
    branch tree branch worker stream  # 49
    commit remote pack worker worker commit  # 50
    remote buffer queue tree queue  # 51
    branch pack merge stream window  # 52
    commit header remote stream branch delta branch pack header handle stream  # 53
    branch handle blob queue blob merge tree ref  # 54
    blob branch tree header  # 55
    cache blob handle worker window  # 56
    index object merge queue branch branch worker ref buffer worker socket  # 57
    handle worker commit delta  # 58
    remote tree socket header header delta socket ref  # 59
    pack tree tree parser branch remote socket worker handle parser socket remote  # 60
    rebase remote handle queue  # 61
    handle buffer index blob window  # 62
    pack header remote commit delta token  # 63
    token stream queue token object token index pack buffer commit  # 64
    commit cache token socket blob stream  # 65
    tree tree index commit object remote blob parser  # 66
    object index handle buffer  # 67
    queue parser tree stream remote pack handle merge token stream window handle  # 68
    object token merge buffer remote blob delta buffer commit worker buffer  # 69
    branch cache handle token buffer cache  # 70
    pack remote worker parser rebase object header cache stream socket delta pack  # 71
    token merge commit cache  # 72
    delta buffer pack parser worker parser pack tree index  # 73
    cache window pack tree  # 74
    pack ref rebase index delta header  # 75
    delta branch delta parser header parser commit socket worker buffer header remote  # 76
    buffer stream commit object stream blob delta remote merge stream  # 77
    ref buffer branch parser merge  # 78
    pack queue socket handle commit token cache index delta  # 79
    merge window token ref parser  # 80
    worker pack buffer worker queue ref commit tree socket commit  # 81
    ref handle merge window  # 82
    pack header stream commit delta object window  # 83
    buffer buffer object stream blob branch header token cache delta rebase cache  # 84
    worker socket socket parser object object stream index queue buffer  # 85
    index token buffer remote window object remote  # 86
    remote socket blob ref socket  # 87
    tree delta branch index pack index token branch index  # 0
    worker blob ref stream window  # 1
    commit delta parser header handle handle branch parser index token stream queue  # 2
    commit handle remote rebase rebase  # 3
    tree index ref rebase stream tree merge queue  # 4
    queue handle delta cache object merge object branch worker  # 5
    buffer window window stream window remote delta ref object window  # 6
    delta object buffer header pack ref index blob commit  # 7
    window worker cache branch handle buffer pack socket window header token  # 8
    index blob object handle merge stream  # 9
    queue remote header commit handle buffer stream header window rebase remote commit  # 10
