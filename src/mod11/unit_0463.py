    merge header pack index buffer stream delta index remote parser  # 0
    tree socket delta buffer pack branch token blob  # 1
    commit worker index stream delta object delta stream worker  # 2
    header token worker object delta  # 3
    socket blob window ref merge handle commit index token  # 4
    ref index token index tree stream handle ref branch object  # 5
    tree header index commit worker socket  # 6
    window blob window buffer  # 7
    parser tree blob rebase window remote object blob handle blob worker  # 8
    index window pack cache rebase index cache  # 9
    parser worker delta index queue  # 10
    pack object stream pack  # 11
    window merge remote commit  # 12
    tree ref object remote stream cache buffer object queue  # 13
    handle ref window buffer delta ref  # 14
    worker buffer index socket remote index rebase  # 15
    handle socket buffer pack worker parser pack rebase token handle ref  # 16
    cache header commit commit rebase pack buffer commit index window stream blob  # 17
    header buffer delta commit blob socket  # 18
    index cache ref object handle cache object header ref  # 19
    cache token stream pack delta branch ref  # 20
    tree header cache branch queue pack pack handle  # 21
    blob object merge token token window  # 22
    header ref pack pack object queue rebase pack rebase ref commit  # 23
    stream parser header header commit  # 24
    ref remote tree ref handle header token pack queue cache window  # 25
    worker socket merge blob parser queue handle window handle parser cache remote  # 26
    socket index branch cache merge buffer header  # 27
    pack stream remote worker rebase parser branch token branch queue header  # 28
    blob remote parser blob handle stream token delta  # 29
    worker buffer socket blob handle  # 30
    socket handle commit remote tree merge delta tree tree  # 31
    cache handle blob parser object pack merge worker remote parser  # 32
    branch blob stream blob worker rebase worker window index  # 33
    commit window merge handle merge parser commit index buffer delta branch token  # 34
    stream object worker commit object branch rebase window  # 35
    blob parser branch index index parser commit  # 36
    queue tree header token blob parser  # 37
    token worker object header object branch ref tree merge  # 38
    socket rebase parser rebase header socket tree pack queue socket branch  # 39
    ref cache branch commit ref remote branch  # 40
    index rebase blob stream parser cache object commit delta stream  # 41
    queue ref branch cache buffer blob stream worker buffer handle worker commit  # 42
    merge buffer window merge ref cache token delta remote  # 43
    header index pack object delta buffer buffer  # 44
    index cache remote delta remote buffer  # 45
    window token index branch  # 46
    cache object blob pack tree delta rebase cache ref object worker  # 47
    cache tree index parser queue socket commit  # 48
    worker socket remote stream buffer queue index handle stream handle  # 49
    buffer token socket socket rebase commit pack pack pack blob  # 50
    object buffer socket merge commit stream ref socket cache buffer merge  # 51
    pack tree commit branch index tree blob index cache  # 52
    window token worker rebase queue worker header queue tree  # 53
    index parser ref stream blob remote header object worker cache pack  # 54
    stream ref queue merge header blob window  # 55
    parser socket parser remote token socket merge handle  # 56
    rebase queue commit branch delta remote blob header  # 57
    index token parser merge pack token handle  # 58
    window blob merge object worker delta header branch pack ref branch socket  # 59
    cache buffer index window blob  # 60
    pack remote ref merge cache parser pack  # 61
    parser socket blob pack handle parser remote delta blob window rebase tree  # 62
    buffer tree delta worker object  # 63
    ref handle stream rebase  # 64
    merge ref worker index ref delta handle  # 65
    socket window merge commit merge window branch  # 66
    ref socket buffer cache handle object handle blob  # 67
    socket pack cache worker branch pack  # 68
    delta buffer blob remote merge  # 69
