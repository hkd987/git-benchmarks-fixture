    index delta socket object pack rebase blob blob  # 0
    parser handle handle remote rebase stream merge object tree remote token  # 1
    token window branch object handle branch window blob merge cache parser  # 2
    remote remote branch rebase token tree  # 3
    queue token handle index rebase merge queue buffer tree pack header stream  # 4
    branch tree worker ref worker rebase buffer parser ref  # 5
    pack pack index ref parser token handle header  # 6
    buffer token index rebase stream blob pack blob  # 7
    cache branch handle header queue delta cache pack merge  # 8
    index blob header token handle window socket stream stream index  # 9
    worker stream socket branch  # 10
    branch branch merge header worker queue socket  # 11
    worker handle buffer cache queue buffer rebase merge  # 12
    merge index merge stream rebase parser  # 13
    stream remote ref delta parser worker parser cache stream stream header socket  # 14
    blob queue rebase ref socket  # 15
    parser window stream cache blob socket worker  # 16
    rebase stream buffer delta merge buffer worker  # 17
    commit rebase pack commit object object cache rebase buffer object parser header  # 18
    tree stream delta queue parser  # 19
    ref delta commit window stream tree socket ref delta  # 20
    rebase object header token delta rebase window parser  # 21
    token window socket token parser queue object  # 22
    remote blob merge window branch token merge buffer header socket ref window  # 23
    remote pack blob branch cache worker header ref header commit remote handle  # 24
    token object parser queue handle  # 25
    commit blob header branch commit delta  # 26
    merge stream worker socket cache buffer pack parser delta  # 27
    commit socket rebase merge queue buffer commit pack  # 28
    merge commit merge pack cache worker commit merge index  # 29
    pack rebase blob blob handle delta commit ref queue queue  # 30
    socket token delta queue stream handle window rebase  # 31
    stream parser token window queue remote socket parser ref  # 32
    merge handle pack worker stream blob socket  # 33
    buffer pack token header parser  # 34
    queue buffer parser branch rebase  # 35
    header worker merge merge cache queue queue  # 36
    blob pack merge object handle parser window worker  # 37
    commit header handle window index  # 38
    header rebase queue stream  # 39
    cache rebase delta worker window handle  # 40
    header object merge merge blob rebase ref  # 41
    token buffer branch commit socket tree delta  # 42
    parser ref index stream merge remote parser stream remote handle remote socket  # 43
    delta delta commit window token cache blob handle object cache  # 44
    handle handle blob rebase stream worker ref worker index index  # 45
    window index ref commit object index branch  # 46
    parser remote pack branch header pack pack commit handle object cache handle  # 47
    delta queue queue ref window delta stream ref handle tree cache handle  # 48
    blob object parser pack rebase token token  # 49
    ref branch ref token commit socket queue rebase  # 50
    ref token worker worker  # 51
    rebase window cache object buffer  # 52
    ref header blob window handle merge commit branch index socket  # 53
    socket branch parser rebase buffer socket handle  # 54
    handle delta rebase index  # 55
    blob rebase handle ref remote  # 56
    remote rebase delta pack index  # 57
    worker window handle merge handle merge index header queue token  # 58
    pack pack blob header branch socket queue cache index  # 59
    buffer branch token blob tree merge token header ref tree  # 60
    buffer window commit parser queue cache ref tree worker remote  # 61
    worker header rebase socket  # 0
    tree token socket cache remote window buffer worker tree queue remote  # 1
    window handle tree window handle merge socket worker worker pack  # 2
    window merge window pack queue delta ref ref commit  # 3
    parser remote cache header rebase merge commit merge queue queue ref  # 4
