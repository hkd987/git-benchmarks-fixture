    blob blob index commit socket  # 0
    worker ref socket index queue token delta delta merge  # 1
    pack merge queue worker remote window worker merge stream delta remote tree  # 2
    ref buffer branch window queue window ref pack parser  # 3
    handle branch header ref object commit commit delta branch ref rebase  # 4
    tree worker parser commit cache branch tree branch pack blob  # 5
    ref handle stream cache header  # 6
    merge ref remote tree worker header stream ref ref delta ref  # 7
    index rebase queue remote delta cache handle  # 8
    tree worker blob merge stream commit  # 9
    window index tree tree stream blob pack header rebase commit  # 10
    handle tree pack buffer blob delta stream blob branch  # 11
    pack token tree remote ref parser pack  # 12
    remote header branch delta  # 13
    window buffer branch rebase  # 14
    blob commit socket parser object ref header ref token  # 15
    window token queue tree tree  # 16
    tree cache pack pack commit parser header pack tree handle tree ref  # 17
    socket branch blob index index tree  # 18
    queue remote rebase tree header ref branch blob  # 19
    merge merge tree rebase delta object socket merge cache rebase cache  # 20
    parser worker object blob worker token pack delta queue worker pack  # 21
    handle tree header branch parser merge commit  # 22
    blob pack token index commit object socket pack window merge  # 23
    tree socket window object remote index commit pack  # 24
    remote queue rebase pack window socket  # 25
    branch pack handle pack commit tree queue buffer window index branch  # 26
    token delta header socket header object tree tree parser queue  # 27
    cache merge buffer header  # 28
    commit merge remote handle stream socket commit  # 29
    tree tree merge tree stream socket  # 30
    token index handle pack ref  # 31
    commit window commit pack  # 32
    branch tree parser header merge  # 33
    delta queue merge header branch parser header blob socket window branch  # 34
    delta parser index rebase  # 35
    pack remote worker branch remote token  # 36
    socket commit handle window remote branch  # 37
