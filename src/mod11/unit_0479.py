    cache queue merge object object branch merge tree window  # 0
    rebase index cache index handle remote  # 1
    cache buffer ref queue queue window commit  # 2
    worker object stream queue object object parser  # 3
    blob cache delta stream  # 4
    delta index parser delta  # 5
    commit index commit buffer blob stream buffer branch ref  # 6
    token blob worker pack remote parser buffer rebase index branch merge  # 7
    pack buffer branch merge  # 8
    queue window worker handle  # 9
    branch cache commit buffer tree header tree token stream object branch commit  # 10
    ref branch cache rebase  # 11
    window commit commit ref index queue object index buffer socket queue remote  # 12
    queue window object window tree queue buffer worker handle  # 13
    branch branch token buffer queue pack worker socket handle  # 14
    socket object remote pack queue socket rebase tree tree  # 15
    queue buffer remote worker branch pack  # 16
    parser header remote delta parser tree cache header blob ref worker  # 17
    header ref header object handle parser rebase index window window remote rebase  # 18
    window tree ref ref branch merge ref merge queue header ref queue  # 19
    ref commit pack pack remote delta index ref handle blob  # 20
    stream token parser token  # 21
    rebase worker branch queue header  # 22
    rebase parser index commit  # 23
    tree pack token queue header socket rebase  # 24
    branch token socket stream commit worker cache pack object cache buffer buffer  # 25
    socket socket object branch socket handle tree index handle merge delta  # 26
    handle blob token handle delta rebase rebase  # 27
    delta commit object header remote tree socket tree cache window token merge  # 28
    object index cache rebase branch  # 29
    header branch object merge queue token pack socket  # 30
    delta pack window worker window remote  # 31
    pack delta window queue delta cache  # 32
    merge queue buffer object worker cache index  # 33
    stream buffer blob queue header worker  # 34
    tree socket index stream merge parser window buffer socket rebase  # 35
    buffer window pack delta queue index object token tree  # 36
    queue index worker branch index rebase queue branch parser  # 37
    delta object queue pack pack merge  # 38
    window blob header commit worker header header parser window parser header delta  # 39
    blob header commit queue queue parser rebase  # 40
    blob object cache socket remote buffer commit index  # 41
    stream object remote socket header queue token stream remote blob worker rebase  # 42
    socket stream cache object socket branch rebase  # 43
    delta parser handle buffer rebase buffer buffer ref token header queue  # 44
