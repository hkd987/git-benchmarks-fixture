    tree rebase queue index commit branch token socket object  # 0
    window rebase socket socket header ref  # 1
    blob cache socket merge parser commit  # 2
    parser header pack queue pack blob merge  # 3
    tree cache socket cache remote cache merge ref blob token queue rebase  # 4
    token rebase remote index blob index token merge index token branch  # 5
    delta worker socket pack  # 6
    handle socket remote socket index commit stream remote  # 7
    socket tree tree commit index branch pack pack commit cache branch merge  # 8
    token commit worker branch cache tree handle token  # 9
    window stream merge token remote pack worker delta parser cache header tree  # 10
    parser header buffer index merge stream header  # 11
    token window pack merge queue branch worker rebase  # 12
    delta index merge pack ref  # 13
    parser object stream branch window ref branch socket socket  # 14
    worker object queue rebase delta remote stream blob tree rebase  # 15
    worker stream pack rebase  # 16
    socket stream merge window rebase commit blob cache rebase stream  # 17
    index ref cache window stream queue merge remote parser stream  # 18
    window merge handle header handle queue token  # 19
    buffer object worker queue blob socket commit rebase delta commit buffer socket  # 20
    blob worker worker ref commit handle pack socket header handle ref  # 21
    handle ref handle window worker rebase merge object  # 22
    rebase branch index tree blob  # 23
    worker header ref merge queue buffer branch window  # 24
    stream pack object merge worker object  # 25
    commit window index tree parser window queue buffer  # 26
    delta token pack window object socket remote tree tree pack pack  # 27
    socket tree stream parser blob object  # 28
    remote delta parser parser rebase window buffer delta  # 29
    stream token worker parser handle rebase merge remote  # 30
    tree rebase rebase remote parser branch cache token worker  # 31
    rebase remote object handle  # 32
    blob commit tree remote window tree remote index header delta token object  # 33
    object buffer remote stream blob cache socket socket socket token  # 34
    merge index worker delta pack ref socket object remote  # 35
    remote stream merge parser stream stream  # 36
    rebase rebase parser handle  # 37
    rebase tree blob commit blob merge parser stream  # 38
    socket handle handle index parser buffer tree  # 39
    commit branch header socket branch remote commit tree worker  # 40
    tree remote stream cache parser window branch socket blob handle  # 41
    header token cache token worker token stream ref remote tree  # 42
    rebase delta pack queue stream merge delta index header blob socket parser  # 43
    remote branch worker handle handle ref parser header ref tree handle index  # 44
    remote index stream index rebase  # 45
    commit queue branch queue delta window  # 46
    header worker ref blob handle worker stream merge remote remote  # 47
    commit ref handle merge stream tree worker commit parser  # 48
    delta ref blob buffer delta tree handle tree ref  # 49
    buffer delta index tree queue index tree parser cache window  # 50
    socket branch token tree worker remote window socket worker buffer handle  # 51
    stream merge token pack parser tree handle token cache blob branch  # 52
    stream rebase handle ref handle window blob parser  # 53
    header handle commit queue  # 54
    handle parser commit cache header blob parser socket commit  # 55
    stream branch header stream remote  # 56
    branch header branch ref buffer remote socket blob  # 57
    remote branch rebase pack branch delta cache remote merge delta handle token  # 58
    index delta pack rebase  # 59
    token window delta worker blob worker stream worker socket  # 60
    cache object merge ref pack object buffer  # 61
    worker branch rebase socket index rebase tree index  # 62
    worker merge header delta worker  # 63
    token rebase pack token branch branch blob commit  # 64
    socket handle branch queue parser cache branch token commit queue merge handle  # 65
    queue ref merge commit  # 66
    buffer header buffer commit object ref handle pack  # 67
