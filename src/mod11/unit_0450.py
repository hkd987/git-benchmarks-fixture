    delta handle object delta stream ref tree branch buffer  # 0
    socket window remote remote commit rebase token remote branch blob  # 1
    merge object cache rebase commit socket object token window  # 2
    header index header object rebase merge buffer worker cache queue merge  # 3
    stream commit token ref merge  # 4
    stream token remote window merge tree merge  # 5
    rebase blob header rebase parser remote commit window merge stream  # 6
    remote parser ref merge handle cache  # 7
    ref rebase blob queue  # 8
    commit object stream merge pack cache handle cache buffer  # 9
    queue delta window merge remote parser parser index branch handle pack  # 10
    ref stream rebase worker remote remote token  # 11
    cache object queue cache stream  # 12
    stream merge window handle stream queue buffer pack  # 13
    socket window cache handle pack merge token delta object  # 14
    buffer merge index stream cache rebase buffer window queue stream  # 15
    merge handle cache stream ref  # 16
    pack remote stream object buffer branch index socket branch blob  # 17
    blob ref header commit ref window merge  # 18
    delta blob queue pack worker socket commit ref  # 19
    commit header handle buffer cache  # 20
    tree header window window merge  # 21
    cache queue handle blob  # 22
    handle buffer parser handle token handle window queue header rebase tree  # 23
    remote pack tree window queue commit buffer ref handle  # 24
    token buffer token blob branch parser window cache  # 25
    worker rebase commit parser delta window index handle token  # 26
    buffer token index blob merge  # 27
    window object parser branch branch handle stream token handle  # 28
    handle index window handle object blob socket stream parser token socket handle  # 29
    cache buffer queue object ref pack object commit queue delta socket  # 30
    tree stream parser cache commit rebase remote object parser pack commit branch  # 31
    buffer commit socket merge cache buffer  # 32
    blob cache header remote cache delta  # 33
    cache commit rebase branch token branch rebase queue blob worker socket  # 34
    header cache queue window  # 35
    ref socket object commit blob token pack merge stream  # 36
    remote queue ref window commit  # 37
    commit socket delta token branch branch tree  # 38
    token header window queue merge remote commit commit tree cache  # 39
    handle pack object index cache buffer index  # 40
    buffer rebase remote tree merge  # 41
    stream handle buffer merge merge stream commit  # 42
    worker header cache delta window ref window cache  # 43
    cache ref commit cache window worker index object rebase pack token  # 44
    ref stream parser worker merge stream ref worker  # 45
    parser queue cache rebase socket header index socket queue delta header index  # 46
    socket pack parser remote  # 47
    worker object cache window pack parser token  # 48
    rebase buffer rebase pack branch tree delta cache commit token  # 49
    index parser delta buffer parser  # 50
    token pack ref branch tree handle token worker rebase cache queue queue  # 51
    pack worker queue commit  # 52
    header remote delta cache token handle  # 53
    token queue index merge  # 54
    pack index header pack merge commit tree rebase index ref commit  # 55
    parser index buffer queue cache  # 56
    index queue parser object  # 57
    handle window merge socket stream header merge delta stream delta worker  # 58
    blob blob rebase token buffer branch worker tree  # 59
    rebase parser ref branch token commit blob  # 60
    blob object window parser merge object  # 61
    socket handle window branch pack stream ref window index stream  # 62
    header queue index pack cache  # 63
    parser header blob merge branch object parser handle  # 64
    blob stream commit window tree window header remote blob delta  # 65
    merge socket rebase token object merge object commit delta  # 66
    handle parser ref commit cache  # 67
    index delta parser index  # 68
    handle branch cache merge remote  # 69
    pack stream handle handle merge remote  # 70
    handle stream commit rebase  # 71
    blob object buffer queue index delta rebase branch pack index pack commit  # 72
    cache socket commit queue handle object  # 73
    worker blob branch rebase worker commit cache  # 74
    rebase merge parser stream remote remote remote socket delta cache ref  # 75
    pack pack handle cache header ref parser parser header rebase merge branch  # 76
    queue ref blob index  # 77
    object socket parser delta  # 78
    socket worker token ref parser rebase parser queue buffer  # 79
    merge rebase tree parser pack index worker commit  # 80
    branch tree ref handle worker blob branch remote parser  # 81
    window merge commit merge tree ref  # 82
    handle delta object worker remote window branch blob buffer index worker buffer  # 83
    token header remote pack cache handle object window socket header  # 84
    token object index delta buffer object tree parser  # 85
    index buffer object parser queue handle  # 86
    buffer handle queue header stream blob header tree  # 87
