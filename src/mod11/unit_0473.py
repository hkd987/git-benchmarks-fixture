    parser parser worker object socket ref parser header rebase worker index stream  # 0
    rebase token socket merge index ref header cache branch stream rebase handle  # 1
    pack handle queue index cache tree  # 2
    pack cache blob commit  # 3
    rebase pack window remote buffer rebase delta branch delta window merge token  # 4
    rebase object branch header index object blob delta delta blob blob  # 5
    delta remote pack delta parser cache ref merge header tree pack  # 6
    window object delta cache delta rebase header pack parser pack  # 7
    commit delta commit tree tree commit blob stream cache  # 8
    tree window delta pack token parser  # 9
    blob index delta rebase object handle pack cache stream branch buffer  # 10
    header branch socket object socket blob index branch  # 11
    tree header branch remote branch header commit handle ref handle token  # 12
    rebase merge parser stream  # 13
    parser commit pack merge window tree socket  # 14
    rebase blob delta index pack ref pack rebase tree rebase parser  # 15
    tree remote cache socket commit cache cache tree object worker buffer index  # 16
    commit object remote stream socket  # 17
    queue socket commit handle delta  # 18
    pack queue parser ref branch branch  # 19
    branch stream object buffer stream remote commit worker merge tree branch delta  # 20
    rebase queue object rebase blob tree pack object rebase  # 21
    worker branch tree blob remote rebase worker parser  # 22
    index socket index commit handle cache ref  # 23
    worker rebase tree buffer stream cache pack commit commit remote queue  # 24
    buffer stream index blob queue queue remote header delta  # 25
    window window handle commit object window delta parser stream blob merge worker  # 26
    remote blob window ref header rebase commit pack  # 27
    object header header remote pack commit index ref window  # 28
    remote window remote index header rebase object  # 29
    buffer header queue buffer buffer ref rebase branch commit  # 30
    object delta queue index branch delta parser  # 31
    merge ref buffer socket  # 32
    header socket ref merge  # 33
    remote buffer queue stream commit rebase stream branch delta pack  # 34
    remote token tree stream delta cache window tree queue merge  # 35
    rebase delta delta window  # 36
    handle worker object blob  # 37
    pack pack branch ref cache handle pack commit socket  # 38
    object index cache index ref remote commit token stream pack  # 39
    token pack tree stream token  # 40
    tree object object index parser  # 41
    commit remote cache rebase parser index worker merge rebase object delta commit  # 42
    merge header pack handle remote commit ref object ref window cache remote  # 43
    token object tree branch  # 44
    rebase pack merge token pack ref  # 45
    stream delta stream queue worker object parser stream index index pack window  # 46
    queue socket rebase tree rebase handle window  # 47
    index index pack ref tree token cache  # 48
    rebase socket queue window handle cache buffer buffer parser object remote  # 49
    tree merge token remote queue  # 50
    ref index buffer object delta window object  # 51
    queue branch commit handle worker parser header branch ref pack  # 52
    window worker remote tree delta cache socket parser merge pack  # 53
    remote parser delta handle header queue queue queue worker rebase commit merge  # 54
    branch pack commit ref socket header remote  # 55
    index ref token tree  # 56
    remote parser cache buffer tree commit blob index ref  # 57
    queue token merge cache pack  # 58
    socket remote handle socket cache pack object  # 59
    header ref socket tree  # 60
    queue branch remote delta cache worker commit commit object rebase socket socket  # 61
    object object queue buffer cache merge remote worker  # 62
    index branch handle object  # 63
    header tree header cache token index cache parser blob  # 64
    cache token pack remote tree tree parser remote  # 65
    branch pack pack branch ref queue  # 66
    parser object handle queue cache cache  # 67
    blob handle socket worker stream buffer  # 68
    stream header parser delta blob queue pack worker pack blob buffer commit  # 69
    index header delta commit cache stream delta buffer window merge handle delta  # 70
    branch object commit handle queue tree rebase  # 71
    socket buffer buffer parser delta ref socket  # 72
    object parser pack merge remote worker buffer  # 73
    buffer object remote handle stream window header  # 74
    socket branch pack handle window  # 75
    remote merge blob branch remote handle pack ref tree  # 76
    header header commit queue parser parser window window  # 77
    buffer ref delta socket object  # 78
    stream commit header stream  # 79
    branch cache commit index rebase  # 80
    ref window rebase blob commit remote window cache  # 81
    token rebase delta pack blob  # 82
    socket object stream stream parser remote pack rebase ref  # 83
    commit worker blob branch remote  # 84
    commit window socket handle cache pack worker buffer merge cache index  # 85
    socket handle parser tree buffer window pack ref handle index  # 86
    parser window pack remote  # 87
    ref merge header stream  # 88
    object worker merge merge merge commit merge merge window  # 0
    commit parser window header branch pack buffer  # 1
    object delta queue blob blob header delta  # 2
    buffer token ref index buffer branch header tree merge blob ref stream  # 3
    queue queue branch pack window cache rebase  # 4
