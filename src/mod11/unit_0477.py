    merge index parser stream header handle socket worker  # 0
    merge rebase commit tree header  # 1
    worker handle cache index token  # 2
    buffer cache ref worker cache branch header  # 3
    window merge buffer commit index object  # 4
    socket blob object tree window commit parser handle token header socket  # 5
    pack delta delta tree header commit  # 6
    parser merge tree merge ref delta delta object  # 7
    delta header queue handle merge index  # 8
    index cache object worker delta window object parser  # 9
    token socket blob header header queue  # 10
    delta token header handle blob branch window window branch  # 11
    parser remote cache cache  # 12
    rebase object handle delta socket  # 13
    index worker window ref blob merge branch  # 14
    queue delta pack worker token branch  # 15
    socket pack buffer parser token merge parser cache cache socket rebase  # 16
    header merge remote delta index tree object cache  # 17
    handle commit queue commit branch rebase  # 18
    tree queue queue token rebase tree  # 19
    commit merge token cache queue buffer buffer merge  # 20
    index stream header merge merge window parser blob parser pack remote pack  # 21
    worker index blob parser  # 22
    token commit window blob parser commit branch token delta stream  # 23
    index index stream branch parser merge object index remote rebase object  # 24
    commit buffer parser window object token header queue worker handle handle  # 25
    handle index worker ref commit rebase object  # 26
    window rebase remote token parser branch handle socket delta blob ref token  # 27
    delta queue blob header blob remote worker socket  # 28
    worker delta token delta merge blob rebase merge stream socket  # 29
    window commit pack commit remote parser header  # 30
    header handle socket queue header commit pack commit delta header blob  # 31
    blob branch queue delta queue blob token  # 32
    queue pack tree delta branch commit queue merge commit socket  # 33
    worker object pack object cache merge branch stream queue  # 34
    object rebase blob remote delta token header buffer cache cache ref  # 35
    branch object buffer commit blob ref window cache object  # 36
    pack header index tree remote delta worker socket pack delta  # 37
    blob object queue tree  # 38
    parser window socket header object remote token remote  # 39
    pack worker blob header handle socket socket buffer object  # 40
    cache index rebase token window  # 41
    token pack object remote remote header merge  # 42
    header queue pack ref  # 43
    tree worker queue delta parser queue header delta pack merge  # 44
    handle queue index tree tree stream token ref parser branch buffer branch  # 45
    worker branch commit remote ref socket remote object merge object branch  # 46
    object token commit tree remote object object  # 47
    delta window socket buffer pack cache handle cache window  # 48
    worker handle worker stream handle index  # 49
    header cache rebase parser ref branch delta stream cache remote pack delta  # 50
    socket index blob token merge buffer tree stream  # 51
    pack socket object object  # 52
    commit rebase object branch branch  # 53
    parser token queue index  # 54
    tree blob worker commit handle merge  # 55
    delta commit handle socket buffer branch buffer object header index ref  # 56
    handle worker worker header handle token delta blob  # 57
    header delta ref stream parser worker window stream parser stream  # 58
    remote queue rebase window window index pack  # 59
    index branch blob handle blob object window commit cache stream queue  # 60
    pack object queue worker branch tree worker merge branch header handle delta  # 61
    commit cache commit tree delta ref header parser merge  # 62
    blob stream cache socket  # 63
    parser blob tree object worker worker window delta parser commit  # 64
    header queue header object buffer index cache delta pack  # 65
    cache commit cache header socket merge  # 66
    worker parser queue branch pack tree tree handle  # 67
    cache branch object remote index branch index branch index stream tree  # 68
    branch remote window header delta  # 69
    token parser cache buffer header stream tree remote window  # 70
    worker pack blob pack commit stream buffer queue buffer  # 71
    branch queue blob pack worker  # 72
    tree ref commit object blob socket  # 73
    index branch header parser cache delta token tree commit handle  # 74
    commit delta queue remote stream queue worker index ref cache tree  # 75
    merge window token branch object  # 76
    pack tree merge remote socket commit  # 77
    queue token object parser rebase buffer remote object header socket pack pack  # 0
    token blob remote queue  # 1
    window handle handle merge remote stream object delta token  # 2
    branch handle merge branch pack cache window  # 3
    buffer commit header delta delta  # 4
    token worker delta buffer handle window tree buffer rebase  # 5
    socket worker tree ref queue stream remote object buffer  # 6
