    handle window object rebase  # 0
    cache handle remote merge socket cache object cache merge  # 1
    delta merge queue handle branch  # 2
    remote token ref commit commit remote ref merge blob blob  # 3
    object header branch header commit ref parser rebase merge window parser token  # 4
    window parser index cache token parser ref tree index index worker delta  # 5
    merge ref object stream commit rebase token index token  # 6
    token tree header object branch buffer branch pack merge  # 7
    handle branch cache branch remote merge blob socket index  # 8
    parser merge worker window worker worker branch  # 9
    rebase blob pack merge token commit index window queue commit  # 10
    branch parser tree ref worker  # 11
    rebase worker stream commit buffer parser  # 12
    stream remote pack handle header tree parser branch commit  # 13
    token ref ref queue cache tree stream token tree remote ref parser  # 14
    buffer worker header remote socket queue  # 15
    handle socket ref worker parser worker parser window token cache buffer tree  # 16
    window worker delta branch ref  # 17
    remote object tree remote commit blob  # 18
    index pack queue ref header stream  # 19
    worker buffer branch object index ref parser stream stream  # 20
    handle merge pack queue  # 21
    commit pack window handle rebase blob blob queue window branch header  # 22
    merge stream queue pack index worker  # 23
    token worker window cache queue merge commit handle handle  # 24
    worker object pack token parser worker  # 25
    blob socket index header buffer window remote merge index index window  # 26
    buffer token queue delta commit cache merge token branch queue delta cache  # 27
    tree queue stream rebase commit commit pack ref  # 28
    index cache cache queue branch blob merge blob merge buffer socket delta  # 29
    commit worker ref tree merge merge commit blob parser ref  # 30
    pack window remote tree commit  # 31
    ref rebase rebase queue index rebase window blob blob cache tree index  # 32
    object delta commit header window queue stream index tree blob  # 33
    remote cache index merge merge commit merge buffer object tree buffer commit  # 34
    rebase delta parser worker parser  # 35
    pack index socket blob ref cache header worker pack parser merge parser  # 36
    branch branch queue blob cache token  # 37
    object delta pack token socket branch tree buffer queue  # 38
    ref stream header handle delta socket window index window  # 39
    stream handle stream pack object  # 40
    blob commit delta stream rebase merge remote commit rebase commit merge handle  # 0
    stream branch commit pack stream worker commit index object  # 1
    queue token commit merge commit ref  # 2
    header remote ref remote commit queue worker socket  # 3
    tree stream tree token blob object remote merge token delta  # 4
    buffer socket remote socket stream  # 5
    rebase branch queue merge parser buffer window parser handle  # 6
    socket header rebase token worker ref  # 7
    queue delta buffer worker worker socket index cache merge socket delta  # 8
    handle blob socket stream blob commit worker remote buffer rebase buffer  # 9
    tree socket window parser delta object  # 10
    worker stream stream pack worker cache worker window  # 11
    remote token token object worker delta blob  # 12
