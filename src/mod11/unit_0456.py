    tree remote ref token merge buffer stream cache socket queue stream  # 0
    pack token delta merge token worker  # 1
    rebase index queue merge delta  # 2
    object token pack queue handle rebase cache  # 3
    tree handle stream blob  # 4
    index parser header pack branch stream pack  # 5
    commit index handle remote blob merge handle object remote parser branch  # 6
    socket stream blob delta socket parser cache index index rebase  # 7
    token object pack remote handle tree token object handle object ref token  # 8
    blob cache delta ref socket  # 9
    stream window buffer queue object queue  # 10
    handle stream tree header pack branch token blob parser  # 11
    index parser index blob ref queue index branch blob rebase  # 12
    ref token object window blob buffer blob commit delta ref token  # 13
    window merge handle tree queue delta stream buffer header handle merge buffer  # 14
    queue merge token token cache header  # 15
    buffer buffer header handle buffer ref delta tree handle  # 16
    tree stream stream token buffer rebase window merge handle index socket  # 17
    merge ref window pack buffer tree tree ref header  # 18
    stream parser rebase token handle token stream  # 19
    window branch buffer index merge index merge delta queue  # 20
    parser stream handle ref ref socket  # 21
    blob commit parser tree header merge merge  # 22
    blob parser worker cache object blob branch object merge  # 23
    pack ref object ref socket header  # 24
    handle index object handle remote cache rebase  # 25
    header delta delta cache header rebase remote token object tree object  # 26
    commit pack queue commit object branch delta index commit  # 27
    handle object branch rebase socket blob pack buffer branch handle  # 28
    index commit stream worker merge branch index ref  # 29
    socket rebase tree tree  # 30
    handle parser worker object remote pack queue handle parser commit queue  # 31
    blob cache window cache branch  # 32
    buffer index rebase remote ref handle handle ref  # 33
    tree token stream stream merge window tree stream handle ref  # 34
    ref buffer ref object handle pack  # 35
    blob merge tree window index branch socket ref parser rebase index  # 36
    worker delta parser object  # 37
    merge window merge object ref remote stream window delta branch tree handle  # 38
    object object merge pack pack object blob remote window  # 39
    stream blob blob handle window worker delta index handle token  # 40
    delta worker token merge queue worker socket window blob object worker  # 41
    queue parser tree parser merge tree window remote rebase ref queue object  # 42
    object ref tree socket rebase stream merge index delta socket delta blob  # 43
    branch blob branch window window blob object ref token  # 44
    remote socket window object parser branch branch socket  # 45
    index header remote header rebase rebase window stream socket commit stream  # 46
    object window pack stream commit pack socket delta index  # 47
    pack merge stream commit pack remote object pack merge parser pack blob  # 48
    token stream socket buffer branch delta cache object branch pack  # 49
    delta window pack worker index window  # 50
    ref buffer delta branch token stream stream buffer pack stream worker  # 51
    object cache queue remote rebase stream parser cache pack blob blob parser  # 52
    merge index delta queue header  # 53
    stream tree rebase blob branch queue stream stream parser commit remote buffer  # 54
    token token remote stream rebase buffer delta object  # 55
    branch branch tree socket token parser pack delta ref  # 56
    header window tree buffer  # 57
    socket tree index tree buffer pack blob ref cache ref  # 58
    rebase branch cache branch header  # 59
    parser merge stream stream object header remote delta window ref parser token  # 60
    blob delta merge delta commit buffer pack  # 61
    blob pack parser window  # 62
    socket tree blob token index buffer parser commit socket  # 63
    header ref commit remote tree cache pack cache worker socket socket  # 64
    tree remote index remote handle blob socket  # 65
    tree worker buffer buffer delta pack stream object  # 66
    token cache object rebase  # 67
    cache ref socket ref ref tree worker  # 68
    rebase worker cache parser ref index branch stream index branch  # 69
    ref blob token remote window token token handle handle merge object parser  # 70
    window branch commit ref header ref  # 71
    handle handle handle object cache rebase remote socket object cache socket  # 72
    tree tree handle queue  # 73
