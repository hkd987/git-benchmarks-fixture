    parser queue rebase ref socket stream merge buffer  # 0
    worker tree rebase handle blob  # 1
    header queue merge remote pack ref socket branch header cache index commit  # 2
    stream blob branch branch merge commit index rebase  # 3
    merge worker window delta worker object  # 4
    cache rebase worker remote window pack pack cache remote  # 5
    tree token parser buffer branch parser merge  # 6
    handle socket handle token index header header buffer branch queue  # 7
    merge object object token rebase object  # 8
    rebase remote pack ref socket buffer pack buffer worker  # 9
    remote handle token object pack delta  # 10
    blob object remote merge worker object ref header parser worker handle  # 11
    rebase index pack object commit  # 12
    header remote blob branch object header  # 13
    rebase cache commit parser pack token pack delta worker  # 14
    queue handle handle cache  # 15
    handle tree stream object tree queue blob window cache cache  # 16
    tree object object index buffer merge object delta queue blob pack index  # 17
    rebase buffer blob worker commit buffer rebase remote commit remote branch stream  # 18
    remote index index header  # 19
    rebase worker cache commit parser ref stream window cache worker token  # 20
    parser object queue stream buffer window remote  # 21
    worker pack queue header ref handle  # 22
    rebase tree delta socket index commit merge pack object buffer  # 23
    blob socket pack handle commit blob parser  # 24
    handle socket cache object index queue remote buffer ref token  # 25
    rebase delta pack rebase buffer buffer  # 26
    worker tree branch rebase worker stream commit pack token commit  # 27
    tree blob socket worker buffer socket merge commit  # 28
    socket branch commit commit branch token buffer window  # 29
    index rebase branch window cache handle merge handle stream commit rebase commit  # 30
    ref tree branch merge cache merge object buffer pack object buffer merge  # 31
    socket socket branch parser object ref  # 32
    commit header pack stream  # 33
    socket token ref index delta tree stream commit  # 34
    header rebase handle stream  # 35
    ref header handle ref parser ref blob socket cache handle  # 36
    commit window socket stream stream pack ref pack token branch handle branch  # 37
    buffer object branch buffer rebase parser object blob delta  # 38
    window rebase window branch token stream  # 39
    pack parser queue rebase delta token rebase  # 40
    branch merge parser remote delta cache rebase  # 41
    token window rebase index  # 42
    cache delta rebase socket object  # 43
    cache parser pack branch index  # 44
    token blob tree parser  # 45
    buffer remote token parser  # 46
    rebase parser socket merge rebase merge rebase tree handle blob  # 47
    commit object buffer remote socket cache buffer worker tree window  # 48
    queue pack stream object cache remote ref branch tree stream  # 49
    parser queue worker delta header cache buffer stream  # 50
    stream ref cache header socket remote  # 51
    index header window remote delta branch rebase rebase handle  # 52
    object tree commit remote blob pack object merge queue branch buffer worker  # 53
    stream object buffer stream pack  # 54
    ref worker blob queue  # 55
    worker handle blob object  # 56
    header header queue socket rebase worker cache pack object  # 57
    queue commit commit stream  # 58
    object worker window branch worker buffer  # 59
    window socket commit token tree queue tree delta branch socket  # 60
    tree tree parser cache object index delta object header tree object  # 61
    merge branch remote socket index handle remote index branch window header header  # 62
    stream header rebase blob cache buffer buffer commit pack delta queue  # 63
    handle commit branch stream object cache queue handle  # 64
    buffer remote branch object  # 65
