    object commit merge object socket parser token remote delta ref pack commit  # 0
    pack pack object buffer stream remote commit rebase  # 1
    handle blob ref buffer index  # 2
    cache pack buffer buffer window remote delta stream branch worker window queue  # 3
    queue remote object index tree remote blob stream merge stream object handle  # 4
    rebase queue queue delta merge blob header worker remote rebase window  # 5
    parser object cache parser queue  # 6
    remote header socket worker queue  # 7
    delta remote remote cache cache commit delta remote buffer  # 8
    remote buffer object cache branch branch parser  # 9
    merge token socket queue window tree commit blob index tree  # 10
    parser worker rebase stream queue socket merge handle commit socket delta  # 11
    rebase header parser merge socket remote blob  # 12
    pack pack object window stream pack token buffer rebase token handle  # 13
    tree tree pack window ref tree queue socket stream window rebase tree  # 14
    queue tree worker branch cache branch delta parser delta buffer worker buffer  # 15
    handle queue commit delta tree ref index buffer  # 16
    remote socket header ref stream token index queue  # 17
    worker ref socket worker pack delta delta  # 18
    header token branch commit  # 19
    remote worker handle cache blob ref commit queue branch  # 20
    token pack pack delta header buffer stream blob tree worker object parser  # 21
    parser header cache socket socket merge socket queue ref  # 22
    window commit stream blob worker  # 23
    branch handle token branch cache cache tree blob commit  # 24
    object commit socket queue tree rebase rebase ref cache index window merge  # 25
    index socket merge parser remote worker rebase tree cache  # 26
    parser index pack token pack  # 27
    merge blob merge commit delta token merge header queue  # 28
    remote token socket token cache parser  # 29
    pack pack object cache token parser token socket remote parser cache stream  # 30
    object cache tree socket tree token commit buffer  # 31
    pack queue rebase buffer remote socket delta  # 32
    queue queue commit branch  # 33
    commit commit token header  # 34
    header rebase stream handle cache cache tree queue  # 35
    blob tree blob header commit pack queue delta header  # 36
    handle branch branch worker merge rebase branch window merge buffer window  # 37
    merge tree header tree socket handle stream  # 38
    cache branch commit blob handle ref token window branch handle merge  # 39
    blob socket tree blob blob cache stream tree tree buffer index  # 40
    queue socket index pack  # 41
    worker buffer branch rebase token tree ref buffer branch rebase index  # 42
    blob buffer queue remote buffer object object  # 43
    socket rebase rebase buffer commit blob remote remote window remote index socket  # 44
    rebase parser parser merge merge cache  # 45
    handle handle commit remote buffer delta remote merge token  # 46
    branch worker socket object commit  # 47
    socket window blob header ref  # 48
    socket stream remote stream  # 49
    branch remote blob header index rebase stream socket worker merge index  # 50
    remote token cache buffer queue object worker stream branch delta object  # 51
    blob index buffer queue ref window branch header cache rebase pack  # 52
    blob socket branch object window tree pack header  # 53
    branch commit commit cache remote handle ref  # 54
    buffer socket worker commit merge queue queue ref merge socket worker queue  # 55
    delta rebase handle rebase cache delta window tree  # 56
    commit blob object worker queue queue ref branch object remote merge  # 57
    blob remote tree socket delta blob buffer remote branch delta ref  # 58
