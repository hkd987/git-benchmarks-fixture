    merge socket index object handle token  # 0
    tree commit merge index blob  # 1
    index index ref queue header  # 2
    delta buffer object socket blob pack delta cache index object  # 3
    ref token ref rebase handle  # 4
    remote handle queue queue handle rebase tree cache index stream  # 5
    cache merge remote worker remote index  # 6
    queue delta index commit ref header worker  # 7
    socket cache index window pack blob token queue handle remote  # 8
    merge rebase token token object remote pack tree object worker rebase  # 9
    stream remote tree blob merge  # 10
    cache header window remote  # 11
    worker index token blob ref header delta rebase blob window merge ref  # 12
    pack commit queue window handle header window stream branch stream  # 13
    cache worker socket pack blob token queue socket merge object  # 14
    commit commit object queue  # 15
    socket blob object object branch merge pack ref handle socket window  # 16
    branch worker stream handle branch ref branch  # 17
    worker header buffer pack  # 18
    queue window parser window blob  # 19
    handle tree stream socket index rebase object  # 20
    cache commit commit window  # 21
    branch parser remote ref merge  # 22
    queue socket token header commit parser branch commit branch handle remote  # 23
    merge header header index blob  # 24
    window commit ref stream pack branch buffer parser delta ref  # 25
    object header index worker rebase header pack  # 26
    stream buffer delta stream token branch rebase buffer stream rebase remote  # 27
    ref pack queue buffer handle branch delta window commit socket  # 28
    ref window index stream header object merge worker buffer tree parser pack  # 29
    rebase stream header handle blob commit ref pack buffer object object buffer  # 30
    branch handle token stream tree window remote  # 31
    cache cache object rebase  # 32
    remote window window queue commit object tree  # 33
    parser object delta remote blob commit header  # 34
    tree merge index cache blob tree ref cache queue cache window commit  # 35
    worker commit worker object cache tree queue delta handle header delta cache  # 36
    branch object commit pack index branch rebase tree object pack delta header  # 37
    queue parser handle rebase index cache token ref blob branch worker  # 38
    blob delta delta stream tree worker token delta commit parser index  # 39
    object socket socket queue merge stream  # 40
    pack window ref cache  # 0
    window pack queue object merge  # 1
    tree index merge handle branch remote rebase header header object  # 2
    object blob tree socket worker  # 3
    rebase commit ref handle tree object object merge ref  # 4
    delta header queue worker window handle  # 5
    pack commit queue queue queue object window branch rebase  # 6
    queue socket object delta socket merge blob  # 7
    index stream branch tree blob stream queue parser  # 8
    stream buffer cache commit ref token header merge stream  # 9
    object index queue buffer cache queue  # 10
    header handle cache pack socket ref delta tree branch  # 11
    remote cache ref remote blob socket index rebase  # 12
