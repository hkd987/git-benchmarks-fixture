    remote delta blob pack  # 0
    tree rebase handle remote stream  # 1
    blob rebase parser worker blob buffer stream  # 2
    cache stream ref object blob object delta parser remote  # 3
    tree ref pack branch  # 4
    token object index handle index handle index rebase  # 5
    socket window handle queue pack  # 6
    socket merge merge rebase object parser ref  # 7
    parser stream blob delta header delta rebase parser rebase queue commit pack  # 8
    window cache object merge  # 9
    ref handle stream delta rebase commit commit  # 10
    commit socket socket header  # 11
    buffer remote header commit worker worker commit remote queue stream  # 12
    tree queue rebase worker  # 13
    parser merge socket commit branch blob tree index handle remote queue  # 14
    buffer socket delta merge header branch ref queue worker stream  # 15
    ref buffer branch worker  # 16
    socket object handle blob branch merge token blob blob cache buffer blob  # 17
    queue pack token parser parser  # 18
    tree window parser ref buffer socket  # 19
    remote merge tree buffer branch worker blob  # 20
    rebase branch remote tree parser queue pack header remote merge  # 21
    rebase token worker cache object buffer rebase worker object socket header  # 22
    index header remote remote  # 23
    queue window token stream merge token pack header header  # 24
    blob merge remote cache handle tree ref parser stream handle parser  # 25
    queue header pack window merge token delta object  # 26
    remote header queue branch stream stream handle handle rebase  # 27
    blob pack socket pack header  # 28
    header token tree pack branch pack delta remote remote merge  # 29
    header index worker worker token blob  # 30
    pack commit ref delta queue branch blob object token  # 31
    remote ref stream object blob merge ref delta delta  # 32
    index stream pack token merge ref queue object stream pack parser  # 33
    buffer commit remote pack  # 34
    object tree header token worker delta blob handle  # 35
    pack stream buffer delta stream  # 36
    rebase worker branch object remote rebase stream remote token ref  # 37
    parser token handle worker window  # 38
    queue stream merge handle merge tree  # 39
    socket buffer window buffer buffer stream handle  # 40
    buffer window delta index ref branch rebase stream  # 41
    remote token delta blob buffer  # 42
    tree window stream pack queue parser  # 43
    rebase cache branch delta  # 44
    remote commit parser merge socket merge stream cache remote stream  # 45
    object parser blob window header tree rebase  # 46
    socket merge token rebase window ref queue rebase tree object  # 47
    remote blob queue merge header tree remote socket  # 48
    header stream merge delta ref branch index tree branch blob commit  # 49
    blob parser socket blob delta  # 50
    branch token window commit merge branch rebase rebase pack handle token  # 51
    tree buffer blob index  # 52
    worker remote merge pack  # 53
    parser stream tree delta stream delta token branch pack object  # 54
    socket header tree header queue remote commit merge  # 55
    parser merge queue stream merge token blob  # 56
    rebase branch buffer token buffer tree stream rebase delta socket ref tree  # 57
