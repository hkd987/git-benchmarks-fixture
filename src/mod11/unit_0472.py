    commit socket cache pack buffer cache  # 0
    token rebase tree remote branch worker cache branch remote parser socket branch  # 1
    branch parser blob header ref delta handle  # 2
    ref rebase worker index window  # 3
    cache window token blob  # 4
    parser parser buffer index  # 5
    index blob parser socket  # 6
    token token parser window parser pack delta  # 7
    rebase header socket header ref index token window token cache  # 8
    commit stream buffer blob queue queue  # 9
    parser pack remote cache socket rebase queue buffer  # 10
    commit worker delta pack handle handle pack queue header  # 11
    commit worker ref window window object window delta  # 12
    branch worker object object object queue  # 13
    blob rebase remote window merge rebase window index  # 14
    queue index delta pack branch header token worker  # 15
    worker pack merge delta handle delta queue index stream  # 16
    worker socket buffer token stream  # 17
    handle worker header worker branch parser branch rebase socket  # 18
    window token parser merge  # 19
    ref queue parser handle tree buffer  # 20
    blob commit merge merge  # 21
    buffer blob delta delta  # 22
    merge index pack pack token worker queue  # 23
    ref delta header stream commit object  # 24
    commit index socket object worker rebase pack blob parser stream  # 25
    ref rebase buffer index buffer object queue handle parser queue rebase  # 26
    blob buffer cache buffer header ref queue buffer window  # 27
    merge tree ref buffer parser index remote  # 28
    rebase rebase buffer stream parser  # 29
    ref remote stream buffer merge commit worker  # 30
    object handle commit delta  # 31
    commit header window socket remote tree rebase blob socket remote tree header  # 32
    tree token remote window handle tree index queue token stream buffer commit  # 33
    pack cache delta blob  # 34
    handle delta ref stream rebase tree merge stream blob queue  # 35
    rebase tree pack buffer index  # 36
    branch parser merge commit commit queue commit header pack rebase  # 37
    tree remote header header socket handle  # 38
    handle tree header commit pack token handle rebase commit blob  # 39
    tree cache handle window remote index  # 40
    queue index cache queue delta  # 41
    buffer object rebase socket queue queue object  # 42
    index buffer rebase socket parser queue buffer header token rebase delta blob  # 43
    window merge header merge handle handle merge delta socket  # 44
    tree header buffer delta worker remote  # 45
    branch cache branch parser ref blob rebase buffer commit  # 46
    object handle tree pack remote object cache buffer queue delta parser blob  # 47
    branch commit rebase handle object cache  # 48
    remote branch index parser tree  # 49
    queue index window delta merge delta token  # 50
    remote stream remote buffer pack cache delta socket rebase rebase rebase  # 51
    queue delta ref window  # 52
    pack queue stream merge token handle object stream remote index token merge  # 53
    rebase branch branch merge blob rebase commit worker token  # 54
    socket stream parser tree parser handle socket header queue merge buffer rebase  # 55
    tree branch parser pack ref token token cache merge  # 56
    parser token socket parser delta tree header tree commit rebase blob  # 57
    queue delta worker worker commit pack socket cache delta  # 58
    remote header parser buffer parser  # 59
    merge rebase object branch cache stream merge pack ref  # 60
    branch index header worker blob worker token  # 61
    window delta index socket index stream cache merge object blob stream  # 62
    handle ref commit parser window worker ref handle worker cache commit  # 63
    window object socket object  # 64
    socket remote buffer stream tree worker tree parser  # 65
    token pack blob token header cache  # 66
    merge stream stream socket pack merge stream remote merge window remote window  # 67
    object parser branch stream delta index blob  # 68
    rebase cache pack cache ref object commit tree tree cache  # 69
    buffer branch cache merge  # 70
    delta buffer queue object delta tree  # 71
    branch branch socket delta blob buffer handle token handle pack token  # 72
    handle delta remote blob worker  # 73
    window window header queue delta cache  # 74
    pack cache cache handle cache remote remote rebase commit header  # 75
    blob commit ref token parser object buffer object  # 76
    stream merge window buffer header window blob parser worker pack  # 77
    tree object window delta rebase window rebase tree tree cache  # 78
    pack index parser index socket merge delta worker pack  # 79
    handle worker tree worker  # 80
