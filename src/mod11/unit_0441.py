    window rebase ref index merge  # 0
    remote window window merge worker window  # 1
    queue tree branch token ref token worker parser merge token  # 2
    index buffer branch stream worker parser branch ref  # 3
    cache delta cache object worker pack ref cache blob  # 4
    parser window window ref token header  # 5
    stream token window token queue header delta index  # 6
    handle queue commit ref token ref  # 7
    ref worker branch branch tree blob pack blob handle socket  # 8
    window merge delta commit ref commit socket  # 9
    buffer stream token commit header branch token worker handle header stream  # 10
    branch blob rebase merge ref socket pack socket header buffer worker  # 11
    commit stream index merge remote queue socket socket  # 12
    index buffer blob blob branch  # 13
    branch index socket stream merge merge  # 14
    token queue cache window socket window tree tree delta index handle header  # 15
    merge branch stream buffer handle queue merge ref ref buffer merge  # 16
    token remote branch ref window token worker parser handle socket  # 17
    window merge token stream  # 18
    cache token commit ref blob index header  # 19
    tree worker index remote handle merge branch commit branch branch ref  # 20
    index parser stream handle object buffer window pack parser stream window  # 21
    window window socket rebase worker  # 22
    commit token handle cache branch pack parser  # 23
    buffer index tree commit socket index socket  # 24
    socket branch token buffer handle delta remote branch buffer  # 25
    delta window tree buffer object  # 26
    pack branch pack pack  # 27
    header token buffer parser object merge  # 28
    commit ref stream merge header  # 29
    tree delta rebase socket branch delta worker object token header  # 30
    ref socket parser commit  # 31
    tree ref tree token pack tree pack handle  # 32
    handle parser remote cache object queue worker queue pack window index  # 33
    object ref rebase object merge socket  # 34
    pack rebase delta handle buffer ref ref parser socket parser commit branch  # 35
    object rebase queue blob token worker window index branch queue  # 36
    token remote parser socket rebase header index rebase  # 37
