    window branch token queue handle rebase  # 0
    object rebase tree commit object  # 1
    window token queue branch ref queue blob merge window  # 2
    ref commit buffer pack  # 3
    object header object pack parser branch branch  # 4
    remote ref worker ref delta cache header object blob remote  # 5
    object token queue remote socket commit header pack  # 6
    commit object branch handle object buffer  # 7
    worker merge commit index socket rebase rebase  # 8
    pack index stream index rebase blob blob stream ref commit blob parser  # 9
    window window pack socket worker tree index tree  # 10
    header index merge index worker header stream branch rebase handle header stream  # 11
    tree object rebase remote merge remote delta  # 12
    buffer buffer ref handle  # 13
    parser object handle tree branch parser stream  # 14
    queue index rebase window queue socket branch window header object token pack  # 15
    index commit socket socket worker header  # 16
    pack merge delta ref  # 17
    worker socket merge blob ref worker handle worker parser  # 18
    tree pack stream delta  # 19
    ref pack tree remote  # 20
    pack cache queue token worker buffer rebase object handle delta  # 21
    blob blob delta tree buffer blob header index queue handle index pack  # 22
    token stream rebase rebase stream handle ref pack worker pack index rebase  # 23
    window index pack worker object handle branch queue delta cache  # 24
    pack handle ref buffer branch cache commit branch index  # 25
    delta remote cache rebase delta merge ref queue buffer merge  # 26
    remote merge remote window  # 27
    parser delta pack rebase ref  # 28
    header index worker tree ref window delta stream cache object worker tree  # 29
    branch token delta delta commit stream  # 30
    index tree branch index parser ref  # 31
    ref cache window index parser buffer worker parser merge object header  # 32
    socket parser branch queue queue header object  # 33
    object pack blob cache ref socket header merge queue cache token  # 0
    window buffer pack handle commit token rebase tree  # 1
    buffer token queue ref delta blob window socket  # 2
    handle window socket worker delta worker window blob window rebase queue  # 0
    merge branch ref parser  # 1
    blob buffer header worker ref cache remote remote index blob cache buffer  # 2
    cache window tree worker token window socket rebase  # 3
    handle merge token object object queue worker index buffer parser stream  # 4
    index cache cache merge ref buffer worker window tree ref buffer commit  # 5
    queue token delta commit rebase cache socket blob buffer rebase ref  # 6
    index rebase cache stream merge branch  # 7
    object rebase blob buffer index object  # 8
    window stream tree cache object branch stream cache handle worker blob stream  # 9
    handle commit queue rebase handle object buffer handle index  # 10
    index index merge merge worker pack object buffer  # 11
    branch token token branch pack buffer stream queue buffer buffer remote window  # 12
