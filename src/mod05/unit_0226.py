    blob queue parser queue parser queue index buffer stream  # 0
    handle socket parser stream ref blob token index  # 1
    queue socket merge merge handle stream header ref  # 2
    index queue worker handle tree blob header object object pack header  # 3
    worker token queue tree rebase socket index  # 4
    parser socket delta cache worker pack header  # 5
    rebase delta parser remote window queue pack pack object commit delta  # 6
    blob branch index window socket stream  # 7
    index token cache socket blob parser token tree handle tree blob ref  # 8
    buffer stream rebase rebase handle ref stream branch ref cache index handle  # 9
    window queue parser socket merge buffer buffer pack handle token rebase queue  # 10
    blob remote worker delta rebase queue merge cache buffer  # 11
    buffer branch remote header token buffer index  # 12
    handle pack parser header window blob rebase worker  # 13
    header rebase delta handle merge index parser index token  # 14
    stream pack worker parser commit stream blob  # 15
    delta token remote token branch header parser object object token rebase  # 16
    tree stream blob worker handle blob commit  # 17
    object delta branch header buffer  # 18
    object header header merge handle index  # 19
    buffer ref buffer index delta delta  # 20
    cache object index blob  # 21
    merge blob remote socket worker buffer  # 22
    token remote branch delta remote object queue token socket window worker  # 23
    header stream ref worker handle queue ref  # 24
    handle tree socket object stream  # 25
    cache remote stream header remote tree  # 26
    object parser worker queue handle queue delta tree cache merge delta  # 27
    window merge header merge parser token object merge token token socket  # 28
    object header tree token  # 29
    token remote branch delta socket rebase handle window pack parser buffer object  # 30
    handle branch pack header  # 31
    handle remote commit rebase tree socket  # 32
    rebase handle blob branch  # 33
    queue commit cache cache parser index  # 34
    ref worker stream window tree tree handle blob  # 35
    handle blob stream parser worker  # 36
    commit remote ref buffer delta rebase remote delta handle token  # 37
    header buffer parser socket handle delta commit delta  # 38
    object pack cache stream delta worker ref  # 39
    window index index commit  # 40
    commit parser token object tree buffer remote blob object ref commit  # 41
    object delta pack blob ref object queue window delta socket handle socket  # 42
    rebase worker parser blob  # 43
    pack index queue queue  # 44
    merge rebase token header stream  # 45
    worker index stream handle branch remote object socket parser  # 46
    parser window merge header window queue buffer rebase  # 47
    tree delta window object index window worker header  # 48
    object stream handle handle delta worker blob socket  # 49
    tree merge remote window index  # 50
    ref worker tree worker buffer header queue window remote  # 51
    pack header stream pack index handle socket object stream window ref  # 52
    queue worker header delta cache ref token remote merge pack  # 53
    header remote token merge worker window object branch worker  # 54
    blob merge rebase merge token tree object header token worker parser  # 55
    index stream commit cache delta delta index handle window  # 56
    delta ref socket tree ref blob token header  # 57
    socket delta blob delta  # 58
    queue buffer ref handle header header token token merge index  # 59
    ref window remote cache handle queue worker rebase index stream  # 60
    worker socket merge branch object buffer parser remote  # 61
    blob index handle ref cache window index delta index parser  # 62
    buffer pack ref ref token remote token window handle  # 63
    token header stream index commit  # 64
    buffer parser cache ref  # 65
    merge buffer worker buffer socket tree socket index blob  # 66
    buffer rebase pack stream worker handle socket commit rebase  # 67
    ref delta window delta rebase pack  # 68
    socket socket worker window branch queue worker socket worker tree index  # 69
    blob tree tree queue merge parser ref branch  # 70
    ref handle cache parser delta parser blob  # 71
    cache tree token rebase rebase branch delta  # 72
    buffer pack rebase socket object  # 73
