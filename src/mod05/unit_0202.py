    ref index socket merge commit stream commit worker  # 0
    branch window token branch  # 1
    token ref blob parser  # 2
    tree token queue blob branch  # 3
    merge header merge tree  # 4
    rebase window queue object stream parser queue token parser  # 5
    merge queue pack parser merge merge rebase merge rebase  # 6
    rebase tree token rebase window  # 7
    ref commit pack window buffer delta object  # 8
    remote object object delta ref merge blob  # 9
    header worker rebase blob merge rebase blob window socket socket  # 10
    ref buffer ref blob parser token token  # 11
    buffer merge stream commit queue socket buffer window socket  # 12
    queue queue object merge tree queue buffer token merge remote worker stream  # 13
    merge blob parser cache parser object remote token cache rebase  # 14
    branch buffer parser rebase branch remote blob  # 15
    tree token commit delta worker header  # 16
    merge socket token socket  # 17
    handle blob remote object object  # 18
    window merge pack commit  # 19
    worker delta object header parser  # 20
    token ref header delta stream buffer buffer blob blob rebase window  # 21
    queue rebase token buffer merge window index  # 22
    buffer buffer ref commit queue cache parser index pack blob buffer  # 23
    socket stream cache stream  # 24
    header header ref rebase tree parser header commit parser stream queue  # 25
    socket branch object socket header socket index  # 26
    branch merge socket rebase cache  # 27
    remote socket ref buffer buffer cache token window delta worker cache  # 28
    delta merge commit socket queue pack remote socket index  # 29
    window stream object window tree ref  # 30
    window index branch branch window queue header object stream  # 31
    cache buffer cache branch  # 32
    object buffer commit header parser merge remote ref branch branch rebase  # 33
    rebase tree blob merge merge ref stream remote socket  # 34
    index handle index parser  # 35
    tree merge commit object handle queue blob  # 36
    index pack worker branch ref merge handle remote socket merge  # 37
    stream blob rebase window  # 38
    tree index commit merge token  # 39
    window commit blob branch cache buffer  # 40
    branch tree remote window queue queue delta queue commit merge worker  # 41
    index queue socket tree handle queue handle branch merge worker token  # 42
    tree socket ref cache worker  # 43
    queue rebase buffer window header queue  # 44
    remote handle tree header parser header merge  # 45
    branch parser cache merge  # 46
    worker rebase buffer merge token tree window merge handle remote object buffer  # 47
    worker buffer token blob index blob commit cache buffer object handle  # 48
    blob handle buffer ref delta worker object rebase  # 49
    object stream merge buffer  # 50
    worker ref socket delta branch merge window token header delta header  # 51
    merge branch ref commit  # 52
    merge tree blob window rebase rebase delta delta branch  # 53
    ref rebase handle blob header buffer worker queue object delta queue  # 54
    queue remote delta merge token delta index remote  # 55
    delta merge token object socket index stream window buffer  # 56
    worker pack parser branch socket commit window pack  # 57
    header rebase header index branch token handle handle  # 58
    cache worker object handle cache parser socket cache cache branch  # 59
    cache window remote buffer parser  # 60
    pack header branch handle commit merge handle commit remote  # 61
    token merge handle socket delta blob remote cache buffer remote rebase  # 62
    index worker tree object merge queue  # 63
    object handle commit header header  # 64
    remote ref buffer delta  # 65
    index handle cache blob cache buffer rebase socket  # 66
    handle tree header queue stream merge parser  # 67
    queue merge token header index rebase pack pack buffer  # 68
    object cache handle index parser buffer  # 69
    buffer index blob rebase  # 70
    blob delta cache parser blob  # 71
    buffer worker parser merge queue ref queue worker socket object remote rebase  # 72
    worker blob ref index header socket blob pack handle branch stream  # 73
    ref index branch buffer  # 74
    index tree tree window window blob header delta queue pack  # 75
    blob header header merge ref buffer header delta header rebase merge  # 76
    header branch handle handle stream ref delta handle pack  # 77
    object worker stream object index delta pack tree  # 78
