    buffer queue ref buffer commit worker delta ref ref blob window  # 0
    object branch worker tree window socket token pack window pack commit  # 1
    delta delta ref socket queue object parser header commit blob remote  # 2
    index branch stream parser cache ref delta object tree  # 3
    window merge blob token  # 4
    pack token pack remote index  # 5
    queue delta header queue token pack  # 6
    ref blob branch index window tree remote header  # 7
    queue object branch pack socket commit remote parser  # 8
    ref pack queue delta branch  # 9
    pack stream cache ref pack handle cache cache token commit queue  # 10
    handle merge pack remote  # 11
    window queue ref worker window pack  # 12
    rebase parser ref merge cache cache stream blob branch buffer blob window  # 13
    worker worker pack token object delta token blob token index token object  # 14
    delta cache ref blob tree header socket handle  # 15
    window handle index ref index blob  # 16
    ref merge token cache  # 17
    pack socket handle header  # 18
    branch parser ref object socket header commit socket  # 19
    worker ref rebase token  # 20
    ref ref merge tree merge parser window  # 21
    handle window ref window token pack cache delta queue commit  # 22
    window window parser token index  # 23
    merge stream header buffer blob delta commit queue cache socket pack  # 24
    index branch delta rebase buffer  # 25
    window stream header rebase stream token merge ref window window blob tree  # 26
    buffer merge delta socket token buffer pack index parser cache tree worker  # 27
    branch object stream merge handle worker  # 28
    token handle merge remote remote queue tree handle  # 29
    socket pack tree header ref object  # 30
    pack pack worker object handle blob  # 31
    pack merge merge merge pack  # 32
    buffer blob header token object remote index commit  # 33
    blob object queue rebase  # 34
    window buffer window worker queue pack  # 35
    worker worker handle commit remote delta object stream  # 36
    branch queue blob queue socket tree merge  # 37
    remote branch buffer delta rebase queue cache cache  # 38
    remote branch branch index ref worker  # 39
    branch parser handle remote commit  # 40
    handle object token handle  # 41
    pack rebase parser branch delta object object cache ref  # 42
    delta branch tree merge  # 43
    index index pack token queue  # 44
    header worker token index buffer ref pack window index branch  # 45
    index remote commit socket parser  # 46
    cache parser object header tree cache token parser commit parser cache  # 47
    commit delta worker commit merge queue commit buffer buffer stream index  # 48
    token stream worker buffer header queue queue  # 49
    tree rebase pack index rebase pack  # 50
    queue handle buffer tree pack buffer worker queue handle  # 51
    token queue queue remote object handle queue ref merge blob delta  # 52
    remote tree object cache  # 53
    blob commit parser delta token header branch  # 54
    object rebase cache remote socket stream remote remote branch token header tree  # 55
    parser blob handle blob worker socket buffer cache ref pack commit delta  # 56
    buffer pack token handle header  # 57
    ref handle ref token rebase ref tree  # 58
    object cache buffer tree  # 59
    delta ref remote parser commit pack commit merge parser  # 60
    tree blob remote worker header remote stream ref object remote tree object  # 61
    stream commit ref stream blob merge header buffer  # 62
    socket socket handle tree token window socket token pack buffer tree  # 63
    index cache socket ref stream object blob tree pack socket index  # 64
    socket remote blob token socket  # 65
    delta header delta window index parser  # 66
    pack tree delta commit tree remote cache stream cache  # 67
    token object stream cache blob commit socket tree buffer window tree  # 68
    cache header stream window ref remote stream stream  # 69
    branch commit pack branch remote stream buffer  # 70
    parser commit cache window handle remote index pack cache  # 71
    cache index ref ref  # 0
    header queue parser queue rebase socket cache  # 1
    worker branch blob index queue blob merge tree  # 2
    parser socket queue merge queue token merge worker merge stream object worker  # 3
    header socket ref cache commit rebase buffer ref remote header socket  # 4
    ref object token socket  # 5
    commit branch branch queue rebase blob stream blob stream socket parser socket  # 6
    window handle queue parser window  # 7
    header index handle branch branch branch window merge header  # 8
    stream object token object window index worker queue  # 9
    window merge stream token commit stream  # 10
    ref delta queue delta  # 11
