    handle cache buffer worker window  # 0
    index cache socket remote index buffer socket ref  # 1
    cache commit header stream  # 2
    window stream ref parser stream ref blob remote ref stream worker ref  # 3
    pack token handle merge  # 4
    index socket parser token commit  # 5
    handle branch object delta buffer object tree handle buffer object queue  # 6
    header cache pack queue remote object parser  # 7
    commit tree rebase handle header worker  # 8
    window branch worker delta ref buffer delta rebase parser object  # 9
    commit pack buffer delta  # 10
    buffer remote cache queue queue queue header cache queue  # 11
    socket object buffer object cache  # 12
    index delta tree queue ref blob cache ref pack  # 13
    stream merge tree delta worker buffer index header  # 14
    queue tree handle window blob remote parser tree index parser token delta  # 15
    branch window window worker  # 16
    commit merge ref window handle pack  # 17
    token ref window window worker rebase tree  # 18
    queue merge delta queue index branch ref token tree object commit window  # 19
    header parser cache commit commit blob  # 20
    index header index rebase  # 21
    queue merge parser pack window index buffer stream buffer queue  # 22
    stream cache rebase worker tree worker commit window  # 23
    index buffer handle object buffer index stream cache ref ref  # 24
    handle tree buffer window index  # 25
    remote window socket socket delta socket queue  # 26
    worker object rebase branch rebase worker remote token window buffer  # 27
    pack remote index buffer pack header remote delta commit header delta  # 28
    rebase header window commit  # 29
    socket parser pack merge socket commit  # 30
