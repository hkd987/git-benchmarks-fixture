    stream header worker buffer queue stream queue  # 0
    index rebase header tree blob index remote buffer blob buffer delta branch  # 1
    blob parser merge blob tree commit merge object  # 2
    tree socket queue rebase tree token blob remote  # 3
    merge object window tree handle buffer cache  # 4
    parser merge pack stream window token handle merge  # 5
    pack socket pack queue tree header buffer  # 6
    window worker socket merge merge ref  # 7
    pack merge cache tree token parser object window buffer ref pack tree  # 8
    object branch commit window handle merge queue branch tree blob merge remote  # 9
    pack token handle rebase blob tree header parser merge tree ref  # 10
    blob delta queue socket object index pack  # 11
    branch socket worker object parser commit commit object  # 12
    commit commit rebase delta queue rebase  # 13
    pack remote rebase pack rebase  # 14
    parser socket remote socket  # 15
    worker header pack socket branch token  # 16
    cache parser queue parser object header object window rebase remote socket  # 17
    buffer tree window merge  # 18
    cache parser tree pack index object index  # 19
    delta stream header pack queue queue token rebase header  # 20
    handle remote delta buffer header commit socket index buffer stream socket  # 21
    merge stream ref token blob delta  # 22
    socket commit index blob commit blob branch rebase  # 23
    commit handle stream tree remote stream blob ref handle stream ref remote  # 24
    tree token branch rebase handle branch cache rebase ref index  # 25
    worker remote socket ref branch blob blob object ref socket  # 26
    tree ref header token ref object merge stream rebase worker queue queue  # 27
    parser index worker pack worker delta worker header  # 28
    handle stream cache tree object buffer  # 29
    ref remote branch remote header ref  # 30
    socket handle object socket buffer  # 31
    index handle tree queue ref  # 32
    header tree commit delta  # 33
    tree worker merge token socket handle cache rebase commit stream  # 34
    pack stream ref parser blob  # 35
    queue commit buffer blob remote delta worker branch object queue merge  # 36
    cache token pack commit token ref tree ref  # 37
    object commit commit tree index  # 38
    ref worker index worker ref queue rebase header cache  # 39
    ref object cache socket blob cache remote window  # 40
    blob merge buffer rebase branch object tree queue delta commit  # 41
    tree merge stream stream stream remote socket parser tree  # 42
    ref merge commit stream pack window rebase queue ref stream  # 43
    cache delta token parser remote socket  # 44
    worker token ref queue socket header buffer  # 45
    stream blob socket header handle window index merge  # 46
    stream delta handle commit object parser object index tree merge token  # 47
    rebase blob parser socket branch cache tree worker merge delta token  # 48
    queue pack window commit rebase  # 49
    branch commit cache window socket  # 50
    socket token cache parser token tree tree token token parser  # 51
    token remote commit index branch buffer socket socket remote  # 52
    object ref buffer cache commit  # 53
    buffer rebase window handle pack rebase socket window merge parser  # 54
    blob remote ref token handle token  # 55
    handle index commit handle rebase tree  # 56
    queue queue socket object tree cache  # 57
    socket window socket header worker branch  # 58
    tree worker buffer cache token rebase queue  # 59
    header worker buffer queue index  # 60
    window commit object header object  # 61
    commit rebase remote token delta pack stream buffer token queue index  # 62
