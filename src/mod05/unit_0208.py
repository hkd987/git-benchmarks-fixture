    blob merge token queue remote tree parser object handle pack  # 0
    merge merge rebase parser  # 1
    tree handle ref commit commit  # 2
    buffer index ref commit branch stream blob ref  # 3
    index branch commit branch blob token merge  # 4
    token rebase cache cache index blob handle pack rebase rebase  # 5
    handle object merge parser  # 6
    commit stream window header delta tree queue  # 7
    ref socket branch merge delta token tree object merge cache  # 8
    ref stream queue cache  # 9
    rebase delta cache header buffer token socket socket rebase rebase stream tree  # 10
    object branch cache queue blob commit pack tree stream blob remote  # 11
    stream commit parser queue branch cache window ref rebase buffer tree  # 12
    stream branch commit cache window  # 13
    token object blob remote pack index queue window worker rebase  # 14
    remote queue commit ref window handle blob header cache branch object object  # 15
    commit stream queue socket tree index blob socket socket header rebase remote  # 16
    stream queue commit socket  # 17
    worker pack parser stream  # 18
    socket buffer ref parser rebase ref buffer commit worker object commit  # 19
    delta merge header queue blob merge merge worker worker handle object  # 20
    stream header token pack cache header index  # 21
    handle worker remote queue merge handle stream  # 22
    index worker blob rebase delta stream worker handle socket remote  # 23
    pack rebase remote delta  # 24
    commit merge socket queue handle parser ref remote index socket branch  # 25
    header object blob stream window header header token merge branch  # 26
    queue index delta header  # 27
    worker delta token ref  # 28
    remote buffer cache rebase token  # 29
    blob window ref parser stream delta worker  # 30
    merge token token token stream index ref  # 31
    window branch window index token branch tree  # 32
    handle header merge branch index pack tree object remote buffer window index  # 33
    handle parser cache remote socket merge commit pack  # 34
    delta merge merge socket  # 35
    rebase socket token rebase remote header worker buffer ref  # 36
    merge index remote object handle remote stream ref token remote parser rebase  # 37
    remote ref token commit index merge branch  # 38
    parser commit stream buffer  # 39
    commit handle stream handle  # 40
    buffer tree cache pack ref window delta index token remote object branch  # 41
    blob token ref blob  # 42
    pack delta tree worker tree delta window branch buffer socket window queue  # 43
    index pack header window  # 44
    branch token stream cache queue socket branch  # 45
    index pack pack stream pack delta branch queue  # 46
    buffer token index handle ref branch header window  # 47
    cache delta commit branch stream stream token tree remote header  # 48
    merge header tree handle socket stream header  # 49
    window token merge commit socket socket  # 50
    object header queue parser tree branch  # 51
    merge cache token branch token cache  # 52
    parser window index pack socket ref rebase queue branch  # 53
    handle delta tree buffer object cache remote ref  # 54
    socket object blob window merge  # 55
    rebase ref pack tree branch cache stream  # 56
    cache stream socket branch index buffer token socket queue parser remote  # 57
    blob index queue stream index worker delta queue  # 58
    pack rebase buffer merge token  # 59
    handle merge ref tree worker index delta commit ref ref token  # 60
    tree token rebase index worker parser socket blob branch tree commit  # 61
    queue ref index branch queue stream merge buffer blob cache tree handle  # 62
    queue remote branch commit object worker blob blob delta  # 63
    object header cache buffer header buffer  # 64
    delta worker ref ref worker rebase ref commit remote  # 65
    object rebase ref blob pack queue index tree blob tree branch  # 66
    socket blob token tree blob commit  # 67
    index delta buffer remote parser index blob  # 68
    worker handle merge merge queue remote remote rebase token branch  # 69
    index delta index handle blob socket rebase socket window  # 70
    handle cache object window merge commit worker delta remote delta commit rebase  # 71
    buffer object branch queue blob socket blob cache window buffer branch  # 72
    tree index cache window index window blob  # 73
    ref stream parser merge  # 74
    parser pack header pack stream tree rebase  # 75
    handle ref socket branch queue remote rebase blob pack header parser  # 76
    socket buffer merge parser rebase  # 77
    parser window blob commit merge queue ref buffer blob  # 78
