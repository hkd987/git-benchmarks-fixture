    stream pack remote pack merge queue parser queue handle queue socket  # 0
    token token handle handle handle  # 1
    token remote object tree object ref object tree buffer window  # 2
    window blob window branch delta rebase token  # 3
    cache handle branch stream pack index buffer rebase worker blob ref  # 4
    queue worker window object handle blob header parser pack remote  # 5
    ref stream header buffer index ref object window pack commit  # 6
    handle index tree ref handle stream parser queue commit  # 7
    queue pack commit index token parser  # 8
    header index tree window token merge pack token queue socket  # 9
    socket parser socket socket  # 10
    delta window ref remote ref index stream socket rebase remote  # 11
    queue merge rebase queue ref token handle handle handle branch  # 12
    tree header token worker index worker  # 13
    delta branch worker pack merge  # 14
    parser delta ref cache object tree token parser commit handle blob  # 15
    pack index merge worker object token delta blob socket buffer window ref  # 16
    delta ref index index window stream buffer queue parser tree  # 17
    token merge rebase index socket  # 18
    rebase stream index window merge branch blob header delta socket remote parser  # 19
    merge worker merge rebase commit blob parser branch remote commit remote  # 20
    worker parser index token delta delta  # 21
    blob remote delta branch window ref index delta object ref ref blob  # 22
    blob pack token worker  # 23
    handle blob parser token rebase token remote ref tree header parser  # 24
    buffer pack worker merge cache ref ref token rebase parser buffer stream  # 25
    worker index stream header handle queue header stream queue token merge  # 26
    commit buffer socket cache pack index merge merge pack  # 27
    merge token window merge ref cache  # 28
    tree index header blob  # 29
    object socket parser remote ref commit merge cache rebase socket window  # 30
    stream pack buffer commit commit remote index object rebase buffer blob ref  # 31
    delta worker stream merge queue buffer pack  # 32
    worker rebase branch handle window socket  # 33
    token rebase buffer delta blob cache index  # 34
    object worker pack ref merge index token remote worker  # 35
    handle tree object stream  # 36
    delta stream remote cache handle queue  # 37
    pack object object window buffer socket commit  # 38
    blob pack delta merge header socket header queue worker tree commit  # 39
    branch tree remote pack rebase branch rebase pack queue worker  # 40
    merge rebase pack index worker  # 41
    blob window header token merge  # 42
    pack stream object header  # 43
    window blob socket pack pack cache  # 44
    token delta index parser commit blob  # 45
    blob header delta stream window merge remote handle branch  # 46
    parser token socket pack cache delta queue tree handle commit  # 47
    queue header header socket  # 48
    ref header ref branch remote worker token buffer delta  # 49
    worker tree handle object ref commit handle cache commit  # 50
    delta socket object queue ref rebase index queue index remote window  # 51
    object cache parser worker object  # 52
    delta header tree object window delta socket delta worker  # 53
    parser commit window commit window header object  # 54
    branch delta index handle branch  # 55
    socket window ref socket  # 56
    cache branch remote handle merge merge queue buffer handle  # 57
    merge queue index remote handle buffer stream token commit  # 58
    tree window buffer index handle token socket stream blob cache object ref  # 59
    index ref index ref handle handle socket ref remote window  # 60
    remote stream parser header blob blob worker window  # 61
    rebase stream pack handle commit cache tree object socket header socket branch  # 0
    parser branch socket cache delta handle merge blob blob  # 1
    commit commit remote header branch branch  # 2
    merge header window parser stream blob socket  # 3
    window tree branch buffer pack rebase queue commit parser cache  # 4
