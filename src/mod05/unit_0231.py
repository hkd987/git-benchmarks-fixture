    branch tree blob tree cache  # 0
    handle window ref header buffer  # 1
    commit rebase index ref blob window delta delta parser  # 2
    handle rebase index cache queue worker branch pack  # 3
    index remote token ref  # 4
    tree ref worker commit remote blob window  # 5
    buffer cache stream remote blob rebase token object  # 6
    window window buffer parser merge ref token  # 7
    object header ref socket parser handle  # 8
    rebase cache buffer delta rebase branch commit  # 9
    parser index handle worker pack  # 10
    header pack blob commit handle handle  # 11
    cache stream ref merge worker handle  # 12
    index queue pack tree pack token  # 13
    socket parser blob window delta branch ref worker window  # 14
    worker index worker ref window merge blob pack merge index  # 15
    cache parser object queue handle  # 16
    branch blob object buffer socket remote rebase object  # 17
    index cache pack cache socket cache merge object rebase index worker merge  # 18
    queue remote queue window parser commit index stream stream tree stream ref  # 19
    pack ref parser socket worker rebase header  # 20
    index stream rebase worker object tree  # 21
    delta token buffer window object merge branch stream queue  # 22
    buffer pack pack buffer object blob  # 23
    cache header delta delta window queue commit queue index  # 24
    blob socket handle delta token  # 25
    token branch merge queue queue socket ref tree window tree  # 26
    delta tree branch header commit ref delta remote  # 27
    buffer cache pack handle commit queue parser  # 28
    worker merge cache worker worker merge delta rebase delta delta  # 29
    socket rebase merge object socket commit ref token handle parser  # 30
    buffer stream blob index delta parser merge window socket handle  # 31
    commit delta queue ref header window stream merge  # 32
    buffer delta tree header remote socket window header tree  # 33
    queue token worker ref parser branch buffer pack tree delta delta  # 34
    branch pack commit token  # 35
    queue index blob window parser token pack  # 36
    worker commit blob stream cache queue delta blob worker handle  # 37
    index index cache remote  # 38
    token window stream tree window rebase parser delta rebase cache rebase blob  # 39
    window rebase header blob parser cache stream parser tree header  # 40
    remote socket stream token delta remote merge parser blob  # 41
    ref parser worker rebase header queue rebase parser stream parser  # 42
    pack ref buffer tree rebase object merge ref buffer blob  # 43
    header socket commit delta worker  # 44
    cache remote pack window pack rebase ref window  # 45
    token object pack object  # 46
    pack worker object handle header commit token cache buffer delta  # 47
    tree tree branch branch parser index  # 48
    branch socket parser ref branch tree token cache window object handle handle  # 49
    parser remote rebase blob header socket delta stream blob  # 50
    blob merge token ref merge window delta cache queue  # 51
    index socket window parser remote  # 52
    tree worker stream handle token blob ref buffer object index rebase index  # 53
    window commit pack remote remote handle worker handle handle rebase delta  # 54
    ref socket index stream handle queue  # 55
    parser object token rebase queue commit  # 56
    handle parser merge tree  # 57
    queue stream blob delta delta commit worker  # 58
    worker merge rebase pack index handle commit rebase  # 59
    commit tree blob parser rebase rebase stream  # 60
    commit stream buffer object commit  # 61
    ref header socket worker window stream header token pack tree  # 62
    header ref object buffer remote object header  # 63
    stream buffer commit object merge buffer queue handle worker remote  # 64
    blob queue token ref cache stream commit remote  # 65
    stream index tree delta  # 66
    queue worker handle cache  # 0
    branch branch branch header object worker index  # 1
    worker ref remote ref  # 2
    pack window ref window ref header handle cache  # 3
    branch buffer parser buffer remote handle merge rebase queue stream rebase  # 4
    index remote worker index branch  # 5
    tree token branch worker object remote  # 6
    delta ref branch stream blob  # 0
    ref ref rebase buffer socket parser delta cache object remote pack index  # 1
    header rebase token queue delta window header buffer  # 2
    queue window worker handle stream merge tree worker  # 3
    token merge header worker handle  # 4
    parser parser cache worker worker rebase  # 5
    cache socket rebase token window delta cache commit window buffer rebase  # 6
