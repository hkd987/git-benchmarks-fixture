    merge object merge stream merge handle rebase worker merge object buffer  # 0
    rebase merge queue window commit socket object  # 1
    stream stream handle branch handle merge tree delta  # 2
    socket socket token window pack branch socket blob  # 3
    object remote index pack tree object merge buffer  # 4
    ref index blob delta ref cache remote window header  # 5
    tree blob branch rebase pack parser  # 6
    merge socket remote buffer pack socket remote branch stream header rebase pack  # 7
    header worker delta delta  # 8
    object buffer object tree  # 9
    branch worker worker merge worker parser header parser window  # 10
    branch handle tree remote pack buffer buffer worker cache ref  # 11
    rebase cache queue index header  # 12
    cache tree index object worker delta header index socket blob  # 13
    socket handle branch branch tree remote  # 14
    ref delta socket object token branch delta index header window  # 15
    object stream commit merge token socket  # 16
    index ref parser token token  # 17
    worker delta rebase window cache delta delta merge buffer tree  # 18
    remote buffer commit worker ref  # 19
    queue stream tree merge ref index ref blob ref window cache parser  # 20
    index worker delta queue stream  # 21
    header cache queue cache parser queue header branch index pack header cache  # 22
    cache delta stream branch queue  # 23
    remote cache worker merge socket parser queue delta remote delta  # 24
    remote object object token object merge ref handle buffer ref object  # 25
    remote object index merge  # 26
    remote commit token blob remote handle delta branch  # 27
    rebase delta delta remote ref  # 28
    parser commit header remote merge ref delta ref  # 29
    tree delta blob socket object worker  # 30
    queue blob stream header rebase worker  # 31
    worker cache cache buffer  # 32
    ref commit handle branch buffer merge object window index  # 33
    remote branch cache blob rebase branch tree object  # 34
    queue parser blob queue commit  # 35
    cache object commit delta header  # 36
    commit queue pack delta socket token parser socket remote socket tree  # 37
    buffer delta socket queue window branch remote ref worker handle worker worker  # 38
    stream branch window token worker rebase cache window socket rebase  # 39
    token remote buffer merge handle  # 40
    pack delta stream handle remote handle delta branch pack  # 41
    delta window pack merge cache handle header  # 42
    pack parser ref parser queue  # 43
    delta tree commit tree window handle queue parser pack ref index window  # 44
    queue commit worker handle object socket  # 45
    buffer merge object window  # 46
    ref branch handle worker token remote buffer worker header blob stream index  # 47
    worker buffer branch header  # 48
    commit commit branch token remote  # 49
    merge blob socket blob cache remote handle buffer merge index handle  # 50
    index worker merge tree rebase token token worker object index worker pack  # 51
    parser delta stream ref object pack  # 52
    handle commit ref queue header parser buffer queue header  # 53
    merge window remote index branch worker buffer socket socket blob  # 54
    commit socket branch branch remote stream buffer tree blob blob parser ref  # 55
    token ref stream window object commit  # 56
    parser cache blob remote queue ref window cache worker  # 57
    remote remote ref index handle stream cache index  # 58
    socket index commit socket  # 59
    branch index tree merge tree handle socket token  # 60
    index delta header socket buffer tree merge object  # 61
    handle object tree merge stream queue delta parser socket object tree object  # 62
    pack pack header cache object cache  # 63
    pack queue worker stream cache tree token remote merge stream  # 64
    stream queue handle commit buffer worker ref socket header window token cache  # 65
    ref delta queue cache parser worker  # 66
