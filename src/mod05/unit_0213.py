    socket parser blob queue token  # 0
    header branch header parser ref queue token ref pack socket  # 1
    handle token token rebase  # 2
    commit pack pack commit handle stream rebase header  # 3
    commit token socket stream header  # 4
    buffer delta window parser header token cache remote merge tree parser queue  # 5
    rebase branch parser ref window handle branch  # 6
    cache socket parser blob pack window worker  # 7
    worker buffer object handle header token pack branch token rebase  # 8
    pack buffer object header rebase commit tree stream object  # 9
    window window parser delta queue  # 10
    buffer branch stream branch rebase header tree token parser stream object header  # 11
    rebase handle parser tree remote tree queue header branch index merge  # 12
    pack branch header window tree remote blob index blob object parser  # 13
    pack socket handle cache handle rebase index token queue  # 14
    remote rebase token blob buffer socket token commit ref parser merge delta  # 15
    worker blob ref buffer  # 16
    parser ref header remote commit ref window handle object queue buffer ref  # 17
    tree header socket header  # 18
    window blob socket pack  # 19
    merge rebase worker handle ref rebase header queue queue token handle delta  # 20
    tree tree commit window queue handle tree buffer rebase socket buffer  # 21
    object commit branch delta  # 22
    remote delta window delta token queue blob index  # 23
    remote stream stream commit ref tree tree ref object delta  # 24
    parser queue buffer commit ref blob object object socket stream  # 25
    token blob delta delta token handle parser branch  # 26
    ref ref parser socket queue  # 27
    buffer header worker pack merge  # 28
    token stream branch window index remote branch  # 29
    delta delta buffer commit index  # 30
    blob branch rebase commit parser branch window queue  # 31
    pack header rebase branch rebase tree index blob  # 32
    object parser merge blob cache blob merge queue parser  # 33
    remote buffer header handle header branch index cache header window  # 34
    delta blob rebase socket token socket object token handle parser remote pack  # 35
    tree socket stream socket commit index  # 36
    buffer worker branch index worker  # 37
    worker worker delta queue object stream  # 38
    parser remote tree buffer  # 39
    merge rebase delta rebase blob window socket branch tree object  # 40
    rebase buffer queue window  # 41
    window worker branch buffer  # 42
    parser header header queue index queue parser blob pack  # 43
    tree merge index rebase handle remote rebase window pack window worker  # 44
    delta stream merge buffer buffer commit queue ref object header buffer  # 45
    branch rebase delta handle merge index tree header rebase rebase  # 46
    index worker rebase queue delta  # 47
    branch index handle queue remote window object buffer handle cache  # 48
    header queue queue handle  # 49
    branch branch ref ref stream worker blob window  # 50
    remote blob parser buffer token merge worker  # 51
    buffer rebase merge tree delta object window commit rebase header  # 52
    blob rebase commit parser delta handle  # 53
    merge socket stream branch tree tree stream buffer delta  # 54
    remote blob branch commit object worker handle index merge  # 55
    pack ref ref token socket  # 56
    worker handle tree index cache  # 57
    stream cache header index token delta object  # 58
    socket tree window rebase pack remote object window handle  # 59
    cache header merge index ref rebase parser delta commit  # 60
    cache buffer token ref parser window remote cache  # 61
    commit remote commit branch queue delta handle  # 62
    window token ref ref pack object window window buffer  # 63
    rebase buffer header handle branch index token  # 64
    buffer merge merge header buffer queue worker  # 65
    token blob window delta object handle buffer  # 66
    queue index blob tree delta header token  # 67
    remote socket buffer pack index delta  # 68
    index ref socket ref commit merge  # 69
    header buffer cache buffer commit tree stream  # 70
    window delta header remote header tree rebase token parser tree pack handle  # 71
    remote merge socket handle object remote cache  # 72
    handle handle pack buffer stream socket remote handle stream queue  # 73
    index header buffer delta  # 74
    merge remote index window merge window  # 75
    handle socket branch handle worker handle ref header  # 76
    queue remote branch handle token ref cache tree  # 77
    cache pack merge worker remote blob rebase  # 78
    remote index queue window  # 79
    window object object token tree blob commit remote commit branch tree  # 80
    window delta token token worker rebase commit rebase blob remote header  # 81
    blob handle token parser pack window buffer object window rebase buffer commit  # 82
    stream cache branch blob remote socket stream window  # 83
