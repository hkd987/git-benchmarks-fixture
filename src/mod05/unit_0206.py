    tree socket queue ref merge  # 0
    merge window socket token  # 1
    index tree handle window stream rebase delta ref  # 2
    queue queue pack parser token delta  # 3
    index handle cache ref object index  # 4
    token socket token rebase commit handle blob cache  # 5
    token buffer cache queue remote stream window buffer handle blob ref stream  # 6
    handle parser remote tree ref blob window delta  # 7
    merge rebase pack commit index cache token queue merge header blob tree  # 8
    branch pack merge worker commit delta tree parser parser commit header  # 9
    remote header token pack  # 10
    remote merge remote window worker commit delta socket merge cache  # 11
    tree blob header remote  # 12
    ref window parser tree index cache  # 13
    window window commit token  # 14
    queue window delta pack tree token delta queue token stream cache  # 15
    cache remote queue cache pack cache  # 16
    commit stream tree tree rebase handle parser token delta pack worker  # 17
    object window rebase socket ref parser delta pack  # 18
    remote object pack merge delta rebase queue blob pack  # 19
    rebase object index pack delta stream buffer commit  # 20
    pack pack queue merge header pack worker remote  # 21
    rebase header window handle merge stream stream ref  # 22
    window ref queue token pack worker commit blob handle cache socket  # 23
    window cache pack index  # 24
    token socket tree cache branch delta buffer index remote token tree  # 25
    header object handle index tree  # 26
    pack object ref header tree tree ref delta parser commit queue  # 27
    cache blob index worker cache window  # 28
    ref window rebase cache index window parser pack parser  # 29
    index parser index parser rebase window parser buffer  # 30
    pack pack ref blob blob  # 31
    buffer token rebase object branch buffer queue tree worker  # 32
    buffer stream queue index rebase branch queue rebase object buffer queue  # 33
    branch handle blob blob ref rebase buffer tree window handle stream buffer  # 34
    stream header rebase header  # 35
    pack delta stream worker token commit rebase merge commit  # 36
    ref object remote merge  # 37
    delta branch buffer queue pack  # 38
    window queue delta queue blob queue pack handle merge object object rebase  # 39
    worker delta parser commit worker socket commit branch parser  # 40
    cache remote blob handle object rebase  # 41
    index stream parser queue cache handle remote  # 42
    header commit pack pack blob ref  # 43
    delta handle cache ref branch  # 44
    worker delta queue merge tree  # 45
    merge ref rebase worker header rebase header cache ref worker buffer  # 46
    index blob tree branch queue cache branch stream index stream branch  # 47
    token remote window rebase index header remote commit handle remote  # 48
    ref queue branch ref worker blob tree delta index  # 49
    remote queue tree merge index blob remote token object  # 50
    ref commit socket object window parser blob commit commit  # 51
    worker commit socket delta branch cache branch  # 52
    ref delta window handle commit worker tree merge  # 53
    object buffer window branch pack tree remote handle  # 54
    rebase cache stream merge blob cache cache buffer branch tree  # 55
    merge object stream token  # 56
    rebase pack pack commit object remote  # 57
    parser branch buffer pack queue cache token delta header ref parser  # 58
    socket cache ref tree index rebase tree object queue  # 59
    delta remote index merge blob  # 60
