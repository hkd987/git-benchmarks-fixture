    token index buffer queue  # 0
    remote ref cache ref commit pack token  # 1
    merge object delta rebase  # 2
    socket commit branch worker window stream blob handle header  # 3
    stream merge tree buffer window  # 4
    window header parser ref ref tree  # 5
    branch index queue index token tree commit  # 6
    socket buffer tree delta branch remote merge token ref worker object worker  # 7
    blob queue stream socket blob commit token tree remote stream  # 8
    buffer window parser delta delta index parser remote worker token ref  # 9
    worker worker buffer token parser index worker queue  # 10
    worker blob cache branch handle handle buffer  # 11
    worker handle commit ref queue commit ref header branch header  # 12
    stream delta branch pack worker pack object window blob queue rebase object  # 13
    worker handle token parser stream branch parser delta  # 14
    merge rebase remote object socket  # 15
    buffer stream queue queue header socket commit socket  # 16
    index rebase commit parser handle merge pack rebase stream ref  # 17
    delta token index rebase ref tree worker blob ref worker  # 18
    token socket pack socket commit remote token buffer  # 19
    branch merge blob delta ref header worker handle blob ref delta  # 20
    stream handle cache stream remote merge merge merge window parser delta blob  # 21
    socket branch ref object handle blob blob commit cache remote rebase  # 22
    queue delta queue merge cache remote worker merge socket window parser  # 23
    token handle parser remote commit worker worker pack buffer object handle  # 24
    ref blob handle socket  # 25
    buffer token handle socket header rebase window buffer socket ref header stream  # 26
    index buffer worker parser delta merge handle  # 27
    buffer stream window header pack merge cache parser commit  # 28
    remote worker rebase remote header handle window  # 29
    branch parser tree rebase  # 30
    blob remote object queue cache window commit tree pack branch  # 31
    object delta merge queue commit cache pack socket queue token  # 32
    tree blob worker header token delta stream delta  # 33
    rebase tree parser parser object remote token socket  # 34
    merge commit object handle token rebase header header  # 35
    blob socket buffer handle window worker  # 36
    blob window branch commit parser socket stream worker queue  # 37
    socket window commit token delta  # 38
    commit branch token rebase parser commit handle delta commit rebase merge  # 39
    blob merge ref queue queue handle token worker queue delta parser merge  # 40
    worker buffer rebase header window socket socket  # 41
    rebase ref worker window socket token parser handle pack cache  # 42
    commit merge header header header rebase  # 43
    window window merge pack pack index worker branch rebase ref merge queue  # 44
    object commit stream handle index  # 45
    cache delta blob remote queue header queue blob rebase ref object  # 46
    object index token handle pack tree ref branch window pack  # 47
    buffer parser token parser merge stream remote worker object remote  # 48
