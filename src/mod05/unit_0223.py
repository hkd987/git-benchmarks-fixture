    rebase buffer socket ref rebase queue merge header stream queue  # 0
    stream cache worker commit rebase handle pack parser index  # 1
    stream socket stream index window delta tree socket rebase index pack branch  # 2
    delta cache token queue stream cache  # 3
    token cache token delta commit buffer merge buffer commit pack tree worker  # 4
    parser header socket object  # 5
    header commit worker window merge  # 6
    handle rebase remote delta remote queue  # 7
    worker commit tree branch index window rebase  # 8
    pack commit merge window merge tree handle worker token  # 9
    merge ref stream delta remote ref delta blob ref token  # 10
    buffer branch object merge pack object  # 11
    parser socket pack branch token buffer queue cache worker header  # 12
    ref delta queue object branch index socket rebase object delta branch  # 13
    tree tree buffer merge merge queue cache remote branch header blob  # 14
    socket object header tree branch window buffer ref rebase  # 15
    handle cache worker branch commit worker queue parser stream stream branch blob  # 16
    header index socket blob branch ref header worker  # 17
    rebase pack remote handle object header delta  # 18
    commit worker index blob header cache queue object blob  # 19
    branch worker branch socket tree cache ref queue  # 20
    index socket buffer rebase  # 21
    cache queue pack token rebase  # 22
    token parser parser commit worker tree cache  # 23
    buffer parser tree worker merge stream  # 24
    remote window blob commit window cache handle header ref rebase header stream  # 25
    rebase commit window tree  # 26
    header stream merge tree merge pack merge tree header handle index commit  # 27
    handle token commit blob header  # 28
    worker queue branch token buffer socket commit rebase  # 29
    window object ref remote rebase header header queue header commit commit commit  # 30
    remote socket commit cache commit tree stream token parser queue queue  # 31
    index ref delta stream window cache queue parser tree parser  # 32
    queue merge handle rebase delta window header  # 33
    parser rebase worker delta  # 34
    buffer object blob window ref cache worker pack token  # 35
    ref tree stream handle object token  # 36
    parser token queue ref tree  # 37
    merge index remote pack delta socket index stream cache delta remote pack  # 38
    remote handle socket branch blob  # 39
    stream queue parser tree  # 40
    remote handle buffer remote tree ref tree remote buffer delta header window  # 41
    socket delta cache index header cache socket  # 42
    rebase branch buffer index handle queue tree rebase handle merge  # 43
    stream cache ref cache object index  # 44
    remote pack handle socket  # 45
    delta socket stream object tree header ref  # 46
    header remote object worker remote  # 47
    delta commit branch stream delta window branch blob  # 48
    commit blob parser queue blob branch  # 49
    remote cache index blob token delta remote parser socket delta object token  # 50
    worker window window commit pack pack handle  # 51
    worker delta cache token buffer  # 52
    object stream parser parser handle socket merge ref  # 53
    window remote tree blob parser blob handle  # 54
    queue blob buffer header parser worker rebase ref  # 55
    object delta worker delta token window socket  # 56
    commit queue object header  # 57
    socket ref remote remote  # 58
    remote index socket header stream stream pack header ref tree object ref  # 59
    remote object blob parser queue branch object pack pack pack  # 0
    parser handle blob remote queue window tree  # 1
    cache window buffer delta  # 2
