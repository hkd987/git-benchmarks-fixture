    branch rebase parser window merge  # 0
    delta pack ref pack parser header handle handle handle  # 1
    stream ref branch tree ref  # 2
    object pack pack cache handle  # 3
    token queue header worker window  # 4
    pack buffer window handle remote ref rebase index rebase  # 5
    branch pack stream ref stream parser cache delta  # 6
    queue handle object branch  # 7
    socket stream merge remote remote merge token ref stream pack  # 8
    token merge token remote header branch ref branch  # 9
    handle branch cache ref  # 10
    socket blob object handle header index object rebase parser blob window  # 11
    cache pack stream window pack header rebase buffer header header pack  # 12
    socket ref pack worker socket buffer index  # 13
    object ref delta window commit commit  # 14
    merge pack window merge commit handle object blob blob commit header  # 15
    cache header rebase buffer buffer object  # 16
    object handle window queue commit stream pack pack ref remote  # 17
    token parser delta header parser delta  # 18
    cache cache cache buffer queue  # 19
    queue cache blob socket buffer remote  # 20
    object stream buffer parser tree ref merge  # 21
    merge delta queue merge token socket  # 22
    remote handle commit branch index index delta  # 23
    branch token remote rebase stream queue merge  # 24
    header parser tree delta queue object header parser  # 25
    handle branch queue stream remote remote cache merge rebase ref merge  # 26
    socket tree merge merge merge header buffer blob header queue socket  # 27
    worker ref blob buffer queue parser index token merge pack cache  # 28
    header merge parser branch remote worker window rebase pack branch tree  # 29
    parser window merge branch pack header handle  # 30
    blob merge stream merge ref token  # 31
    window commit socket token merge rebase index socket rebase commit index  # 32
    blob delta rebase stream parser  # 33
    cache worker pack rebase  # 34
    blob buffer rebase object ref rebase delta worker ref ref  # 35
    token ref blob window stream branch  # 36
    rebase merge cache token buffer  # 37
    header commit token remote buffer tree  # 38
    buffer queue header handle worker tree socket  # 39
    remote object worker branch rebase stream rebase  # 40
    cache handle pack remote window  # 41
    merge handle blob rebase  # 42
    tree cache index delta ref index parser  # 43
    worker ref remote branch queue  # 44
    parser handle parser parser stream handle buffer delta parser header  # 45
    index pack handle cache handle  # 46
    ref queue commit object header parser  # 47
    blob cache cache pack queue commit socket  # 48
    remote object handle worker ref branch cache queue  # 49
    branch worker remote branch object object buffer tree  # 50
    queue worker delta token delta queue worker  # 51
    stream commit object index parser commit tree index  # 52
    token blob merge index commit commit cache pack header pack handle stream  # 53
    commit token stream token parser ref  # 54
    buffer stream pack worker parser blob buffer delta token  # 55
    header cache branch handle handle header  # 56
    tree merge commit delta stream blob cache branch worker tree  # 57
    tree parser index branch token window  # 58
    ref socket queue cache socket remote pack branch tree buffer  # 59
    stream ref handle parser stream commit token  # 60
    merge worker pack rebase  # 61
    merge commit branch blob header window stream index  # 62
    tree commit queue stream object token token  # 63
    merge token object socket worker blob buffer remote pack  # 64
    merge buffer pack index queue parser buffer ref parser remote  # 65
