    parser buffer ref tree pack rebase worker pack  # 0
    header tree branch delta pack commit socket index parser parser worker tree  # 1
    socket rebase ref queue merge blob buffer ref cache merge  # 2
    header parser index window queue  # 3
    header merge merge handle queue index blob merge handle ref branch  # 4
    window delta index cache remote buffer window worker blob object blob index  # 5
    commit worker buffer worker index  # 6
    index blob stream worker worker buffer token rebase worker queue cache  # 7
    blob handle socket queue queue blob token cache  # 8
    socket header worker pack parser  # 9
    stream stream queue worker cache commit index parser  # 10
    cache blob buffer object stream header window window handle window  # 11
    token cache worker ref rebase stream remote rebase handle socket buffer commit  # 12
    window blob merge merge ref handle  # 13
    window header blob rebase  # 14
    parser pack ref rebase worker buffer worker index index parser handle  # 15
    blob ref stream rebase commit buffer pack rebase  # 16
    worker object pack parser delta  # 17
    tree queue header tree  # 18
    tree object stream socket token delta window  # 19
    index parser blob index pack tree pack window header ref buffer buffer  # 20
    queue socket worker branch merge buffer branch header commit stream  # 21
    buffer index remote socket pack socket stream pack delta rebase buffer  # 22
    ref delta index buffer rebase  # 23
    stream remote socket ref blob token window token stream worker window  # 24
    index delta branch index  # 25
    index worker object merge stream queue token window  # 26
    delta stream index ref blob header stream commit ref branch token  # 27
    parser parser stream pack parser  # 28
    queue remote pack commit window buffer buffer object header merge index  # 29
    token commit socket worker blob commit cache object  # 30
    cache remote parser index worker pack rebase  # 31
    worker branch window blob merge pack buffer object handle  # 32
    merge branch merge ref buffer index worker token  # 33
    tree window worker remote header blob window object stream queue  # 34
    remote stream pack parser token header window  # 35
    header cache socket tree window parser stream delta parser stream object  # 36
    merge pack delta index rebase handle queue cache header merge header merge  # 37
    remote tree pack socket  # 38
    ref parser rebase commit merge handle tree  # 39
    parser delta queue pack parser socket blob object buffer parser  # 40
    pack window window pack object object buffer  # 41
    socket rebase queue token remote merge parser index buffer object  # 42
    commit window rebase handle  # 43
    worker queue index rebase  # 44
    window branch window token index pack  # 45
    index remote pack tree index  # 46
    parser index window token window  # 47
    header handle queue socket parser queue token remote branch  # 48
    object queue commit queue  # 49
    delta ref blob header object stream  # 50
    object commit tree blob delta rebase handle merge  # 51
    object token parser delta header delta index worker  # 52
    token token blob buffer window index cache  # 53
    remote token object header merge handle stream branch merge handle handle socket  # 54
