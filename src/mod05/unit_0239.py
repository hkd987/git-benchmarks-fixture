    rebase delta commit pack blob commit parser worker stream  # 0
    object handle handle tree rebase worker ref ref rebase branch token remote  # 1
    ref blob index object stream tree remote token delta header  # 2
    buffer window branch object parser  # 3
    index rebase ref handle cache  # 4
    rebase token tree ref  # 5
    handle blob index merge object header worker stream token branch  # 6
    delta queue merge merge object blob  # 7
    queue tree token remote merge rebase  # 8
    ref queue window rebase buffer buffer commit cache merge index  # 9
    window index buffer delta stream header header buffer rebase  # 10
    stream object window worker handle token blob worker  # 11
    socket ref cache commit tree stream stream rebase rebase token parser merge  # 12
    ref branch merge object worker delta socket token branch  # 13
    parser ref queue merge rebase token parser index  # 14
    queue tree tree remote socket rebase socket cache handle  # 15
    remote parser ref branch commit rebase worker buffer stream rebase index  # 16
    parser index blob pack delta cache  # 17
    header cache queue cache stream object tree token handle rebase  # 18
    buffer window cache handle commit parser object object window socket window worker  # 19
    merge cache token ref object tree delta ref  # 20
    rebase delta merge parser header  # 21
    branch remote cache stream remote tree ref index  # 22
    header header buffer pack parser commit commit merge header parser  # 23
    commit commit handle window queue queue ref parser delta worker parser header  # 24
    delta buffer cache cache index rebase queue branch commit header  # 25
    worker pack object socket worker parser buffer merge  # 26
    stream token remote branch  # 27
    branch index stream rebase branch tree handle commit cache rebase  # 28
    buffer token window rebase  # 29
    delta tree merge worker blob header  # 30
    token handle branch delta  # 31
    token ref socket buffer object header window rebase handle tree window blob  # 32
    header index cache merge header  # 33
    stream cache queue object header tree  # 34
    tree blob remote queue parser  # 35
    ref merge delta rebase buffer  # 36
    worker header remote index socket window parser parser token commit commit  # 37
    parser parser cache window buffer  # 38
    buffer index window remote remote worker branch tree parser handle  # 39
    header window pack pack worker commit worker worker branch window  # 40
    merge remote header commit cache index ref window tree pack buffer delta  # 41
    commit header header window buffer object header stream delta worker window  # 42
