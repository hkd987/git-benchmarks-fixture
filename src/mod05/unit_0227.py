    worker stream cache parser rebase cache  # 0
    handle window token merge header buffer  # 1
    tree remote object header handle buffer branch header  # 2
    ref stream cache pack object object stream header  # 3
    blob worker pack token window  # 4
    handle ref window ref remote  # 5
    stream queue token rebase cache worker cache socket socket branch  # 6
    ref buffer parser rebase rebase ref delta delta handle worker ref tree  # 7
    object handle handle object tree queue ref branch branch object  # 8
    pack parser stream header commit  # 9
    merge queue tree worker parser stream branch  # 10
    window handle branch queue window commit buffer queue rebase cache  # 11
    ref buffer tree rebase worker object buffer window  # 12
    object index window rebase  # 13
    parser merge queue token  # 14
    window commit ref blob window index tree delta cache  # 15
    commit header delta branch window object branch  # 16
    rebase parser stream merge header handle  # 17
    queue stream merge window rebase socket stream merge  # 18
    tree buffer object header blob delta tree index worker  # 19
    commit rebase parser stream tree rebase buffer handle window queue queue  # 20
    socket stream pack cache remote delta  # 21
    object delta pack ref merge rebase handle socket  # 22
    header stream parser merge blob object ref queue pack commit index remote  # 23
    header buffer commit token ref worker  # 24
    cache buffer object parser merge ref  # 25
    buffer rebase tree header  # 26
    ref ref merge parser cache remote token branch handle handle index rebase  # 27
    header queue object object tree index pack  # 28
    merge branch index ref delta buffer ref buffer pack handle header cache  # 29
    window buffer worker remote object handle commit merge header window header handle  # 30
    header delta queue ref merge object  # 31
    window header index remote  # 32
    tree buffer queue commit queue worker parser parser header object tree ref  # 33
    queue parser object index stream header window commit cache pack rebase  # 34
    window branch index commit queue branch token  # 35
    object commit buffer stream ref index token commit queue  # 36
    cache delta blob branch socket blob rebase buffer ref window handle index  # 37
    parser queue branch handle  # 38
    rebase cache queue pack cache  # 39
    delta delta parser object blob  # 40
    index worker socket delta tree object index index handle socket  # 41
    window delta delta merge object rebase parser  # 42
    stream parser token queue rebase window merge cache cache  # 43
    socket header object socket remote delta rebase worker socket index socket queue  # 44
    header parser blob branch stream merge delta blob cache delta rebase  # 45
    handle queue object window branch header merge parser  # 46
    ref handle ref stream rebase delta index delta ref  # 47
    remote cache token object index pack queue  # 48
    branch handle remote pack window parser blob stream pack buffer branch delta  # 49
    remote worker socket window branch cache commit index header buffer merge stream  # 50
    queue tree ref remote token handle pack index  # 51
    token window pack handle window commit  # 52
    stream index commit object stream  # 53
    window ref header window pack queue blob socket commit commit worker ref  # 54
    socket worker window branch socket commit delta object  # 55
    queue cache handle pack branch  # 56
    index commit cache token rebase  # 57
    handle token window worker index cache  # 58
    remote parser window commit queue header commit token blob index socket branch  # 59
    object token socket token index window delta merge object worker  # 60
    tree index merge ref object pack header  # 61
    socket branch handle tree buffer object  # 62
    blob cache stream buffer worker stream window remote  # 63
    token worker header tree commit  # 64
    parser window header header worker branch token blob token rebase queue  # 65
    object index merge remote queue commit parser rebase index delta blob  # 66
    queue token buffer token worker index  # 67
    rebase token tree parser merge delta buffer header  # 68
    buffer cache buffer delta blob index window buffer tree  # 69
    object cache header blob buffer  # 70
    stream socket header window token ref cache remote commit buffer  # 71
    token header ref commit stream blob window object socket delta  # 72
    blob header window queue merge buffer remote delta delta delta  # 73
    merge cache index rebase merge index index branch pack token  # 74
