    queue index tree merge tree handle pack worker socket  # 0
    merge rebase index delta  # 1
    remote commit worker delta blob worker index blob rebase  # 2
    delta blob pack token queue token commit blob token branch object  # 3
    cache remote tree header parser parser rebase ref index handle ref  # 4
    commit window remote handle  # 5
    handle blob window header cache remote tree ref handle handle buffer blob  # 6
    socket delta stream buffer header rebase handle  # 7
    delta window index buffer pack rebase  # 8
    commit pack handle object token  # 9
    handle token buffer stream worker stream commit handle window rebase stream ref  # 10
    merge worker commit pack pack window commit  # 11
    handle remote parser tree tree buffer index window remote delta stream  # 12
    parser parser buffer token rebase blob  # 13
    worker cache worker object merge blob window pack tree delta  # 14
    remote parser cache buffer ref delta socket handle rebase token  # 15
    header ref token object rebase buffer blob pack branch parser  # 16
    handle index object stream branch delta parser socket cache socket blob  # 17
    branch index index tree merge header rebase blob worker blob  # 18
    worker stream delta parser object handle delta parser index socket worker  # 19
    branch blob cache window  # 20
    window branch stream worker merge merge  # 21
    ref socket index index cache remote commit index delta blob  # 22
    commit pack blob header delta tree worker socket rebase worker  # 23
    rebase tree pack rebase object pack remote token cache  # 24
    handle merge blob rebase delta worker token buffer window queue queue socket  # 25
    index header rebase window stream blob commit stream branch socket  # 26
    merge socket pack delta  # 27
    worker pack ref pack header handle rebase tree  # 28
    stream remote cache delta remote rebase index queue object tree pack  # 29
    delta rebase commit ref handle header rebase  # 30
    blob queue merge tree stream blob handle header buffer merge header commit  # 31
    queue window branch ref header queue index branch rebase  # 32
    delta cache window remote queue cache header header pack index socket stream  # 33
    buffer pack ref delta tree queue ref  # 34
    handle token ref window delta rebase blob delta pack delta index branch  # 35
    pack remote object object merge branch  # 36
    parser header commit merge header  # 37
    index pack stream window window object pack  # 38
    header ref cache handle token stream window rebase cache pack index delta  # 39
    object tree delta pack commit ref tree  # 40
    merge stream index merge handle queue index merge  # 41
    buffer cache handle ref socket queue stream buffer  # 42
    token delta window stream blob delta  # 43
    object remote worker pack buffer remote queue tree  # 44
    stream cache pack ref object handle buffer socket merge  # 45
    commit object handle window stream header remote branch socket worker  # 46
    rebase handle object token blob worker queue index token handle  # 47
    stream branch rebase commit  # 48
    branch pack socket object cache blob branch pack  # 49
    stream object worker blob  # 50
    socket delta handle queue socket  # 51
    rebase pack window parser tree window stream merge buffer ref delta  # 52
    ref stream buffer cache rebase handle  # 53
    worker pack rebase stream merge commit blob branch merge  # 54
    cache commit delta delta remote object  # 55
    commit index commit socket pack stream pack queue  # 56
    remote blob cache rebase remote token handle header header merge  # 57
    merge header delta branch commit cache parser worker blob  # 58
    cache worker worker socket  # 59
    header ref window delta token token rebase cache queue  # 60
    queue socket object token socket worker  # 61
    stream socket ref worker merge index blob commit ref queue queue  # 62
    parser ref pack index  # 63
    object index rebase pack branch commit ref commit delta parser blob  # 64
    header commit rebase window ref token queue tree queue  # 65
    ref delta delta socket window cache worker token cache ref  # 66
    index socket object branch ref commit handle tree blob worker  # 67
    header rebase header merge commit index parser merge  # 68
