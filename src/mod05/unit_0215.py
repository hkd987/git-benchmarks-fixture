    token ref window rebase rebase socket stream  # 0
    socket blob merge worker blob commit  # 1
    blob ref handle handle parser delta remote queue  # 2
    rebase ref delta header remote tree parser socket delta parser object cache  # 3
    pack merge header object pack blob index branch commit delta object rebase  # 4
    token object pack stream tree buffer handle worker  # 5
    pack token ref remote remote buffer handle header  # 6
    handle pack branch index window header object pack buffer buffer  # 7
    worker window socket tree merge remote header cache remote queue delta parser  # 8
    header merge header queue handle header handle cache  # 9
    commit tree token parser  # 10
    header tree token object buffer index queue parser  # 11
    buffer cache delta window stream  # 12
    delta worker window cache token delta  # 13
    remote ref worker token  # 14
    header remote pack index worker parser index  # 15
    commit socket blob socket commit object  # 16
    tree commit socket socket blob socket parser ref worker buffer ref stream  # 17
    object rebase header socket parser cache  # 18
    pack tree rebase pack merge  # 19
    stream tree buffer index token handle socket socket delta header remote  # 20
    handle window header queue blob ref remote  # 21
    commit header worker delta socket handle worker rebase queue remote commit  # 22
    remote delta token merge  # 23
    socket window delta buffer token queue buffer token  # 24
    stream parser pack branch pack token pack header  # 25
    pack header rebase rebase token buffer pack socket stream  # 26
    commit commit delta cache handle index socket object  # 27
    handle window object commit parser socket index  # 28
    remote window blob object token buffer header stream stream merge ref blob  # 29
    socket ref object window queue branch window pack stream branch  # 30
    queue token stream stream remote handle handle rebase worker buffer blob rebase  # 31
    blob handle token handle  # 32
    queue stream worker window commit remote merge cache cache  # 33
    token tree stream object header rebase merge blob rebase  # 34
    header parser remote handle index window parser buffer rebase cache  # 35
    queue tree ref delta  # 36
    ref ref tree object object pack object merge stream worker delta header  # 37
    buffer header rebase token ref buffer index handle pack pack object  # 38
    pack remote index handle  # 39
    window commit blob cache  # 40
    commit delta commit object stream handle window commit queue socket worker  # 41
    socket object blob buffer parser index index parser  # 42
    window branch object stream blob ref worker header socket branch  # 43
    header branch branch window pack blob worker  # 44
    delta window cache window pack  # 45
    buffer socket index buffer branch stream buffer commit index delta merge  # 46
    remote merge rebase remote ref branch object header tree  # 47
    rebase delta ref object queue rebase rebase commit worker pack handle cache  # 48
    remote token queue window stream header delta tree  # 49
    queue tree cache worker stream token worker delta merge delta  # 50
    object window index commit ref handle parser cache merge index ref  # 51
    ref parser commit commit pack worker handle index rebase pack cache  # 52
    commit branch socket worker delta window ref merge socket remote  # 53
    token cache stream pack object commit buffer ref  # 54
    stream window tree merge remote pack worker delta  # 55
    blob object ref delta rebase window ref delta token rebase delta  # 56
    pack merge object blob handle  # 57
    handle object pack token  # 58
    blob tree queue queue ref commit  # 59
    ref ref pack merge blob buffer worker  # 60
    tree handle index merge rebase object  # 61
    queue handle tree parser socket delta ref window rebase remote  # 62
    tree blob parser object blob  # 63
    remote window merge socket cache parser worker buffer token token  # 64
    branch commit cache socket window delta merge pack branch handle ref  # 65
    queue socket header blob merge  # 66
    handle tree window branch parser ref queue  # 67
    delta rebase branch queue token delta queue header blob  # 68
    worker parser pack index tree window header header branch ref rebase delta  # 69
    blob delta stream pack tree pack  # 70
    window socket socket token pack  # 71
