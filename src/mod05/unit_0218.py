    rebase blob parser merge blob parser ref delta  # 0
    branch worker header stream header ref object delta cache remote  # 1
    cache header buffer socket  # 2
    commit tree merge tree buffer blob delta socket socket merge ref  # 3
    index stream cache delta delta object rebase worker queue pack ref  # 4
    window cache ref stream stream window pack socket blob  # 5
    pack commit stream buffer window  # 6
    buffer buffer window socket  # 7
    tree buffer handle rebase buffer  # 8
    merge rebase merge blob blob merge parser  # 9
    buffer socket stream window queue header delta  # 10
    worker queue handle blob ref ref object parser handle token  # 11
    pack delta object object window cache commit rebase  # 12
    queue commit remote merge token queue remote queue queue cache worker  # 13
    index delta commit parser buffer rebase parser index remote pack header  # 14
    parser token pack branch queue worker header tree  # 15
    handle queue commit parser tree  # 16
    tree remote tree stream remote pack tree window  # 17
    commit tree object token header  # 18
    handle object stream handle blob ref header delta buffer delta object remote  # 19
    socket stream stream stream buffer socket object  # 20
    object rebase buffer socket cache index header  # 21
    queue queue index window  # 22
    tree blob socket queue stream buffer  # 23
    buffer delta rebase pack tree branch ref commit  # 24
    stream commit ref ref  # 25
    ref worker token cache handle  # 26
    delta token socket merge object pack remote header worker token handle header  # 27
    ref window index delta header socket remote commit remote parser header pack  # 28
    queue ref merge parser pack stream window token tree handle  # 29
    merge rebase pack delta object handle stream socket branch tree worker  # 30
    handle pack merge stream  # 31
    cache merge delta blob tree parser token worker queue  # 32
    header pack object delta ref queue queue pack  # 33
    commit merge rebase ref remote handle branch pack object header socket parser  # 34
    parser pack remote merge branch stream rebase rebase tree cache blob  # 35
    tree socket queue commit queue ref handle object object  # 36
    commit object handle delta delta remote handle merge worker stream parser commit  # 37
    index tree tree index ref worker object header  # 38
    cache commit cache merge token merge commit header parser header socket object  # 39
    header handle branch blob socket rebase merge  # 40
    token index window branch  # 41
    token parser remote blob header cache delta merge  # 42
    buffer buffer handle blob blob rebase buffer delta pack remote commit remote  # 43
    header blob queue tree ref handle window index branch queue remote socket  # 44
    rebase socket window handle buffer socket rebase branch commit  # 45
    delta queue commit remote queue tree window commit header  # 46
    window socket merge worker window cache  # 47
    tree token pack parser token  # 48
    rebase pack ref index remote socket socket socket blob header  # 49
    index blob queue ref token  # 50
