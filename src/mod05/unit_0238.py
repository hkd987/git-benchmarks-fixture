    index worker merge object worker  # 0
    branch cache token tree object pack queue branch remote remote cache  # 1
    header rebase commit tree cache merge delta tree tree blob buffer pack  # 2
    ref delta cache stream object socket index parser queue window  # 3
    branch window ref blob  # 4
    token merge buffer parser queue index  # 5
    delta socket merge buffer parser object branch worker delta cache delta commit  # 6
    stream commit object buffer stream blob parser worker stream worker header  # 7
    commit rebase rebase tree branch window  # 8
    object rebase handle pack queue blob branch merge stream  # 9
    handle tree token buffer  # 10
    window pack handle ref stream socket index  # 11
    buffer branch object window queue cache blob  # 12
    blob header header window commit queue tree  # 13
    cache header worker handle socket rebase worker remote window remote  # 14
    cache stream merge header remote stream branch window branch  # 15
    handle object ref rebase merge remote remote window buffer ref rebase token  # 16
    remote worker index object pack token branch object ref  # 17
    index ref remote token object branch blob buffer handle  # 18
    parser window commit token handle worker tree object  # 19
    merge queue window remote queue  # 20
    parser stream object object delta socket  # 21
    remote ref cache object remote window socket pack handle worker  # 22
    handle window delta parser object tree cache branch blob handle header index  # 23
    worker rebase queue remote worker stream buffer pack ref  # 24
    token commit header commit buffer remote parser pack handle rebase pack index  # 25
    token handle parser stream token merge object  # 26
    cache commit index worker worker  # 27
    merge delta socket remote parser tree window window window stream  # 28
    index index handle remote  # 29
    blob delta ref blob handle rebase commit delta window merge delta merge  # 30
    object queue buffer ref remote handle handle buffer buffer pack blob  # 31
    socket remote window cache remote header  # 32
    cache token handle delta worker queue  # 33
    stream rebase socket tree worker commit  # 34
    token ref worker queue queue  # 35
    tree handle merge header stream parser rebase commit index token socket  # 36
    rebase branch object stream  # 37
    parser queue merge handle pack pack delta cache commit branch  # 38
    queue handle pack remote stream  # 39
    delta index parser index remote rebase  # 40
    token parser stream blob remote window parser merge  # 41
    handle delta queue parser socket delta tree worker parser pack queue rebase  # 42
    rebase header parser object ref pack socket queue branch handle branch  # 43
    rebase stream remote worker index queue queue delta header  # 44
    worker blob delta header stream remote tree socket  # 45
    merge stream worker token worker cache commit  # 46
    buffer buffer ref rebase cache object worker index branch token commit rebase  # 47
    token rebase object tree worker branch  # 48
    commit tree object window  # 49
    parser queue buffer blob commit ref index  # 50
    tree stream merge pack tree queue blob blob parser  # 51
    parser remote token stream handle commit handle  # 52
    worker token delta tree header  # 53
    merge delta window index header worker parser worker worker rebase header  # 54
    merge parser merge header remote delta index header merge ref  # 55
    object token index merge pack rebase header stream pack remote blob ref  # 56
    window buffer delta branch  # 57
