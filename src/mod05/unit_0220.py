    stream blob queue buffer  # 0
    blob blob index stream blob  # 1
    window handle pack branch delta blob window  # 2
    ref merge rebase rebase object blob commit ref branch  # 3
    parser worker pack rebase token worker blob pack token token stream  # 4
    object socket blob header header blob blob socket  # 5
    remote worker socket commit rebase window socket  # 6
    socket merge handle object header object tree tree  # 7
    remote queue window merge blob handle index header parser object buffer queue  # 8
    buffer remote index queue handle blob blob tree branch worker  # 9
    branch pack queue remote header buffer object commit socket worker object tree  # 10
    rebase index header stream window queue  # 11
    handle header socket handle remote commit remote parser remote branch  # 12
    merge pack queue delta remote stream rebase merge  # 13
    cache blob index worker merge  # 14
    queue branch rebase pack tree header index cache token branch window delta  # 15
    socket branch rebase branch index blob index  # 16
    index handle rebase object pack rebase window  # 17
    cache stream branch index index remote blob merge pack stream  # 18
    remote rebase pack parser merge object queue handle commit branch branch rebase  # 19
    window tree parser socket commit queue handle  # 20
    rebase index merge rebase delta handle cache socket socket buffer index  # 21
    window index pack ref token buffer socket parser worker  # 22
    socket ref object cache rebase buffer header index parser handle handle ref  # 23
    blob queue tree header commit rebase rebase  # 24
    header worker merge stream rebase window index socket index merge ref delta  # 25
    cache remote stream pack blob rebase merge worker  # 26
    remote blob pack ref blob header commit blob handle token parser header  # 27
    object object window cache cache  # 28
    delta stream commit commit remote delta branch remote  # 29
    stream header object ref window socket socket header parser pack branch pack  # 30
    ref cache index header rebase tree queue handle branch stream parser token  # 31
    object buffer cache stream parser object buffer  # 32
    token token commit blob socket branch parser stream stream tree ref cache  # 33
    commit delta cache rebase blob tree  # 34
    buffer remote header buffer stream tree branch rebase  # 35
    branch remote buffer window cache tree handle ref window buffer worker  # 36
    merge worker socket worker  # 37
    worker object blob merge  # 38
    pack queue pack ref delta token blob tree  # 39
    window buffer socket buffer parser branch stream queue cache blob parser window  # 40
    handle merge rebase queue stream header  # 41
    buffer commit window header stream  # 42
    index index queue worker cache ref index buffer tree delta delta merge  # 43
    token ref branch pack ref remote merge  # 44
    header object buffer queue  # 45
    ref blob header commit ref worker window rebase tree  # 46
    window index header delta pack index  # 47
    blob pack worker blob branch commit branch  # 48
    parser pack delta object rebase window rebase buffer  # 49
    rebase index header header commit blob remote parser handle remote commit  # 50
    cache token branch handle socket remote  # 51
    merge window pack socket tree rebase token worker  # 52
    index queue socket tree pack index stream blob  # 53
    pack blob commit cache window handle remote pack  # 54
    socket handle socket cache queue  # 55
    rebase commit remote ref socket queue queue pack commit buffer  # 56
    remote ref window stream commit blob merge blob cache  # 57
    queue tree blob queue merge tree remote rebase remote worker worker  # 58
    parser worker buffer delta window merge remote queue  # 59
    cache buffer worker token buffer pack parser buffer  # 60
    handle tree header header header tree token socket pack  # 61
    branch ref blob parser blob pack rebase handle ref  # 62
    tree delta pack parser remote pack  # 63
    delta commit object merge rebase parser header  # 64
    branch stream header commit socket delta queue merge  # 65
    handle object merge blob queue buffer  # 66
    commit branch index tree stream handle  # 67
    remote rebase blob header tree merge  # 68
    ref socket ref socket handle header header rebase rebase branch header object  # 69
    object blob index index  # 70
    ref token token remote blob ref object blob token  # 71
    delta parser tree token  # 72
    object stream window pack remote delta pack delta cache  # 73
    token buffer index remote tree token window delta blob  # 74
    header index window object buffer parser delta worker ref blob  # 75
    parser stream handle buffer ref object ref buffer queue  # 76
    remote token socket parser queue rebase  # 77
    branch remote index pack parser merge merge object  # 78
    stream queue cache token blob  # 79
    remote commit buffer queue token delta handle parser tree object header  # 80
    parser delta branch socket pack ref blob queue rebase tree handle  # 81
    handle socket buffer rebase delta ref tree stream  # 82
