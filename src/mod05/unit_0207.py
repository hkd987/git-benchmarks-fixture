    buffer queue tree parser ref branch object remote queue parser stream cache  # 0
    window tree socket window merge  # 1
    blob window blob pack rebase index branch pack  # 2
    branch stream merge queue  # 3
    tree handle stream blob queue rebase queue ref stream pack remote token  # 4
    queue window delta rebase  # 5
    remote object window cache pack tree commit merge token merge rebase socket  # 6
    cache token blob window stream pack parser token window commit merge  # 7
    remote commit window rebase index cache stream  # 8
    token blob merge header ref  # 9
    ref pack index object object remote pack  # 10
    remote socket tree blob socket  # 11
    queue index index tree commit window worker parser commit tree  # 12
    cache commit buffer ref token rebase  # 13
    merge token commit object handle parser window merge  # 14
    commit handle commit worker handle index remote remote branch  # 15
    ref remote commit token buffer blob blob stream header blob  # 16
    tree header blob object stream header object pack branch  # 17
    worker socket token index buffer merge queue handle  # 18
    remote merge merge window delta socket stream index queue object  # 19
    stream token ref remote handle index index index  # 20
    commit object index merge parser index  # 21
    index merge token delta parser queue commit handle window commit commit  # 22
    pack ref delta buffer handle object  # 23
    index commit branch pack stream rebase window  # 24
    header stream blob delta ref branch merge  # 25
    object pack merge header window blob  # 26
    delta cache index rebase tree ref  # 27
    remote branch queue parser queue remote cache buffer  # 28
    tree delta object queue blob stream worker ref socket ref  # 29
    blob blob blob ref tree tree commit remote ref delta merge  # 30
    branch branch window worker buffer object parser index socket  # 31
    handle cache stream buffer blob parser  # 32
    ref blob buffer ref cache branch index token window token stream header  # 33
    queue commit cache parser handle token stream window commit index  # 34
    rebase stream socket window branch parser token merge commit buffer pack  # 35
    tree stream remote queue index object object cache branch rebase object commit  # 36
    cache branch socket pack rebase worker cache cache header header handle  # 37
    object window cache blob queue merge merge blob  # 38
    object commit worker index index worker blob commit merge blob object remote  # 39
    commit blob tree object remote  # 40
    handle index merge header remote window  # 41
    branch buffer buffer cache object  # 42
    parser commit worker tree  # 43
    index object header parser  # 44
    header rebase ref object object parser stream delta  # 45
    cache object branch header  # 46
    blob cache blob worker buffer blob socket queue  # 47
