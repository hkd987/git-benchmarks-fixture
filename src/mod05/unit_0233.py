    merge handle branch parser stream token merge queue merge  # 0
    remote rebase stream worker buffer delta pack window branch  # 1
    socket pack index header delta worker ref merge header stream ref ref  # 2
    parser handle index window blob window delta header cache commit  # 3
    ref queue delta delta  # 4
    header commit window tree ref rebase merge rebase remote  # 5
    delta socket header tree tree socket buffer worker remote ref socket  # 6
    token pack object token delta header handle socket index index worker  # 7
    stream ref tree delta index tree index stream queue worker window  # 8
    object remote rebase socket tree socket header blob rebase stream window  # 9
    delta tree cache stream delta rebase commit  # 10
    tree rebase delta merge merge socket buffer index  # 11
    commit parser branch token window delta stream merge ref  # 12
    index header commit remote remote parser  # 13
    branch pack socket remote token merge pack delta remote stream  # 14
    tree branch branch parser blob stream parser queue  # 15
    cache stream rebase branch buffer stream  # 16
    token handle header stream delta handle handle queue window rebase handle socket  # 17
    buffer commit window delta handle blob rebase handle branch blob  # 18
    buffer object rebase parser socket pack branch handle token commit worker  # 19
    socket buffer worker queue blob delta object rebase branch  # 20
    socket buffer window rebase header worker branch token header buffer window pack  # 21
    commit worker branch header delta header  # 22
    queue pack commit blob branch blob  # 23
    token blob rebase cache commit socket stream parser cache  # 24
    socket ref buffer buffer token  # 25
    window branch index delta parser branch delta branch branch  # 26
    rebase object buffer index token  # 27
    ref pack worker header object parser header parser index ref tree handle  # 28
    remote ref rebase buffer ref parser token blob index rebase object blob  # 29
    worker stream cache queue remote remote socket  # 30
    index pack window object remote ref handle socket  # 31
    commit object blob branch cache branch tree cache  # 32
    cache stream tree delta socket pack object  # 33
    worker queue buffer ref window delta delta ref token cache  # 34
    remote buffer rebase branch blob delta cache branch socket remote  # 35
    tree commit socket delta blob  # 36
    worker commit header token ref  # 37
    delta tree ref buffer branch remote rebase worker blob  # 38
    buffer blob queue handle header remote buffer index delta parser delta  # 39
    ref rebase object delta tree  # 40
    handle blob window remote branch buffer socket remote remote commit commit ref  # 41
    merge queue commit delta ref branch header token merge header blob header  # 42
    buffer commit delta worker cache  # 43
    worker pack cache delta commit branch worker pack worker rebase socket index  # 44
    buffer rebase rebase blob worker socket stream socket tree ref  # 45
    handle index object cache header socket token  # 46
    queue queue tree delta window header rebase  # 47
    tree tree worker ref ref ref handle window rebase queue  # 48
