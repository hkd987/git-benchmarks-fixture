    header window merge handle commit stream index index object object  # 0
    rebase pack rebase window tree pack delta worker index  # 1
    branch commit stream parser delta  # 2
    object delta index object merge  # 3
    pack parser window header window header delta header parser stream  # 4
    worker parser branch cache socket merge commit delta  # 5
    window blob handle window parser object pack merge delta stream tree  # 6
    buffer window queue remote socket token token ref worker rebase tree remote  # 7
    queue buffer ref pack  # 8
    branch pack token delta merge window  # 9
    worker socket delta index  # 10
    token ref stream tree socket parser stream  # 11
    worker rebase merge buffer ref token ref cache stream remote delta rebase  # 12
    remote blob object header branch ref merge delta branch rebase queue commit  # 13
    remote ref rebase index worker tree merge rebase pack commit remote  # 14
    remote header index token buffer branch pack object socket branch  # 15
    parser cache object parser tree blob branch queue cache queue  # 16
    queue ref object blob blob handle token tree branch remote queue  # 17
    parser index queue tree blob branch token stream rebase merge worker  # 18
    rebase parser branch index  # 19
    socket remote rebase handle  # 20
    delta tree blob header socket commit branch socket pack pack commit handle  # 21
    window merge index commit token branch index tree worker object queue handle  # 22
    token window worker cache cache pack ref delta queue token worker  # 23
    object commit remote merge pack merge stream remote delta  # 24
    delta commit tree handle branch remote  # 25
    merge rebase remote buffer delta delta  # 26
    blob blob buffer remote delta  # 27
    window queue stream cache header  # 28
    queue handle pack tree object worker index parser merge  # 29
    tree object tree worker ref delta socket socket merge blob header commit  # 30
    header header merge index handle commit pack remote blob delta token merge  # 31
    ref queue buffer tree socket object blob remote  # 32
    index ref pack handle branch worker merge parser rebase index  # 33
    rebase blob commit object socket object cache merge cache worker pack  # 34
    commit token worker merge handle socket tree stream socket commit  # 35
    delta socket blob token worker ref parser stream  # 36
    merge blob ref rebase token ref  # 37
    delta queue commit commit delta  # 38
    commit index commit commit parser worker pack header buffer pack  # 39
    rebase pack rebase remote tree delta  # 40
    token token window delta pack queue remote  # 41
    worker rebase queue index blob stream worker rebase worker queue merge branch  # 42
    tree index window parser ref blob merge object worker handle ref  # 0
    remote token branch parser commit merge token header rebase handle delta  # 1
    remote socket merge stream window tree  # 2
    worker commit delta tree header ref header  # 3
    pack buffer buffer rebase commit queue window object ref blob socket  # 4
    pack pack blob pack stream index stream token  # 5
    queue stream token blob  # 6
    header handle worker index commit tree token queue handle worker  # 7
    remote queue token rebase delta buffer  # 8
