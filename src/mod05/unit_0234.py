    worker commit commit buffer blob queue object pack ref merge merge  # 0
    token commit delta handle commit rebase delta worker stream handle  # 1
    remote delta pack object socket  # 2
    rebase rebase delta rebase  # 3
    delta queue tree stream tree delta window ref buffer object  # 4
    buffer blob pack delta tree handle  # 5
    ref pack parser queue ref window branch ref buffer token header index  # 6
    stream socket window tree  # 7
    index token pack token ref merge buffer ref object header  # 8
    token parser handle buffer merge tree commit pack branch index buffer  # 9
    object pack ref header pack handle cache token remote header merge  # 10
    handle object tree parser buffer merge queue handle cache header merge parser  # 11
    commit buffer pack queue stream remote parser pack pack remote  # 12
    stream blob delta queue queue header  # 13
    rebase tree merge buffer header queue stream ref index ref  # 14
    window header header rebase window  # 15
    rebase header header buffer token worker cache blob commit  # 16
    delta branch blob window token buffer blob tree object window cache stream  # 17
    queue tree object cache  # 18
    socket buffer tree object rebase queue parser merge parser token window  # 19
    cache blob socket ref window cache index socket  # 20
    merge worker commit tree object object merge queue queue  # 21
    window delta window header buffer ref branch pack handle cache  # 22
    index handle object blob worker  # 23
    window handle stream commit rebase cache worker delta  # 24
    socket parser delta index merge tree merge remote queue rebase  # 25
    socket handle ref blob tree queue queue merge cache stream  # 26
    rebase rebase tree ref stream object stream queue  # 27
    commit window tree stream object  # 28
    parser merge cache ref handle rebase  # 29
    socket worker stream header stream  # 30
    index worker object blob cache handle  # 31
    parser delta remote remote queue ref queue pack pack cache object  # 32
    handle delta header stream branch window stream buffer buffer index remote merge  # 33
    index commit branch ref window window blob commit merge  # 34
    tree object handle object window delta buffer header stream buffer object blob  # 35
    queue delta branch handle pack merge merge token  # 36
    cache token ref merge tree cache stream commit remote buffer header commit  # 37
    window rebase rebase remote merge index rebase buffer parser stream queue  # 38
    merge socket stream ref cache window pack index  # 39
    token cache delta cache branch merge window window remote ref handle  # 40
    object object tree token tree rebase object merge worker buffer merge  # 41
    worker worker object object cache tree commit remote window index index  # 42
    parser rebase delta object pack parser pack merge buffer pack  # 43
    buffer token header rebase remote header commit stream  # 44
    header object queue branch ref handle rebase remote tree  # 45
    parser index token socket queue index delta merge handle  # 46
    ref header worker merge tree index tree delta  # 47
    object blob window index delta  # 48
    socket cache cache pack blob blob tree buffer tree ref  # 49
    handle handle merge branch  # 50
    tree tree parser rebase window commit tree commit header header merge worker  # 51
    worker branch delta cache handle  # 52
    window header stream blob commit cache cache object delta buffer  # 53
    merge header commit worker ref  # 54
    header handle ref queue  # 55
    branch remote branch blob token rebase window token queue pack  # 56
    branch remote window handle queue rebase  # 57
    delta commit branch branch buffer handle  # 58
    delta tree remote tree tree worker socket window worker cache ref delta  # 59
    buffer token blob pack  # 60
    queue queue pack rebase delta blob parser window ref socket  # 61
    token branch ref commit merge handle commit  # 62
    delta header header blob pack token stream  # 63
    commit queue header token delta blob header token parser blob worker remote  # 64
    pack queue stream header parser worker socket  # 65
    socket merge buffer pack stream socket buffer delta tree ref cache index  # 66
    window object index token merge delta token queue  # 67
    commit commit blob index  # 68
    socket tree blob blob branch header queue commit parser blob  # 69
    socket socket tree index merge token delta window  # 70
    pack cache object token ref index object parser object object index  # 71
    commit token object stream stream pack  # 72
    window index token stream queue parser  # 73
    buffer merge remote rebase branch  # 74
    remote queue parser token  # 75
    rebase pack delta blob header header queue header commit handle ref delta  # 76
    stream rebase delta parser window remote  # 77
    ref handle remote window tree parser queue  # 78
    cache merge socket object object stream stream  # 79
    header parser socket queue window  # 80
    header token parser socket parser cache index delta remote rebase delta queue  # 81
    queue window window buffer branch branch pack object pack stream blob  # 82
    token handle delta delta branch tree socket index tree  # 83
    header ref worker commit  # 84
    queue queue object object rebase delta header pack  # 85
    branch index rebase parser token index window header object header  # 86
    object handle socket parser handle tree token blob  # 87
    commit stream queue header pack pack index buffer  # 88
