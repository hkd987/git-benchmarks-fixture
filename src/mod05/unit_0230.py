    pack queue token buffer pack window token buffer delta socket  # 0
    branch remote worker cache blob stream parser delta queue socket parser  # 1
    rebase index buffer stream  # 2
    branch branch tree commit branch commit delta rebase remote header  # 3
    pack merge parser delta delta ref token blob delta cache  # 4
    handle rebase stream remote rebase worker  # 5
    commit socket header object pack parser blob delta remote header  # 6
    handle blob ref blob index handle worker header tree pack cache branch  # 7
    merge object remote socket rebase branch remote delta  # 8
    buffer index parser tree buffer handle parser  # 9
    merge buffer token pack header  # 10
    cache commit cache cache  # 11
    blob token handle worker merge blob ref  # 12
    token commit rebase merge buffer parser  # 13
    blob parser cache tree handle remote object handle merge  # 14
    queue buffer socket queue rebase delta stream cache parser parser object commit  # 15
    branch pack ref queue index tree remote  # 16
    rebase ref token window cache cache branch  # 17
    ref cache blob header pack commit queue index merge remote delta  # 18
    rebase branch index stream index stream stream ref socket blob parser cache  # 19
    commit header worker pack branch branch socket cache token stream blob remote  # 20
    cache buffer queue stream buffer remote branch tree index commit remote  # 21
    remote pack worker delta delta branch handle  # 22
    stream blob socket rebase cache queue  # 23
    cache tree header ref commit stream branch rebase remote window worker  # 24
    window index window parser window object index  # 25
    blob object window pack window handle tree  # 26
    commit stream branch branch header commit queue pack  # 27
    pack commit delta socket branch socket window merge  # 28
    stream tree cache branch header rebase  # 29
    tree index handle tree  # 30
    socket queue tree stream  # 31
    parser rebase index index merge handle blob  # 32
    handle cache queue tree blob token object tree token stream  # 33
    parser cache parser queue buffer handle branch merge worker  # 34
    tree cache queue branch branch buffer worker  # 35
    branch index queue index delta delta delta tree  # 36
    pack buffer parser object queue index window tree  # 37
    worker blob handle worker parser index worker  # 38
    handle window rebase parser  # 39
    token tree pack header merge remote  # 40
    header object window header stream  # 41
    window token index remote parser queue queue socket parser cache queue socket  # 42
    cache buffer ref window token remote  # 43
    delta branch index header delta parser header merge tree  # 44
